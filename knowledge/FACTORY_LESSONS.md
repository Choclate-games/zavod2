# Уроки фабрики: на чём игры спотыкаются на самом деле

Документ собирается автоматически из отчётов приёмки всех игр (`.factory/gate.json`). Это не теория и не советы вообще — это список того, что уже ломалось в выпущенных проектах, с указанием, у скольких игр именно эта проверка была красной.

Обновлено: 23.08.2026. Игр с приёмкой: 2, из них зелёных: 0.

## Средние числа по выпущенным играм

- кадров в секунду: **27.0**
- вызовов отрисовки: **2184.0**
- вес сборки: **2.74 МБ**
- задержка первого кадра: **1012.0 мс**

Игра, которая заметно хуже этих чисел, сделана хуже среднего по фабрике, а не «примерно как все».

## Чаще всего красное

| Проверка | Что это | Игр | Прогонов |
|---|---|---|---|
| `B1` | Нет src/ui/theme.css — единственного места со значениями токенов | 1 | 1 |
| `B6` | Нигде нет pointer-events: none — оверлей съест игровой ввод | 1 | 1 |
| `C1` | game_ready упоминается слишком часто: он обязан отправляться ровно один раз | 1 | 1 |
| `C12` | Нет public/playgama-bridge-config.json — bridge.initialize() поймает 404 и площадка не ответит | 1 | 1 |
| `F1` | Нет DESIGN.md — дизайн игры не описан (секция 7 мастер-промпта) | 1 | 1 |
| `G2` | События шины подключены с одной стороны | 1 | 1 |
| `G6` | JS пишет CSS-переменную, которую не читает ни одно правило | 1 | 1 |
| `G7` | Ни одного @media во всём CSS: интерфейс свёрстан под один размер экрана | 1 | 1 |
| `S2` | Игра открывается без ошибок | 1 | 1 |
| `C13` | Мост площадки только изображён: свой сервис есть, а @playgama/bridge не объявлен ни в package.json, ни скриптом в index.html — на площадке не будет ни авторизации, ни облачного сохранения, ни рекламы | 1 | 1 |

## Что с этим делать

## Чек-лист

- [ ] Консоль игры пуста при запуске: ни одного исключения в первые секунды
- [ ] `npm run build` проходит начисто — до того, как работа считается сделанной
- [ ] Игровой цикл не встаёт после ввода — кадры идут и через минуту игры
- [ ] В кадре что-то есть: сцена, свет и камера сведены, отрисовка не нулевая
- [ ] Игра переживает клавиши, мышь и палец, а не только показ первого кадра
- [ ] На экране 390×844 ничего не вылезает за ширину и не требует горизонтальной прокрутки
- [ ] Интерфейс виден: корни экранов вставлены в документ, а не только созданы
- [ ] Каждый импорт объявлен: `.css` и ассеты не роняют строгую сборку
- [ ] Раздел 0 приёмки закрыт: игра про то, что заказал пользователь
