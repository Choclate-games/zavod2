pydantic>=2.10.0
pydantic-settings>=2.7.0
pyyaml>=6.0.2
jinja2>=3.1.5
typer>=0.15.0
rich>=13.9.0
pillow>=11.0.0
requests>=2.32.0
python-dotenv>=1.0.1
# Веб-интерфейс фабрики (python run_web.py / start.bat).
fastapi>=0.115.0
uvicorn>=0.30.0
# Внутренний браузер предпросмотра. Без него фабрика откроет игру в окне
# Edge/Chrome (--app), а если и его нет — в системном браузере.
pywebview>=5.3
# Реестр проектов, пользователи и журнал архивов в MySQL (app/db.py). Чистый
# Python — не требует ни компилятора, ни libmysqlclient в образе.
pymysql>=1.1.1
# Панель состояния машины (app/sysinfo.py): нагрузка процессора и топ
# процессов. Память, диск и температура читаются из /proc и /sys напрямую.
psutil>=6.0.0
# Вебсокеты uvicorn: терминалы CLI-агентов в браузере (app/web/terminals.py).
# Без этого пакета uvicorn поднимется, но апгрейд соединения отклонит.
websockets>=13.0
