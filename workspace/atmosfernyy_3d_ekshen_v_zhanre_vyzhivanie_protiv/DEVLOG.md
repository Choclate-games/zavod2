# Журнал разработки — Маяк: Ночная Вахта

Этот файл ведёт ИИ-агент. Каждая сессия работы = одна запись:
что просили, что сделано, какие файлы затронуты, что проверено, что осталось.

---

## 2026-08-23 17:14 — Инициализация проекта
- **Задача**: создан каталог проекта в workspace, подготовлена спецификация.
- **Сделано**: сгенерирован пакет документации фабрики.
- **Следующий шаг**: реализация игрового кода по AI_DEVELOPER_PROMPT.md.

---

## 2026-08-23 18:40 — Полная реализация игры: от каркаса до зелёного smoke
- **Задача**: по AI_DEVELOPER_PROMPT.md и skills/ создать полную рабочую структуру HTML5-игры — рендер, симуляция, управление, аудио, Playgama Bridge, UI; довести check-spec и smoke до зелёных.
- **Сделано**:
  - Шаг 0 (`fetch-knowledge`): НЕ ВЫПОЛНЕН технически — скрипт вернул HTTP 403 для всех 49 файлов (токен не задан, репозиторий недоступен). `docs/ref/` пуст. Работал офлайн по локальным выжимкам `skills/` (GAME/GAMEPLAY/RENDERER/PLAYGAMA/CONTROLS), ACCEPTANCE.md и полным текстам спецификаций в корне.
  - Готовый код фабрики (LIBRARY.md / knowledge-showcase): НЕ ВЗЯТ НИ ОДИН ФАЙЛ — причина та же: `node scripts/fetch-knowledge.mjs <путь>` на каждый файл из таблицы секции 3c возвращал 403. AudioManager (процедурный Web Audio) и вся логика орды написаны с нуля с опорой на чек-листы из ACCEPTANCE H.
  - DESIGN.md написан до кода: палитра дизелпанка, камера 50°, пять экранов со сценой за меню, таблица глаголов управления, осознанный отказ от вертикали.
  - Каркас: Vite + TypeScript strict (`noEmit`), `src/vite-env.d.ts`, фиксированный цикл 60 Гц с накопителем и клампом дельты, PlaygamaService подключён сразу, `game_ready` отправляется ровно один раз после интерактивного меню + сторожевой таймер 15 с.
  - Числа: `balance.yaml` парсится при старте (`src/config/Balance.ts`, импорт `?raw`); резервные значения продублированы в коде дословно — правка баланса не требует правки формул.
  - Механики: термодинамика линзы (28 °C/с нагрев, 35 °C/с охлаждение, локаут 3 с, lerp конуса 0.18 с, 60°↔10°), световой стаггер 65% с адаптацией за 4 с и шоком 0.25 с, цепной катализ био-мин (0.8 с прогрева → 250 ед. в радиусе 4.5 м, каскад с задержкой 0.12 с), паровой сброс (кольцо 6 м, сброс до 20 °C, заряд за 15 цепных убийств), процедурный спавн (интервал 2.4→0.6 с, сектора 1→4), левиафан в фазе IV, победа/поражение по таймеру и HP, счёт по формуле спецификации.
  - Рендер: одна Three.js-сцена (остров, маяк, шейдерный океан с пеной, небо-купол с рассветом, дождь), луч — аддитивный конус + SpotLight, орда — 8 InstancedMesh (тела+глаза), частицы — два пула (искры аддитивно/дым), день-ночь режиссура без пересозданий. Тени отключены (дубль прохода отрисовки + краш MeshDepthMaterial на программном WebGL).
  - Физика: Rapier3D НЕ ВЗЯТ сознательно — WASM съедал бы заметную долю бюджета 4.5 МБ ради круговых коллизий на плоскости; вместо него SpatialGrid + аркадное разделение роя и радиальные импульсы пара (отказы размечены `[~]` в ACCEPTANCE H). bitECS заменён SoA-массивами на 220 сущностей.
  - Управление: InputRouter с двумя схемами (выбор по `bridge.device.type`, форс `?input=`/`?touch=`), переключение на лету со сбросом зажатий; тач-слой только в тач-схеме, Pointer Events + setPointerCapture, мультитач фокус+драг.
  - UI: токены только в theme.css (палитра, --ui-scale с @media, --z-*, --vp-h читается из CSS), пять экранов поверх живой сцены, HUD пишет только в кэшированные узлы, полосы через scaleX, tabular-nums, SVG-спрайт вместо эмодзи, i18n ru/en.
  - Аудио: процедурный синтез (гул дуги, ветер, детонации, пар, фанфары), ленивый AudioContext, один мастер-гейн, раздельные мьюты игрока и площадки, suspend/resume по вкладке.
  - Площадка: инициализация с таймаутом 10 с, прогресс загрузки по вехам, облачный сейв одним ключом с нормализацией и зеркалом localStorage (дебаунс 1.5 с + flush на pagehide), rewarded только по `state === 'rewarded'` с off(), interstitial 90 с по клику с экрана итога, лидерборд по флагу возможности, конфиг моста в public/.
- **Затронутые файлы**: package.json, vite.config.ts, tsconfig.json, index.html, public/playgama-bridge-config.json, DESIGN.md, README.md, CHANGELOG.md, ACCEPTANCE.md (разделы H), src/** (main.ts, core/{Game,GameLoop,EventBus}, config/Balance.ts, platform/{PlaygamaService,StorageService,BridgeTypes}.ts, physics/{SpatialGrid,PhysicsWorld}.ts, entities/EnemyManager.ts, systems/{LensSystem,CombatSystem,WaveDirector}.ts, rendering/{SceneManager,ProceduralModels,OceanRenderer,AtmosphereRenderer,BeamRenderer,EnemyRenderer,ParticleSystem,DayNightCycle}.ts, input/InputRouter.ts, audio/AudioManager.ts, ui/{theme.css,UiRoot,ScreenRouter,Hud,components,icons,i18n,TouchControls}.ts, ui/screens/{MainMenu,PauseModal,ResultScreens}.ts)
- **Проверено**:
  - `npx tsc --noEmit` — 0 ошибок; `npm run build` — код 0, бандл 0.88 МБ (< 4.5).
  - `node scripts/check-spec.mjs` — все проверки зелёные, кроме F2 до этой записи (закрыта данным журналом); H1: сделано 133, осознанных отказов 52; O1 — раздела заказа нет.
  - `node scripts/smoke.mjs` — S1–S7 ✅: сборка 0.88 МБ, 0 ошибок консоли, кадры идут после ввода, WebGL рисует, телефон 390×844 без разъездов, интерфейс нажимается. FPS 19 в headless SwiftShader (программный рендер — не показатель устройства).
- **Известные проблемы / следующий шаг**:
  - База знаний фабрики недоступна (403): как только появится токен, прогнать `node scripts/fetch-knowledge.mjs` и сверить реализацию с docs/ref/knowledge (особенно playgama/* и ux/*).
  - Схема управления фиксируется при старте по типу устройства моста до завершения initialize (fallback браузера); перевыбор после initialize не делается — добавить синхронизацию, если площадка определит устройство иначе.
  - E1/E5 (адаптивное качество и FPS на реальном телефоне) требуют проверки руками на устройстве; автотюнер сознательно не вводился.

