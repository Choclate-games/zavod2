# FINAL AI DEVELOPER PROMPT: БРАУЗЕРНУЮ «ПОЧТАЛЬОН УЛИТОК» THREE.JS 🎮⚡

> **INSTRUCTION FOR AI CODING AGENT**:
> You are the **Lead Game Developer & Systems Architect**. Your task is to build and deliver the complete, production-ready, fully playable HTML5/WebGL game described in this specification from start to finish.
> Follow the technical architecture, physics specifications, Playgama Bridge integration, and mobile ergonomics strictly.
> Do NOT omit systems, use fake placeholder stubs, or leave TODOs. The end result must satisfy every single item in the **Definition of Done**.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: Браузерную «почтальон улиток» three.js
- **Project Slug**: `brauzernuyu-pochtalon-ulitok-three-js`
- **Genre**: 3D Менеджмент колонии / Idle-стратегия (Жизнь и рост поселения)
- **Target Platform**: Playgama Bridge (Yandex Games / VK / Web)
- **Orientation**: Landscape
- **Target Audience**: Игроки Яндекс Игр, CrazyGames и мобильных веб-порталов.
- **Player Fantasy**: Растите колонию через сезоны: распределяйте роли, готовьтесь к непогоде и расширяйте владения.
- **Core Hook**: Вы задаёте правила, а колония живёт сама и приносит последствия ваших решений.
- **Session Model**: Короткие сессии по 5-8 минут с высоким удержанием и мета-прокачкой.


---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite 5.x
- **Renderer**: **THREEJS** (^0.170.0)
  - *Selection Rationale*: User explicitly specified 'threejs'.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat 0.13.x)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Howler.js (^2.2.4) с WebAudio API
- **State Management**: Custom TinyEventBus & Reactive GameStore
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 75
- **Max Triangles / Active Sprites**: < 40000
- **Max Bundle Size**: < 4.2 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Планирование дня -> Работа колонии -> Событие сезона -> Распределение прироста -> Новый день.
```

**Фирменный момент**: Игрок успевает провести влажную феромонную тропу через тень, пока стая птиц накрывает сад, а три разные улитки синхронно доставляют хрупкие росинные письма в последний безопасный цветок.

**Чем петля отличается от соседей по жанру**: Игрок не управляет одной всесильной улиткой и не нажимает кнопку сбора. Он задаёт почтовые правила, прокладывает физически проходимые тропы, распределяет живых работников и заранее строит инфраструктуру, после чего колония самостоятельно исполняет план и раскрывает его последствия.

**Шаблон жанра, который НЕ реализуем**: Отвергаем шаблон арены с бесконечными волнами врагов и автоматическим ростом урона: он превращает сад в статичное поле боя и не поддерживает фантазию о живой почтовой колонии, где последствия возникают из маршрутов, погоды и распределения ролей.

**Схема петли**:
```text
[Утренний план]
      |
      v
[Письма цветов] -> [Феромонные тропы] -> [Самостоятельный ход улиток]
      ^                                      |
      |                                      v
[Роли и ячейки] <- [Нектар, влага, рост] <- [Паутина и хищники]
      |
      v
[Сезонное событие] -> [Потери или прирост] -> [Новый день]
```

**Микро-петля (посекундно)**:
- **Разметка утренней доставки** (4-12 с): игрок — Игрок проводит мышью или пальцем маршрут от цветка-отправителя к нужной ячейке гнезда.; игра — На траве появляется светящаяся влажная феромонная дорожка, а подходящая улитка выбирает её.; решение — Выбрать короткий солнечный путь или более длинный теневой маршрут с меньшим риском высыхания.
- **Назначение раковинных ролей** (3-8 с): игрок — Игрок перетаскивает значки письма, капли и щита на конкретных улиток.; игра — Улитки меняют поведение: курьер несёт груз, сборщик пополняет склад, страж перекрывает вход.; решение — Оставить больше курьеров ради скорости или снять их с доставки ради запаса влаги и защиты.
- **Наблюдение за движением** (20-45 с): игрок — Игрок тапает по листьям и лужам, чтобы временно перенаправить капли и открыть безопасный участок.; игра — Меняется сцепление раковин, скорость скольжения и доступность соседних ветвей маршрута.; решение — Потратить редкую каплю на текущую доставку или сохранить её до сезонного события.
- **Проверка почтового результата** (10-25 с): игрок — Игрок наблюдает за вручением писем и исправляет только критические разрывы маршрутов.; игра — Доставленные письма повышают доверие цветов, а ошибки создают новые требования и локальные проблемы.; решение — Принять небольшой убыток сейчас или перестроить сеть ценой времени и феромонов.

**Мезо-петля (этап)**:
- **Планирование дня** (20-40 с): игрок — Игрок выбирает 3-7 писем, приоритеты цветов и стартовые роли колонии.; игра — Игра показывает прогноз влажности, тени, движения птиц и расход запасов.; решение — Собрать быстрые благодарственные письма или рискнуть ради редкого письма сезона.
- **Самостоятельная работа колонии** (90-180 с): игрок — Игрок наблюдает за сетью и вмешивается жестами в отдельные узлы.; игра — Улитки перемещают письма, добывают нектар, сталкиваются с паутиной и меняют строй при угрозе.; решение — Исправить одну аварию вручную или сохранить внимание для более опасного участка.
- **Событие сезона** (15-30 с): игрок — Игрок выбирает подготовленный ответ на дождь, жару, птиц или нашествие жуков.; игра — Сезон применяет последствия к влажности, запасам, постройкам и доверию цветов.; решение — Закрыть почтовые ворота ради выживания или продолжить доставку ради редкого прироста.
- **Распределение прироста** (30-60 с): игрок — Игрок размещает новые сотовые ячейки и назначает выросших улиток на роли.; игра — Изменяются пропускная способность гнезда, доступные маршруты и устойчивость к следующему сезону.; решение — Увеличить склад, маточную ячейку, защиту входа или сеть теневых переходов.

**Макро-петля (забег)**:
- **Почтовый забег** (5-8 мин): игрок — Игрок проходит серию из 4-6 коротких дневных планов.; игра — Сад меняется: цветы раскрываются, лужи высыхают, птицы запоминают шумные маршруты.; решение — Стабильно обслуживать знакомые цветы или расшириться к опасной дальней клумбе.
- **Годовой перелом** (30-60 с): игрок — Игрок переживает главное сезонное событие и сверяет состояние колонии.; игра — Меняются правила движения, набор писем и доступность ресурсов следующего года.; решение — Считать год успешным при устойчивом росте или пожертвовать запасом ради быстрого расширения.
- **Возвращение в сад** (1-3 мин): игрок — Игрок открывает новые виды улиток, чертежи ячеек и почтовые соглашения.; игра — Следующий год получает новые условия, но сохраняет результаты предыдущих решений.; решение — Начать с безопасной специализации или проверить более суровый сезонный набор.

**Кривая напряжения**: Первые 30 с дают спокойное чтение сада и настройку маршрутов. К 60-120 с одновременно появляются высыхание, пересечения троп и первые хищники. На 150-210 с игрок видит цену первоначального плана: запас влаги и защитные ячейки либо спасают сеть, либо заставляют закрыть дальние цветы. После сезонного события напряжение резко спадает, когда уцелевшие улитки возвращаются в гнездо и игрок распределяет прирост.

**Формулы ядра (реализуй буквально)**:
- `Скорость улитки: speed = base_speed * (0.55 + humidity * 0.45) * shell_load_modifier; при влажности 100% базовая скорость составляет 0.42 м/с.`
- `Риск срыва доставки: failure_risk = route_length * dryness_rate * load_fragility * predator_pressure; если риск превышает 65%, письмо повреждается.`
- `Расход феромона: pheromone_cost = route_length / 6 м, округление вверх; один заряд обслуживает 6 м тропы.`
- `Прирост колонии: growth = delivered_letters * 2 улитки + stored_nectar / 12 ед; рост уменьшается на 50% при дефиците влаги.`
- `Удар хищника: predator_impact = 1 - guard_count / (guard_count + 3); при нулевой охране нападение блокирует 1-3 ячейки на 12 с.`
- `Устойчивый рост года: colony_growth >= 12 улиток и reserve_ratio >= 25% после четвёртого сезонного события.`

**Прогрессия внутри забега**:
- День 1: доступны 2 курьера, 1 сборщик, 1 страж, 3 письма и 2 типа поверхности.
- День 2-3: открываются пересечения троп, хрупкие письма и цветы с дедлайном 45 с.
- День 4-5: появляются паутина длиной 2-5 м, птицы с обзором 8 м и необходимость строить теневые переходы.
- День 6-7: игрок получает 1 сезонный приказ, который изменяет влажность, скорость и потребность цветов.
- Каждое повышение ранга улитки немедленно меняет одну видимую характеристику: +0.06 м/с к движению, +1 груз или +2 с устойчивости к паутине.

**Мета-прогрессия**:
- Сохранённые чертежи расширяют гнездо на 1 ячейку после каждого успешно завершённого года.
- Новые породы улиток открываются за доставку 20 писем одного типа и имеют уникальную склонность к тени, лужам или защите.
- Дневник почтмейстера сохраняет 3 постоянных правила сада, например снижение высыхания на 8% или увеличение стартового запаса на 6 ед.
- Улучшения не дают плоского урона: они меняют форму сети, время реакции колонии и доступные безопасные маршруты.

### Феромонная почтовая сеть и роли (CORE)
- **Category**: Двигатель петли и главный источник значимых решений: она превращает намерение игрока в автономную работу колонии.
- **Description**: Маршрут не является просто указателем пути: он имеет срок жизни, стоимость, пропускную способность и социальное значение для цветов. Игрок управляет правилами движения, а не каждым шагом улитки.
- **Player Input**: Мышь: зажать левую кнопку и провести линию между цветком и ячейкой. Сенсорный экран: провести пальцем по саду. Тап по улитке открывает смену роли, удержание на маршруте удаляет его.
- **Hit & Sensory Feedback**: Визуал/VFX: влажная янтарная линия пульсирует с частотой 1.2 Гц, а у выполненного письма вспыхивает маленькая росинная печать.; Звук Web Audio: движение пальца создаёт тихий шорох, выбор роли звучит отдельным щелчком, а пробка понижает тон маршрута на 2 полутона.; Камера: при выборе узла выполняется приближение на 6%, при вручении письма возникает импульс длительностью 0.12 с.; UI/Тактильность: цвет линии показывает запас феромона, на мобильном устройство даёт вибрацию 18 мс при создании узла и 45 мс при разрыве.; Информационный слой: рядом с тропой отображаются время доставки в секундах и прогноз расхода капель.
- **Technical Complexity**: Medium
- **Решение игрока**: Для каждого письма выбрать маршрут, порядок остановок и роль улитки: курьер, сборщик, страж или проводник капель.
- **Состояния**: `Пустая почва: маршрут не существует, улитка ищет ближайшую метку.`, `Размеченная тропа: феромон действует, пока его сила выше 20%.`, `Перегруженный узел: на участке стоят 3 улитки, скорость каждой снижается на 35%.`, `Разрыв тропы: сила метки ниже 20% или путь закрыт паутиной, курьер возвращается к последнему узлу.`, `Исполненная доставка: письмо вручено, маршрут получает краткое усиление на 6 с.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Длина одной тропы` = `18-65 м` — Ниже 18 м сад ощущается как меню с кнопками, выше 65 м игрок перестаёт читать маршрут и планирование становится угадыванием.
  - `Скорость курьера без груза` = `0.42 м/с` — Ниже 0.30 м/с исчезает ощущение живого ответа, выше 0.58 м/с игрок не успевает заметить ошибки маршрута.
  - `Угасание феромона` = `0.8%/с` — При 0.3%/с сеть почти не требует обслуживания, при 1.5%/с игрок занят рисованием вместо наблюдения за колонией.
  - `Допустимое число улиток в узле` = `3 улитки` — Лимит 1 делает пересечения слишком хрупкими, лимит 5 превращает дорожные пробки в незаметную косметику.
- **Слои отклика**:
  - Визуал/VFX: влажная янтарная линия пульсирует с частотой 1.2 Гц, а у выполненного письма вспыхивает маленькая росинная печать.
  - Звук Web Audio: движение пальца создаёт тихий шорох, выбор роли звучит отдельным щелчком, а пробка понижает тон маршрута на 2 полутона.
  - Камера: при выборе узла выполняется приближение на 6%, при вручении письма возникает импульс длительностью 0.12 с.
  - UI/Тактильность: цвет линии показывает запас феромона, на мобильном устройство даёт вибрацию 18 мс при создании узла и 45 мс при разрыве.
  - Информационный слой: рядом с тропой отображаются время доставки в секундах и прогноз расхода капель.
- **Режим отказа игрока**: Курьер останавливается у серого разрыва, письмо теряет одну печать, а UI показывает конкретную причину: высохшая метка, пробка, паутина или отсутствие роли проводника.
- **Кривая мастерства**: Через 30 с новичок соединяет два цветка короткой линией. К десятому забегу эксперт строит кольцевую сеть с одним общим узлом, разводит роли по времени и оставляет запас феромона на сезонное событие.
- **Сопротивление игры**: Солнечные пятна высушивают отдельные сегменты, птицы реагируют на слишком прямые маршруты, а цветы меняют приоритет после успешной доставки. Давление растёт не волнами, а через накопление незакрытых почтовых обязательств.
- **Синергии**:
  - Теневая ячейка снижает угасание феромона, поэтому строительство напрямую меняет геометрию сети.
  - Капельное управление повышает скорость на мокрых сегментах, но лишает запас влаги для сезонной защиты.
  - Стражи используют пересечения троп как точки перехвата хищников.
- **Почему это не жанровый шаблон**: Маршрут не является просто указателем пути: он имеет срок жизни, стоимость, пропускную способность и социальное значение для цветов. Игрок управляет правилами движения, а не каждым шагом улитки.
- **Псевдокод тика**:
```text
получить route_input, snail_roles и map_state
если route_input содержит новый отрезок, проверить длину и занятость почвы
создать pheromone_path и списать pheromone_cost
для каждого курьера выбрать ближайшее письмо с максимальным приоритетом
если узел перегружен, снизить speed_modifier на 35%
если сила тропы ниже 20%, перевести курьера в состояние возврата
при вручении письма увеличить trust_flow и вызвать mail_delivered
обновить визуал, звук, камеру и тактильный отклик
```

### Влажностный рельеф и сцепление раковины (SECONDARY)
- **Category**: Физический источник риска и пространственный пазл: игрок выбирает между скоростью сухой дорожки и надёжностью влажной тени.
- **Description**: Сад является физической логистической картой: одна и та же феромонная линия имеет разную стоимость и риск в зависимости от текущей влаги, тени и наклона листьев.
- **Player Input**: Мышь или палец: тап по капле переносит её на соседний участок почвы, свайп по листу поворачивает лист на 15 градусов и меняет тень.
- **Hit & Sensory Feedback**: Визуал/VFX: капли имеют объём, отражают утренний свет, а сухой грунт постепенно покрывается матовой пылью.; Звук Web Audio: в луже слышно мягкое плескание, на сухом грунте усиливается шорох, скольжение получает короткий восходящий тон.; Камера: срыв на листе даёт наклон на 2 градуса и импульс 0.10 с, перегрев слегка отдаляет камеру на 4%.; UI/Тактильность: кольцо вокруг улитки показывает влажность, мобильная вибрация длительностью 25 мс предупреждает о входе в сухую зону.; Информационный слой: при удержании на поверхности показываются влажность в процентах, температура в градусах Цельсия и прогноз времени перехода.
- **Technical Complexity**: Medium
- **Решение игрока**: Решить, провести ли письмо через лужу, тень, лист или открытый грунт, а также потратить ли каплю на временное увлажнение опасного сегмента.
- **Состояния**: `Роса: влажность 75-100%, высокая скорость и устойчивое сцепление.`, `Мокрый лист: влажность 55-75%, скорость стабильна, но есть короткое скольжение.`, `Сухой грунт: влажность 25-55%, скорость падает, феромон угасает быстрее.`, `Перегрев: влажность ниже 25% или температура выше 31°C, улитка останавливается на 4 с.`, `Паутина: движение замедлено, пока страж или другой курьер не очистит участок.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Рабочий диапазон влажности` = `25-100%` — Если безопасная зона шире 70%, выбор поверхности исчезает; если уже 15%, даже хороший маршрут кажется случайным.
  - `Глубина лужи` = `0.02-0.12 м` — Ниже 0.02 м лужа выглядит декорацией, выше 0.12 м она превращается в обязательный тормоз для всех писем.
  - `Скользящий угол листа` = `18 градусов` — При 8 градусах лист не требует внимания, при 30 градусах курьеры часто срываются и игрок винит физику.
  - `Высыхание на солнце` = `1.4%/с` — При 0.6%/с солнечный риск не ощущается, при 2.2%/с невозможно планировать маршрут дольше 30 с.
- **Слои отклика**:
  - Визуал/VFX: капли имеют объём, отражают утренний свет, а сухой грунт постепенно покрывается матовой пылью.
  - Звук Web Audio: в луже слышно мягкое плескание, на сухом грунте усиливается шорох, скольжение получает короткий восходящий тон.
  - Камера: срыв на листе даёт наклон на 2 градуса и импульс 0.10 с, перегрев слегка отдаляет камеру на 4%.
  - UI/Тактильность: кольцо вокруг улитки показывает влажность, мобильная вибрация длительностью 25 мс предупреждает о входе в сухую зону.
  - Информационный слой: при удержании на поверхности показываются влажность в процентах, температура в градусах Цельсия и прогноз времени перехода.
- **Режим отказа игрока**: Улитка не исчезает и не получает абстрактный штраф: она заметно скользит, оставляет рваный след и останавливается. UI подписывает причину конкретным словом: сухо, скользко, жарко или липкая паутина.
- **Кривая мастерства**: Через 30 с новичок выбирает лужу для одного письма. К десятому забегу эксперт использует поворот листьев для создания движущегося коридора тени и рассчитывает, какая часть тропы высохнет к моменту возвращения.
- **Сопротивление игры**: Солнце перемещается на 12 градусов за 30 с, ветер высушивает листья, а дождь временно повышает влажность, но делает поверхность скользкой. Сопротивление заставляет менять путь, а не просто ждать перезарядку.
- **Синергии**:
  - Скорость по влажности определяет, успеет ли письмо до дедлайна.
  - Сотовые ячейки для росы увеличивают радиус увлажнения на 2 м.
  - Феромонный проводник может безопасно провести курьера через скользкий лист.
- **Почему это не жанровый шаблон**: Сад является физической логистической картой: одна и та же феромонная линия имеет разную стоимость и риск в зависимости от текущей влаги, тени и наклона листьев.
- **Псевдокод тика**:
```text
получить snail_position, surface_type, humidity и temperature
вычислить speed_modifier по влажности и типу поверхности
если humidity < 25% или temperature > 31°C, включить состояние перегрева
если surface_type равен листу и угол > 18 градусов, применить скольжение
если игрок перенёс каплю, добавить 18% влажности ближайшему сегменту
уменьшать влажность на солнце на 1.4%/с
при входе в лужу проиграть splash_sound и обновить camera_impulse
передать новый риск маршруту и почтовому грузу
```

### Хрупкие письма и живая сортировка (SECONDARY)
- **Category**: Источник награды и темпа: письма задают цели дня, связывают цветы с гнездом и превращают рост доверия в новые последствия.
- **Description**: Почтовый груз не является обычным ресурсом: каждое письмо меняет отношения с конкретным цветком и перестраивает будущий поток заказов, поэтому ошибка сегодня создаёт другой сад завтра.
- **Player Input**: Игрок перетаскивает карточки писем в почтовую ленту, тапает по цветку для назначения получателя и удерживает карточку для просмотра маршрута и срока.
- **Hit & Sensory Feedback**: Визуал/VFX: печать письма трескается по сегментам прочности, а вручение создаёт веер из золотых пыльцевых частиц.; Звук Web Audio: каждый приоритет имеет свой тембр, ускорение таймера повышает частоту тихого тика, успешная доставка звучит как короткий колокольчик.; Камера: при передаче письма камера следует за курьером 0.35 с и мягко приближает цветок-получатель на 8%.; UI/Тактильность: лента сортировки показывает срок и хрупкость, вибрация 35 мс срабатывает за 10 с до истечения срока.; Информационный слой: на карте появляется пунктир от текущей позиции курьера к следующей цели и прогноз остатка прочности.
- **Technical Complexity**: Medium
- **Решение игрока**: Выбрать порядок доставки, допустимый риск повреждения и письмо, которое будет отложено ради сохранения всей сети.
- **Состояния**: `Запрошено: цветок сформировал письмо и таймер ещё не запущен.`, `В пути: письмо находится у курьера, его хрупкость уменьшается при сухости и столкновениях.`, `На сортировке: письмо ждёт свободного курьера или нужную почтовую ячейку.`, `Вручено: цветок повышает доверие и открывает новый ресурс.`, `Испорчено: письмо теряет ценность, а цветок меняет запрос на более сложный.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Количество писем в дне` = `3-7 писем` — Меньше 3 писем не создаёт маршрутизационной задачи, больше 7 перегружает короткую сессию и превращает её в таблицу.
  - `Срок обычного письма` = `45-90 с` — Ниже 35 с решение зависит от реакции, выше 120 с исчезает ощущение срочной почты.
  - `Вместимость курьера` = `1 письмо или 2 лёгких письма` — Одна единица делает логистику понятной, две тяжёлые посылки ломают чтение приоритетов.
  - `Хрупкость росинного письма` = `35 единиц прочности` — При 20 единицах письмо слишком случайно портится, при 60 единицах влажность и столкновения теряют смысл.
- **Слои отклика**:
  - Визуал/VFX: печать письма трескается по сегментам прочности, а вручение создаёт веер из золотых пыльцевых частиц.
  - Звук Web Audio: каждый приоритет имеет свой тембр, ускорение таймера повышает частоту тихого тика, успешная доставка звучит как короткий колокольчик.
  - Камера: при передаче письма камера следует за курьером 0.35 с и мягко приближает цветок-получатель на 8%.
  - UI/Тактильность: лента сортировки показывает срок и хрупкость, вибрация 35 мс срабатывает за 10 с до истечения срока.
  - Информационный слой: на карте появляется пунктир от текущей позиции курьера к следующей цели и прогноз остатка прочности.
- **Режим отказа игрока**: Просроченное письмо не вызывает мгновенного поражения: цветок закрывается, доверие падает на 1 уровень, а UI показывает, где именно исчезло время или прочность.
- **Кривая мастерства**: Через 30 с новичок доставляет обычное письмо в ближайший цветок. К десятому забегу эксперт комбинирует два лёгких письма, использует обратный путь курьера и сознательно отдаёт одно письмо, чтобы сохранить редкое сезонное.
- **Сопротивление игры**: Цветы меняют запросы после каждой доставки, сезонные письма имеют короткие сроки, а птицы увеличивают хрупкость груза на открытых участках. Давление создаётся конкуренцией целей, а не спавном одинаковых врагов.
- **Синергии**:
  - Феромонные приоритеты определяют порядок выбора письма автономным курьером.
  - Влажные маршруты сохраняют прочность росинных писем.
  - Складская ячейка позволяет временно снять письмо с маршрута без запуска нового таймера.
- **Почему это не жанровый шаблон**: Почтовый груз не является обычным ресурсом: каждое письмо меняет отношения с конкретным цветком и перестраивает будущий поток заказов, поэтому ошибка сегодня создаёт другой сад завтра.
- **Псевдокод тика**:
```text
получить mail_queue, courier_state и route_condition
выбрать письмо с максимальным priority_score среди доступных
если courier_load превышает вместимость, перевести лишнее письмо на сортировку
уменьшать mail_durability по dryness_rate и predator_pressure
если distance_to_target / current_speed > time_left, показать предупреждение
если courier_reached_target, увеличить trust_level и выдать resource_reward
если mail_durability <= 0 или time_left <= 0, пометить письмо как испорченное
обновить ленту, таймер, звук и маршрут следующего курьера
```

### Сотовое гнездо и сезонный запас росы (SECONDARY)
- **Category**: Тормоз расширения и долгосрочная страховка: строительство определяет, какие ошибки колония может пережить.
- **Description**: Гнездо не является экраном улучшений: оно физически меняет карту маршрутов, тени, запасы и точки обороны, поэтому каждый год колония получает новую логистическую геометрию.
- **Player Input**: Мышь или палец: выбрать контур гнезда и перетащить ячейку на соседнюю свободную грань. Поворот двумя пальцами меняет ориентацию переборки.
- **Hit & Sensory Feedback**: Визуал/VFX: новая ячейка собирается из полупрозрачных слоёв слюды, внутри заполняется голубыми каплями.; Звук Web Audio: перенос нектара звучит ритмом мягких деревянных щелчков, завершение строительства даёт низкий тёплый аккорд.; Камера: размещение ячейки вызывает подъём камеры на 0.25 м и плавный обзор ближайших маршрутов.; UI/Тактильность: контур зелёный при полезном соединении и красный при блокировке входа, подтверждение сопровождается вибрацией 30 мс.; Информационный слой: возле каждого контура отображаются вместимость, сезонная устойчивость и число связанных троп.
- **Technical Complexity**: Medium
- **Решение игрока**: Разместить ограниченную ячейку так, чтобы усилить матку, хранение, тень, сортировку или входную защиту.
- **Состояния**: `Чертёж: ячейка показана призрачным контуром, ресурсы ещё не списаны.`, `Строительство: рабочие улитки переносят нектар в течение заданного времени.`, `Рабочая ячейка: её эффект активен, пока есть 1 свободный вход.`, `Переполненная ячейка: запас превышает вместимость, излишек теряется.`, `Сезонная трещина: сильный дождь или жук временно отключает ячейку на 8-20 с.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Размер ячейки` = `0.8 м` — Ниже 0.6 м ячейки теряются на экране, выше 1.1 м гнездо становится слишком маленьким для модульной головоломки.
  - `Стоимость постройки` = `12 ед нектара` — Стоимость 6 ед делает расширение автоматическим, 20 ед заставляет откладывать строительство слишком долго.
  - `Вместимость ячейки росы` = `30 ед` — Ниже 18 ед сезонный запас не спасает, выше 45 ед игрок перестаёт планировать ежедневный расход.
  - `Время строительства` = `18 с` — При 8 с ячейки можно переставлять без последствий, при 35 с одно неверное размещение портит всю короткую сессию.
- **Слои отклика**:
  - Визуал/VFX: новая ячейка собирается из полупрозрачных слоёв слюды, внутри заполняется голубыми каплями.
  - Звук Web Audio: перенос нектара звучит ритмом мягких деревянных щелчков, завершение строительства даёт низкий тёплый аккорд.
  - Камера: размещение ячейки вызывает подъём камеры на 0.25 м и плавный обзор ближайших маршрутов.
  - UI/Тактильность: контур зелёный при полезном соединении и красный при блокировке входа, подтверждение сопровождается вибрацией 30 мс.
  - Информационный слой: возле каждого контура отображаются вместимость, сезонная устойчивость и число связанных троп.
- **Режим отказа игрока**: Неверная ячейка не пропадает бесследно: она блокирует одну грань, подсвечивает перекрытый вход и показывает прогноз потери пропускной способности в процентах.
- **Кривая мастерства**: Через 30 с новичок строит склад рядом с маткой. К десятому забегу эксперт формирует кольцо из теневых и росинных ячеек, оставляя узкий защищённый вход, через который проходят все курьеры.
- **Сопротивление игры**: Каждый сезон проверяет разные стороны гнезда: жара сушит наружные ячейки, дождь перегружает нижние, жуки ищут самый короткий вход. Одно универсальное расположение не работает.
- **Синергии**:
  - Склад увеличивает число писем, которые можно временно отложить.
  - Теневая ячейка снижает высыхание феромонной сети в радиусе 4 м.
  - Защитная переборка даёт стражам дополнительное время реакции на входе.
- **Почему это не жанровый шаблон**: Гнездо не является экраном улучшений: оно физически меняет карту маршрутов, тени, запасы и точки обороны, поэтому каждый год колония получает новую логистическую геометрию.
- **Псевдокод тика**:
```text
получить cell_blueprint, nest_grid и resource_stock
если выбранная грань не соседствует с гнездом, отклонить размещение
если resource_stock.nektar < 12, показать нехватку и не запускать стройку
списать 12 ед нектара и добавить cell_state=building
каждые 1 с продвигать строительство на 1/18
после завершения активировать эффект по типу ячейки
при сезонном ударе проверить устойчивость и временно отключить повреждённую ячейку
пересчитать радиусы тени, запаса и защиты для всех маршрутов
```

### Следовая оборона от птиц, ос и жуков (SECONDARY)
- **Category**: Источник риска и тактического перераспределения: игрок защищает не абстрактные жизни, а конкретные почтовые коридоры.
- **Description**: Оборона строится вокруг управления следами и видимостью. Игрок не атакует врагов кнопкой, а решает, какой запах оставить, какой путь скрыть и какой почтовый груз поставить под риск.
- **Player Input**: Мышь или палец: поставить до 3 феромонных постов тревоги на узлах, провести дугу отвлечения вокруг листа и тапом приказать стражу перехватить цель.
- **Hit & Sensory Feedback**: Визуал/VFX: тень птицы скользит по траве за 2.5 с до атаки, оса оставляет жёлтую пыльцу, а пост тревоги мигает с частотой 2 Гц.; Звук Web Audio: птица слышна низким взмахом слева или справа, оса получает направленный жужжащий слой, успешный перехват заканчивается хлопком листа.; Камера: угроза вне экрана вызывает короткий сдвиг камеры на 0.15 м в сторону звука, столкновение даёт импульс 0.18 с.; UI/Тактильность: опасный узел обводится красным сектором с оставшимися секундами, мобильное устройство вибрирует серией 20-20-60 мс.; Информационный слой: линия обзора птицы и вероятность обнаружения показываются только при удержании на посту, сохраняя чистый вид сада.
- **Technical Complexity**: Medium
- **Решение игрока**: Выбрать, где держать стражей, какой маршрут сделать незаметным и какую доставку временно отменить при угрозе.
- **Состояния**: `Следовая тишина: угрозы нет, стражи восстанавливают силы.`, `Запах замечен: птица или насекомое обнаружило груз, но ещё не выбрало точку атаки.`, `Перехват: страж движется к входу или отвлекает хищника на ложный феромонный след.`, `Столкновение: хищник блокирует узел на короткий срок, груз может потерять прочность.`, `Отступление: после успешного перехвата угроза покидает участок и оставляет временную безопасную зону.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Время реакции стража` = `1.4 с` — Ниже 0.8 с оборона решает всё автоматически, выше 2.5 с игрок не успевает связать сигнал с действием.
  - `Радиус обзора птицы` = `8 м` — При 4 м птица почти не создаёт маршрутизационной угрозы, при 12 м безопасные зоны становятся слишком редкими.
  - `Число постов тревоги` = `3 поста` — Один пост не даёт выбора, пять постов позволяют закрыть весь сад без жертв.
  - `Время блокировки узла` = `12 с` — Ниже 6 с столкновение не меняет план, выше 24 с одна ошибка парализует короткий уровень.
- **Слои отклика**:
  - Визуал/VFX: тень птицы скользит по траве за 2.5 с до атаки, оса оставляет жёлтую пыльцу, а пост тревоги мигает с частотой 2 Гц.
  - Звук Web Audio: птица слышна низким взмахом слева или справа, оса получает направленный жужжащий слой, успешный перехват заканчивается хлопком листа.
  - Камера: угроза вне экрана вызывает короткий сдвиг камеры на 0.15 м в сторону звука, столкновение даёт импульс 0.18 с.
  - UI/Тактильность: опасный узел обводится красным сектором с оставшимися секундами, мобильное устройство вибрирует серией 20-20-60 мс.
  - Информационный слой: линия обзора птицы и вероятность обнаружения показываются только при удержании на посту, сохраняя чистый вид сада.
- **Режим отказа игрока**: Игрок видит причину до потери: тень птицы показывает сектор обзора, оса подсвечивает запах груза, жук отмечает кратчайший вход. После ошибки блокируется конкретный узел, а не вся колония.
- **Кривая мастерства**: Через 30 с новичок ставит стража у главного входа. К десятому забегу эксперт создаёт ложную феромонную ветку, заманивает осу к пустой ячейке и держит настоящие письма в движущемся теневом коридоре.
- **Сопротивление игры**: Птицы реагируют на открытые прямые линии, осы на хрупкие письма, жуки на запах нектара. Угрозы появляются из состояния сада и действий игрока, а не в виде одинаковых волн.
- **Синергии**:
  - Плотная сеть феромонов привлекает больше ос, но позволяет быстрее перебросить стражей.
  - Защитная переборка увеличивает время реакции на 0.8 с.
  - Ложный маршрут и поворот листа позволяют вывести хищника из зоны доставки без прямого столкновения.
- **Почему это не жанровый шаблон**: Оборона строится вокруг управления следами и видимостью. Игрок не атакует врагов кнопкой, а решает, какой запах оставить, какой путь скрыть и какой почтовый груз поставить под риск.
- **Псевдокод тика**:
```text
получить predator_state, mail_trails и guard_posts
для каждой угрозы вычислить target по запаху, видимости и ценности груза
если угроза вошла в радиус 8 м, создать danger_signal
если guard_distance / guard_speed <= 1.4 с, назначить перехват
при наличии ложного следа с вероятностью 70% сменить target
если guard_reached_target, уменьшить predator_pressure и создать safe_zone на 6 с
если threat_reached_node, заблокировать узел на 12 с и снизить прочность груза
обновить тень, Web Audio, камеру, UI и тактильный отклик
```

### Ритмичная нарезка и подготовка (SECONDARY)
- **Category**: генератор ресурсов и заготовок
- **Description**: Превращает рутину в драйвовый ритм-геймплей
- **Player Input**: Тап по экрану / Пробел в такт мерцающему маркеру
- **Hit & Sensory Feedback**: Визуал: разлетающиеся дольки, частицы пара, вспышка на комбо; Звук: сочный стук ножа о деревянную доску, нарастающий звон комбо; Камера: микро-акцент при закрытии целого ингредиента; UI: множитель серии ×1.5/×2.0 над разделочным столом
- **Technical Complexity**: Medium
- **Решение игрока**: Нарезать ингредиенты сериями с риском порезать палец или работать медленно
- **Состояния**: `ОЖИДАНИЕ`, `НАРЕЗКА`, `ЗОЛОТОЙ_ТАЙМИНГ`, `ЗАМЕДЛЕНИЕ`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Окно идеального тапа` = `0.12 с` — Меньше — игрок сбивается; больше — исчезает ритм
  - `Число кусочков на порцию` = `4 шт` — Влияет на темп прохождения заказов
- **Слои отклика**:
  - Визуал: разлетающиеся дольки, частицы пара, вспышка на комбо
  - Звук: сочный стук ножа о деревянную доску, нарастающий звон комбо
  - Камера: микро-акцент при закрытии целого ингредиента
  - UI: множитель серии ×1.5/×2.0 над разделочным столом
- **Режим отказа игрока**: Промах мимо такта сбрасывает комбо и задерживает подачу блюда на 1.0 с
- **Кривая мастерства**: Новичок нарезает по одному клику; эксперт отбивает ритм 180 BPM без пауз
- **Сопротивление игры**: Очередь гостей уплотняется, добавляются блюда с 3 типами нарезки
- **Синергии**:
  - Подготовленные продукты идут в «Контроль жара вока»
- **Почему это не жанровый шаблон**: Превращает рутину в драйвовый ритм-геймплей
- **Псевдокод тика**:
```text
on_knife_tap():
    diff = abs(now - beat_target)
    if diff < 0.12: combo += 1; emit_slice_vfx(); add_ingredient()
    else: combo = 1; knife_stuck_timer = 0.5; play_dull_sound()
```

### Контроль жара вока и сковороды (SECONDARY)
- **Category**: ключевой фактор качества блюда
- **Description**: Честная физика жара вместо простого таймера ожидания
- **Player Input**: Удержание регулятора пламени / свайп подбрасывания
- **Hit & Sensory Feedback**: Визуал: шипящее масло, золотистая корочка, поднимающийся дымок; Звук: сочное шипение, треск пламени горелки; Камера: мягкий жар-шейдер по краям экрана при перегреве; UI: круговой индикатор степени прожарки с зеленой зоной
- **Technical Complexity**: Medium
- **Решение игрока**: Держать максимальный огонь ради скорости или убавить жар во избежание гари
- **Состояния**: `ХОЛОДНО`, `ИДЕАЛЬНАЯ_ОБЖАРКА`, `ПЕРЕГРЕВ`, `СГОРЕЛО`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Время идеальной готовности` = `3.5 с` — Короче — игрок не успевает; длиннее — скука
  - `Окно до сгорания` = `1.8 с` — Создает приятный стресс
- **Слои отклика**:
  - Визуал: шипящее масло, золотистая корочка, поднимающийся дымок
  - Звук: сочное шипение, треск пламени горелки
  - Камера: мягкий жар-шейдер по краям экрана при перегреве
  - UI: круговой индикатор степени прожарки с зеленой зоной
- **Режим отказа игрока**: Передержанное блюдо сгорает в угли и отправляется в ведро
- **Кривая мастерства**: Умение одновременно вести 3 сковороды на грани сгорания
- **Сопротивление игры**: Случайные скачки пламени и наплыв VIP-клиентов
- **Синергии**:
  - Успешная обжарка кормит «Очередь и чаевые»
- **Почему это не жанровый шаблон**: Честная физика жара вместо простого таймера ожидания
- **Псевдокод тика**:
```text
on_tick(dt):
    temp += flame_power * dt * 15
    if temp in IDEAL_ZONE: cook_progress += dt * 1.5
    elif temp > MAX_ZONE: burn_timer += dt; if burn_timer > 1.8: set_burnt()
```



---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Application Layer**: Инициализация Vite, ресайз канваса, полноэкранный режим, прелоадер.
- **Platform & Ads Layer**: Адаптер Playgama Bridge, менеджеры Interstitial и Rewarded рекламы, Cloud Save.
- **Core Engine Layer**: Фиксированный цикл GameLoop 60Гц, EventBus, маршрутизация ввода.
- **Physics Simulation Layer**: Шаги симуляции Rapier3D, фильтрация коллизий, рэгдолл-синхронизация.
- **Gameplay Systems Layer**: Боевая система, спавн врагов, выбор 3 карт улучшений, расчет комбо.
- **Entity Management Layer**: Пул сущностей игрока, врагов, снарядов и осколков.
- **Rendering Layer**: Three.js граф сцены, InstancedMesh батчинг, тени, эмиттеры частиц.
- **UI & HUD Layer**: HTML5/CSS3 оверлей, виртуальный джойстик, полосы HP, модальные окна.

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   ├── PhysicsWorld.ts        # Rapier3D / Physics world manager
│   └── RagdollController.ts    # Joint solver, balance spring torque, knockback
├── entities/
│   ├── Player.ts              # Player character entity & input impulses
│   ├── Enemy.ts               # Enemy AI behavior tree & ragdoll death
│   └── Weapon.ts              # Weapon mass, hitboxes, collision queries
├── systems/
│   ├── CombatSystem.ts        # Hitbox resolution, parry timing, damage formulas
│   ├── WaveManager.ts         # Spawning curves, elite bosses, wave clears
│   ├── UpgradeManager.ts      # 3-card roguelite selection & stat application
│   └── CrowdFavorSystem.ts    # Hype calculation and dynamic drop rewards
├── rendering/
│   ├── SceneManager.ts        # Three.js / PixiJS scene graph, lighting, camera lerp
│   ├── MeshPool.ts            # InstancedMesh pooling for debris & effects
│   └── Shaders.ts             # Optimized mobile shaders & materials
├── ui/
│   ├── UIManager.ts           # DOM HUD overlay, screen transitions
│   ├── VirtualJoystick.ts     # Mobile touch floating joystick
│   └── CardModal.ts           # 3-choice upgrade modal
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic battle BGM
```

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Второе Дыхание (Возрождение) (`revive_run`)**: Восстановление 50% HP + 3 сек неуязвимости с силовой волной, раскидывающей врагов. (Trigger: При получении смертельного урона., Limit: 1 раз за забег.)
- **Удвоение Наград (2x Золото) (`double_gold_run`)**: Удваивает все заработанные шестеренки и монеты за завершенный раунд. (Trigger: Экран окончания игры., Limit: Доступно на каждом экране результатов.)
- **Переброс Карт Улучшений (`free_card_reroll`)**: Обновляет список 3 карт улучшений с гарантией Редкой или Эпической карты. (Trigger: Окно выбора 3 карт., Limit: До 2 раз за забег.)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"player_save_v1"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Landscape
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Обязательный контракт тач-управления
- **Одним пальцем**: панорамирование карты, тап — выбор объекта.
- **Двумя пальцами**: пинч-зум и поворот камеры.
- **Справа снизу**: панель постройки/действий, кнопки ≥ 64 px.

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Плавающий стик**: зона захвата — вся левая половина экрана, база стика
  появляется под пальцем. Мёртвая зона 8%, иначе управление дрожит.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `WASD` / стрелки — движение
- ЛКМ / `J` — основная атака
- ПКМ / `K` — тяжёлая атака / блок
- `Space` / `Shift` — рывок / уклонение
- `P` / `Esc` — пауза

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/brauzernuyu-pochtalon-ulitok-three-js/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ART DIRECTION & VISUAL GUIDELINES
- **Style**: Тёплая акварель: луга и солнечный свет
- **Camera Perspective**: Изометрическая 3D камера с динамическим зумом (FOV: 50°, Pitch: 45°)
- **Environment**: Живая природа: травы, пыльца, свет сквозь листву
- **Lighting**: Мягкий рассеянный дневной свет, тёплые тени
- **Visual Feedback**: Screen-space hitstop (40ms on critical hit), directional particle sparks, additive ribbon weapon trails.

---

## 8. STEP-BY-STEP DEVELOPMENT ROADMAP
### Phase 1: Архитектура и Базовый Прототип (3 days)
- **Deliverable**: Управляемая модель машины/персонажа на 3D арене с 60 FPS
  - Настройка Vite, TypeScript и @playgama/bridge
  - Инициализация сцены Three.js и физического мира Rapier3D
  - Реализация контроллера движения и физики заноса
### Phase 2: Боевая Система и Спавн Врагов (4 days)
- **Deliverable**: Рабочий боевой цикл с волнами противников
  - Система расчета коллизий, урона и хит-стопа
  - Пул врагов с базовым AI преследования
  - Реализация шкалы ярости и супер-ударов
### Phase 3: Рогалик-Прогрессия и UI (3 days)
- **Deliverable**: Полноценный цикл забега с прокачкой способностей
  - Система 3-Card улучшений и синергий
  - Верстка адаптивного HUD и меню на HTML5/CSS3
  - Звуковые эффекты через Howler.js
### Phase 4: Интеграция Playgama Bridge и Полишинг (2 days)
- **Deliverable**: Полная интеграция с порталом Яндекс Игры
  - Интеграция Rewarded и Interstitial рекламы
  - Сохранение прогресса в Cloud Storage
  - Подключение глобальных таблиц рекордов
### Phase 5: QA, Оптимизация и Релиз (2 days)
- **Deliverable**: Готовая релизная сборка для публикации
  - Тестирование на мобильных устройствах (iOS/Android)
  - Профайлинг WebGL памяти и вызовов отрисовки
  - Финальная сборка и проверка Definition of Done


---

## 9. NON-NEGOTIABLE PLATFORM RULES
Every rule below corresponds to a bug that reached production or a moderation rejection in a shipped game. They override any conflicting habit, tutorial or example — including snippets found in the Playgama/Yandex docs, many of which describe the deprecated Bridge v1 contract.

Distilled from shipped HTML5 games on Yandex Games, VK, OK, CrazyGames and
Playgama. Every rule here corresponds to a bug that reached production or a
moderation rejection. Violating any of them ships a broken or rejected game.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Renderer

52. Auto-tuning quality from raw frame time does not work under vsync — every frame
    takes ≈ the refresh interval regardless of GPU load. Measure the cadence between
    **rendered** frames and discover headroom by optimistic probing.
53. Never target above the panel's refresh rate, and never launch in reduced quality
    and crawl up — start optimistic and step down.
54. Apply resolution and shadow-map changes **before** `render()`, on a frame you
    actually render; doing it after clears the canvas and flashes a blank frame.
55. One group, one degree of freedom. A mesh that both steers and spins (wheel,
    turret, limb) needs **nested** groups — `Euler('XYZ')` applies the inner
    rotation around the parent's axis and visibly skews the part otherwise.
    See `knowledge/threejs/vehicle_wheel_rig.md`.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Physics vehicles

60. A physics vehicle is **never** driven by writing `setLinvel()` every frame.
    That overwrites the solver, ignores slopes and heading, and leaves the wheels
    as decoration — the "wheels don't turn, the truck doesn't move" bug. Use the
    engine's own ray-cast vehicle controller (`world.createVehicleController`).
61. Visual wheels of a physics vehicle are **children of the chassis group**, and
    their suspension travel, steering angle and roll angle are read from the
    controller. A separate wheel root that copies only `translation()` detaches
    the wheels from the body the moment it tilts, and leaves parts overlapping
    where the body has moved on. The opposite rule in `vehicle_wheel_rig.md` §3
    applies to kinematic arcade cars only.
62. `updateVehicle(dt)` runs **before** `world.step()`, and the wheels' ray-casts
    are filtered to the ground collision group — unfiltered they hit the chassis
    or the cargo and the vehicle climbs its own load.
63. Cargo and props carried by a body spawn from **body-local** slots that do not
    intersect its colliders. An overlap at spawn is resolved by ejection: the load
    fires out of the bed on frame one. Anything meant to stay put needs real wall
    colliders, not a painted lip.
64. Terrain is one continuous displaced ribbon whose collider is built from the
    **same buffers** as the visible mesh. A road assembled from individually
    rotated boxes has a ledge at every joint. Trimesh colliders are for static
    bodies only.
65. Restarting a run **teleports** existing bodies (translation, rotation and both
    velocities, then `wakeUp()`); it never rebuilds meshes and bodies. Rebuilds
    leak the old body, dispose shared geometry and orphan the vehicle controller.
    Removed items are disabled, not destroyed.
66. Physics handling is verified head-lessly before it is verified by eye — the
    physics engine runs in Node without a renderer. Keep the vehicle spec in a
    renderer-free module and assert acceleration, wheel-rotation sign, steering
    sign, suspension settling and cargo retention in a script.
    See `knowledge/threejs/rapier_vehicle_controller.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

---

## 10. DEFINITION OF DONE (MANDATORY VERIFICATION CHECKLIST)
To mark this game as complete, every single requirement below must be verified and working:

- [ ] Полная компиляция проекта на TypeScript без ошибок сборки
- [ ] Стабильные 60 FPS в браузере с временем отклика управления < 50мс
- [ ] Рабочий цикл сессии: старт -> волны врагов -> 3-Card апгрейды -> битва с боссом -> результат
- [ ] Интеграция Playgama Bridge: Rewarded видео, баннеры, Cloud Save и Leaderboards
- [ ] Мобильное управление на Pointer Events: движение и основное действие работают одновременно, мультитач, отмена скролла и контекстного меню, safe-area
- [ ] Слой тач-управления виден только в игровом процессе и сбрасывается при паузе, сворачивании вкладки и показе рекламы
- [ ] Мобильную раскладку можно проверить на десктопе флагом ?touch=1
- [ ] Звуковое сопровождение: фоновая музыка и сочные звуки попаданий/дрифта
- [ ] `npm install && npm run dev` поднимают играбельную сборку без ошибок в консоли
- [ ] Ведутся DEVLOG.md и CHANGELOG.md, в README.md описаны запуск и управление
- [ ] Соответствие всем критериям публикации на Яндекс Играх

---

## 11. FACTORY KNOWLEDGE BASE
Deep, worked-out detail behind the rules in section 9 — read the relevant file before implementing that area:

- `knowledge/CRITICAL_RULES.md`
- `knowledge/audio/web_audio_and_muting.md`
- `knowledge/compliance/qa_checklist.md`
- `knowledge/compliance/yandex_moderation.md`
- `knowledge/mechanics/base_building.md`
- `knowledge/mechanics/card_synergy.md`
- `knowledge/mechanics/chain_reaction.md`
- `knowledge/mechanics/combo_juggling.md`
- `knowledge/mechanics/cooking_flow.md`
- `knowledge/mechanics/dash.md`
- `knowledge/mechanics/drone_swarm.md`
- `knowledge/mechanics/evidence_board.md`
- `knowledge/mechanics/fluid_buoyancy.md`
- `knowledge/mechanics/grappling_hook.md`
- `knowledge/mechanics/grid_building.md`
- `knowledge/mechanics/mining_drill.md`
- `knowledge/mechanics/parry.md`
- `knowledge/mechanics/physics_destruction.md`
- `knowledge/mechanics/ragdoll.md`
- `knowledge/mechanics/rhythm_sync.md`
- `knowledge/mechanics/stealth_detection.md`
- `knowledge/mechanics/time_rewind.md`
- `knowledge/mechanics/upgrade_choices.md`
- `knowledge/mechanics/vehicle_physics.md`
- `knowledge/mechanics/wave_survival.md`
- `knowledge/monetization/in_app_purchases.md`
- `knowledge/monetization/interstitial_best_practices.md`
- `knowledge/monetization/rewarded_ads_patterns.md`
- `knowledge/patterns/arena_combat_loop.md`
- `knowledge/patterns/builder_defense_loop.md`
- `knowledge/patterns/physics_arcade_loop.md`
- `knowledge/patterns/roguelike_loop.md`
- `knowledge/patterns/score_attack_loop.md`
- `knowledge/patterns/survivor_loop.md`
- `knowledge/pixijs/particle_systems.md`
- `knowledge/pixijs/sprite_batching.md`
- `knowledge/platform_builds/android_capacitor.md`
- `knowledge/playgama/ads_integration.md`
- `knowledge/playgama/auth_and_player.md`
- `knowledge/playgama/banners_and_layout.md`
- `knowledge/playgama/bridge_api_reference.md`
- `knowledge/playgama/game_ready_and_loading.md`
- `knowledge/playgama/lifecycle_and_orientation.md`
- `knowledge/playgama/platform_matrix.md`
- `knowledge/playgama/social_features.md`
- `knowledge/playgama/storage_and_cloud.md`
- `knowledge/references/brotato.md`
- `knowledge/references/dome_keeper.md`
- `knowledge/references/gladihoppers.md`
- `knowledge/references/slay_the_spire.md`
- `knowledge/references/toribash.md`
- `knowledge/references/vampire_survivors.md`
- `knowledge/threejs/adaptive_quality.md`
- `knowledge/threejs/mobile_shaders.md`
- `knowledge/threejs/performance_guide.md`
- `knowledge/threejs/physics_integration.md`
- `knowledge/threejs/rapier_vehicle_controller.md`
- `knowledge/threejs/vehicle_wheel_rig.md`
- `knowledge/ux/localization_system.md`
- `knowledge/ux/touch_controls.md`
- `knowledge/ux/ui_design_system.md`

---

## 12. DETAILED REFERENCE DOCUMENTS
For extended deep specifications, refer to the accompanying project documentation files:
- [Инструкция агенту (AGENTS.md)](./AGENTS.md)
- [Журнал разработки (DEVLOG.md)](./DEVLOG.md)
- [Changelog](./CHANGELOG.md)
- [Game Design Document](./GAME_DESIGN_DOCUMENT.md)
- [Gameplay Specification](./GAMEPLAY_SPECIFICATION.md)
- [Technical Specification](./TECHNICAL_SPECIFICATION.md)
- [Architecture Document](./ARCHITECTURE_DOCUMENT.md)
- [Playgama Integration](./PLAYGAMA_INTEGRATION.md)
- [Monetization Specification](./MONETIZATION.md)
- [Mobile Controls](./MOBILE_CONTROLS.md)
- [QA Plan](./QA_PLAN.md)
- [Обещание игроку (PLAYER_PROMISE.md)](./PLAYER_PROMISE.md)
- [Дизайн-ядро (DESIGN_NUCLEUS.md)](./DESIGN_NUCLEUS.md)
- [Плотность впечатлений (EXPERIENCE_DENSITY.md)](./EXPERIENCE_DENSITY.md)
- [Спецификация телеметрии (TELEMETRY_SPEC.md)](./TELEMETRY_SPEC.md)
- [Реестр допущений (ASSUMPTIONS.md)](./ASSUMPTIONS.md)
- [План валидации (VALIDATION_PLAN.md)](./VALIDATION_PLAN.md)
- [Журнал решений (DECISIONS.md)](./DECISIONS.md)
- [Человеческие ворота (HUMAN_GATES.md)](./HUMAN_GATES.md)
- [Game Skill Guidelines](./skills/GAME_SKILL.md)
- [Renderer Skill](./skills/RENDERER_SKILL.md)