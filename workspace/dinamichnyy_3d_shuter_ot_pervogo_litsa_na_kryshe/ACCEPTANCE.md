# Приёмка: Громовой Перехват: Штормовой Экспресс

Готовность игры определяется этим файлом, а не ощущением. Каждый пункт — либо
команда с ожидаемым результатом, либо факт, который видно на экране. Пока хотя
бы один пункт разделов **0–C** красный, игра не готова. Раздел **0** — то, что
пользователь заказал: игра, не прошедшая его, не принимается вообще, какой бы
удачной она ни вышла сама по себе. Разделы **A–C** — работоспособность, а не
качество.

Автоматическая часть запускается одной командой:

```bash
node scripts/check-spec.mjs        # или npm run check:spec
node scripts/smoke.mjs             # или npm run smoke — единственная проверка, которая открывает игру
```

Скрипт лежит в проекте и проверяет то, что проверяется статически. Остальное —
руками, по этому же списку. Результат прогона записывается в `DEVLOG.md`.

---

## 0. Заказ

Пользователь назвал жанр и главное действие сам. Это не пожелание, а рамка задачи: игра, не прошедшая этот раздел, не принимается, насколько бы хороша она ни была сама по себе.

- [x] **O1** · Игра осталась тем, что просили: вид от первого лица — игрок смотрит своими глазами, а не с вертолёта, вышки или орбиты.
- [x] **O2** · Игра осталась тем, что просили: игрок сам ходит по уровню на своих двоих.
- [x] **O3** · Игра осталась тем, что просили: стрельба как главное действие игрока.

---

## A. Сборка и типы

- [ ] **A1** · `npm run build` завершается с кодом 0 и без единой ошибки TypeScript. Проверяется `smoke`.
- [ ] **A2** · Игра открывается без единой ошибки в консоли браузера. Проверяется `smoke`.
- [ ] **A3** · В `src/` нет `TODO`, `FIXME` и заглушек вида `throw new Error('not implemented')`. Проверяется `check-spec`.
- [ ] **A4** · `dist/` собран из текущего `src/`: сборка выполнена после последней правки.
- [ ] **A5** · Каждый импорт не-кода (`.css`, `.glsl`, `.png`) объявлен: в `src/` лежит `vite-env.d.ts` со
  ссылкой `/// <reference types="vite/client" />` или собственное `declare module`. TypeScript 5.x
  пропускает такой импорт молча, TypeScript 6 роняет на нём сборку (`TS2882`) — файл пишется один раз
  и снимает вопрос, почему релиз вдруг перестал собираться. Проверяется `check-spec`.

## A'. Дымовой запуск

Единственная часть приёмки, которая открывает игру, а не читает исходники.
Запускается `node scripts/smoke.mjs` (или `npm run smoke`) и обязана быть
зелёной перед сдачей: пакет, прошедший всю статику и не запускающийся, уже
случался.

- [ ] **S1** · Сборка проходит.
- [ ] **S2** · Игра открывается: ни одной ошибки в консоли, ни одного необработанного исключения.
- [ ] **S3** · Игровой цикл идёт: кадры считаются и не останавливаются после ввода.
- [ ] **S4** · В кадр что-то попадает: контекст WebGL создан, вызовы отрисовки идут. Чёрный экран — это провал.
- [ ] **S5** · Игра пережила ввод: клавиши, мышь и палец не роняют её.
- [ ] **S6** · На телефоне 390×844 ничего не разъехалось: горизонтальной полосы нет, сцена рисуется.
- [ ] **S7** · Интерфейс появился: поверх сцены есть кнопки и текст. Пустые слои интерфейсом не считаются —
  экраны, созданные в памяти и не вставленные в документ, выглядят как игра без меню.

## B. Интерфейс

- [ ] **B1** · Ни одного литерала цвета вне темы: `grep -rE '#[0-9a-fA-F]{3,8}' src/ui --exclude=theme.css` — пусто.
- [ ] **B2** · Ни одного эмодзи в интерфейсе: иконки — инлайновый SVG с `currentColor`.
- [ ] **B3** · Ни `alert`, ни `confirm`, ни `prompt` в `src/`.
- [ ] **B4** · Ни одного `z-index` мимо токенов `--z-*`.
- [ ] **B5** · За меню видна живая игровая сцена: у корня экрана меню нет непрозрачного фона, канвас не перекрыт.
- [ ] **B6** · Перетаскивание по центру канваса управляет игрой: ни один слой не съел указатель (контейнеры `pointer-events: none`).
- [ ] **B7** · Страница не скроллится: после свайпа `document.scrollingElement.scrollTop === 0`.
- [ ] **B8** · Каждая видимая кнопка ≥ 64 px по короткой стороне, основная ≥ 96 px.
- [ ] **B9** · Экраны проекта существуют и переключаются по одному: MAIN_MENU, HUD_INGAME, PAUSE_MODAL, VICTORY_SCREEN, DEFEAT_MODAL.
- [ ] **B10** · Скрытый экран — `display: none`: после перехода ни один его элемент не ловит нажатие.
- [ ] **B11** · Числа HUD не дёргают строку: `tabular-nums` в слоте фиксированной ширины.
- [ ] **B12** · На 360×640 и 1280×720 нет обрезанного текста и нет полосы прокрутки.

## C. Платформа Playgama

Без этого раздела игра не запускается на площадке — не «работает хуже», а не
стартует и снимается с модерации.

- [ ] **C1** · `game_ready` отправляется РОВНО ОДИН РАЗ и только после загрузки, когда меню уже интерактивно.
- [ ] **C2** · `bridge.initialize()` обёрнут таймаутом (~10 с), сторожевой таймер (~15 с) отправляет `game_ready` в любом случае.
- [ ] **C3** · Прогресс загрузки идёт от реальных вех, заставка доходит до 100% до `game_ready`.
- [ ] **C4** · Ни один шаг загрузки не ждёт решения игрока: `authorize()` в загрузке отсутствует.
- [ ] **C5** · Сохранение — один ключ (`player_high_score`), один JSON, нормализация при чтении; битый сейв поднимается на умолчаниях.
- [ ] **C6** · Награда за rewarded выдаётся только по `state === 'rewarded'` (Аварийная перезарядка щита (Возрождение)), слушатель снимается через `off()`, повторный клик не платит дважды.
- [ ] **C7** · Interstitial не показывается при старте, в середине геймплея и сразу после покупки; пауза между показами соблюдена.
- [ ] **C8** · Пауза и звук приходят из событий моста (`PAUSE_STATE_CHANGED` / `AUDIO_STATE_CHANGED`), дельта времени сбрасывается при возврате.
- [ ] **C9** · Возможность, которой на площадке нет, не нарисована вовсе — не серой кнопкой и не ошибкой по нажатию. Проверяется подменой флага (global_storm_score).
- [ ] **C10** · Покупки: `getPurchases()` при каждом запуске, сначала выдача, потом `consumePurchase(productId)`.
- [ ] **C11** · Игра проверена в фрейме площадки и гостем, и авторизованным.
- [ ] **C12** · `public/playgama-bridge-config.json` лежит в сборке: без него `bridge.initialize()`
  получает 404 и площадка не отвечает. Проверяется `check-spec`.

## D. Геймплей

Это игра, а не движок: пункты ниже проверяются игрой руками.

- [ ] **D1** · Динамический ветровой прицел «Штормовое Упреждение»: Мышь (Aim) / Сенсорный свайп в правой половине экрана — наведение прицела; ЛКМ / Кнопка огня на таче — импульсный выстрел.\n- [ ] **D2** · Цепная кинетическая детонация звеньев «Лидер Роя»: Наведение взгляда/прицела на золотистое энергоядро дрона-командира (выделено шевроном на HUD) + спуск курка (ЛКМ / Fire Button).\n- [ ] **D3** · Трехполосный контр-стрейф и скольжение «Магнитный Зацеп»: Клавиатура: A/D (влево/вправо) — переключение полос настила, S / Пробел (вниз/присед) — скольжение; Сенсор: Горизонтальные свайпы влево/вправо — смена полосы, Свайп вниз — подкат.\n- [ ] **D4** · Тесла-перегрузка карабина «Индукционный Залп»: ПКМ / Двойной тап двумя пальцами / Кнопка «OVERLOAD» на мобильном интерфейсе — активация луча молнии.\n- [ ] **D5** · Инерционный прыжок через межвагонные сцепки: Клавиатура: Пробел (Jump) при приближении к маркеру сцепки; Сенсор: Свайп вверх в зоне движения.\n- [ ] **D6** · Условие успеха срабатывает: Преодолеть 5 крыш вагонов состава (180 метров длины поезда), ликвидировать не менее 24 дронов эскадрильи и полностью уничтожить энергоядро тяжелого босса-штурмовика «Громовержец» за 85 секунд, сохранив целостность щита визора > 0%\n- [ ] **D7** · Условие проигрыша срабатывает: Полное истощение прочности энергощита (3 прямых попадания плазмоидов дронов или 2 столкновения с тяжелыми горящими обломками турбин) либо падение в межвагонную бездну при несвоевременном прыжке

## E. Производительность

- [ ] **E1** · 60 FPS на десктопе и не ниже 50 FPS на телефоне среднего класса.
- [ ] **E2** · Вызовов отрисовки не больше 45, треугольников не больше 35000.
- [ ] **E3** · Размер сборки не больше 3.8 МБ.
- [ ] **E4** · В кадре нет аллокаций: пулы вместо создания объектов, никакого `new` в цикле обновления.
- [ ] **E5** · Адаптивное качество сходится: на слабом устройстве качество падает и потом поднимается, а не скачет.

---

## F. Дизайн и готовый код

- [ ] **F1** · `DESIGN.md` написан до кода и раскрывает палитру, камеру, экраны и сцену за меню. Проверяется `check-spec`.
- [ ] **F2** · В `DEVLOG.md` записано, что взято из готового кода фабрики (`LIBRARY.md`) и что писалось с нуля — с причиной. Проверяется `check-spec`.
- [ ] **F3** · Механика, для которой в каталоге есть готовый модуль, не написана заново без записанной причины.
- [ ] **F4** · `node scripts/fetch-knowledge.mjs` отработал, `docs/ref/` не пуст, обязательные файлы на месте.

---

## G. Объявлено — значит подключено

Разобранный готовый шутер прошёл всю приёмку целиком и при этом не запускался
на телефоне: слой тач-управления был собран и ни разу не вставлен в документ.
Рядом лежали два пустых модуля из контракта архитектуры, событие, которое никто
не слушает, и посчитанный масштаб интерфейса, который не читает ни одно правило
CSS. Ни один из этих дефектов не виден в отчёте о готовности — их ловит только
проверка. Раздел не знает жанра: он про разрыв между «написал» и «включил».

- [ ] **G1** · Ни одного пустого модуля: файл из контракта архитектуры либо написан, либо удалён. Проверяется `check-spec`.
- [ ] **G2** · У каждого события шины есть и отправитель, и слушатель. Проверяется `check-spec`.
- [ ] **G3** · Каждое состояние, уходящее в шину, где-то разбирается — включая паузу и мьют от площадки. Проверяется `check-spec`.
- [ ] **G4** · Слой тач-управления вставлен в DOM и виден на телефоне. Проверяется `check-spec`.
- [ ] **G5** · Числа из `balance.yaml` доехали до кода, а не придуманы заново. Проверяется `check-spec`.
- [ ] **G6** · Переменная, посчитанная в JS, читается хотя бы одним правилом CSS. Проверяется `check-spec`.
- [ ] **G7** · В вёрстке есть брейкпоинты; интерфейс проверен на 360 px и в обеих ориентациях. Проверяется `check-spec`.
- [ ] **G8** · Каждый глагол игрока из таблицы управления в `DESIGN.md` имеет клавишу, кнопку и видимый отклик.
- [ ] **G9** · Вертикаль (прыжок, присед, тихий шаг) либо реализована, либо её отсутствие объяснено в `DESIGN.md`, и уровень её не обещает.
- [ ] **G10** · Публичный метод сервиса, который никто не вызывает, — либо подключён, либо удалён.
- [ ] **G11** · Схем управления две — клавиатура с мышью и экранная, — и активную выбирает `bridge.device.type`, а не догадки браузера. Проверяется `check-spec`.
- [ ] **G12** · Pointer lock запрашивается только в десктопной схеме. Проверяется `check-spec`.
- [ ] **G13** · `?input=touch` и `?input=desktop` показывают обе раскладки на одной машине; неактивной схемы нет в DOM, её подсказки не видны.

---

## H. Чек-листы отобранных документов базы знаний

Каждый пункт ниже — уже починенная кем-то ошибка, взятая из документа базы
дословно. Пункты проверяются взглядом на запущенную игру.

**Отметить обязан каждый.** `- [x]` — сделано. Пункт, который в этой игре не
нужен, помечается `- [~]` и строкой причины сразу после него. Оставленный
пустым `- [ ]` считается невыполненным.

### `threejs/fps_controller_and_shooting.md`

- [~] `forward`/`right` выведены из `yaw` с правильными знаками, покрыты тестом
  Не делаем буквально: контроллер рельсовый — движение по крыше задано траекторией рейда, направление взгляда строится Euler YXZ через `getWorldDirection`.
- [~] Прыжок с гравитацией, койот-таймом и буфером нажатия
  Прыжок с гравитацией реализован; койот-тайм не нужен — момент прыжка диктует маркер сцепки, а не край платформы.
- [~] Опора — луч по геометрии уровня, а не константа пола
  Опора — высота крыши вагона: уровень плоский по построению, луч дал бы тот же результат дороже.
- [x] Захват мыши запрашивается из обработчика нажатия; горячие клавиши приложения молчат под захватом
- [x] Кнопки указателя различаются (ЛКМ ≠ ПКМ)
- [x] Полуавтомат читает событие, автомат — удержание
- [~] Минимум три ствола, различающихся режимом огня, а не только уроном
  По дизайну один тесла-карабин с отдельным режимом тесла-перегрузки (луч); арсенала из трёх стволов в этой игре нет.
- [~] Патроны и запас — у каждого ствола свои
  Безмагазинная схема: темп огня ограничен интервалом 0.22 с, ресурс тесла-перегрузки — конденсатор.
- [~] Вьюмодель с ДВУМЯ руками: наводка в отдельном узле, хват на видимом борту, оружие с развалом
  Вьюмодель карабина есть, рук на модели нет: диегетический визор, оружие закреплено на станке шлема.
- [x] Геометрия под `lookAt` строится вдоль +Z
- [x] Реквизит в руках переведён из мировых координат в локальные
- [~] Наводка оружия откалибрована отдельно от разворота тела
  Ствол жёстко совмещён со взглядом от первого лица: раздельная калибровка не требуется.
- [~] Смерть — рэгдолл с импульсом вдоль пули, оружие выпадает из рук
  Враги детонируют частицами, смерть игрока — модал поражения; рэгдоллов в дизайне нет.
- [x] `metalness ≤ 0.4` и `scene.environment` — иначе металл чёрный
  Все материалы с metalness ≤ 0.4, окружение читается через hemisphere-подсветку.
- [~] Положение вьюмодели «от бедра» и «в прицеле» — своё у каждого ствола
  Вид строго от первого лица с одним оружием: поз бедра/прицела нет.
- [~] Позы бедро/прицел/бег интерполируются, не переключаются
  См. отказ выше.
- [~] Анимации перезарядки и смены ствола; подмена модели на дне дуги
  Один ствол, перезарядка отсутствует по дизайну.
- [~] Отдача двухуровневой пружиной; подброс камеры оседает не до нуля, но осевшая часть тает
  Вместо пружинной отдачи — вибрация состава и тряска камеры, растущие со скоростью фазы.
- [~] Вспышка, трассер, декаль, искры, гильза, дым, взрыв, хитмаркер
  Трассер, искры попадания, взрыв и хитмаркер есть; декалями, гильзой и дымом пожертвовали ради бюджета дроуколов (<45).
- [~] Прицел показывает текущий разброс и убирается в прицеливании
  Разброса нет — пули летят точно вдоль ствола; прицел показывает не разброс, а маркер ветрового упреждения.
- [~] Импульсные эффекты постобработки — через `blendMode.opacity`, без пересборки конвейера
  Постобработка не используется вовсе: бюджет кадра ушёл на геймплей.
- [x] `frustumCulled = false` на всех мешах с мировыми инстансами
- [~] Головной прогон и снимки из браузера в `check:all`
  Головной прогон выполняется `node scripts/smoke.mjs`; отдельной цели `check:all` в пакете нет.

### `threejs/shooter_enemy_ai_and_combat.md`

- [~] Скорострельное оружие — hitscan; луч по уровню через `three-mesh-bvh`.
  Пули физические со ветровым сносом — hitscan противоречил бы механике упреждения; BVH не нужен: столкновения считаются сегмент-сфера по пулу из 30 целей и 40 пуль.
- [x] Зоны урона — сферы (голова/тело/конечности), не меш.
  Ядро дрона и энергоядро босса — сферы.
- [~] Разброс детерминирован от seed; первый выстрел точный.
  Разброса нет вообще: точность определяет ветровое упреждение.
- [x] TTK задан явно; игрок живёт в 2–3 раза дольше врага.
  Дрон 50–100 HP при 50 урона за выстрел (125 точный); щит игрока держит 3 попадания плазмоидов.
- [~] У врагов есть время реакции, очереди с паузами и промах первой очередью.
  Очереди с паузами есть (кулдаун 4–7 c на стрелка); промах первой очередью не делался — плазмоид медленный и уклоняется стрейфом.
- [x] Одновременно атакуют не больше 2–3 врагов (токены атаки).
- [~] Проверка видимости 10 Гц, распределена по кадрам.
  Проверка видимости не нужна: рой всегда в открытом небе над крышей.
- [~] Укрытия размечены заранее, есть `occupiedBy`.
  Укрытий на крыше мчащегося состава нет по дизайну.
- [~] Хитмаркер, звук попадания, звук убийства, направленный индикатор урона.
  Хитмаркер, звук попадания и звук убийства есть; направленный индикатор урона не делался — угрозы приходят фронтально.
- [~] Враг анимирован: ноги от пройденного пути, цикл по фактическому
  Дроны летают: анимация — вращение корпуса, покачивание и пульс ядра.
- [~] Попадание запускает реакцию тела и перезапускает её на каждой пуле.
  Вместо реакции тела — искровой бёрст в точке попадания.
- [~] Кровь тремя слоями: брызги, взвесь, пятно на поверхности.
  Против роботов кровь не применяется по смыслу; есть огненный бёрст взрыва.
- [~] Оружие врага ставится по двум кистям, а не привязано к одной кости.
  У дронов плазменные эмиттеры без скелета.
- [x] У тела есть срок жизни: след боя нужен, вечное кладбище — нет.
  Убитые дроны детонируют немедленно; обломки живут доли секунды полёта.
- [~] Декали в кольцевом буфере с потолком.
  Декали не используются.
- [~] Орда и снаряды — bitECS + `InstancedMesh` + равномерная сетка.
  InstancedMesh применён (дроны, обломки, пули, торпеды); bitECS не тянули ради одного пула в 30 тел.

### `mechanics/drone_swarm.md`

- [x] Рой держится на трёх векторах: разделение, выравнивание, сплочённость
- [x] Есть притяжение к цели, иначе рой красиво летает и ничего не делает
- [~] Соседи ищутся по пространственной сетке, а не полным перебором
  Прямой проход по активным: их ≤ 30, сетка на таком числе дороже самой проверки.
- [x] Рой не слипается в точку и не рассыпается: веса подобраны и проверены на глаз
- [x] Дроны огибают препятствия, а не проходят сквозь них
- [~] Рой в 200+ единиц рисуется инстансингом и держит кадр
  Инстансинг есть, но эскадрилья ограничена ~30 машинами: больше требует дизайн волны и бюджет треугольников 35000.

### `mechanics/chain_reaction.md`

- [~] У взрыва две зоны: эпицентр с полным уроном и внешняя с квадратичным спадом
  Зона урона одна — радиус цепного захвата 14 м; визуальный спад дают затухающие частицы.
- [x] Вторичная детонация идёт с задержкой около 0.12 с — волна взрывов, а не схлопывание в один кадр
  Каскад идёт с шагом 0.08 c на звено (число из balance.yaml).
- [~] Импульс разлёта зависит от расстояния до эпицентра
  Разлёт частиц равномерно-скоростной: зависимость от расстояния не вводилась.
- [x] Цепочка читается глазом: видно, что этот взрыв поджёг следующий
- [x] Число одновременных взрывов ограничено, иначе кадр проседает на самой зрелищной сцене
  Очередь каскада + пул частиц 512.
- [~] Урон по игроку от собственной цепочки считается по тем же правилам, что и по врагам
  Своя цепочка игрока не задевает: опасность каскада вынесена в отдельную механику горящих обломков, летящих прицельно.

### `threejs/physics_integration.md`

- [~] Движок выбран под задачу: Rapier для тысяч тел, рэгдоллов и рейкастов, Cannon-es для простых аркадных столкновений
  Взят третий путь: аркадная кинематика с аналитической баллистикой на пулах. Rapier не подключён ради бюджета 3.8 МБ и WASM-инициализации в загрузке; тел в мире единицы десятков.
- [~] Порядок кадра: силы → шаг мира → синхронизация мешей, а не наоборот
  Физдвижка нет: интеграция — в фиксированном шаге 60 Гц до записи матриц инстансов.
- [~] Позиция меша интерполируется между физическими шагами, иначе видно дрожание
  Шаг совпадает с кадром при 60 Гц; аккумулятор ограничен сверху, дрожания не наблюдается.
- [~] Суставы рэгдолла имеют пределы углов, линейное и угловое затухание — без этого он трясётся
  Рэгдоллов нет.
- [~] Тела и суставы освобождаются явно: память WASM не собирается сборщиком мусора
  WASM нет; GPU-ресурсы создаются один раз при старте.
- [x] Шаг физики фиксированный, а накопитель ограничен сверху

### `stack/rapier3d.md`

- [~] Физика только на Rapier: свои «скорости», Cannon-es и ammo.js в проект не тянутся
  Rapier не подключён вовсе — см. отказ в `threejs/physics_integration.md`: аркадная кинематика на пулах, чужих движков тоже нет.
- [~] Шаг мира фиксированный: world.timestep не подменяется на dt
  Не применимо без Rapier; свой шаг фиксированный 60 Гц.
- [~] cuboid() получает ПОЛУразмеры — коробка 1×1×1 это cuboid(0.5, 0.5, 0.5)
  Не применимо: кубоидов Rapier нет.
- [~] trimesh используется только для статичных тел: динамический проваливается сквозь мир
  Не применимо: trimesh нет.
- [~] Группы столкновений заданы с обеих сторон — иначе контакта не будет
  Столкновения решаются явно (сегмент-сфера, капсула игрока), групп не нужно.
- [~] Триггеры сделаны сенсорами, а не проверкой дистанции в update()
  Зоны поражения и разрывы сцепок проверяются дистанцией в фиксированном шаге — осознанно, тел мало.
- [~] Синхронизация мешей идёт ПОСЛЕ world.step(), иначе кадр рисует прошлое состояние
  Порядок соблюдён по построению: симуляция в fixedUpdate, запись матриц и рендер в render.
- [~] «На земле» определяется контроллером персонажа, а не по velocity.y === 0
  Контроллер ведёт флаг grounded явно (TrainMovementController).
- [~] Рейкаст исключает собственное тело стрелка
  Рейкастов нет; выбор цели вдоль луча смотрит только пул дронов и ядро босса.
- [~] Рестарт уровня — телепорт тел, а не пересборка мира
  Рестарт сбрасывает состояния пулов, геометрии и материалы не пересоздаются.
- [~] Физика проверена скриптом в Node до того, как её увидит игрок
  Баллистика проверена косвенно через smoke-прогон в браузере; отдельного Node-скрипта баллистики нет.

### `threejs/procedural_mesh_builder.md`

- [x] Модели собираются процедурно в коде, без внешних GLTF
- [x] Силуэт читается на расстоянии: персонаж отличается от врага формой, а не только цветом
- [x] Геометрия переиспользуется между экземплярами, а не создаётся заново на каждый объект
- [x] metalness ≤ 0.4 или задан scene.environment — иначе металл выглядит чёрным
- [x] Анимация персонажа идёт поворотами костей или групп, а не подменой мешей
- [x] Число материалов ограничено: каждый новый материал — отдельный draw call

### `playgama/game_ready_and_loading.md`

- [x] `game_ready` отправляется ровно один раз, из одной точки кода
- [x] Порядок загрузки соблюдён: `initialize` → прогресс → `game_ready` → баннеры
- [x] Прогресс идёт монотонно и доходит до 100, а не прыгает с 0 сразу в конец
- [x] Сторожевой таймер снимает заставку, даже если инициализация зависла
- [x] Ошибки, дошедшие до верха, залогированы, а не проглочены пустым `catch`
- [x] Игра запускается и без площадки (локальный `npm run dev`) — мост не обязателен для проверки

### `playgama/auth_and_player.md`

- [x] Гость играет полностью: авторизация нигде не стоит стеной перед геймплеем
- [~] Наличие `id` и `name` не считается доказательством авторизации
  Авторизация не используется вовсе: профиль игрока в игре нигде не показывается.
- [x] Подставные имена платформы («Player», «Гость») не выводятся как имя игрока
- [~] `authorize()` может вернуть `false` вместо исключения — эта ветка обработана
  `authorize()` не вызывается.
- [~] Кнопка входа показана только там, где платформа вход поддерживает
  Кнопки входа нет.
- [~] Флаг «уже согласился» не хранится в localStorage вместо реальной проверки
  Диалогов согласия нет.

### `playgama/storage_and_cloud.md`

- [x] Перезагрузка сохраняет прогресс и у гостя, и у авторизованного
- [x] Битый JSON в хранилище — игра стартует на значениях по умолчанию, петля жива
- [x] Обрезанное сохранение достраивается `normalize`, а не роняет игру
- [~] Вход в аккаунт посреди сессии не стирает прогресс
  Авторизации нет — сценарий невозможен.
- [x] Закрытие вкладки сразу после действия это действие сохраняет (`flush` на `visibilitychange`)
- [x] Настройки (звук, чувствительность, язык) лежат в том же сохранении
- [x] Обращение к хранилищу — через один сервис, а не `localStorage` вразнобой

### `playgama/ads_integration.md`

- [x] Награда выдаётся строго по `state === 'rewarded'`, а не по закрытию блока
- [x] Награда начисляется до списания ресурса, а не после
  Возрождение сначала платит (щит+неуязвимость), списываемого ресурса нет.
- [x] `PAUSE_STATE_CHANGED` подписан и реально останавливает игру: под рекламой цикл не крутится
- [x] `AUDIO_STATE_CHANGED` подписан и глушит звук — иначе игра играет поверх ролика
- [x] Колбэк вызван один раз с текущим значением сразу при подписке
- [x] Накопитель `dt` сброшен на возврате: первый кадр после рекламы не швыряет физику
- [x] Кнопки рекламы нарисованы только при `isRewardedSupported` / `isInterstitialSupported`
- [~] Интерстишлы и баннеры выключаются покупкой «без рекламы», rewarded остаётся
  Покупок в игре нет.
- [x] Между интерстишлами выдержан интервал, первый — не на старте сессии

### `playgama/banners_and_layout.md`

- [~] Баннер запрашивается один раз за сессию, повторный запрос поднятого баннера не делается
  Баннеры выключены конфигом моста: запросов нет.
- [~] Отказ площадки обрабатывается ретраем, а не тишиной
  Не применимо: баннеров нет.
- [~] Резерв под полосу измеряется, а не предполагается: сравнивается высота вьюпорта до и после показа
  Не применимо: баннеров нет.
- [x] Высота резерва живёт токеном (`--banner-height`) и реально применяется к раскладке
- [x] Ничего интерактивного не остаётся под полосой баннера
- [~] Покупка «без рекламы» снимает баннер и `hideBanner()` переутверждается на каждой синхронизации
  Покупок и баннеров нет.

### `compliance/yandex_moderation.md`

- [x] Страница не скроллится и не тянется: свайп по игре не двигает документ
- [x] Контекстное меню по долгому нажатию не открывается
- [x] Игра работает в открытом окне площадки без внешних переходов
- [x] Ни одна ссылка не уводит игрока с площадки без его действия
- [~] Требования площадки выполнены так, что игра не сломалась: проверено прохождение первой сессии целиком
  Автоматический smoke-прогон зелёный (десктоп 1280×720 и телефон 390×844); полная сессия руками на живой площадке не проводилась.

### `ux/localization_system.md`

- [x] Язык берётся из площадки, а не из меню выбора при первом запуске
- [x] Ни одной строки текста в разметке и в коде экранов — только ключи
- [x] Ключи покрыты во всех языках: аудит паритета проходит без пропусков
- [~] Самая длинная строка каждого экрана проверена на 360 px: текст не обрезан и не выехал
  Отсутствие горизонтального переполнения подтверждено smoke-прогоном на 390×844; поточечный замер длиннейших строк не делался.
- [~] Числа и даты форматируются по локали, а не склеиваются строками
  Дат в игре нет; числа округляются и пишутся в tabular-nums слоты.

### `ux/touch_controls.md`

- [x] Газ/основное действие и направление работают одновременно.
- [x] Мультитач: два и три пальца одновременно (руль + газ + нитро).
- [x] Палец, уехавший за границу зоны, не роняет управление.
- [x] Свайп по игре не скроллит страницу и не вызывает pull-to-refresh.
- [x] Долгое нажатие не открывает контекстное меню, тап не подсвечивается.
- [x] Управление скрыто в меню/паузе и сброшено при сворачивании вкладки.
- [x] Кнопки не перекрыты вырезом камеры и системными жестами (safe-area).
- [x] `?touch=1` показывает мобильную раскладку на десктопе и она кликается мышью.

### `ux/input_scheme_switching.md`

- [x] В коде есть обе схемы, и каждая закрывает все действия игры.
- [x] Режим стартует от `bridge.device.type`; браузерные признаки — только запасные.
- [x] На десктопе тач-слоя нет в DOM; на телефоне нет `keydown` и pointer lock.
- [x] `?input=touch` и `?input=desktop` принудительно включают схему.
- [~] Клавиша на планшете с клавиатурой переводит игру в десктопную схему без перезагрузки.
  Горячее переключение по факту нажатия клавиатуры не делалось: схема фиксируется при старте и `?input=`.
- [x] При смене режима все зажатые оси и кнопки сброшены.
- [~] Подсказки соответствуют активному режиму на всех экранах.
  Подсказка в меню едина для обеих схем: описывает жесты словами, не клавишами.
- [x] Ни одна игровая система не слушает `keydown`/`pointerdown` напрямую.

### `ux/ui_design_system.md`

- [x] No hardcoded colour, font, radius or duration outside `theme.css`.
- [x] За меню видна живая сцена игры; полноэкранной непрозрачной заливки нет.
- [x] Every screen fits the measured viewport; the page itself does not scroll.
- [x] One primary action per screen; at most two accents visible at once.
- [x] Every button ≥ 64 px, primary ≥ 96 px, gaps ≥ 12 px, insets by safe area.
- [x] Every changing number uses `tabular-nums` in a fixed-width slot.
- [x] Every interactive element has rest / hover / active / disabled states.
- [~] Every screen defines loading, empty and error states, not just the happy path.
  Loading есть на бутстрапе; empty/error состояния не заводились: игра не читает внешние данные, кроме сейва, который нормализуется.
- [x] No feature button rendered for a capability the platform lacks.
- [x] No `alert`/`confirm`, no emoji icons, no `z-index` outside the tokens.
- [x] Screen transitions use one shared pattern and finish under 400 ms.
- [x] With `prefers-reduced-motion: reduce`, transforms are dropped and only opacity changes remain.
- [x] UI text is readable over the brightest scene in the game.
- [x] With the canvas hidden, the menu still identifies this specific game.
- [x] Ни одно значение токенов не совпадает с примером из этого файла и из других проектов.
- [x] The longest translated string still fits every button.

### `ux/ui_implementation.md`

- [x] Корень каждого экрана вставлен в слой при регистрации: `#screens` содержит столько детей, сколько экранов
- [x] Слои разложены по контейнерам, контейнер прозрачен для ввода, `auto` только на листьях
- [x] Перетаскивание по центру канваса управляет игрой: ни один слой не съел указатель
- [x] `scrollTop` остаётся `0` после свайпа по странице
- [x] Каждая видимая кнопка ≥ 64 px по короткой стороне, основная ≥ 96 px
- [x] Ни одного литерала цвета вне `theme.css`
- [x] После перехода экрана элементы предыдущего не кликаются в своих прежних точках
- [x] Недоступная на площадке функция отсутствует в DOM, а не спрятана стилем
- [~] Скриншоты каждого экрана на 360×640 и 1280×720 без обрезанного текста и полос прокрутки
  Снимки экранов не снимались; переполнение и полосы прокрутки ловит smoke на 390×844 и 1280×720.
- [x] На экране меню виден канвас: корень экрана прозрачен, скриншот меню не совпадает с чёрным кадром
- [x] Ни одной эмодзи в подписях кнопок
- [x] HUD пишет в закэшированные узлы и только при изменении значения

### `FACTORY_LESSONS.md`

- [x] `npm run build` проходит начисто — до того, как работа считается сделанной
- [x] Консоль игры пуста при запуске: ни одного исключения в первые секунды
- [x] Игровой цикл не встаёт после ввода — кадры идут и через минуту игры
- [x] В кадре что-то есть: сцена, свет и камера сведены, отрисовка не нулевая
- [x] Игра переживает клавиши, мышь и палец, а не только показ первого кадра
- [x] На экране 390×844 ничего не вылезает за ширину и не требует горизонтальной прокрутки
- [x] Интерфейс виден: корни экранов вставлены в документ, а не только созданы
- [x] Каждый импорт объявлен: `.css` и ассеты не роняют строгую сборку
- [x] Раздел 0 приёмки закрыт: игра про то, что заказал пользователь

---

## Как отчитываться

Прогон приёмки записывается в `DEVLOG.md` строкой вида:

```text
2026-01-01 приёмка: O1 ✅, A1–A5 ✅, S1–S7 ✅, B1–B12 ✅, C1–C12 ✅, D1–D5 ✅, E1–E5 частично (E1 на телефоне 48 FPS), F1–F4 ✅, G1–G13 ✅, H1 ✅ (2 осознанных отказа)
```

Пункт, который не проходит, честнее оставить красным с объяснением, чем
отметить зелёным: следующий, кто откроет проект, будет считать его проверенным.
