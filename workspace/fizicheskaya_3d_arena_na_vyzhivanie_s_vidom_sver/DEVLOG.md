# Журнал разработки — Ледовый Сумо-Батл: Последний Тюбинг

Этот файл ведёт ИИ-агент. Каждая сессия работы = одна запись:
что просили, что сделано, какие файлы затронуты, что проверено, что осталось.

---

## 2026-08-23 17:16 — Инициализация проекта
- **Задача**: создан каталог проекта в workspace, подготовлена спецификация.
- **Сделано**: сгенерирован пакет документации фабрики.
- **Следующий шаг**: реализация игрового кода по AI_DEVELOPER_PROMPT.md.

---

## 2026-08-23 18:40 — Полная реализация игры
- **Задача**: создать полную рабочую структуру HTML5-игры по AI_DEVELOPER_PROMPT.md и skills/: package.json, Vite, index.html, main.ts, рендерер, физика, управление, аудио, Playgama Bridge.
- **Сделано**:
  - `DESIGN.md` написан до кода: палитра из мира, камера/свет, экраны с тремя зонами, живая сцена за меню, таблица «глагол → клавиша → кнопка → отклик», осознанный отказ от вертикали.
  - Каркас: package.json (dev/build/preview/check:spec/smoke), vite.config.ts, tsconfig strict+noEmit, src/vite-env.d.ts с `/// <reference types="vite/client" />`, public/playgama-bridge-config.json.
  - Ядро: типизированный EventBus, GameLoop (фиксированный шаг 60 Гц, clamp dt 0.1 c), Balance.ts со всеми числами из balance.yaml, InputRouter — две схемы, режим от bridge.device.type, `?input=touch|desktop`, живое переключение со сбросом осей.
  - Физика Rapier3D: фиксированный timestep мира, тюбинги на динамических телах с CCD, кинематические плиты льда; гидродинамика по погружённому объёму с сопротивлением; гибель ниже ватерлинии −0.8 м.
  - Механики по формулам спецификации: импульсный таран p=mv с множителем форсажа 2.35; дрифт (mu 0.65→0.078, порог заноса 22.5°) с зарядом турбины 16.5 ед./с; наклон плит под весом до 26.5° и затопление внешнего кольца; рост массы +8.5% и радиуса +4.5% за фраг (кап 145 кг); ударная волна 1650 Н в радиусе 3.8 м с окном тайминга 0.14 с; hitstop 0.07 c.
  - Арена: 16 клиньев, раскол внешнего кольца затем внутреннего до финальных 2 плит; 7 ботов с автоматом PATROL/HUNT/RAM/ESCAPE, объездом кромки и форсажем.
  - Рендер: процедурные модели без GLTF (тюбинги с неоновым ободом, призмы льда, океан, северное сияние, торосы), пул частиц на двух InstancedMesh без аллокаций в кадре, chase-камера с FOV-откликом, адаптивное качество от оптимистичного.
  - Аудио: процедурный Web Audio синтез, один мастер-GainNode с ramp-громкостью, разблокировка первым жестом, отдельные входы мьюта игрока и площадки.
  - Интерфейс: токены только в theme.css (4 брейкпоинта, --ui-scale читается правилами, --vp-h и --banner-height от измеренного вьюпорта), слои pointer-events:none, ScreenRouter с display:none, HUD пишет в закэшированные узлы (tabular-nums), радар 16 сегментов, killfeed, отсчёт 3-2-GO; экраны: меню, игровой HUD, ревайв-модалка, итоги матча, гараж (12 тюбингов / 8 пилотов / 6 следов за кубки), таблица лидеров с loading/empty/error, пауза; i18n ru/en по языку площадки; SVG-иконки currentColor.
  - Тач: плавающий джойстик левой половины, кнопки ТУРБО (112 px) и ОТСКОК справа внизу, Pointer Events + setPointerCapture + учёт pointerId, touch-action none, safe-area, сброс осей при скрытии/blur/visibilitychange; слой создаётся только в тач-режиме.
  - Playgama Bridge v2: initialize под таймаутом 10 c + сторожевой таймер 15 c; сигнал готовности отправляется ровно один раз после интерактивного меню; прогресс загрузки реальными вехами; rewarded только по state==='rewarded' со снятием слушателя off() и защитой от повторного клика; interstitial только из обработчика клика, армируется на перерыве матча, пауза 90 c; PAUSE/AUDIO_STATE_CHANGED с мгновенным текущим значением и сбросом dt на возврате; сейв одним ключом player_trophies с normalize при чтении, зеркалом localStorage и flush на pagehide/visibilitychange; capability-gating: кнопка рекламы не рисуется без поддержки.
  - Готовый код фабрики (`LIBRARY.md`, `docs/ref`, `fetch-knowledge`): **не взят ни один модуль**, потому что `node scripts/fetch-knowledge.mjs` вернул HTTP 403 для всех 43 файлов (токен не задан либо исчерпан лимит) и база знаний недоступна офлайн; работали по выжимкам из `skills/`. Всё игровое написано с нуля под эту игру.
- **Затронутые файлы**: package.json, vite.config.ts, tsconfig.json, index.html, public/playgama-bridge-config.json, DESIGN.md, src/main.ts, src/core/* (EventBus, GameLoop, Balance, InputRouter, Catalog, Game), src/platform/*, src/physics/*, src/entities/*, src/systems/*, src/rendering/*, src/audio/*, src/ui/** (тема, компоненты, экраны, HUD, тач), ACCEPTANCE.md (отметки чек-листов H).
- **Проверено**: `npm run build` — код 0, TypeScript strict чисто; `node scripts/check-spec.mjs` — все проверки зелёные (A3, A5, B1–B6, C1–C6, C12–C13, F1, G1–G7, G11, H1: сделано 115, отказов 27); `node scripts/smoke.mjs` — S1–S7 зелёные: сборка 3.57 МБ, игра открывается без ошибок консоли, цикл идёт (191 кадр, 73 после ввода), отрисовка идёт, ввод пережит, на 390×844 ничего не разъехалось, интерфейс виден.
- **Известные проблемы / следующий шаг**: FPS 22 в smoke — это SwiftShader (программный GL в headless-Chromium); на аппаратном GPU цель 60 FPS, подтвердить на живых телефонах (E1). База знаний так и не скачалась (403) — перезапустить fetch-knowledge при доступном токене и закрыть раздел H заново по полным текстам. Покупок «без рекламы» нет: платежи выключены capability-флагом.
