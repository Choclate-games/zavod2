# FINAL AI DEVELOPER PROMPT: ГОНКУ СТИЛЕ НФС PLAYGAMA 🎮⚡

> **INSTRUCTION FOR AI CODING AGENT**:
> You are the **Lead Game Developer & Systems Architect**. Your task is to build and deliver the complete, production-ready, fully playable HTML5/WebGL game described in this specification from start to finish.
> Follow the technical architecture, physics specifications, Playgama Bridge integration, and mobile ergonomics strictly.
> Do NOT omit systems, use fake placeholder stubs, or leave TODOs. The end result must satisfy every single item in the **Definition of Done**.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: Гонку стиле нфс playgama
- **Project Slug**: `gonku-stile-nfs-playgama`
- **Genre**: 3D Экшен-Дрифт / Выживание на Арене (Аркадный Авто-Рогалик)
- **Target Platform**: Playgama Bridge (Yandex Games / VK / Web)
- **Orientation**: Landscape
- **Target Audience**: Любители экшен-гонок, зомби-апокалипсиса и аркадного дрифта на Яндекс Играх и мобильных платформах.
- **Player Fantasy**: Управляйте боевым броневиком в постапокалиптической пустоши, входите в управляемый дрифт в толпе зомби, накапливайте нитро-ярость и улучшайте модули вооружения.
- **Core Hook**: Физический дрифт броневика с шипами, таран сотен зомби, нитро-ускорения и слоу-мо финишеры боссов.
- **Session Model**: Короткие сессии по 5-8 минут с высоким удержанием и мета-прокачкой.


---

## 1b. РАМКА ПРОЕКТА: ЧТО ЭТО ЗА ИГРА И ЧЕМ ОНА НЕ ЯВЛЯЕТСЯ


**Направление**: Ночной Перехват: Heat Run

**Почему выбрано именно оно**: Вариант D-01 дает стопроцентное попадание в ДНК культовой серии Need for Speed (атмосфера Most Wanted / Hot Pursuit): адреналиновый конфликт с полицией, тактическое использование города (разрушаемые конструкции Pursuit Breakers) и драматичные аварии. Для браузера и мобильных устройств это идеальный формат: сессия длится 90-120 секунд, управление интуитивно раскладывается на две половины экрана (руление/дрифт + свайп нитро), а зрелищность мигалок, сирен и разлетающихся барьеров захватывает внимание игрока в первую же секунду без необходимости долгого обучения.

**Узнаваемая сцена**: Темный тюнингованный маслкар с неоновой подсветкой днища входит в дрифт на мокром перекрестке ночного города, таранит опору гигантского рекламного щита и срезает через заправку, в то время как за ним под вой сирен обрушивающаяся конструкция сминает три полицейских крузера в снопах искр.

**Без чего проект перестаёт быть собой** (реализовать обязательно):
- Активная полиция с системой уровней розыска (Heat Level), таранящая игрока и перекрывающая дороги
- Интерактивные разрушаемые ловушки на карте (Pursuit Breakers: рекламные щиты, строительные леса, бензоколонки), уничтожающие преследователей при столкновении
- Механика мгновенного закиси азота (Nitro Burst) с визуальным эффектом туннельного зрения и ускорения физического тела машины
- Управление, адаптированное под мгновенную реакцию на тачскрине (двухпальцевая схема: поворот/дрифт + свайп нитро без виртуальных стиков)

**ЗАПРЕЩЕНО ВВОДИТЬ** — даже если спецификация где-то умалчивает и даже если «так принято в жанре»:
- Кольцевая гонка по кругу с безликими ботами без полиции и драмы
- Бесконечный раннер с перестроением по трем фиксированным полосам
- Гиперказуальная аркада с управлением одним пальцем и автоповоротами
- Платная прокачка деталей через всплывающие окна с тремя карточками
- Медленный симулятор с реалистичным сцеплением и штрафами за каждое касание бордюра
- волны врагов на арене
- окно выбора из трёх карт апгрейда
- универсальная связка «дэш + парирование + удар»
- заимствование Vampire Survivors / Brotato как основы
- джойстик слева + кнопки атаки справа как раскладка по умолчанию
- серые кубы вместо стилизованной геометрии
- мета-прогрессия «золото за забег -> постоянные апгрейды» без связи с темой

Если по ходу реализации кажется, что без запрещённого элемента игра не работает — это находка для `DEVLOG.md`, а не разрешение его добавить.

**Рассмотренные и отклонённые направления** (не возвращай их через механики):
- D-02 (Тогу-Дрифт) отвергнут из-за высокого порога входа: точное дозирование угла заноса на тачскрине мобильных устройств приводит к частым ошибкам и раздражению казуальной браузерной аудитории, а отсутствие прямого соперничества снижает динамику.
- D-03 (Шашки на Хайвее) отвергнут из-за риска скатиться в примитивный гиперказуальный раннер по прямой: в нем отсутствует фирменная глубина NFS (свобода выбора маршрута, срезы, повороты, тактическая борьба с патрулями).


---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite 5.x
- **Renderer**: **THREEJS** (^0.185.1)
  - *Selection Rationale*: Three.js + Rapier3D: пространственный геймплей, физика и освещение. Перспективная камера.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat ^0.20.0)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Howler.js (^2.2.4) с WebAudio API
- **State Management**: Custom TinyEventBus & Reactive GameStore
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 75
- **Max Triangles / Active Sprites**: < 40000
- **Max Bundle Size**: < 4.2 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Управляемый дрифт на арене -> Таран зомби и сбор шестеренок -> Выбор 1 из 3 рогалик-модулей -> Битва с боссом-мутантом -> Мета-прокачка гаража.
```

**Фирменный момент**: Вход в занос под углом 60 градусов сквозь толпу из сотни зомби на скорости 110 км/ч с последующим таранным ударом по опоре водонапорной башни, обрушивающейся на преследующие броневики спецназа в кинематографичном слоу-мо под рев V8 и тяжелый фонк-бит.

**Чем петля отличается от соседей по жанру**: Вместо пассивного уклонения от мобов игрок использует сам дрифт как генератор ресурса агрессии: глубокий занос заряжает Нитро-Ярость, превращая броневик в неуязвимый таранный снаряд, а разрушаемые элементы окружения (Pursuit Breakers) служат локальным тактическим супероружием.

**Шаблон жанра, который НЕ реализуем**: Отказ от аркадного top-down управления с мгновенным разворотом машины без инерции (twin-stick shooter template). В нашей игре вектор движения строго подчинен физической инерции массы Rapier3D: броневик невозможно мгновенно повернуть на месте, атака строится на контроле траектории скольжения, переносе центра тяжести и боковом смещении кузова.

**Схема петли**:
```text
+-----------------------------------------------------------------------+
|                           SAFEHOUSE / GARAGE                          |
|  [Мета-Тюнинг Кузова] -> [Выбор Броневика] -> [Контракт Района Города] |
+-----------------------------------------------------------------------+
                                   |  СТАРТ ЗАЕЗДА (Heat 1)
                                   v
+-----------------------------------------------------------------------+
|                         DRIFT & COMBAT ARENA                          |
|                                                                       |
|   +-------------------> [Инерционный Дрифт] <-------------------+     |
|   |                     (Угол 25°-70°, сбор шестеренок)          |     |
|   |                                |                             |     |
|   |                                v [Зарядка x3.2]              |     |
|   |                     [Шкала Нитро-Ярости: 100%]               |     |
|   |                                |                             |     |
|   |                                v [Активация Shift/Tap]       |     |
|   |                     [Нитро-Таранный Прорыв]                  |     |
|   |                     (140 км/ч, Shockwave, неуязвимость)      |     |
|   |                                |                             |     |
|   |              +-----------------+-----------------+           |     |
|   |              v                                   v           |     |
|   |   [Удар в Pursuit Breaker]              [Распил Орды Пилами] |     |
|   |   (Обрушение моста/вышки)               (Сбор шестеренок)    |     |
|   |              |                                   |           |     |
|   |              +-----------------+-----------------+           |     |
|   |                                |                             |     |
|   |                                v                             |     |
|   |                     [Кинетическая Эволюция]                  |     |
|   |                     (Выбор 1 из 3 модулей оружия)            |     |
|   |                                |                             |     |
|   +--------------------------------+                             |     |
|                                                                  |     |
|                   [Эскалация Розыска: Heat 1 -> 5]               |     |
|                                    |                             |     |
|                                    v [10-я минута]               |     |
|                     [Босс-Мутант + Rhino Cops]                   |     |
|                                    |                             |     |
|                                    v [HP Босса < 15%]            |     |
|                     [Ram-Breaker Slomo Finisher]                 |     |
+-----------------------------------------------------------------------+
                                   |  ЭВАКУАЦИЯ В УБЕЖИЩЕ
                                   v
+-----------------------------------------------------------------------+
|                   MOST WANTED REPUTATION & BLACKLIST                  |
|       (Рост ранга, новые обвесы, моторы V8, суперкары и районы)       |
+-----------------------------------------------------------------------+
```

**Микро-петля (посекундно)**:
- **Срыв задней оси в занос перед скоплением зомби** (0.8 с): игрок — Поворот руля с кратковременным зажатием ручного тормоза [Пробел] / резкий свайп; игра — Броневик срывается в скольжение, резина дымит, шкала Нитро-Ярости начинает стремительно заполняться с множителем x2.2; решение — Удерживать глубокий угол заноса ради максимального заряда нитро или выпрямить руль для сохранения линейной скорости
- **Контактный распил боковыми модулями в скольжении** (1.2 с): игрок — Корректировка угла заноса подруливанием в сторону внешнего радиуса толпы; игра — Шипованные диски и пилы кромсают зомби по дуге, из тел вылетают шестеренки, счетчик комбо растет; решение — Срезать глубже в плотную толпу ради очков или держаться края, избегая замедления ниже 28 км/ч
- **Выброс Нитро-Тарана сквозь блокаду** (1.85 с): игрок — Нажатие кнопки Нитро-Инжектора [Shift] при 100% шкале ярости; игра — Взрывной импульс 45000 Н, машина разгоняется до 140 км/ч с радиальным размытием экрана, раскидывая элитных мутантов физической ударной волной; решение — Направить таран в толпу мутантов для сбора ресурсов или пробить заграждение полицейских перехватчиков
- **Вакуумный сбор шестеренок и выбор траектории выхода** (0.7 с): игрок — Стабилизация машины и сбор выпавших шестеренок магнитным полем капота; игра — Шестеренки притягиваются к броневику, заполняя шкалу эволюции модуля, камера возвращается на стандартный угол; решение — Начать новый дрифт в следующий карман арены или направиться к опоре Pursuit Breaker

**Мезо-петля (этап)**:
- **Фаза 1: Разведка и Сбор Билда (Heat 1–2, 0:00–1:30)** (90 с): игрок — Агрессивный дрифт по периметру, сбивание легких зомби и деревянных барьеров; игра — Спавн редких патрульных седанов, быстрый набор первых 3 уровней модулей (пилы, турель, огнемет); решение — Фокусироваться на модулях кругового урона или инвестировать в скорость накопления нитро
- **Фаза 2: Эскалация и Прорыв Блокад (Heat 3, 1:30–3:30)** (120 с): игрок — Использование узких переулков и активация Pursuit Breakers против скоростных перехватчиков; игра — Полиция перекрывает широкие магистрали машинами борт-к-борту (Roadblocks), на арену выходят плеватели кислотой; решение — Идти на лобовой таран блокпоста с нитро или срезать через разрушаемый забор промзоны
- **Фаза 3: Охота Тяжелой Спецтехники (Heat 4–5, 3:30–5:30)** (120 с): игрок — Маневрирование на пределе сцепления, уклонение от лобовых атак броневиков Rhino и лент с шипами; игра — Вертолет слепит прожектором, тяжелые Rhino таранят на встречных курсах, спавнятся мутанты-титаны; решение — Беречь нитро исключительно для уклонения от Rhino или сжигать его для зачистки орд вокруг опор ловушек
- **Фаза 4: Дуэль с Боссом-Мутантом и Эвакуация (5:30–7:00)** (90 с): игрок — Разрушение бронепластин босса модулями и проведение финального кинематографичного тарана Ram-Breaker; игра — Босс оглушается при HP < 15%, включается слоу-мо финишер (0.1x timescale), открываются ворота секретного Убежища (Safehouse); решение — Мгновенно эвакуироваться или рискнуть добить оставшихся элитных врагов ради дополнительной Репутации

**Макро-петля (забег)**:
- **Анализ Ущерба и Репутации в Сводке Заезда** (25 с): игрок — Просмотр итогов заезда: нанесенный ущерб городу (Cost to State), сбитые зомби и уничтоженные патрули; игра — Начисление очков Most Wanted Репутации, рост позиции в рейтинге Черного Списка города; решение — Инвестировать очки в усиление прочности кузова или разблокировку нового класса машин
- **Тюнинг и Модернизация Броневика в Гараже** (45 с): игрок — Установка усиленных кенгурятников, расширителей колесной базы для резкого заноса и неоновой подсветки; игра — Обновление физических параметров машины (момент инерции, множитель урона тарана, объем баллона нитро); решение — Собрать тяжелый бронированный маслкар для тарана или легкий японский дрифт-кар для сверхбыстрого накопления ярости
- **Выбор Района и Контракта на Карте Мегаполиса** (20 с): игрок — Выбор следующей зоны вылазки (Индустриальная промзона, Доки, Небоскребы) с уникальными модификаторами; игра — Отображение наград за контракт, сложности спавна копов и расположения секретных убежищ; решение — Пойти в высокорисковый район с модификатором 'Шипованные заслоны' ради x2 чертежей или фармить безопасную промзону

**Кривая напряжения**: Волнообразно нарастающая синусоида с резкими пиками адреналина. 0:00–1:30 (Низкое напряжение) — ощущение превосходства, игрок крутит легкие 'пончики' в толпе зомби. 1:30–3:30 (Среднее напряжение) — появление быстрых копов и блокад сжимает безопасное пространство арены. 3:30–5:30 (Экстремальное напряжение) — лобовые атаки броневиков Rhino и вертолет создают постоянную угрозу мгновенной смерти при потере скорости. 5:30–7:00 (Кульминация) — босс-файт требует ювелирного балансирования между дрифтом для нитро и уворотом от спецтехники, завершаясь катарсическим слоу-мо финишером и спадом напряжения при прорыве в безопасную зону Safehouse.

**Формулы ядра (реализуй буквально)**:
- `Урон тарана броневика: Ram_Damage = BaseMass_kg * (CurrentVelocity_kmh / 10)^1.85 * NitroMultiplier * BumperSpikeBonus`
- `Генерация Нитро-Ярости в секунду: Rage_Gain_Sec = 18.5 * (Clamp(SlipAngle_deg, 22.5, 75.0) / 45.0) * (CurrentSpeed_kmh / 60.0) * ProximityEnemyMultiplier`
- `Множитель плотности спавна врагов от розыска: EnemyDensity = BaseSpawnRate * (1.0 + HeatLevel^1.45) + (ElapsedTime_sec / 90.0)^1.2`
- `Очки Репутации за заезд: Rep_Earned = (ZombiesCrushed * 3) + (CopVehiclesDestroyed * 75) + (CostToState_USD / 500) * BossFinisherBonusMultiplier`

**Прогрессия внутри забега**:
- Минута 0–2: Установка 2 базовых модулей (например, Дисковые пилы на ступицах + Крышный пулемет 7.62-мм), броневик легко пробивает скопления зомби до 20 тел.
- Минута 2–5: Эволюция модулей до 3-го ранга (Пилы получают плазменный нагрев, пулемет модифицируется в спаренную автопушку 20-мм), открытие синергии 'Огненный След + Напалмовый Дрифт'.
- Минута 5–8: Получение легендарного прототипа (например, 'Электромагнитный Кенгурятник Тесла', бьющий дуговыми молниями при каждом таране на скорости > 80 км/ч), максимизация объема баллона нитро до 2 зарядов.

**Мета-прогрессия**:
- Ранг в Черном Списке (Blacklist #15 -> #1): Открывает новые классы авто (Маслкар 'Джаггернаут', Дрифт-купе 'Сакура R34', Пустынный Рейд-Трак 'Апокалипсис').
- Гаражные улучшения каркаса: Перманентная прибавка к базовой прочности кузова (+5% за уровень, до +50%), магнитному радиусу сбора шестеренок (от 4.5 м до 9.0 м) и охлаждению инжектора нитро.
- Кастомизация и Визуальный Тюнинг: Неоновые трубки под днищем (меняют цвет искр дрифта), кастомные выхлопные патрубки с цветным пламенем, уникальные граффити-винилы Most Wanted, дающие +10% к приросту Репутации в профильных районах.

### Инерционный Дрифт-Накопитель Нитро-Ярости (Inertial Drift Rage Accumulator) (CORE)
- **Category**: Двигатель геймплея и ключевой источник боевого ресурса
- **Description**: В отличие от классических авто-шутеров с плоским рулением, броневик атакует именно боковым смещением массы через физику твердых тел Rapier3D, превращая дрифт из маневра уклонения в главное ударное оружие.
- **Player Input**: Клавиатура: [A]/[D] или [Стрелки] — подруливание, [Пробел] — импульсный ручной тормоз для срыва задней оси. Тач: горизонтальный виртуальный джойстик + динамический жест свайпа вниз для срыва в занос.
- **Hit & Sensory Feedback**: Визуал/VFX: Густые клубы черного жженого корда из-под задних покрышек, процедурные следы протектора на асфальте с кровавыми брызгами, сноп оранжево-синих искр от металлических дисков при угле заноса > 45°.; Звук (Web Audio): Синтезированный визг резины (Bandpass фильтр 1.8-3.2 кГц с модуляцией частоты от угла заноса), перегруженный низкочастотный рокот компрессора V8, нарастающий свист турбины по мере заполнения шкалы.; Камера: Динамическое смещение точки обзора на 12° в сторону, противоположную вектору заноса (counter-steer look-ahead), легкое расширение FOV от 60° до 68° при удержании дуги более 1.5 с.; UI/Тактильность: Дуговая неоновая шкала угла заноса вокруг спидометра с переходом цвета от желтого к пульсирующему синему; вибрация геймпада/смартфона с частотой 45 Гц при идеальном угле скольжения.
- **Technical Complexity**: Medium
- **Решение игрока**: Рискнуть войти в глубокий занос (угол 45°-65°) вплотную к плотной толпе зомби ради x3.2 множителя заряда Нитро, рискуя застрять в массе врагов при падении скорости ниже 28 км/ч, или удерживать безопасную дугу на пустой периферии арены с медленной зарядкой.
- **Состояния**: `GRIP_DRIVING: Скорость > 40 км/ч, угол скольжения < 15°, базовый прирост нитро 0%/с, стандартное сцепление шин.`, `DRIFT_ENTRY: Угловая скорость рыскания > 1.8 рад/с, срыв задней оси, старт генерации дыма и искр.`, `SUSTAINED_DRIFT: Угол скольжения 22.5°-75.0°, скорость 28-120 км/ч, генерация 18.5% нитро/с, множитель очков x1.5-x3.2.`, `OVERSTEER_SPINOUT: Угол > 75°, критическая потеря скорости (-60%), отключение накопления нитро, штраф к управляемости на 1.2 с.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `min_drift_angle_deg` = `22.5 град` — При снижении до 10° занос триггерится случайно при любом мелком маневре; при повышении до 40° вход в дрифт требует чрезмерных усилий и ломает аркадную динамику.
  - `rage_charge_rate_per_sec` = `18.5 %/с` — При значении > 35%/с игрок находится в бесконечном нитро-таране; при < 8%/с дрифт перестает ощущаться как выгодное тактическое действие.
  - `critical_speed_stall_kmh` = `28.0 км/ч` — Если поднять до 45 км/ч, броневик намертво вязнет в любой группе зомби; если опустить до 10 км/ч, полностью исчезает риск остановки и окружения.
  - `drift_multiplier_max` = `3.2 x` — Если снизить до 1.5x, игроки перестают рисковать глубоким заносом в толпе; если поднять до 6x, ломается ценность модулей оружия.
- **Слои отклика**:
  - Визуал/VFX: Густые клубы черного жженого корда из-под задних покрышек, процедурные следы протектора на асфальте с кровавыми брызгами, сноп оранжево-синих искр от металлических дисков при угле заноса > 45°.
  - Звук (Web Audio): Синтезированный визг резины (Bandpass фильтр 1.8-3.2 кГц с модуляцией частоты от угла заноса), перегруженный низкочастотный рокот компрессора V8, нарастающий свист турбины по мере заполнения шкалы.
  - Камера: Динамическое смещение точки обзора на 12° в сторону, противоположную вектору заноса (counter-steer look-ahead), легкое расширение FOV от 60° до 68° при удержании дуги более 1.5 с.
  - UI/Тактильность: Дуговая неоновая шкала угла заноса вокруг спидометра с переходом цвета от желтого к пульсирующему синему; вибрация геймпада/смартфона с частотой 45 Гц при идеальном угле скольжения.
- **Режим отказа игрока**: Игрок перекручивает руль (угол > 75°) или сбрасывает скорость ниже 28 км/ч: броневик разворачивает на 180°, машина теряет инерцию, шкала заноса сбрасывается с резким звуком срыва сцепления, а орда зомби мгновенно облепляет борта.
- **Кривая мастерства**: Новичок на 30-й секунде заносов влетает в бетонные ограждения и крутит неуправляемые 'спинауты'; мастер на 10-м забеге непрерывно нарезает идеальные восьмерки вокруг спавнеров зомби, балансируя угол строго на 60° с максимальным притоком Нитро.
- **Сопротивление игры**: Зомби-плеватели заливают асфальт едкой кислотой (снижает коэффициент трения колес на 70%, провоцируя мгновенный разворот), а бронированные спецназовцы выставляют шипованные ленты.
- **Синергии**:
  - Заряжает шкалу для активации Нитро-Таранного Прорыва.
  - Раскручивает боковые Шипованные Диски и Пилы, наносящие x2.5 режущий урон по радиусу дрифта.
- **Почему это не жанровый шаблон**: В отличие от классических авто-шутеров с плоским рулением, броневик атакует именно боковым смещением массы через физику твердых тел Rapier3D, превращая дрифт из маневра уклонения в главное ударное оружие.
- **Псевдокод тика**:
```text
function tickDrift(car, dt) {
  const slipAngle = calculateSlipAngle(car.velocity, car.forwardVector);
  if (car.speedKmH > 28.0 && slipAngle >= 22.5 && slipAngle <= 75.0) {
    const angleRatio = (slipAngle - 22.5) / (75.0 - 22.5);
    const multiplier = 1.0 + angleRatio * 2.2;
    car.nitroRage = Math.min(100.0, car.nitroRage + 18.5 * multiplier * dt);
    VFX.spawnDriftSparks(car.rearWheels, slipAngle);
    Audio.setDriftPitch(0.8 + angleRatio * 0.6);
  } else if (slipAngle > 75.0) {
    car.applySpinoutPenalty(1.2);
  }
}
```

### Нитро-Таранный Прорыв с Ударной Волной (Nitro Ram Shockwave) (SECONDARY)
- **Category**: Источник взрывного снятия напряжения и преодоления критических блокад
- **Description**: Таран рассчитывает кинетическую массу сбитых тел через физический движок Rapier3D: тела зомби превращаются в тяжелые снаряды, которые сбивают стоящих сзади врагов по принципу падающего домино.
- **Player Input**: Клавиатура: [Shift] или двойное нажатие [W]. Тач: Выделенная кнопка-педаль 'NITRO BURST' в правой зоне экрана или резкий двойной тап по экрану.
- **Hit & Sensory Feedback**: Визуал/VFX: Радиальное размытие экрана (Radial Blur 0.45), синяя хроматическая аберрация, 2.5-метровые факелы пламени из выхлопных труб, разлет ragdoll-моделей зомби на 10+ метров с объемными брызгами крови.; Звук (Web Audio): Суб-басовый ударный дроп (35 Гц -> 80 Гц за 0.08 с), оглушительный рев инжектора закиси азота, звонкий скрежет деформируемого листового металла при коллизиях.; Камера: Резкий рывок назад с увеличением FOV от 65° до 82° за 0.15 с с последующим упругим возвратом, сильный высокочастотный шейк амплитудой 0.4 ед. при сокрушении тяжелых целей.; UI/Тактильность: Двойной тяжелый хаптик-импульс на мобильных устройствах; неоновая синяя вспышка по периметру экрана и счетчик комбо-сбиваний 'RAMPAGE x25'.
- **Technical Complexity**: Medium
- **Решение игрока**: Активировать мгновенный выброс нитро на 1.85 с для пробития плотного кольца мутантов, или приберечь 100% заряд для тарана тяжелого полицейского броневика Rhino / босса в уязвимое сочленение брони.
- **Состояния**: `READY: Заряд 100%, пламя из выхлопных патрубков тлеет пульсирующим синим светом.`, `IGNITION_BURST: Момент детонации смеси (0.12 с), стартовый импульс 45000 Н, расталкивание контактных врагов.`, `OVERDRIVE_RAM: Фаза неуязвимости (1.85 с), скорость 140 км/ч, физический урон x4.5, фронтальный силовой барьер раскидывает врагов.`, `EXHAUST_COOLDOWN: Остывание инжектора (0.8 с), шлейф белого пара, блокировка накопления ярости.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `nitro_duration_sec` = `1.85 с` — Если увеличить до 3.5 с, игра превращается в бесконечный режим бога без тактики; если уменьшить до 0.8 с, игрок не успевает пробить глубокий кордон спецтехники.
  - `ram_damage_multiplier` = `4.5 x` — При множителе < 2.0x таран не ваншотит тяжелых зомби с щитами, делая лобовой удар суицидальным; при > 8.0x боссы уничтожаются за 2 нажатия.
  - `speed_boost_multiplier` = `2.4 x` — Если сделать > 3.5x, камера теряет читаемость геометрии и броневик врезается в невидимые углы; при < 1.5x теряется ощущение сокрушительного рывка.
  - `shockwave_radius_m` = `6.5 м` — При радиусе < 3 м разлет тел выглядит неубедительно; при > 12 м зачищается весь экран без необходимости точного наведения машины.
- **Слои отклика**:
  - Визуал/VFX: Радиальное размытие экрана (Radial Blur 0.45), синяя хроматическая аберрация, 2.5-метровые факелы пламени из выхлопных труб, разлет ragdoll-моделей зомби на 10+ метров с объемными брызгами крови.
  - Звук (Web Audio): Суб-басовый ударный дроп (35 Гц -> 80 Гц за 0.08 с), оглушительный рев инжектора закиси азота, звонкий скрежет деформируемого листового металла при коллизиях.
  - Камера: Резкий рывок назад с увеличением FOV от 65° до 82° за 0.15 с с последующим упругим возвратом, сильный высокочастотный шейк амплитудой 0.4 ед. при сокрушении тяжелых целей.
  - UI/Тактильность: Двойной тяжелый хаптик-импульс на мобильных устройствах; неоновая синяя вспышка по периметру экрана и счетчик комбо-сбиваний 'RAMPAGE x25'.
- **Режим отказа игрока**: Игрок активирует нитро прямо в монолитную бетонную стену или в лоб броневику Rhino без прокачанного таранного кенгурятника: машина получает 40 урона от отдачи коллизии, а заряд нитро сгорает впустую.
- **Кривая мастерства**: Новичок жмет нитро наугад сразу при заполнении шкалы; эксперт рассчитывает вектор удара так, чтобы пробить цепочку из 30 зомби, срезать угол блокпоста копов и влететь в уязвимый борт разворачивающегося перехватчика.
- **Сопротивление игры**: Появление тяжелых броневиков Rhino, идущих на встречный таран на скорости 100 км/ч (лобовое столкновение наносит обоюдный урон, требуется уворот или удар строго в борт).
- **Синергии**:
  - Расходует ресурс, накопленный в Инерционном Дрифте.
  - Активирует цепную детонацию Напалмового Шлейфа по траектории прорыва.
- **Почему это не жанровый шаблон**: Таран рассчитывает кинетическую массу сбитых тел через физический движок Rapier3D: тела зомби превращаются в тяжелые снаряды, которые сбивают стоящих сзади врагов по принципу падающего домино.
- **Псевдокод тика**:
```text
function triggerNitroRam(car) {
  if (car.nitroRage < 100.0 || car.isNitroActive) return;
  car.nitroRage = 0.0;
  car.isNitroActive = true;
  car.invulnerabilityTimer = 1.85;
  car.rigidBody.applyImpulse(car.forwardVector.clone().multiplyScalar(45000), true);
  PostProcess.triggerRadialBlur(0.45, 1.85);
  Audio.playWebAudio('nitro_subdrop');
  Physics.triggerAreaShockwave(car.position, 6.5, 3500);
}
```

### Обрушение Городских Конструкций (Pursuit Breakers Demolition) (SECONDARY)
- **Category**: Источник тактического контроля арены и мгновенного уничтожения элитных преследователей
- **Description**: Адаптация культовой механики Pursuit Breakers из NFS Most Wanted под изометрическую физику арены: интерактивное окружение превращается в управляемое супероружие игрока.
- **Player Input**: Физический контакт: точный таран опорных балок объекта передним кенгурятником или контролируемым боковым заносом (хлыстом заднего бампера).
- **Hit & Sensory Feedback**: Визуал/VFX: Кинематографичный ракурс разрушения (Look-At на падающую вывеску/водонапорную вышку), фонтан электро-искр от рвущихся проводов, объемные клубы серой пыли и взрыв с разлетом 20+ физических обломков.; Звук (Web Audio): Глухой металлический скрежет деформируемых двутавровых балок, резкий щелчок лопающихся стальных тросов, массивный низкочастотный взрыв (Lowpass 200 Гц) при ударе крыши о землю.; Камера: Кратковременное переключение на динамический нижний ракурс под углом 30° к падающему объекту, плавный зум назад с последующим возвратом за бампер машины.; UI/Тактильность: Всплывающая золотая плашка 'PURSUIT BREAKER: 12 ПОЛИЦЕЙСКИХ УНИЧТОЖЕНО' с анимацией разбивающегося стекла; затухающая вибрация длительностью 400 мс.
- **Technical Complexity**: Medium
- **Решение игрока**: Сохранить на арене опорные балки гигантского рекламного щита или крыши заправки до появления тяжелых перехватчиков Heat 4 и босса, либо снести их сейчас для экстренной расчистки проезда.
- **Состояния**: `INTACT: Конструкция устойчива, подсвечена пульсирующим красным маркером разрушения на миникарте и в мире.`, `STRUCTURAL_IMPACT: Контакт на скорости > 42 км/ч: деформация балки, замедление времени до 0.25x на 0.35 с.`, `COLLAPSING: Физическое обрушение: спавн 15-25 обломков с активными коллайдерами, зона летального урона 14 м.`, `DEBRIS_FIELD: Обломки остаются на земле, блокируя проезд зомби и образуя трамплин для машины игрока.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `impact_trigger_speed_kmh` = `42.0 км/ч` — Если установить < 20 км/ч, опоры будут падать от случайного касания бродячих зомби; если > 70 км/ч, игрок не сможет активировать ловушку в дрифте.
  - `demolition_kill_radius_m` = `14.0 м` — При радиусе < 6 м ловушка не накрывает преследующий хвост копов; при > 25 м разрушается баланс арены и исчезает челлендж позиционирования.
  - `slomo_duration_sec` = `0.35 с` — Если сделать > 1.0 с, нарушается динамика экшен-гонки и ритм саундтрека; если < 0.15 с, игрок не успевает зафиксировать зрелищность катастрофы.
  - `gear_reward_bonus` = `45 шт` — При значении < 10 шт игроки игнорируют ловушки; при > 120 шт происходит мгновенный оверлевелинг в первые 60 секунд заезда.
- **Слои отклика**:
  - Визуал/VFX: Кинематографичный ракурс разрушения (Look-At на падающую вывеску/водонапорную вышку), фонтан электро-искр от рвущихся проводов, объемные клубы серой пыли и взрыв с разлетом 20+ физических обломков.
  - Звук (Web Audio): Глухой металлический скрежет деформируемых двутавровых балок, резкий щелчок лопающихся стальных тросов, массивный низкочастотный взрыв (Lowpass 200 Гц) при ударе крыши о землю.
  - Камера: Кратковременное переключение на динамический нижний ракурс под углом 30° к падающему объекту, плавный зум назад с последующим возвратом за бампер машины.
  - UI/Тактильность: Всплывающая золотая плашка 'PURSUIT BREAKER: 12 ПОЛИЦЕЙСКИХ УНИЧТОЖЕНО' с анимацией разбивающегося стекла; затухающая вибрация длительностью 400 мс.
- **Режим отказа игрока**: Игрок врезается в опору на недостаточной скорости (< 42 км/ч) или под прямым углом 90° без запаса брони: конструкция не падает, машина теряет 30 HP и намертво застревает перед хвостом преследователей.
- **Кривая мастерства**: Новичок сносит опоры в панике и попадает под собственные падающие обломки; ветеран дрифтует в миллиметре от опоры, обрушивая многотонную конструкцию строго на крыши преследующих маслкаров спецназа.
- **Сопротивление игры**: Зомби-мутанты класса 'Тяжеловес' могут блокировать проезд к опорам своими телами, а вертолет спецназа подсвечивает разрушенные зоны прожектором, сбрасывая туда десант.
- **Синергии**:
  - Активируется на высокой скорости из Нитро-Таранного Прорыва.
  - Генерирует фонтан шестеренок для ускоренной прокачки Кинетических Модулей.
- **Почему это не жанровый шаблон**: Адаптация культовой механики Pursuit Breakers из NFS Most Wanted под изометрическую физику арены: интерактивное окружение превращается в управляемое супероружие игрока.
- **Псевдокод тика**:
```text
function onBreakerCollision(car, breakerObj, impactSpeed) {
  if (impactSpeed >= 42.0 && breakerObj.state === 'INTACT') {
    breakerObj.state = 'COLLAPSING';
    TimeManager.triggerSlomo(0.25, 0.35);
    Physics.spawnDebrisField(breakerObj.position, 20);
    dealAreaDamage(breakerObj.position, 14.0, 9999);
    Economy.spawnGears(breakerObj.position, 45);
    Camera.triggerDramaticLookAt(breakerObj.position, 0.35);
  }
}
```

### Кинетическая Эволюция Модулей Броневика (Kinetic Module Evolution) (SECONDARY)
- **Category**: Двигатель долгосрочной силы внутри забега и вариативности билдостроения
- **Description**: Модули физически монтируются на 3D-кузов броневика и зависят от кинематики движения: дисковые пилы режут строго по касательной траектории заноса, а огнемет заливает напалмом внешнюю дугу дрифта.
- **Player Input**: UI-модальное окно: выбор 1 из 3 карточек апгрейда кликом мыши или тапом по экрану во время автоматического слоу-мо (0.05x timescale) при левелапе.
- **Hit & Sensory Feedback**: Визуал/VFX: Неоновый голографический интерфейс в стилистике Most Wanted, вспышки дуговой сварки на кузове при установке апгрейда, лазерные трассеры и огненные шлейфы от работающих модулей.; Звук (Web Audio): Приглушение музыки Lowpass фильтром (до 400 Гц) при левелапе, сочный металлический лязг пневматического гайковерта, мощный басовый дроп при возврате в нормальное время.; Камера: Плавный кинематографичный облет машины на 45 градусов вокруг вертикальной оси во время выбора карточки, затем резкий динамический зум в бампер при подтверждении.; UI/Тактильность: Карточки модулей с анимированными 3D-иконками и неоновыми рамками редкости (Обычный, Редкий, Прототип); тактильный клик-виброотклик при выборе.
- **Technical Complexity**: Medium
- **Решение игрока**: Выбрать модуль ближней дрифт-зачистки (Циркулярные пилы на дисках) для агрессивного контакта, модуль контроля дистанции (Термический огнеметный шлейф) для отсечения орды, или автоматическую импульсную турель 20-мм.
- **Состояния**: `XP_ACCUMULATION: Сбор шестеренок магнитным полем капота в радиусе 4.5 м.`, `LEVEL_UP_SLOMO: Замедление времени до 0.05x, появление неонового интерфейса выбора модулей.`, `MODULE_INSTALLATION: Визуальный монтаж модуля на 3D-модель машины (анимация дуговой сварки и лязг пневмопистолета).`, `ACTIVE_SYNERGY: Автоматическая работа модулей, синхронизированная с углами заноса и скоростью движения.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `base_pickup_radius_m` = `4.5 м` — При радиусе < 2.5 м игрок вынужден нарезать круги за каждой деталью, сбивая темп гонки; при > 10 м сбор ресурсов становится полностью пассивным.
  - `slomo_selection_timescale` = `0.05 x` — Если ставить 0.0x (полную паузу), сбивается ритм фонового фонк-трека; если > 0.2x, броневик успевают разбить зомби во время чтения описания карт.
  - `max_active_modules_count` = `4 слота` — При < 3 слотов билды слишком примитивны; при > 6 слотов экран перегружен спецэффектами и Three.js теряет стабильные 60 FPS на мобильных устройствах.
  - `level_exp_scaling_factor` = `1.28 x` — При факторе > 1.5x прокачка стопорится на 5-й минуте заезда; при < 1.1x игрок максимизирует все слоты за первые 2 минуты.
- **Слои отклика**:
  - Визуал/VFX: Неоновый голографический интерфейс в стилистике Most Wanted, вспышки дуговой сварки на кузове при установке апгрейда, лазерные трассеры и огненные шлейфы от работающих модулей.
  - Звук (Web Audio): Приглушение музыки Lowpass фильтром (до 400 Гц) при левелапе, сочный металлический лязг пневматического гайковерта, мощный басовый дроп при возврате в нормальное время.
  - Камера: Плавный кинематографичный облет машины на 45 градусов вокруг вертикальной оси во время выбора карточки, затем резкий динамический зум в бампер при подтверждении.
  - UI/Тактильность: Карточки модулей с анимированными 3D-иконками и неоновыми рамками редкости (Обычный, Редкий, Прототип); тактильный клик-виброотклик при выборе.
- **Режим отказа игрока**: Игрок собирает несбалансированный билд (только дальнобойные пулеметы без модулей прочности и контактного дрифт-урона) и оказывается неспособен разорвать плотное кольцо бронированных мутантов на Heat 4.
- **Кривая мастерства**: Новичок берет случайные модули с наибольшим разовым уроном; эксперт собирает синергетический 'Vortex Drift Build' (притягивающий вихрь + дисковые пилы + напалмовый след), зачищающий арену за секунды.
- **Сопротивление игры**: Появление элитных мутантов с электромагнитными щитами (временно отключают турели на крыше, требуя контактного распила циркулярными пилами на ступицах).
- **Синергии**:
  - Модули напрямую масштабируют урон от параметров Инерционного Дрифта и Нитро-Тарана.
  - Шестеренки от Pursuit Breakers ускоряют получение ключевых эволюций.
- **Почему это не жанровый шаблон**: Модули физически монтируются на 3D-кузов броневика и зависят от кинематики движения: дисковые пилы режут строго по касательной траектории заноса, а огнемет заливает напалмом внешнюю дугу дрифта.
- **Псевдокод тика**:
```text
function onCollectGear(car, gearValue) {
  car.currentExp += gearValue * car.expMultiplier;
  if (car.currentExp >= car.expToNextLevel) {
    car.currentExp -= car.expToNextLevel;
    car.expToNextLevel = Math.floor(car.expToNextLevel * 1.28);
    TimeManager.setTimescale(0.05);
    UI.showUpgradeCards(car.getAvailableModules(), (selectedModule) => {
      car.installModule(selectedModule);
      TimeManager.restoreTimescale(0.2);
    });
  }
}
```

### Слоу-мо Финишер Босса с Таранным Разломом (Boss Finisher Ram-Breaker) (SECONDARY)
- **Category**: Кульминация заезда, источник максимального катарсиса и условие победы
- **Description**: Превращает завершение забега из рутинного расстрела полоски здоровья в интерактивный кинематографичный финишер в духе автомобильного фаталити, вознаграждающий смелость игрока.
- **Player Input**: Клавиатура: Удержание [Shift] + [Пробел] для фиксации вектора тарана. Тач: Удержание двух пальцев на экране для фокусировки кинематографичного удара.
- **Hit & Sensory Feedback**: Визуал/VFX: Отделение камеры от машины в кинематографичный рельсовый пролет Bullet-Time, рентген-вспышка точки удара кенгурятника, взрывной разрыв босса на куски с эффектом объемной волны.; Звук (Web Audio): Мгновенное заглушение всех фоновых звуков с эффектом звона в ушах (Tinnitus 4 кГц notch фильтр), раскатистый замедленный бас-буст взрыва и финальный тяжелый гитарный рифф.; Камера: Динамическая траектория Bullet-Time с облетом точки столкновения на 180 градусов, голландский угол наклона (Dutch Angle 18°), экстремальный зум на деформирующийся радиатор.; UI/Тактильность: Серия из трех нарастающих хаптик-импульсов (имитация сердцебиения) перед столкновением и мощнейшая непрерывная вибрация на 600 мс в момент контакта; титр 'TARGET ELIMINATED'.
- **Technical Complexity**: Medium
- **Решение игрока**: Рискнуть пойти в лобовую атаку на финальной стадии босса (HP < 15%) на максимальной скорости с нитро, чтобы активировать кинематографичный финишер, или добивать его безопасными турелями с дальней дистанции.
- **Состояния**: `BOSS_STAGGERED: Босс оглушен, его броня расколота, открыта точка критического удара Core-Node.`, `TARGET_LOCK_IN: Игрок разгоняется по прямой, траектория подсвечивается неоновым коридором прицеливания.`, `CINEMATIC_IMPACT_SLOMO: Замедление времени до 0.1x, динамическая камера берет крупный план бампера и морды мутанта.`, `EXPLOSIVE_DISMEMBERMENT: Взрыв мутанта, разлет бронеплит, финальный экран победы с начислением Репутации.`
- **Числовые параметры (реализуй именно эти значения)**:
  - `finisher_hp_threshold_pct` = `15.0 %` — Если поднять до 35%, босс-файт заканчивается слишком рано без ощущения тяжелой битвы; если опустить до 5%, игрок часто случайно убивает босса турелью без зрелищного тарана.
  - `finisher_slomo_timescale` = `0.10 x` — При значении > 0.3x теряется эпичность и кинематографичность; при < 0.03x сцена кажется зависшей.
  - `cam_orbit_distance_m` = `5.2 м` — При дистанции < 3 м геометрия босса перекрывает экран; при > 10 м теряется детальность деформации металла кенгурятника и разлета плоти.
  - `reputation_bonus_multiplier` = `2.5 x` — Мотивирует игрока завершать бой именно рискованным лобовым тараном, а не пассивной стрельбой из турелей.
- **Слои отклика**:
  - Визуал/VFX: Отделение камеры от машины в кинематографичный рельсовый пролет Bullet-Time, рентген-вспышка точки удара кенгурятника, взрывной разрыв босса на куски с эффектом объемной волны.
  - Звук (Web Audio): Мгновенное заглушение всех фоновых звуков с эффектом звона в ушах (Tinnitus 4 кГц notch фильтр), раскатистый замедленный бас-буст взрыва и финальный тяжелый гитарный рифф.
  - Камера: Динамическая траектория Bullet-Time с облетом точки столкновения на 180 градусов, голландский угол наклона (Dutch Angle 18°), экстремальный зум на деформирующийся радиатор.
  - UI/Тактильность: Серия из трех нарастающих хаптик-импульсов (имитация сердцебиения) перед столкновением и мощнейшая непрерывная вибрация на 600 мс в момент контакта; титр 'TARGET ELIMINATED'.
- **Режим отказа игрока**: Игрок пытается протаранить босса, когда у того еще более 15% HP или когда босс проводит встречный бросок: броневик отлетает назад, теряя 70% здоровья от сокрушительного контрудара.
- **Кривая мастерства**: Новичок боится идти на таран босса и погибает от спама его миньонов; ветеран рассчитывает тайминг оглушения босса, разворачивает броневик на 180° в дрифте и всаживает полный заряд Нитро точно в открытое ядро.
- **Сопротивление игры**: Босс призывает элитных миньонов-камикадзе, пытающихся закрыть собой уязвимую точку и принять таран на себя.
- **Синергии**:
  - Требует 100% готовности Нитро-Таранного Прорыва.
  - Разблокирует максимальную награду Most Wanted в Мета-Прогрессии Гаража.
- **Почему это не жанровый шаблон**: Превращает завершение забега из рутинного расстрела полоски здоровья в интерактивный кинематографичный финишер в духе автомобильного фаталити, вознаграждающий смелость игрока.
- **Псевдокод тика**:
```text
function checkBossFinisher(car, boss) {
  if (boss.hp / boss.maxHp <= 0.15 && car.isNitroActive && car.speedKmH >= 80.0) {
    TimeManager.triggerSlomo(0.10, 2.0);
    Camera.playBulletTimeOrbit(car.position, boss.position, 5.2);
    boss.triggerDismemberment();
    Economy.grantReputation(boss.baseRep * 2.5);
    GameState.triggerVictorySequence();
  }
}
```


---

## 3b. ⚠️ СТРОГИЙ ЗАПРЕТ ЖАНРОВЫХ ШАБЛОНОВ И КЛОНОВ (CRITICAL ANTI-CLICHÉ RULES)
Кодовый агент ОБЯЗАН реализовать уникальную игру, спроектированную в этом ТЗ, а не шаблонный автошутер:
1. **ЗАПРЕТ ШАБЛОННЫХ РОГАЛИКОВ И КАРТОЧЕК**: Если игра прямо не требует карточный драфт в GDD, **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** добавлять спам волн врагов и всплывающее окно «Выберите 1 из 3 карт апгрейда».
2. **ЗАПРЕТ СЕРЫХ ПРИМИТИВОВ НА ПУСТОЙ ПЛОСКОСТИ**: Создавай выразительную процедурную низкополигональную 3D/2D графику, точно соответствующую `ART_DIRECTION.md` (тематические персонажи, рельеф, модули, объекты окружения, частицы), а не бегающий куб на сером полу.
3. **ТОЧНОЕ СОБЛЮДЕНИЕ МЕХАНИК**: Реализуй все состояния, формулы, тайминги и слои отклика из `MECHANICS.md` (включая Web Audio звуки, импульсы камеры, визуал и тач-инпут).
4. **СПЕЦИФИЧЕСКОЕ УПРАВЛЕНИЕ**: Реализуй схему управления под конкретную механику этой игры (свайпы, жесты, траектории, физический дрифт, прицеливание), а не стандартный стик.


---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Application Layer**: Инициализация Vite, ресайз канваса, полноэкранный режим, прелоадер.
- **Platform & Ads Layer**: Адаптер Playgama Bridge, менеджеры Interstitial и Rewarded рекламы, Cloud Save.
- **Core Engine Layer**: Фиксированный цикл GameLoop 60Гц, EventBus, маршрутизация ввода.
- **Physics Simulation Layer**: Шаги симуляции Rapier3D, фильтрация коллизий, рэгдолл-синхронизация.
- **Gameplay Systems Layer**: Боевая система, спавн врагов, выбор 3 карт улучшений, расчет комбо.
- **Entity Management Layer**: Пул сущностей игрока, врагов, снарядов и осколков.
- **Rendering Layer**: Three.js граф сцены, InstancedMesh батчинг, тени, эмиттеры частиц.
- **UI & HUD Layer**: HTML5/CSS3 оверлей, виртуальный джойстик, полосы HP, модальные окна.

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   └── PhysicsWorld.ts        # Rapier3D / Physics world manager & colliders
├── entities/
│   ├── Player.ts              # Player entity & input handling
│   └── EntityManager.ts       # Dynamic entity pool & lifecycle
├── systems/
│   ├── System.ts                # Обеспечить четкое и отзывчивое управление с у
│   ├── System.ts                # Обработка ударов, тарана, хитбоксов, расчета 
├── rendering/
│   ├── SceneManager.ts        # Three.js scene graph, lights, perspective camera lerp
│   ├── ProceduralModels.ts    # Styled models / geometry for Гонку стиле нфс playgama
│   └── ParticleSystem.ts      # Particle effects & visual feedback
├── ui/
│   ├── UIManager.ts           # HUD overlay, state transitions
│   └── TouchControls.ts       # Mobile touch input adapter
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic audio feedback
```

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Второе Дыхание (Возрождение) (`revive_run`)**: Восстановление 50% HP + 3 сек неуязвимости с силовой волной, раскидывающей врагов. (Trigger: При получении смертельного урона., Limit: 1 раз за забег.)
- **Удвоение Наград (2x Золото) (`double_gold_run`)**: Удваивает все заработанные шестеренки и монеты за завершенный раунд. (Trigger: Экран окончания игры., Limit: Доступно на каждом экране результатов.)
- **Переброс Карт Улучшений (`free_card_reroll`)**: Обновляет список 3 карт улучшений с гарантией Редкой или Эпической карты. (Trigger: Окно выбора 3 карт., Limit: До 2 раз за забег.)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"player_save_v1"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Landscape
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Обязательный контракт тач-управления
- **Слева — РУЛЬ**: плавающий стик, работает только по горизонтали (поворот). Вертикаль стика не управляет газом.
- **Справа — ПЕДАЛИ И ДЕЙСТВИЯ**: большая кнопка **ГАЗ** (≥ 104 px) под большим пальцем, рядом **НАЗАД/ТОРМОЗ**, выше — **НИТРО** и **РУЧНИК/ДРИФТ**.
- Газ, руль и нитро должны нажиматься одновременно (мультитач на 3 пальца).

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Плавающий стик**: зона захвата — вся левая половина экрана, база стика
  появляется под пальцем. Мёртвая зона 8%, иначе управление дрожит.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `W` / `S` / `↑` / `↓` — газ и тормоз/задний ход
- `A` / `D` / `←` / `→` — руль
- `Space` / `E` — нитро
- `Shift` / `F` — ручной тормоз (дрифт)
- `P` / `Esc` — пауза

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/gonku-stile-nfs-playgama/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ART DIRECTION & VISUAL GUIDELINES
- **Style**: Выжженная пустошь: ржавчина и пыль
- **Camera Perspective**: Изометрическая 3D камера с динамическим зумом (FOV: 50°, Pitch: 45°)
- **Environment**: Постапокалиптическая арена под низким закатным солнцем
- **Lighting**: Жёсткое закатное солнце, длинные тени, пыль в контровом свете
- **Visual Feedback**: Screen-space hitstop (40ms on critical hit), directional particle sparks, additive ribbon weapon trails.

---

## 8. STEP-BY-STEP DEVELOPMENT ROADMAP
### Phase 1: Архитектура и Базовый Прототип (3 days)
- **Deliverable**: Управляемая модель машины/персонажа на 3D арене с 60 FPS
  - Настройка Vite, TypeScript и @playgama/bridge
  - Инициализация сцены Three.js и физического мира Rapier3D
  - Реализация контроллера движения и физики заноса
### Phase 2: Боевая Система и Спавн Врагов (4 days)
- **Deliverable**: Рабочий боевой цикл с волнами противников
  - Система расчета коллизий, урона и хит-стопа
  - Пул врагов с базовым AI преследования
  - Реализация шкалы ярости и супер-ударов
### Phase 3: Рогалик-Прогрессия и UI (3 days)
- **Deliverable**: Полноценный цикл забега с прокачкой способностей
  - Система 3-Card улучшений и синергий
  - Верстка адаптивного HUD и меню на HTML5/CSS3
  - Звуковые эффекты через Howler.js
### Phase 4: Интеграция Playgama Bridge и Полишинг (2 days)
- **Deliverable**: Полная интеграция с порталом Яндекс Игры
  - Интеграция Rewarded и Interstitial рекламы
  - Сохранение прогресса в Cloud Storage
  - Подключение глобальных таблиц рекордов
### Phase 5: QA, Оптимизация и Релиз (2 days)
- **Deliverable**: Готовая релизная сборка для публикации
  - Тестирование на мобильных устройствах (iOS/Android)
  - Профайлинг WebGL памяти и вызовов отрисовки
  - Финальная сборка и проверка Definition of Done


---

## 9. NON-NEGOTIABLE PLATFORM RULES
Every rule below corresponds to a bug that reached production or a moderation rejection in a shipped game. They override any conflicting habit, tutorial or example — including snippets found in the Playgama/Yandex docs, many of which describe the deprecated Bridge v1 contract.

Distilled from shipped HTML5 games on Yandex Games, VK, OK, CrazyGames and
Playgama. Every rule here corresponds to a bug that reached production or a
moderation rejection. Violating any of them ships a broken or rejected game.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Stack

**S1.** The factory ships **Three.js only**. A 2D game is the same scene under an
orthographic camera — never a second renderer. See
`knowledge/threejs/orthographic_2d_and_pointer_input.md`.

**S2.** The stack is fixed and pinned: `three ^0.185.1`,
`@dimforge/rapier3d-compat ^0.20.0`, `three-mesh-bvh ^0.9.14`, `yuka ^0.7.8`,
`recast-navigation ^0.43.1`, `bitecs ^0.4.0`, `postprocessing ^6.39.4`. Online
snippets written for `bitecs 0.3` (`defineComponent`/`defineQuery`) or
`rapier 0.13` do not compile against these — check `knowledge/stack/`.

**S3.** **If the stack solves it, take the library.** A hand-rolled A\*, boids
flock, broadphase, character controller, vision/memory timer or bloom chain is a
review defect, not an optimisation. The full "task → library" table is
`knowledge/stack/README.md` §1: physics and vehicles → Rapier; raycast against
static level geometry → three-mesh-bvh; steering, FSM, fuzzy decisions and
perception → Yuka; NPC pathfinding and crowds → recast-navigation; hundreds of
identical entities → bitECS + `InstancedMesh`; screen effects → postprocessing.

**S4.** Frame order is fixed: input → AI → controllers → `world.step()` → ECS →
transform sync → camera → quality changes → `composer.render()`.
`renderer.render()` is **not** called next to `composer.render()` — that draws the
scene twice.

**S5.** Physics steps at a **fixed** timestep with a substep cap (≤ 4). Passing
`dt` into the solver destabilises suspension and joints; an uncapped catch-up loop
turns a slow frame into a hang.

**S6.** WASM (Rapier, Recast) is initialised on the loading screen behind the boot
watchdog, and Recast is loaded **only** when the game actually needs a navmesh.

### Renderer

52. Auto-tuning quality from raw frame time does not work under vsync — every frame
    takes ≈ the refresh interval regardless of GPU load. Measure the cadence between
    **rendered** frames and discover headroom by optimistic probing.
53. Never target above the panel's refresh rate, and never launch in reduced quality
    and crawl up — start optimistic and step down.
54. Apply resolution and shadow-map changes **before** `render()`, on a frame you
    actually render; doing it after clears the canvas and flashes a blank frame.
55. One group, one degree of freedom. A mesh that both steers and spins (wheel,
    turret, limb) needs **nested** groups — `Euler('XYZ')` applies the inner
    rotation around the parent's axis and visibly skews the part otherwise.
    See `knowledge/threejs/vehicle_wheel_rig.md`.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Physics vehicles

60. A physics vehicle is **never** driven by writing `setLinvel()` every frame.
    That overwrites the solver, ignores slopes and heading, and leaves the wheels
    as decoration — the "wheels don't turn, the truck doesn't move" bug. Use the
    engine's own ray-cast vehicle controller (`world.createVehicleController`).
61. Visual wheels of a physics vehicle are **children of the chassis group**, and
    their suspension travel, steering angle and roll angle are read from the
    controller. A separate wheel root that copies only `translation()` detaches
    the wheels from the body the moment it tilts, and leaves parts overlapping
    where the body has moved on. The opposite rule in `vehicle_wheel_rig.md` §3
    applies to kinematic arcade cars only.
62. `updateVehicle(dt)` runs **before** `world.step()`, and the wheels' ray-casts
    are filtered to the ground collision group — unfiltered they hit the chassis
    or the cargo and the vehicle climbs its own load.
63. Cargo and props carried by a body spawn from **body-local** slots that do not
    intersect its colliders. An overlap at spawn is resolved by ejection: the load
    fires out of the bed on frame one. Anything meant to stay put needs real wall
    colliders, not a painted lip.
64. Terrain is one continuous displaced ribbon whose collider is built from the
    **same buffers** as the visible mesh. A road assembled from individually
    rotated boxes has a ledge at every joint. Trimesh colliders are for static
    bodies only.
65. Restarting a run **teleports** existing bodies (translation, rotation and both
    velocities, then `wakeUp()`); it never rebuilds meshes and bodies. Rebuilds
    leak the old body, dispose shared geometry and orphan the vehicle controller.
    Removed items are disabled, not destroyed.
66. Physics handling is verified head-lessly before it is verified by eye — the
    physics engine runs in Node without a renderer. Keep the vehicle spec in a
    renderer-free module and assert acceleration, wheel-rotation sign, steering
    sign, suspension settling and cargo retention in a script.
    See `knowledge/threejs/rapier_vehicle_controller.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

---

## 10. DEFINITION OF DONE (MANDATORY VERIFICATION CHECKLIST)
To mark this game as complete, every single requirement below must be verified and working:

- [ ] Полная компиляция проекта на TypeScript без ошибок сборки
- [ ] Стабильные 60 FPS в браузере с временем отклика управления < 50мс
- [ ] Рабочий цикл сессии: старт -> волны врагов -> 3-Card апгрейды -> битва с боссом -> результат
- [ ] Интеграция Playgama Bridge: Rewarded видео, баннеры, Cloud Save и Leaderboards
- [ ] Мобильное управление на Pointer Events: движение и основное действие работают одновременно, мультитач, отмена скролла и контекстного меню, safe-area
- [ ] Слой тач-управления виден только в игровом процессе и сбрасывается при паузе, сворачивании вкладки и показе рекламы
- [ ] Мобильную раскладку можно проверить на десктопе флагом ?touch=1
- [ ] Звуковое сопровождение: фоновая музыка и сочные звуки попаданий/дрифта
- [ ] `npm install && npm run dev` поднимают играбельную сборку без ошибок в консоли
- [ ] Ведутся DEVLOG.md и CHANGELOG.md, в README.md описаны запуск и управление
- [ ] Соответствие всем критериям публикации на Яндекс Играх

---

## 11. FACTORY KNOWLEDGE BASE
Deep, worked-out detail behind the rules in section 9 — read the relevant file before implementing that area:

Набор отобран под этот проект: Проект опирается на связку аркадной физики дрифта и кинетических таранов на Rapier3D с рогаликовым циклом сессии, разрушаемым окружением и процедурным визуалом.

**Ядро — прочитать до начала реализации:**
- `knowledge/threejs/rapier_vehicle_controller.md` — Реализация физически корректного рэйкаст-контроллера броневика с настройкой сцепления и подвески на Rapier3D.
- `knowledge/threejs/arcade_racing_and_drift.md` — Аркадная физика срыва в инерционный дрифт, отрисовка следов шин и накопление заряда нитро-ярости в заносе.
- `knowledge/stack/rapier3d.md` — WASM-движок физики твердых тел для обработки высокоскоростных столкновений броневика, отлетающих тел и обломков.
- `knowledge/mechanics/chain_reaction.md` — Расчет кинетического импульса тарана для превращения сбитых зомби в снаряды, поражающие ряды врагов по принципу домино.
- `knowledge/mechanics/physics_destruction.md` — Реализация механики Pursuit Breakers с обрушением опорных городских конструкций на преследователей при таране.
- `knowledge/patterns/roguelike_loop.md` — Организация структуры забега с нарастающим уровнем Heat, битвой с боссом-мутантом и мета-прогрессией в гараже.

**Вспомогательные:**
- `knowledge/mechanics/drift_scoring.md` — Подсчет множителя и непрерывности заноса для конвертации угла дрифта в шкалу нитро-ускорения.
- `knowledge/threejs/game_map_and_world_design.md` — Создание процедурной городской арены постапокалипсиса со срезами через дворы и интерактивными объектами.
- `knowledge/threejs/juice_and_vfx_pool.md` — Инстансированные частицы для закиси азота, искр заноса, динамической тряски камеры и слоу-мо финишеров.
- `knowledge/threejs/procedural_mesh_builder.md` — Процедурная генерация стилизованного 3D-кузова броневика, модулей вооружения и разрушаемых балок без внешних моделей.
- `knowledge/audio/procedural_sound_synthesizer.md` — Процедурный Web Audio синтез рева мотора, визга резины в дрифте, хлопков нитро и звуков кинетического сокрушения.

**Архетип петли проекта**: `knowledge/patterns/roguelike_loop.md`

**Платформенные требования (обязательны всегда):**
- `knowledge/playgama/game_ready_and_loading.md`
- `knowledge/playgama/auth_and_player.md`
- `knowledge/playgama/storage_and_cloud.md`
- `knowledge/playgama/ads_integration.md`
- `knowledge/playgama/banners_and_layout.md`
- `knowledge/compliance/yandex_moderation.md`
- `knowledge/ux/localization_system.md`
- `knowledge/ux/touch_controls.md`

**Осознанно НЕ включены**: `threejs/racing_track_and_opponents.md`, `mechanics/checkpoint_lap_racing.md`, `patterns/racing_event_loop.md`, `threejs/horde_survivor_core.md`, `patterns/survivor_loop.md`, `stack/bitecs.md`, `stack/recast_navigation.md`, `mechanics/rubberband_opposition.md`, `mechanics/upgrade_choices.md`. Игра строится на физическом таране броневиком и дрифте в забеге до точки эвакуации, поэтому исключены кольцевые гоночные механики кругов и чекпоинтов, шаблоны пеших автошутеров, а также избыточные стеки ECS и навмеша.
Не подтягивай решения из этих документов — они про другую игру.

---

## 12. DETAILED REFERENCE DOCUMENTS
For extended deep specifications, refer to the accompanying project documentation files:
- [Инструкция агенту (AGENTS.md)](./AGENTS.md)
- [Журнал разработки (DEVLOG.md)](./DEVLOG.md)
- [Changelog](./CHANGELOG.md)
- [Game Design Document](./GAME_DESIGN_DOCUMENT.md)
- [Gameplay Specification](./GAMEPLAY_SPECIFICATION.md)
- [Technical Specification](./TECHNICAL_SPECIFICATION.md)
- [Architecture Document](./ARCHITECTURE_DOCUMENT.md)
- [Playgama Integration](./PLAYGAMA_INTEGRATION.md)
- [Monetization Specification](./MONETIZATION.md)
- [Mobile Controls](./MOBILE_CONTROLS.md)
- [QA Plan](./QA_PLAN.md)
- [Обещание игроку (PLAYER_PROMISE.md)](./PLAYER_PROMISE.md)
- [Дизайн-ядро (DESIGN_NUCLEUS.md)](./DESIGN_NUCLEUS.md)
- [Плотность впечатлений (EXPERIENCE_DENSITY.md)](./EXPERIENCE_DENSITY.md)
- [Спецификация телеметрии (TELEMETRY_SPEC.md)](./TELEMETRY_SPEC.md)
- [Реестр допущений (ASSUMPTIONS.md)](./ASSUMPTIONS.md)
- [План валидации (VALIDATION_PLAN.md)](./VALIDATION_PLAN.md)
- [Журнал решений (DECISIONS.md)](./DECISIONS.md)
- [Человеческие ворота (HUMAN_GATES.md)](./HUMAN_GATES.md)
- [Game Skill Guidelines](./skills/GAME_SKILL.md)
- [Renderer Skill](./skills/RENDERER_SKILL.md)