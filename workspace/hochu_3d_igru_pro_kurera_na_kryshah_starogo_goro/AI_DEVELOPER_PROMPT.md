# FINAL AI DEVELOPER PROMPT: ЧЕРЕПИЧНЫЙ СПРИНТ: ЧИСТЫЙ ФЛОУ 🎮⚡

> **ИНСТРУКЦИЯ КОДОВОМУ АГЕНТУ**
> Ты ведущий разработчик этой игры. Задача — довести её до состояния, в котором
> она проходит приёмку из `ACCEPTANCE.md`: работающая, играбельная, принятая
> площадкой. Заглушки, TODO и «пока так» результатом не считаются.

### Как читать этот документ

Требования размечены по обязательности. Когда обязательно всё, не обязательно ничего.

| Уровень | Что значит | Что делать при сомнении |
|---|---|---|
| **MUST** | Без этого игра не работает или не проходит модерацию площадки. | Не обсуждается. Проверяется в `ACCEPTANCE.md`, разделы A–C. |
| **SHOULD** | Проектное решение фабрики: так игра задумана. | Отступать можно, но с записью в `DEVLOG.md` — что и почему. |
| **MAY** | На твоё усмотрение. | Решай сам, спрашивать не нужно. |

Неразмеченное считается **SHOULD**. Всё, что помечено «ЗАПРЕЩЕНО» в секции 1b, —
это **MUST**, даже если спецификация где-то умалчивает.

### Порядок работы

Читать документ целиком до первой строчки кода не нужно. Порядок такой:

0. `node scripts/fetch-knowledge.mjs` — забрать базу знаний в `docs/ref/`. Одна команда, дальше работа идёт офлайн. **MUST**, и раньше всего остального: без этого половина адресов в документе никуда не ведёт.
1. `ACCEPTANCE.md` — чем закончится работа. Дальше всё делается под него.
2. Секция 1b — чем эта игра является и чем она не является. Рамка всего остального.
3. `DESIGN.md` — написать самому, до первой строчки кода. Секция 7 говорит, что в нём должно быть и чего в нём быть не должно.
4. Каркас: `package.json`, Vite, TypeScript strict, пустая сцена Three.js, цикл с фиксированным шагом. Playgama Bridge и `game_ready` — сразу, а не в конце (проверки **C1–C4**).
5. Главная механика из секции 3 — одна, до играбельного состояния. Не все сразу. Перед тем как писать её с нуля — секция 3c: возможно, она уже написана.
6. Дальше по фазам роадмапа (секция 8), каждая фаза закрывает свои номера проверок.
7. `node scripts/check-spec.mjs` — после каждой фазы, а не один раз в конце.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: Черепичный Спринт: Чистый Флоу
- **Project Slug**: `hochu_3d_igru_pro_kurera_na_kryshah_starogo_goro`
- **Genre**: 3D Паркур-Раннер / Спидран-Аркада (Флоу-Раннер / Тайминг-Платформер с Физикой Инерции Груза)
- **Target Platform**: Playgama Bridge (Yandex Games / VK / Mobile Web)
- **Orientation**: Portrait
- **Target Audience**: Игроки мобильных веб-платформ (16–35 лет), ценящие ритмичные раннеры, состояние потока (flow state), паркур и тайм-атаку, с короткими игровыми сессиями по 1–2 минуты и мгновенным управлением одним пальцем.
- **Player Fantasy**: Ты — виртуозный руфер-курьер в викторианском городе эпохи пара и золотого часа, скользящий по скатам крыш на головокружительной высоте, спасающий драгоценные хрупкие посылки за счет безупречной грации и молниеносной реакции.
- **Core Hook**: Секунда затяжного прыжка над 10-метровым разрывом между особняками на закате: игрок зажимает палец в воздухе, курьер группируется, идеально амортизирует приземление на крутой черепичный скат, сохраняя 100% целостности алхимической колбы и удерживая комбо-множитель скорости х4.
- **Session Model**: Срочный курьерский контракт длительностью 45–75 секунд на дистанцию 350–700 метров по процедурно сгенерированным крышам с 1–2 развилками риска и моментальным финальным расчетом наград.


---

## 1b. РАМКА ПРОЕКТА: ЧТО ЭТО ЗА ИГРА И ЧЕМ ОНА НЕ ЯВЛЯЕТСЯ


**Направление**: Черепичный Спринт: Чистый Флоу

**Почему выбрано именно оно**: Направление D1 («Черепичный Спринт: Чистый Флоу») выбрано как наиболее точно и выразительно воплощающее замысел пользователя об адреналиновом паркуре курьера. Для мобильных браузеров это идеальный формат: управление одним пальцем на базе свайпов и удержания без виртуальных кнопок интуитивно понятно с первой секунды. Направление обеспечивает мгновенный вход в состояние потока (Flow State), легкую процедурную геометрию на Three.js со стабильными 60 FPS, быстрое определение коллизий через BVH и мощную игровую петлю «чище трюки — быстрее доставка — выше чаевые».

**Узнаваемая сцена**: Вид от третьего лица из-за плеча курьера в кульминационной точке прыжка над десятиметровым разрывом между двумя мансардами: в косых лучах заходящего золотого солнца ярко блестит влажная терракотовая черепица и медные флюгеры, курьер в развевающемся твидовом плаще вытягивает руку к кованому карнизу соседнего здания, бережно прижимая к груди хрупкую светящуюся стеклянную колбу с посылкой, а далеко внизу в глубокой синей тени переулка виднеются миниатюрные крыши экипажей и зажженные газовые фонари.

**Без чего проект перестаёт быть собой** (реализовать обязательно):
- Инерционная шкала целостности посылки (Parcel Integrity): удары о препятствия и жесткие падения без амортизации повреждают груз и снижают оплату заказа.
- Управление одной рукой естественными жестами (One-Thumb Flow): свайпы вверх/вниз и тап-удержание без наэкранных джойстиков и виртуальных кнопок.
- Процедурная генерация маршрута по крышам из модульных архитектурных блоков (скаты, мансарды, вентиляционные трубы, карнизы, бельевые веревки) с гарантированной проходимостью.
- Множитель непрерывного темпа (Flow Combo Multiplier): преодоление препятствий в идеальный тайминг без торможения разгоняет курьера и увеличивает финальные чаевые.
- Фатальность падения на улицу: промах мимо крыши или срыв с карниза приводит к падению в переулок и провалу текущего контракта.

**ЗАПРЕЩЕНО ВВОДИТЬ** — даже если спецификация где-то умалчивает и даже если «так принято в жанре»:
- Клон Subway Surfers или Temple Run с тремя дискретными полосами движения и перестроением по сетке
- Виртуальный экранный джойстик и наэкранные кнопки управления
- Боевые механики: враги на крышах, драки, холодное или огнестрельное оружие и стрельба
- Рогаликовый выбор из трех случайных карт навыков посреди забега (шаблон Survivor)
- Невидимые стены и мгновенная гибель без физического срыва в пропасть
- Серые нетекстурированные кубы (blockout) вместо стилизованной европейской архитектуры старого города (черепица, медь, кирпич, мансарды)
- Оторванная от темы мета-прокачка статов (+5% к скорости) вместо курьерских контрактов разной сложности и кастомизации экипировки
- волны врагов на арене
- окно выбора из трёх карт апгрейда
- универсальная связка «дэш + парирование + удар»
- заимствование Vampire Survivors / Brotato как основы
- джойстик слева + кнопки атаки справа как раскладка по умолчанию
- серые кубы вместо стилизованной геометрии
- мета-прогрессия «золото за забег -> постоянные апгрейды» без связи с темой

Если по ходу реализации кажется, что без запрещённого элемента игра не работает — это находка для `DEVLOG.md`, а не разрешение его добавить.

**Рассмотренные и отклонённые направления** (не возвращай их через механики):
- Направление D2 («Крышный Траекторщик: Прыжок в Окно») отвергнуто, так как пошаговое натяжение траекторий (slingshot) с паузами Bullet Time разрушает непрерывный ритм паркура и ощущение стремительной доставки, превращая динамичный экшен в медленную баллистическую головоломку.
- Направление D3 («Балансир Верхолаз: Экспресс на Карнизе») отвергнуто из-за перегруженности управления на сенсорном экране (одновременное выравнивание центра тяжести и поочередный перехват карнизов), высокого порога входа для мобильных игроков и избыточной вычислительной нагрузки от физики Active Ragdoll в браузере.

---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite
- **Renderer**: **THREEJS** (^0.185.1)
  - *Selection Rationale*: Three.js + Rapier3D: пространственный геймплей, физика и освещение. Перспективная камера.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat ^0.20.0)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Web Audio API (Howler.js with html5: false)
- **State Management**: Reactive EventBus / Micro-Store (Zustand-like zero-dep)
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 65
- **Max Triangles / Active Sprites**: < 42000
- **Max Bundle Size**: < 3.8 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Получение курьерского контракта на доставку хрупкой посылки -> Скоростной паркур-забег по процедурным крышам с таймингом прыжков, подкатов и амортизации приземлений -> Доставка груза на балкон заказчика -> Подсчет гонорара и чаевых за скорость и сохранность (Flow Multiplier & Integrity) -> Кастомизация снаряжения (сумки с амортизацией, нескользящая обувь) и открытие новых сложных районов города.
```

**Фирменный момент**: Затяжной прыжок над 12-метровым провалом между часовой башней и мансардой на фоне багрового заката: игрок зажимает экран за 0.2 секунды до медного ската, курьер в идеальной группировке впечатывается в наклонную черепицу, выполняет молниеносный перекат с высеканием медных искр, сохраняя 100% целостности вибрирующей алхимической колбы, мгновенно разгоняется до 22 м/с и взлетает на х4 Flow-множителе прямо в узкую террасу заказчика под звонкий свист пара и осыпание черепичной крошки.

**Чем петля отличается от соседей по жанру**: В отличие от классических раннеров, где препятствие — это мгновенная смерть или замедление, в «Черепичном Спринте» каждое препятствие — это источник потенциальной кинетической энергии при правильном тайминге. Главный конфликт механики: максимальная скорость создаёт разрушительную перегрузку (G-force) для хрупкого груза в рюкзаке, требуя филигранной амортизации каждого приземления и подката. Ошибка не просто останавливает персонажа, а физически деформирует/разбивает посылку, снижая гонорар от богатых лордов города.

**Шаблон жанра, который НЕ реализуем**: Отвергнут шаблон 3-полосного коридорного раннера (Subway Surfers / Temple Run) с дискретными боковыми перестроениями по дорожкам и автоматическим сбором парящих золотых монет. В нашей игре пространство крыш непрерывно и трёхмерно (smooth 3D traversal с физикой уклонов и высот Rapier3D), а мотивация исходит не из сбора рассыпанных монет, а из сохранения физического груза (parcel integrity) и удержания непрерывного кинетического импульса (flow-состояния) через тайминг взаимодействия с геометрией архитектуры.

**Схема петли**:
```text
+-----------------------------------------------------------------------+
|                 ГИЛЬДИЯ КУРЬЕРОВ (ДИСПЕТЧЕРСКАЯ)                     |
|      [Выбор контракта: дистанция 500м, тип груза: Эфирная Колба]      |
+-----------------------------------------------------------------------+
                                  |
                                  v
+-----------------------------------------------------------------------+
|                      КРЫШИ ВИКТОРИАНСКОГО ГОРОДА                      |
|                                                                       |
|  +-------------------> [ РАЗГОН ПО ЧЕРЕПИЦЕ ] --------------------+  |
|  |                     (12 м/с -> 20 м/с, Flow x1)                |  |
|  |                                  |                             |  |
|  |                                  v                             |  |
|  |                      [ ЗОНА ОТРЫВА КАРНИЗА ]                   |  |
|  |                     (Тап / Свайп вверх: Прыжок)                |  |
|  |                                  |                             |  |
|  |                                  v                             |  |
|  |                   [ ФАЗА ПОЛЕТА НАД ПРОВАЛОМ ]                 |  |
|  |                                  |                             |  |
|  |             +--------------------+--------------------+        |  |
|  |             |                                         |        |  |
|  |             v (Недолет до крыши)                      v        |  |
|  |   [ЗАЦЕП ЗА МЕДНЫЙ КАРНИЗ]                [ЗАЖАТИЕ В ВОЗДУХЕ]  |  |
|  |   (Свайп вверх: рывок наверх)             (Hold: Группировка)  |  |
|  |             |                                         |        |  |
|  |             v                                         v        |  |
|  |   [ПОТЕРЯ ТЕМПА / СТОП FLOW]              [АМОРТИЗАЦИЯ ПРИЗЕМЛЕНИЯ] |
|  |             |                             (Перекат, +4.5 м/с,  |  |
|  |             |                              100% Integrity,     |  |
|  |             |                              Flow x2 -> x3 -> x4)|  |
|  |             |                                         |        |  |
|  |             +--------------------+--------------------+        |  |
|  |                                  |                             |  |
|  |                                  v                             |  |
|  |                      [ ПРЕПЯТСТВИЯ НА СКАТЕ ]                  |  |
|  |                 (Дымоходы, Паровые вытяжки, Канаты)            |  |
|  |                                  |                             |  |
|  |             +--------------------+--------------------+        |  |
|  |             |                                         |        |  |
|  |             v (Свайп вниз)                            v (Удар) |  |
|  |   [ПОДКАТ / СЛАЙД ПО СЛАНЦУ]               [ТРЕЩИНА В КОЛБЕ]   |  |
|  |   (Хитбокс 0.6м, искры, ускорение)         (-25% HP, сброс x1) |  |
|  |             |                                         |        |  |
|  +-------------+-----------------------------------------+        |  |
|                                                                       |
+-----------------------------------------------------------------------+
                                  |
            (Финишная черта / Балкон особняка заказчика)
                                  v
+-----------------------------------------------------------------------+
|                    ФИНАЛЬНЫЙ РАСЧЕТ И АПГРЕЙДЫ                        |
|  [Гонорар + Чаевые за Integrity & Flow] -> [Прокачка сумок и обуви]   |
+-----------------------------------------------------------------------+
```

**Микро-петля (посекундно)**:
- **Сканирование рельефа и чтение точки отрыва** (0.3-0.8 с): игрок — Оценка расстояния до края крыши и уклона черепицы, подготовка тапа; игра — Приближение медного карниза со скоростью 16 м/с, подсветка золотистым бликом зоны идеального толчка (0.22 с); решение — Прыгнуть раньше по пологой траектории на нижнюю террасу или сделать поздний затяжной прыжок с разгона на верхний мостик
- **Фаза полёта и воздушная группировка (Hold-to-Cushion)** (0.4-1.2 с): игрок — Зажатие пальца на экране в воздухе за 0.25 с до контакта со скатом крыши; игра — Персонаж поджимает ноги к корпусу, фиксатор рюкзака натягивается, время слегка замедляется (0.8x timescale chromatic focus); решение — Удерживать ли хват для переката или отпустить палец раньше для моментального отскока в подкат под паровую трубу
- **Контакт со скатом и поглощение кинетического удара (Cushion Impact)** (0.2-0.5 с): игрок — Удержание касания в момент соприкосновения с черепицей; игра — Выполнение гладкого переката (Roll), перевод вертикального падения в горизонтальный рывок +4.5 м/с, сохранение 100% Integrity груза; решение — Перейти сразу в подкат по наклонному сланцу свайпом вниз или выпрямиться для набора шага
- **Реакция на низкое препятствие (Dodge / Vent Slide)** (0.3-0.6 с): игрок — Свайп вниз перед вращающейся вытяжкой или бельевой растяжкой; игра — Курьер уходит в низкое скольжение на коленях, хитбокс снижается до 0.6 м, пролёт под трубой с искрами от медных наколенников; решение — Завершить слайд прыжком на конце ската или продлить скольжение для удержания безопасного центра тяжести

**Мезо-петля (этап)**:
- **Вход в квартал и преодоление стартового прямого ската** (5.0-10.0 с): игрок — Точный тайминг первых 3 прыжков и перекатов без потери импульса; игра — Шкала Flow Multiplier заполняется до х2, музыка обогащается медными духовыми и перкуссией, скорость вырастает с 12 до 16 м/с; решение — Сохранять осторожный темп для 100% сохранности колбы или форсировать риск ради бонуса за время
- **Архитектурная развилка повышенного риска (Route Fork)** (8.0-15.0 с): игрок — Выбор траектории: свайп в воздухе в сторону узкого натянутого троса либо падение на широкую, но загроможденную трубами террасу; игра — Камера смещается по вектору маршрута, на тросе генерируются порывы бокового ветра, на террасе — плотный каскад низких вытяжек; решение — Пойти по скоростному воздушному каналу (срезка 6 сек, риск сорваться) или безопасному ломаному маршруту (потеря времени)
- **Критическая связка препятствий квартала (Choke Point)** (5.0-8.0 с): игрок — Серия быстрых действий: прыжок над пропастью -> зацеп за карниз при недолете -> выталкивание вверх -> мгновенный подкат; игра — Проверка физики груза на кавитацию: при идеальном исполнении колба светится лазурным, при ошибке — появляются трещины (-25 HP); решение — Пожертвовать 15% прочности посылки ради срезания дуги прыжка или аккуратно карабкаться с потерей темпа

**Макро-петля (забег)**:
- **Выбор срочного курьерского контракта в Диспетчерской гильдии** (5.0-15.0 с): игрок — Выбор заказа на карте города: оценка типа груза (алхимическая колба, оптические линзы, часовой механизм), дистанции и награды; игра — Отображение параметров: лимит 60 с, хрупкость IV класса, прогноз погоды (туман/ветер), базовый гонорар 150 шиллингов + чаевые; решение — Взять высокооплачиваемый хрупкий контракт под риск разбить груз или стандартную доставку для стабильного заработка
- **Исполнение скоростного забега по крышам (The Delivery Run)** (45.0-75.0 с): игрок — Паркур-забег с непрерывным управлением свайпами и удержанием пальца для амортизации на дистанции 500 м; игра — Процедурный рендеринг крыш Three.js, симуляция физики Rapier3D, расчёт перегрузок груза, отсчёт таймера 60.0 -> 0.0 с; решение — Балансирование между агрессивным спидраном на х4 Flow и сохранением целостности колбы при прыжках с большой высоты
- **Финальное приземление на балкон заказчика и расчет гонорара** (5.0-10.0 с): игрок — Финальный тач-даун в посадочный маркер на веранде особняка; игра — Курьер передает посылку дворецкому; вывод чека: базовый гонорар + бонус за секунды запаса + множитель чаевых за целостность груза; решение — Забрать награду шиллингами или удвоить бонус гильдии просмотром короткого рекламного контракта
- **Мастерская экипировки и прогрессия районов (Workshop & Map)** (10.0-25.0 с): игрок — Инвестирование заработанных шиллингов в пневмо-подвеску сумки, сланцевые подошвы и разблокировку Алхимического квартала; игра — Повышение характеристик (амортизация +15%, окно зацепа +0.05 с), открытие новых контрактов с викторианскими дирижаблями; решение — Прокачать демпфер сумки под более хрупкие грузы или купить ботинки для экстремального сцепления на мокрой черепице

**Кривая напряжения**: Пилообразная нарастающая кривая (Sawtooth Escalation Curve):
1. Фаза старта (0–12 с): Низкое напряжение (2/10). Широкие черепичные платформы, мягкий ритм, игрок входит в состояние потока и разгоняет Flow до х2.
2. Фаза первого каскада (12–30 с): Нарастание до 6/10. Появление чередующихся низких дымоходов (требующих подката) и провалов в 6–8 метров. Напряжение спадает на 2 секунды на плоской террасе после успешной связки.
3. Фаза критического давления (30–55 с): Взлёт до 9/10. Узкие скаты со скользким мокрым сланцем, натянутые бельевые стропы над глубокими улицами, перепады высоты в 12 метров, где ошибка в амортизации грозит мгновенным уничтожением колбы. Окна тайминга сжимаются до 0.16 с.
4. Кульминация финишного рывка (55–65 с): Пик 10/10. Финальный гигантский разрыв между башнями: игрок на предельной скорости 22 м/с обязан совершить идеальный прыжок и зацеп за карниз балкона.
5. Фаза катарсиса (65–70 с): Спад до 0/10. Мягкое скольжение на ковер балкона, звон шиллингов, вздох облегчения курьера и вывод идеального счета сохранности 100%.

**Формулы ядра (реализуй буквально)**:
- `Damage = max(0, (Impact_Velocity_Y - Cushion_Threshold) * 4.5 * (1.0 - Bag_Damping_Stat)) — урон целостности груза при жестком падении без амортизации; при вертикальной скорости падения выше 7.5 м/с без зажатого переката отнимает от 15 до 60 HP целостности из 100 HP.`
- `V_current = clamp(V_base + (Flow_Multiplier - 1) * 2.5 + Slope_Boost - Slide_Drag, 10.0, 24.0) — горизонтальная скорость курьера в м/с в зависимости от текущего множителя потока, уклона ската крыши (Slope_Boost = sin(alpha) * 5.0) и трения подката.`
- `Flow_Tier = min(4, 1 + floor(Consecutive_Perfect_Actions / 3)) — множитель потока (х1, х2, х3, х4), повышающийся за каждые 3 идеально скоординированных действия подряд (Cushion Roll, Ledge Pop-Up, Flue Slide); сбрасывается до х1 при любом столкновении или недолете.`
- `Payout_Shillings = round((Base_Reward + (Time_Remaining_Sec * 3.5)) * (Parcel_Integrity_Percent / 100.0) * (1.0 + (Final_Flow_Tier - 1) * 0.5)) — итоговая выплата за контракт: включает остаток времени, процент сохранности колбы и итоговый ранг мастерства потока.`
- `Grab_Window_Time = clamp(0.20 + Glove_Grip_Bonus - (V_current / 100.0), 0.12, 0.38) — окно времени в секундах, доступное для свайпа вверх при пролёте мимо выступа карниза; уменьшается на высоких скоростях движения.`

**Прогрессия внутри забега**:
- Кинетический разгон маховика курьера (Kinetic Momentum Build-Up): Первые 3 идеальных приземления разгоняют бег с 12.0 м/с до 18.5 м/с, удлиняя дистанцию базового прыжка с 6.5 м до 11.0 м.
- Активация эфирного резонанса колбы (Aetheric Vessel Stabilization): При удержании Flow х3 и выше люминесцентный эфир в посылке входит в фазу стабильного свечения, создавая вокруг курьера аэродинамический кокон, снижающий сопротивление воздуха на 20%.
- Инстинкт крылатой ласточки (Roofer's Hyper-Focus): После 30 секунд чистого бега без повреждений активируется расширенное восприятие: визуальные маркеры оптимальной точки отрыва на карнизах вспыхивают ярко-бирюзовым светом за 15 метров до края.
- Тепловой форсаж медных шарниров (Copper Joint Steam Burst): Накопленный непрерывный поток заряжает паровой клапан на поясе курьера, превращая каждый подкат под вытяжкой в мини-рывок с выбросом пара на +3.0 м/с.

**Мета-прогрессия**:
- Курьерские сумки с гидравлическим демпфером (Hydraulic Suspension Satchels): 5 уровней модернизации ранца (от парусинового мешка до гироскопической подвески с медными амортизаторами), снижающие получаемый грузом урон от жестких контактов на 10%–50%.
- Сапоги с шипованными свинцово-латунными накладками (Lead-Brass Roofer Treads): Улучшение подошв, увеличивающее коэффициент трения на мокром сланце на 40% и расширяющее тайминг-окно зацепа за карниз на 0.08 с.
- Гильдейские лицензии на элитные секторы города (Guild District Warrants): Покупка прав на доставку в опасные богатые кварталы (Мансарды знати -> Алхимический шпиль -> Доки дирижаблей -> Королевский Парящий Мост) с ростом стоимости контрактов в 4 раза.
- Поясные пневматические стабилизаторы (Pneumatic Balance Belt): Снаряжение, дающее право на 1 бесплатное автоматическое гашение критической перегрузки колбы за забег при грубой ошибке приземления.

### Амортизационный перекат по черепице (Cushion Roll & Kinetic Transfer) (CORE)
- **Category**: Ядро двигательной системы и главный генератор наград / сохранения груза
- **Description**: В большинстве раннеров приземление пассивно и автоматизировано. Здесь приземление — это активный QTE-гейт физического сохранения импульса и груза, где игрок физически поглощает вертикальную гравитацию.
- **Player Input**: Тач: Зажатие пальца в любой точке экрана во время фазы падения / Клавиатура: Удержание клавиши Space или Стрелки вниз в воздухе.
- **Technical Complexity**: Medium
- **Решение игрока**: В какой момент зажать палец при падении с высоты: зажать слишком рано — персонаж преждевременно заблокирует ноги и потеряет дальность полета; зажать вовремя — конвертировать вертикальный удар в молниеносный горизонтальный разгон; не зажать — разбить колбу о жесткую черепицу.
- **Состояния**: `IN_AIR_FALLING (свободное падение, набор вертикальной скорости Vy > 6 м/с)`, `HOLD_GROUPING (зажатие ввода, группировка тела курьера, натяжение ремней сумки)`, `PERFECT_ROLL_EXEC (контакт с крышей в окне 0.22 с: гашение удара, горизонтальный буст +4.5 м/с, сохранение 100% HP)`, `EARLY_TUCK_PENALTY (зажатие раньше чем за 0.45 с до земли: курьер приземляется жестко на полусогнутые, потеря скорости -3 м/с)`, `HARD_CRASH_IMPACT (контакт без зажатия: жесткий удар о кровлю, потеря 35 HP груза, сброс Flow до х1, курьер спотыкается на 0.6 с)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Окно идеального зажатия амортизации (Perfect Cushion Window)` = `0.22 с` — Если увеличить до 0.40 с, игра превращается в однокнопочный раннер без чувства мастерства; если уменьшить до 0.12 с, игроки на тачскринах с задержкой ввода 60 Гц будут постоянно разбивать колбу.
  - `Буст горизонтальной скорости при идеальном перекате (Roll Velocity Boost)` = `+4.5 м/с` — При значении ниже +2.0 м/с игрок не чувствует эйфории от сохранения кинетического потока; при значении выше +7.0 м/с камера не успевает за курьером и следующий скат оказывается за границей экрана.
  - `Базовый порог опасной вертикальной скорости (Safe Fall Velocity Cap)` = `7.5 м/с` — Если поднять до 12.0 м/с, механика переката обесценится на малых высотах; если опустить до 4.0 м/с, курьер будет получать урон даже от микро-прыжков.
  - `Штраф к целостности колбы при пропуске амортизации (Hard Impact Damage)` = `30.0 ед` — Значение откалибровано ровно на 3 грубые ошибки за забег (100 HP / 30 = 3 жизни), что держит игрока в постоянном тонусе.
- **Слои отклика**:
  - Визуал/VFX: Золотисто-медный шлейф искр из-под подошв при идеальном кувырке, разлетающиеся осколки черепицы, свечение колбы лазурным ореолом.
  - Звук (Web Audio): Глухой упругий хлопок натянутой кожи и звонкий щелчок медного карабина рюкзака, переходящий в нарастающий свист рассекаемого воздуха (Low-pass filter открывается с 800Hz до 8000Hz).
  - Камера (импульс/зум): Мгновенная динамическая просадка FOV (с 65° до 58°) в момент контакта с последующим кинетическим выстрелом камеры вперед и легким шейком по вертикали (амплитуда 0.15м).
  - UI/Тактильность: Короткая сочная вибрация смартфона (35 мс, pattern: 'medium'), всплывающий текст '+FLOW COMBO' золотым тиснением над шкалой целостности.
- **Режим отказа игрока**: Игрок отвлекается на сбор времени и касается крыши стоя на прямых ногах. Сигнал: оглушительный стеклянный звон трещины, вспышка красного виньетирования экрана, анимация спотыкания курьера и просадка шкалы Integrity на треть.
- **Кривая мастерства**: Новичок на 30-й секунде судорожно жмет экран заранее, получая штраф за раннюю группировку и теряя скорость; эксперт на 10-м забеге жмет ровно за 0.08 с до черепицы под крутым углом, выжимая максимальный разгон до 22 м/с на крутых скатах.
- **Сопротивление игры**: Игра генерирует черепичные скаты под переменными углами (от 15° до 45°), маскирует рельеф дымом из труб и сжимает дистанцию видимости до 25 метров на скоростях выше 18 м/с.
- **Синергии**:
  - Синергия с 'Инерционной гидродинамикой колбы': идеальный перекат обнуляет накопившуюся перегрузку жидкости внутри сосуда.
  - Синергия со 'Скоростным подкатом под трубы': выход из переката позволяет моментально перейти в низкий слайд без потери набранного буста +4.5 м/с.
- **Почему это не жанровый шаблон**: В большинстве раннеров приземление пассивно и автоматизировано. Здесь приземление — это активный QTE-гейт физического сохранения импульса и груза, где игрок физически поглощает вертикальную гравитацию.
- **Псевдокод тика**:
```text
function on_physics_collision_tick(courier, surface, dt):
  if not courier.is_grounded and surface.is_roof:
    if courier.vertical_velocity < -SAFE_VELOCITY_CAP:
      if courier.input_hold_time_before_contact in [0.05, 0.25]:
        courier.trigger_perfect_roll()
        courier.horizontal_velocity += 4.5
        courier.flow_combo_multiplier += 0.5
        vfx.spawn_copper_sparks(courier.feet_position)
      else:
        let damage = (abs(courier.vertical_velocity) - SAFE_VELOCITY_CAP) * 4.5
        courier.parcel_integrity -= damage * (1.0 - courier.bag_defense)
        courier.horizontal_velocity *= 0.5
        courier.flow_combo_multiplier = 1.0
        sfx.play_glass_crack()
```

### Катапультирующий зацеп за карниз (Ledge Grab & Pop-Up Vault) (SECONDARY)
- **Category**: Механика спасения от фатального падения и сохранения темпа при рискованных дальних прыжках
- **Description**: В большинстве платформеров ledge grab — это автоматическая пауза с медленным подтягиванием. Здесь это активный slingshot-механизм (катапульта), конвертирующий потенциальное падение в ускорение при наличии молниеносного рефлекса.
- **Player Input**: Тач: Резкий свайп вверх в зоне триггера карниза (0.25 м от медного отлива) / Клавиатура: Клавиша W или Стрелка вверх в момент пересечения карниза.
- **Technical Complexity**: Medium
- **Решение игрока**: Использовать ли зацеп как аварийный тормоз при недолете, либо намеренно целиться в край крыши по низкой пологой траектории, чтобы срезать дугу полета и катапультироваться наверх свайпом в тайминг.
- **Состояния**: `AIR_FREE_TRAJECTORY (полет по баллистической траектории над уличным провалом)`, `LEDGE_PROXIMITY_TRIGGER (вход коллайдера рук в зону медного отлива глубиной 0.8 м)`, `PERFECT_VAULT_EJECT (свайп вверх в первые 0.18 с: курьер мгновенно забрасывает ноги на крышу и выбрасывает тело вперед со скоростью +6.0 м/с)`, `HANG_STALL_RECOVERY (запоздалый свайп: курьер повисает на руках, карабкается 0.7 с, полный сброс скорости до 5 м/с)`, `MISS_FALL_DEATH (отсутствие ввода: курьер соскальзывает пальцами с меди и падает на мостовую города — Game Over)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Окно идеального выброса на карниз (Perfect Pop-Up Window)` = `0.18 с` — Если сделать 0.30 с, прыжки над пропастями теряют элемент риска; если сделать 0.10 с, зацеп станет невозможным при частоте кадров 30-45 FPS на слабых телефонах.
  - `Импульс катапультирования вперед-вверх (Pop-Up Vault Impulse)` = `6.2 м/с` — Если опустить до 3.0 м/с, игрок после зацепа будет врезаться в первое же препятствие на краю крыши; если задрать до 9.0 м/с, персонаж перелетит всю следующую секцию крыши.
  - `Зона детекции медного карниза (Ledge Detection Bounds)` = `0.85 м` — Откалибровано под габариты персонажа (рост 1.8 м) для визуально правдоподобного захвата ладонями за козырек.
  - `Потеря времени при медленном вытягивании (Hang Stall Time)` = `0.75 с` — Достаточно суровое наказание за неидеальный тайминг, ставящее под угрозу 3-звездочный золотой таймер контракта.
- **Слои отклика**:
  - Визуал/VFX: Яркая бирюзовая вспышка патинированной меди на кромке желоба, срыв капель дождевой воды/пара с карниза, динамическая анимация силового выхода на две руки.
  - Звук (Web Audio): Металлический звонкий лязг хвата кожаных перчаток за листовую медь, резкий шипящий выдох персонажа, нарастающий свист ветра.
  - Камера (импульс/зум): Резкий кинематографический наклон камеры вниз на 12° с акцентом на головокружительную глубину улицы, затем быстрый рывок вверх за траекторией подъема курьера.
  - UI/Тактильность: Двойной тактильный импульс (haptic 'heavy-click' 40 мс при хвате + 'light-pulse' 20 мс при выталкивании), вспышка желтой стрелки вверх по центру экрана.
- **Режим отказа игрока**: Игрок не рассчитывает дистанцию и не делает свайп вверх, полагая, что долетит на крышу ногами. Сигнал: анимация скольжения пальцев по влажной меди, крик курьера, отдаляющаяся крыша и затемнение экрана с надписью 'Срыв в переулок'.
- **Кривая мастерства**: Новичок использует зацеп только как паническое спасение при явном недолете; эксперт строит спидран-траектории через намеренный низкий зацеп за карниз, отменяя высокую параболу прыжка и выстреливая себя вперед на бреющем полете.
- **Сопротивление игры**: Обледенелые и разрушенные карнизы на старых мансардах, где эффективное окно свайпа сужается до 0.12 с, а также чередование карнизов с торчащими острыми громоотводами.
- **Синергии**:
  - Синергия с 'Амортизационным перекатом': идеальный выброс с карниза переводит курьера в низкую позицию, готовую к мгновенному перекату на следующем крутом скате.
  - Синергия с 'Ветровым балансированием': возможность зацепиться за опору натяжного троса в последний момент при сдувании ветром.
- **Почему это не жанровый шаблон**: В большинстве платформеров ledge grab — это автоматическая пауза с медленным подтягиванием. Здесь это активный slingshot-механизм (катапульта), конвертирующий потенциальное падение в ускорение при наличии молниеносного рефлекса.
- **Псевдокод тика**:
```text
function check_ledge_grab_tick(courier, ledge_colliders, input_events):
  for ledge in ledge_colliders:
    if ledge.bounds.contains(courier.hands_hitbox) and courier.is_falling:
      if input_events.has_swipe_up():
        if input_events.time_since_trigger <= 0.18:
          courier.state = STATE_PERFECT_VAULT
          courier.apply_impulse(Vector3(6.2, 3.5, 0))
          vfx.spawn_copper_patina_flash(ledge.position)
          return
        else:
          courier.state = STATE_HANG_CLIMB
          courier.velocity = Vector3(0, 1.2, 0)
          courier.flow_combo_multiplier = 1.0
          return
      courier.trigger_fall_death()
```

### Скоростной подкат под паровые вытяжки (Slate Slide & Flue Duck) (SECONDARY)
- **Category**: Механика уклонения от горизонтальных препятствий и конвертации уклона крыши в ускорение
- **Description**: Подкат здесь не просто кнопка 'проехать под препятствием', а инструмент динамического набора скорости за счет гравитации скатов крыш с глубокой механикой отмены в прыжок на срезе карниза.
- **Player Input**: Тач: Свайп вниз с удержанием пальца для продления скольжения / Клавиатура: Клавиша S или Стрелка вниз.
- **Technical Complexity**: Medium
- **Решение игрока**: Скользить ли в подкате минимальное время ровно под трубой, чтобы быстрее встать на ноги, либо удерживать подкат на крутом сланцевом уклоне, максимизируя гравитационный разгон ценой временной потери прыгучести.
- **Состояния**: `UPRIGHT_RUN (стандартный бег, высота хитбокса 1.85 м)`, `SLIDE_ENTRY (переход в скольжение на коленях за 0.08 с, уменьшение хитбокса до 0.60 м)`, `SLOPE_ACCELERATION (скольжение по скату > 20°: разгон +3.2 м/с²)`, `FLAT_DECELERATION (скольжение по плоской террасе: торможение -4.0 м/с² после 0.8 с)`, `OBSTACLE_COLLISION (столкновение головой с трубой: потеря 25 HP посылки, сброс скорости до 0, откат назад на 2 м)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Высота хитбокса в подкате (Slide Hitbox Height)` = `0.60 м` — Если увеличить до 0.85 м, персонаж будет цеплять низкие вентиляционные трубы и бельевые веревки; если уменьшить до 0.35 м, анимация скольжения станет визуально неправдоподобной (курьер 'лежит пластом').
  - `Ускорение на наклонном сланце (Slope Gravity Acceleration)` = `+3.2 м/с²` — Позволяет на длинных скатах разгоняться до предельных 24 м/с, создавая мощный всплеск адреналина перед обрывом.
  - `Минимальная длительность подката (Min Slide Duration)` = `0.35 с` — Предотвращает высокочастотный спам подката и заставляет игрока осмысленно выбирать момент входа.
  - `Окно отмены подката в прыжок (Slide-to-Jump Cancel Window)` = `0.15 с` — Дает возможность мастерского резкого выпрыгивания из подката прямо с края крыши для максимальной дистанции полета.
- **Слои отклика**:
  - Визуал/VFX: Сноп белых и медных искр от наколенников курьера по сланцу, плотный шлейф пара из рассекаемых выхлопов вытяжек, разлетающиеся капли сажи.
  - Звук (Web Audio): Скрежет металла по камню и мокрой сланцевой плитке (высокочастотный фрикционный шум 4-6 kHz), сопровождаемый резким шипением горячего пара над головой.
  - Камера (импульс/зум): Камера опускается ближе к кровле (с высоты 2.2 м до 0.9 м), создавая усиленный эффект параллакса и гипертрофированное ощущение скорости за счет близости черепицы.
  - UI/Тактильность: Непрерывная низкоамплитудная вибрация на протяжении всего скольжения (haptic continuous buzz), мгновенный щелчок при успешном проходе под трубой.
- **Режим отказа игрока**: Игрок поздно делает свайп вниз или отпускает его раньше времени: голова курьера врезается в чугунную трубу. Сигнал: звонкий металлический бум (dull clang), курьер отлетает назад, экран покрывается сажей на 0.5 с, колба теряет 25 HP.
- **Кривая мастерства**: Новичок делает подкат за 2 секунды до препятствия на ровной поверхности и теряет всю скорость от трения; эксперт входит в подкат на самом гребне крутого ската, разгоняется в узком зазоре под трубой и выпрыгивает с края с максимальным импульсом.
- **Сопротивление игры**: Двойные трубы с переменным клиренсом (высокая -> низкая) и внезапные паровые прорывы из вентиляции, снижающие видимость пространства под трубой.
- **Синергии**:
  - Синергия с 'Инерционной гидродинамикой': при подкате центр тяжести курьера снижается, что стабилизирует жидкость в колбе и временно защищает её от боковой тряски.
  - Синергия с 'Амортизационным перекатом': возможность войти в подкат сразу из завершающей фазы переката без единого промежуточного шага.
- **Почему это не жанровый шаблон**: Подкат здесь не просто кнопка 'проехать под препятствием', а инструмент динамического набора скорости за счет гравитации скатов крыш с глубокой механикой отмены в прыжок на срезе карниза.
- **Псевдокод тика**:
```text
function update_slide_tick(courier, ground_angle, input_events, dt):
  if courier.is_sliding:
    if ground_angle > 15.0:
      courier.horizontal_velocity += 3.2 * sin(ground_angle) * dt
      vfx.emit_sparks_continuous(courier.knees_position)
    else:
      courier.horizontal_velocity -= 4.0 * dt
    if input_events.has_swipe_up() and courier.slide_time >= 0.15:
      courier.exit_slide_into_super_jump(jump_boost=1.2)
      return
    if courier.check_head_collision():
      courier.take_impact_damage(25.0)
      courier.exit_slide_knockback()
```

### Инерционная гидродинамика алхимической колбы (Aetheric Parcel Fluid Inertia) (SECONDARY)
- **Category**: Главный регулятор риска, балансир жадности/осторожности и основа экономической оценки забега
- **Description**: Вместо стандартного таймера или статичного HP персонажа, здесь здоровье груза является динамической физической переменной, реагирующей на перегрузки в реальном времени, превращая раннер в высокоскоростную доставку хрупкого сокровища.
- **Player Input**: Пассивно-реактивная механика, управляемая через чистоту выполнения всех активных действий (тайминги приземлений, плавность траекторий, отсутствие резких вертикальных перегрузок).
- **Technical Complexity**: Medium
- **Решение игрока**: Рисковать ли поддержанием предельного темпа 22 м/с на неровном ландшафте (где малейшая микро-колебание угрожает кавитацией и взрывом колбы), либо сознательно сбросить темп на опасном участке ради гарантированных 100% чаевых.
- **Состояния**: `STABLE_EQUILIBRIUM (колебания жидкости < 15%, колба светится ровным лазурным светом, множитель чаевых 3.0x)`, `TURBULENT_SLOSHING (колебания 15–50%, жидкость пенится от неидеальных приземлений, предупреждающее мерцание)`, `CAVITATION_STRESS (колебания > 50%, нарастание давления пара, появление микротрещин на стекле, -2 HP/с)`, `CRITICAL_FRACTURE (удар силой > 12G: мгновенная потеря 30–50 HP, выплескивание светящегося эфира)`, `CATASTROPHIC_BURST (целостность 0 HP: колба детонирует безвредным для курьера облаком пара, контракт провален)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Запас прочности алхимической колбы (Max Parcel Integrity)` = `100.0 ед` — Базовый пул здоровья груза; если сделать 200 ед, ошибки перестанут волновать игрока; если сделать 50 ед, ни один мобильный игрок не дойдет до финиша.
  - `Порог критической перегрузки кавитации (Cavitation G-Force Threshold)` = `3.8 G` — При превышении вертикального ускорения 3.8G (жесткий удар о крышу без амортизации) жидкость бьет по стенкам колбы изнутри.
  - `Скорость естественного успокоения жидкости (Fluid Stabilization Rate)` = `18.0 %/с` — Определяет, как быстро колба возвращается в стабильное состояние при плавном беге курьера в ритме Flow.
  - `Бонусный множитель чаевых за 100% сохранность (Pristine Tip Multiplier)` = `3.0x` — Главный экономический стимул для хардкорных игроков и спидраннеров стремиться к совершенству исполнения.
- **Слои отклика**:
  - Визуал/VFX: 3D-модель колбы на рюкзаке с анимированным шейдером жидкости (wobble shader), меняющей цвет от спокойного лазурного до тревожного оранжево-пурпурного при турбулентности; сетка трещин на стекле колбы.
  - Звук (Web Audio): Реалистичный плеск тяжелой жидкости внутри стеклянного сосуда (булькающие резонансные обертоны 300–1200 Hz), звонкий нарастающий звон вибрирующего хрусталя при кавитации.
  - Камера (импульс/зум): При критическом ударе по колбе — легкая хроматическая аберрация по краям экрана и микро-задержка времени (hitstop 45 мс) для осязаемости физического удара.
  - UI/Тактильность: Индикатор целостности в виде викторианского барометра/манометра в левом верхнем углу; при падении Integrity — серийная тревожная вибрация (3 коротких импульса по 20 мс).
- **Режим отказа игрока**: Игрок развивает огромную скорость, но совершает подряд 2 жестких приземления без амортизации и цепляет карниз. Сигнал: оглушительный взрыв стекла за спиной, синий фонтан пара, звук свистка диспетчера и надпись 'Груз уничтожен'.
- **Кривая мастерства**: Новичок постоянно трясет посылку, донося ее с 15–30% HP и получая копейки; мастер бежит на предельной скорости 22 м/с, идеально нейтрализуя каждый гравитационный импульс и принося колбу в состоянии 100% Pristine.
- **Сопротивление игры**: Процедурные перепады высот крыш с глубокими провалами до 15 метров, где неизбежна высокая скорость свободного падения, требующая ювелирного расчета времени амортизации.
- **Синергии**:
  - Синергия со всеми активными механиками: перекат, подкат и зацеп являются единственными инструментами нейтрализации турбулентности жидкости.
  - Синергия с 'Множителем потока (Flow)': удержание стабильности колбы подпитывает шкалу Flow, не давая множителю сгорать на ровных участках.
- **Почему это не жанровый шаблон**: Вместо стандартного таймера или статичного HP персонажа, здесь здоровье груза является динамической физической переменной, реагирующей на перегрузки в реальном времени, превращая раннер в высокоскоростную доставку хрупкого сокровища.
- **Псевдокод тика**:
```text
function simulate_fluid_dynamics_tick(parcel, courier_accel, dt):
  let current_g_force = courier_accel.length() / 9.81
  if current_g_force > CAVITATION_THRESHOLD:
    parcel.turbulence = clamp(parcel.turbulence + (current_g_force - CAVITATION_THRESHOLD) * 25.0 * dt, 0.0, 100.0)
  else:
    parcel.turbulence = max(0.0, parcel.turbulence - STABILIZATION_RATE * dt)
  if parcel.turbulence > 75.0:
    parcel.integrity -= 2.0 * dt
    vfx.emit_glass_stress_particles(parcel.position)
  if parcel.integrity <= 0.0:
    courier.trigger_delivery_failure(reason='PARCEL_DESTROYED')
```

### Ветровой баланс на подвесных бельевых стропах (Skyline Cable-Sprint & Gale Balance) (SECONDARY)
- **Category**: Механика скоростных воздушных срезных путей с высоким риском срыва от бокового ветра
- **Description**: Канатные переходы превращают линейный раннер в динамический тест на микро-балансировку на головокружительной высоте, вознаграждая смелость максимальной прибавкой к таймеру.
- **Player Input**: Тач: Микро-свайпы влево/вправо для компенсации крена от бокового ветра во время бега по канату / Клавиатура: Клавиши A/D или Стрелки влево/вправо.
- **Technical Complexity**: Medium
- **Решение игрока**: Свернуть ли на узкий канат между особняками ради срезания 8 секунд дистанции и получения бонуса х4 Flow, рискуя быть сдутым порывом ветра, или остаться на широкой, но длинной черепичной кровле.
- **Состояния**: `CABLE_MOUNT (запрыгивание на канат, фиксация ног на тросе)`, `SUPER_FLOW_SPRINT (сверхскоростной спринт по тросу со скоростью 20.0 м/с, удвоенный набор очков)`, `WIND_GUST_IMPACT (порыв бокового ветра 8.5 м/с, отклоняющий ось баланса курьера)`, `BALANCE_RECOVERY (успешный контр-свайп навстречу ветру, стабилизация положения)`, `BLOWN_OFF_FALL (крен превысил 35°: курьер теряет баланс и срывается в пропасть переулка)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Базовая скорость спринта по канату (Cable Sprint Velocity)` = `20.5 м/с` — Дает ощутимое превосходство в скорости над обычным бегом по крыше (15.0 м/с), создавая огромную ценность рискованного пути.
  - `Сила бокового порыва ветра (Crosswind Gust Force)` = `8.5 м/с²` — Если увеличить до 14.0 м/с², удержание баланса на тачскрине станет слишком хаотичным; если уменьшить до 4.0 м/с², игрок будет пробегать канаты не обращая внимания на ветер.
  - `Критический угол срыва с каната (Max Balance Tilt Angle)` = `35.0 градусов` — Пороговый угол наклона торса, после которого физический движок сбрасывает персонажа с троса.
  - `Окно реакции на порыв ветра (Wind Counter-Action Window)` = `0.28 с` — Справедливое окно реакции для мобильного игрока при внезапном шквале ветра.
- **Слои отклика**:
  - Визуал/VFX: Потоки белого тумана и развевающиеся простыни на бельевых веревках, наклон тела курьера с динамическим размахиванием руками для удержания равновесия, искры от трения подошв о стальной витой трос.
  - Звук (Web Audio): Нарастающий низкий гул шквального ветра (White noise bandpass 200-800 Hz), свистящий звон натянутой струны троса под ногами, хлопанье мокрой ткани на ветру.
  - Камера (импульс/зум): Динамический крен камеры по оси Roll на угол наклона курьера (до ±15°), легкий зум назад для демонстрации высоты над улицей.
  - UI/Тактильность: Нарастающая вибрация смартфона в сторону крена, появление дугового индикатора гироскопа над головой персонажа.
- **Режим отказа игрока**: Игрок игнорирует порыв ветра и не делает компенсирующий свайп: курьер заваливается на бок и падает с каната. Сигнал: крик, резкий срыв камеры вниз, треск лопнувшей веревки, экран Game Over.
- **Кривая мастерства**: Новичок боится заходить на канаты и оббегает их по длинной дуге крыш; эксперт целенаправленно ищет канаты, на ходу парируя микро-свайпами порывы ветра и сохраняя максимальную скорость 22 м/с.
- **Сопротивление игры**: Внезапные парные порывы ветра переменного направления (слева направо, затем справа налево) и болтающиеся на тросе тяжелые викторианские ковры, требующие прыжка прямо на канате.
- **Синергии**:
  - Синергия с 'Катапультирующим зацепом': сход с каната часто завершается прыжком на дальний карниз, требуя немедленного зацепа.
  - Синергия с 'Инерционной гидродинамикой': бег по канату очень плавный (нет ударов о ступени), что позволяет стабилизировать жидкость в колбе при условии удержания баланса.
- **Почему это не жанровый шаблон**: Канатные переходы превращают линейный раннер в динамический тест на микро-балансировку на головокружительной высоте, вознаграждая смелость максимальной прибавкой к таймеру.
- **Псевдокод тика**:
```text
function update_cable_balance_tick(courier, wind_vector, input_events, dt):
  if courier.is_on_cable:
    courier.tilt_angle += (wind_vector.x * 12.0) * dt
    if input_events.has_swipe_counter_wind():
      courier.tilt_angle *= 0.2
      vfx.emit_balance_spark(courier.feet_position)
    if abs(courier.tilt_angle) >= MAX_BALANCE_ANGLE:
      courier.fall_from_cable_into_abyss()
      return
    courier.horizontal_velocity = CABLE_SPRINT_VELOCITY
    courier.flow_combo_multiplier = min(4.0, courier.flow_combo_multiplier + 0.1 * dt)
```


---

## 3b. ⚠️ СТРОГИЙ ЗАПРЕТ ЖАНРОВЫХ ШАБЛОНОВ И КЛОНОВ (CRITICAL ANTI-CLICHÉ RULES)
Кодовый агент ОБЯЗАН реализовать уникальную игру, спроектированную в этом ТЗ, а не шаблонный автошутер:
1. **ЗАПРЕТ ШАБЛОННЫХ РОГАЛИКОВ И КАРТОЧЕК**: Если игра прямо не требует карточный драфт в GDD, **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** добавлять спам волн врагов и всплывающее окно «Выберите 1 из 3 карт апгрейда».
2. **ЗАПРЕТ СЕРЫХ ПРИМИТИВОВ НА ПУСТОЙ ПЛОСКОСТИ**: Создавай выразительную процедурную низкополигональную 3D/2D графику, точно соответствующую `ART_DIRECTION.md` (тематические персонажи, рельеф, модули, объекты окружения, частицы), а не бегающий куб на сером полу.
3. **ТОЧНОЕ СОБЛЮДЕНИЕ МЕХАНИК**: Реализуй все состояния, формулы, тайминги и слои отклика из `MECHANICS.md` (включая Web Audio звуки, импульсы камеры, визуал и тач-инпут).
4. **СПЕЦИФИЧЕСКОЕ УПРАВЛЕНИЕ**: Реализуй схему управления под конкретную механику этой игры (свайпы, жесты, траектории, физический дрифт, прицеливание), а не стандартный стик.

---

## 3c. ГОТОВОЕ: ЧТО УЖЕ НАПИСАНО И ОТЛАЖЕНО — SHOULD

Ниже — рабочий код фабрики: не примеры из статьи, а модули, которые крутились в
живых сценах, и в которых числа выставлены не наугад. Каждый файл объясняет в
своей шапке, почему константы именно такие — это та часть работы, которую с
нуля не воспроизвести за один заход.

**Правило.** Прежде чем писать механику с нуля, посмотри, нет ли её здесь.
Нашёл — тяни файл и подгоняй под свою игру. Не нашёл или не подходит — пиши
сам, но запиши в `DEVLOG.md` строку «взял/не взял <файл>, потому что …».
Молча проигнорировать готовое нельзя: это стоило нам рэгдолла, который писали
заново, хотя он лежал написанным.

Загрузка любого файла — одной командой, путь берётся из таблицы:

```bash
node scripts/fetch-knowledge.mjs workspace/knowledge-showcase/src/game/stealthSensing.ts
```

Файл появится в `docs/ref/<путь>`, оттуда копируй в `src/`.

Колонка «Готовность» — это часы работы, а не оттенок смысла:
- **копируется как есть** — ни одного импорта, чистая логика и числа. Взял, переименовал, поехал.
- **нужен three, больше ничего** — тянет только `three`, чужого проекта в нём нет.
- **образец, переписать под себя** — тянет модули стенда. Читать как показ того, как это собирается, а не копировать.

**Похоже, пригодится именно этой игре:**

| Файл | Что даёт | Строк | Готовность |
|---|---|---|---|
| `workspace/knowledge-showcase/src/game/fightingMoves.ts` | Фрейм-дата: единственный источник баланса файтинга | 412 | копируется как есть |
| `workspace/knowledge-showcase/src/game/meleeCombat.ts` | Ближний бой: комбо-цепочка, окна отмены, парирование, hit-stop | 330 | копируется как есть |
| `workspace/knowledge-showcase/src/game/survivorRun.ts` | Забег в survivor-игре: кривая опыта, карточки апгрейдов, эскалация орды | 279 | копируется как есть |
| `workspace/knowledge-showcase/src/game/fluidPhysics.ts` | Fluid Buoyancy, Mining Drill & Physics Destruction logic module | 244 | копируется как есть |
| `workspace/knowledge-showcase/src/game/towerDefense.ts` | Tower defense: контракт волн, приоритет целей, экономика | 225 | копируется как есть |
| `workspace/knowledge-showcase/src/game/arcadeCar.ts` | Аркадная модель машины — общая для игрока и соперников | 201 | копируется как есть |
| `workspace/knowledge-showcase/src/game/stealthSensing.ts` | Стелс: конус зрения, шкала подозрения, шум | 145 | копируется как есть |
| `workspace/knowledge-showcase/src/game/fightAnimStates.ts` | Список анимаций бойца в одном месте: чем вызывается и сколько длится | 131 | копируется как есть |
| `workspace/knowledge-showcase/src/world/boxerRagdoll.ts` | Рэгдолл ИЗ САМОГО ПЕРСОНАЖА, а не из капсул-заменителей | 408 | образец, переписать под себя |
| `workspace/knowledge-showcase/src/world/ragdoll.ts` | Рэгдолл на Rapier: семь тел, шесть сферических суставов | 214 | образец, переписать под себя |

Это не весь каталог: всего готовых модулей 67. Полный список с описаниями — в `LIBRARY.md` рядом с этим файлом. Загляни туда, когда возьмёшься за механику, которой нет в таблице выше.

---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Core / Platform Layer**: 
- **Render / Scene Layer**: 
- **Physics / Collision Layer**: 
- **Gameplay Systems Layer**: 
- **Audio Layer**: 
- **UI / HUD Layer**: 

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   └── PhysicsWorld.ts        # Rapier3D / Physics world manager & colliders
├── entities/
│   ├── Player.ts              # Player entity & input handling
│   └── EntityManager.ts       # Dynamic entity pool & lifecycle
├── systems/
│   ├── ParcelIntegritySystem.ts # Отслеживание физического состояния груза, рас
│   ├── RooftopProceduralGeneratorSystem.ts # Бесшовная сборка бесконечной полосы разнообра
│   ├── FlowComboSystem.ts       # Стимулирование непрерывного скоростного движе
│   ├── GuildContractDispatchSystem.ts # Управление мета-игрой: генерация заказов разн
├── rendering/
│   ├── SceneManager.ts        # Three.js scene graph, lights, perspective camera lerp
│   ├── ProceduralModels.ts    # Styled models / geometry for Черепичный Спринт: Чистый Флоу
│   └── ParticleSystem.ts      # Particle effects & visual feedback
├── ui/
│   ├── theme.css              # Единственное место со значениями: цвета, шрифты, шкала, слои
│   ├── UiRoot.ts              # Слои над канвасом, измеренный вьюпорт, safe-area + баннер
│   ├── ScreenRouter.ts        # Экраны как автомат: один видимый, один общий переход
│   ├── Hud.ts                 # Запись в закэшированные узлы только при изменении значения
│   ├── components/            # Button, IconButton, Panel, Modal, Toast, Meter, Stat, ListRow
│   ├── screens/               # По файлу на экран, каждый — со своими loading/empty/error
│   ├── icons.ts               # Один инлайновый SVG-спрайт, цвет через currentColor
│   └── TouchControls.ts       # Mobile touch input adapter
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic audio feedback
```

### Контракт модулей — MUST

Карта выше говорит, какие файлы завести. Ниже — чего им нельзя, потому что
именно на этом ломаются сгенерированные проекты:

- **DOM создаётся только в `src/ui/`.** Ни один файл вне `src/ui/components/` и
  `src/ui/screens/` не вызывает `document.createElement` и не присваивает
  `element.style.*`. Экран, собранный инлайновыми стилями в обход `theme.css`, —
  это второй экран, который перестанет совпадать с первым.
- **Значения — только в `src/ui/theme.css`.** Проверяется **B1**.
- **`src/core/` не знает про площадку.** Playgama Bridge живёт в
  `src/platform/`; всё остальное общается с ним через свой интерфейс. Иначе
  игру нельзя запустить локально без SDK.
- **`src/ui/` не знает про физику и рендер.** Интерфейс читает состояние
  через `EventBus` и не держит ссылок на `THREE.*` и на мир Rapier.
- **Числа игры — из `balance.yaml`.** Литерал скорости, урона, тайминга или
  радиуса в коде — баг: правка баланса не должна быть правкой кода.
- **Цикл обновления не аллоцирует.** Ни `new`, ни литерала объекта, ни
  `array.map` в кадре: пулы заводятся заранее. Проверяется **E4**.
- **Каждый модуль экспортирует один публичный класс или функцию** и не тянет
  импорт из соседнего слоя мимо своего интерфейса.

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Страховка Гильдии (Второе дыхание) (`courier_insurance`)**: Мгновенное восстановление разбитой посылки до 100% целостности и продолжение текущего забега с сохранением прогресса (Trigger: Экран аварии при разрушении груза, Limit: 1 раз за контракт)
- **Королевские чаевые x2 (`double_tips`)**: Удвоение заработанных за доставку шиллингов и очков репутации гильдии (Trigger: Экран победы и расчета контракта, Limit: Неограниченно при успешной доставке)
- **Проба элитной сумки (`master_gear_trial`)**: Временная аренда сумки мастера с повышенным демпфированием на 3 следующих забега (Trigger: Экран мастерской перед сложным контрактом, Limit: 1 раз в 15 минут)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"courier_rank"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Portrait
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Визуальный контракт интерфейса
Интерфейс — первое, что видит игрок: до геймплея он смотрит на меню. Меню,
собранное из умолчаний браузера, обесценивает всё, что сделано за ним, и это
самый заметный признак недоделанной игры. Всё ниже — требование, а не пожелание.

**Материал интерфейса**: Стилизованный винтажный викторианский пергамент с латунными шестернями, золотым тиснением и современными неоновыми акцентами для показателей скорости и целостности

**Визуальный язык**: Высококонтрастные силуэты на фоне теплого заката, латунные и пергаментные элементы интерфейса с четкими бирюзовыми и золотыми индикаторами фокуса.

**Типографика**: Заголовки: стилизованный выразительный шрифт 'Cinzel' / 'Playfair Display' с засечками; интерфейс и цифры: четкий геометрический гротеск 'Montserrat' / 'Inter' Bold.

**Сцена за меню.** Курьер сидит на коньке черепичной крыши на фоне заката, поправляя перчатку, рядом лежит светящаяся посылка; на заднем плане видны силуэты башен и пролетающий дирижабль

Меню, пауза и итог сессии рисуются ПОВЕРХ этой сцены. Непрозрачный слой на весь экран (`background: #111` на корне экрана, полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка допускается только под текстовым блоком и кнопками. Игровой цикл в меню продолжает крутиться на сниженной нагрузке — камера едет, сцена живёт. Игрок обязан понять, во что он играет, до нажатия «Играть»; меню на глухой заливке — самый заметный признак недоделанной игры, и это первое, что увидит и игрок, и модератор площадки.

**Акценты — один цвет = один смысл** (разный цвет у каждой кнопки «чтобы отличались» читается как мусор):

| Токен | Единственный смысл |
|---|---|
| `danger_state` | #FF0054 (неоново-малиновый цвет трещин и критического урона) |
| `interactive_climb` | #48A9A6 (медный маркер доступных карнизов) |
| `safe_state` | #00F5D4 (бирюзовый свет целостности и идеальных действий) |
| `warning_state` | #FFD166 (золотой закатный цвет ускорения и комбо) |

**Набор компонентов — закрытый.** Всё на экране обязано быть одним из них; одноразовый `<div>` с инлайновыми стилями — это баг, из-за которого второй экран перестаёт совпадать с первым:
- IntegrityFlaskWidget
- FlowStreakBadge
- BrassChronometer
- DistrictCard
- DeliveryBillParchment
- ReviveModal

**Якоря HUD** — пять и только пять; посередине экрана не висит ничего, кроме временной обратной связи:

| Якорь | Что там |
|---|---|
| `bottom_center` | Distance-to-Destination Progress Bar |
| `top_center` | Brass Countdown Timer |
| `top_left` | Integrity Flask & Courier Health |
| `top_right` | Flow Multiplier & Pause Button |

**Экраны и их композиция.** У каждого экрана три зоны: чем он себя называет, единственное главное действие (самое крупное и единственное с основным акцентом) и второстепенный ряд одним весом. Карточка с колонкой кнопок по центру — не композиция, а её отсутствие:
- **splash** · Стильный силуэт курьера на фоне закатного солнца с прогресс-баром загрузки ассетов; name: Экран загрузки · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **main_menu** · Интерактивная карта кварталов города, выбор активного контракта, кнопка входа в мастерскую экипировки; name: Главное меню / Карта Гильдии · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **gameplay_hud** · Минималистичный HUD без виртуальных кнопок, максимальный обзор 3D-пространства крыш; name: Игровой экран · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **workshop** · Кастомизация снаряжения (сумки с рессорами, плащи, сапоги), просмотр характеристик и прокачка; name: Мастерская курьера · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **victory_screen** · Сургучная грамота гильдии: время, сохранность посылки, множитель флоу, расчет чаевых, кнопка x2 за Rewarded Video; name: Отчет о доставке (Победа) · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **defeat_screen** · Причина провала (груз разбит / падение в переулок / время вышло), предложение 'Страховки Гильдии' (Revive) или рестарт; name: Аварийный отчет (Поражение) · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

**Состояние, показанное миром, а не оверлеем**:
- Светящаяся колба на груди курьера, визуально трескающаяся в реальном времени в 3D
- Развевающийся плащ курьера, цвет трейлов которого меняется в зависимости от Flow Multiplier
- Дымящиеся трубы и развевающееся белье, отклоняющиеся от направления движения курьера

**Переходы экранов**: Splash -> Main Menu (District Map) -> Gameplay HUD -> [Victory Screen -> Main Menu] / [Defeat Screen -> (Revive -> Gameplay HUD) / Main Menu]

**Отклик интерфейса на действие**:
- Идеальная амортизация: вспышка бирюзового кольца вокруг курьера, надпись 'ИДЕАЛЬНО!', виброотклик 30 мс, сохранение темпа
- Повреждение груза: экранная красная вспышка, звук бьющегося стекла, цифра '-25' урона, виброотклик 80 мс
- Максимальный Флоу x4: золотое пламя по краям экрана, свист ветра в ушах, динамический духовой дроп в музыке
- Успешный финиш: эффект замедления времени (0.3x на 1 секунду), золотые конфетти, фанфары гильдии

**Состояния экранов** — загрузка, пустота и ошибка проектируются наравне с удачным путём: сохранение, таблица лидеров, покупки и реклама на этих площадках отваливаются регулярно:
- Загрузка: анимированный бег курьера по проволочному контуру крыши
- Игра: чистый HUD с адаптацией под Safe Area для устройств с вырезами экрана (iPhone Dynamic Island / Android Notch)
- Пауза: полупрозрачное затемнение с возможностью перезапуска или выхода
- Конец игры: карточка доставки с детальным расчетом наград

**Значения выводятся из этого мира, а не берутся из примеров.** Имена токенов (`--color-primary`, `--font-display`, `--space-*`) одинаковы во всех играх — на них держится код компонентов. Сами значения, обе гарнитуры, силуэт рамки и язык иконок выводятся из материала интерфейса, названного выше, по процедуре из раздела 12 `knowledge/ux/ui_design_system.md`: из чего сделана панель, при каком свете она лежит, чем в этом мире помечают главное и опасное, чем написаны цифры, как режут край. Скопировать палитру, шрифты или фаску из примера в базе знаний или из другой игры — это тот же дефект, что и оставить умолчания браузера: интерфейс выглядит решением, принятым не для этой игры. Обратная проверка: если от интерфейса другой игры фабрики ваш отличается только оттенком акцента — процедура была пропущена.

**Обязательные технические правила интерфейса** (каждое — из починенного бага, подробности в `knowledge/ux/ui_design_system.md` и `knowledge/ux/ui_implementation.md`):
- Все значения — токенами в одном `src/ui/theme.css`: цвета, две гарнитуры, шкала отступов, радиусы, длительности, `--z-*`. Литерал `#RRGGBB` или `z-index: 9999` внутри экрана запрещён; `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` мимо темы обязан быть пуст.
- Слои над канвасом прозрачны для ввода: контейнер — `pointer-events: none`, `auto` включают только листовые интерактивные элементы. Слой HUD не кликается никогда. Полноэкранный оверлей, оставленный на `auto`, съедает игровой ввод — и это выглядит как «на телефоне не работает управление».
- Интерфейс тянется одним множителем `--ui-scale`, а не отдельной вёрсткой под каждый телефон, — но множитель обязан где-то **читаться**. Он объявляется в `theme.css` брейкпоинтами (`:root { --ui-scale: 1 }`, `@media (max-width: 720px)` → `0.86`, `@media (max-width: 420px)` → `0.74`) и умножает отступы и кегли: `padding: calc(var(--space-4) * var(--ui-scale))`, `font-size: clamp(14px, calc(16px * var(--ui-scale)), 18px)`. Посчитать масштаб в JS и не использовать его ни в одном правиле — ровно тот же результат, что не считать вовсе: интерфейс, свёрстанный в фиксированных `px`, на 360-пиксельном экране разъезжается. Ноль `@media` в `theme.css` — признак, что этот пункт пропущен.
- Портрет и ландшафт — два разных экрана, а не один и тот же в другом масштабе. Вертикальный FOV камеры пересчитывается под аспект (иначе в портрете горизонтальный обзор схлопывается в трубу), а раскладка HUD и кнопок проверяется в обеих ориентациях.
- Размеры зон нажатия НЕ масштабируются: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px — палец не уменьшается вместе с экраном. Ниже 460 px по высоте кнопки допускается ужать до 56 px, и это единственное исключение.
- Меняющиеся числа — `font-variant-numeric: tabular-nums` в слоте фиксированной ширины; полосы — `transform: scaleX()`, не `width`. HUD пишет в закэшированные узлы и только при изменении значения: пересборка разметки в кадре стоит кадров.
- Один экран виден за раз, скрытый — `display: none`; переход один на всю игру и укладывается в 300 мс. Анимируются только `transform` и `opacity`. `prefers-reduced-motion: reduce` убирает трансформации.
- Любое нажатие отвечает в том же кадре; действие с ожиданием переводит кнопку в `loading` и снимает его в `finally`.
- Запрещено: `alert`/`confirm`/`prompt`, эмодзи вместо иконок (один инлайновый SVG-спрайт с `currentColor`), голый `<input type=range>` и `<select>`, текст над геймплеем без подложки или обводки, серая неактивная кнопка вместо отсутствующей на площадке возможности — такой элемент не рисуется вовсе.
- Геометрия меню считается от ИЗМЕРЕННОГО вьюпорта (`visualViewport`, пересчёт после поворота, выхода из фуллскрина и закрытия рекламы), а не от `100vh`; нижний отступ включает измеренную высоту липкого баннера. Страница под игрой не скроллится — длинные списки скроллятся во внутреннем контейнере.
- `backdrop-filter: blur()` на полноэкранном слое — самое дорогое свойство оверлея на мобильных GPU; только на маленьких панелях.

### Контракт тач-управления
- **Раскладка этой игры** (спроектирована в MOBILE_CONTROLS.md, переносить сюда раскладку из другой игры нельзя): Экран свободен от виртуальных кнопок: вся область экрана является сенсорной панелью жестов (One-Thumb Flow). Свайп вверх в любом месте — прыжок/зацеп, свайп вниз — подкат, удержание пальца — группировка/амортизация.
- Раскладка выводится из главного действия игрока. Виртуальный джойстик уместен там, где игрок непрерывно ведёт персонажа; там, где действие точечное (тап по цели, свайп, удержание), джойстик — лишний элемент, который занимает половину экрана и ничего не делает.

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Если в раскладке есть стик** — он плавающий: зона захвата — вся левая
  половина экрана, база появляется под пальцем, мёртвая зона 8%, иначе
  управление дрожит. Если раскладка обходится без стика — не добавляйте его.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `WASD` / стрелки — движение
- ЛКМ / `J` — основная атака
- ПКМ / `K` — тяжёлая атака / блок
- `Space` / `Shift` — рывок / уклонение
- `P` / `Esc` — пауза

**Раскладка выше — отправная точка под похожие игры, а не список того, что умеет ЭТА игра.** Профилей в фабрике меньше, чем жанров, и любой шаблон заведомо описывает не вашу игру: в нём есть лишние клавиши и нет нужных. Источник истины — глаголы игрока, перечисленные ниже.

**Контракт: каждый глагол получает и клавишу, и кнопку.** Таблицу «глагол → клавиша → тач-кнопка → что происходит на экране и в звуке» положи в `DESIGN.md` до первой строчки кода по управлению. Глагол без строки в таблице не реализован; клавиша из шаблона, которой в таблице нет, — удаляется.

**Отдельно решается то, чего в списке механик обычно нет, но игрок ждёт по виду уровня.** Вертикаль (прыжок, присед, тихий шаг), взаимодействие с предметом, отмена действия. Каждое — либо сделано (вертикаль — это гравитация, опора лучом по геометрии и высота глаз от ступней, а не константа пола), либо осознанно не сделано, и тогда в `DESIGN.md` стоит строка почему, **а уровень не обещает того, чего игрок не может**: без прыжка на карте нет ящиков, выступов и укрытий по пояс, к которым тянет забраться.

**Глаголы игрока этой игры** (каждому нужна строка в таблице управления):
- **Амортизационный перекат по черепице (Cushion Roll & Kinetic Transfer)** — игрок делает это так: Тач: Зажатие пальца в любой точке экрана во время фазы падения / Клавиатура: Удержание клавиши Space или Стрелки вниз в воздухе.
- **Катапультирующий зацеп за карниз (Ledge Grab & Pop-Up Vault)** — игрок делает это так: Тач: Резкий свайп вверх в зоне триггера карниза (0.25 м от медного отлива) / Клавиатура: Клавиша W или Стрелка вверх в момент пересечения карниза.
- **Скоростной подкат под паровые вытяжки (Slate Slide & Flue Duck)** — игрок делает это так: Тач: Свайп вниз с удержанием пальца для продления скольжения / Клавиатура: Клавиша S или Стрелка вниз.
- **Инерционная гидродинамика алхимической колбы (Aetheric Parcel Fluid Inertia)** — игрок делает это так: Пассивно-реактивная механика, управляемая через чистоту выполнения всех активных действий (тайминги приземлений, плавность траекторий, отсутствие резких вертикальных перегрузок).
- **Ветровой баланс на подвесных бельевых стропах (Skyline Cable-Sprint & Gale Balance)** — игрок делает это так: Тач: Микро-свайпы влево/вправо для компенсации крена от бокового ветра во время бега по канату / Клавиатура: Клавиши A/D или Стрелки влево/вправо.

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/hochu_3d_igru_pro_kurera_na_kryshah_starogo_goro/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ДИЗАЙН: ЕГО ПИШЕШЬ ТЫ — MUST

Готового арт-дирекшена в этом пакете нет намеренно. Дизайн, продиктованный
списком полей, получается одинаковым от игры к игре, и узнать в нём конкретную
игру невозможно. Поэтому первым делом, до кода, ты пишешь `DESIGN.md` сам.

**Что в нём должно быть** (без этих пяти пунктов файл не считается написанным):

1. **Одна фраза о том, как игра выглядит.** Не «мрачный киберпанк», а что
   именно видит игрок в первую секунду.
2. **Палитра** — 5–7 цветов с ролями (фон, поверхность, текст, акцент, опасность,
   успех) и HEX. Они же становятся токенами в `theme.css`.
3. **Камера и свет** — где стоит камера, что она захватывает, откуда светит и
   почему сцена читается на телефоне.
4. **Экраны** — для каждого: что на нём, где три зоны (заголовок, содержимое,
   действие), какое действие главное.
5. **Сцена за меню** — что живёт на фоне главного меню. Не заливка, не блюр
   поверх ничего: работающая сцена игры под пониженной нагрузкой.

**Чего в нём быть не должно** — это уровень **MUST**, проверяется приёмкой:
эмодзи вместо иконок, `alert()` вместо модалки, непрозрачная плашка поверх
канваса, фиолетово-синий градиент, интерфейс из умолчаний браузера.

**Отправная точка от фабрики** — это подсказка, а не приговор. Не подошло —
меняй и запиши в `DEVLOG.md`, что и почему:

- Стиль: Стилизованный викторианский стимпанк с теплым золотым освещением (Golden Hour Steampunk Realism Lite)
- Камера: Third-Person Chase Cam (вид сзади-сверху из-за правого плеча на высоте 2.2м, дистанция 3.8м с динамическим отдалением при ускорении) (FOV 55°, наклон 18°)
- Среда: Крыши европейского старого города XIX века на закате: рельефная терракотовая и сине-серая сланцевая черепица, медные трубы с зеленым налетом патины, кованые флюгеры, кирпичные дымоходы, развевающееся белье на веревках, сизый дымок из труб и золотисто-розовый закатный горизонт
- Свет: Низкое закатное направленное солнце (Directional Light, теплый золотисто-янтарный цвет #FFA447, угол 25° к горизонту) + холодный небесный эмбиент (#394867) для глубоких синих теней в переулках + точечный свет от светящейся колбы (#4DEEEA)
- Тема интерфейса: Стилизованный винтажный викторианский пергамент с латунными шестернями, золотым тиснением и современными неоновыми акцентами для показателей скорости и целостности (реализация — секция 6)
- Сцена за меню: Курьер сидит на коньке черепичной крыши на фоне заката, поправляя перчатку, рядом лежит светящаяся посылка; на заднем плане видны силуэты башен и пролетающий дирижабль

Подробности отбора — в `ART_DIRECTION.md` и `UI_UX_SPECIFICATION.md` рядом.

---

## 8. РОАДМАП

Фаза закончена не тогда, когда код написан, а когда закрыты её проверки из
`ACCEPTANCE.md`. Прогоняй `node scripts/check-spec.mjs` в конце каждой фазы.

| Фаза | Чем закрывается |
|---|---|
| Подготовка | **F4**, **F1** — база загружена в `docs/ref/`, `DESIGN.md` написан |
| Каркас и площадка | **A1**, **A3**, **C1–C4** — игра собирается и стартует на площадке |
| Главная механика | **D1**, **B6** — в неё можно играть, ввод доходит до игры |
| Остальные механики и петля | **D2–D5**, **B9–B11** — петля проходится целиком |
| Интерфейс и подача | **B1–B5**, **B7–B8**, **B12** — интерфейс собран из токенов, меню стоит на живой сцене |
| Полировка и релиз | **C5–C11**, **E1–E5**, **A2**, **A4**, **F2–F3** — сохранения, реклама, кадры, сборка |

### Phase 1: Прототип ядра движения и BVH-коллизий (2 days)
- **Deliverable**: Играбельный прототип с непрерывным бегом, прыжками и таймингом приземлений
  - Настройка Three.js сцены и камеры слежения от 3-го лица
  - Реализация кинематического контроллера курьера (бег, гравитация, свайпы)
  - Интеграция three-mesh-bvh и базовых наклонных крыш
  - Механика амортизации приземления (Cushion Roll)
### Phase 2: Система посылки, генератор крыш и Флоу (2 days)
- **Deliverable**: Полноценный геймплейный цикл забега с хрупким грузом и процедурным городом
  - Разработка Parcel Integrity System с нанесением урона и визуалом трещин
  - Процедурный генератор крыш из 12 модулей с развилками
  - Система множителя скорости Flow Combo x1..x4
  - Зацеп за карнизы и подкаты под трубы
### Phase 3: Арт, освещение, VFX и викторианская эстетика (2 days)
- **Deliverable**: Финальный визуальный стиль заката старого города со стабильными 60 FPS
  - Интеграция стилизованных моделей черепицы, мансард, дымоходов и флюгеров
  - Настройка низкого закатного освещения и объемного тумана
  - Создание партиклов (пыль, искры, трейлы плаща, дым труб)
  - Стилизованный 3D-персонаж курьера с анимациями
### Phase 4: UI/UX, звук, Playgama Bridge и полировка (2 days)
- **Deliverable**: Релизная версия игры, полностью готовая к публикации на площадках
  - Винтажный викторианский пергаментный интерфейс и адаптивный HUD
  - Подключение Web Audio API, звуковых эффектов и адаптивной музыки
  - Интеграция Playgama Bridge SDK (сохранения, лидерборды, реклама)
  - QA-тестирование на мобильных устройствах и балансировка таймингов

---

## 9. ПРАВИЛА ПЛОЩАДКИ — УРОВЕНЬ MUST
Каждое правило ниже — след бага, дошедшего до продакшена, или отказа модерации.
Они перекрывают любую привычку, туториал и пример — включая куски из документации
Playgama и Яндекса, многие из которых описывают устаревший контракт Bridge v1.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

### Orientation & pointer lock

69. `Object3D.lookAt` aims the object's **+Z** at the target; −Z is the convention
    for cameras and lights only. Geometry that will later be aimed with `lookAt`
    (viewmodel arms, tracers, arrows, turret beams) must be built along **+Z**.
    Built along −Z it points backwards after aiming, and the bug reads as "broken
    model" rather than a flipped axis.

70. `requestPointerLock()` is called **from the pointer-down handler**, never from
    the frame loop: after an Esc exit a request issued inside
    `requestAnimationFrame` is silently rejected and the game looks dead. While
    `document.pointerLockElement` is set, application hotkeys (level switch, menu,
    tabs) must stay silent — otherwise an in-game key throws the player out of the
    game.

71. A `MeshStandardMaterial` with high `metalness` and **no environment map
    renders black** — metal has nothing to reflect. Set `scene.environment`
    (a `RoomEnvironment` PMREM costs no file and no request) **and** keep
    `metalness ≤ 0.4` in the material: the environment is the first thing a
    low tier drops, and the asset must not turn into a hole in the frame
    when it does.

72. A node whose base orientation is not zero — anything aimed with
    `lookAt`, `setFromUnitVectors` or a baked rest pose — is animated
    through a **wrapper**, never by writing its own `rotation`. Code that
    returns a limb to rest with `rotation.x = lerp(rotation.x, 0, k)` reads
    the Euler of that very quaternion and erases the aim: the part turns
    away from what it was holding, and the bug reads as "the model is
    missing a piece".

73. Anything positioned from **world** coordinates — a prop placed by two
    hand positions, an effect anchored to a bone, a decal following a
    moving platform — must be converted into the parent's local space
    before it is written into `position`/`quaternion`. Writing world
    numbers into a child of a transformed node applies that transform a
    second time, and the object drifts away by exactly the owner's offset
    from the origin. It stays invisible while the owner sits at the origin
    (which is where every unit test and every bake script puts it), and
    appears only in the game, as "the weapon is flying around the level".

74. Where the character **looks** and where the weapon **points** are two
    different directions, and gameplay owns the second one. A mocap firing
    stance is bladed: the axis through the hands is 40–60° away from the
    model's forward, so turning the body at the target aims the barrel past
    it. Measure the offset once on the assembled rig, turn the body by
    `heading − offset`, and take up the remainder by rotating the chest —
    with an exact quaternion `from barrel to target`, not by tweaking a
    single Euler axis: a barrel that sits at an angle to the rotation plane
    turns by only `angle · cos(that angle)`.

### Interface

75. UI layers over the canvas are **transparent to input by default**: every
    layer container is `pointer-events: none` and only leaf interactive elements
    turn it back on. A full-screen overlay left at `auto` swallows every gameplay
    pointer, and the bug reads as "the car does not steer on mobile". The HUD
    layer is read-only and never becomes interactive at all — pause and settings
    are buttons on the screen layer.

76. Every colour, font, radius, spacing step and duration comes from tokens in
    one theme file. A literal `#RRGGBB`, `px` padding or `z-index: 9999` written
    inside a screen is the reason the second screen stops matching the first.
    `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` minus the theme file returns nothing.
    Имена токенов при этом одинаковы во всех играх, а **значения — нет**: палитра,
    обе гарнитуры и силуэт рамки выводятся из материала мира этой игры по
    процедуре в `knowledge/ux/ui_design_system.md`, раздел 12. Скопированный из
    примера или из соседней игры набор значений — такой же дефект, как умолчания
    браузера, только заметнее: он выглядит осознанным решением, принятым не для
    этой игры.

77. Browser dialogs (`alert`, `confirm`, `prompt`) and emoji used as icons are
    **banned in shipped UI**. The first blocks the game loop, cannot be styled and
    breaks immersion; the second renders differently on every OS and reads as a
    placeholder. Use the project's `Modal` component and one inline SVG sprite
    coloured by `currentColor`.

78. The HUD updates by writing to **cached nodes** and only when the value
    changed — never by rebuilding markup inside the frame loop, and never by
    reading layout (`offsetWidth`, `getBoundingClientRect`) there. Changing
    numbers carry `font-variant-numeric: tabular-nums` in a fixed-width slot, and
    bars animate with `transform: scaleX()`, not `width`; otherwise the row
    reflows on every tick and the whole overlay repaints.

79. Every screen defines its **loading, empty and error** states, not only the
    happy path. Saves, leaderboards, purchases and ads are network calls that fail
    routinely on these portals; a failure that renders an empty frame or says
    nothing costs the player their run and reads as a broken game.

80. Menu and HUD geometry is sized against the **measured** viewport
    (`visualViewport`, re-measured after orientation change, fullscreen exit and
    ad close), never `100vh`, and every UI layer is inset by
    `env(safe-area-inset-*)` **plus** the measured sticky-banner height. The page
    itself never scrolls — long content scrolls in an inner container.
    See `knowledge/ux/ui_design_system.md` and `knowledge/ux/ui_implementation.md`.

81. Меню, пауза и экран итога сессии рисуются **поверх живой игровой сцены**, а
    не поверх заливки. Тот же рендерер, та же сцена, медленная камера, работающий
    свет; игровой цикл в меню продолжает крутиться на сниженной нагрузке.
    Непрозрачный слой на весь экран (`background: #111` на корне экрана,
    полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка
    допускается только под текстовым блоком и кнопками. Меню — это первое и
    иногда единственное, что видит игрок: за глухой плашкой с колонкой кнопок
    вся работа над геймплеем невидима, и игра читается как недоделанная ещё до
    запуска. Постановку сцены задаёт `ART_DIRECTION.md` (раздел «Сцена за меню»).

82. У каждого экрана **три зоны**: чем экран себя называет, единственное главное
    действие (самое крупное и единственное с основным акцентом) и второстепенный
    ряд одним весом. Одна карточка с колонкой кнопок по центру — не композиция, а
    её отсутствие; композиция каждого экрана задана в `UI_UX_SPECIFICATION.md`,
    раздел «Каталог экранов», и придумывать её заново не нужно.

### Разделы, которые открываются по месту
Эти правила нужны не с первой строки, а когда доберёшься до соответствующей области. Полный текст — в `docs/ref/knowledge/CRITICAL_RULES.md` (файл обязательный, приезжает шагом 0). Обязательность от переноса не меняется.
- **Stack** — выбор библиотек стека — перед подключением любой из них
- **Renderer** — настройки рендерера — когда собираешь сцену
- **Physics vehicles** — физика транспорта — только если в игре есть машины

---

## 10. ПРИЁМКА — MUST

Полный список проверок с номерами лежит в `ACCEPTANCE.md`, рядом с этим файлом.
Он и есть определение готовности: пока хотя бы один пункт разделов **A–C**
красный, игра не готова — это не качество, это работоспособность.

Статическая часть проверяется командой (скрипт уже лежит в проекте, зависимостей
не требует — пропиши его в `package.json` как `check:spec`):

```bash
node scripts/check-spec.mjs
```

Прогонять после каждой фазы роадмапа, а не один раз в конце. Результат
записывать строкой в `DEVLOG.md`. Пункт, который не проходит, честнее оставить
красным с объяснением, чем отметить зелёным: следующий, кто откроет проект,
будет считать его проверенным.

Критерии, которые относятся именно к этой игре:

- [ ] Полноценный 3D-контроллер курьера с управлением одним пальцем (свайпы вверх/вниз, удержание для амортизации) и поддержкой клавиатуры на ПК
- [ ] Механика амортизации приземления (Cushion Roll) с активным тайминг-окном 0.18 с, защищающая посылку от разрушения
- [ ] Физическая шкала целостности посылки (Parcel Integrity 100 HP) с нанесением урона при ударах и визуализацией трещин
- [ ] Множитель непрерывного темпа Flow Combo (x1.0..x4.0) с динамическим ускорением и эффектами скорости
- [ ] Процедурный генератор крыш старого города из 12 архитектурных модулей (скаты, мансарды, дымоходы, карнизы, бельевые веревки)
- [ ] Красивая стилизованная сцена старого европейского города на закате с терракотовой черепицей, патинированной медью и лучами заходящего солнца
- [ ] Быстрый расчет коллизий и рейкастов через three-mesh-bvh со стабильными 60 FPS на мобильных устройствах
- [ ] Полный цикл сессии: выбор контракта -> забег 45–75 секунд -> расчет гонорара и чаевых -> мастерская экипировки
- [ ] Интеграция Playgama Bridge SDK 2.x (облачные сохранения, лидерборды, Rewarded Video для Страховки и удвоения чаевых)
- [ ] Адаптивный интерфейс под Safe Area мобильных экранов в книжной ориентации без виртуальных кнопок
- [ ] Звуковая подсистема на Web Audio API с процедурным ветром, сочными звуками паркура и динамической музыкой
- [ ] Project builds cleanly with zero TypeScript errors (`npm run build`).
- [ ] Playgama Bridge initializes and dispatches GAME_READY platform message.
- [ ] Петля играбельна целиком: Получение курьерского контракта на доставку хрупкой посылки -> Скоростной паркур-забег по процедурным крышам с таймингом прыжков, подкатов и амортизации приземлений -> Доставка груза на балкон заказчика -> Подсчет гонорара и чаевых за скорость и сохранность (Flow Multiplier & Integrity) -> Кастомизация снаряжения (сумки с амортизацией, нескользящая обувь) и открытие новых сложных районов города..
- [ ] Реализованы механики проекта: Амортизационный перекат по черепице (Cushion Roll & Kinetic Transfer), Катапультирующий зацеп за карниз (Ledge Grab & Pop-Up Vault), Скоростной подкат под паровые вытяжки (Slate Slide & Flue Duck), Инерционная гидродинамика алхимической колбы (Aetheric Parcel Fluid Inertia).
- [ ] Условие успеха (Успешная доставка посылки на целевой балкон адресата за время таймера (менее 60 секунд) с сохранением целостности груза > 0% (идеально 100% для 3 звезд и максимальных чаевых).) и условие проигрыша (Срыв в переулок к пешеходам (падение с крыши), полное разрушение груза (целостность 0% от неотринутых ударов о трубы) или истечение лимита времени контракта (0.0 сек).) работают.
- [ ] Тач-управление отвечает без задержки, страница под игрой не скроллится.
- [ ] Интерфейс собран из токенов одного `src/ui/theme.css`: в экранах нет литералов цвета, шрифта и `z-index`.
- [ ] Слои интерфейса прозрачны для игрового ввода — управление работает под всеми оверлеями, слой HUD не кликается.
- [ ] У каждого экрана работают состояния загрузки, пустоты и ошибки; возможность, которой нет на площадке, не нарисована вовсе.
- [ ] Меню и HUD помещаются в измеренный вьюпорт с учётом safe-area и баннера, а с закрытым игровым полем меню узнаётся как «Черепичный Спринт: Чистый Флоу».
- [ ] За меню и экраном итога видна живая игровая сцена: полноэкранной непрозрачной заливки поверх канваса нет, подложка только под текстом и кнопками.
- [ ] Ни одного эмодзи в подписях интерфейса: иконки — инлайновый SVG с `currentColor`.
- [ ] Rewarded-реклама выдаёт награду ровно один раз (Страховка Гильдии (Второе дыхание), Королевские чаевые x2, Проба элитной сумки); interstitial соблюдает паузу 90 с.
- [ ] Audio and physics automatically pause/resume on tab blur and ad display.
- [ ] Persistent progress saves and loads from Playgama Cloud Storage.
- [ ] Runs at steady 60 FPS on desktop and >= 50 FPS on mobile.

---

## 11. ГДЕ ЛЕЖИТ ОСТАЛЬНОЕ

База знаний не лежит в пакете копиями — она приезжает по требованию:

```bash
node scripts/fetch-knowledge.mjs        # всё обязательное → docs/ref/
node scripts/fetch-knowledge.mjs <путь> # отдельный файл
```

Что именно тянуть, перечислено в `knowledge.manifest.json`. После загрузки адреса ниже открываются как обычные локальные файлы, и сеть больше не нужна.

Набор отобран под этот проект: Проект опирается на архитектуру скоростного раннера с комбо-множителем флоу, процедурную генерацию викторианских крыш и персонажа без внешних ассетов, физику скольжения и перегрузок на Rapier3D с BVH-запросами, процедурный Web Audio звук и строгую мобильную оптимизацию.

**Строка после адреса — ярлык документа, а не его содержание.** Она названа так, чтобы документ можно было отличить от соседнего, и покрывает от силы десятую часть файла: за ярлыком «управление от первого лица и покачивание вьюмодели» в прошлый раз лежали гравитация, прыжок, вторая рука на вьюмодели, анимации перезарядки, пружина отдачи и хитмаркер — и ничего из этого в игру не попало, потому что файл сочли уже понятным. Документы ядра открываются целиком, до первой строчки кода по своей теме.

Пункты `- [ ]` под адресом — чек-лист самого документа, дословно. Это не пожелания: каждый пункт закрывает уже сделанную кем-то ошибку и проверяется взглядом на запущенную игру.

Здесь стоят чек-листы только тех документов, что отобраны под эту игру. **Полный список, включая платформенные документы, лежит в `ACCEPTANCE.md`, раздел H** — там же он и закрывается: у каждого пункта либо `[x]`, либо `[~]` со строкой причины. Незакрытый пункт валит `node scripts/check-spec.mjs`, и проект не считается готовым.

**Ядро — прочитать до начала реализации:**
- `docs/ref/knowledge/patterns/score_attack_loop.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Определяет архитектуру курьерского спринт-раннера с нарастающим темпом, комбо-множителем флоу и финальным расчетом наград за сохранность груза.
  - [ ] Множитель держится рискованными действиями и падает при простое
  - [ ] Скорость растёт непрерывно, запас на ошибку сокращается
  - [ ] Режим ярости даёт короткую неуязвимость и всплеск очков, и это читается по экрану
  - [ ] Проигрыш мгновенный и понятный: игрок видит, обо что он ударился
  - [ ] Результат уходит в таблицу лидеров сразу после смерти
  - [ ] Рестарт мгновенный, без загрузки: «ещё один раз» не упирается в экран ожидания
- `docs/ref/knowledge/threejs/performance_guide.md` (выжимка — `skills/RENDERER_SKILL.md`) — Обеспечивает стабильные 60 FPS на мобильных устройствах при быстром беге по плотному процедурному городу через инстансинг и объединение геометрии.
  - [ ] Повторяющиеся объекты (враги, снаряды, обломки, трава) — через InstancedMesh, а не отдельными мешами
  - [ ] Статичное окружение слито в общую геометрию
  - [ ] В кадре меньше 50 000 активных треугольников
  - [ ] Геометрии, материалы и текстуры освобождаются при смене сцены
  - [ ] Один направленный свет с тенями; заполняющий — ambient или hemisphere, а не россыпь точечных
  - [ ] Карта теней не больше 1024 на мобильном
  - [ ] pixelRatio ограничен 1.5 на мобильном
  - [ ] Дельта времени ограничена сверху: возврат из свёрнутой вкладки не взрывает физику
- `docs/ref/knowledge/threejs/physics_integration.md` (выжимка — `skills/RENDERER_SKILL.md`) — Связывает Three.js с физическим движком для обработки скольжения по наклонным скатам крыш, амортизации перекатов и расчета перегрузок колбы.
  - [ ] Движок выбран под задачу: Rapier для тысяч тел, рэгдоллов и рейкастов, Cannon-es для простых аркадных столкновений
  - [ ] Порядок кадра: силы → шаг мира → синхронизация мешей, а не наоборот
  - [ ] Позиция меша интерполируется между физическими шагами, иначе видно дрожание
  - [ ] Суставы рэгдолла имеют пределы углов, линейное и угловое затухание — без этого он трясётся
  - [ ] Тела и суставы освобождаются явно: память WASM не собирается сборщиком мусора
  - [ ] Шаг физики фиксированный, а накопитель ограничен сверху
- `docs/ref/knowledge/threejs/procedural_mesh_builder.md` (выжимка — `skills/RENDERER_SKILL.md`) — Позволяет процедурно генерировать здания викторианского города, черепичные скаты, мансарды, дымоходы и натянутые тросы без внешних 3D-моделей.
  - [ ] Модели собираются процедурно в коде, без внешних GLTF
  - [ ] Силуэт читается на расстоянии: персонаж отличается от врага формой, а не только цветом
  - [ ] Геометрия переиспользуется между экземплярами, а не создаётся заново на каждый объект
  - [ ] metalness ≤ 0.4 или задан scene.environment — иначе металл выглядит чёрным
  - [ ] Анимация персонажа идёт поворотами костей или групп, а не подменой мешей
  - [ ] Число материалов ограничено: каждый новый материал — отдельный draw call
- `docs/ref/knowledge/threejs/game_map_and_world_design.md` (выжимка — `skills/RENDERER_SKILL.md`) — Определяет принципы процедурной сборки раннер-трассы из модульных блоков крыш с развилками риска и бесшовной подгрузкой на дистанции 700 метров.
  - [ ] **Вид сверху (Top-Down Overview)**: все участки трассы изолированы, встречные полосы не пересекаются, нет самопересечений сплайна.
  - [ ] **Стартовая зона**: стартовые слоты ориентированы строго по направлению движения, светофоры и арка развернуты навстречу машинам, трибуны не залезают на полотно.
  - [ ] **Разметка**: белые линии идут параллельно кромке, пунктир не имеет поперечных стяжек, отсутствует мерцание (Z-fighting).
  - [ ] **Кромка дороги и поребрики**: переход в повороты гладкий, без зазубрин; поребрики лежат точно на внутренних апексах.
  - [ ] **Заземление декораций**: деревья, столбы, таблички торможения и барьеры надежно стоят на поверхности холмов без левитации.
  - [ ] **Физика**: болиды и соперники стартуют вперед, проходят первый поворот на скорости без переворотов и провалов сквозь полигоны.
- `docs/ref/knowledge/threejs/procedural_character_rig.md` (выжимка — `skills/RENDERER_SKILL.md`) — Отвечает за процедурный риг курьера, анимации спринта, подкатов, зацепов за карниз и визуальную реакцию тела на инерцию и приземления.
  - [ ] Позвоночник из двух сегментов; таз и корпус — независимые корни.
  - [ ] Каждая степень свободы — своя группа (плечо → локоть → предплечье).
  - [ ] Разворот к цели через `root`, ±90° для боковой камеры, и он зеркалится.
  - [ ] Есть шея, нос глубиной ≥ 0.07, суставы на сгибах, обувь выше щиколотки.
  - [ ] Лицо и телосложение — от seed, не от `Math.random()`.
  - [ ] Износ по зонам, накладки с `depthWrite: false` и `visible = false` при нуле.
  - [ ] Рэгдолл использует меши персонажа; кости считаются «листья → корень»;
  - [ ] Тела рэгдолла создаются одним проходом, а сшиваются **вторым**: список
  - [ ] Колено проверено замером выноса вперёд — во **всех** анимациях, не
  - [ ] Анимация — цели + один коэффициент приближения; дуга удара выводится
  - [ ] Каждый сустав возвращается к цели по всем трём осям, а не только по «своей».
  - [ ] Каждая анимация вызывается клавишей и снята витриной (§7).
  - [ ] Ни одна поза не уводит стопу под пол и не роняет голову ниже пояса.
  - [ ] У ударов ногами стопа доходит до хитбокса своего же приёма — замером,
  - [ ] Удар ногой разбит на занос, выхлест и возврат; выхлест заканчивается
  - [ ] Рэгдолл — не меньше двенадцати тел: конечности разбиты по суставам.
  - [ ] Колено и локоть — шарниры с пределами от текущей позы, а не шары.
  - [ ] Нокдаун меряется тремя числами: разброс тела, высота головы в конце,
- `docs/ref/knowledge/threejs/juice_and_vfx_pool.md` (выжимка — `skills/RENDERER_SKILL.md`) — Реализует сочный отклик: инстансинг пара из вытяжек, искры скольжения, динамический крен камеры и шейк при приземлении.
  - [ ] Частицы живут в пуле на InstancedMesh, без аллокаций в кадре
  - [ ] Гравитация, время жизни и размер — параметры частицы, а не константы пула: иначе дым падает как искры
  - [ ] Искры, гильзы и кровь летят направленным конусом вдоль нормали или выстрела, а не разлетаются шаром
  - [ ] Аддитивные искры и полупрозрачный дым — два меша и два пула, а не один материал
  - [ ] Затухание сделано цветом через setColorAt: прозрачность живёт в общем материале
  - [ ] frustumCulled = false на мешах с мировыми инстансами — иначе эффекты пропадают при отвороте камеры
  - [ ] Активные частицы пишутся в буфер подряд, mesh.count выставляется в конце, а неактивные не прячутся в (0,-999,0)
  - [ ] Шейк камеры затухает сам и не оставляет камеру смещённой
- `docs/ref/knowledge/audio/procedural_sound_synthesizer.md` (выжимка — `skills/RENDERER_SKILL.md`) — Синтезирует процедурные звуки шагов по черепице, свиста встречного ветра, бульканья колбы и ударов приземления без внешних аудиофайлов.
  - [ ] AudioContext создаётся лениво, по первому pointerdown/keydown, а не в конструкторе
  - [ ] Все звуки идут через один мастер-гейн: громкость и мьют трогают только его
  - [ ] Свёрнутая вкладка приглушает звук через visibilitychange (suspend/resume контекста)
  - [ ] Осцилляторы и узлы отключаются после затухания, а не копятся за сессию
  - [ ] Каждое действие игрока имеет свой звук: выстрел, попадание, подбор, кнопка интерфейса
  - [ ] Непрерывный звук (двигатель, ветер) — один осциллятор с меняющейся частотой, а не рестарт каждый кадр
  - [ ] Звук интерфейса отличается от звука мира: клик не путается с событием игры
- `docs/ref/knowledge/audio/web_audio_and_muting.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Обеспечивает надежное управление Web Audio API, корректную инициализацию по первому свайпу и глушение звука при сворачивании игры.
  - [ ] Звук только через Web Audio API: ни одного <audio> или <video> — это отказ модерации, а не мелочь
  - [ ] В панели уведомлений телефона и в системном плеере игра не появляется
  - [ ] Один мастер-гейн — единственное, чего касаются мьют, приглушение и флаг площадки
  - [ ] Мьют сделан гейном, а не паузой контекста
  - [ ] Загрузка не ждёт разблокировки звука: меню интерактивно при suspended-контексте, game_ready не задерживается
  - [ ] Пауза и мьют приходят от площадки двумя независимыми событиями, оба обработаны
  - [ ] Состояние мьюта лежит в сохранении игры, а не в конструкторе движка: кнопка не сбрасывается после перезагрузки
- `docs/ref/knowledge/monetization/rewarded_ads_patterns.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Интегрирует добровольную rewarded-рекламу для удвоения чаевых за чистую доставку и второго шанса при срыве с карниза.
  - [ ] Игра проходима без единого просмотра рекламы
  - [ ] Награда выдаётся только по событию 'rewarded', подписка снимается после
  - [ ] Есть защита от двойного нажатия кнопки просмотра
  - [ ] Кнопка визуально сообщает, что за ней реклама, ещё до нажатия
  - [ ] Предложение показывается всегда, когда действие уместно, а не по случайному числу
  - [ ] Помощь рекламой ограничена долей типичного дохода за сессию и не покрывает дорогую покупку
  - [ ] У покупателя «без рекламы» rewarded остаётся: убирают прерывания, а не возможности
  - [ ] У каждого места показа есть предел за забег или кулдаун
- `docs/ref/knowledge/stack/rapier3d.md` (выжимка — `skills/STACK_SKILL.md`) — WASM-движок для высокоточного расчета физики скольжения, инерционных сил алхимической колбы и детекции триггеров карнизов.
  - [ ] Физика только на Rapier: свои «скорости», Cannon-es и ammo.js в проект не тянутся
  - [ ] Шаг мира фиксированный: world.timestep не подменяется на dt
  - [ ] cuboid() получает ПОЛУразмеры — коробка 1×1×1 это cuboid(0.5, 0.5, 0.5)
  - [ ] trimesh используется только для статичных тел: динамический проваливается сквозь мир
  - [ ] Группы столкновений заданы с обеих сторон — иначе контакта не будет
  - [ ] Триггеры сделаны сенсорами, а не проверкой дистанции в update()
  - [ ] Синхронизация мешей идёт ПОСЛЕ world.step(), иначе кадр рисует прошлое состояние
  - [ ] «На земле» определяется контроллером персонажа, а не по velocity.y === 0
  - [ ] Рейкаст исключает собственное тело стрелка
  - [ ] Рестарт уровня — телепорт тел, а не пересборка мира
  - [ ] Физика проверена скриптом в Node до того, как её увидит игрок

**Вспомогательные:**
- `docs/ref/knowledge/threejs/adaptive_quality.md` (выжимка — `skills/RENDERER_SKILL.md`) — Динамически масштабирует разрешение рендера и дальность прорисовки черепичных крыш для предотвращения троттлинга на смартфонах.
- `docs/ref/knowledge/threejs/mobile_shaders.md` (выжимка — `skills/RENDERER_SKILL.md`) — Задает правила создания легких мобильных материалов черепицы, меди и пара без ресурсоемких физических шейдеров.
- `docs/ref/knowledge/monetization/interstitial_best_practices.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Регулирует показ межстраничной рекламы исключительно в естественных паузах между завершенными курьерскими контрактами.
- `docs/ref/knowledge/monetization/in_app_purchases.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Обеспечивает покупку косметических сумок, обуви для паркура и премиальных контрактов без пейволлов в геймплее.
- `docs/ref/knowledge/stack/postprocessing.md` (выжимка — `skills/STACK_SKILL.md`) — Создает атмосферу золотого часа через слитный однопроходный EffectPass с блумом заката и виньеткой скорости.
- `docs/ref/knowledge/stack/three_mesh_bvh.md` (выжимка — `skills/STACK_SKILL.md`) — Ускоряет пространственные raycast-запросы для мгновенного определения точек зацепа за медные карнизы и контакта со скатами.

**Архетип петли проекта**: `docs/ref/knowledge/patterns/score_attack_loop.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`)

**Платформенные требования — обязательны всегда, независимо от жанра.** Без Playgama Bridge игра на площадке не стартует: платформа держит свою заставку до таймаута и снимает игру с модерации.
- `docs/ref/knowledge/playgama/game_ready_and_loading.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/auth_and_player.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/storage_and_cloud.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/ads_integration.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/banners_and_layout.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/compliance/yandex_moderation.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/localization_system.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/touch_controls.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_design_system.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_implementation.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)

**Осознанно НЕ включены**: `mechanics/upgrade_choices.md`, `mechanics/fluid_buoyancy.md`, `mechanics/checkpoint_lap_racing.md`, `mechanics/drift_scoring.md`, `mechanics/rhythm_sync.md`, `threejs/skinned_character_models.md`, `stack/recast_navigation.md`, `stack/bitecs.md`, `stack/yuka_ai.md`, `patterns/survivor_loop.md`, `mechanics/parry.md`. Исключены механики боевых игр, карточных рогаликов с паузами, симуляции плавучести в воде и кольцевых гонок, а также избыточные библиотеки ИИ и ECS, противоречащие жанру динамичного соло-раннера по крышам.
Не подтягивай решения из этих документов — они про другую игру.

**Документы проекта** (лежат рядом, в корне игры): `ACCEPTANCE.md` — приёмка, `AGENTS.md` — правила работы, `PROJECT_DIRECTION.md` — рамка проекта, `GAME_DESIGN_DOCUMENT.md`, `MECHANICS.md`, `CORE_LOOP.md`, `UI_UX_SPECIFICATION.md`, `ART_DIRECTION.md`, `MOBILE_CONTROLS.md`, `PLAYGAMA_INTEGRATION.md`, `MONETIZATION.md`, `QA_PLAN.md`, `DEVLOG.md` и `CHANGELOG.md` — их ведёшь ты.