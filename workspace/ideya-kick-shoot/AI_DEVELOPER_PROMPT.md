# FINAL AI DEVELOPER PROMPT: ИДЕЯ: KICK & SHOOT 🎮⚡

> **INSTRUCTION FOR AI CODING AGENT**:
> You are the **Lead Game Developer & Systems Architect**. Your task is to build and deliver the complete, production-ready, fully playable HTML5/WebGL game described in this specification from start to finish.
> Follow the technical architecture, physics specifications, Playgama Bridge integration, and mobile ergonomics strictly.
> Do NOT omit systems, use fake placeholder stubs, or leave TODOs. The end result must satisfy every single item in the **Definition of Done**.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: Идея: kick & shoot
- **Project Slug**: `ideya-kick-shoot`
- **Genre**: 3D Аркадный экшен-рогалик (Короткие забеги с прокачкой)
- **Target Platform**: Playgama Bridge (Yandex Games / VK / Web)
- **Orientation**: Landscape
- **Target Audience**: Игроки Яндекс Игр, CrazyGames и мобильных веб-порталов.
- **Player Fantasy**: Овладейте ключевой механикой, соберите свой билд и пройдите дальше, чем в прошлый раз.
- **Core Hook**: Динамичный игровой процесс с сочным откликом и рогалик-прокачкой между забегами.
- **Session Model**: Короткие сессии по 5-8 минут с высоким удержанием и мета-прокачкой.


---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite 5.x
- **Renderer**: **THREEJS** (^0.170.0)
  - *Selection Rationale*: User explicitly specified 'threejs'.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat 0.13.x)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Howler.js (^2.2.4) с WebAudio API
- **State Management**: Custom TinyEventBus & Reactive GameStore
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 75
- **Max Triangles / Active Sprites**: < 40000
- **Max Bundle Size**: < 4.2 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Управление персонажем -> Преодоление угроз -> Выбор 1 из 3 улучшений -> Испытание -> Прокачка в меню.
```

**Фирменный момент**: Игрок на полном ходу с ноги выбивает тяжелую бронедверь отсека: слетевшая с петель створка впечатывает пулеметчика в стену, выбивая у него в воздух раскаленный пулемет. Включается 0.8-секундный адреналиновый bullet-time: игрок на лету перехватывает пулемет, разряжает очередь в двух парящих штурмовиков, после чего пинком отправляет летящую навстречу взрывную бочку в толпу подкрепления, вызывая огненный цепной взрыв и звон комбо-ранга SSS.

**Чем петля отличается от соседей по жанру**: Вместо пассивного кайтинга врагов по кругу игра принуждает к агрессивному рашу вперед: дефицит патронов вынуждает регулярно сближаться для пинка, а любая туша врага, бочка или выбитая дверь становятся смертоносным метательным снарядом, экономящим боеприпасы.

**Шаблон жанра, который НЕ реализуем**: Отвергаем шаблон 'пассивного автошутера/рогалика с зачисткой открытых пустых арен и выбором скучных карточек +15% урона от пуль'. В нашем FPS урон и победа рождаются не из сухих DPS-статов, а из геометрии комнат, кинетических ragdoll-столкновений и агрессивного ближнего контакта, где пинок — главный инструмент контроля толпы, добычи ресурсов и запуска цепных реакций.

**Схема петли**:
```text
+---------------------------------------------------+
|            ВХОД В ОТСЕК / BREACH KICK            |
|     (Вынос двери с ноги + Bullet-Time 0.8 с)      |
+-------------------------+-------------------------+
                          |
                          v
+---------------------------------------------------+
|                 ОЦЕНКА ДИСТАНЦИИ                  |
|    Патроны есть?                   Патронов мало? |
+------------+------------------------------+-------+
             |                              |
             v                              v
+-----------------------+     +---------------------+
|  ПРИЦЕЛЬНЫЙ ВЫСТРЕЛ   |     | КИНЕТИЧЕСКИЙ ПИНОК  |
| (Снятие брони / стан) |     |  (Полет туши 17 м/с)|
+------------+----------+     +----------+----------+
             |                           |
             +-------------+-------------+
                           |
                           v
+---------------------------------------------------+
|       КАСКАДНЫЙ ЭФФЕКТ / ВОЗДУШНЫЙ ЖОНГЛЯЖ        |
| (Удар о стену 300% / Боулинг в бочку / Skeet-крит)|
+--------------------------+------------------------+
                           |
                           v
+---------------------------------------------------+
|     ВЫПАДЕНИЕ ПАТРОНОВ + РОСТ КОМБО-РАНГА (SSS)   |
+--------------------------+------------------------+
                           |
                           v
+---------------------------------------------------+
|             ВЫБОР БИО-ЧИПА И СЛЕДУЮЩИЙ ОТСЕК      |
+---------------------------------------------------+
```

**Микро-петля (посекундно)**:
- **Рывок на ударную дистанцию** (0.35 с): игрок — Спринт и сближение с противником на расстояние менее 2.2 м; игра — Динамический сдвиг FOV с 85° до 95°, свист рассекаемого воздуха, подсветка контура цели желтым маркером кинетического захвата; решение — Пнуть цель в глухую стену для мгновенного стана или запустить в толпу для кегельбана?
- **Кинетический удар берцем** (0.2 с): игрок — Нажатие клавиши пинка (ПКМ / E / экранная кнопка); игра — Анимация резкого удара ногой, хит-стоп замирание на 0.06 с, импульсный толчок камеры назад на 0.25 м, ragdoll-полет туши врага со скоростью 17.5 м/с; решение — Сделать выстрел вдогонку по летящей мишени или уклониться от ответного огня с фланга?
- **Воздушный жонгляж / Выстрел** (0.45 с): игрок — Прицельный выстрел из дробовика/пистолета по находящемуся в воздухе врагу; игра — Звуковой триггер звонкого щелчка Skeet-крита (урон x2.5), вспышка искр, выброс дополнительного боекомплекта из тела; решение — Поймать выбитое оружие в воздухе или добить следующую цель бочкой?
- **Каскадная детонация и сбор ресурсов** (0.6 с): игрок — Пинок взрывного баллона в точку приземления тел; игра — Глухой удар металла о металл, детонация с радиусом 4.2 м, сбор выпавших сфер био-плазмы; решение — Двигаться к следующей штурмовой двери или перезарядить основной ствол?

**Мезо-петля (этап)**:
- **Штурмовой прорыв отсека (Breach)** (1.2 с): игрок — Силовой пинок во входную дверь новой комнаты; игра — Дверь срывается с петель и сбивает авангард, активируется bullet-time замедление (time_scale 0.25 на 0.8 с); решение — Кого устранить в первую очередь: снайпера на высоте или тяжелого щитовика?
- **Тактическая зачистка арены** (10-15 с): игрок — Уничтожение 6-10 врагов комбинированием пинков, рикошетов и стрельбы; игра — Рост комбо-множителя (C -> B -> A -> S -> SSS), сопровождаемый нарастанием бас-партии динамического саундтрека; решение — Рисковать ради поддержания комбо-таймера или занять безопасную позицию за колонной?
- **Выбор тактического био-чипа** (3-5 с): игрок — Выбор 1 из 3 синергетических модификаторов на терминале отсека; игра — Мгновенное применение эффекта (например, 'Электро-шипы на подошве' или 'Взрывные рикошеты туш'); решение — Усилить физику пинка, повысить боезапас оружия или взять лечение при сминании врагов о стены?

**Макро-петля (забег)**:
- **Забег по сектору (10-12 отсеков)** (5-7 мин): игрок — Последовательное прохождение цепочки штурмовых комнат и битва с финальным экзо-боссом; игра — Постепенное усложнение условий: появление шипов на стенах, лазеров, щитовиков с анти-кик защитой; решение — Выбрать маршрут через оружейный склад (много патронов) или химлабораторию (много взрывных бочек)?
- **Фиксация результатов и добычи** (30 с): игрок — Завершение забега (победа над боссом сектора или гибель от полученного урона); игра — Экран статистики: подсчет отправленных в полет тел, выбитых дверей, стиля комбо и начисление био-плазмы; решение — Какие постоянные узлы экзоскелета прокачать в мастерской перед следующим спуском?
- **Мета-модернизация в мастерской** (40 с): игрок — Трата био-плазмы на перманентные апгрейды экзо-ботинок, арсенала и адреналиновых желез; игра — Разблокировка новых стартовых стволов (помповый отбойник, гвоздомет), усиление базового импульса; решение — Инвестировать в силу кинетического удара (+15% к импульсу) или в магнитный захват выбитого оружия?

**Кривая напряжения**: Пилообразная кривая с 4 фазами на каждый отсек: 1. Фаза прорыва (Spike: 95%) — взрывной вынос двери ногой, всплеск адреналина в замедлении времени; 2. Фаза кинетического хаоса (Plateau: 80-90%) — непрерывный бег, серия пинков, жонглирование в воздухе и уклонение от снарядов; 3. Фаза добивания (Decrescendo: 40%) — ликвидация единичных оглушенных противников и сбор патронов; 4. Фаза передышки (Rest: 10%) — выбор перка на консоли, оценка билда и подготовка к следующей двери.

**Формулы ядра (реализуй буквально)**:
- `KickLaunchVelocity = BaseLaunchSpeed (17.5 m/s) * (1.0 + BootBoosterUpgrade * 0.12) * clamp(PlayerForwardSpeed / 6.0 m/s, 1.0, 1.6)`
- `WallImpactDamage = BaseWallDamage (45 HP) + (RagdollImpactVelocity ^ 1.4) * MassFactor (1.15) * (IsSpikeWall ? 2.5 : 1.0)`
- `AirSkeetMultiplier = BaseCrit (2.2x) + clamp(AirHangTime / 0.8 s, 0.0, 1.5) * WeaponPrecisionFactor (1.3x)`
- `DominoChainDamage = VictimVelocity * TargetBodyMass * 2.8 * (ChainDepth ^ 0.75)`
- `AmmoDropChance = BaseChance (0.15) + (KilledByWallOrBarrelKick ? 0.70 : 0.0) + (ComboLevel * 0.05)`

**Прогрессия внутри забега**:
- Отсеки 1-3 (Стартовый прорыв): Базовые берцы отбрасывают пехотинцев на 7 метров, пистолет с 12 патронами, редкие взрывные бочки у стен.
- Отсеки 4-7 (Разгон синергий): Получение 2-3 тактических био-чипов (например, зажигательный след от пинка, автоперезарядка при ударе врага о стену), появление тяжелых автоматчиков.
- Отсеки 8-11 (Кинетический шторм): Пинок создает ударную волну в конусе 45°, враги разлетаются веером, сбивая друг друга как домино; на стенах появляются стационарные шипы и электрощиты.
- Отсек 12 (Финальный Экзо-Колосс): Босс со сбиваемой кинетическими ударами энерго-броней; для нанесения урона требуется пинать в босса его собственную свиту и летящие ракеты.

**Мета-прогрессия**:
- Титановые пластины экзо-берцев (0/10 рангов): +7% к силе кинетического импульса и +6% к урону от впечатывания в препятствия за уровень.
- Адреналиновый инжектор штурма (0/5 рангов): Увеличивает окно замедления времени при вышибании дверей с 0.80 с до 1.60 с.
- Магнитная перчатка перехвата (0/5 рангов): Увеличивает радиус автоматического подбора выбитого из рук оружия с 1.8 м до 5.5 м.
- Увеличенный разгрузочный жилет (0/8 рангов): +12% к начальному запасу патронов для дробовика, автомата и гранатомета.
- Мастерская модификаций обуви: Открытие альтернативных типов подошв (Шоковые подошвы, Пневмо-таран, Реактивные каблуки).

### Кинетический тактический пинок (Kinetic Spartan Kick) (CORE)
- **Category**: Двигатель темпа и главный инструмент контроля дистанции/ресурсов
- **Description**: В отличие от стандартного ножа/мили-удара в FPS, пинок перемещает физическое тело в пространстве с сохранением вектора и превращает самого врага в разрушительный снаряд.
- **Player Input**: ПКМ / Клавиша 'E' / Тап по большой кнопке ноги справа внизу экрана
- **Hit & Sensory Feedback**: Визуал/VFX: Появление полупрозрачного конуса сжатого воздуха shockwave, частицы пыли и искр из-под подошвы, мгновенный ragdoll врага с инерционным разворотом суставов.; Звук (Web Audio): Низкочастотный саб-басовый панч (60 Гц) в момент касания, хлесткий металлический щелчок сервопривода экзоскелета, свистящий звук рассекаемого воздуха.; Камера: Мгновенный толчок камеры назад на 0.2 м с возвратом за 0.15 с, угловой шейк 2.5 градуса по оси Pitch.; UI/Тактильность: Короткий вибрационный импульс на мобильных устройствах (45 мс), дуговой радиальный индикатор отката вокруг перекрестья прицела.
- **Technical Complexity**: Medium
- **Решение игрока**: Использовать пинок прямо сейчас для мгновенного контроля опасного врага и экономии патронов или приберечь для удара по взрывной бочке?
- **Состояния**: `READY: Нога заряжена, готовность к мгновенному удару`, `WINDUP: Замах экзо-ботинка (0.07 с), блокировка стрельбы`, `ACTIVE_HITBOX: Конус удара 60 градусов, длина 2.1 м (0.12 с)`, `HIT_FREEZE: Микро-пауза 0.05 с при контакте для сочного импакта`, `RECOVERY: Возврат ноги в исходное положение (0.16 с), возможность прерывания спринтом`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Базовая скорость запуска цели` = `17.5 м/с` — Если опустить ниже 12 м/с — враг вяло отваливается, пропадает ощущение супергеройской силы; если поднять выше 24 м/с — игрок не успевает среагировать и расстрелять цель в воздухе.
  - `Длительность хит-стопа при попадании` = `0.06 с` — При 0 с удар кажется 'ватным' и невесомым; при значении выше 0.12 с игра ощущается залагавшей и тормозящей.
  - `Конус поражения удара` = `55 градусов` — Меньше 35 градусов — частые промахи в мобильной версии; больше 80 градусов — пинок превращается в дешевую круговую атаку без необходимости целиться.
  - `Урон от соударения со стеной` = `50 ед` — Меньше 30 ед — игроки перестают целиться в стены и просто стреляют; больше 90 ед — огнестрельное оружие становится ненужным.
- **Слои отклика**:
  - Визуал/VFX: Появление полупрозрачного конуса сжатого воздуха shockwave, частицы пыли и искр из-под подошвы, мгновенный ragdoll врага с инерционным разворотом суставов.
  - Звук (Web Audio): Низкочастотный саб-басовый панч (60 Гц) в момент касания, хлесткий металлический щелчок сервопривода экзоскелета, свистящий звук рассекаемого воздуха.
  - Камера: Мгновенный толчок камеры назад на 0.2 м с возвратом за 0.15 с, угловой шейк 2.5 градуса по оси Pitch.
  - UI/Тактильность: Короткий вибрационный импульс на мобильных устройствах (45 мс), дуговой радиальный индикатор отката вокруг перекрестья прицела.
- **Режим отказа игрока**: Игрок пинает бронированного мини-босса с активным щитом: ботинок отскакивает с металлическим звоном, игрок получает стан на 0.35 с и отбрасывается назад.
- **Кривая мастерства**: Новичок на 30-й секунде хаотично пинает одиночных врагов перед собой; эксперт на 10-м забеге пинает на бегу в прыжке под углом 45 градусов, отправляя одного врага рикошетом через двух других прямо в бочку с горючим.
- **Сопротивление игры**: Враги со штурмовыми щитами блокируют фронтальный пинок, а берсерки с шипованной броней наносят 15 ед ответного урона при попытке ударить их ногой без предварительного снятия брони выстрелом.
- **Синергии**:
  - Воздушный жонгляж: подброшенный пинком враг зависает в воздухе под критические выстрелы
  - Каскадный снос окружения: запущенное тело служит тяжелым кинетическим снарядом для других объектов
  - Штурмовой вышиб дверей: разгон перед пинком суммирует скорость спринта с силой удара
- **Почему это не жанровый шаблон**: В отличие от стандартного ножа/мили-удара в FPS, пинок перемещает физическое тело в пространстве с сохранением вектора и превращает самого врага в разрушительный снаряд.
- **Псевдокод тика**:
```text
function onKickTick(delta):
  if not input.kickPressed or kickCooldown > 0: return
  state = WINDUP; playSound('servo_kick'); yield(0.07)
  target = findTargetInCone(player.pos, player.forward, angle=55, dist=2.1)
  if target:
    triggerHitStop(0.06); triggerCameraShake(amp=0.35)
    impulseVector = (player.forward + Vector3(0, 0.25, 0)).normalize() * 17.5
    target.applyRagdollImpulse(impulseVector)
    target.addStatusEffect('AIRBORNE_SKEET', duration=1.2)
  state = RECOVERY; kickCooldown = 0.45
```

### Воздушный жонгляж и экзекуция (Air-Skeet Juggling) (SECONDARY)
- **Category**: Источник высокой награды, урона и стиля за меткость
- **Description**: Объединяет механику стендовой стрельбы по тарелочкам (Skeet shooting) с аркадным FPS: игрок сам создает себе летящие мишени собственным пинком.
- **Player Input**: ЛКМ / Удержание кнопки огня / Тап по кнопке 'Выстрел'
- **Hit & Sensory Feedback**: Визуал/VFX: Яркая радиальная вспышка золотисто-оранжевого цвета при попадании в воздухе, разлетающиеся гильзы и искры, текст 'SKEET CRIT!' всплывает над целью.; Звук (Web Audio): Высокий хрустальный звук 'дзынь' колокольчика поверх мощного выстрела дробовика, ускоряющийся темп музыкального сэмпла.; Камера: Микро-зум FOV (на 3 градуса ближе на 0.08 с) в момент результативного попадания по парящей мишени.; UI/Тактильность: Визуальное разрастание перекрестья прицела с красным индикатором 'CRITICAL HIT', двойной тактильный тик (20 мс + 20 мс).
- **Technical Complexity**: Medium
- **Решение игрока**: Потратить дефицитные патроны на летящего врага ради гарантированного крита и дропа ресурсов или переключиться на наземные цели?
- **Состояния**: `TARGET_AIRBORNE: Противник находится в воздухе после пинка (флаг SKEET_READY)`, `AIM_LOCK: Прицел наведен на парящую тушу (динамический ромб захвата)`, `EXECUTION_SHOT: Попадание пули/картечи в фазе полета`, `BURST_REWARD: Дробление цели на куски, спавн патронов и начисление комбо-очков`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Множитель урона по парящей цели (Skeet Crit)` = `2.5x` — Ниже 1.8x — игрокам лень целиться в воздухе, проще расстрелять на земле; выше 3.5x — боссы и элитники ваншотаются без усилий.
  - `Окно зависания врага в воздухе` = `0.95 с` — Меньше 0.5 с — мобильные игроки не успевают довести палец до кнопки огня; больше 1.5 с — враги неестественно 'плавают' как в невесомости.
  - `Бонусный дроп патронов при Skeet-убийстве` = `+3 патрона` — Если 0 — игрок быстро проседает по боекомплекту; если больше 6 — стрельба становится бесконечной и пропадает менеджмент ресурсов.
- **Слои отклика**:
  - Визуал/VFX: Яркая радиальная вспышка золотисто-оранжевого цвета при попадании в воздухе, разлетающиеся гильзы и искры, текст 'SKEET CRIT!' всплывает над целью.
  - Звук (Web Audio): Высокий хрустальный звук 'дзынь' колокольчика поверх мощного выстрела дробовика, ускоряющийся темп музыкального сэмпла.
  - Камера: Микро-зум FOV (на 3 градуса ближе на 0.08 с) в момент результативного попадания по парящей мишени.
  - UI/Тактильность: Визуальное разрастание перекрестья прицела с красным индикатором 'CRITICAL HIT', двойной тактильный тик (20 мс + 20 мс).
- **Режим отказа игрока**: Игрок стреляет мимо летящего врага: патроны потрачены впустую, враг приземляется на ноги без урона от падения и немедленно стреляет в ответ.
- **Кривая мастерства**: Новичок успевает сделать лишь один неприцельный выстрел из пистолета; мастер производит связку 'пинок -> подброс -> переключение на дробовик -> два выстрела в воздухе по разным целям'.
- **Сопротивление игры**: Летающие дроны и тяжелые пулеметчики имеют гравитационные стабилизаторы, сокращающие время нахождения в воздухе на 70%.
- **Синергии**:
  - Кинетический тактический пинок: основной источник подбрасывания врагов на удобную высоту
  - Выбивание оружия: выбитый из рук ствол можно подстрелить в воздухе, вызвав его детонацию
- **Почему это не жанровый шаблон**: Объединяет механику стендовой стрельбы по тарелочкам (Skeet shooting) с аркадным FPS: игрок сам создает себе летящие мишени собственным пинком.
- **Псевдокод тика**:
```text
function onBulletHit(target, bullet):
  if target.hasStatus('AIRBORNE_SKEET'):
    finalDamage = bullet.damage * 2.5
    playSkeetAudioFeedback(); spawnParticles('gold_spark', target.pos)
    target.applyDamage(finalDamage)
    if target.isDead():
      spawnAmmoPickup(target.pos, amount=3)
      comboSystem.addScore(points=250, style='AIR_SKEET')
  else:
    target.applyDamage(bullet.damage)
```

### Каскадный снос окружения 'Кегельбан' (Domino Bowling Collisions) (SECONDARY)
- **Category**: Источник массового AoE-урона и цепных реакций
- **Description**: Превращает врагов из источников угрозы в бесплатные боеприпасы: чем плотнее стоит группа противников, тем быстрее и зрелищнее они уничтожают друг друга.
- **Player Input**: Позиционирование взгляда перед пинком / Выбор угла атаки
- **Hit & Sensory Feedback**: Визуал/VFX: Разлет осколков дерева, стекла и искр, оранжевый огненный шар при подрыве бочек, деформация бочек шейдером при ударе туши.; Звук (Web Audio): Сочный металлический звон сминаемой бочки, гулкий грохот взрыва с динамическим low-pass фильтром на 0.4 с.; Камера: Пространственная тряска (Radial Shake) с затуханием от эпицентра взрыва/удара.; UI/Тактильность: Всплывающий счетчик комбо 'STRIKE! x3', протяжная тяжелая вибрация (70 мс).
- **Technical Complexity**: Medium
- **Решение игрока**: Пнуть ли врага напрямую в толпу за ним, в красную взрывную бочку или в электрический щиток на стене?
- **Состояния**: `PROJECTILE_BODY: Враг/бочка летит как активный физический снаряд с включенным хитбоксом урона`, `PRIMARY_COLLISION: Столкновение с первой помехой (враг/стена/бочка)`, `CHAIN_TRANSFER: Передача 60% импульса следующему объекту в цепочке`, `EXPLOSIVE_RESOLVE: Детонация интерактивных бочек/баллонов при соприкосновении`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Радиус взрыва бочки от удара телом` = `4.2 м` — Меньше 3.0 м — редко задевает соседних врагов; больше 6.0 м — взрывает полкомнаты и самого игрока.
  - `Коэффициент передачи импульса второму врагу` = `0.65` — Меньше 0.4 — цепная реакция затухает на первом же враге; больше 0.85 — один пинок сносит всю комнату как шар для боулинга.
  - `Порог скорости для пробивания дверей/стекол` = `8.0 м/с` — Если выше 12 м/с — объекты почти никогда не пробиваются; если ниже 4 м/с — случайные легкие касания ломают декорации.
- **Слои отклика**:
  - Визуал/VFX: Разлет осколков дерева, стекла и искр, оранжевый огненный шар при подрыве бочек, деформация бочек шейдером при ударе туши.
  - Звук (Web Audio): Сочный металлический звон сминаемой бочки, гулкий грохот взрыва с динамическим low-pass фильтром на 0.4 с.
  - Камера: Пространственная тряска (Radial Shake) с затуханием от эпицентра взрыва/удара.
  - UI/Тактильность: Всплывающий счетчик комбо 'STRIKE! x3', протяжная тяжелая вибрация (70 мс).
- **Режим отказа игрока**: Игрок пинает взрывную бочку слишком близко к стене: бочка рикошетит обратно в игрока и взрывается ему под ноги.
- **Кривая мастерства**: Новичок взрывает бочки стрельбой издалека; мастер использует пинок по телу врага так, чтобы туша срезала двух стрелков и в конце траектории сдетонировала баллон с газом у пулеметного гнезда.
- **Сопротивление игры**: Тяжелые стационарные турели приварены к полу и не сдвигаются телами, принимая урон на себя и останавливая цепочку домино.
- **Синергии**:
  - Кинетический тактический пинок: первичный импульс запуска 'шара для боулинга'
  - Штурмовой вышиб дверей: створка двери выступает гигантским щитом-снарядом с огромной массой
- **Почему это не жанровый шаблон**: Превращает врагов из источников угрозы в бесплатные боеприпасы: чем плотнее стоит группа противников, тем быстрее и зрелищнее они уничтожают друг друга.
- **Псевдокод тика**:
```text
function onRagdollCollision(flyingBody, hitObject):
  speed = flyingBody.velocity.length()
  if speed < 3.0: return
  if hitObject.isEnemy:
    damage = speed * 4.5
    hitObject.applyDamage(damage)
    hitObject.applyImpulse(flyingBody.velocity * 0.65)
    spawnFloatingText('DOMINO!', hitObject.pos)
  elif hitObject.isExplosive:
    hitObject.detonate(radius=4.2, damage=120)
  flyingBody.velocity *= 0.4
```

### Выбивание оружия и перехват на лету (Disarm & Mid-Air Gun Catch) (SECONDARY)
- **Category**: Источник тактической адаптации и мгновенного пополнения огневой мощи
- **Description**: Избавляет от скучной паузы на перезарядку: вместо отсидки в укрытии самый быстрый способ получить патроны — агрессивно отобрать ствол у врага ногой.
- **Player Input**: Прицельный пинок в правое плечо/руку + Клавиша 'F' / Автозахват при приближении
- **Hit & Sensory Feedback**: Визуал/VFX: Золотистый силуэтный трейл за летящим в воздухе оружием, замедленное вращение ствола, вспышка в руках при успешном перехвате.; Звук (Web Audio): Звонкий лязг выбиваемого металла, щелчок взведения затвора в момент хвата, восторженный голосовой возглас протагониста.; Камера: Быстрый микро-доворот взгляда (Snapping на 10%) в сторону летящего оружия при нажатии кнопки подбора.; UI/Тактильность: Значок нового оружия всплывает по центру экрана с мигающим счетчиком патронов, резкий тактильный импульс (35 мс).
- **Technical Complexity**: Medium
- **Решение игрока**: Пнуть вооруженную руку врага, чтобы забрать мощный дробовик на 4 выстрела, или просто отправить его в стену?
- **Состояния**: `ARMED_ENEMY: Враг ведет огонь из специализированного оружия`, `DISARM_KICK: Попадание пинком в сектор оружия, оружие вылетает вверх с вращением`, `MID_AIR_GRAB: Игрок ловит ствол в воздухе в радиусе 2.5 м`, `OVERDRIVE_MAG: Игрок получает ствол с 1 полным магазином без перезарядки`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Высота подброса выбитого оружия` = `2.8 м` — Меньше 1.5 м — оружие падает под ноги и теряется в суматохе; больше 4.5 м — оружие улетает за пределы экрана и потолка.
  - `Радиус окна перехвата оружия` = `2.2 м` — Меньше 1.2 м — поймать на бегу в Three.js крайне сложно; больше 3.5 м — ствол 'примагничивается' сам без чувства скилла.
  - `Бонус к скорострельности перехваченного ствола` = `+35%` — Дает ощущение яростного адреналинового овердрайва на время отстрела магазина.
- **Слои отклика**:
  - Визуал/VFX: Золотистый силуэтный трейл за летящим в воздухе оружием, замедленное вращение ствола, вспышка в руках при успешном перехвате.
  - Звук (Web Audio): Звонкий лязг выбиваемого металла, щелчок взведения затвора в момент хвата, восторженный голосовой возглас протагониста.
  - Камера: Быстрый микро-доворот взгляда (Snapping на 10%) в сторону летящего оружия при нажатии кнопки подбора.
  - UI/Тактильность: Значок нового оружия всплывает по центру экрана с мигающим счетчиком патронов, резкий тактильный импульс (35 мс).
- **Режим отказа игрока**: Игрок не успевает поймать оружие, оно падает на пол и взрывается от перегрева через 1.5 секунды, нанося легкий урон вокруг.
- **Кривая мастерства**: Новичок подбирает оружие с пола после боя; мастер выбивает ногой ракетомет у элитника, ловит его на лету в воздухе и тут же стреляет в толпу до приземления.
- **Сопротивление игры**: Некоторые элитные охранники пристегивают оружие к экзоскелету тросом — их разоружение требует предварительного сбивания силовой брони.
- **Синергии**:
  - Кинетический тактический пинок: активатор механики выбивания
  - Штурмовой вышиб дверей: во время bullet-time поймать оружие в воздухе намного проще и зрелищнее
- **Почему это не жанровый шаблон**: Избавляет от скучной паузы на перезарядку: вместо отсидки в укрытии самый быстрый способ получить патроны — агрессивно отобрать ствол у врага ногой.
- **Псевдокод тика**:
```text
function onKickHitLimb(enemy, hitLimb):
  if hitLimb == 'WEAPON_ARM' and enemy.hasWeapon():
    gun = enemy.dropWeapon()
    gun.applyImpulse(Vector3(0, 4.2, 0) + player.forward * -1.5)
    gun.enableMidAirCatchTrigger(radius=2.2, duration=1.4)
    enemy.enterState('DISARMED_PANIC', duration=2.0)
function onGunTriggerEnter(player, gun):
  if input.interactPressed or player.hasAutoMagnetPerk:
    player.equipTempWeapon(gun.type, ammo=gun.maxMag, fireRateBonus=1.35)
    playSound('gun_cock_fast')
```

### Штурмовой вышиб дверей с адреналиновым фокусом (Breach Kick & Adrenaline Slow-Mo) (SECONDARY)
- **Category**: Инициатор боевой ситуации, тактический переключатель комнат
- **Description**: Превращает банальную загрузку/переход между комнатами в кульминационную микро-сцену боевика, задающую ритм всему бою в отсеке.
- **Player Input**: Спринт + Пинок (Shift + E / Свайп вперед во время бега)
- **Hit & Sensory Feedback**: Визуал/VFX: Радиальный блюр по краям экрана (Radial Speed Lines), выбивание щепок и петель, хроматическая аберрация в фазе Slow-Mo.; Звук (Web Audio): Оглушительный грохот ломающегося дерева/металла, плавный срез высоких частот (low-pass filter) и глухой стук сердца (Heartbeat 80 BPM).; Камера: Динамический наезд камеры вперед (Kick Zoom-In) на 0.4 м с последующим расширением FOV до 100°.; UI/Тактильность: Нарастающий вибро-отклик при ударе (60 мс), силуэтная подсветка врагов сквозь пыль красным контуром.
- **Technical Complexity**: Medium
- **Решение игрока**: Вынести дверь по центру с максимальным шумом и bullet-time или заглянуть сбоку, сэкономив адреналин?
- **Состояния**: `APPROACH: Сближение с запертой/приоткрытой дверью отсека`, `BREACH_IMPACT: Контакт подошвы с дверью, расчет разрушения петель`, `DOOR_FLIGHT: Полотно двери летит вперед, сокрушая все на пути`, `BULLET_TIME: Замедление игрового времени до 25% на 0.8 с для оценки угроз`, `NORMAL_TIME: Плавный возврат к нормальной скорости времени (0.2 с)`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Коэффициент замедления времени (Time Scale)` = `0.25x` — Если ниже 0.15x — темп игры падает, становится скучно ждать; если выше 0.45x — игрок не успевает осмотреться и расставить приоритеты.
  - `Длительность адреналинового фокуса` = `0.80 с` — Меньше 0.5 с — эффект не воспринимается как bullet-time; больше 1.5 с — рушится ощущение драйвового быстрого экшена.
  - `Урон от летящего дверного полотна` = `90 ед` — Должно гарантированно ваншотать базовых охранников первой линии, создавая мгновенный вау-эффект прорыва.
- **Слои отклика**:
  - Визуал/VFX: Радиальный блюр по краям экрана (Radial Speed Lines), выбивание щепок и петель, хроматическая аберрация в фазе Slow-Mo.
  - Звук (Web Audio): Оглушительный грохот ломающегося дерева/металла, плавный срез высоких частот (low-pass filter) и глухой стук сердца (Heartbeat 80 BPM).
  - Камера: Динамический наезд камеры вперед (Kick Zoom-In) на 0.4 м с последующим расширением FOV до 100°.
  - UI/Тактильность: Нарастающий вибро-отклик при ударе (60 мс), силуэтная подсветка врагов сквозь пыль красным контуром.
- **Режим отказа игрока**: Попытка выбить укрепленную титановую дверь без накопленного импульса скорости: игрок спотыкается, теряет скорость и не активирует bullet-time.
- **Кривая мастерства**: Новичок выбивает дверь и стреляет в ближайшего охранника; мастер в фазе Slow-Mo успевает оценить всю комнату, выбить пистолет у левого врага, бросить баллон в правого и приземлиться за укрытием.
- **Сопротивление игры**: Заминированные двери с растяжками: при выбивании двери взрыв происходит прямо в проеме, требуя от игрока вовремя сделать подкат в сторону.
- **Синергии**:
  - Каскадный снос окружения: выбитая дверь — самый тяжелый метательный снаряд на арене
  - Выбивание оружия: замедление времени дает идеальное окно для захвата выбитого оружия на лету
- **Почему это не жанровый шаблон**: Превращает банальную загрузку/переход между комнатами в кульминационную микро-сцену боевика, задающую ритм всему бою в отсеке.
- **Псевдокод тика**:
```text
function onBreachDoor(door, playerSpeed):
  if playerSpeed < 4.0: door.openNormal(); return
  door.breakHinges()
  door.applyPhysicsImpulse(player.forward * 22.0, torque=Vector3(5,0,0))
  timeManager.setTimeScale(0.25, duration=0.8, easeOut=0.2)
  postProcessing.enableBreachFX(blur=0.6, chromatic=0.4)
  audio.playLowPassSnapshot('slowmo_heartbeat', duration=0.8)
  camera.punchFOV(targetFOV=100, duration=0.8)
```

### Динамическое перемещение и инерция (CORE)
- **Category**: movement
- **Description**: Плавное управление с сочным разгоном, торможением и контролем дистанции.
- **Player Input**: Основной жест управления на тач-экране / ЛКМ / Пробел
- **Hit & Sensory Feedback**: Визуал: сочный шлейф частиц и явное изменение состояния цели; Звук: двухслойный звуковой эффект (удар + подтверждение успеха); Камера: короткий направленный импульс тряски (shake); UI: микро-индикатор готовности рядом с точкой взгляда
- **Technical Complexity**: Medium
- **Решение игрока**: Когда и с каким запасом применять «Динамическое перемещение и инерция», зная цену промаха
- **Состояния**: `ГОТОВО`, `АКТИВНО`, `ВОССТАНОВЛЕНИЕ`, `ЗАБЛОКИРОВАНО`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Окно исполнения` = `0.25 с` — Меньше — механика ощущается несправедливой; больше — исчезает мастерство
  - `Восстановление` = `0.9 с` — Короче — спам действия; длиннее — игрок скучает
- **Слои отклика**:
  - Визуал: сочный шлейф частиц и явное изменение состояния цели
  - Звук: двухслойный звуковой эффект (удар + подтверждение успеха)
  - Камера: короткий направленный импульс тряски (shake)
  - UI: микро-индикатор готовности рядом с точкой взгляда
- **Режим отказа игрока**: Применение раньше времени дает осечку и оставляет персонажа открытым
- **Кривая мастерства**: Новичок нажимает наугад; мастер готовит ситуацию и применяет в пик тайминга
- **Сопротивление игры**: Игра постепенно сужает безопасные зоны применения
- **Синергии**:
  - Успешное применение подготавливает «Тактическое применение ключевой способности»
- **Почему это не жанровый шаблон**: Спроектировано вокруг крючка «Динамичный игровой процесс с сочным откликом и рогалик-прокачкой между забегами.»
- **Псевдокод тика**:
```text
on_action():
    if state != READY: return reject_feedback()
    state = ACTIVE; apply_action_physics()
    emit_feedback(); start_cooldown(0.9)
```

### Тактическое применение ключевой способности (CORE)
- **Category**: combat
- **Description**: Активация профильного действия с расчетом тайминга и зоны поражения.
- **Player Input**: Вторичная кнопка / свайп / ПКМ
- **Hit & Sensory Feedback**: Визуал: радиальная ударная волна и смещение окружающих объектов; Звук: низкочастотный бас-панч с реверберацией; Камера: мягкий зум-импульс; UI: шкала энергии с подсветкой готовности
- **Technical Complexity**: Medium
- **Решение игрока**: Потратить ресурс на «Тактическое применение ключевой способности» сейчас или сберечь для критической ситуации
- **Состояния**: `ГОТОВО`, `ДЕЙСТВИЕ`, `ПЕРЕЗАРЯДКА`
- **Числовые параметры (реализуй именно эти значения)**:
  - `Расход энергии` = `30 ед` — Балансирует частоту использования
  - `Радиус эффекта` = `4.5 м` — Определяет тактическую зону контроля
- **Слои отклика**:
  - Визуал: радиальная ударная волна и смещение окружающих объектов
  - Звук: низкочастотный бас-панч с реверберацией
  - Камера: мягкий зум-импульс
  - UI: шкала энергии с подсветкой готовности
- **Режим отказа игрока**: Активация в пустом пространстве впустую сжигает накопленный ресурс
- **Кривая мастерства**: Точный расчет тайминга для максимального охвата целей
- **Сопротивление игры**: Мобильные цели выходят из радиуса действия
- **Синергии**:
  - Усиливает отдачу от «Динамическое перемещение и инерция»
- **Почему это не жанровый шаблон**: Дает игроку инструмент перелома сложной ситуации
- **Псевдокод тика**:
```text
on_secondary():
    if energy < 30: return energy_empty_vfx()
    energy -= 30; apply_aoe_effect(4.5)
    emit_bass_punch(); state = COOLDOWN
```



---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Application Layer**: Инициализация Vite, ресайз канваса, полноэкранный режим, прелоадер.
- **Platform & Ads Layer**: Адаптер Playgama Bridge, менеджеры Interstitial и Rewarded рекламы, Cloud Save.
- **Core Engine Layer**: Фиксированный цикл GameLoop 60Гц, EventBus, маршрутизация ввода.
- **Physics Simulation Layer**: Шаги симуляции Rapier3D, фильтрация коллизий, рэгдолл-синхронизация.
- **Gameplay Systems Layer**: Боевая система, спавн врагов, выбор 3 карт улучшений, расчет комбо.
- **Entity Management Layer**: Пул сущностей игрока, врагов, снарядов и осколков.
- **Rendering Layer**: Three.js граф сцены, InstancedMesh батчинг, тени, эмиттеры частиц.
- **UI & HUD Layer**: HTML5/CSS3 оверлей, виртуальный джойстик, полосы HP, модальные окна.

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   ├── PhysicsWorld.ts        # Rapier3D / Physics world manager
│   └── RagdollController.ts    # Joint solver, balance spring torque, knockback
├── entities/
│   ├── Player.ts              # Player character entity & input impulses
│   ├── Enemy.ts               # Enemy AI behavior tree & ragdoll death
│   └── Weapon.ts              # Weapon mass, hitboxes, collision queries
├── systems/
│   ├── CombatSystem.ts        # Hitbox resolution, parry timing, damage formulas
│   ├── WaveManager.ts         # Spawning curves, elite bosses, wave clears
│   ├── UpgradeManager.ts      # 3-card roguelite selection & stat application
│   └── CrowdFavorSystem.ts    # Hype calculation and dynamic drop rewards
├── rendering/
│   ├── SceneManager.ts        # Three.js / PixiJS scene graph, lighting, camera lerp
│   ├── MeshPool.ts            # InstancedMesh pooling for debris & effects
│   └── Shaders.ts             # Optimized mobile shaders & materials
├── ui/
│   ├── UIManager.ts           # DOM HUD overlay, screen transitions
│   ├── VirtualJoystick.ts     # Mobile touch floating joystick
│   └── CardModal.ts           # 3-choice upgrade modal
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic battle BGM
```

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Второе Дыхание (Возрождение) (`revive_run`)**: Восстановление 50% HP + 3 сек неуязвимости с силовой волной, раскидывающей врагов. (Trigger: При получении смертельного урона., Limit: 1 раз за забег.)
- **Удвоение Наград (2x Золото) (`double_gold_run`)**: Удваивает все заработанные шестеренки и монеты за завершенный раунд. (Trigger: Экран окончания игры., Limit: Доступно на каждом экране результатов.)
- **Переброс Карт Улучшений (`free_card_reroll`)**: Обновляет список 3 карт улучшений с гарантией Редкой или Эпической карты. (Trigger: Окно выбора 3 карт., Limit: До 2 раз за забег.)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"player_save_v1"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Landscape
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Обязательный контракт тач-управления
- **Слева — ДВИЖЕНИЕ**: плавающий виртуальный джойстик на 2 оси, зона захвата — вся левая половина экрана.
- **Справа — ДЕЙСТВИЯ**: крупная основная кнопка (атака/использование) и 2–3 второстепенные (дэш, блок, спец-умение).
- Движение и атака должны работать одновременно.

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Плавающий стик**: зона захвата — вся левая половина экрана, база стика
  появляется под пальцем. Мёртвая зона 8%, иначе управление дрожит.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `WASD` / стрелки — движение
- ЛКМ / `J` — основная атака
- ПКМ / `K` — тяжёлая атака / блок
- `Space` / `Shift` — рывок / уклонение
- `P` / `Esc` — пауза

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/ideya-kick-shoot/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ART DIRECTION & VISUAL GUIDELINES
- **Style**: Чистый стилизованный стиль с высоким контрастом
- **Camera Perspective**: Изометрическая 3D камера с динамическим зумом (FOV: 50°, Pitch: 45°)
- **Environment**: Читаемая сцена, где игрок и опасность различимы силуэтом
- **Lighting**: Направленный свет с мягкими тенями и тёплым заполнением
- **Visual Feedback**: Screen-space hitstop (40ms on critical hit), directional particle sparks, additive ribbon weapon trails.

---

## 8. STEP-BY-STEP DEVELOPMENT ROADMAP
### Phase 1: Архитектура и Базовый Прототип (3 days)
- **Deliverable**: Управляемая модель машины/персонажа на 3D арене с 60 FPS
  - Настройка Vite, TypeScript и @playgama/bridge
  - Инициализация сцены Three.js и физического мира Rapier3D
  - Реализация контроллера движения и физики заноса
### Phase 2: Боевая Система и Спавн Врагов (4 days)
- **Deliverable**: Рабочий боевой цикл с волнами противников
  - Система расчета коллизий, урона и хит-стопа
  - Пул врагов с базовым AI преследования
  - Реализация шкалы ярости и супер-ударов
### Phase 3: Рогалик-Прогрессия и UI (3 days)
- **Deliverable**: Полноценный цикл забега с прокачкой способностей
  - Система 3-Card улучшений и синергий
  - Верстка адаптивного HUD и меню на HTML5/CSS3
  - Звуковые эффекты через Howler.js
### Phase 4: Интеграция Playgama Bridge и Полишинг (2 days)
- **Deliverable**: Полная интеграция с порталом Яндекс Игры
  - Интеграция Rewarded и Interstitial рекламы
  - Сохранение прогресса в Cloud Storage
  - Подключение глобальных таблиц рекордов
### Phase 5: QA, Оптимизация и Релиз (2 days)
- **Deliverable**: Готовая релизная сборка для публикации
  - Тестирование на мобильных устройствах (iOS/Android)
  - Профайлинг WebGL памяти и вызовов отрисовки
  - Финальная сборка и проверка Definition of Done


---

## 9. NON-NEGOTIABLE PLATFORM RULES
Every rule below corresponds to a bug that reached production or a moderation rejection in a shipped game. They override any conflicting habit, tutorial or example — including snippets found in the Playgama/Yandex docs, many of which describe the deprecated Bridge v1 contract.

Distilled from shipped HTML5 games on Yandex Games, VK, OK, CrazyGames and
Playgama. Every rule here corresponds to a bug that reached production or a
moderation rejection. Violating any of them ships a broken or rejected game.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Renderer

52. Auto-tuning quality from raw frame time does not work under vsync — every frame
    takes ≈ the refresh interval regardless of GPU load. Measure the cadence between
    **rendered** frames and discover headroom by optimistic probing.
53. Never target above the panel's refresh rate, and never launch in reduced quality
    and crawl up — start optimistic and step down.
54. Apply resolution and shadow-map changes **before** `render()`, on a frame you
    actually render; doing it after clears the canvas and flashes a blank frame.
55. One group, one degree of freedom. A mesh that both steers and spins (wheel,
    turret, limb) needs **nested** groups — `Euler('XYZ')` applies the inner
    rotation around the parent's axis and visibly skews the part otherwise.
    See `knowledge/threejs/vehicle_wheel_rig.md`.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Physics vehicles

60. A physics vehicle is **never** driven by writing `setLinvel()` every frame.
    That overwrites the solver, ignores slopes and heading, and leaves the wheels
    as decoration — the "wheels don't turn, the truck doesn't move" bug. Use the
    engine's own ray-cast vehicle controller (`world.createVehicleController`).
61. Visual wheels of a physics vehicle are **children of the chassis group**, and
    their suspension travel, steering angle and roll angle are read from the
    controller. A separate wheel root that copies only `translation()` detaches
    the wheels from the body the moment it tilts, and leaves parts overlapping
    where the body has moved on. The opposite rule in `vehicle_wheel_rig.md` §3
    applies to kinematic arcade cars only.
62. `updateVehicle(dt)` runs **before** `world.step()`, and the wheels' ray-casts
    are filtered to the ground collision group — unfiltered they hit the chassis
    or the cargo and the vehicle climbs its own load.
63. Cargo and props carried by a body spawn from **body-local** slots that do not
    intersect its colliders. An overlap at spawn is resolved by ejection: the load
    fires out of the bed on frame one. Anything meant to stay put needs real wall
    colliders, not a painted lip.
64. Terrain is one continuous displaced ribbon whose collider is built from the
    **same buffers** as the visible mesh. A road assembled from individually
    rotated boxes has a ledge at every joint. Trimesh colliders are for static
    bodies only.
65. Restarting a run **teleports** existing bodies (translation, rotation and both
    velocities, then `wakeUp()`); it never rebuilds meshes and bodies. Rebuilds
    leak the old body, dispose shared geometry and orphan the vehicle controller.
    Removed items are disabled, not destroyed.
66. Physics handling is verified head-lessly before it is verified by eye — the
    physics engine runs in Node without a renderer. Keep the vehicle spec in a
    renderer-free module and assert acceleration, wheel-rotation sign, steering
    sign, suspension settling and cargo retention in a script.
    See `knowledge/threejs/rapier_vehicle_controller.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

---

## 10. DEFINITION OF DONE (MANDATORY VERIFICATION CHECKLIST)
To mark this game as complete, every single requirement below must be verified and working:

- [ ] Полная компиляция проекта на TypeScript без ошибок сборки
- [ ] Стабильные 60 FPS в браузере с временем отклика управления < 50мс
- [ ] Рабочий цикл сессии: старт -> волны врагов -> 3-Card апгрейды -> битва с боссом -> результат
- [ ] Интеграция Playgama Bridge: Rewarded видео, баннеры, Cloud Save и Leaderboards
- [ ] Мобильное управление на Pointer Events: движение и основное действие работают одновременно, мультитач, отмена скролла и контекстного меню, safe-area
- [ ] Слой тач-управления виден только в игровом процессе и сбрасывается при паузе, сворачивании вкладки и показе рекламы
- [ ] Мобильную раскладку можно проверить на десктопе флагом ?touch=1
- [ ] Звуковое сопровождение: фоновая музыка и сочные звуки попаданий/дрифта
- [ ] `npm install && npm run dev` поднимают играбельную сборку без ошибок в консоли
- [ ] Ведутся DEVLOG.md и CHANGELOG.md, в README.md описаны запуск и управление
- [ ] Соответствие всем критериям публикации на Яндекс Играх

---

## 11. FACTORY KNOWLEDGE BASE
Deep, worked-out detail behind the rules in section 9 — read the relevant file before implementing that area:

- `knowledge/CRITICAL_RULES.md`
- `knowledge/audio/web_audio_and_muting.md`
- `knowledge/compliance/qa_checklist.md`
- `knowledge/compliance/yandex_moderation.md`
- `knowledge/mechanics/base_building.md`
- `knowledge/mechanics/card_synergy.md`
- `knowledge/mechanics/chain_reaction.md`
- `knowledge/mechanics/combo_juggling.md`
- `knowledge/mechanics/cooking_flow.md`
- `knowledge/mechanics/dash.md`
- `knowledge/mechanics/drone_swarm.md`
- `knowledge/mechanics/evidence_board.md`
- `knowledge/mechanics/fluid_buoyancy.md`
- `knowledge/mechanics/grappling_hook.md`
- `knowledge/mechanics/grid_building.md`
- `knowledge/mechanics/mining_drill.md`
- `knowledge/mechanics/parry.md`
- `knowledge/mechanics/physics_destruction.md`
- `knowledge/mechanics/ragdoll.md`
- `knowledge/mechanics/rhythm_sync.md`
- `knowledge/mechanics/stealth_detection.md`
- `knowledge/mechanics/time_rewind.md`
- `knowledge/mechanics/upgrade_choices.md`
- `knowledge/mechanics/vehicle_physics.md`
- `knowledge/mechanics/wave_survival.md`
- `knowledge/monetization/in_app_purchases.md`
- `knowledge/monetization/interstitial_best_practices.md`
- `knowledge/monetization/rewarded_ads_patterns.md`
- `knowledge/patterns/arena_combat_loop.md`
- `knowledge/patterns/builder_defense_loop.md`
- `knowledge/patterns/physics_arcade_loop.md`
- `knowledge/patterns/roguelike_loop.md`
- `knowledge/patterns/score_attack_loop.md`
- `knowledge/patterns/survivor_loop.md`
- `knowledge/pixijs/particle_systems.md`
- `knowledge/pixijs/sprite_batching.md`
- `knowledge/platform_builds/android_capacitor.md`
- `knowledge/playgama/ads_integration.md`
- `knowledge/playgama/auth_and_player.md`
- `knowledge/playgama/banners_and_layout.md`
- `knowledge/playgama/bridge_api_reference.md`
- `knowledge/playgama/game_ready_and_loading.md`
- `knowledge/playgama/lifecycle_and_orientation.md`
- `knowledge/playgama/platform_matrix.md`
- `knowledge/playgama/social_features.md`
- `knowledge/playgama/storage_and_cloud.md`
- `knowledge/references/brotato.md`
- `knowledge/references/dome_keeper.md`
- `knowledge/references/gladihoppers.md`
- `knowledge/references/slay_the_spire.md`
- `knowledge/references/toribash.md`
- `knowledge/references/vampire_survivors.md`
- `knowledge/threejs/adaptive_quality.md`
- `knowledge/threejs/mobile_shaders.md`
- `knowledge/threejs/performance_guide.md`
- `knowledge/threejs/physics_integration.md`
- `knowledge/threejs/rapier_vehicle_controller.md`
- `knowledge/threejs/vehicle_wheel_rig.md`
- `knowledge/ux/localization_system.md`
- `knowledge/ux/touch_controls.md`
- `knowledge/ux/ui_design_system.md`

---

## 12. DETAILED REFERENCE DOCUMENTS
For extended deep specifications, refer to the accompanying project documentation files:
- [Инструкция агенту (AGENTS.md)](./AGENTS.md)
- [Журнал разработки (DEVLOG.md)](./DEVLOG.md)
- [Changelog](./CHANGELOG.md)
- [Game Design Document](./GAME_DESIGN_DOCUMENT.md)
- [Gameplay Specification](./GAMEPLAY_SPECIFICATION.md)
- [Technical Specification](./TECHNICAL_SPECIFICATION.md)
- [Architecture Document](./ARCHITECTURE_DOCUMENT.md)
- [Playgama Integration](./PLAYGAMA_INTEGRATION.md)
- [Monetization Specification](./MONETIZATION.md)
- [Mobile Controls](./MOBILE_CONTROLS.md)
- [QA Plan](./QA_PLAN.md)
- [Обещание игроку (PLAYER_PROMISE.md)](./PLAYER_PROMISE.md)
- [Дизайн-ядро (DESIGN_NUCLEUS.md)](./DESIGN_NUCLEUS.md)
- [Плотность впечатлений (EXPERIENCE_DENSITY.md)](./EXPERIENCE_DENSITY.md)
- [Спецификация телеметрии (TELEMETRY_SPEC.md)](./TELEMETRY_SPEC.md)
- [Реестр допущений (ASSUMPTIONS.md)](./ASSUMPTIONS.md)
- [План валидации (VALIDATION_PLAN.md)](./VALIDATION_PLAN.md)
- [Журнал решений (DECISIONS.md)](./DECISIONS.md)
- [Человеческие ворота (HUMAN_GATES.md)](./HUMAN_GATES.md)
- [Game Skill Guidelines](./skills/GAME_SKILL.md)
- [Renderer Skill](./skills/RENDERER_SKILL.md)