# Журнал разработки — Тени Фестиваля: Клинок и Эшелон

Этот файл ведёт ИИ-агент. Каждая сессия работы = одна запись:
что просили, что сделано, какие файлы затронуты, что проверено, что осталось.

---

## 2026-08-23 17:13 — Инициализация проекта
- **Задача**: создан каталог проекта в workspace, подготовлена спецификация.
- **Сделано**: сгенерирован пакет документации фабрики.
- **Следующий шаг**: реализация игрового кода по AI_DEVELOPER_PROMPT.md.

---

## 2026-08-23 18:40 — Полная реализация игры с нуля
- **Задача**: создать рабочую структуру HTML5-игры (package.json, Vite, index.html,
  src/main.ts, рендерер, физика, управление, аудио, Playgama Bridge) — весь код.
- **Сделано**:
  - Каркас: `package.json` (three ^0.185, @playgama/bridge ^2.1, vite ^5, TS strict),
    `vite.config.ts`, `tsconfig.json`, `index.html`, `public/playgama-bridge-config.json`,
    `src/vite-env.d.ts`, скрипты dev/build/preview/check:spec/smoke.
  - `DESIGN.md` написан до кода: палитра, камера, экраны, сцена за меню, таблица глаголов,
    объяснение отсутствия вертикали.
  - Мир: три платформы-эшелона с мостами, процедурные меши без GLTF, ящики-укрытия
    (блокируют взгляд), тень навесов, пьедестал тотема, контрольная точка переулка.
  - Механики по balance.yaml: маскировка в потоке танцоров (радиус/группа/−85%),
    ритмический выпад (окно ±0.15 с от BPM 128, сектор со спины 135°, шум 6 м),
    парирование (окно 0.18 с, сектор 110°, оглушение 1.2 с, штраф промаха 0.35 с),
    пинок-рэгдолл (верлет, импульс 14.5 м/с), переноска тотема (−35% скорости,
    разворот 120°/с, таран 1.6 м, левитация в такт −70%), конфетти-хлопушки
    (облако 3.5 м, слепота 2.5 с, приманивает на 8 м), тревога/таймер 120 с,
    победа у переулка, провал по тревоге 100%, трём ударам или времени.
  - Стража: конусы зрения с углом и дальностью, двухступенчатая проверка
    (дешёвый угол+дистанция каждый кадр, проверка преград раз в 0.15 с), период
    прощения, подозрение с тремя состояниями и маркерами над головой, шум переводит
    в проверку, но тревогу поднимают только глаза; элиты на 2–3 эшелонах.
  - Аудио: WebAudio-синтез без файлов, ленивый контекст по первому жесту,
    один мастер-гейн, трек шествия планировщиком по AudioContext.currentTime,
    suspend/resume по visibilitychange.
  - Управление: две схемы — клавиатура+мышь и тач (джойстик с pointerId-захватом,
    зона «Шаг шествия», кнопки выпада/блока/пинка/рывка/хлопушки); режим от
    bridge.device.type с форсом ?input=touch|desktop и ?touch=1; слой тача
    монтируется в DOM только активной схемы и скрыт в меню/паузе.
  - Площадка: initialize с таймаутом 10 с + сторожевой 15 с, game_ready ровно
    один раз после интерактива меню, прогресс заставки по реальным вехам до 100%,
    сохранение одним ключом thief_gold с normalize, rewarded строго по состоянию
    «rewarded» со снятием слушателя, interstitial с интервалом и запретом на старте,
    покупки no_ads (getPurchases → выдача → consumePurchase), пауза/звук из событий моста.
  - UI: четыре экрана (MainMenu/HUD/Result/Workshop) в роутере, корни вставлены
    при регистрации, скрытые — display:none; токены темы в единственном theme.css,
    z-index только через --z-*, tabular-nums в слоте таймера, кнопки ≥64 px
    (основная ≥96 px), локализация ключами RU/EN с полным паритетом, язык с площадки.
- **Взял из готового кода фабрики**: ничего — `node scripts/fetch-knowledge.mjs`
  вернул HTTP 403 для всех 44 файлов базы знаний (токен не задан), `docs/ref/`
  остался пуст, модули knowledge-showcase недоступны. Весь код написан с нуля;
  структура повторяет уроки из skills/ и чек-листов ACCEPTANCE.md.
- **Затронутые файлы**: package.json, vite.config.ts, tsconfig.json, index.html,
  public/playgama-bridge-config.json, DESIGN.md, src/** (config/balance.ts,
  core/events.ts, core/loop.ts, platform/playgama.ts, platform/save.ts,
  audio/audio.ts, render/renderer.ts, render/world.ts, render/vfx.ts,
  render/meshUtil.ts, game/heist.ts, game/player.ts, game/guards.ts,
  game/dancers.ts, input/inputManager.ts, input/touchControls.ts, ui/theme.css,
  ui/lang.ts, ui/ui.ts, ui/screens/*.ts, main.ts, vite-env.d.ts),
  ACCEPTANCE.md (разделы O и H отработаны), DEVLOG.md, CHANGELOG.md.
- **Проверено**: `npm run build` — 0 ошибок TS; `node scripts/smoke.mjs` — S1–S7
  зелёные (26 FPS на swiftshader, 0 ошибок консоли, сборка 0.88 МБ);
  `node scripts/check-spec.mjs` — все проверки зелёные, H1: сделано ~140,
  осознанных отказов ~60 с причинами.
- **Известные проблемы / следующий шаг**: физдвижок не подключён (кинематика +
  верлет — осознанный отказ, отмечен в ACCEPTANCE); связок из трёх приёмов нет —
  боёвку держат выпад/блок/пинок; на слабых устройствах адаптивное качество
  снижает масштаб рендера ступенчато — стоит проверить на реальном телефоне;
  следующий шаг — живая приёмка D1–D7 глазами и E1 на реальном устройстве.
