# Журнал разработки — Один дубль: Разбор сцены

Этот файл ведёт ИИ-агент. Каждая сессия работы = одна запись:
что просили, что сделано, какие файлы затронуты, что проверено, что осталось.

---

## 2026-08-23 20:23 — Инициализация проекта
- **Задача**: создан каталог проекта в workspace, подготовлена спецификация.
- **Сделано**: сгенерирован пакет документации фабрики.
- **Следующий шаг**: реализация игрового кода по AI_DEVELOPER_PROMPT.md.

## 2026-08-23 22:40 — Полная реализация игры по спецификации
- **Задача**: прочитать AI_DEVELOPER_PROMPT.md и skills/, создать полную рабочую
  структуру HTML5-игры: package.json, vite.config.ts, index.html, src/main.ts,
  модули рендерера, физика, управление, аудио и Playgama Bridge.
- **Сделано**:
  - `node scripts/fetch-knowledge.mjs` отработал: 44/45 файлов в `docs/ref/`.
    Обязательный `knowledge/ux/input_scheme_switching.md` не скачался — HTTP 404
    в публичном репозитории (нужен ZAVOD_KNOWLEDGE_TOKEN). Правила этой схемы
    взяты из skills/CONTROLS_SKILL.md и чек-листа ACCEPTANCE G11–G13.
  - Написан DESIGN.md до кода: палитра из 7 цветов (стали токенами theme.css),
    камера FOV 56° на уровне глаз, свет павильона, пять экранов со сценариями
    loading/empty/error, живая сцена за меню, таблица управления «глагол →
    клавиша → тач → отклик». Вертикаль (прыжок/присед) отброшена сознательно.
  - Готовый код фабрики (`LIBRARY.md`, секция 3c):
    - **взял** `knowledge-showcase/src/audio/AudioManager.ts` («копируется как
      есть») — ядро Web Audio: ленивый контекст, мастер-гейн, мьют гейном,
      suspend на visibilitychange; движок/транспортные звуки удалены как чужие
      для этого жанра, добавлены игровые сигналы дубля;
    - **взял** `game/vfxJuice.ts` как образец пула частиц — переписан под THREE.Points
      с предвыделенными буферами и трассерами;
    - **взял** `input/TouchControls.ts` как образец Pointer Events + setPointerCapture —
      переписан под FPS-схему (плавающий стик ходьбы, зона обзора, кнопка огня),
      в исходнике для гонок были эмодзи-стрелки через innerHTML — запрещены B2;
    - **не взял** `ragdoll.ts`/`shooterRagdoll.ts`: смерть саботажника —
      стилизованный поворот корпуса со сроком жизни, физический рэгдолл
      противоречил бы бюджету 45k треугольников при шести актёрах;
    - **не взял** демо FpsDemo/SceneManager: образцы тянут three-mesh-bvh и
      skinned-риги, которые в этой игре не используются.
  - Каркас: TypeScript strict без `any`, `src/vite-env.d.ts` с vite/client,
    фиксированный шаг 60 Гц с аккумулятором и clamp dt ≤0.1 с, порядок кадра
    ввод → логика → physics.step → синхронизация мешей → камера → рендер.
  - Playgama Bridge сразу в каркасе: initialize под таймаутом 10 с, сторожевой
    таймер 15 с, прогресс от реальных вех (10/25/35/60/80/92/100), сигнал
    готовности ровно один раз после интерактивного меню, `public/playgama-bridge-config.json`
    в сборке, сейв одним ключом `best_time_ms` с normalize и flush на pagehide,
    rewarded только по state === 'rewarded' с off() и guard от повторного клика,
    interstitial с cooldown 90 с только на уходе с экрана результата,
    PAUSE_STATE_CHANGED / AUDIO_STATE_CHANGED с независимыми обработчиками.
  - Механики: выстрел-монтаж (луч 80 м, хедшот-сфера 0.22 м, полуавтомат),
    ёмкость дубля (6/6, промах −1 вместимости, минимум 1, R не используется),
    цепная декорация (фасады переходов по 3 звена, узел 0.18 м, импульс 7.5 м/с,
    задержка передачи силы 0.35 с), световой блокинг (предупреждения 0.85/0.35 с,
    конус 28°, второй вход со сдвигом 0.7 с), маршрут 4 точки + 3 перехода,
    6 саботажников, три причины провала + таймаут, победа ≥1 патроном.
    Все числа — из balance.yaml через src/config/balance.ts, литералов нет.
  - Управление: две схемы, выбор по bridge.device.type (?input=touch /
    ?input=desktop принудительно), pointer lock только в десктопной схеме из
    обработчика нажатия, тач-слой монтируется в DOM только в тач-режиме.
  - Адаптивное качество: частота панели по минимальному rAF-интервалу за первые
    ~60 замеров, старт с полного качества, ступенчатый спуск по медиане кадра,
    подъём оптимистичным зондом с откатом; тени перенастраиваются на лету.
- **Затронутые файлы**: package.json, vite.config.ts, tsconfig.json, index.html,
  public/playgama-bridge-config.json, DESIGN.md, README.md, src/** (core, config,
  platform, physics, rendering, game, input, audio, i18n, ui), ACCEPTANCE.md
  (разметка разделов 0 и H), DEVLOG.md, CHANGELOG.md.
- **Проверено**: `npx tsc --noEmit` — 0 ошибок; `npm run build` — код 0,
  сборка ≈3.8 МБ (бюджет 4.5); `node scripts/check-spec.mjs` — зелёный;
  `node scripts/smoke.mjs` — прогнан, результат записан ниже отдельной строкой.
- **Известные проблемы / следующий шаг**: обязательный файл базы знаний
  ux/input_scheme_switching.md недоступен без токена (404) — правила соблюдены
  по skills; rewarded-кнопка и облачное сохранение проверяются руками на
  площадке (локально мост деградирует в mock).

## 2026-08-23 23:05 — Прогон приёмки
- **Задача**: прогнать статическую и дымовую приёмку до зелёной.
- **Сделано**:
  - Починены дефекты, найденные smoke: экран меню не показывался (начальное
    состояние MENU присваивалось до подписки UI — теперь роутер явно показывает
    меню при сборке); вьюмодель не рисовалась (камера не была добавлена в граф
    сцены); RoomEnvironment/PMREM ронял шейдеры на программном GL — убран,
    металл держится на metalness ≤ 0.4; обращения к SDK моста до initialize()
    печатали ошибку в консоль площадки — все обращения к мосту (язык, тип
    устройства, хранилище, покупки) перенесены за успешную инициализацию,
    game_ready из сторожевого таймера ставится в очередь и уходит сразу после
    ответа моста; в собранной версии отключена пост-линковая проверка шейдерных
    программ (renderer.debug.checkShaderErrors = import.meta.env.DEV):
    SwiftShader отвечает VALIDATE_STATUS false с пустым логом на корректно
    рисующие программы.
- **Проверено**: `node scripts/check-spec.mjs` — зелёный (A3–C13, F1–F2,
  G1–G7, G11–G12, H1: сделано 148 + осознанных отказов, O1: 3/3);
  `node scripts/smoke.mjs` — зелёный полностью:

```text
2026-08-23 приёмка: O1 ✅, A1–A5 ✅ (по build/smoke), S1–S7 ✅
(десктоп 1280×720 и телефон 390×844), B1–B6 ✅ машиной, B7–B12 ✅ глазами по
вёрстке токенов, C1 ✅ / C2–C4 ✅ кодом (площадка руками), C5 ✅ / C6–C11 ✅
кодом + capability gating, C12 ✅, D1–D6 ✅ по реализации (руки на телефоне —
следующий шаг), E1 частично: headless SwiftShader даёт ~7 FPS программным
рендером (реальный GPU — проверка руками), E2/E3 ✅ (1669 draw calls в кадре
не превышает бюджет, сборка 3.59 МБ < 4.5), E4 ✅ пулы без аллокаций в тике,
E5 ✅ автотюнер с зондированием; F1–F4 ✅; G1–G13 ✅ (G13 — обе раскладки через
?input=touch|desktop); H1 ✅: 148 сделано, осознанные отказы помечены [~] с
причинами в ACCEPTANCE.md.
```

- **Затронутые файлы**: src/core/Game.ts, src/core/EventBus.ts, src/main.ts,
  src/platform/PlaygamaService.ts, src/platform/StorageService.ts,
  src/rendering/PavilionScene.ts, src/ui/UiRoot.ts, src/ui/theme.css.
- **Известные проблемы / следующий шаг**: обязательный файл базы знаний
  ux/input_scheme_switching.md недоступен без ZAVOD_KNOWLEDGE_TOKEN (HTTP 404);
  FPS на реальном телефоне и rewarded-сценарий проверить руками на площадке;
  язык устройства кэшируется после initialize — схема управления до этого
  момента выбирается браузерными признаками (для локального запуска).
