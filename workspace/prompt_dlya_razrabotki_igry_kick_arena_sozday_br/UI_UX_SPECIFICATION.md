# UI/UX Specification: Судебный Пристав: Штурм Локдауна

## 1. Материал интерфейса
Интерфейс живёт в том же мире, что и сцена. Проверка: если закрыть игровое поле,
меню обязано выдавать именно эту игру, а не любую другую.

- **Тема от арт-директора**: Тактический кибер-диэгетический HUD: полупрозрачные угловые рамки визора шлема, высококонтрастные неоновые индикаторы боезапаса и таймера
- **Визуальный язык**: Четкая контрастная графика: враги подсвечены оранжево-красным контуром опасности, разрушаемые объекты и двери имеют желтую маркировку износа, союзные терминалы светятся бирюзовым.
- **Типографика**: Рубленый технический гротеск Rajdhani / Chakra Petch с высокой читаемостью чисел и статусов на малых экранах

## 2. Акценты: один цвет — один смысл
Разный цвет у каждой кнопки «чтобы отличались» — главная ошибка игрового
интерфейса. На одном экране одновременно видно не больше двух акцентов.

| Токен | Единственный смысл |
|---|---|
| `danger` | #FF2A2A (вражеские пули, таймер критического локдауна, индикатор урона) |
| `interactive` | #FFB800 (выбиваемые двери, взрывоопасные баллоны, ящики) |
| `player_tech` | #00E5FF (визор, голографический щит, точки рикошета) |
| `safe_zone` | #00E676 (взломанный терминал, эвакуационный шлюз) |

Все значения — токенами в `src/ui/theme.css`: цвета, гарнитуры, шкала отступов,
радиусы, длительности, порядок слоёв `--z-*`. Литерал цвета внутри экрана — баг.

## 3. Композиция экрана
Меню и экраны между сессиями лежат ПОВЕРХ живой игровой сцены — той же, что в
игре, только с медленной камерой. Непрозрачная заливка на весь экран запрещена:
подложка только под текстом и кнопками.

- **Сцена за меню**: 3D вид из шлюза десантного броневика перед штурмом: видны руки пристава в экзоперчатках, настраивающие карабин на фоне запертой бронедвери бункера

Экран — не колонка кнопок по центру. Три зоны, и каждый элемент лежит ровно в одной:

1. **Идентичность** — что это за экран: заголовок, режим, состояние игрока.
2. **Главное действие** — ровно одно на экран, самое крупное и единственное с
   основным акцентом.
3. **Второстепенный ряд** — всё остальное, одним рядом или сеткой, одним весом.

Зоны нажатия: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px, отступы через
`env(safe-area-inset-*)` плюс измеренная высота липкого баннера. Ни один экран не
скроллит страницу: длинный список скроллится во внутреннем контейнере.

## 4. Набор компонентов (закрытый)
Всё на экране — один из этих компонентов. Одноразовый `<div>` с инлайновыми
стилями — это то, из-за чего второй экран перестаёт совпадать с первым.

- VisorReticle
- HealthShieldBar
- LockdownCountdown
- BreachKickButton
- CompartmentTracker
- TerminalProgressBar

Каждый интерактивный компонент несёт пять состояний: покой, наведение, нажатие,
недоступность, `:focus-visible`; асинхронное действие — ещё и `loading`.

## 5. HUD
Не больше пяти постоянных элементов на телефоне. Пять якорей, посередине экрана
не висит ничего, кроме временной обратной связи.

| Якорь | Что там |
|---|---|
| `bottom_left` | Индикатор здоровья и щита экзокостюма |
| `bottom_right` | Боезапас, статус пинка и сенсорные кнопки действий |
| `center` | Динамическое перекрестье прицела с индикацией рикошета |
| `top_center` | Таймер локдауна и индикатор отсека |

Элементы:
- Тактический визор шлема с угловыми рамками захвата целей
- Индикатор целостности экзокостюма (HP) и прочности щита
- Счетчик патронов кинетического карабина и иконка готовности пинка
- Крупный таймер локдауна бункера (120 с) с алертом при < 30 с
- Мини-индикатор зачистки текущего отсека (1/4, 2/4, 3/4, 4/4)

Меняющиеся числа — `tabular-nums` в слоте фиксированной ширины, полосы —
`transform: scaleX()`, а не `width`. Текст поверх геймплея всегда с подложкой или
обводкой: белое число исчезает на светлой сцене.

## 6. Состояние, показанное миром, а не оверлеем
- Проекция интерфейса прямо на стекло тактического шлема пристава
- Физические светодиодные индикаторы на корпусе штурмового карабина
- Голографический таймер блокировки, проецируемый над гермодверями отсеков

## 7. Каталог экранов
### Экран: MainMenu
- **Что на экране**: Главное меню: выбор штурмовых контрактов бункера, арсенал, настройки и таблица лидеров
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: BunkerBriefing
- **Что на экране**: Тактическая голографическая схема бункера, состав охраны и таймер локдауна
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: GameplayHUD
- **Что на экране**: Диэгетический визор первого лица: HP/щит, таймер, боезапас, сенсорные кнопки
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: PauseOverlay
- **Что на экране**: Меню паузы с возможностью перезапуска или выхода в главное меню
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: VictoryScreen
- **Что на экране**: Итоги штурма: время зачистки, сбитые рэгдоллы, кредиты и кнопка удвоения награды
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: DefeatScreen
- **Что на экране**: Экран блокировки/гибели: таймер истек или броня разрушена, кнопка возрождения за Rewarded
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: UpgradeArmory
- **Что на экране**: Экран прокачки экзокостюма, выбора тяжелого оружия и модификаций пинка
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

**Переходы**: Загрузка -> Главное меню (Выбор контракта) -> Брифинг -> Геймплей (Отсеки 1-4) -> Финал отсека 4 -> Победа/Поражение -> Арсенал/Улучшения -> Главное меню

## 8. Состояния экрана
- Loading
- InGame_Breaching
- InGame_Combat
- InGame_TerminalHack
- Paused
- Victory_Extraction
- Defeat_Lockdown

## 9. Отклик и движение
- Удар ноги по двери: экранная встряска 15 px, звук срыва петель, пылевой взрыв
- Успешное парирование: стоп-кадр 40 мс, синяя радиальная вспышка, оглушение врага
- Попадание рикошетом: золотой маркер хит-маркера и характерный звон ДЗЫНЬ
- Критический урон игроку: красный виньетированный пульс и глитч тактического визора

Переход один на всю игру и укладывается в 300 мс; анимируются только `transform`
и `opacity`; `prefers-reduced-motion: reduce` убирает трансформации.

## 10. Вайрфрейм игрового экрана
```text
+-------------------------------------------------------------------------------+
| [HP: ======    ] [ЩИТ: ====  ]      ЛОКДАУН: 01:45         [ОТСЕК 2/4: СКЛАД] |
|                                                                               |
|       \                                                               /       |
|        \                      [+] Прицел                             /        |
|                                                                               |
|                                                                               |
|                                                                               |
|                                                                               |
|   ( ЛЕВАЯ ЗОНА:               [ ВЗЛОМ E ]              [ (ПИНОК) ]            |
|     ДВИЖЕНИЕ К УКРЫТИЮ )                               [ (ПАРИРОВАНИЕ) ]      |
|                                                        [ ПАТРОНЫ: 24/30 ]     |
+-------------------------------------------------------------------------------+
```

## 11. Чек-лист приёмки интерфейса
- [ ] Ни одного литерала цвета, шрифта, радиуса или длительности вне `theme.css`.
- [ ] Слои над канвасом не перехватывают игровой ввод: контейнеры `pointer-events: none`.
- [ ] Каждый экран помещается в измеренный вьюпорт, страница не скроллится.
- [ ] Одно главное действие на экран, не больше двух акцентов одновременно.
- [ ] Меняющиеся числа не дёргают строку HUD.
- [ ] У каждого экрана описаны загрузка, пустота и ошибка.
- [ ] Возможность, которой нет на площадке, не нарисована вовсе.
- [ ] Ни `alert`/`confirm`, ни эмодзи вместо иконок, ни `z-index` мимо токенов.
- [ ] Самая длинная переведённая строка помещается в кнопку.
- [ ] За меню видна живая сцена игры, а не заливка цветом.
- [ ] Ни одного эмодзи в подписи кнопки: иконка — инлайновый SVG с `currentColor`.
- [ ] У каждого экрана расписаны три зоны, а не «карточка по центру».
- [ ] С закрытым игровым полем меню всё ещё узнаётся как «Судебный Пристав: Штурм Локдауна».
