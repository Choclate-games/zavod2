# UI/UX Specification: Вышибала: Сброс за борт

## 1. Материал интерфейса
Интерфейс живёт в том же мире, что и сцена. Проверка: если закрыть игровое поле,
меню обязано выдавать именно эту игру, а не любую другую.

- **Тема от арт-директора**: Неоновый кибер-ретро HUD: полупрозрачные темные плашки со скошенными углами (chamfered corners), неоновая обводка #00F0FF, четкие моноширинные и жирные акцидентные шрифты, светящиеся радиальные индикаторы кулдауна.
- **Визуальный язык**: Высококонтрастный неоновый ретрофутуризм с чистой читаемостью боевой обстановки: враги выделяются яркими светящимися силуэтами, интерактивные предметы подсвечиваются мягким золотым контуром, а опасные края платформы обозначены неоновой желто-черной полосой.
- **Типографика**: Заголовки: 'Orbitron' / 'Russo One' (Bold, Uppercase), HUD и счетчики: 'Chakra Petch' / 'Share Tech Mono' (SemiBold), описания: 'Roboto' / 'Inter' (Regular).

## 2. Акценты: один цвет — один смысл
Разный цвет у каждой кнопки «чтобы отличались» — главная ошибка игрового
интерфейса. На одном экране одновременно видно не больше двух акцентов.

| Токен | Единственный смысл |
|---|---|
| `chips_gold` | #FFE600 — валюта, фишки казино и интерактивные объекты |
| `danger` | #FF2A55 — индикация вражеских атак и критического здоровья |
| `kick_ready` | #00F0FF — готовность спартанского пинка и кинетического выстрела |
| `shield_armor` | #7A5CFF — броня боссов и тяжелых киборгов |

Все значения — токенами в `src/ui/theme.css`: цвета, гарнитуры, шкала отступов,
радиусы, длительности, порядок слоёв `--z-*`. Литерал цвета внутри экрана — баг.

## 3. Композиция экрана
Меню и экраны между сессиями лежат ПОВЕРХ живой игровой сцены — той же, что в
игре, только с медленной камерой. Непрозрачная заливка на весь экран запрещена:
подложка только под текстом и кнопками.

- **Сцена за меню**: Вид от первого лица в холле казино перед открытым выходом на вертолетную площадку-арену: мерцающие неоновые автоматы, стол с выбором арсенала и панорама ночного футуристического города.

Экран — не колонка кнопок по центру. Три зоны, и каждый элемент лежит ровно в одной:

1. **Идентичность** — что это за экран: заголовок, режим, состояние игрока.
2. **Главное действие** — ровно одно на экран, самое крупное и единственное с
   основным акцентом.
3. **Второстепенный ряд** — всё остальное, одним рядом или сеткой, одним весом.

Зоны нажатия: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px, отступы через
`env(safe-area-inset-*)` плюс измеренная высота липкого баннера. Ни один экран не
скроллит страницу: длинный список скроллится во внутреннем контейнере.

## 4. Набор компонентов (закрытый)
Всё на экране — один из этих компонентов. Одноразовый `<div>` с инлайновыми
стилями — это то, из-за чего второй экран перестаёт совпадать с первым.

- NeonButton
- HealthBarSegmented
- CooldownRadialProgress
- ChipsCounterBadge
- ComboPopupBanner
- CardUpgradeSelector

Каждый интерактивный компонент несёт пять состояний: покой, наведение, нажатие,
недоступность, `:focus-visible`; асинхронное действие — ещё и `loading`.

## 5. HUD
Не больше пяти постоянных элементов на телефоне. Пять якорей, посередине экрана
не висит ничего, кроме временной обратной связи.

| Якорь | Что там |
|---|---|
| `bottom_center` | CrosshairVectorReticle |
| `bottom_right` | KickCooldownGauge, AmmoCounter, ActionButtons |
| `top_center` | WaveContractProgress, EnemiesRemaining |
| `top_left` | HealthBar, PlayerAvatar |
| `top_right` | ChipsBadge, ComboMultiplier |

Элементы:
- Кольцевой прицел-индикатор кинетического вектора по центру экрана
- Полоса здоровья игрока (слева вверху)
- Счетчик волны 'КОНТРАКТ: ВОЛНА 2/4' и оставшихся врагов (по центру вверху)
- Индикатор кулдауна спартанского пинка (справа внизу)
- Боезапас кинетического пистолета (4 энергоячейки справа внизу)
- Счетчик заработанных фишек казино и комбо-множитель 'x3 RAGDOLL STRIKE' (справа вверху)

Меняющиеся числа — `tabular-nums` в слоте фиксированной ширины, полосы —
`transform: scaleX()`, а не `width`. Текст поверх геймплея всегда с подложкой или
обводкой: белое число исчезает на светлой сцене.

## 6. Состояние, показанное миром, а не оверлеем
- Электронный дисплей на тыльной стороне перчатки игрока с индикатором заряда пистолета
- Голографические неоновые табло над ареной, показывающие счет и статус волны
- Искрящие повреждения на самом оружии при снижении его прочности

## 7. Каталог экранов
### Экран: Screen HUD
- **Что на экране**: Отображение боевого состояния персонажа, кулдаунов и прогресса волны
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: Screen UpgradeModal
- **Что на экране**: Выбор 1 из 3 тактических физических перков между волнами
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: Screen VictorySummary
- **Что на экране**: Итоги контракта, статистика ринг-аутов, удвоение фишек за Rewarded Video
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: Screen DefeatModal
- **Что на экране**: Экран падения за борт с предложением воскрешения за Rewarded Video
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: Screen LobbyArmory
- **Что на экране**: Выбор разблокированного оружия, скинов ботинок и прокачка характеристик
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

**Переходы**: LobbyArmory -> ContractWave1 -> UpgradeSelection -> ContractWave2 -> UpgradeSelection -> ContractWave3 -> UpgradeSelection -> BossWave4 -> (VictorySummary / DefeatSummary) -> LobbyArmory

## 8. Состояния экрана
- Loading
- Lobby
- ActiveCombat
- UpgradeChoice
- Paused
- VictorySummary
- DefeatSummary
- AdReviveWaiting

## 9. Отклик и движение
- Успешный спартанский пинок: микро-задержка кадра (hit-stop 60 мс) + импульсная вспышка + басовый панч
- Ринг-аут врага: исчезновение в глубине + звон фишек + всплывающая плашка 'OVERBOARD!'
- Цепное домино столкновение: легкий slow-mo 100 мс + звук треска + множитель 'STRIKE x2/x3'
- Получение урона игроком: виньетка красного свечения по краям экрана + встряска камеры

Переход один на всю игру и укладывается в 300 мс; анимируются только `transform`
и `opacity`; `prefers-reduced-motion: reduce` убирает трансформации.

## 10. Вайрфрейм игрового экрана
```text
+-----------------------------------------------------------------------------------+
| [HP: ======    ]       [ КОНТРАКТ: ВОЛНА 2/4 | ОСТАЛОСЬ: 6 ]      [ ФИШКИ: 1,450 ]|
|                                                                    [ COMBO x3 !! ]|
|                                                                                   |
|                                         (+)                                       |
|                                  [ Векторный прицел ]                             |
|                                                                                   |
|                                                                                   |
|                                                                  ( ВЫСТРЕЛ [4] )  |
|                                                                                   |
|     ( О )                                                      (( ПИНОК [1.2s] )) |
|   [Джойстик]                                                                      |
|                                                                   ( УДАР / E )    |
+-----------------------------------------------------------------------------------+
```

## 11. Чек-лист приёмки интерфейса
- [ ] Ни одного литерала цвета, шрифта, радиуса или длительности вне `theme.css`.
- [ ] Слои над канвасом не перехватывают игровой ввод: контейнеры `pointer-events: none`.
- [ ] Каждый экран помещается в измеренный вьюпорт, страница не скроллится.
- [ ] Одно главное действие на экран, не больше двух акцентов одновременно.
- [ ] Меняющиеся числа не дёргают строку HUD.
- [ ] У каждого экрана описаны загрузка, пустота и ошибка.
- [ ] Возможность, которой нет на площадке, не нарисована вовсе.
- [ ] Ни `alert`/`confirm`, ни эмодзи вместо иконок, ни `z-index` мимо токенов.
- [ ] Самая длинная переведённая строка помещается в кнопку.
- [ ] За меню видна живая сцена игры, а не заливка цветом.
- [ ] Ни одного эмодзи в подписи кнопки: иконка — инлайновый SVG с `currentColor`.
- [ ] У каждого экрана расписаны три зоны, а не «карточка по центру».
- [ ] С закрытым игровым полем меню всё ещё узнаётся как «Вышибала: Сброс за борт».
