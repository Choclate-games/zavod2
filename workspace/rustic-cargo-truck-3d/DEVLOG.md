# Журнал разработки — Лесной Рейс: Доставка на Лесопилку 3D

Этот файл ведёт ИИ-агент. Каждая сессия работы = одна запись:
что просили, что сделано, какие файлы затронуты, что проверено, что осталось.

---

## 2026-08-18 23:55 — Инициализация проекта
- **Задача**: создан каталог проекта в workspace, подготовлена спецификация.
- **Сделано**: сгенерирован пакет документации фабрики.
- **Следующий шаг**: реализация игрового кода по AI_DEVELOPER_PROMPT.md.

## 2026-08-19 00:40 — Полный игровой прототип доставки
- **Задача**: создать рабочую HTML5/WebGL игру по AI_DEVELOPER_PROMPT.md с Vite, Three.js, Rapier3D, управлением, звуком и Playgama Bridge.
- **Сделано**: добавлены Vite/TypeScript-конфигурация, fixed-step игровой цикл, typed EventBus, процедурная ухабистая трасса с деревней и лесопилкой, Three.js камера и тёплое освещение, физическое шасси и отдельные тела брёвен/ящиков, расчёт потерь груза и награды, HUD/меню/пауза/результат, Web Audio мотор и удары, нормализованное локальное/Bridge-сохранение, watchdog и одноразовый `game_ready`.
- **Затронутые файлы**: `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `src/`, `README.md`, `CHANGELOG.md`.
- **Проверено**: `npm install` завершился успешно; `npm run build` завершился успешно; `npm run dev -- --host=127.0.0.1` поднял Vite, HTML доступен через локальный сервер.
- **Известные проблемы / следующий шаг**: production-бандл крупный из-за Rapier WASM и Three.js; перед публикацией стоит вынести зависимости в чанки и прогнать ручной QA в мобильном iframe Playgama.

## 2026-08-19 01:05 — Финальная интеграция SDK и проверка запуска
- **Задача**: завершить рабочую структуру и проверить запуск после подключения реального npm-модуля Playgama Bridge.
- **Сделано**: Bridge теперь использует установленный `@playgama/bridge` с безопасным window/fallback-выбором; добавлены lifecycle-события паузы и аудио, сохранение апгрейда двигателя и освобождение геометрии динамических объектов при новом рейсе.
- **Затронутые файлы**: `src/platform/PlaygamaService.ts`, `src/core/Game.ts`, `src/audio/AudioManager.ts`, `src/rendering/SceneManager.ts`, `src/vehicle/CargoManager.ts`, `src/ui/UIManager.ts`, `CHANGELOG.md`.
- **Проверено**: `npm run build` завершился успешно; `npm run dev -- --host=127.0.0.1` поднял сервер; `http://127.0.0.1:5173/` вернул HTML игры.
- **Известные проблемы / следующий шаг**: автоматический браузерный smoke-тест Three.js/мультитача не запускался в окружении без Playwright; нужен ручной прогон в фабричном браузере и платформенном iframe.

## 2026-08-20 09:10 — Исправление висящих деревьев, инверсии управления, добавление ухабов и продвинутой системы грязи
- **Задача**: исправить висящие в воздухе деревья и постройки; исправить инверсию/поведение управления; добавить кочки, ухабы, ямы и препятствия для усложнения маршрута; реализовать продвинутую физическую систему грязи (машина застревает, буксует, постепенно загрязняется, дорога деформируется под колесами).
- **Сделано**:
  1. **Исправление висящих деревьев и расширение уровня**: ширина рельефа `TERRAIN.halfWidth` увеличена с 9 м до 32 м (64 м долина с холмами и лесом). Деревья, березовые рощи, кустарники, валуны, постройки деревни и лесопилки теперь строго заземлены по точной аналитической высоте `heightAt(x, z)` с заглублением комля в грунт.
  2. **Исправление управления и камеры**: устранено визуальное инвертированное качание камеры в `SceneManager` за счет сглаженного демпфированного следования за грузовиком; в `TouchControls` добавлен полноценный 2-осевой плавающий джойстик (вверх — газ, вниз — реверс/тормоз, влево/вправо — плавный поворот) синхронно с педалями.
  3. **Усложнение маршрута (кочки, ухабы, рытвины, ямы и препятствия)**: профиль дороги обогащен ритмичными моголами (32–88 м), поперечным креном трассы, глубокой болотной низиной (88–152 м), каменистым ущельем с резкими ямами (155–222 м) и крутым грязевым подъемом (224–276 м). Добавлены физические валуны-препятствия с коллайдерами на опасных поворотах.
  4. **Продвинутая система грязи**:
     - *Физика и застревание*: динамический расчет коэффициента сцепления шин `wheelFrictionSlip` (падение сцепления до 72% в топях), бокового сноса и вязкостного гидродинамического сопротивления грязи `MUD.baseDragForce` и `speedDragCoeff`. При остановке в глубокой грязи колеса буксуют, требуется раскачка и контроль тяги.
     - *Деформация дороги*: колеса под нагрузкой в реальном времени выдавливают колеи в меше дороги через `deformRoad()`, формируя валики выдавленной грязи по краям и темнея в мокрую грязь.
     - *Загрязнение грузовика*: кузов, колеса и кабина динамически накапливают грязь (`mudLevel`), меняя оттенок на комья засохшей/мокрой грязи.
     - *Частицы брызг*: вращающиеся с пробуксовкой в грязи колеса выбрасывают частицы комьев и брызг назад и вверх.
     - *UI*: в HUD добавлен индикатор «ВЯЗКАЯ ГРЯЗЬ» / «ГЛУБОКАЯ ТОПЬ · ТЯГА ↓».
- **Затронутые файлы**: `src/world/terrain.ts`, `src/world/RoadGenerator.ts`, `src/vehicle/TruckController.ts`, `src/vehicle/truckSpec.ts`, `src/rendering/SceneManager.ts`, `src/physics/PhysicsWorld.ts`, `src/input/TouchControls.ts`, `src/core/Game.ts`, `src/ui/UIManager.ts`, `src/styles.css`, `tools/physicsSanity.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) завершился без ошибок; `npm run check:physics` — все 14/14 физических проверок пройдены успешно (включая тест сопротивления и пробуксовки в грязи).
- **Известные проблемы / следующий шаг**: провести тестирование производительности деформации сетки на сверхбюджетных смартфонах.

## 2026-08-20 09:20 — Генерация 15 уровней, устранение сброса грязи, стабилизация камеры и переключатель инверсии
- **Задача**: полностью устранить ощущение инверсии управления; сделать так, чтобы налипшая грязь оставалась на грузовике на протяжении всей поездки, а не сбрасывалась при выезде на сухую дорогу; сгенерировать 15 уникальных уровней с прогрессией, выбором уровня, звёздами и магазином улучшений.
- **Сделано**:
  1. **Генерация 15 уровней (`src/world/levels.ts`, `src/world/terrain.ts`, `src/world/RoadGenerator.ts`)**:
     - Создана система из 15 уровней с уникальной топографией, нарастающей длиной (от 220 до 460 м), типами препятствий (кочки, моголы, болота, каньоны, горные перевалы), плотностью грязи и валунов.
     - Добавлено меню выбора уровня (`showLevelSelect`) со статусом (открыт/заблокирован), звёздами (1–3 ⭐) и тегами сложности.
     - Реализован плавный переход «Следующий уровень» на победном экране.
  2. **Постоянная грязь на машине и колесах (`src/vehicle/TruckController.ts`)**:
     - Исправлен сброс грязи: слой налипшей грязи `mudLevel` теперь накапливается и сохраняется на кузове, кабине и колёсах в течение всего рейса, оставаясь засохшим и реалистично затемнённым даже при езде по сухому покрытию. Сброс происходит только при старте нового заезда.
     - Добавлено улучшение «Грязевые шины» (1–3 ур.), снижающее падение сцепления в топях и облегчающее преодоление болот.
  3. **Стабилизация камеры и переключатель инверсии (`src/rendering/SceneManager.ts`, `src/vehicle/TruckController.ts`, `src/ui/UIManager.ts`)**:
     - Точка прицела камеры зафиксирована строго по центру кабины грузовика, полностью устранив иллюзию «обратного сноса» при поворотах.
     - В меню паузы добавлен удобный переключатель «Инверсия руля: ВКЛ/ВЫКЛ», позволяющий игроку моментально настроить комфортное направление руления.
- **Затронутые файлы**: `src/world/levels.ts` (новый), `src/world/terrain.ts`, `src/world/RoadGenerator.ts`, `src/vehicle/TruckController.ts`, `src/rendering/SceneManager.ts`, `src/physics/PhysicsWorld.ts`, `src/platform/StorageService.ts`, `src/core/types.ts`, `src/core/Game.ts`, `src/ui/UIManager.ts`, `src/styles.css`, `tools/physicsSanity.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — собрано за 5.59с без ошибок; `npm run check:physics` — все 14 тестов пройдены успешно.
- **Известные проблемы / следующий шаг**: провести тестирование баланса цен улучшений и времени прохождения на поздних уровнях (11–15).

## 2026-08-20 09:30 — Масштабирование до 50 уровней, водные преграды, физические деревья и выравнивание руля по экрану
- **Задача**: сгенерировать 50 полноценных уровней с прогрессией и вкладками глав; добавить водные преграды (броды, аквапланирование, смывание грязи, брызги воды); сделать деревья твёрдыми физическими объектами с коллайдерами; расставить камни прямо на полосе движения; исправить инверсию руления относительно экрана игрока.
- **Сделано**:
  1. **Исправление инверсии руления по экрану (`src/vehicle/TruckController.ts`, `tools/physicsSanity.ts`)**:
     - Обнаружена первопричина: при взгляде камеры Three.js по оси +Z мировая координата +X проецируется на *левую* сторону экрана, а -X — на *правую*.
     - Направление руления выровнено с экраном игрока: нажатие `D` / вправо поворачивает машину в сторону правого края экрана (мировой -X), нажатие `A` / влево — в сторону левого края экрана (мировой +X). Колёса на 3D-модели поворачиваются строго в сторону маневра.
  2. **Масштабирование до 50 уникальных уровней (`src/world/levels.ts`, `src/ui/UIManager.ts`, `src/styles.css`, `src/core/Game.ts`)**:
     - Сгенерировано 50 уровней, разбитых на 5 глав (1–10 «Проселки», 11–20 «Топи», 21–30 «Перевалы», 31–40 «Тайга», 41–50 «Экстрим»). Длина трасс масштабируется от 220 до 520 м.
     - В окне выбора уровней добавлены вкладки быстрой навигации по главам (1–10, 11–20, 21–30, 31–40, 41–50), статус открытости и звёзды.
  3. **Водные преграды и гидродинамика (`src/world/terrain.ts`, `src/world/RoadGenerator.ts`, `src/vehicle/TruckController.ts`, `src/rendering/SceneManager.ts`, `src/ui/UIManager.ts`)**:
     - Добавлены водные зоны (`waterZones`) с заглублением рельефа и полупрозрачными водными гладями.
     - Физика воды: гидродинамическое сопротивление ($F_{\text{drag}} \propto v^2$), эффект аквапланирования (снижение сцепления шин) и эффект смывания налипшей грязи с грузовика.
     - Добавлен пул частиц водных брызг (голубовато-белые всплески из-под колёс) и плашка в HUD «ВОДНАЯ ПРЕГРАДА · АКВАПЛАНИРОВАНИЕ».
  4. **Твёрдые деревья и камни на дороге (`src/world/RoadGenerator.ts`, `src/physics/PhysicsWorld.ts`)**:
     - В `PhysicsWorld` добавлен метод `createTreeCollider`: для всех придорожных сосен и берёз генерируются твердые цилиндрические коллайдеры стволов (врезание в дерево останавливает машину и выбивает брёвна из кузова).
     - Валуны теперь генерируются не только по обочинам, но и прямо на полосе движения в скалистых и экстремальных уровнях.
- **Затронутые файлы**: `src/world/levels.ts`, `src/world/terrain.ts`, `src/world/RoadGenerator.ts`, `src/vehicle/TruckController.ts`, `src/physics/PhysicsWorld.ts`, `src/rendering/SceneManager.ts`, `src/core/Game.ts`, `src/ui/UIManager.ts`, `src/styles.css`, `tools/physicsSanity.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция завершена за 5.77с; `npm run check:physics` — все 14 тестов пройдены успешно (включая проверку экранного руления).
- **Известные проблемы / следующий шаг**: протестировать поведение на мобильных устройствах при прохождении уровней 40–50.

## 2026-08-20 09:50 — Автобаза и Гараж (4 грузовика 4х2, 4х4, 6х6), тюнинг узлов, покраска и 6 типов грузов
- **Задача**: разнообразить поставки грузов (не только брёвна); создать полноценный Гараж, в котором можно прокачивать грузовик и покупать новые, более мощные грузовики.
- **Сделано**:
  1. **Разнообразие грузов (7 типов объектов, 6 тематических пакетов)**:
     - 7 видов физических грузов с индивидуальными шейпами, массами, трением и детализированными 3D-моделями:
       - 🌲 `log` — длинные хвойные брёвна с кольцами распила и корой ($m=30\text{ кг}$, $r=0.28\text{ м}$, $L=1.9\text{ м}$).
       - 📦 `crate` — дощатые прочные ящики со стальными уголками и крестообразными планками ($m=25\text{ кг}$).
       - 🛢️ `barrel` — металлические топливные бочки с рёбрами жёсткости и пробками ($m=38\text{ кг}$, $r=0.28\text{ м}$, $h=0.76\text{ м}$).
       - 🧱 `concrete` — тяжёлые армированные бетонные фундаментные блоки ($m=55\text{ кг}$, повышенное трение $\mu=0.9$).
       - 🌾 `hay` — плотные тюки золотистого прессованного сена с бечёвками ($m=18\text{ кг}$, $\mu=0.75$).
       - 🔩 `pipe` — стальные промышленные трубы с полыми торцами и ободьями ($m=42\text{ кг}$, $r=0.26\text{ м}$).
       - ⚠️ `fragile` — ценные защитные контейнеры со знаком радиации/хрупкости и сигнальными замками ($m=22\text{ кг}$).
     - 6 готовых тематических пакетов доставки (`logs`, `barrels`, `construction`, `farm`, `fragile`, `mixed`), привязанных к 50 уровням и отображаемых в меню выбора, HUD и результатах.
     - Индивидуальное отслеживание выпадания с точным оповещением в UI (например, «Бочка ГСМ потеряна! Осталось: 7»).
  2. **Автопарк из 4 уникальных грузовиков (`TRUCKS`)**:
     - 🚚 **ЗиЛ-130 «Ветеран»** (4х2 задний привод, 150 л.с., базовый).
     - 🚙 **ГАЗ-66 «Шишига»** (4х4 полный привод, 120 л.с., короткая база, шноркель и лебёдка, высокая манёвренность).
     - 🚜 **КрАЗ-255 «Богатырь»** (6х6 тяжеловес, 240 л.с., 6 больших колёс, 4 ведущих задних колеса, усиленный каркас).
     - 🚛 **Урал-4320 «Тайфун»** (6х6 экспедиционный, 240 л.с., максимальная проходимость, защита и мощность).
     - Честная многоколёсная физика Rapier Vehicle Controller (4 и 6 колёс с отдельным приводом и рулением).
  3. **Мастерская, тюнинг и кастомизация кузова (`TruckController`, `UIManager`, `SceneManager`)**:
     - Полноценный 3D шоурум Гаража с орбитальной камерой осмотра.
     - 4 ветки модернизации для каждого грузовика:
       - ⚙️ **Двигатель** (0..5 ур.): прирост крутящего момента и скорости на подъёмах.
       - 🛞 **Грязевые шины** (0..4 ур.): усиленный протектор и грунтозацепы в болотах и бродах.
       - 🔩 **Подвеска и лифт** (0..3 ур.): увеличение клиренса и жёсткости амортизаторов.
       - 📦 **Высокие борта кузова** (0..3 ур.): физическое и визуальное наращивание стенок кузова (увеличение высоты коллайдеров бортов), предотвращающее выпадание брёвен и ящиков на ухабах.
     - 🎨 **Покрасочный цех**: палитра из 6 аутентичных цветов кузова (Лесной Оранж, Таёжный Небесный, Армейский Хаки, Экспедиционный Рубин, Золотой Песок, Спецназ Графит) с обновлением 3D-модели в реальном времени.
  4. **Сохранение и интеграция**:
     - В `SaveData` добавлена поддержка `selectedTruck`, `unlockedTrucks`, `truckUpgrades` с полной обратной совместимостью и нормализацией.
- **Затронутые файлы**: `src/core/types.ts`, `src/vehicle/truckSpec.ts`, `src/vehicle/TruckController.ts`, `src/vehicle/CargoManager.ts`, `src/rendering/SceneManager.ts`, `src/physics/PhysicsWorld.ts`, `src/world/levels.ts`, `src/platform/StorageService.ts`, `src/ui/UIManager.ts`, `src/styles.css`, `src/core/Game.ts`, `tools/physicsSanity.ts`, `DEVLOG.md`, `CHANGELOG.md`, `README.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — собрано без ошибок; `npm run check:physics` — все тесты (включая проверку 6х6 тягача и валидацию всех 6 пакетов грузов) пройдены успешно.
- **Известные проблемы / следующий шаг**: провести плейтест баланса цен на КрАЗ и Урал в рамках полного прохождения 50 уровней.
## 2026-08-20 11:45 — Извилистые трассы (1–50 уровни) и развилки с выбором альтернативных путей (с 10 уровня)
- **Задача**: сделать уровни не прямыми, а извилистыми с 1-го уровня, и добавить разветвления / альтернативные пути начиная с 10-го уровня.
- **Сделано**:
  1. **Извилистые трассы и кривизна рельефа (1–50 уровни)**:
     - Все 50 уровней получили горизонтальные изгибы и S-образные виражи (`curveAmp` от 3.2 до 7.5 м, `curveFreq`), при этом старт в деревне (z <= 16) и финиш у пилорамы (z >= len - 16) плавно центрируются.
     - Ширина долины расширена до 76 м (`TERRAIN.halfWidth = 38`, `segmentsX = 64`), чтобы вместить даже самые широкие повороты и разветвления без краевых артефактов.
     - Топография `heightAt(x, z)`, `roadHeightAt(z, x)`, `getMudIntensity(x, z)`, `getWaterIntensity(x, z)` и окраска вершин `buildTerrainGeometry()` теперь динамически рассчитываются от истинного центра дороги `getRoadProximity(x, z)`.
  2. **Система развилок и альтернативных маршрутов (с 10-го уровня)**:
     - Начиная с 10-го уровня (уровни 10, 12, 14, 15, 16, 18, 20, 22, 24, 25, 26, 28, 30, 32, 34, 35, 36, 38, 40, 42, 44, 46, 48 и кульминационный 50-й с двумя развилками) трассы разделяются на две физически проходимые ветви (Левая и Правая) с разделительным лесным островком посредине:
       - *Примеры развилок*: «ТОПЬ · ОБЪЕЗД», «ВАЛУНЫ · ХОЛМ», «ПОДЪЁМ · СЕРПАНТИН», «БРОД · ГАТЬ», «КАРЬЕР · ОБЪЕЗД», «СТУПЕНИ · БОЛОТО» и др.
       - *Индивидуальные условия ветвей*: разные перепады высот (`leftElevation`, `rightElevation`), глубина грязи (`leftMudIntensity`, `rightMudIntensity`), речные броды (`leftWaterDepth`, `rightWaterDepth`), моголы и валуны на полосе.
  3. **Дорожные указатели и антураж развилок**:
     - Перед развилкой устанавливается двойной щит-указатель направлений (например, `⬅️ ТОПЬ · ОБЪЕЗД ➡️`).
     - На острие разделительного островка установлен направляющий треугольный шевронный маяк (`◀️ ▶️`), а между ветками растут сосны и берёзы на гребне островка.
     - На выходе из развилки установлен знак слияния дорог.
  4. **Интерфейс и HUD**:
     - В HUD добавлен динамический бейдж `🔀 РАЗВИЛКА: ⬅️ [ЛЕВЫЙ] · [ПРАВЫЙ] ➡️` при въезде на участок развилки.
     - В меню выбора уровней на карточках маршрутов отображается плашка `🔀 [Левый] / [Правый]`.
- **Затронутые файлы**: `src/world/levels.ts`, `src/world/terrain.ts`, `src/world/RoadGenerator.ts`, `src/core/Game.ts`, `src/ui/UIManager.ts`, `src/styles.css`, `tools/physicsSanity.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция и бандлинг без ошибок; `npm run check:physics` — все 15 физических тестов пройдены успешно (включая тест развилки и устойчивости на ветви 10-го уровня).
## 2026-08-20 11:55 — Широкое разделение развилок (28–35м), контрастные пути (лучше/хуже) и устранение левитации воды
- **Задача**: развести развилки шире друг от друга, сделать пути по-настоящему разными (один лучше/безопаснее, другой хуже/экстремальнее), исправить левитацию водной глади на уклонах и поворотах.
- **Сделано**:
  1. **Широкое пространственное разделение развилок (28–35 метров между путями)**:
     - Смещение левой ветви увеличено до -14.5..-17.5 м, правой — до +14.5..+17.5 м (суммарное расстояние между дорогами теперь 28–35 метров).
     - Ширина долины расширена до 104 метров (`TERRAIN.halfWidth = 52`, `segmentsX = 80`), что даёт полноценный обзор и манёвр.
     - Между двумя дорогами сформирован массивный центральный физический хребет/остров высотой до 4.5–5.4 м, поросший сосновым лесом, исключающий срезку напрямую через целину.
  2. **Чёткий контраст путей (Один путь ЛУЧШЕ, другой — ХУЖЕ / РИСКОВАННЕЙ)**:
     - Все 25 развилок на уровнях 10..50 получили выраженную полярность с информативными тегами:
       - *«💥 ТОПЬ vs ✨ ОБЪЕЗД»*: срезка через глубокую вязкую жижу (грязь 0.95, просадка -1.2м) против сухого плотного песчаного холма (грязь 0.0, подъем +1.8м).
       - *«✨ ПЕСЧАНИК vs 💥 ВАЛУНЫ»*: ровная скоростная полоса против частокола валунов на крутых моголах.
       - *«✨ ГАТЬ vs 💥 ГЛУБОКИЙ БРОД»*: сухой бревенчатый настил против глубокой речной стремнины (вода 0.70, аквапланирование).
       - *«💥 ШТУРМ ПОДЪЁМА vs ✨ СЕРПАНТИН»*: крутой грязевой уклон (для 6х6 с мощным мотором) против пологого гравийного траверса.
  3. **Конформная геометрия воды (полное устранение левитации)**:
     - Переработан генератор водных зон `createConformalWaterMesh`: водная поверхность теперь строится не плоским щитом, а многосекционной лентой, высота каждой вершины которой строго рассчитывается от русла дороги `roadHeightAt(worldZ, worldX) + 0.05`.
     - Вода идеально облегает кривизну русла, речные ямы и склоны, нигде не повисая в воздухе.
- **Затронутые файлы**: `src/world/levels.ts`, `src/world/terrain.ts`, `src/world/RoadGenerator.ts`, `src/core/Game.ts`, `tools/physicsSanity.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция и бандлинг без ошибок; `npm run check:physics` — все 15 тестов пройдены успешно (включая проверку разделения 29.0 м и физики обеих ветвей 10-го уровня).
- **Известные проблемы / следующий шаг**: провести плейтест на мобильных устройствах для проверки читаемости дорожных знаков на высокой скорости.

## 2026-08-20 13:45 — Детализированные 3D-модели грузовиков, визуальный тюнинг и продвинутая система частиц (VFX)
- **Задача**: улучшить модели грузовиков (аутентичный советский low-poly стиль для всех 4 машин, детализация узлов, визуальная прогрессия улучшений) и реализовать богатую высокопроизводительную систему частиц (выхлопной дизельный дым под нагрузкой, пыль из-под колёс, баллистическая грязь, всплески воды, искры от ударов, осенний листопад и салют на финише).
- **Сделано**:
  1. **Детализация 3D-моделей всех 4 грузовиков (`TruckController.ts`, `SceneManager.ts`)**:
     - 🚚 **ЗиЛ-130 «Ветеран»**: культовая белая решётка радиатора с изогнутыми прорезями и горизонтальными ребрами, двухцветная кабина со скруглённой крышей и верхними габаритными огнями автопоезда, передние крылья с круглыми фарами и хромированными ободками, боковые зеркала на трубчатых кронштейнах, хромированные дверные ручки, топливный бак и инструментальный ящик на лонжеронах, реалистичный интерьер с рулевым колесом, брызговики с шевронной разметкой.
     - 🚙 **ГАЗ-66 «Шишига»**: военная бескапотная компоновка, двойное панорамное лобовое стекло со стеклоочистителями, защитные стальные решетки на фарах, передний бампер с механической лебедкой и буксирным крюком, шноркель с циклоническим фильтром, смотровой люк на крыше, канистры ГСМ и вертикально установленная запаска за кабиной.
     - 🚜 **КрАЗ-255 «Богатырь»**: массивный гранёный капот с боковыми жалюзи охлаждения двигателя, массивная решетка радиатора с мощным push-баром на швеллерном бампере, фары в цилиндрических ковшах со стальными сетками, противосолнечный козырек, высокие сдвоенные цилиндрические бензобаки с крепежными лентами и культовая высокая выхлопная труба с защитным клапаном.
     - 🚛 **Урал-4320 «Тайфун»**: хищный наклонный капот, силовой трубчатый кенгурятник с лебедкой, экспедиционный багажник на крыше с 5 галогеновыми прожекторами («люстра») и пневмосигналами, защитный трубчатый каркас кузова с диагональными раскосами, выносной шноркель и боковая экранированная выхлопная система.
  2. **Визуальная прогрессия тюнинга узлов в Гараже**:
     - 🛞 **Шины (0..4 ур.)**: 3D-моделирование агрессивного протектора: переход от стандартных дорожек к глубоким грязевым блокам, бедлокам, боковым зацепам и исполинским V-образным тракторным шевронам для КрАЗа.
     - ⚙️ **Двигатель (0..5 ур.)**: появление хромированных турбо-воздухозаборников, мощных фильтров и кастомных патрубков.
     - 🔩 **Подвеска (0..3 ур.)**: видимые яркие спортивные пружины амортизаторов и многолистовые рессорные пакеты над мостами.
     - 📦 **Борта (0..3 ур.)**: трансформация кузова: от базовых деревянных досок до нарощенных бортов со стойками, стального сетчатого каркаса и усиленного бронированного кузова.
  3. **Продвинутая система частиц (`ParticleSystem.ts`, `SceneManager.ts`, `Game.ts`, `CargoManager.ts`)**:
     - 💨 **Дизельный выхлоп**: генерация дыма точно из среза выхлопной трубы каждого грузовика (вертикальный стек для КрАЗа/Урала, боковой/подкузовной для ЗиЛа/ГАЗа) — легкие полупрозрачные клубы на холостых и густые клубы темного дыма под тягой и на подъемах.
     - 🌪️ **Пылевые шлейфы**: клубы сухой дорожной пыли из-под колес при скоростной езде по сухим участкам.
     - 🟤 **Баллистические комья грязи**: куски и брызги вязкой грязи с реалистичной гравитационной дугой полета из-под буксующих колес.
     - 🌊 **Водные всплески**: фонтаны и пена при форсировании речных бродов.
     - ✨ **Снопы искр**: яркие золотистые искры при ударах днищем о валуны и пробое подвески на кочках.
     - 🍂 **Таежный листопад**: кружащиеся золотые, оранжевые и красные листья, взмывающие в воздух в турбулентном следе грузовика.
     - 🎉 **Финишный салют**: праздничный залп разноцветного конфетти и золотистых опилок при пересечении финиша лесопилки.
     - *Производительность*: 100% Zero-Allocation в цикле рендера (пулы объектов, переиспользуемые геометрии и векторы, мягкое затухание прозрачности и масштаба).
- **Затронутые файлы**: `src/rendering/ParticleSystem.ts` (новый), `src/rendering/SceneManager.ts`, `src/vehicle/TruckController.ts`, `src/core/Game.ts`, `src/vehicle/CargoManager.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция и бандлинг без единой ошибки; `npm run check:physics` — все 15 физических тестов пройдены успешно.
- **Известные проблемы / следующий шаг**: провести тестирование производительности системы частиц на мобильных устройствах в режиме 60 FPS.

## 2026-08-20 13:58 — SnowRunner-деформация рельефа, персистентные 3D-следы от шин и многослойная гидродинамика воды
- **Задача**: улучшить частицы и эффекты воды (устранить ощущение странности, сделать реалистичные брызги, пену, рябь); реализовать качественные следы от колёс на грунте и грязи; внедрить продвинутую деформацию дороги по типу SnowRunner/MudRunner (продавливание глубоких колей, вытеснение грунта в боковые валики, закапывание при пробуксовке, потемнение почвы до мокрого торфа).
- **Сделано**:
  1. **SnowRunner-деформация рельефа и глубокая колея (`RoadGenerator.ts`)**:
     - Переработан алгоритм `deformRoad()`: теперь он использует 2D-гауссовский радиус продавливания (до 32 см в глубокой грязи и 12 см на грунте).
     - Реализован закон сохранения объёма грунта: вытесняемая из-под шины земля приподнимает боковые вершины на $+38\%$ от глубины вдавливания, образуя реалистичные рельефные валики выдавленной грязи по краям обеих колей.
     - Добавлена динамическая прогрессивная пробуксовка (`isSpinning`): если машина застряла и буксует на месте с газом, колёса закапываются глубже, выбрасывая комья грязи.
     - Дно колеи окрашивается в тёмно-торфяную жижу (`#1a0f07`), а боковые валики — в комковатую рельефную грязь с real-time пересчётом нормалей сетки (`computeVertexNormals()`).
  2. **Персистентные 3D-следы от шин (`TireTracksManager.ts`)**:
     - Создан модуль `TireTracksManager`, генерирующий непрерывную 3D-ленту отпечатков протектора для каждого колеса (до 160 сегментов на колесо) с нулевыми аллокациями памяти (статические кольцевые буферы).
     - Динамическая адаптация текстуры следа: глубокие тёмные борозды в болотах, пылевой след на сухой грунтовке и мокрые блестящие следы, тянущиеся за машиной после выезда из речного брода на сухую дорогу.
  3. **Многослойная гидродинамика и реалистичные частицы воды (`ParticleSystem.ts`, `SceneManager.ts`)**:
     - Заменены простые сферы на 5 согласованных слоёв водного VFX:
       - 🌊 **Веерные водяные струи (`waterSpray`)**: диагональные направленные вееры брызг, вылетающие из-под рассекающих воду колёс на скорости.
       - 🫧 **Белая пена и пузыри (`waterFoam`)**: мягкие хлопьевидные гребни пены, всплывающие и дрейфующие на поверхности воды.
       - 🔘 **Расходящиеся круги и рябь (`waterRipple`)**: динамические концентрические кольца волн на водной глади.
       - 🌫️ **Водяная дымка (`waterMist`)**: атмосферный шлейф влажного тумана за несущимся через брод грузовиком.
       - 💧 **Стекающие капли**: капли воды, капающие с мокрых колёс и шасси при выезде на сушу.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция завершена за 5.89с без ошибок; `npm run check:physics` — все 15 физических тестов пройдены успешно.
- **Известные проблемы / следующий шаг**: провести тестирование читаемости дорожных следов в тёмных участках леса.

## 2026-08-20 14:05 — Устранение угловатости частиц воды и высокополигональная деформация дорожного полотна
- **Задача**: устранить квадратную форму брызг воды и сделать их органичными, текучими и плавными; исправить деформацию рельефа (ранее на некоторых участках и при слабой грязи деформация не активировалась или не захватывала вершины из-за низкого разрешения сетки и малого радиуса поиска).
- **Сделано**:
  1. **Устранение квадратных частиц воды (`ParticleSystem.ts`)**:
     - Полностью удалены плоские прямоугольные `PlaneGeometry` из водного эмиттера.
     - Водные брызги (`waterSpray`) переведены на аэродинамические конические 3D-капли (`ConeGeometry`), ориентирующиеся в пространстве точно по вектору своего полёта (`quaternion.setFromUnitVectors`).
     - Круги на воде (`waterRipple`) переведены на 24-сегментную сглаженную кольцевую геометрию.
     - Искры и листья переведены на октаэдрические и 6-гранные формы, исключающие квадратные артефакты.
  2. **Высокополигональная непрерывная деформация дороги (`terrain.ts`, `RoadGenerator.ts`, `TruckController.ts`)**:
     - Повышена плотность полигональной сетки рельефа: `segmentsX = 130` (шаг 0.80м) и `segmentLength = 0.85м`.
     - Расширен радиус фильтра деформации в `RoadGenerator.deformRoad()` до 7x7 окрестности (радиус 2.4м), что гарантирует захват вершин при любом смещении колёс.
     - Деформация активируется на всех типах грунта (деревенская земля, сухая грунтовка, мокрая колея, болото) с увеличенной базовой глубиной.
     - В `RoadGenerator.ts` добавлен метод `getDeformedHeightAt()`, благодаря которому 3D-лента следов шин (`TireTracksManager.ts`) плотно ложится на деформированное дно колеи и валики без зависания в воздухе.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция завершена за 6.10с без ошибок; `npm run check:physics` — все 15 физических тестов пройдены успешно.
- **Известные проблемы / следующий шаг**: провести тестирование читаемости дорожных следов в тёмных участках леса.

## 2026-08-20 14:15 — Переход на оптимальную архитектуру (Вариант 1): 60 FPS, визуальное погружение колёс и 3D-протекторы
- **Задача**: реализовать выбранный Вариант 1 — устранить просадку FPS от тяжёлого пересчёта нормалей сетки на CPU, убрать визуальный зазор между физическими лучами колеса и дном колеи, обеспечить красивое и честное «утопание» колёс в грязи и бродах, сохранить плавные органические брызги воды и персистентные 3D-следы протектора на грунте.
- **Сделано**:
  1. **Оптимизация рельефа и полное восстановление 60 FPS (`terrain.ts`, `RoadGenerator.ts`)**:
     - Возвращена оптимальная сетка ландшафта (`segmentsX = 80`, `segmentLength = 1.2`), строящаяся статически один раз при старте уровня.
     - Полностью убран цикл деформации сетки на CPU и вызовы `computeVertexNormals()`, вызывавшие микрофризы и просадку частоты кадров.
  2. **Реалистичное визуальное погружение колёс (`TruckController.ts`)**:
     - В методе `render()` внедрено динамическое погружение ступиц колёс (`targetSink` до 25–30 см в грязи и лужах) с учётом прокачки шин (`currentUpgrades.tires`).
     - При наезде на грязь, жижу или водный брод колесо погружается в поверхность ровно настолько, насколько глубока топь, создавая честный эффект тяжести и преодоления бездорожья без визуальных багов.
  3. **3D-следы протектора на грунте (`TireTracksManager.ts`)**:
     - 3D-лента отпечатков шин плотно и гладко ложится на поверхность дороги с нулевыми аллокациями памяти (мокрые следы после воды, тёмные жирные борозды в болотах, лёгкие пылевые следы на грунтовке).
  4. **Органический водный VFX (`ParticleSystem.ts`)**:
     - Сохранены гладкие конические капли воды по вектору скорости, белая хлопьевидная пена и 24-сегментные круги ряби на воде.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция завершена за 6.28с без ошибок; `npm run check:physics` — все 15 физических тестов пройдены успешно.
- **Известные проблемы / следующий шаг**: провести тестирование сбалансированности времени высыхания мокрых следов после брода.

## 2026-08-20 14:22 — Тонкая настройка высоты и разлёта частиц воды, грязи и мягкой прозрачности пены
- **Задача**: опустить точку вылета частиц воды и грязи к уровню водной глади/пятна контакта шины; увеличить радиальный разлёт брызг воды в стороны; сделать белую пену, дымку и клубы пыли более мягкими и полупрозрачными; сохранить честную реакцию физики машины на грязь и мягкое погружение колёс.
- **Сделано**:
  1. **Низкая высота и широкий радиальный веер воды (`ParticleSystem.ts`)**:
     - Точка генерации водных брызг опущена строго к поверхности брода (`groundY = pos.y - wheelRadius * 0.92 + 0.02`).
     - Скорость бокового разлёта увеличена до $6.5 \dots 14.5$ м/с, а вертикальная скорость снижена, благодаря чему капли разлетаются широким низким веером по бокам от колёс.
     - Радиус расхождения волн ряби на воде (`waterRipple`) увеличен до $3.6 \dots 5.8$ м.
  2. **Мягкая полупрозрачность пены, тумана и выхлопа (`SceneManager.ts`, `ParticleSystem.ts`)**:
     - Белая пена (`waterFoam`) и дымка (`waterMist`) получили деликатную прозрачность (`0.35` и `0.18`), исключающую плотные белые непрозрачные пятна.
     - Начальная плотность дорожной пыли (`dustParticle`) и холостого дыма уменьшена до лёгкого воздушного шлейфа.
  3. **Низкая баллистическая траектория грязи (`ParticleSystem.ts`)**:
     - Частицы грязи вылетают из самого низа колеса (`pos.y - wheelRadius * 0.96 + 0.02`) по низкой настильной траектории вдоль дороги.
  4. **Физическая реакция на грязь и погружение (`TruckController.ts`)**:
     - Сохранено падение сцепления, гидродинамическое сопротивление жижи и мягкое визуальное погружение колёс в толщу топи.
- **Затронутые файлы**: `src/rendering/ParticleSystem.ts`, `src/rendering/SceneManager.ts`, `src/vehicle/TruckController.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция завершена за 6.29с без ошибок; `npm run check:physics` — все 15 физических тестов пройдены успешно.
- **Известные проблемы / следующий шаг**: провести контрольный тест на разных разрешениях экрана.

## 2026-08-20 14:40 — Комплексная оптимизация рендера и CPU
- **Задача**: снизить нагрузку на CPU и GPU, сохранив игровые ощущения и 60 FPS на мобильных устройствах. Оптимизировать сборку (бандл, кэширование).
- **Сделано**:
  1. **Ликвидирован скрытый CPU-бутылочный горлышко в TireTracksManager (`TireTracksManager.ts`)**:
     - Убрана покадровая `geometry.computeVertexNormals()` в `flush()`. Лента следов шин лежит почти горизонтально на поверхности трассы, поэтому статические нормали «вверх» визуально неотличимы от пересчитанных, а экономия — несколько мс CPU каждый кадр.
  2. **Устранена аллокация Vector3 каждый кадр (`SceneManager.ts`)**:
     - В `render()` вместо `new THREE.Vector3(forward.x, 0, forward.z)` используется переиспользуемый `scratchForwardXZ`, инициализируемый один раз. Минус один `new` объект на каждый рендер-кадр (60×/сек).
  3. **Оптимизированы настройки WebGL-рендерера (`SceneManager.ts`)**:
     - Отключён MSAA (`antialias: false`) — дорогостоящий для мобильных GPU.
     - Теневая карта снижена с 1024×1024 → 512×512 (`BasicShadowMap`), обрезка тени с 32 → 28 единиц — в 4 раза меньше пикселей в shadow pass.
     - Максимальный Device Pixel Ratio снижен с 1.5 → 1.0: на Retina-экранах рендерится вдвое меньше пикселей без ощутимой потери качества.
  4. **Снижена сложность геометрий и пул частиц (`ParticleSystem.ts`)**:
     - Puff-сфера: уровень детализации с 1 → 0 (80 → 12 треугольников на частицу).
     - Конус брызг: 8 → 6 граней; кольцо ряби: 24 → 12 сегментов.
     - Суммарный пул частиц: 243 → 148 Mesh-объектов (-39%). Количество активных draw calls во время эффектов воды и грязи пропорционально снижается.
  5. **Разбивка бандла на кэшируемые чанки (`vite.config.ts`)**:
     - Three.js (478 KB gz: 121 KB) и Rapier WASM (2067 KB gz: 759 KB) вынесены в отдельные чанки (`vendor-three`, `vendor-rapier`). После первой загрузки обе библиотеки кэшируются браузером и не скачиваются повторно при обновлениях игрового кода.
     - Игровой чанк: 2975 KB → 427 KB (-86%), что ускоряет перезагрузку в dev и hot-reload.
- **Затронутые файлы**: `src/rendering/TireTracksManager.ts`, `src/rendering/SceneManager.ts`, `src/rendering/ParticleSystem.ts`, `vite.config.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция 4.91с, 0 ошибок; `npm run check:physics` — все 15 физических тестов пройдены успешно.
- **Известные проблемы / следующий шаг**: BasicShadowMap оказался неприемлемым по качеству — исправлено в следующей записи.

## 2026-08-20 14:52 — Хотфикс: восстановление качества теней
- **Задача**: тени стали угловатыми и пикселизированными после оптимизации (BasicShadowMap + 512px).
- **Сделано**:
  - Возвращён `THREE.PCFSoftShadowMap` — мягкие сглаженные тени, соответствующие стилистике игры.
  - Размер shadow map восстановлен до 1024×1024 с оригинальным охватом камеры (±32 ед., far=160).
  - Shadow bias восстановлен до -0.0012.
  - Остальные оптимизации сохранены: antialias off, DPR не более 1.0, уменьшенный пул частиц, чанки бандла.
- **Затронутые файлы**: `src/rendering/SceneManager.ts`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` — компиляция 4.49с, 0 ошибок.
- **Известные проблемы / следующий шаг**: нет.

## 2026-08-20 15:00 — Переработка UI и мобильное управление для портретного и альбомного режимов
- **Задача**: Переработать UI и создать мобильное управление с полноценной поддержкой двух режимов: портретного (Portrait) и альбомного (Landscape).
- **Сделано**:
  1. **Адаптивное мобильное управление (`src/input/TouchControls.ts`, `src/input/InputManager.ts`)**:
     - Реализован эргономичный тач-слой на базе Pointer Events с поддержкой мультитача и `setPointerCapture`.
     - *Левая зона (Руление)*: 2-осевой плавающий джойстик с авто-центрированием и мёртвой зоной (8%) + тактильные кнопки прямого нажатия `◀ ВЛЕВО` и `ВПРАВО ▶`.
     - *Правая зона (Педали и действия)*: крупная рифлёная педаль «ГАЗ» (с тактильным зелёно-янтарным откликом), педаль «ТОРМОЗ / РЕВЕРС», кнопка «РУЧНИК» и кнопка «СБРОС / ПЕРЕВЕРНУТЬ» для возврата на трассу.
     - Полный сброс осей и кнопок при `blur`, `visibilitychange` и переходе в меню/паузу.
     - Поддержка отладочного флага `?touch=1` / `?touch=0` и автоматического распознавания тачскринов.
  2. **Адаптивная 3D-камера для портрета и альбома (`src/rendering/SceneManager.ts`)**:
     - В `onResize()` внедрён динамический расчёт вертикального угла обзора (FOV: от 52° в альбоме до 82° в портрете), предотвращающий обрезание дороги по бокам.
     - Дистанция и высота следования камеры автоматически корректируются в зависимости от ориентации, сохраняя идеальный обзор грузовика, груза и рельефа впереди.
     - В шоуруме Гаража радиус и высота орбиты адаптированы под пропорции экрана.
  3. **Переработка и редизайн всего интерфейса (`src/ui/UIManager.ts`, `src/styles.css`)**:
     - *HUD*: индикатор монет `🪙`, информативная шкала целостности груза с динамическим цветом (зелёный/оранжевый/красный), плашки опасных участков («ГЛУБОКИЙ БРОД», «ВЯЗКАЯ ГРЯЗЬ», «РАЗВИЛКА»), быстрый переключатель звука `🔊`/`🔇`, кнопка паузы и спидометр с мини-треком маршрута.
     - *Главное меню, Гараж, Выбор уровня (50 маршрутов), Пауза, Итоги*: полная адаптивность под портрет и альбом, поддержка Safe Area Insets (`calc(16px + env(safe-area-inset-*))`), эффект матового стекла и тёплая лесная цветовая палитра.
  4. **Улучшение физического контроллера грузовика (`src/vehicle/TruckController.ts`)**:
     - Добавлена функция мгновенного переворота на дорогу `recoverNow()` по кнопке «СБРОС» на мобильных и клавише `R` на ПК.
  5. **Аудиосистема (`src/audio/AudioManager.ts`)**:
     - Добавлен метод мгновенного переключения звука `setMuted()` с синхронизацией состояния во всех меню и сохранениях.
- **Затронутые файлы**:
  - `src/input/TouchControls.ts`
  - `src/input/InputManager.ts`
  - `src/rendering/SceneManager.ts`
  - `src/ui/UIManager.ts`
  - `src/styles.css`
  - `src/vehicle/TruckController.ts`
  - `src/audio/AudioManager.ts`
  - `src/core/types.ts`
  - `README.md`
  - `CHANGELOG.md`
  - `DEVLOG.md`
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция завершилась за 4.99с, 0 ошибок.
- **Известные проблемы / следующий шаг**: провести тестирование в мобильных iframe Playgama Bridge на iOS и Android.

## 2026-08-20 15:10 — Переход на чистое управление джойстиком и оптимизация удобства UI
- **Задача**: Оставить только виртуальный джойстик, убрать все лишние кнопки управления на экране. Сделать весь UI максимально удобным и комфортным для игрока.
- **Сделано**:
  1. **Чистое управление одним джойстиком (`src/input/TouchControls.ts`, `src/styles.css`)**:
     - Полностью удалены экранные педали и вспомогательные кнопки (газ, тормоз, ручник, кнопки поворота).
     - Реализован полноэкранный интуитивный плавающий джойстик на Pointer Events: касание в любой точке экрана динамически создаёт красивую светящуюся базу со стрелками-направляющими.
     - *Тяга / Газ*: свайп вверх (`Y < 0`).
     - *Тормоз / Реверс*: свайп вниз (`Y > 0`).
     - *Руление*: свайп вправо/влево (`X > 0` / `X < 0`).
     - Мягкая мёртвая зона (6%) и плавная кривая отклика для ювелирного маневрирования на брёвнах и полного газа на прямых.
  2. **Повышение удобства и эргономики UI (`src/ui/UIManager.ts`, `src/styles.css`)**:
     - *HUD*: компактный верхний бар со счётчиком монет `🪙`, шкалой груза `🪵 8/8`, быстрой кнопкой звука `🔊`/`🔇` и паузой `⏸`. Спидометр перенесён в нижний правый угол (в альбоме) и вниз по центру (в портрете) с мини-треком маршрута.
     - *Главное меню*: карточка активного грузовика с аватаром и характеристиками, крупная кнопка «В рейс!», удобная сетка кнопок «Гараж», «Выбор уровня», «Звук».
     - *Гараж*: наглядная шкала улучшений с точками прогресса (`●●●○○`), удобные карточки грузовиков, мгновенная смена цвета в 3D и фиксированная кнопка выбора/покупки внизу.
     - *Карта уровней*: компактные карточки с тегами грузов, звёздами и быстрым переключением глав 1–10, 11–20, 21–30, 31–40, 41–50.
- **Затронутые файлы**:
  - `src/input/TouchControls.ts`
  - `src/ui/UIManager.ts`
  - `src/styles.css`
  - `README.md`
  - `CHANGELOG.md`
  - `DEVLOG.md`
- **Проверено**: `npm run build` (`tsc -b && vite build`) — компиляция 4.55с, 0 ошибок.
- **Известные проблемы / следующий шаг**: нет.

## 2026-08-20 15:53 — Тайга: Экспедиция, чистый HUD и адаптивный гараж
- **Задача**: убрать лишние элементы из игрового HUD, временно скрыть инверсию в паузе, переделать интерфейс гаража по приложенным примерам, переименовать игру и исправить отображение скорости и груза.
- **Сделано**:
  - Игровой HUD очищен: оставлены только количество груза, прогресс маршрута, скорость, пауза и ситуативные предупреждения; звук, тип груза и название уровня убраны из игрового экрана.
  - Скорость для HUD теперь читается после шага физики Rapier; значения скорости и груза нормализуются перед выводом, а стартовое количество груза показывается сразу.
  - Гараж перестроен в адаптивный шоурум: верхняя панель ресурсов, профиль автомобиля, характеристики, покраска, центральная 3D-зона с переключением машин, улучшения, автопарк и нижняя навигация.
  - На телефоне гараж переходит в одну колонку, автопарк получает горизонтальную прокрутку, а safe-area отступы защищают панели от краёв и вырезов.
  - Кнопка инверсии руля в паузе закомментирована и не создаётся; название игры изменено на «Тайга: Экспедиция».
- **Затронутые файлы**: `src/ui/UIManager.ts`, `src/styles.css`, `src/vehicle/TruckController.ts`, `index.html`, `README.md`, `generation.json`, `DEVLOG.md`, `CHANGELOG.md`.
- **Проверено**: `npm run build` — успешно, без ошибок TypeScript; `npm run dev -- --host=127.0.0.1` — Vite запустился и сообщил локальный адрес.
- **Известные проблемы / следующий шаг**: автоматическая проверка визуального результата и консоли браузера в этом окружении не выполнялась; нужен ручной плейтест гаража в портретном и альбомном режимах.

## 2026-08-20 16:55 — Комплексная оптимизация FPS для мобильных устройств
- **Задача**: устранить низкий FPS и просадки производительности на мобильных телефонах, обеспечить стабильные 60 FPS на смартфонах с соблюдением бюджетов по draw calls и треугольникам.
- **Сделано**:
  1. **Оптимизация рендеринга и теней (`src/rendering/SceneManager.ts`)**:
     - Заменён тяжёлый шейдер мягких теней `PCFSoftShadowMap` на аппаратный `THREE.PCFShadowMap` (2x2 билинейный PCF depth-compare). Достигнуто 3-5x ускорение расчёта теней на мобильных GPU с сохранением гладких антиалиасированных контуров.
     - Оптимизирована пирамида теней (`shadow.camera.near = 12`, `far = 130`, границы `±26`), повышена плотность разрешения теней без увеличения текстурной памяти.
     - WebGLRenderer настроен с `precision: 'mediump'`, `stencil: false`, `powerPreference: 'high-performance'`.
     - DPR ограничен до 1.0 на тач- и мобильных экранах.
  2. **Аппаратный GPU-инстансинг системы частиц (`src/rendering/ParticleSystem.ts`)**:
     - Полностью ликвидированы 148 индивидуальных `THREE.Mesh` объектов частиц из графа сцены.
     - Создано 10 легковесных `THREE.InstancedMesh` (для выхлопного дыма, пыли, комьев грязи, капель брызг воды, пены, ряби волн, водяного тумана, искр, листьев и конфетти).
     - Количество draw calls от системы частиц снижено со 148 до 0..10. Нагрузка на CPU по обходу графа сцены снижена на 90%.
  3. **Снижение числа вызовов отрисовки окружения (`src/world/RoadGenerator.ts`)**:
     - Придорожные валуны (20–50 штук) переведены на 2 `THREE.InstancedMesh` (по материалам `rock` и `rockDark`), сократив вызовы отрисовки с 50 до 2.
     - Дорожные щиты и указатели переведены на 2 `THREE.InstancedMesh` (для столбов и табличек), сократив вызовы отрисовки с 30 до 2.
     - Отключено отбрасывание теней для мелких кустарников (`bushMesh.castShadow = false`), что исключило 90+ ненужных объектов из shadow pass.
  4. **Объединение геометрии колёс (`src/vehicle/TruckController.ts`)**:
     - С помощью `BufferGeometryUtils.mergeGeometries` цилиндры шин и все 14–16 выступающих 3D-грунтозацепов объединены в единую геометрию протектора.
     - Ободья, ступицы и спицы дисков объединены в единую геометрию диска.
     - Число мешей колёс на 6-колёсных тягачах сокращено со 120 до 12.
  5. **Оптимизация сетки ландшафта и физики (`src/world/terrain.ts`, `src/core/GameLoop.ts`)**:
     - Плотность сетки ландшафта оптимизирована (`segmentsX = 56`, `segmentLength = 1.5`), число треугольников рельефа сокращено с ~72 000 до ~36 000 (в рамках бюджета < 45 000 полигонов).
     - Скорость трассировки лучей колёс в Rapier3D возросла в 2 раза.
     - В `GameLoop` внедрено ограничение физического аккумулятора (`fixedStep * 2`), исключающее каскадные зависания («spiral of death») при системных прерываниях на мобильных устройствах.
- **Затронутые файлы**:
  - `src/rendering/SceneManager.ts`
  - `src/rendering/ParticleSystem.ts`
  - `src/world/RoadGenerator.ts`
  - `src/vehicle/TruckController.ts`
  - `src/world/terrain.ts`
  - `src/core/GameLoop.ts`
  - `CHANGELOG.md`
  - `DEVLOG.md`
- **Проверено**:
  - `npm run build` (`tsc -b && vite build`) — компиляция и сборка успешно завершены (4.81с, 0 ошибок).
  - `npm run check:physics` — все 15 физических тестов пройдены успешно (включая тесты подвески, 6х6 тягача, грязевых секторов, развилок и удержания груза).
  - `npm run dev -- --host=127.0.0.1` — локальный сервер успешно запускается без ошибок.
- **Известные проблемы / следующий шаг**: провести контрольный замер FPS в реальном iframe Playgama Bridge на бюджетных Android-смартфонах.
