# Лесной Рейс: Доставка на Лесопилку 3D

> **Заведи старый добрый грузовик и доставь брёвна из тихой деревни на лесопилку! Дорога полна кочек, ям и крутых подъемов — удерживай баланс газа и тормоза, следи за спидометром и довези груз в целости, чтобы заработать максимум на прокачку машины.**

---

## Обзор
- **Жанр**: физический 3D-симулятор доставки по бездорожью.
- **Стек**: TypeScript, Vite, Three.js и Rapier3D.
- **Платформа**: Web, мобильные браузеры и Playgama Bridge с офлайн-fallback.
- **Ориентация**: landscape.

## Запуск
```bash
npm install
npm run dev
```

Для production-проверки:
```bash
npm run build
npm run preview
```

Открой адрес Vite в браузере. Для проверки мобильной раскладки мышью добавь `?touch=1`; `?touch=0` принудительно отключает её.

## Управление
- **Клавиатура**: `W` / `↑` газ, `S` / `↓` тормоз и реверс, `A` / `←` и `D` / `→` баланс грузовика, `Space` ручник, `P` или `Esc` пауза.
- **Телефон**: слева плавающий стик для направления, справа большая педаль газа, отдельные тормоз и ручник. Все элементы используют Pointer Events и поддерживают мультитач.
- Управление скрыто в меню, на паузе и на экране результата; safe-area отступы учитываются через CSS env.

---

## Структура каталогов
```text
workspace/rustic-cargo-truck-3d/
├── index.html                       # HTML-точка входа
├── src/main.ts                      # Boot guards, Bridge и запуск Game
├── src/core/                        # Game, fixed-step loop, EventBus и типы
├── src/world/                       # Процедурная трасса, деревня и лесопилка
├── src/rendering/                   # Three.js сцена, камера, свет и материалы
├── src/physics/                     # Rapier3D world и синхронизация мешей
├── src/vehicle/                     # TruckController и CargoManager
├── src/input/                       # Keyboard + Pointer Events touch controls
├── src/ui/                          # HUD, меню, пауза и результат рейса
├── src/audio/                       # Web Audio мотор, удары и финиш
├── src/platform/                    # Playgama Bridge и нормализованное сохранение
├── skills/                          # Обязательные инструкции домена проекта
├── DEVLOG.md                        # Журнал рабочих сессий
└── CHANGELOG.md                     # Изменения глазами игрока
```

---

## Разработка
1. Изменения игровой логики находятся в `src/`, а документация спецификации остаётся в корне.
2. После изменения исходников запускай `npm run build`.
3. Каждая рабочая сессия фиксируется в `DEVLOG.md` и `CHANGELOG.md`.
