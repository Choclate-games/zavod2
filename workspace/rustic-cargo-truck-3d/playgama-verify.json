{
  "_": "Разобранные ложные срабатывания yandex-games-debug-checker. Каждая запись проверена вручную; без причины длиной от 20 символов запись игнорируется. Пункт не исчезает из отчёта — он печатается отдельным блоком вместе с причиной.",
  "checkerAcknowledged": [
    {
      "match": "No profanity in UI text",
      "reason": "Совпадение на слове «гребёнка» (24-й уровень, «Гребёнка лесорубов»): русский стем «ебё» из правила чекера встречается внутри чистого слова. Проверено grep-ом по src/ — другой брани в тексте игры нет."
    },
    {
      "match": "No hardcoded",
      "reason": "Совпадение на минифицированном коде: терсер выдал `qs=700,$s=45` из констант SPLASH_SETTLE_MS и PROGRESS_SPEED, и регулярка чекера прочитала `700,$` как цену. Витрина берёт цены из payments.getCatalog(), хардкода валюты в src/ нет."
    },
    {
      "match": "Music via Web Audio",
      "reason": "`new Audio(` приходит из бандла @playgama/bridge — мост так проигрывает свой звук загрузки. Игра использует только Web Audio: AudioContext и осцилляторы в src/audio/AudioManager.ts, ни одного HTMLAudioElement и ни одного обращения к mediaSession."
    }
  ]
}
