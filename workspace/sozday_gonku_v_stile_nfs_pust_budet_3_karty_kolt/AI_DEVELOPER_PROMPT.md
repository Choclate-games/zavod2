# FINAL AI DEVELOPER PROMPT: НОЧНОЙ СИНДИКАТ: ДУЭЛИ И КОНТРАКТЫ 🎮⚡

> **ИНСТРУКЦИЯ КОДОВОМУ АГЕНТУ**
> Ты ведущий разработчик этой игры. Задача — довести её до состояния, в котором
> она проходит приёмку из `ACCEPTANCE.md`: работающая, играбельная, принятая
> площадкой. Заглушки, TODO и «пока так» результатом не считаются.

### Как читать этот документ

Требования размечены по обязательности. Когда обязательно всё, не обязательно ничего.

| Уровень | Что значит | Что делать при сомнении |
|---|---|---|
| **MUST** | Без этого игра не работает или не проходит модерацию площадки. | Не обсуждается. Проверяется в `ACCEPTANCE.md`, разделы A–C. |
| **SHOULD** | Проектное решение фабрики: так игра задумана. | Отступать можно, но с записью в `DEVLOG.md` — что и почему. |
| **MAY** | На твоё усмотрение. | Решай сам, спрашивать не нужно. |

Неразмеченное считается **SHOULD**. Всё, что помечено «ЗАПРЕЩЕНО» в секции 1b, —
это **MUST**, даже если спецификация где-то умалчивает.

### Порядок работы

Читать документ целиком до первой строчки кода не нужно. Порядок такой:

0. `node scripts/fetch-knowledge.mjs` — забрать базу знаний в `docs/ref/`. Одна команда, дальше работа идёт офлайн. **MUST**, и раньше всего остального: без этого половина адресов в документе никуда не ведёт.
1. `ACCEPTANCE.md` — чем закончится работа. Дальше всё делается под него.
2. Секция 1b — чем эта игра является и чем она не является. Рамка всего остального.
3. `DESIGN.md` — написать самому, до первой строчки кода. Секция 7 говорит, что в нём должно быть и чего в нём быть не должно.
4. Каркас: `package.json`, Vite, TypeScript strict, `src/vite-env.d.ts` с `/// <reference types="vite/client" />` (TypeScript 5 пропускает `import './ui/theme.css'` молча, TypeScript 6 роняет на нём сборку — один файл снимает вопрос навсегда), пустая сцена Three.js, цикл с фиксированным шагом. Playgama Bridge и `game_ready` — сразу, а не в конце (проверки **C1–C4**).
5. Главная механика из секции 3 — одна, до играбельного состояния. Не все сразу. Перед тем как писать её с нуля — секция 3c: возможно, она уже написана.
6. Дальше по фазам роадмапа (секция 8), каждая фаза закрывает свои номера проверок.
7. `node scripts/check-spec.mjs` — после каждой фазы, а не один раз в конце.
8. `node scripts/smoke.mjs` — перед каждым отчётом. Он собирает игру, открывает её в браузере и трогает управление. Зелёный `check-spec` при красном `smoke` означает ровно одно: код выглядит правильно и не работает.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: Ночной Синдикат: Дуэли и Контракты
- **Project Slug**: `sozday_gonku_v_stile_nfs_pust_budet_3_karty_kolt`
- **Genre**: 3D Аркадный Автосимулятор (Уличные ночные гонки с физикой дрифта и нитро)
- **Target Platform**: Playgama Bridge (Yandex Games / VK / Web)
- **Orientation**: Landscape
- **Target Audience**: Мужчины и женщины 12–35 лет, поклонники Need for Speed: Underground/Most Wanted, любители эффектных управляемых заносов, быстрых аркадных дуэлей и неоновой эстетики ночных мегаполисов.
- **Player Fantasy**: Неуловимый ночной уличный гонщик, виртуозно контролирующий мощный японский спорткар в миллиметровых заносах на мокром неоновом асфальте спящего мегаполиса.
- **Core Hook**: Выход из затяжной шпильки под 50 градусов в метре от бетонного отбойника с мгновенным подрывом бирюзового нитро, когда камера расширяет FOV до 85°, а соперник эффектно обходится по внутренней кромке в клубах подсвеченного неоном дыма!
- **Session Model**: Сессия состоит из 2–4 заездов длительностью по 60–90 секунд каждый в 3 различных форматах (Кольцо Downtown Loop на 2 круга по 1.8 км, Шоссейный Спринт Neon Highway на 3.5 км точка-в-точку, Техничный Дрифт-Трек Port Docks на 3 круга до 15 000 очков) с общим временем игровой сессии 4–7 минут.


---

## 1b. РАМКА ПРОЕКТА: ЧТО ЭТО ЗА ИГРА И ЧЕМ ОНА НЕ ЯВЛЯЕТСЯ


**Направление**: Ночной Синдикат: Дуэли и Контракты

**Почему выбрано именно оно**: Направление D1 максимально точно и бескомпромиссно реализует исходный заказ пользователя на гонку в духе Need for Speed с 3 картами. Чейз-камера от третьего лица создает глубокое погружение и идеальное ощущение скорости на мокром неоновом асфальте. Аркадный цикл 'занос заряжает нитро -> нитро дает мощный рывок' моментально понятен с первой секунды на телефоне и ПК без обучения. Все три запрошенные карты (Кольцо по даунтауну, Спринт по шоссе, закрытый дрифт-Трек) органично раскрывают разные аспекты управления машиной на базе проверенной физики Rapier3D с высокой частотой кадров.

**Узнаваемая сцена**: Ночь, мокрый глянцевый асфальт под эстакадой ночного мегаполиса. Тюнингованный японский спорткар цвета полуночного ультрамарина запечатлен в глубоком управляемом заносе под 45 градусов, пролетая в сантиметрах от бетонного отбойника с неоновой рекламой. Задние колеса размыты от вращения и окутаны плотными клубами дыма, подсвеченными снизу фиолетовым неоном днища, а из двух сопел титанового глушителя вырывается яркое бирюзовое пламя закиси азота. Слева в контровом свете фар виден силуэт обгоняемого черного маслкара, а на мокрой дороге идеально отражаются вертикальные неоновые иероглифы и высотные башни на заднем плане.

**Без чего проект перестаёт быть собой** (реализовать обязательно):
- Физический контроллер автомобиля на базе RAPIER.DynamicRayCastVehicleController с дифференцированным сцеплением колес и управляемым ручником для срыва в занос.
- Механика накопления нитро за счет поддержания угла дрифта и рискованного вождения с эффектным расширением FOV камеры и пламенем из выхлопа.
- Полноценная реализация всех 3 заявленных карт: городское кольцо (Circuit), загородный спринт точка-в-точку (Sprint) и техничный автодром (Track).
- Адаптивный ИИ соперников (rubberband opposition), поддерживающий плотную контактную борьбу на протяжении всей дистанции заезда.

**ЗАПРЕЩЕНО ВВОДИТЬ** — даже если спецификация где-то умалчивает и даже если «так принято в жанре»:
- Никаких волн врагов, зомби или авторогаликов с карточками в стиле Vampire Survivors.
- Никаких серых кубов вместо графики: весь мир собирается из стилизованной low-poly архитектуры с неоновым свечением и шейдером мокрого асфальта.
- Никакого экранного плавающего джойстика: управление четко разделено на удобные зоны тапов и удержания без потери контроля над машиной.
- Никакого рельсового автопилота со свайпами (как в мобильных спиноффах): игрок полностью и непрерывно контролирует физику, траекторию и газ машины.
- Никаких громоздких меню и искусственных таймеров ожидания на ремонт или бензин — мгновенный рестарт и переход между трассами.
- волны врагов на арене
- универсальная связка «дэш + парирование + удар»
- заимствование Vampire Survivors / Brotato как основы
- джойстик слева + кнопки атаки справа как раскладка по умолчанию
- серые кубы вместо стилизованной геометрии
- мета-прогрессия «золото за забег -> постоянные апгрейды» без связи с темой

Если по ходу реализации кажется, что без запрещённого элемента игра не работает — это находка для `DEVLOG.md`, а не разрешение его добавить.

**Рассмотренные и отклонённые направления** (не возвращай их через механики):
- D2 (Апекс-Мастер: Скоростной Тайм-Атак) отвергнуто из-за смещения в строгий симуляторный тайм-триал с жестким требованием точного торможения; бамперная камера скрывает внешний вид и кастомизацию автомобиля (визитная карточка NFS), а высокий порог входа снижает удержание казуальной аудитории на мобильных устройствах.
- D3 (Перехват: Прорыв из Окружения) отвергнуто из-за отдаления камеры в изометрию сверху: ракурс с высоты птичьего полета нивелирует чувство скорости и драйв от управления машиной от третьего лица, превращая гонку в аркадный тактический боевик с разрушением окружения вместо аутентичного стрит-рейсинга.

---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite
- **Renderer**: **THREEJS** (^0.185.1)
  - *Selection Rationale*: Three.js + Rapier3D: пространственный геймплей, физика и освещение. Перспективная камера.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat ^0.20.0)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Web Audio API (Howler.js with html5: false)
- **State Management**: Reactive EventBus / State Store
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 75
- **Max Triangles / Active Sprites**: < 45000
- **Max Bundle Size**: < 4.5 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Выбор контракта и трассы -> Заезд на 60–90 сек (дрифт заряжает нитро -> мощный рывок на прямой -> контактный обгон соперников) -> Финиш в топ-3 -> Награда (кредиты + репутация) -> Тюнинг авто в гараже -> Доступ к новым трассам и сложным дуэлям
```

**Фирменный момент**: Ночной спринт по хайвею: на скорости 195 км/ч на мокром асфальте игрок закладывает спорткар в поворот под 48 градусов в 0.3 метра от бетонного отбойника, искры от касания дисков осыпают асфальт, множитель риска подскакивает до x4.0, мгновенно заполнив 3-й баллон бирюзового нитро; игрок сбрасывает ручник, вдавливает кнопку форсажа — камера рывком отдаляется с расширением FOV с 60° до 85°, радиальный блюр смазывает неоновые небоскребы, и болид с ревом перегруженной турбины на 240 км/ч протискивается в 50-сантиметровый зазор между лидирующим маслкаром соперника и фурой трафика, пересекая финишную черту за 0.12 секунды до соперника под взрыв неона и фанфары репутации Синдиката!

**Чем петля отличается от соседей по жанру**: В отличие от стандартных аркад, где нитро восстанавливается по пассивному таймеру или подбором пикапов с трассы, в «Ночном Синдикате» нитро — это конвертируемая валюта риска и мастерства. Заряд генерируется только через два сопряженных действия: удержание предельного угла заноса (угол 35°–60° на скорости выше 90 км/ч) и экстремальное сближение с отбойниками и трафиком («Лезвие бритвы», <1.2 м). Риск катастрофичен: малейшее касание сбрасывает комбо-множитель и прерывает закись азота, но идеальный тайминг выхода из заноса с прострелом нитро катапультирует спорткар из апекса поворота быстрее любого ИИ.

**Шаблон жанра, который НЕ реализуем**: Отвергнут шаблон «бесцельного открытого мира с рутинными переездами между маркерами» (NFS Underground 2 / Forza Horizon) и шаблон «стерильного кольцевого симулятора с замедлением на газоне» (Gran Turismo). Причина отказа: для 3D WebGL-сессии (Playgama / Яндекс Игры) открытый мир размывает фокус, перегружает оперативную память браузера фоновыми ассетами и создает простои до 70% времени без адреналина. Вместо этого выбрана концентрированная аркадная структура сессий по 60–90 секунд на 3 характерных трассах (Downtown Loop, Neon Highway, Port Docks), где 100% времени игрок находится в состоянии плотной дуэли, непрерывного управляемого дрифта и рискованного накопления нитро без холостой езды.

**Схема петли**:
```text
+-----------------------------------------------------------------------+
  |                   ПОДЗЕМНЫЙ ГАРАЖ СИНДИКАТА                           |
  |  [Выбор Контракта] ---> [Тюнинг: Турбина / Шины / Неон] ---> [Старт]  |
  +-----------------------------------+-----------------------------------+
                                      |
                                      v
  +=======================================================================+
  |              ГОНОЧНЫЙ ЗАЕЗД (ДЛИТЕЛЬНОСТЬ: 60-90 СЕК)                 |
  |                                                                       |
  |   +-----------------> [ ПРЯМАЯ: РАЗГОН ] <------------------------+   |
  |   |                   |  - Вход в слипстрим за ИИ                 |   |
  |   |                   |  - Достижение 180+ км/ч                   |   |
  |   |                   v                                           |   |
  |   |          [ ТОРМОЖЕНИЕ / РУЧНИК ]                              |   |
  |   |          - Срыв задней оси (Скандинавский щелчок)             |   |
  |   |          - Контрруление + балансировка газом                  |   |
  |   |                   v                                           |   |
  |   |       [ ДРИФТ В ПОВОРОТЕ: 35°-60° ]                           |   |
  |   |       +-- Близость к отбойнику (<1.0м) = Множитель x2..x4     |   |
  |   |       +-- Накопление баллонов Нитро (0 -> 100%)               |   |
  |   |                   v                                           |   |
  |   |       [ ВЫХОД ИЗ АПЕКСА + ПРОЖИГ НИТРО ]                      |   |
  |   |       - Рывок форсажа (+45 км/ч, FOV 60°->85°)                |   |
  |   |       - Контактный обгон соперника по кромке                  |   |
  |   |                   |                                           |   |
  |   +-------------------+                                           |   |
  |                                                                   |   |
  |   [ ФИНИШНАЯ ЧЕРТА (Чекпоинт 0.00 с / Топ-3 / 15 000 Drift Pts) ]  |
  +===================================+===================================+
                                      |
                                      v
  +-----------------------------------------------------------------------+
  |                    ЭКРАН НАГРАД И ПРОГРЕССИИ                          |
  |  [Кредиты Синдиката] + [Очки Репутации (Rep Tiers 1-5)]               |
  |  --> Разблокировка новых запчастей, трасс и Дуэлей с Боссами          |
  +-----------------------------------------------------------------------+
```

**Микро-петля (посекундно)**:
- **MICRO_01_APPROACH** (1.5–2.5 с): игрок — Держит газ в пол (W / Тач-кнопка «Газ») на прямой, нагоняет соперника в воздушном кармане (слипстрим).; игра — Спидометр растет до 180 км/ч, шум ветра усиливается (WhiteNoise gain 0.35), за кормой соперника отрисовывается неоновый след разрежения.; решение — Атаковать соперника по внешней дуге для широкого дрифта или нырнуть во внутренний радиус на торможении.
- **MICRO_02_DRIFT_INITIATION** (0.2–0.4 с): игрок — Кратковременно жмет Пробел / Тач-ручник на 0.22 с и отклоняет руль (A/D или Стик) в сторону поворота.; игра — Задняя ось теряет 65% латерального сцепления, кузов разворачивается под углом 35°, задние покрышки дымят бирюзовым дымом, звучит визг резины.; решение — Сколько миллисекунд удерживать ручник, чтобы сорвать заднюю ось, но не уйти в неуправляемый разворот на 360 градусов.
- **MICRO_03_DRIFT_BALANCING_RISK** (2.0–4.0 с): игрок — Контррулит в противоположную сторону (D при левом заносе), дозируя газ импульсами для удержания угла 45.0°.; игра — Растет шкала угла (45°), счетчик очков заноса крутит +150 pts/сек. При дистанции <1.0 м до отбойника вспыхивает индикатор «RAZOR EDGE x2.5» и сыплются искры.; решение — Прижаться еще на 20 см ближе к бетону ради множителя x4.0 или отойти на безопасную траекторию.
- **MICRO_04_NITRO_BLAST** (1.5–2.5 с): игрок — Выравнивает колеса на прямую и зажимает Shift / Кнопку «NITRO».; игра — Взрыв синего пламени из выхлопа, FOV рывком расширяется с 60° до 85°, физический импульс толкает машину вперед (+45 км/ч за 0.8 с), экран сотрясает вибрация.; решение — Сжечь весь баллон сразу для гарантированного обгона или сберечь заряд на финальную прямую.
- **MICRO_05_OVERTAKE_EXIT** (1.0–2.0 с): игрок — Прорезает траекторию соперника, блокируя его контратаку корпусом болида.; игра — Срабатывает событие «CLEAN OVERTAKE» (+500 очков стиля), ИИ соперника уходит на торможение, на табло обновляется позиция (P1).; решение — Занять защитную траекторию перед следующим связным поворотом.

**Мезо-петля (этап)**:
- **MESO_01_GRID_LAUNCH** (10–15 с): игрок — Ловит идеальные обороты двигателя (Green Zone 6200 RPM) на обратном отсчете старта и пробивается сквозь стартовую решетку из 4 машин.; игра — Идеальный старт дает мгновенный рывок Perfect Launch (+15% ускорения без пробуксовки) и 0.5 баллона нитро.; решение — Идти на жесткий контакт с соперниками слева или безопасно занять внешнюю линию для раннего дрифта.
- **MESO_02_HIGHWAY_SLIPSTREAM** (25–35 с): игрок — Маневрирует в потоке трафика, пристраивается в хвост лидеру гонки для набора скорости в аэродинамическом слипстриме.; игра — Сопротивление воздуха снижается на 40%, скорость вырастает со 170 до 210 км/ч, активно заполняя баллоны нитро.; решение — Рискнуть на обгон в миллиметрах от встречного трафика ради множителя x4 или выждать безопасный карман.
- **MESO_03_TECHNICAL_CHICANE** (25–30 с): игрок — Выполняет быструю связку из трех перекладок заноса (S-turn drift combo) с ювелирным контролем инерции возле контейнеров.; игра — Комбо очков дрифта удерживается, множитель достигает x4.0, баллоны нитро заполняются до максимума (3/3).; решение — Связать связку поворотов непрерывным дрифтом через ручник или стабилизировать машину для набора чистой скорости.
- **MESO_04_FINAL_NITRO_DUEL** (10–15 с): игрок — Активирует тройной прожиг нитро (Triple Burn) на финишной прямой под прессингом накатывающего сзади соперника.; игра — Звучит кульминационный рев турбины, включается замедление времени на 0.5 с при пересечении финиша (Finishing Cam), фиксируется победа.; решение — Блокировать правый борт от тарана ИИ или сосредоточиться на идеальной траектории финишного створа.

**Макро-петля (забег)**:
- **MACRO_01_CONTRACT_SELECTION** (10–20 с): игрок — Выбирает контракт Синдиката в меню карты ночного города (напр. «Дуэль за набережную», сложность Tier-2, награда 4 500 кредитов + 120 репутации).; игра — Отображает требования к классу автомобиля, погодные условия (мокрый неоновый асфальт) и бонусные условия начисления очков репутации.; решение — Выбрать прибыльный высокорискованный контракт со ставкой кредитов или надежную кольцевую гонку для стабильного фарма.
- **MACRO_02_RACE_EXECUTION** (60–90 с): игрок — Проходит 3D заезд в реальном времени, комбинируя срыв в занос ручником, форсаж нитро на прямых и контактный обгон соперников.; игра — Физический движок Rapier3D рассчитывает коллизии и сцепление колес, адаптивный ИИ навязывает контактную дуэль, фиксируется итоговый результат заезда.; решение — Балансировать риск между чистой победной траекторией 1-го места и рискованным скольжением возле стен ради дрифт-бонуса контракта.
- **MACRO_03_REWARD_PAYOUT** (10–15 с): игрок — Просматривает экран итогов заезда, собирает базовые кредиты, дрифт-бонусы за стиль и очки уважения синдиката.; игра — Анимирует начисление кредитов, заполняет шкалу репутации Синдиката до нового ранга (Tier 3) и разблокирует новые детали в каталоге.; решение — Потратить заработанные кредиты немедленно на апгрейд турбины Stage-2 или накопить на спортивные шины повышенной термостойкости.
- **MACRO_04_GARAGE_TUNING** (20–40 с): игрок — Устанавливает в гараже спортивный дифференциал, прокачивает турбину, монтирует бирюзовый неон под днище и кастомный спойлер.; игра — Пересчитывает физические характеристики авто в реальном времени (Drift Control 72 -> 85, Top Speed 210 -> 228 км/ч) и обновляет 3D-модель с неоновой подсветкой.; решение — Сконфигурировать спорткар в чистый дрифт-корч с широким углом выворота или в скоростной хайвей-снаряд с максимальной тягой нитро.
- **MACRO_05_TIER_UNLOCK** (10–15 с): игрок — Принимает вызов босса Синдиката на дуэль за право контроля центрального шоссе.; игра — Открывает доступ к элитной дуэли «Pink Slip» 1-на-1 и разблокирует сложную закрытую трассу Port Docks Drift Arena.; решение — Бросить вызов боссу района немедленно или провести тренировочный заезд на новой конфигурации деталей.

**Кривая напряжения**: Пилообразная синусоида высокой частоты с кульминацией на финише: 1. Секунды 0–10 (Старт): Резкий стартовый всплеск адреналина (80%) — борьба в узком горлышке первого поворота, риск завала; 2. Секунды 10–35 (Хайвей/Кольцо): Циклы напряжения (40% на прямых во время набора слипстрима -> 90% в апексах поворотов при удержании дрифта в 30 см от стены на множителе x4); 3. Секунды 35–65 (Средняя фаза): Плато высокой концентрации (75%) — адаптивный ИИ прессингует бампер в бампер, любая ошибка срывает комбо; 4. Секунды 65–85 (Техничная шпилька): Пик напряжения (95%) — череда двойных перекладок ручником, дефицит времени на секундомере чекпоинта; 5. Секунды 85–90 (Финишный створ): Взрывная разрядка (100% -> катарсис) — тройной прожиг нитро, финиш нос к носу с лидером, замедление времени и победный фанфарный аккорд.

**Формулы ядра (реализуй буквально)**:
- `Формула скорости накопления нитро в дрифте: dNitro/dt = BaseChargeRate * (DriftAngle / IdealAngle) * (CurrentSpeed / MaxSpeed) * RiskMultiplier, где BaseChargeRate = 25.0% в сек, IdealAngle = 45.0°, RiskMultiplier = [1.0..4.0] в зависимости от дистанции до отбойника.`
- `Формула множителя риска «Лезвие бритвы» (Razor Edge Proximity): RiskMultiplier = 1.0 + clamp((WallProximityThreshold - DistanceToObstacle) / WallProximityThreshold, 0.0, 1.0) * 3.0, где WallProximityThreshold = 1.20 м. При дистанции <= 0.35 м множитель равен ровно 4.0x.`
- `Формула начисления дрифт-очков стиля: DriftScoreTick = (CurrentSpeed_kmh * 0.50) * sin(DriftAngle_rad) * RiskMultiplier * DeltaTime_sec * (ComboStreak + 1).`
- `Формула адаптивной тяги соперников (Rubberband AI): AiTorqueModifier = 1.0 + clamp((DistanceToPlayer_m - DesiredGap_m) * 0.035, -0.25, 0.40), где DesiredGap = 15.0 м. Если бот отстает на 30 м, его крутящий момент растет на +40%, если опережает на 30 м — падает на 25%.`
- `Формула динамического латерального сцепления шин (Rapier Lateral Grip): LateralGrip = BaseGrip * (1.0 - HandbrakeInput * 0.65) * (1.0 / (1.0 + (SlipAngle / DriftTransitionAngle)^2)), где DriftTransitionAngle = 18.5°.`
- `Формула динамического ускорения при форсаже Нитро: BoostAcceleration = (EngineTorque * 1.60 / VehicleMass_kg) + (NitroThrust_N / VehicleMass_kg) * NitroTierMult, где NitroThrust = 4500 Н, NitroTierMult = [1.0..1.5].`

**Прогрессия внутри забега**:
- Стартовый статус: 0% шкалы нитро, комбо-множитель риска x1.0, холодные шины (базовый зацеп 100%), позиция P4/P4.
- Накопление 1-го баллона нитро (секунды 10–25): за счет прохождения первых 2 поворотов в заносе открывается первый тактический рывок для обгона P3.
- Разгон комбо-стрика «Лезвие бритвы» (секунды 25–50): безаварийный дрифт вблизи отбойников разгоняет множитель очков стиля до x3.0–x4.0, нитро восполняется за 2.0 секунды дрифта вместо 6.0 секунд.
- Переход в состояние Overdrive (секунды 50–75): заполнение всех 3 баллонов активирует доступ к сдвоенному и тройному прожигу (Mega Nitro Boost с прибавкой +65 км/ч), позволяющему сократить отставание от лидера P1.
- Пик гоночной формы на финише (секунды 75–90): максимальная скорость болида достигает 245 км/ч в слипстриме, финальный затяжной дрифт приносит свыше 12 000 очков стиля, гарантируя золотой кубок заезда.

**Мета-прогрессия**:
- Ранги Репутации Синдиката (Tiers 1–5): за каждый заезд начисляется от 40 до 250 очков репутации. Tier 1 (Новичок улиц, 0-500 rep) -> Tier 2 (Ночной призрак, 500-1500 rep) -> Tier 3 (Теневой гонщик, 1500-3500 rep) -> Tier 4 (Мастер синдиката, 3500-7000 rep) -> Tier 5 (Легенда полуночи, 7000+ rep).
- Техническое древо тюнинга (4 ветки по 5 уровней прокачки за кредиты): 1. Двигатель (ECU & Блок) — рост макс. скорости (+10 км/ч за уровень, со 190 до 240 км/ч); 2. Турбонаддув (Твин-скролл) — сокращение турбоямы (+8% крутящего момента за уровень); 3. Спортивные шины и Подвеска (Drift Camber) — расширение угла заноса (с 40° до 65°) и термостойкости (+15°C); 4. Нитро-система — расширение емкости (с 1 до 3 баллонов) и силы импульса (+20% тяги).
- Разблокировка трасс и режимов: Tier 1: Downtown Loop (Кольцевая 1.8 км, широкие полосы); Tier 2: Neon Highway Sprint (Спринт 3.5 км точка-в-точку, трафик); Tier 3: Port Docks Drift Arena (Техничный трек, шпильки 180°, требование к очкам стиля >15 000); Tier 4-5: Дуэли «Pink Slip» 1-на-1 против боссов синдиката.
- Визуальный стайлинг и престиж: разблокировка неоновой подсветки днища (бирюзовый, пурпурный, кислотно-зеленый), кастомных винилов кузова и 6 оттенков цветного дыма из-под колес при заносе.

### Аркадный Срыв в Дрифт и Инерционная Перекладка (CORE)
- **Category**: Фундаментальный двигатель геймплея и первичный источник заряда нитро-системы
- **Description**: В отличие от бинарного дрифта в мобильных аркадах (нажал кнопку — машина едет по рельсам заноса), здесь реализована честная аркадная физика с переносом массы: угол и радиус полностью поддаются микроконтролю газом и контррулением.
- **Player Input**: Клавиатура: Вход в поворот A/D + кратковременный Пробел (ручник) на 0.22 с, стабилизация частыми нажатиями W (газ) и контррулением A/D. Тачскрин: виртуальный аналоговый стик руления влево/вправо + кнопка с иконкой ручника (Handbrake tap) справа.
- **Technical Complexity**: Medium
- **Решение игрока**: В какой миллисекундный момент перед апексом сорвать ось ручником, под каким углом удерживать занос газом и когда начать перекладку инерции в контр-поворот, чтобы не потерять скорость.
- **Состояния**: `GRIP_RUNNING: Прямолинейное движение или плавный поворот с полным поперечным зацепом шин (SlipAngle < 12°)`, `DRIFT_INITIATING: Кратковременный срыв задней оси при нажатии ручника, падение латерального трения на 65% (0.0-0.35 с)`, `DRIFT_SUSTAINED: Стабильный занос под углом 25°-60°, генерация очков стиля и заряда нитро`, `DRIFT_TRANSITION: Быстрая инерционная перекладка кузова в противоположную сторону (Scandi Flick) с сохранением комбо`, `SPINOUT_FAIL: Критический угол заноса (>68°) или падение скорости ниже 35 км/ч с потерей управления и сбросом комбо`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Время срабатывания срыва ручником` = `0.22 с` — При >0.4 с управление ощущается ватным и неотзывчивым; при <0.1 с машина срывается мгновенно, теряя вес кузова.
  - `Оптимальный рабочий угол заноса` = `45.0 градусов` — При снижении до 25° дрифт перестает выглядеть эффектно; при увеличении до 65° игроку невозможно парировать занос без разворота.
  - `Коэффициент падения латерального трения` = `0.35 (падение на 65%)` — При >0.55 машина не уходит в скольжение и мгновенно выравнивается; при <0.20 машина неуправляемо улетает с трека.
  - `Минимальная скорость входа в дрифт` = `45.0 км/ч` — При <20 км/ч занос инициируется на парковочных скоростях без динамики; при >80 км/ч невозможно зайти в медленные шпильки.
  - `Окно удержания комбо при перекладке` = `0.85 с` — При <0.4 с переложить машину между связками поворотов без сброса счета невозможно; при >1.5 с комбо обесценивается.
- **Слои отклика**:
  - Визуал/VFX: Густые объемные клубы бирюзового дыма из-под задних арок (particle emitter, 60 част/с), черные следы жженой резины на мокром асфальте (decal system), динамический крен кузова 6.5° на амортизаторах.
  - Звук (Web Audio): Пронзительный двухтональный визг покрышек (FM-синтез с модуляцией частоты 520 Гц -> 740 Гц в зависимости от угла заноса), басовитый рев прямоточного выхлопа с отстрелами антилага при сбросе газа (Noise burst 80 мс).
  - Камера (импульс/зум): Динамическое смещение точки обзора на внешнюю дугу поворота (Spring Arm lag 0.18 с), легкий голландский угол наклона камеры (Roll 4.5°) навстречу направлению заноса.
  - UI/Тактильность: Полукруглая неоновая дуга угла дрифта вокруг спидометра (зеленая зона 35°-55°, красная критическая зона >60°), тикающий счетчик очков с плавающими цифрами (+250 PTS), короткая виброотдача 45 мс на мобильных устройствах при срыве оси.
- **Режим отказа игрока**: Игрок передерживает руль без контрруления или зажимает ручник дольше 0.5 с -> угол заноса превышает критические 68° -> разворот на 360° (Spinout), падение скорости до 0 км/ч и сброс комбо очков.
- **Кривая мастерства**: Новичок на 30-й секунде дрифтует короткими рывками по 1 секунде с частыми разворотами и ударами об отбойники; эксперт на 10-м заезде связывает 5 поворотов подряд непрерывной перекладкой инерции, удерживая угол 52° в 20 сантиметрах от стен.
- **Сопротивление игры**: Трасса сужается в апексе, внешняя дуга покрыта лужами с падением сцепления на 30%, а соперники физически блокируют свободную траекторию заноса.
- **Синергии**:
  - Заряжает шкалу «Кинетический Нитро-Инжектор» со скоростью до 25% емкости за секунду глубокого заноса.
  - Активирует и разгоняет множитель «Лезвие бритвы» при скольжении вблизи отбойников.
  - Генерирует базовые очки стиля для выполнения дрифт-контрактов Синдиката.
- **Почему это не жанровый шаблон**: В отличие от бинарного дрифта в мобильных аркадах (нажал кнопку — машина едет по рельсам заноса), здесь реализована честная аркадная физика с переносом массы: угол и радиус полностью поддаются микроконтролю газом и контррулением.
- **Псевдокод тика**:
```text
function updateDriftMechanic(vehicle, input, dt) {
  const slipAngle = calculateSlipAngle(vehicle.velocity, vehicle.forwardVector);
  if (input.handbrake && vehicle.speedKmh > 45.0) {
    vehicle.lateralFriction = Math.max(0.35, vehicle.lateralFriction - dt * 2.5);
    vehicle.isDrifting = true;
  } else if (!vehicle.isDrifting && slipAngle > 18.5) {
    vehicle.isDrifting = true;
  }
  if (vehicle.isDrifting) {
    const driftAngleDeg = Math.abs(slipAngle * (180 / Math.PI));
    if (driftAngleDeg > 68.0) triggerSpinout(vehicle);
    else vehicle.driftScore += (vehicle.speedKmh * 0.5) * (driftAngleDeg / 45.0) * dt * 100;
    vehicle.nitroCharge = Math.min(100, vehicle.nitroCharge + (driftAngleDeg / 45.0) * 25.0 * dt);
    spawnDriftSmoke(vehicle.rearTires, driftAngleDeg);
  }
}
```

### Кинетический Нитро-Инжектор с Перегрузкой Турбины (SECONDARY)
- **Category**: Главный источник взрывного ускорения, инструмент тактического обгона и компенсации ошибок в заносе
- **Description**: Система разделена на 3 дискретных тактических заряда с синергией: сжигание одиночного заряда дает стабильный разгон, а удержание всех трех активирует скрытый режим «Overdrive», меняющий аэродинамику машины.
- **Player Input**: Клавиатура: Зажатие клавиши Shift или клавиши E. Тачскрин: Большая круглая кнопка «NITRO» в правом нижнем углу экрана с визуальным индикатором 3 секций заряда.
- **Technical Complexity**: Medium
- **Решение игрока**: Сжечь ли 1 баллон сразу на выходе из апекса для мгновенного набора скорости или накопить 3 полных баллона для активации сверхмощного режима «Overdrive Burn» на длинном финишном хайвее.
- **Состояния**: `NITRO_IDLE: Накопитель закиси азота пассивен, идет зарядка от дрифта и рискованной езды`, `NITRO_IGNITING: Момент детонации (0.0–0.15 с), резкий скачок крутящего момента +60%`, `NITRO_ACTIVE_BURNING: Постоянный форсаж, ускорение до лимита +45 км/ч, расширенный FOV до 85°`, `NITRO_OVERDRIVE: Активация при сжигании 3 баллонов подряд: супер-ускорение +65 км/ч и пробивание сопротивления воздуха`, `NITRO_EXHAUSTED: Опустошение баллонов, плавный возврат камеры и параметров тяги в норму (0.6 с)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Длительность форсажа одного баллона` = `2.20 с` — При >3.5 с нитро становится слишком дешевым и стирает необходимость дрифта; при <1.2 с игрок не успевает совершить тактический обгон.
  - `Прирост пиковой скорости при нитро` = `+45.0 км/ч` — При <25 км/ч форсаж визуально не впечатляет; при >70 км/ч машина на WebGL-трассах становится неуправляемым снарядом.
  - `Мгновенный импульс крутящего момента` = `+60.0% к Torque` — При <30% на выходе из поворота сохраняется турбояма; при >100% срывает переднюю ось даже на прямой.
  - `Максимальная емкость баллонов` = `3 заряда (100 ед/баллон)` — 1 заряд лишает выбора накопления; 5 зарядов перегружают интерфейс и делают нитро бесконечным.
  - `Угол расширения FOV камеры` = `с 60.0° до 85.0° (дельта +25°)` — При дельте <10° теряется кинематографическое ощущение гипер-скорости; при >35° картинка превращается в «рыбий глаз» с искажением дистанции.
- **Слои отклика**:
  - Визуал/VFX: Двухметровые бирюзовые языки пламени с искрами из сдвоенного выхлопа, радиальное размытие по краям экрана (Radial Blur pass strength 0.45), неоновые световые шлейфы за задними фонарями.
  - Звук (Web Audio): Низкочастотный глухой хлопок детонации (Sub-bass drop 65 Гц), переходящий в высокочастотный турбинный вой компрессора (Sweep 1.2 кГц -> 3.8 кГц) и шипение клапана сброса Wastegate.
  - Камера (импульс/зум): Резкий толчок камеры назад на 0.6 м с последующим плавным удержанием, высокочастотная вибрация (Micro-shake frequency 40 Гц, amplitude 0.25) для имитации перегрузки.
  - UI/Тактильность: Заполнение 3 сегментов неонового резервуара с бирюзовой пульсацией, цифры спидометра вспыхивают ярко-голубым светом, продолжительный плотный виброотклик на мобильных устройствах во время удержания.
- **Режим отказа игрока**: Активация нитро в середине заноса под критическим углом >50° или перед глухим поворотом -> спорткар теряет управляемость и впечатывается в бетонный отбойник на скорости 220 км/ч с потерей 80% скорости.
- **Кривая мастерства**: Новичок жжет нитро хаотично сразу после наполнения даже на входе в повороты; эксперт точно рассчитывает тайминг инициирования в апексе шпильки, используя нитро как гироскопический выпрямитель кузова с катапультированием на прямую.
- **Сопротивление игры**: Ограниченная длина прямых участков трассы, плотный трафик на узком хайвее и соперники, активно подрезающие траекторию ускорения.
- **Синергии**:
  - Питается напрямую от механик «Аркадный срыв в дрифт» и «Множитель Лезвие бритвы».
  - Позволяет мгновенно ворваться в зону «Контактного слипстрима» лидирующего соперника.
  - Нивелирует потерю скорости при легких скользящих контактах с отбойниками.
- **Почему это не жанровый шаблон**: Система разделена на 3 дискретных тактических заряда с синергией: сжигание одиночного заряда дает стабильный разгон, а удержание всех трех активирует скрытый режим «Overdrive», меняющий аэродинамику машины.
- **Псевдокод тика**:
```text
function updateNitroMechanic(vehicle, input, dt) {
  if (input.nitro && vehicle.nitroTanks > 0) {
    vehicle.isNitroActive = true;
    vehicle.nitroTankCharge -= 45.45 * dt; // Сгорание 1 баллона за 2.2 с
    vehicle.appliedTorque *= 1.60;
    vehicle.maxSpeedKmh = vehicle.baseMaxSpeed + 45.0;
    camera.targetFov = 85.0;
    emitNitroFlame(vehicle.exhaustPipes);
    if (vehicle.nitroTankCharge <= 0) {
      vehicle.nitroTanks--;
      vehicle.nitroTankCharge = vehicle.nitroTanks > 0 ? 100.0 : 0.0;
    }
  } else {
    vehicle.isNitroActive = false;
    camera.targetFov = 60.0;
  }
}
```

### Множитель Риска «Лезвие Бритвы» (Razor Edge Near-Miss) (SECONDARY)
- **Category**: Катализатор риска и награды, определяющий разрыв между безопасной ездой и рекордным результатом заезда
- **Description**: Механика непрерывно считывает лучом Raycast расстояние до препятствий в реальном времени, создавая плавную градацию множителя (от x1.0 до x4.0) вместо бинарного триггера «Near Miss».
- **Player Input**: Управление траекторией через руление (A/D / виртуальный стик) и ювелирные микро-коррекции газа (W / Тач-зона) во время прохождения поворотов и обгонов.
- **Technical Complexity**: Medium
- **Решение игрока**: Прижиматься ли бортом в заносе к бетонному ограждению на критические 20–40 см ради множителя x4.0 и мгновенной зарядки нитро или держать безопасный зазор в 1.5 метра с риском проиграть заезд по таймеру.
- **Состояния**: `SAFE_ZONE: Дистанция до препятствий и соперников > 1.2 м (множитель x1.0, стандартный темп зарядки)`, `PROXIMITY_ACTIVE: Дистанция от 0.35 м до 1.2 м (множитель x2.0–x3.0, искры от отбойника, ускоренная зарядка)`, `RAZOR_EDGE_CRITICAL: Экстремальная дистанция < 0.35 м (максимальный множитель x4.0, сверхбыстрая зарядка нитро +300%)`, `COLLISION_RESET: Физический удар о препятствие с перегрузкой > 3.5G (сброс комбо до x1.0, штраф к скорости -40%)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Пороговая дистанция активации риска` = `1.20 м` — При >2.0 м множитель срабатывает случайно даже на середине полосы; при <0.6 м окно риска становится слишком узким для 60 FPS браузера.
  - `Критическая дистанция максимума x4.0` = `0.35 м` — При <0.15 м погрешность физических коллайдеров Rapier вызывает ложные столкновения; при >0.6 м риск перестает вызывать адреналин.
  - `Максимальный комбо-множитель` = `x4.0` — При значении x2.0 стимул рисковать недостаточен; при x8.0 возникает дисбаланс, ломающий экономику очков за один поворот.
  - `Окно удержания множителя вне риска` = `1.80 с` — При <0.8 с множитель сгорает на прямых участках между поворотами; при >3.5 с множитель держится пассивно без риска.
  - `Штрафной порог силы удара коллизии` = `3.50 G (34.3 м/с²)` — При <1.5 G легкие касания отбойника спойлером сбрасывают комбо; при >6.0 G даже лобовой таран не наказывает игрока.
- **Слои отклика**:
  - Визуал/VFX: Плотный каскад ярких желто-оранжевых искр из зоны сближения борта с бетоном (Spark emitter, 80 част/с), пульсирующая неоновая рамка вокруг экрана (Vignette edge glow).
  - Звук (Web Audio): Нарастающий металлический скрежет и звон трения металла об абразив (Metal-on-concrete resonance 1.8 кГц), учащающийся пульсирующий синтезаторный бип комбо-множителя.
  - Камера (импульс/зум): Легкое приближение камеры к корме автомобиля (зум -0.4 м к фокусному расстоянию) с динамическим акцентом на сторону сближения.
  - UI/Тактильность: Всплывающий золотой бейдж «RAZOR EDGE x4.0» по центру экрана с прогресс-баром затухания, серия резких микро-вибраций (Haptic ticks 25 мс) при удержании критической зоны.
- **Режим отказа игрока**: Игрок смещает спорткар на 10 см ближе положенного -> происходит жесткий контакт кузова (Impulse > 3.5G) -> множитель мгновенно сбрасывается с x4.0 до x1.0 со звоном разбитого стекла, шкала очков гаснет.
- **Кривая мастерства**: Новичок держится строго по центру полосы, набирая скромные x1.0–x1.5; мастер синдиката облизывает каждый отбойник на всей длине трассы, удерживая постоянный x4.0 множитель и генерируя полный баллон нитро за 2 секунды.
- **Сопротивление игры**: Разрывы в отбойниках, выступающие бетонные опоры мостов, неожиданно маневрирующий городской трафик и попытки ИИ прижать игрока к стене.
- **Синергии**:
  - Ускоряет генерацию шкалы «Кинетический Нитро-Инжектор» до 4-кратного темпа.
  - Умножает базовые очки от механики «Аркадный Срыв в Дрифт».
  - Максимизирует кредитную награду за выполнение контрактов в мета-прогрессии Синдиката.
- **Почему это не жанровый шаблон**: Механика непрерывно считывает лучом Raycast расстояние до препятствий в реальном времени, создавая плавную градацию множителя (от x1.0 до x4.0) вместо бинарного триггера «Near Miss».
- **Псевдокод тика**:
```text
function updateRazorEdgeMechanic(vehicle, trackWalls, dt) {
  const minWallDist = raycastMinDistance(vehicle.bodyColliders, trackWalls);
  if (minWallDist < 1.20 && vehicle.speedKmh > 60.0) {
    const riskFactor = 1.0 - Math.min(1.0, Math.max(0.0, (minWallDist - 0.35) / 0.85));
    vehicle.riskMultiplier = 1.0 + riskFactor * 3.0; // от 1.0x до 4.0x
    vehicle.riskTimer = 1.80; // Сброс таймера затухания
    emitWallSparks(vehicle.sidePosition, riskFactor);
  } else {
    vehicle.riskTimer -= dt;
    if (vehicle.riskTimer <= 0) vehicle.riskMultiplier = Math.max(1.0, vehicle.riskMultiplier - dt * 2.0);
  }
  if (vehicle.lastImpactForceG > 3.50) {
    vehicle.riskMultiplier = 1.0;
    triggerComboBreakFeedback();
  }
}
```

### Адаптивный Дуэльный Прессинг и Аэродинамический Слипстрим (SECONDARY)
- **Category**: Драматический регулятор плотности заезда, исключающий одиночную езду и создающий контактную борьбу
- **Description**: Слипстрим совмещен с адаптивной системой дуэльного ИИ, создавая эффект «натянутой резинки»: дуэль всегда решается контактной борьбой на последних 100 метрах дистанции, исключая скучные заезды с минутным отрывом.
- **Player Input**: Позиционирование болида точно в створе кормы соперника на дистанции от 2.0 до 15.0 метров с последующим выходом на обгон резким отклонением стика/клавиш руления (A/D).
- **Technical Complexity**: Medium
- **Решение игрока**: Использовать ли воздушный карман впереди идущего соперника для набора скорости на прямой или сместиться в сторону для защиты от контратаки накатывающего сзади бота.
- **Состояния**: `CLEAN_AIR: Одиночное движение вне воздушных потоков соперников (стандартная аэродинамика)`, `DRAFTING_ACTIVE: Нахождение в аэродинамическом конусе соперника (снижение сопротивления воздуха на 40%, пассивный разгон +18%)`, `SLIPSTREAM_SLINGSHOT: Выход из кармана с накопленной кинетической энергией (бонус к разгону на 1.5 секунды)`, `RUBBERBAND_PRESS: Адаптивная корректировка ИИ: отстающие боты получают до +40% тяги, оторвавшийся лидер слегка сбавляет темп`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Длина зоны действия слипстрима` = `15.0 м` — При >25.0 м эффект работает через пол-трассы без необходимости точного пристраивания; при <6.0 м попасть в зону на высокой скорости почти невозможно.
  - `Снижение лобового сопротивления в кармане` = `-40.0% Drag` — При значении <-60% машина ускоряется как ракета без нитро; при >-15% эффект слипстрима слабо ощутим игроком.
  - `Максимальный гандикап адаптивной тяги ИИ` = `+40.0% / -25.0%` — При завышении (+80%) боты неестественно телепортируются за игроком; при занижении (+10%) заезд теряет накал при первом же отрыве.
  - `Ширина аэродинамического тоннеля` = `2.20 м` — При >4.0 м слипстрим ловится слишком легко; при <1.2 м малейшее смещение выбивает игрока из зоны потока.
  - `Время действия эффекта выстреливания (Slingshot)` = `1.50 с` — При <0.6 с бонус пропадает до момента завершения маневра обгона; при >3.0 с обгон становится тривиальным.
- **Слои отклика**:
  - Визуал/VFX: Неоновые полупрозрачные вихревые струи воздуха (Aerodynamic Streamlines), срывающиеся со спойлера соперника, белые линии разрежения по бокам экрана.
  - Звук (Web Audio): Приглушение общего гула ветра (Low-pass filter cutoff 800 Гц в зоне разрежения) с резким свистящим «вздохом» воздуха при выходе из слипстрима.
  - Камера (импульс/зум): Динамическое натяжение камеры (Camera pull-in на 0.3 м ближе к кузову) с легким эффектом кинематографической тряски при выходе на обгон.
  - UI/Тактильность: Индикатор наведения в виде неоновой мишени «DRAFTING» над машиной соперника, заполняющийся круговой прогресс-бар заряда выстреливания (Slingshot Ready).
- **Режим отказа игрока**: Игрок слишком долго сидит в слипстриме на скорости 220 км/ч и не успевает сманеврировать перед торможением соперника -> таран кормы, потеря всего темпа и разрушение аэродинамики.
- **Кривая мастерства**: Новичок объезжает соперников по широким траекториям без использования драфтинга; ветеран синдиката использует слипстрим как бесплатный нитро-разгон перед каждой связкой, экономя баллоны закиси азота.
- **Сопротивление игры**: ИИ-соперники отслеживают зеркала заднего вида и совершают защитные блокирующие маневры (Weaving block), пытаясь перекрыть воздушный тоннель.
- **Синергии**:
  - Компенсирует отсутствие нитро на длинных прямых участках трассы Neon Highway.
  - Сочетается с «Кинетическим Нитро-Инжектором» для совершения двойного гипер-обгона.
  - Позволяет быстро вернуться в лидирующую группу после ошибки или разворота в дрифте.
- **Почему это не жанровый шаблон**: Слипстрим совмещен с адаптивной системой дуэльного ИИ, создавая эффект «натянутой резинки»: дуэль всегда решается контактной борьбой на последних 100 метрах дистанции, исключая скучные заезды с минутным отрывом.
- **Псевдокод тика**:
```text
function updateSlipstreamAndAI(player, aiCars, dt) {
  for (let ai of aiCars) {
    const distToPlayer = player.position.distanceTo(ai.position);
    const inDraftZone = isInsideDraftingCone(player, ai, 15.0, 2.20);
    if (inDraftZone && player.speedKmh > 100.0) {
      player.aerodynamicDrag = player.baseDrag * 0.60; // -40% сопротивления
      player.draftTime += dt;
      emitDraftStreamlines(ai.rearPosition, player.frontPosition);
    }
    const gapDelta = ai.trackDistance - player.trackDistance;
    const rubberbandMult = Math.min(1.40, Math.max(0.75, 1.0 - gapDelta * 0.025));
    ai.engineTorque = ai.baseTorque * rubberbandMult;
  }
}
```

### Тепловой Менеджмент Сцепления Шин и Температурный Зацеп (SECONDARY)
- **Category**: Физический регулятор длительности непрерывного заноса, предотвращающий монотонное бесконечное скольжение
- **Description**: Вместо абстрактной полоски выносливости игра симулирует термодинамику трения, где управляемость динамически меняется в зависимости от агрессивности вождения.
- **Player Input**: Управление через прекращение зажатия ручника, выравнивание руля прямо и движение накатом для охлаждения набегающим потоком воздуха.
- **Technical Complexity**: Medium
- **Решение игрока**: Продолжать ли скольжение на перегретых шинах с риском потерять контроль в пользу набора очков или кратковременно стабилизировать болид на прямой для охлаждения резины до оптимальных 90°C.
- **Состояния**: `TIRE_COLD: Температура шин < 60°C (пониженный базовый зацеп на 15%, медленная инициация заноса)`, `TIRE_OPTIMAL: Температура шин 70°C–105°C (идеальный баланс зацепа и скольжения, максимальная управляемость)`, `TIRE_OVERHEAT: Температура шин > 120°C (падение латерального зацепа на 45%, машина «плывет», дым становится черным)`, `TIRE_COOLING: Прямолинейное движение без пробуксовки, активное охлаждение встречным воздухом (-12°C/сек)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Оптимальный диапазон температуры шин` = `70.0°C – 105.0°C` — При слишком узком диапазоне (80-90°C) игрок постоянно перегревает шины; при широком (40-150°C) механика теряет тактическую значимость.
  - `Скорость нагрева шин в заносе` = `+18.5°C в секунду` — При >30°C/с шины перегреваются за 2 секунды дрифта; при <8°C/с перегреть шины на трассе становится невозможно.
  - `Скорость охлаждения набегающим потоком` = `-12.0°C в секунду` — При <5°C/с остудить резину на коротких прямых невозможно; при >25°C/с температура мгновенно сбрасывается.
  - `Коэффициент падения зацепа при перегреве (>120°C)` = `-45.0% Lateral Grip` — При значении <-70% машина мгновенно улетает с трассы; при >-20% перегрев не создает ощутимой угрозы разворота.
  - `Критическая температура кипения компаунда` = `140.0°C` — Потолок шкалы, при котором шины полностью теряют сцепление с асфальтом.
- **Слои отклика**:
  - Визуал/VFX: Плавное изменение цвета дыма из-под арок от полупрозрачного бирюзового к густому едкому серо-черному с раскаленными красными частицами корда при перегреве >120°C.
  - Звук (Web Audio): Изменение тембра визга резины от чистого визжащего свиста (High resonant pitch) к хриплому рваному скрежету (Distorted low friction scrub).
  - Камера (импульс/зум): Нарастающая нестабильность траектории камеры, легкое рыскание (Yaw wobble) при потере сцепления задней оси.
  - UI/Тактильность: Термальный индикатор шин в углу спидометра (4 пиктограммы колес, меняющие цвет от синего к зеленому, затем к пульсирующему оранжево-красному), предупреждающий звуковой зуммер при 125°C.
- **Режим отказа игрока**: Игрок непрерывно дрифтует по кругу дольше 6 секунд -> температура шин достигает 135°C -> машина теряет поперечное сопротивление и центробежной силой выносится во внешнее ограждение.
- **Кривая мастерства**: Новичок игнорирует температуру и перегревает шины во втором повороте, теряя управление; мастер синдиката чередует глубокий дрифт с секундными прокатами по прямой, удерживая температуру ровно в зеленой зоне 90°C.
- **Сопротивление игры**: Длинные затяжные дуги поворотов на трассе Port Docks Drift вынуждают игрока балансировать между риском перегрева и удержанием комбо очков.
- **Синергии**:
  - Ограничивает бесконечное удержание механики «Аркадный Срыв в Дрифт».
  - Заставляет грамотно использовать «Кинетический Нитро-Инжектор» для быстрого прострела прямых и охлаждения шин.
  - Прокачка спортивных шин в мета-гараже увеличивает термостойкость компаунда на +15°C за уровень.
- **Почему это не жанровый шаблон**: Вместо абстрактной полоски выносливости игра симулирует термодинамику трения, где управляемость динамически меняется в зависимости от агрессивности вождения.
- **Псевдокод тика**:
```text
function updateTireThermals(vehicle, dt) {
  const isSlipping = vehicle.isDrifting || Math.abs(vehicle.wheelSlip) > 0.2;
  if (isSlipping) {
    const heatRate = 18.5 * (vehicle.speedKmh / 100.0) * (vehicle.driftAngle / 45.0);
    vehicle.tireTemp = Math.min(140.0, vehicle.tireTemp + heatRate * dt);
  } else {
    const coolRate = 12.0 * (vehicle.speedKmh / 120.0);
    vehicle.tireTemp = Math.max(25.0, vehicle.tireTemp - coolRate * dt);
  }
  if (vehicle.tireTemp > 120.0) {
    const overheatFactor = (vehicle.tireTemp - 120.0) / 20.0;
    vehicle.lateralGrip = vehicle.baseLateralGrip * (1.0 - overheatFactor * 0.45);
    setTireSmokeColor(vehicle, 'BURNT_BLACK');
  } else {
    vehicle.lateralGrip = vehicle.baseLateralGrip;
    setTireSmokeColor(vehicle, 'NEON_CYAN');
  }
}
```


---

## 3b. ⚠️ СТРОГИЙ ЗАПРЕТ ЖАНРОВЫХ ШАБЛОНОВ И КЛОНОВ (CRITICAL ANTI-CLICHÉ RULES)
Кодовый агент ОБЯЗАН реализовать уникальную игру, спроектированную в этом ТЗ, а не шаблонный автошутер:
1. **ЗАПРЕТ ШАБЛОННЫХ РОГАЛИКОВ И КАРТОЧЕК**: Если игра прямо не требует карточный драфт в GDD, **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** добавлять спам волн врагов и всплывающее окно «Выберите 1 из 3 карт апгрейда».
2. **ЗАПРЕТ СЕРЫХ ПРИМИТИВОВ НА ПУСТОЙ ПЛОСКОСТИ**: Создавай выразительную процедурную низкополигональную 3D/2D графику, точно соответствующую `ART_DIRECTION.md` (тематические персонажи, рельеф, модули, объекты окружения, частицы), а не бегающий куб на сером полу.
3. **ТОЧНОЕ СОБЛЮДЕНИЕ МЕХАНИК**: Реализуй все состояния, формулы, тайминги и слои отклика из `MECHANICS.md` (включая Web Audio звуки, импульсы камеры, визуал и тач-инпут).
4. **СПЕЦИФИЧЕСКОЕ УПРАВЛЕНИЕ**: Реализуй схему управления под конкретную механику этой игры (свайпы, жесты, траектории, физический дрифт, прицеливание), а не стандартный стик.

---

## 3c. ГОТОВОЕ: ЧТО УЖЕ НАПИСАНО И ОТЛАЖЕНО — SHOULD

Ниже — рабочий код фабрики: не примеры из статьи, а модули, которые крутились в
живых сценах, и в которых числа выставлены не наугад. Каждый файл объясняет в
своей шапке, почему константы именно такие — это та часть работы, которую с
нуля не воспроизвести за один заход.

**Правило.** Прежде чем писать механику с нуля, посмотри, нет ли её здесь.
Нашёл — тяни файл и подгоняй под свою игру. Не нашёл или не подходит — пиши
сам, но запиши в `DEVLOG.md` строку «взял/не взял <файл>, потому что …».
Молча проигнорировать готовое нельзя: это стоило нам рэгдолла, который писали
заново, хотя он лежал написанным.

Загрузка любого файла — одной командой, путь берётся из таблицы:

```bash
node scripts/fetch-knowledge.mjs workspace/knowledge-showcase/src/game/stealthSensing.ts
```

Файл появится в `docs/ref/<путь>`, оттуда копируй в `src/`.

Колонка «Готовность» — это часы работы, а не оттенок смысла:
- **копируется как есть** — ни одного импорта, чистая логика и числа. Взял, переименовал, поехал.
- **нужен three, больше ничего** — тянет только `three`, чужого проекта в нём нет.
- **образец, переписать под себя** — тянет модули стенда. Читать как показ того, как это собирается, а не копировать.

**Похоже, пригодится именно этой игре:**

| Файл | Что даёт | Строк | Готовность |
|---|---|---|---|
| `workspace/knowledge-showcase/src/game/survivorRun.ts` | Забег в survivor-игре: кривая опыта, карточки апгрейдов, эскалация орды | 279 | копируется как есть |
| `workspace/knowledge-showcase/src/game/towerDefense.ts` | Tower defense: контракт волн, приоритет целей, экономика | 225 | копируется как есть |
| `workspace/knowledge-showcase/src/game/arcadeCar.ts` | Аркадная модель машины — общая для игрока и соперников | 201 | копируется как есть |
| `workspace/knowledge-showcase/src/game/stealthSensing.ts` | Стелс: конус зрения, шкала подозрения, шум | 145 | копируется как есть |
| `workspace/knowledge-showcase/src/game/raceTrack.ts` | Трасса: одна кривая — один источник истины | 208 | нужен three, больше ничего |
| `workspace/knowledge-showcase/src/world/carRig.ts` | Процедурный low-poly болид. Никаких .gltf: вся геометрия — код | 72 | нужен three, больше ничего |
| `workspace/knowledge-showcase/src/world/shooterRig.ts` | Скиненный стрелок (Mixamo X Bot / Y Bot) с драйверами суставов | 306 | образец, переписать под себя |
| `workspace/knowledge-showcase/src/world/ragdoll.ts` | Рэгдолл на Rapier: семь тел, шесть сферических суставов | 214 | образец, переписать под себя |
| `workspace/knowledge-showcase/src/demos/TruckDemo.ts` | Эталонная вкладка стенда: ЗиЛ-130 на честной физике Rapier3D | 101 | образец, переписать под себя |
| `workspace/knowledge-showcase/src/stack/bvhSetup.ts` | Установка расширений three-mesh-bvh — ОДИН раз на приложение | 28 | образец, переписать под себя |

Это не весь каталог: всего готовых модулей 67. Полный список с описаниями — в `LIBRARY.md` рядом с этим файлом. Загляни туда, когда возьмёшься за механику, которой нет в таблице выше.

---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Domain / State Layer**: 
- **Physics Engine (Rapier3D)**: 
- **Rendering Engine (Three.js r170+)**: 
- **UI / Presentation Layer (HTML5/DOM & Canvas Overlay)**: 
- **Platform Bridge Layer**: 

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   └── PhysicsWorld.ts        # Rapier3D / Physics world manager & colliders
├── entities/
│   ├── Player.ts              # Player entity & input handling
│   └── EntityManager.ts       # Dynamic entity pool & lifecycle
├── systems/
│   ├── RapierRaycastVehicleSystem.ts # Обеспечение стабильного и отзывчивого аркадно
│   ├── 3TrackRaceModeSystem.ts  # Управление геометрией, чекпоинтами и логикой 
│   ├── RubberbandAiSystem.ts    # Поддержание постоянной соревновательной интри
│   ├── NitroDynamicsDriftScoringSystem.ts # Конвертация мастерского скольжения и рискован
│   ├── GarageProgressionSystem.ts # Управление накоплением валюты, покупкой улучш
├── rendering/
│   ├── SceneManager.ts        # Three.js scene graph, lights, perspective camera lerp
│   ├── ProceduralModels.ts    # Styled models / geometry for Ночной Синдикат: Дуэли и Контракты
│   └── ParticleSystem.ts      # Particle effects & visual feedback
├── ui/
│   ├── theme.css              # Единственное место со значениями: цвета, шрифты, шкала, слои
│   ├── UiRoot.ts              # Слои над канвасом, измеренный вьюпорт, safe-area + баннер
│   ├── ScreenRouter.ts        # Экраны как автомат: один видимый, один общий переход
│   ├── Hud.ts                 # Запись в закэшированные узлы только при изменении значения
│   ├── components/            # Button, IconButton, Panel, Modal, Toast, Meter, Stat, ListRow
│   ├── screens/               # По файлу на экран, каждый — со своими loading/empty/error
│   ├── icons.ts               # Один инлайновый SVG-спрайт, цвет через currentColor
│   └── TouchControls.ts       # Mobile touch input adapter
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic audio feedback
```

### Контракт модулей — MUST

Карта выше говорит, какие файлы завести. Ниже — чего им нельзя, потому что
именно на этом ломаются сгенерированные проекты:

- **DOM создаётся только в `src/ui/`.** Ни один файл вне `src/ui/components/` и
  `src/ui/screens/` не вызывает `document.createElement` и не присваивает
  `element.style.*`. Экран, собранный инлайновыми стилями в обход `theme.css`, —
  это второй экран, который перестанет совпадать с первым.
- **Значения — только в `src/ui/theme.css`.** Проверяется **B1**.
- **`src/core/` не знает про площадку.** Playgama Bridge живёт в
  `src/platform/`; всё остальное общается с ним через свой интерфейс. Иначе
  игру нельзя запустить локально без SDK.
- **`src/ui/` не знает про физику и рендер.** Интерфейс читает состояние
  через `EventBus` и не держит ссылок на `THREE.*` и на мир Rapier.
- **Числа игры — из `balance.yaml`.** Литерал скорости, урона, тайминга или
  радиуса в коде — баг: правка баланса не должна быть правкой кода.
- **Цикл обновления не аллоцирует.** Ни `new`, ни литерала объекта, ни
  `array.map` в кадре: пулы заводятся заранее. Проверяется **E4**.
- **Каждый модуль экспортирует один публичный класс или функцию** и не тянет
  импорт из соседнего слоя мимо своего интерфейса.

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Удвоение Награды за Заезд (`double_rewards`)**: Удвоение всех заработанных за заезд кредитов и очков репутации (x2) (Trigger: Экран победы/результатов заезда, Limit: Без лимита (1 раз за завершенный заезд))
- **Мгновенный Реванш (`instant_revive`)**: Мгновенный рестарт заезда без потери накопленного комбо-множителя очков (Trigger: Экран поражения при провале заезда, Limit: 1 раз за заезд)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"player_profile"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Landscape
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Визуальный контракт интерфейса
Интерфейс — первое, что видит игрок: до геймплея он смотрит на меню. Меню,
собранное из умолчаний браузера, обесценивает всё, что сделано за ним, и это
самый заметный признак недоделанной игры. Всё ниже — требование, а не пожелание.

**Материал интерфейса**: Темный неоновый Glassmorphism с диагональным срезом 12°, светящимися бирюзово-пурпурными акцентами и четкой контрастной типографикой

**Визуальный язык**: Агрессивная гоночная стилистика с наклоном панелей 12°, неоновыми градиентами Cyan-to-Magenta, темными полупрозрачными стеклянными подложками и четкой цветовой дифференциацией состояний.

**Типографика**: Chakra Petch / Russo One (рубленый технологичный шрифт с наклоном 12° для спидометра, таймера и заголовков), Inter (чистый контрастный шрифт для интерфейсных надписей)

**Сцена за меню.** Спорткар медленно вращается на мокром подиуме в темном подземном гараже под неоновыми лампами с реалистичными отражениями в лужах и парящими полупрозрачными панелями характеристик

Меню, пауза и итог сессии рисуются ПОВЕРХ этой сцены. Непрозрачный слой на весь экран (`background: #111` на корне экрана, полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка допускается только под текстовым блоком и кнопками. Игровой цикл в меню продолжает крутиться на сниженной нагрузке — камера едет, сцена живёт. Игрок обязан понять, во что он играет, до нажатия «Играть»; меню на глухой заливке — самый заметный признак недоделанной игры, и это первое, что увидит и игрок, и модератор площадки.

**Акценты — один цвет = один смысл** (разный цвет у каждой кнопки «чтобы отличались» читается как мусор):

| Токен | Единственный смысл |
|---|---|
| `background_overlay` | #0A0B10CC |
| `danger_collision` | #FF3366 |
| `drift_combo` | #FFB800 |
| `nitro_boost` | #00E5FF |
| `primary_action` | #00F0FF |

**Набор компонентов — закрытый.** Всё на экране обязано быть одним из них; одноразовый `<div>` с инлайновыми стилями — это баг, из-за которого второй экран перестаёт совпадать с первым:
- Speedometer & RPM Radial Gauge (неоновый спидометр с цифровой индикацией скорости и передачи)
- Nitro Battery Bar (3-секционная неоновая шкала заряда закиси азота)
- Drift Score & Multiplier Toast (динамический всплывающий счетчик очков с множителем x1..x4)
- Race Progress & Mini-Map Tracker (индикатор дистанции трассы с метками 4 машин)
- Touch Steering Pads & Action Buttons (сенсорные кнопки руля, тормоза/дрифта и нитро)
- Garage Upgrade Cards (карточки 4 узлов прокачки с ползунками прироста характеристик)

**Якоря HUD** — пять и только пять; посередине экрана не висит ничего, кроме временной обратной связи:

| Якорь | Что там |
|---|---|
| `bottom_center` | Кнопки тач-управления газом, тормозом/ручником и нитро |
| `bottom_left` | Сенсорные зоны руления (Steer Left / Steer Right) |
| `bottom_right` | Круговой неоновый спидометр, индикатор передачи и шкала 3 зарядов Нитро |
| `top_center` | Счетчик очков дрифта, комбо-множитель и статус Near Miss |
| `top_left` | Текущая позиция в гонке (1st..4th), номер круга (Lap 1/2) и таймер заезда |
| `top_right` | Мини-карта трассы с положением соперников и кнопка Паузы |

**Экраны и их композиция.** У каждого экрана три зоны: чем он себя называет, единственное главное действие (самое крупное и единственное с основным акцентом) и второстепенный ряд одним весом. Карточка с колонкой кнопок по центру — не композиция, а её отсутствие:
- **Garage & Main Menu** · Спорткар вращается на мокром подиуме; меню выбора трассы, прокачки узлов, стайлинга и запуска заезда · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **Track Selection Screen** · Выбор между 3 трассами (Downtown Loop, Neon Highway, Port Docks) с отображением рекордов и наград · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **Race HUD Overlay** · Игровой интерфейс заезда: спидометр, нитро-бар, мини-карта, позиция, дрифт-скор и сенсорные зоны · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **Pause Modal** · Затемнение экрана с кнопками 'Продолжить', 'Рестарт' и 'В Гараж', настройками звука · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **Race Results & Rewards Screen** · Итоги заезда: занятое место, время, очки дрифта, заработанные кредиты, кнопка 'x2 за рекламу' и 'В Гараж' · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

**Состояние, показанное миром, а не оверлеем**:
- Свет фар болида реально освещает дорожное полотно и мокрые лужи впереди
- Стоп-сигналы автомобиля загораются при торможении, подсвечивая асфальт красным светом
- Неоновая подсветка днища освещает дорогу под спорткаром выбранным цветом тюнинга
- Бирюзовые факелы пламени вырываются из выхлопных труб при переключении передач и нитро

**Переходы экранов**: Splash / Bridge Init -> Garage & Main Menu -> Track Selection (Downtown Loop / Highway Sprint / Port Drift) -> Race Countdown (3-2-1-GO!) -> Race Gameplay -> Race Results / Rewards -> Garage Upgrade / Next Race

**Отклик интерфейса на действие**:
- Срыв в занос: появление неонового дыма, визг шин, всплывающий золотой счетчик очков дрифта
- Включение нитро: вспышка бирюзового огня, резкое расширение FOV 65°->85°, радиальный блюр
- Near Miss обгон: всплывающая неоновая плашка '+150 NEAR MISS', звуковой свист рассекаемого воздуха
- Удар об отбойник: сноп искр, глухой металлический скрежет, вибрация экрана и сброс комбо
- Финиш: вспышка неонового салюта, победная фанфара, отображение времени и заработанных кредитов

**Состояния экранов** — загрузка, пустота и ошибка проектируются наравне с удачным путём: сохранение, таблица лидеров, покупки и реклама на этих площадках отваливаются регулярно:
- loading
- garage_idle
- track_selected
- race_countdown
- racing_normal
- drifting_active
- nitro_boost_active
- near_miss_triggered
- race_paused
- race_finished_win
- race_finished_loss
- reward_claimed

**Значения выводятся из этого мира, а не берутся из примеров.** Имена токенов (`--color-primary`, `--font-display`, `--space-*`) одинаковы во всех играх — на них держится код компонентов. Сами значения, обе гарнитуры, силуэт рамки и язык иконок выводятся из материала интерфейса, названного выше, по процедуре из раздела 12 `knowledge/ux/ui_design_system.md`: из чего сделана панель, при каком свете она лежит, чем в этом мире помечают главное и опасное, чем написаны цифры, как режут край. Скопировать палитру, шрифты или фаску из примера в базе знаний или из другой игры — это тот же дефект, что и оставить умолчания браузера: интерфейс выглядит решением, принятым не для этой игры. Обратная проверка: если от интерфейса другой игры фабрики ваш отличается только оттенком акцента — процедура была пропущена.

**Обязательные технические правила интерфейса** (каждое — из починенного бага, подробности в `knowledge/ux/ui_design_system.md` и `knowledge/ux/ui_implementation.md`):
- Все значения — токенами в одном `src/ui/theme.css`: цвета, две гарнитуры, шкала отступов, радиусы, длительности, `--z-*`. Литерал `#RRGGBB` или `z-index: 9999` внутри экрана запрещён; `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` мимо темы обязан быть пуст.
- Слои над канвасом прозрачны для ввода: контейнер — `pointer-events: none`, `auto` включают только листовые интерактивные элементы. Слой HUD не кликается никогда. Полноэкранный оверлей, оставленный на `auto`, съедает игровой ввод — и это выглядит как «на телефоне не работает управление».
- Интерфейс тянется одним множителем `--ui-scale`, а не отдельной вёрсткой под каждый телефон, — но множитель обязан где-то **читаться**. Он объявляется в `theme.css` брейкпоинтами (`:root { --ui-scale: 1 }`, `@media (max-width: 720px)` → `0.86`, `@media (max-width: 420px)` → `0.74`) и умножает отступы и кегли: `padding: calc(var(--space-4) * var(--ui-scale))`, `font-size: clamp(14px, calc(16px * var(--ui-scale)), 18px)`. Посчитать масштаб в JS и не использовать его ни в одном правиле — ровно тот же результат, что не считать вовсе: интерфейс, свёрстанный в фиксированных `px`, на 360-пиксельном экране разъезжается. Ноль `@media` в `theme.css` — признак, что этот пункт пропущен.
- Портрет и ландшафт — два разных экрана, а не один и тот же в другом масштабе. Вертикальный FOV камеры пересчитывается под аспект (иначе в портрете горизонтальный обзор схлопывается в трубу), а раскладка HUD и кнопок проверяется в обеих ориентациях.
- Размеры зон нажатия НЕ масштабируются: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px — палец не уменьшается вместе с экраном. Ниже 460 px по высоте кнопки допускается ужать до 56 px, и это единственное исключение.
- Меняющиеся числа — `font-variant-numeric: tabular-nums` в слоте фиксированной ширины; полосы — `transform: scaleX()`, не `width`. HUD пишет в закэшированные узлы и только при изменении значения: пересборка разметки в кадре стоит кадров.
- Один экран виден за раз, скрытый — `display: none`; переход один на всю игру и укладывается в 300 мс. Анимируются только `transform` и `opacity`. `prefers-reduced-motion: reduce` убирает трансформации.
- Любое нажатие отвечает в том же кадре; действие с ожиданием переводит кнопку в `loading` и снимает его в `finally`.
- Запрещено: `alert`/`confirm`/`prompt`, эмодзи вместо иконок (один инлайновый SVG-спрайт с `currentColor`), голый `<input type=range>` и `<select>`, текст над геймплеем без подложки или обводки, серая неактивная кнопка вместо отсутствующей на площадке возможности — такой элемент не рисуется вовсе.
- Геометрия меню считается от ИЗМЕРЕННОГО вьюпорта (`visualViewport`, пересчёт после поворота, выхода из фуллскрина и закрытия рекламы), а не от `100vh`; нижний отступ включает измеренную высоту липкого баннера. Страница под игрой не скроллится — длинные списки скроллятся во внутреннем контейнере.
- `backdrop-filter: blur()` на полноэкранном слое — самое дорогое свойство оверлея на мобильных GPU; только на маленьких панелях.

### Контракт тач-управления
- **Раскладка этой игры** (спроектирована в MOBILE_CONTROLS.md, переносить сюда раскладку из другой игры нельзя): Левая половина экрана: сенсорные зоны Steer Left / Steer Right; Правая половина: кнопка газа Throttle (удержание), кнопка ручника Drift / Handbrake (тап), светящаяся кнопка Nitro в правом верхнем углу зоны.
- Раскладка выводится из главного действия игрока. Виртуальный джойстик уместен там, где игрок непрерывно ведёт персонажа; там, где действие точечное (тап по цели, свайп, удержание), джойстик — лишний элемент, который занимает половину экрана и ничего не делает.

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Если в раскладке есть стик** — он плавающий: зона захвата — вся левая
  половина экрана, база появляется под пальцем, мёртвая зона 8%, иначе
  управление дрожит. Если раскладка обходится без стика — не добавляйте его.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `W` / `S` / `↑` / `↓` — газ и тормоз/задний ход
- `A` / `D` / `←` / `→` — руль
- `Space` / `E` — нитро
- `Shift` / `F` — ручной тормоз (дрифт)
- `P` / `Esc` — пауза

**Раскладка выше — отправная точка под похожие игры, а не список того, что умеет ЭТА игра.** Профилей в фабрике меньше, чем жанров, и любой шаблон заведомо описывает не вашу игру: в нём есть лишние клавиши и нет нужных. Источник истины — глаголы игрока, перечисленные ниже.

**Контракт: каждый глагол получает и клавишу, и кнопку.** Таблицу «глагол → клавиша → тач-кнопка → что происходит на экране и в звуке» положи в `DESIGN.md` до первой строчки кода по управлению. Глагол без строки в таблице не реализован; клавиша из шаблона, которой в таблице нет, — удаляется.

**Отдельно решается то, чего в списке механик обычно нет, но игрок ждёт по виду уровня.** Вертикаль (прыжок, присед, тихий шаг), взаимодействие с предметом, отмена действия. Каждое — либо сделано (вертикаль — это гравитация, опора лучом по геометрии и высота глаз от ступней, а не константа пола), либо осознанно не сделано, и тогда в `DESIGN.md` стоит строка почему, **а уровень не обещает того, чего игрок не может**: без прыжка на карте нет ящиков, выступов и укрытий по пояс, к которым тянет забраться.

**Глаголы игрока этой игры** (каждому нужна строка в таблице управления):
- **Аркадный Срыв в Дрифт и Инерционная Перекладка** — игрок делает это так: Клавиатура: Вход в поворот A/D + кратковременный Пробел (ручник) на 0.22 с, стабилизация частыми нажатиями W (газ) и контррулением A/D. Тачскрин: виртуальный аналоговый стик руления влево/вправо + кнопка с иконкой ручника (Handbrake tap) справа.
- **Кинетический Нитро-Инжектор с Перегрузкой Турбины** — игрок делает это так: Клавиатура: Зажатие клавиши Shift или клавиши E. Тачскрин: Большая круглая кнопка «NITRO» в правом нижнем углу экрана с визуальным индикатором 3 секций заряда.
- **Множитель Риска «Лезвие Бритвы» (Razor Edge Near-Miss)** — игрок делает это так: Управление траекторией через руление (A/D / виртуальный стик) и ювелирные микро-коррекции газа (W / Тач-зона) во время прохождения поворотов и обгонов.
- **Адаптивный Дуэльный Прессинг и Аэродинамический Слипстрим** — игрок делает это так: Позиционирование болида точно в створе кормы соперника на дистанции от 2.0 до 15.0 метров с последующим выходом на обгон резким отклонением стика/клавиш руления (A/D).
- **Тепловой Менеджмент Сцепления Шин и Температурный Зацеп** — игрок делает это так: Управление через прекращение зажатия ручника, выравнивание руля прямо и движение накатом для охлаждения набегающим потоком воздуха.

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/sozday_gonku_v_stile_nfs_pust_budet_3_karty_kolt/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ДИЗАЙН: ЕГО ПИШЕШЬ ТЫ — MUST

Готового арт-дирекшена в этом пакете нет намеренно. Дизайн, продиктованный
списком полей, получается одинаковым от игры к игре, и узнать в нём конкретную
игру невозможно. Поэтому первым делом, до кода, ты пишешь `DESIGN.md` сам.

**Что в нём должно быть** (без этих пяти пунктов файл не считается написанным):

1. **Одна фраза о том, как игра выглядит.** Не «мрачный киберпанк», а что
   именно видит игрок в первую секунду.
2. **Палитра** — 5–7 цветов с ролями (фон, поверхность, текст, акцент, опасность,
   успех) и HEX. Они же становятся токенами в `theme.css`.
3. **Камера и свет** — где стоит камера, что она захватывает, откуда светит и
   почему сцена читается на телефоне.
4. **Экраны** — для каждого: что на нём, где три зоны (заголовок, содержимое,
   действие), какое действие главное.
5. **Сцена за меню** — что живёт на фоне главного меню. Не заливка, не блюр
   поверх ничего: работающая сцена игры под пониженной нагрузкой.

**Чего в нём быть не должно** — это уровень **MUST**, проверяется приёмкой:
эмодзи вместо иконок, `alert()` вместо модалки, непрозрачная плашка поверх
канваса, фиолетово-синий градиент, интерфейс из умолчаний браузера.

**Отправная точка от фабрики** — это подсказка, а не приговор. Не подошло —
меняй и запиши в `DEVLOG.md`, что и почему:

- Стиль: Cyber-Neon Stylized Low-Poly / Wet Asphalt Realism
- Камера: Third-Person Dynamic Chase Camera (шейк при ускорении, наклон при дрифте, расширение FOV до 85° при нитро) (FOV 65°, наклон 15°)
- Среда: Ночной залитый дождем мегаполис: отражающие неоновые вывески, мокрый асфальт, подвесные мосты, эстакады и портовые краны
- Свет: Контрастное ночное освещение: направленный лунный свет (DirectionalLight, intensity 0.4), точечные цветные неоны (PointLights), прожекторы фар (SpotLights с объемным конусом) и световые отражения на мокром асфальте
- Тема интерфейса: Темный неоновый Glassmorphism с диагональным срезом 12°, светящимися бирюзово-пурпурными акцентами и четкой контрастной типографикой (реализация — секция 6)
- Сцена за меню: Спорткар медленно вращается на мокром подиуме в темном подземном гараже под неоновыми лампами с реалистичными отражениями в лужах и парящими полупрозрачными панелями характеристик

Подробности отбора — в `ART_DIRECTION.md` и `UI_UX_SPECIFICATION.md` рядом.

---

## 8. РОАДМАП

Фаза закончена не тогда, когда код написан, а когда закрыты её проверки из
`ACCEPTANCE.md`. Прогоняй `node scripts/check-spec.mjs` в конце каждой фазы,
а `node scripts/smoke.mjs` — начиная с фазы «Каркас и площадка»: с этого момента
игра обязана открываться, и каждая следующая фаза обязана оставлять её открывающейся.

| Фаза | Чем закрывается |
|---|---|
| Подготовка | **F4**, **F1** — база загружена в `docs/ref/`, `DESIGN.md` написан |
| Каркас и площадка | **A1**, **A3**, **A5**, **S1–S4** — игра собирается, открывается и рисует кадр |
| Главная механика | **D1**, **B6** — в неё можно играть, ввод доходит до игры |
| Остальные механики и петля | **D2–D5**, **B9–B11** — петля проходится целиком |
| Интерфейс и подача | **B1–B5**, **B7–B8**, **B12**, **S6** — интерфейс собран из токенов, меню стоит на живой сцене, на телефоне ничего не разъехалось |
| Полировка и релиз | **C5–C11**, **E1–E5**, **A2**, **A4**, **S5**, **F2–F3** — сохранения, реклама, кадры, сборка |

### Phase 1: Фаза 1: Физическое ядро, контроллер машины и камера (2 days)
- **Deliverable**: Прототип управляемого автомобиля в заносе с чейз-камерой на тестовой площадке
  - Интеграция Rapier3D и Three.js r170+
  - Реализация RAPIER.DynamicRayCastVehicleController с дифференцированным сцеплением колес
  - Настройка динамической чейз-камеры с креном и расширением FOV
  - Базовая схема ввода для клавиатуры (WASD/Space/Shift) и тач-скрина
### Phase 2: Фаза 2: Архитектура 3 трасс и адаптивный ИИ соперников (3 days)
- **Deliverable**: Все 3 играбельные трассы с чекпоинтами, сплайнами ИИ и контактными соперниками
  - Моделирование и сборка 3 трасс: Downtown Loop, Neon Highway, Port Docks Drift
  - Создание системы чекпоинтов, хронометража кругов и зон респавна
  - Разработка адаптивного ИИ соперников с поддержкой слипстрима и раббербендинга
  - Оптимизация геометрии трассы через Three.js InstancedMesh
### Phase 3: Фаза 3: Нитро, дрифт-скоринг, VFX и аудио-движок (2 days)
- **Deliverable**: Полноценный аркадный геймплейный цикл с нитро-бустом, дымом, неоном и звуком
  - Реализация механики накопления нитро в дрифте и впрыска с эффектом FOV 85°
  - Создание системы дрифт-скоринга с множителем x1..x4 и Near-Miss бонусами
  - Разработка кастомного GLSL-шейдера мокрого асфальта с отражениями
  - Интеграция партиклов дыма, пламени выхлопа и Web Audio эффектов (Howler.js)
### Phase 4: Фаза 4: Гараж, мета-прогрессия, Playgama SDK и релизная полировка (2 days)
- **Deliverable**: Релизный билд игры с интеграцией Playgama Bridge, готовый к публикации на площадках
  - Создание UI Гаража: покупка 4 веток апгрейдов и неоновой кастомизации
  - Интеграция Playgama Bridge 2.x: сохранение в Cloud, Interstitial/Rewarded реклама, лидерборды
  - Полировка мобильного touch-управления, SafeArea и адаптация верстки HUD
  - Финальное профилирование FPS, балансировка цен апгрейдов и QA-тестирование

---

## 9. ПРАВИЛА ПЛОЩАДКИ — УРОВЕНЬ MUST
Каждое правило ниже — след бага, дошедшего до продакшена, или отказа модерации.
Они перекрывают любую привычку, туториал и пример — включая куски из документации
Playgama и Яндекса, многие из которых описывают устаревший контракт Bridge v1.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

### Orientation & pointer lock

69. `Object3D.lookAt` aims the object's **+Z** at the target; −Z is the convention
    for cameras and lights only. Geometry that will later be aimed with `lookAt`
    (viewmodel arms, tracers, arrows, turret beams) must be built along **+Z**.
    Built along −Z it points backwards after aiming, and the bug reads as "broken
    model" rather than a flipped axis.

70. `requestPointerLock()` is called **from the pointer-down handler**, never from
    the frame loop: after an Esc exit a request issued inside
    `requestAnimationFrame` is silently rejected and the game looks dead. While
    `document.pointerLockElement` is set, application hotkeys (level switch, menu,
    tabs) must stay silent — otherwise an in-game key throws the player out of the
    game.

71. A `MeshStandardMaterial` with high `metalness` and **no environment map
    renders black** — metal has nothing to reflect. Set `scene.environment`
    (a `RoomEnvironment` PMREM costs no file and no request) **and** keep
    `metalness ≤ 0.4` in the material: the environment is the first thing a
    low tier drops, and the asset must not turn into a hole in the frame
    when it does.

72. A node whose base orientation is not zero — anything aimed with
    `lookAt`, `setFromUnitVectors` or a baked rest pose — is animated
    through a **wrapper**, never by writing its own `rotation`. Code that
    returns a limb to rest with `rotation.x = lerp(rotation.x, 0, k)` reads
    the Euler of that very quaternion and erases the aim: the part turns
    away from what it was holding, and the bug reads as "the model is
    missing a piece".

73. Anything positioned from **world** coordinates — a prop placed by two
    hand positions, an effect anchored to a bone, a decal following a
    moving platform — must be converted into the parent's local space
    before it is written into `position`/`quaternion`. Writing world
    numbers into a child of a transformed node applies that transform a
    second time, and the object drifts away by exactly the owner's offset
    from the origin. It stays invisible while the owner sits at the origin
    (which is where every unit test and every bake script puts it), and
    appears only in the game, as "the weapon is flying around the level".

74. Where the character **looks** and where the weapon **points** are two
    different directions, and gameplay owns the second one. A mocap firing
    stance is bladed: the axis through the hands is 40–60° away from the
    model's forward, so turning the body at the target aims the barrel past
    it. Measure the offset once on the assembled rig, turn the body by
    `heading − offset`, and take up the remainder by rotating the chest —
    with an exact quaternion `from barrel to target`, not by tweaking a
    single Euler axis: a barrel that sits at an angle to the rotation plane
    turns by only `angle · cos(that angle)`.

### Interface

75. UI layers over the canvas are **transparent to input by default**: every
    layer container is `pointer-events: none` and only leaf interactive elements
    turn it back on. A full-screen overlay left at `auto` swallows every gameplay
    pointer, and the bug reads as "the car does not steer on mobile". The HUD
    layer is read-only and never becomes interactive at all — pause and settings
    are buttons on the screen layer.

76. Every colour, font, radius, spacing step and duration comes from tokens in
    one theme file. A literal `#RRGGBB`, `px` padding or `z-index: 9999` written
    inside a screen is the reason the second screen stops matching the first.
    `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` minus the theme file returns nothing.
    Имена токенов при этом одинаковы во всех играх, а **значения — нет**: палитра,
    обе гарнитуры и силуэт рамки выводятся из материала мира этой игры по
    процедуре в `knowledge/ux/ui_design_system.md`, раздел 12. Скопированный из
    примера или из соседней игры набор значений — такой же дефект, как умолчания
    браузера, только заметнее: он выглядит осознанным решением, принятым не для
    этой игры.

77. Browser dialogs (`alert`, `confirm`, `prompt`) and emoji used as icons are
    **banned in shipped UI**. The first blocks the game loop, cannot be styled and
    breaks immersion; the second renders differently on every OS and reads as a
    placeholder. Use the project's `Modal` component and one inline SVG sprite
    coloured by `currentColor`.

78. The HUD updates by writing to **cached nodes** and only when the value
    changed — never by rebuilding markup inside the frame loop, and never by
    reading layout (`offsetWidth`, `getBoundingClientRect`) there. Changing
    numbers carry `font-variant-numeric: tabular-nums` in a fixed-width slot, and
    bars animate with `transform: scaleX()`, not `width`; otherwise the row
    reflows on every tick and the whole overlay repaints.

79. Every screen defines its **loading, empty and error** states, not only the
    happy path. Saves, leaderboards, purchases and ads are network calls that fail
    routinely on these portals; a failure that renders an empty frame or says
    nothing costs the player their run and reads as a broken game.

80. Menu and HUD geometry is sized against the **measured** viewport
    (`visualViewport`, re-measured after orientation change, fullscreen exit and
    ad close), never `100vh`, and every UI layer is inset by
    `env(safe-area-inset-*)` **plus** the measured sticky-banner height. The page
    itself never scrolls — long content scrolls in an inner container.
    See `knowledge/ux/ui_design_system.md` and `knowledge/ux/ui_implementation.md`.

81. Меню, пауза и экран итога сессии рисуются **поверх живой игровой сцены**, а
    не поверх заливки. Тот же рендерер, та же сцена, медленная камера, работающий
    свет; игровой цикл в меню продолжает крутиться на сниженной нагрузке.
    Непрозрачный слой на весь экран (`background: #111` на корне экрана,
    полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка
    допускается только под текстовым блоком и кнопками. Меню — это первое и
    иногда единственное, что видит игрок: за глухой плашкой с колонкой кнопок
    вся работа над геймплеем невидима, и игра читается как недоделанная ещё до
    запуска. Постановку сцены задаёт `ART_DIRECTION.md` (раздел «Сцена за меню»).

82. У каждого экрана **три зоны**: чем экран себя называет, единственное главное
    действие (самое крупное и единственное с основным акцентом) и второстепенный
    ряд одним весом. Одна карточка с колонкой кнопок по центру — не композиция, а
    её отсутствие; композиция каждого экрана задана в `UI_UX_SPECIFICATION.md`,
    раздел «Каталог экранов», и придумывать её заново не нужно.

### Разделы, которые открываются по месту
Эти правила нужны не с первой строки, а когда доберёшься до соответствующей области. Полный текст — в `docs/ref/knowledge/CRITICAL_RULES.md` (файл обязательный, приезжает шагом 0). Обязательность от переноса не меняется.
- **Stack** — выбор библиотек стека — перед подключением любой из них
- **Renderer** — настройки рендерера — когда собираешь сцену
- **Physics vehicles** — физика транспорта — только если в игре есть машины

---

## 10. ПРИЁМКА — MUST

Полный список проверок с номерами лежит в `ACCEPTANCE.md`, рядом с этим файлом.
Он и есть определение готовности: пока хотя бы один пункт разделов **A–C**
красный, игра не готова — это не качество, это работоспособность.

Проверяется двумя скриптами. Оба уже лежат в проекте и зависимостей не требуют —
пропиши их в `package.json` как `check:spec` и `smoke`:

```bash
node scripts/check-spec.mjs   # чтение исходников: недописанное, мёртвое, несогласованное
node scripts/smoke.mjs        # сборка, запуск в браузере, кадры, ввод, телефон
```

Второй важнее первого. `check-spec` умеет только читать код и потому не видит
самого главного: собирается ли проект, открывается ли игра, попадает ли
что-нибудь в кадр, переживает ли она нажатие. Уже случался пакет, зелёный по
всей статике и не запускающийся вовсе. Писать код, подогнанный под проверки, —
не то же самое, что писать работающую игру: проверки описывают минимум, а не
цель.

Прогонять после каждой фазы роадмапа, а не один раз в конце. Результат
записывать строкой в `DEVLOG.md`. Пункт, который не проходит, честнее оставить
красным с объяснением, чем отметить зелёным: следующий, кто откроет проект,
будет считать его проверенным.

Критерии, которые относятся именно к этой игре:

- [ ] Все 3 трассы (Downtown Loop, Neon Highway, Port Docks Drift) полностью реализованы, оптимизированы и проходимы от старта до финиша
- [ ] Контроллер автомобиля на RAPIER.DynamicRayCastVehicleController обеспечивает стабильную физику, управляемый дрифт ручником и четкий отклик на управление
- [ ] Реализовано раздельное сенсорное управление для мобильных устройств (лево/право, газ/тормоз/дрифт, нитро) и клавиатурная раскладка WASD/Space/Shift
- [ ] Механика дрифта корректно аккумулирует закись азота, а прожиг нитро сопровождается расширением FOV (65°->85°) и эффектами пламени/блюра
- [ ] 3 машины соперников под управлением адаптивного ИИ держат плотную контактную борьбу, огибают препятствия и ведут честный спринт на финише
- [ ] Система гаража позволяет улучшать 4 технических узла автомобиля и настраивать неоновую кастомизацию с сохранением в Cloud Storage
- [ ] Интегрирован Playgama Bridge 2.x: сохранение прогресса, показ Interstitial/Rewarded рекламы и 3 глобальные таблицы лидеров
- [ ] Обеспечены стабильные 60 FPS на средних мобильных устройствах и в браузерах при строгом лимите Draw Calls < 75
- [ ] Игра осталась тем, что просили: игрок ведёт машину: руль, газ, трасса.
- [ ] Project builds cleanly with zero TypeScript errors (`npm run build`).
- [ ] Playgama Bridge initializes and dispatches GAME_READY platform message.
- [ ] Петля играбельна целиком: Выбор контракта и трассы -> Заезд на 60–90 сек (дрифт заряжает нитро -> мощный рывок на прямой -> контактный обгон соперников) -> Финиш в топ-3 -> Награда (кредиты + репутация) -> Тюнинг авто в гараже -> Доступ к новым трассам и сложным дуэлям.
- [ ] Реализованы механики проекта: Аркадный Срыв в Дрифт и Инерционная Перекладка, Кинетический Нитро-Инжектор с Перегрузкой Турбины, Множитель Риска «Лезвие Бритвы» (Razor Edge Near-Miss), Адаптивный Дуэльный Прессинг и Аэродинамический Слипстрим.
- [ ] Условие успеха (Занять 1-е место в кольцевой гонке (Downtown Loop, 2 круга по 1.8 км быстрее 1:25.00), прийти первым в шоссейном спринте (Neon Highway, 3.5 км быстрее 1:15.00) или набрать более 15 000 очков стиля в дрифт-режиме (Port Docks Drift, 3 круга).) и условие проигрыша (Прийти к финишу ниже 3-го места в спринте или кольцевой гонке, не успеть доехать до контрольного чекпоинта до истечения таймера (0.00 сек) или набрать менее 8 000 очков дрифта на закрытом треке.) работают.
- [ ] Тач-управление отвечает без задержки, страница под игрой не скроллится.
- [ ] Интерфейс собран из токенов одного `src/ui/theme.css`: в экранах нет литералов цвета, шрифта и `z-index`.
- [ ] Слои интерфейса прозрачны для игрового ввода — управление работает под всеми оверлеями, слой HUD не кликается.
- [ ] У каждого экрана работают состояния загрузки, пустоты и ошибки; возможность, которой нет на площадке, не нарисована вовсе.
- [ ] Меню и HUD помещаются в измеренный вьюпорт с учётом safe-area и баннера, а с закрытым игровым полем меню узнаётся как «Ночной Синдикат: Дуэли и Контракты».
- [ ] За меню и экраном итога видна живая игровая сцена: полноэкранной непрозрачной заливки поверх канваса нет, подложка только под текстом и кнопками.
- [ ] Ни одного эмодзи в подписях интерфейса: иконки — инлайновый SVG с `currentColor`.
- [ ] Rewarded-реклама выдаёт награду ровно один раз (Удвоение Награды за Заезд, Мгновенный Реванш); interstitial соблюдает паузу 90 с.
- [ ] Audio and physics automatically pause/resume on tab blur and ad display.
- [ ] Persistent progress saves and loads from Playgama Cloud Storage.
- [ ] Runs at steady 60 FPS on desktop and >= 50 FPS on mobile.

---

## 11. ГДЕ ЛЕЖИТ ОСТАЛЬНОЕ

База знаний не лежит в пакете копиями — она приезжает по требованию:

```bash
node scripts/fetch-knowledge.mjs        # всё обязательное → docs/ref/
node scripts/fetch-knowledge.mjs <путь> # отдельный файл
```

Что именно тянуть, перечислено в `knowledge.manifest.json`. После загрузки адреса ниже открываются как обычные локальные файлы, и сеть больше не нужна.

Набор отобран под этот проект: Проект опирается на связку Three.js и физического движка Rapier3D с raycast-контроллером автомобиля, процедурной low-poly генерацией ночного мегаполиса, Web Audio синтезом и оптимизированными postprocessing-эффектами дрифта и нитро.

**Строка после адреса — ярлык документа, а не его содержание.** Она названа так, чтобы документ можно было отличить от соседнего, и покрывает от силы десятую часть файла: за ярлыком «управление от первого лица и покачивание вьюмодели» в прошлый раз лежали гравитация, прыжок, вторая рука на вьюмодели, анимации перезарядки, пружина отдачи и хитмаркер — и ничего из этого в игру не попало, потому что файл сочли уже понятным. Документы ядра открываются целиком, до первой строчки кода по своей теме.

Пункты `- [ ]` под адресом — чек-лист самого документа, дословно. Это не пожелания: каждый пункт закрывает уже сделанную кем-то ошибку и проверяется взглядом на запущенную игру.

Здесь стоят чек-листы только тех документов, что отобраны под эту игру. **Полный список, включая платформенные документы, лежит в `ACCEPTANCE.md`, раздел H** — там же он и закрывается: у каждого пункта либо `[x]`, либо `[~]` со строкой причины. Незакрытый пункт валит `node scripts/check-spec.mjs`, и проект не считается готовым.

**Ядро — прочитать до начала реализации:**
- `docs/ref/knowledge/patterns/racing_event_loop.md` (выжимка — `skills/rapier-vehicle-physics.md`) — Определяет базовый цикл игры: выбор контракта и трассы, заезд на 60–90 секунд, получение кредитов и репутации за топ-3 и тюнинг болида в гараже.
  - [ ] Соперники едут через тот же контроллер, что и игрок, а не по рельсам
  - [ ] Подтягивание соперников честное: они не телепортируются и не тормозят у финиша
  - [ ] Смена покрытия наказывает лишнюю скорость, и это видно по поведению машины
  - [ ] Контакт со стеной имеет последствия внутри гонки, но не требует рестарта
  - [ ] Позиция, круги и разбивка по секторам показаны относительно личного рекорда
  - [ ] Нитро зарабатывается ездой, а не ожиданием
  - [ ] Апгрейды в гараже ощущаются на трассе
  - [ ] Есть повод на второй заезд: свой призрак или круг без касаний
- `docs/ref/knowledge/threejs/arcade_racing_and_drift.md` (выжимка — `skills/RENDERER_SKILL.md`) — Реализует аркадную физику срыва в занос, инерционную перекладку и динамический след протекторов на мокром асфальте.
  - [ ] Занос считается по честному slip ratio, а не по эвристике «газ в пол = дрифт»
  - [ ] Пробуксовка на месте (line-lock) реализована разными осями, а не соревнованием «кто победит»
  - [ ] Следы шин персистентные, ложатся под колесо и не мигают между кадрами
  - [ ] Дым от жжёной резины появляется от проскальзывания, а не от нажатия кнопки
  - [ ] Камера в заносе отводится и возвращается плавно, скорость читается по кадру, а не по цифре на HUD
  - [ ] Занос можно поймать и вытянуть: между «держит» и «сорвало» есть управляемая зона
- `docs/ref/knowledge/threejs/rapier_vehicle_controller.md` (выжимка — `skills/RENDERER_SKILL.md`) — Обеспечивает эталонную симуляцию raycast-болида на физическом движке Rapier3D с честной работой подвески и переносом массы.
  - [ ] Лучи подвески фильтруются `WHEEL_RAY_GROUPS` и видят только землю — иначе машина взлетает на собственном грузе
  - [ ] Маска `VEHICLE_GROUPS` содержит саму `GROUP_VEHICLE`, иначе кузова проходят друг сквозь друга
  - [ ] Порядок кадра ровно такой: `updateVehicle` → `step()` → `postStep`
  - [ ] Позиция меша интерполируется `lerp(prev, curr, alpha)` — без этого на 120 Гц видно дрожание
  - [ ] Камера следует за интерполированной позицией со сглаживанием `1 - exp(-k * dt)`, а не за физическим телом напрямую
  - [ ] `scene.background` задан цветом неба — иначе постобработка даёт чёрное небо
  - [ ] Кузов приподнят над точками контакта (`offset.y ≈ 0.22`, `half.y ≈ 0.16`), запас хода подвески есть — машина не лежит на брюхе
  - [ ] Знак руления проверен на живой машине: клавиша «вправо» поворачивает вправо, а не влево
  - [ ] Индексы секций террейна смещены на длину предыдущих массивов вершин — иначе коллайдер сам себя пересекает и машина стоит на старте
- `docs/ref/knowledge/threejs/racing_track_and_opponents.md` (выжимка — `skills/RENDERER_SKILL.md`) — Описывает организацию гоночных сплайнов, расстановку чекпоинтов на 3 картах и базовое поведение ИИ-соперников.
  - [ ] Трасса — одна замкнутая 3D-кривая, а не набор состыкованных кусков
  - [ ] Коллайдер трассы единый trimesh; индексы дороги, поребриков и террейна смещены друг относительно друга
  - [ ] Тест на перевороты репера пройден (`flips === 0`) — сплайн нигде не перекручен
  - [ ] Стартовая арка и светофор построены на матричном базисе из касательной, а не на глобальных осях
  - [ ] Чекпойнты считаются по порядку, срез круга не засчитывается, позиция в гонке пересчитывается каждый кадр
  - [ ] ИИ соперников рулит со знаком `-toTarget.x` — иначе бот разворачивается и уезжает назад
  - [ ] Машины сталкиваются между собой, а не проходят насквозь
  - [ ] Следы шин и партиклы живут в пуле и не растут бесконечно за гонку
- `docs/ref/knowledge/mechanics/checkpoint_lap_racing.md` (выжимка — `skills/racing-track-and-ai.md`) — Отвечает за последовательный прогресс по контрольным точкам, подсчет кругов на кольце Downtown и спринте Highway, а также определение позиции в заезде.
  - [ ] Трасса размечена упорядоченными чекпойнтами: круги, позиция, респавн и срезы — из одной структуры
  - [ ] Прогресс не мозолит глаза, пока всё правильно: он проявляется только при ошибке
  - [ ] Время по секторам показано относительно личного рекорда цветом
  - [ ] Финальный круг объявлен отдельно
  - [ ] «Не туда» появляется в течение секунды после разворота
  - [ ] Респавн ставит на гоночную линию с кратковременной потерей сцепления — это не бесплатный сброс
  - [ ] Чекпойнты стоят достаточно часто, чтобы респавн не отбрасывал далеко
- `docs/ref/knowledge/mechanics/drift_scoring.md` (выжимка — `skills/rapier-vehicle-physics.md`) — Реализует подсчет очков за непрерывный угол заноса и комбо-множители для режима дрифт-трека в порту и накопления заряда нитро.
  - [ ] Занос считается по реальному углу скольжения, а не по нажатию газа
  - [ ] Очки копятся пропорционально скорости и углу и банкуются при чистом выравнивании
  - [ ] Разворот, удар о стену и полная остановка обрывают несданную цепочку
  - [ ] Счёт тикает в реальном времени с растущим тоном
  - [ ] Плотность дыма и след покрышек привязаны к тому же скольжению, что и очки
  - [ ] Звук «зачтено» и звук «потеряно» невозможно перепутать
  - [ ] Угол выше примерно 50° не даёт очков, иначе выгодно крутиться на месте
  - [ ] Механика имеет смысл только при управляемом заносе: физика позволяет держать скольжение
- `docs/ref/knowledge/mechanics/rubberband_opposition.md` (выжимка — `skills/racing-track-and-ai.md`) — Обеспечивает адаптивную подстройку скорости соперников для плотной контактной борьбы на последних 100 метрах дистанции дуэли.
  - [ ] Подстройка идёт параметрами вождения, а не телепортацией соперника
  - [ ] Отклонение скорости держится в пределах примерно ±10%: дальше игрок замечает помощь и победа обесценивается
  - [ ] Отстающий бот никогда не превышает лучший круг игрока за сессию
  - [ ] Видимые средства догона предпочтены невидимым: воздушный мешок, ускоритель на трассе
  - [ ] Отрыв в секундах показан игроку, чтобы напряжение читалось
  - [ ] Гонка остаётся напряжённой и в начале, и в конце: нет ни недосягаемого ИИ, ни победы с первого круга
- `docs/ref/knowledge/stack/rapier3d.md` (выжимка — `skills/STACK_SKILL.md`) — Единый WebAssembly-движок физики для честных твердотельных столкновений спорткаров с отбойниками и симуляции колес.
  - [ ] Физика только на Rapier: свои «скорости», Cannon-es и ammo.js в проект не тянутся
  - [ ] Шаг мира фиксированный: world.timestep не подменяется на dt
  - [ ] cuboid() получает ПОЛУразмеры — коробка 1×1×1 это cuboid(0.5, 0.5, 0.5)
  - [ ] trimesh используется только для статичных тел: динамический проваливается сквозь мир
  - [ ] Группы столкновений заданы с обеих сторон — иначе контакта не будет
  - [ ] Триггеры сделаны сенсорами, а не проверкой дистанции в update()
  - [ ] Синхронизация мешей идёт ПОСЛЕ world.step(), иначе кадр рисует прошлое состояние
  - [ ] «На земле» определяется контроллером персонажа, а не по velocity.y === 0
  - [ ] Рейкаст исключает собственное тело стрелка
  - [ ] Рестарт уровня — телепорт тел, а не пересборка мира
  - [ ] Физика проверена скриптом в Node до того, как её увидит игрок

**Вспомогательные:**
- `docs/ref/knowledge/threejs/vehicle_wheel_rig.md` (выжимка — `skills/RENDERER_SKILL.md`) — Задает корректный визуальный риг 4 колес с правильными осями вращения, поворотом передней пары и реакцией подвески.
- `docs/ref/knowledge/threejs/game_map_and_world_design.md` (выжимка — `skills/RENDERER_SKILL.md`) — Позволяет процедурно собрать три разнообразные трассы: ночное городское кольцо, прямое шоссе и техничный портовый трек.
- `docs/ref/knowledge/threejs/procedural_mesh_builder.md` (выжимка — `skills/RENDERER_SKILL.md`) — Генерирует стилизованные low-poly здания ночного мегаполиса, отбойники и уличные фонари без использования внешних 3D-моделей.
- `docs/ref/knowledge/threejs/juice_and_vfx_pool.md` (выжимка — `skills/RENDERER_SKILL.md`) — Создает пул частиц для эффектного бирюзового дыма из-под колес, вспышек закиси азота и искр при скольжении по отбойникам.
- `docs/ref/knowledge/threejs/adaptive_quality.md` (выжимка — `skills/RENDERER_SKILL.md`) — Динамически балансирует разрешение рендера, плотность частиц и дальность прорисовки для стабильных 60 FPS на мобильных устройствах.
- `docs/ref/knowledge/threejs/performance_guide.md` (выжимка — `skills/RENDERER_SKILL.md`) — Содержит правила инстансинга городского окружения и ограничения draw calls для быстрой ночной гонки.
- `docs/ref/knowledge/stack/three_mesh_bvh.md` (выжимка — `skills/STACK_SKILL.md`) — Ускоряет пространственные лучевые проверки расстояния до препятствий для работы непрерывного множителя риска «Лезвие Бритвы».
- `docs/ref/knowledge/stack/postprocessing.md` (выжимка — `skills/STACK_SKILL.md`) — Реализует неоновое свечение ночного города, размытие скорости при включении нитро и динамический эффект расширения FOV.
- `docs/ref/knowledge/audio/procedural_sound_synthesizer.md` (выжимка — `skills/RENDERER_SKILL.md`) — Синтезирует процедурные звуки рева двигателя, визга покрышек при дрифте и подрыва закиси азота через Web Audio API без внешних аудиофайлов.
- `docs/ref/knowledge/monetization/rewarded_ads_patterns.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Внедряет механику добровольного удвоения кредитов за финиш в топ-3 и мгновенный бонусный заряд нитро перед стартом.

**Архетип петли проекта**: `docs/ref/knowledge/patterns/racing_event_loop.md` (выжимка — `skills/rapier-vehicle-physics.md`)

**Платформенные требования — обязательны всегда, независимо от жанра.** Без Playgama Bridge игра на площадке не стартует: платформа держит свою заставку до таймаута и снимает игру с модерации.
- `docs/ref/knowledge/playgama/game_ready_and_loading.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/auth_and_player.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/storage_and_cloud.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/ads_integration.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/banners_and_layout.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/compliance/yandex_moderation.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/localization_system.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/touch_controls.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_design_system.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_implementation.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)

**Осознанно НЕ включены**: `patterns/score_attack_loop.md`, `stack/recast_navigation.md`, `stack/bitecs.md`, `stack/yuka_ai.md`, `threejs/procedural_character_rig.md`, `threejs/horde_survivor_core.md`, `threejs/fighting_game_core.md`, `mechanics/upgrade_choices.md`. Отклоненные документы содержат механики чуждых жанров (авторогалики, файтинги, пешие персонажи) либо избыточный стек (NavMesh, bitECS, Yuka), так как соперники используют сплайн трассы с адаптивным rubber-banding, а прогрессия завязана на гараж, а не на карточки апгрейдов.
Не подтягивай решения из этих документов — они про другую игру.

**Документы проекта** (лежат рядом, в корне игры): `ACCEPTANCE.md` — приёмка, `AGENTS.md` — правила работы, `PROJECT_DIRECTION.md` — рамка проекта, `GAME_DESIGN_DOCUMENT.md`, `MECHANICS.md`, `CORE_LOOP.md`, `UI_UX_SPECIFICATION.md`, `ART_DIRECTION.md`, `MOBILE_CONTROLS.md`, `PLAYGAMA_INTEGRATION.md`, `MONETIZATION.md`, `QA_PLAN.md`, `DEVLOG.md` и `CHANGELOG.md` — их ведёшь ты.