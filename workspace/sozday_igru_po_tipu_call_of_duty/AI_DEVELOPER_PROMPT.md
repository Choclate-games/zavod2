# FINAL AI DEVELOPER PROMPT: AC-130: НОЧНОЙ ТЕПЛОВИЗОР 🎮⚡

> **ИНСТРУКЦИЯ КОДОВОМУ АГЕНТУ**
> Ты ведущий разработчик этой игры. Задача — довести её до состояния, в котором
> она проходит приёмку из `ACCEPTANCE.md`: работающая, играбельная, принятая
> площадкой. Заглушки, TODO и «пока так» результатом не считаются.

### Как читать этот документ

Требования размечены по обязательности. Когда обязательно всё, не обязательно ничего.

| Уровень | Что значит | Что делать при сомнении |
|---|---|---|
| **MUST** | Без этого игра не работает или не проходит модерацию площадки. | Не обсуждается. Проверяется в `ACCEPTANCE.md`, разделы A–C. |
| **SHOULD** | Проектное решение фабрики: так игра задумана. | Отступать можно, но с записью в `DEVLOG.md` — что и почему. |
| **MAY** | На твоё усмотрение. | Решай сам, спрашивать не нужно. |

Неразмеченное считается **SHOULD**. Всё, что помечено «ЗАПРЕЩЕНО» в секции 1b, —
это **MUST**, даже если спецификация где-то умалчивает.

### Порядок работы

Читать документ целиком до первой строчки кода не нужно. Порядок такой:

0. `node scripts/fetch-knowledge.mjs` — забрать базу знаний в `docs/ref/`. Одна команда, дальше работа идёт офлайн. **MUST**, и раньше всего остального: без этого половина адресов в документе никуда не ведёт.
1. `ACCEPTANCE.md` — чем закончится работа. Дальше всё делается под него.
2. Секция 1b — чем эта игра является и чем она не является. Рамка всего остального.
3. `DESIGN.md` — написать самому, до первой строчки кода. Секция 7 говорит, что в нём должно быть и чего в нём быть не должно.
4. Каркас: `package.json`, Vite, TypeScript strict, пустая сцена Three.js, цикл с фиксированным шагом. Playgama Bridge и `game_ready` — сразу, а не в конце (проверки **C1–C4**).
5. Главная механика из секции 3 — одна, до играбельного состояния. Не все сразу. Перед тем как писать её с нуля — секция 3c: возможно, она уже написана.
6. Дальше по фазам роадмапа (секция 8), каждая фаза закрывает свои номера проверок.
7. `node scripts/check-spec.mjs` — после каждой фазы, а не один раз в конце.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: AC-130: Ночной Тепловизор
- **Project Slug**: `sozday_igru_po_tipu_call_of_duty`
- **Genre**: 3D Тактический Рельсовый Авиа-Шутер (FLIR-симулятор огневой поддержки / Аркадный ганшип)
- **Target Platform**: Playgama Bridge (Yandex Games / VK / Web)
- **Orientation**: Landscape
- **Target Audience**: Мужчины 16–45 лет, поклонники Call of Duty (культовая миссия «Death From Above»), фанаты военной авиации, тактических шутеров и игр с зрелищной физикой разрушений.
- **Player Fantasy**: Ты — оператор бортового комплекса вооружения тяжелого самолета огневой поддержки AC-130 Spectre, кружащего на высоте 1000 метров над ночной зоной боевых действий. В твоих руках — абсолютное превосходство в воздухе и сокрушительная мощь трех калибров, от своевременных и точных выстрелов которой зависит выживание союзного отряда «Браво» на земле.
- **Core Hook**: Залп 105-мм гаубицы с высоты 1000 метров с баллистической задержкой в 2 секунды: ослепительно белая тепловая вспышка в монохромном тепловизоре разрывает вражескую колонну на горящие обломки ровно в 10 метрах от укрытия союзного спецназа под хладнокровный радиодоклад «Direct hit, target destroyed!».
- **Session Model**: Спецоперация огневой поддержки длительностью 60–90 секунд: эскорт союзного отряда спецназа из 4 бойцов через 3 тактических сектора до зоны вертолетной эвакуации.


---

## 1b. РАМКА ПРОЕКТА: ЧТО ЭТО ЗА ИГРА И ЧЕМ ОНА НЕ ЯВЛЯЕТСЯ


**Направление**: AC-130: Ночной Тепловизор

**Почему выбрано именно оно**: Направление «AC-130: Ночной Тепловизор» — сильнейший выбор для браузера и мобильных устройств. Оно берет самую узнаваемую и зрелищную миссию в истории Call of Duty («Death From Above») и идеально адаптирует её под специфику платформы:
1. Идеальный мобильный UX: Устранена главная проблема мобильных шутеров — неуклюжие виртуальные джойстики перемещения. Игрок целится естественным свайпом и нажимает на иконки калибров, получая мгновенный тактический отклик.
2. Безупречная производительность: Монохромный FLIR-шейдер тепловизора с эффектом White-Hot выдает стабильные 60 FPS на любом бюджетном смартфоне, выглядя при этом предельно стильно и реалистично без тяжелых PBR-материалов.
3. Мгновенный вау-эффект: С первой секунды игрок слышит радиопереговоры («Angel 2-0, cleared hot»), видит термо-сигнатуры вражеской техники и разносит укрепления 105-мм гаубицей с честной баллистикой и физикой взрывных цепных реакций.

**Узнаваемая сцена**: Ночной вид через монохромный тепловизор бортового комплекса AC-130 под углом 50 градусов вниз на перекресток разрушенного ближневосточного города. В центре экрана в зеленовато-белом прицельном перекрестии 105-мм гаубицы ярко светится раскаленный силуэт вражеского БТР в момент детонации боекомплекта: во все стороны разлетаются белые контрастные осколки брони и гусеницы, освещая темные руины зданий. Слева за бетонным забором укрылись три яркие фигурки союзного спецназа с мерцающими ИК-стробоскопами на шлемах, а справа по улице к ним наперерез мчится вражеский пикап с пулеметом, двигатель которого раскален добела.

**Без чего проект перестаёт быть собой** (реализовать обязательно):
- Три калибра с принципиально разной баллистикой и тактической ролью: 25-мм скорострельный пулемет Гатлинга (против разрозненной пехоты), 40-мм автопушка Бофорс (против легких пикапов) и 105-мм гаубица (против танков и зданий с долгой перезарядкой).
- Аутентичный визуальный стиль тепловизора FLIR (White-Hot): монохромная картинка с ярко светящимися силуэтами живой силы, горячими двигателями техники и тепловыми следами от взрывов.
- Союзный отряд спецназа под управлением ИИ с инфракрасными стробоскопами и строгим запретом на Friendly Fire (попадание тяжелым калибром по союзникам проваливает операцию).
- Баллистическое время полета снарядов: снаряды летят с орбиты 1000м с задержкой 1.2–2.5 секунды, требуя от игрока брать упреждение по движущейся технике.

**ЗАПРЕЩЕНО ВВОДИТЬ** — даже если спецификация где-то умалчивает и даже если «так принято в жанре»:
- Никаких виртуальных джойстиков ходьбы и прыжков: самолет AC-130 движется по заданной орбите автоматически, игрок управляет только прицельным комплексом и вооружением.
- Никаких карточек случайных апгрейдов (+15% к урону пули), клонов Vampire Survivors / Brotato и автострельбы.
- Никаких бесконечных волн безликих монстров на замкнутой арене: только конкретная срежиссированная боевая операция с фазами прикрытия союзного спецназа.
- Никаких серых прототипных кубов: локация и техника стилизованы под аутентичную картинку военного тепловизора FLIR с шумом матрицы и CRT-сканлайнами.
- Никаких полосок здоровья над головами врагов и цветных цифр урона: повреждения рассчитываются через радиус сплеша, кинетический импульс и рэгдолл.
- волны врагов на арене
- окно выбора из трёх карт апгрейда
- универсальная связка «дэш + парирование + удар»
- заимствование Vampire Survivors / Brotato как основы
- джойстик слева + кнопки атаки справа как раскладка по умолчанию
- серые кубы вместо стилизованной геометрии
- мета-прогрессия «золото за забег -> постоянные апгрейды» без связи с темой

Если по ходу реализации кажется, что без запрещённого элемента игра не работает — это находка для `DEVLOG.md`, а не разрешение его добавить.

**Рассмотренные и отклонённые направления** (не возвращай их через механики):
- D2 (Гонка Вооружений: Ближний Бой): Отвергнут из-за высокого порога входа и неудобства мобильного управления: одновременный контроль перемещения, прицеливания, прыжков и стрельбы на сенсорном экране смартфона создает высокий фрустрационный барьер в браузере и проигрывает нативным мобильным шутерам.
- D3 (Снайперский Рубеж: Невидимый Выстрел): Отвергнут из-за слишком медленного медитативного темпа сессии и долгих пауз между выстрелами: случайный промах нарушает стелс и требует перезапуска, что снижает динамику и адреналин, характерные для бренда Call of Duty.

---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite
- **Renderer**: **THREEJS** (^0.185.1)
  - *Selection Rationale*: Three.js + Rapier3D: пространственный геймплей, физика и освещение. Перспективная камера.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat ^0.20.0)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Web Audio API (Howler.js with html5: false)
- **State Management**: Reactive EventBus / State Store
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 80
- **Max Triangles / Active Sprites**: < 45000
- **Max Bundle Size**: < 4.5 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Сканирование сектора в тепловизор FLIR -> Выбор калибра (25мм / 40мм / 105мм) -> Расчет баллистического упреждения и радиуса поражения -> Залп по технике и живой силе противника -> Физическая цепная детонация укрытий -> Обеспечение безопасного прохода союзного спецназа к эвакуации -> Получение боевого рейтинга.
```

**Фирменный момент**: На 72-й секунде ночной миссии союзный отряд Bravo-6 зажат в тупике узкой улочки под перекрестным огнем двух тяжелых БТР и гранатометчиков с крыш; ИК-стробоскопы спецназовцев мигают в 14 метрах от брони врага. Игрок переключается на 105-мм гаубицу, свайпом закладывает 2.2-секундное баллистическое упреждение перед ведущим БТР и жмет спуск. Грохот отдачи сотрясает FLIR-камеру, 2 секунды гробовой радиотишины в наушниках — и ослепительно белая вспышка White-Hot превращает БТР в огненный шар, взрывная волна сносит угол кирпичного здания прямо на второй БТР, вызывая вторичную детонацию боекомплекта, а в рации на фоне свиста осколков хрипит капитан Прайс: «Danger close! Direct hit, hostiles wiped, Bravo-6 moving out!».

**Чем петля отличается от соседей по жанру**: В отличие от Zombie Gunship и аркадных тиров, где игрок отстреливает волны безликой биомассы, идущей по прямой, здесь петля строится на асимметричной эскорт-тактике в условиях жесткого 'Danger Close' (риска уничтожить союзников собственным сплешем). Игрок управляет не летающим танком с бесконечной пальбой, а хладнокровным термооптическим комплексом: 25-мм пулемет мгновенно подавляет стрелков и расчищает пехоту, 40-мм автопушка 'шьет' легкобронированные пикапы с упреждением по курсу движения, а 105-мм гаубица используется как хирургический инструмент с 2-секундным таймингом для обрушения зданий на технику врага. Успех определяется не скоростью закликивания, а чтением теплового контраста (White-Hot/Black-Hot), контролем температуры стволов и расчетом цепных физических взрывов в трехмерной городской среде Three.js/Rapier3D.

**Шаблон жанра, который НЕ реализуем**: Отвергаем шаблон 'Hitscan-тир с автодоводкой и аркадным овердрайвом' (характерный для казуальных рельсовых шутеров, где пули попадают мгновенно по тапу в силуэт врага, а урон масштабируется полосками HP). Причина отказа: мгновенный урон полностью уничтожает фундаментальное ощущение высоты (1000 метров), веса орудий и тактического напряжения оператора ганшипа. Мы заменяем хитскан на честную орбитальную баллистику с физическим временем подлета снарядов (1.2–2.2 с), радиусами разлета осколков и опасностью дружественного огня, превращая каждый выстрел из бездумного закликивания в расчетную баллистическую дуэль.

**Схема петли**:
```text
+-------------------------------------------------------------------------------+
|                      AC-130 SPECTRE: FLIR TACTICAL LOOP                       |
+-------------------------------------------------------------------------------+
                                        |
                                        v
                       [ 1. FLIR ТЕРМОСКАНИРОВАНИЕ ]
                       - Сканирование White-Hot (2x/4x/8x Zoom)
                       - Поиск тепловых контрастов (пикапы/пехота)
                       - Мониторинг ИК-стробоскопов отряда Bravo-6
                                        |
                                        v
                       [ 2. ТАКТИЧЕСКИЙ ВЫБОР КАЛИБРА ]
                       +----------------+----------------+
                       | 25мм GAU-12    | 40мм Bofors    | 105мм Howitzer |
                       | (Пехота/Крыши) | (Техника/Стены)| (Танки/Завалы) |
                       +----------------+----------------+----------------+
                                        |
                                        v
                       [ 3. РАСЧЕТ БАЛЛИСТИКИ И УПРЕЖДЕНИЯ ]
                       - Упреждение по скорости цели (v = 8..22 м/с)
                       - Время подлета снаряда (t = 1.2..2.2 с)
                       - Проверка Danger Close (дистанция до союзников)
                                        |
                                        v
                       [ 4. ОГНЕВОЙ ЗАЛП И ФИЗИЧЕСКИЙ УДАР ]
                       - Кинетический импакт Three.js/Rapier3D
                       - Разрушение зданий и обрушение завалов
                       - Вторичные цепные детонации бензовозов/БК
                                        |
                                        v
                       [ 5. ПРОДВИЖЕНИЕ ЭСКОРТА BRAVO-6 ]
                       - Спецназ делает рывок по зачищенной улице
                       - Подавление ответного огня противника
                       - Спасены 4/4 бойцов -> Эвакуация на вертолете
                                        |
                                        v
                       [ 6. ОРУЖЕЙНАЯ МОДЕРНИЗАЦИЯ AC-130 ]
                       - Прокачка охлаждения GAU-12, гиростабилизатора,
                         автомата заряжания 105мм и FLIR матрицы Gen-4
                                        |
                                        +-----> (Возврат на новый ТВД)
```

**Микро-петля (посекундно)**:
- **Термосканирование сектора и захват тепловых сигнатур** (0.8–1.5 с): игрок — Плавное панорамирование FLIR-прицела свайпом/мышью и переключение оптического зума (2x/4x/8x) по вспышкам выстрелов и горячим двигателям техники.; игра — Рендеринг тепловой контрастности White-Hot: горячие двигатели пикапов и силуэты пехоты светятся белым (85–350°C), холодный бетон окружения остается темно-серым (15°C); ИК-стробоскопы спецназа пульсируют с частотой 2 Гц.; решение — Идентифицировать вектор главной угрозы: гранатометчик на крыше (цель для 25мм) или колонна джипов на перекрестке (цель для 40мм/105мм).
- **Селекция калибра и расчет баллистического упреждения** (0.3–0.8 с): игрок — Тап по иконке калибра (1 / 2 / 3) с учетом таймера перезарядки и вынос перекрестья прицела вперед по вектору движения цели.; игра — Смена прицельной сетки (милы, поправка на орбитальный снос 3.5 м/с), звуковой щелчок затвора механизма подачи и звуковая индикация готовности орудия.; решение — Выбрать быстрый 25-мм пулемет с нулевым риском сплеша или мощный 105-мм снаряд с 2.2-секундной задержкой подлета и риском накрыть своих.
- **Залп и баллистический полет снаряда в невесомости** (1.2–2.2 с): игрок — Нажатие гашетки (Fire / Space / Тап по кнопке огня) с фиксацией точки падения снаряда.; игра — Тяжелый акустический импульс выстрела с эхо-шлейфом в ночном небе, вспышка дульного пламени, трассер уходит по крутой баллистической дуге с высоты 1000м к земле (время подлета t = 1.2–2.2 с).; решение — Удерживать сектор огня для корректировки очереди или перенести внимание на соседний фланг обороны Bravo-6.
- **Кинетический удар, детонация и разлет осколков** (0.5–1.0 с): игрок — Визуальный контроль точки попадания через термооптику FLIR.; игра — Ослепительная тепловая вспышка на сенсоре (bloom 1.8x), физический разлет рэгдоллов и обломков в Rapier3D, детонация баков техники (радиус 12м), радиоклик 'Target neutralized!'.; решение — Требуется ли добивание остатков пехоты 25-мм очередью или сектор зачищен и союзники могут продолжить бег.

**Мезо-петля (этап)**:
- **Прорыв через Сектор 1: Уличная засада пехоты (0:00–0:25)** (25 с): игрок — Подавление пулеметных огневых точек и снайперов на балконах короткими очередями 25-мм GAU-12 без перегрева ствола.; игра — Вражеская пехота вжимается в укрытия (suppression 100%), отряд Bravo-6 ускоренным маршем преодолевает первые 80 метров улицы под прикрытием трассеров.; решение — Экономить ресурс нагрева 25-мм пулемета или отсекать наступающие группы плотным подавляющим огнем.
- **Оборона Сектора 2: Моторизованный перехват и блокпост (0:25–0:55)** (30 с): игрок — Стрельба отсечками по 3 снаряда из 40-мм Bofors по скоростным техничкам и разрушение кирпичного забора блокпоста.; игра — Прямые попадания подрывают пикапы, обломки стен погребают засевших стрелков; союзный отряд занимает круговую оборону у баррикады, запрашивая расчистку пути.; решение — Стрелять прямо в пикап на полной скорости с упреждением 35 метров или обрушить водонапорную башню на дорогу перед ним.
- **Кульминация Сектора 3: Танковый прорыв Danger Close и эвакуация (0:55–1:30)** (35 с): игрок — Прицельный залп 105-мм гаубицы по танку Т-72 в 18 метрах от позиции спецназа с точным контролем границы поражения союзников.; игра — Катастрофический взрыв боеукладки танка, отрыв башни физическим импульсом 85000 Н, посадка вертолета MH-60 Black Hawk с дымовой завесой, посадка выживших бойцов на борт.; решение — Рискнуть выстрелом 105-мм гаубицы на грани радиуса поражения спецназа или попытаться разобрать тяжелую броню серией 40-мм снарядов за 6 секунд.

**Макро-петля (забег)**:
- **Брифинг спецоперации и выбор подвеса вооружения** (10–15 с): игрок — Изучение тепловой спутниковой карты миссии, выбор активных модификаций калибров (осколочно-фугасные / бронебойные снаряды).; игра — Отображение маршрута эскорта Bravo-6, плотности вражеской техники, расчетного бонуса за спасенных бойцов и целевых таймингов.; решение — Сфокусировать билд на скорострельности 25-мм пулемета для плотной городской застройки или на радиусе поражения 105-мм гаубицы для открытых пространств.
- **Боевой вылет огневой поддержки (60–90 с)** (60–90 с): игрок — Выполнение боевой задачи по сопровождению, зачистке угроз, набиванию комбо-серий и спасению всех 4 оперативников.; игра — Динамическая генерация волн противника, расчет точности, начисление боевого рейтинга (Accuracy, Collateral, Bravo Survival) и тактических кредитов.; решение — Балансировать между агрессивной зачисткой ради максимального комбо-счета и осторожной защитой союзников от случайного сплеша.
- **Дебрифинг, начисление кредитов и оружейная мастерская** (15–30 с): игрок — Инвестирование заработанных кредитов в апгрейды ганшипа: стабилизация гироскопа камеры, тепловые матрицы FLIR Gen-4, титановый автомат заряжания M102.; игра — Разблокировка новых ТВД (Ночная Промзона, Нефтяной Терминал, Горный Перевал), повышение ранга оператора (от Second Lieutenant до Master Gunner) и прирост боевых ТТХ.; решение — Вложить ресурсы в уменьшение дрожания прицела при зуме 8x или сократить время перезарядки 105-мм орудия на 0.4 с.

**Кривая напряжения**: Кривая напряжения представляет собой трехступенчатую пилообразную синусоиду с нарастающей амплитудой и взрывным пиком на 80-й секунде: 1) 0:00–0:15 (Напряжение 20%): Спокойное сканирование ночного города, одиночные патрули пехоты, игрок пристреливается из 25-мм пулемета, спецназ спокойно выдвигается из точки высадки. 2) 0:15–0:30 (Напряжение 50%): Внезапная засада на перекрестке — гранатометчики на крышах открывают огонь, здоровье спецназа начинает таять; игрок лихорадочно переключается между 25мм и 40мм, подавляя угрозы. Спад напряжения (30%) после зачистки перекрестка. 3) 0:30–0:60 (Напряжение 75%): В сектор врываются 3 скоростных пикапа с крупнокалиберными пулеметами ДШК. Требуется расчет упреждения в 1.5–2.0 секунды и обрушение придорожных построек для блокировки проезда. Спецназ укрывается за бетонными блоками. 4) 0:60–0:85 (Напряжение 100% — Апогей): Прорыв тяжелого танка Т-72 и двух БТР в непосредственной близости (12–15м) от союзного отряда. Режим Danger Close: малейшая ошибка в наведении 105-мм гаубицы убьет спецназ, а промедление на 3 секунды приведет к расстрелу отряда танковым орудием. 5) 0:85–0:90 (Спад до 0% -> Катарсис): Детонация танка, посадка эвакуационного вертолета Black Hawk, доклад 'All Bravo teams aboard, dustoff!', триумфальный взлет под звуки радиоэфира и подсчет боевых наград.

**Формулы ядра (реализуй буквально)**:
- `Время подлета снаряда к земле: t_flight = H_alt / V_proj = 1000м / V_proj (для 25мм: 1000 / 850 = 1.18 с; для 40мм: 1000 / 600 = 1.67 с; для 105мм: 1000 / 455 = 2.20 с)`
- `Баллистическое упреждение точки прицеливания: Lead_pos = Target_pos + Target_velocity * t_flight + Orbit_drift_offset (где Orbit_drift_offset = 3.5 м/с * t_flight по касательной траектории виража AC-130)`
- `Урон от фугасного сплеша по расстоянию r: Dmg(r) = Base_Dmg * max(0, 1 - (r / Splash_Radius)^1.5) [для 105мм: Base_Dmg = 2500 ед, Splash_Radius = 18.0 м; для 40мм: Base_Dmg = 450 ед, Splash_Radius = 6.5 м]`
- `Проверка протокола Friendly Fire (Danger Close check): Friendly_Casualty = (Distance(Impact_pos, Bravo_pos) < Ally_Danger_Radius) && (Caliber_Index >= 2) [где Ally_Danger_Radius_105mm = 14.5 м, Ally_Danger_Radius_40mm = 5.2 м]`
- `Термодинамика нагрева и охлаждения ствольного блока GAU-12: Heat_t = clamp(Heat_prev + Shots * 1.85°C - 18.0°C/с * dt, 20°C, 100°C); при Heat >= 100°C наступает Jam_Lockout на 3.5 с`
- `Физический импульс ударной волны Rapier3D: Impulse_Vector = normalize(Entity_pos - Blast_Center) * (Blast_Force / max(1.0, Distance^2)) [для 105мм: Blast_Force = 120000 Н*с; для бензовоза: Blast_Force = 180000 Н*с]`
- `Итоговый боевой рейтинг спецоперации: Total_Score = (Enemies_Killed * 150 + Armor_Destroyed * 500 + Chain_Explosions * 300) * Accuracy_Multiplier * (Bravo_Survivors / 4) + Time_Bonus_Credits`

**Прогрессия внутри забега**:
- Заряд орбитального лазерного целеуказателя Pave Penny: за каждые 5 точных попаданий подряд накапливается 1 тактический маркер мгновенной подсветки упреждения (HUD показывает точную точку падения для 105мм снаряда).
- Температурный овердрайв FLIR-сенсора: комбо-серия x10 активирует режим ультра-контрастности 'Super White-Hot' на 8 секунд, подсвечивающий врагов сквозь тонкие перекрытия крыш с 1.25x множителем очков.
- Экстренный запрос сброса ИК-дымовой завесы спецназом Bravo-6 (1 заряд на миссию): союзники активируют термо-маскировочный блок, снижая меткость вражеских стрелков на 85% в течение 6.0 секунд.
- Огневой раж расчета гаубицы: уничтожение 3 единиц тяжелой техники снижает базовое время ручной перезарядки 105-мм орудия с 4.5 с до 3.2 с до конца текущей спецоперации.

**Мета-прогрессия**:
- Гиростабилизированный кардан FLIR Mark IV: 5 уровней апгрейда, снижающих амплитуду рыскания и дрожания прицельной сетки при 8x зуме с 0.45° до 0.08° за 1200–8500 кредитов.
- Титановый гидропневматический досылатель 105-мм гаубицы M102: сокращение базового времени перезарядки орудия с 5.2 с до 3.8 с (шаг 0.28 с за уровень) за 2500–15000 кредитов.
- Криогенное охлаждение ствольного блока GAU-12/U Equalizer: увеличение порога перегрева с 28 непрерывных выстрелов до 65 выстрелов и ускорение сброса температуры с 12°C/с до 28°C/с.
- Высокочувствительная неохлаждаемая тепловизионная матрица HD-FLIR Gen-4: открывает режим фильтрации ложных тепловых помех и подсвечивает контуры бронетехники желтым термоконтуром.
- Разблокировка оперативных театров боевых действий (ТВД): 'Нефтяной Терминал Басра' (обилие взрывоопасных резервуаров для цепных реакций), 'Горная Застава Кандагар' (многоярусный серпантин) и 'Ночная Промзона'.

### Баллистическое жонглирование тройным калибром (Triple-Caliber Orbital Ballistics) (CORE)
- **Category**: Двигатель боевого темпа и основной инструмент решения огневых задач
- **Description**: В отличие от большинства мобильных тиров с мгновенным уроном, здесь реализована честная физическая задержка орбитальной баллистики (2.2 с для 105мм), требующая мышления на 2 секунды вперед и дарящая эйфорический выброс дофамина при точном попадании.
- **Player Input**: Клавиатура: Клавиши 1, 2, 3 — мгновенный выбор калибра; Пробел / ЛКМ — стрельба; Колесо мыши — оптический зум 2x/4x/8x. Мобильный тач: Крупные боковые плашки выбора калибров '25mm', '40mm', '105mm' под правый палец; тап/удержание правой зоны экрана — ведение огня; свайп левой зоны — наведение турели.
- **Technical Complexity**: Medium
- **Решение игрока**: Какой калибр применить по текущему типу угрозы: мгновенно подавить пехоту 25-мм пулеметом, выдать серию 40-мм снарядов по быстрому пикапу или выпустить 105-мм гаубичный снаряд по танку/зданию с учетом 2.2-секундного упреждения и 4.5-секундного кулдауна.
- **Состояния**: `CALIBER_READY: выбранный калибр заряжен, прицельная марка активна, орудие готово к открытию огня.`, `FIRING_BURST: ведется стрельба (25мм — непрерывная очередь до отпускания гашетки; 40мм — полуавтоматическая отсечка по 1-3 выстрела; 105мм — одиночный залп).`, `RELOADING_COOLDOWN: орудие на перезарядке (105мм — 4.5 с, 40мм — 1.8 с на кассету); доступно мгновенное переключение на другие калибры.`, `OVERHEAT_JAM: ствольный блок 25мм нагрелся до 100°C; принудительная блокировка огня на 3.5 с с воспроизведением аварийного зуммера.`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Скорость полета 105-мм снаряда (V_105)` = `455 м/с (время подлета с 1000м: 2.20 с)` — Если увеличить скорость выше 700 м/с, пропадет магия предвкушения попадания и расчет упреждения; если уменьшить ниже 350 м/с, попадать по движущейся технике станет невозможно без чистого везения.
  - `Радиус сплошного поражения 105-мм ОФС` = `18.0 м (Base Damage 2500 ед)` — Если сделать больше 25 м, гаубица будет зачищать весь экран в один клик и постоянно взрывать спецназ; если меньше 12 м, попадание покажется слабым и не оправдает 4.5-секундную перезарядку.
  - `Скорострельность 25-мм пулемета GAU-12` = `1800 выстр/мин (30 выстр/с, 85 ед урона/пуля)` — При занижении ниже 1200 выстр/мин пулемет теряет тактильный кайф лазерной 'пилы'; при завышении выше 2500 выстр/мин перегрев наступает мгновенно за 0.8 с.
  - `Время перезарядки 105-мм гаубицы` = `4.50 с` — Если снизить до 2.0 с, игрок перестанет использовать 40мм и 25мм; если поднять выше 6.5 с, темп игры в 90-секундной сессии провисает в мучительное ожидание.
- **Слои отклика**:
  - Визуал/VFX: Для 25мм — сплошная трассирующая белая нить в тепловизоре с россыпью микро-вспышек на бетоне; для 40мм — пульсирующие яркие вспышки разрывов диаметром 4м; для 105мм — гигантская ослепительно белая термическая вспышка с эффектом FLIR-засветки сенсора (bloom 2.2x), огненный шар диаметром 22м и столб раскаленной пыли.
  - Звук (Web Audio): Приглушенный тяжелый грохот выстрела внутри фюзеляжа ганшипа с пространственной реверберацией, глухой свист рассекаемого воздуха, через 2.2 с — низкочастотный басовый удар (45 Гц sub-bass) детонации на земле и радиодоклад штурмана: 'Kaboom! Direct hit!'.
  - Камера (импульс/зум): Для 105мм — резкий вертикальный угловой толчок виртуальной камеры (шейк 0.35° с затуханием 0.4 с) в момент выстрела и вторичный легкий микро-шейк при детонации на земле.
  - UI/Тактильность: Для мобильных устройств — мощный короткий виброотклик (45 мс) при выстреле 105мм и пульсирующая вибрация (12 мс) при стрельбе из 25мм; динамическая смена прицельной сетки с отображением термошкалы нагрева ствола и таймера заряжания.
- **Режим отказа игрока**: Игрок стреляет 105-мм снарядом в быструю технику без упреждения — снаряд падает в пустоту позади машины, орудие уходит на 4.5-секундную перезарядку, а техничка безнаказанно подъезжает к спецназу и открывает огонь. Сигнал: радиодоклад 'Splash, no hit! Lead the target!' и маркер падения в 20 метрах за целью.
- **Кривая мастерства**: Новичок на 30-й секунде судорожно палит из 105мм по одиночным пехотинцам, перегревает 25мм пулемет и оказывается безоружным перед танком. Эксперт на 10-м забеге непрерывно жонглирует оружием: удерживает пехоту короткими отсечками 25мм (3–5 пуль), 40мм кассетами срезает пикапы на виражах с упреждением в 1.8 с, а 105мм оставляет строго для тяжелой брони и узловых зданий.
- **Сопротивление игры**: Противник выпускает скоростные пикапы 'Тойота' (скорость до 22 м/с), которые резко меняют траекторию движения на перекрестках при звуках близких разрывов, сбивая прицельное упреждение.
- **Синергии**:
  - Связь с Механикой 2 (Danger Close): выбор калибра жестко лимитируется дистанцией до отряда Bravo-6 (вблизи своих разрешен только точечный 25-мм пулемет).
  - Связь с Механикой 3 (Физическая детонация): 105-мм снаряд подрывает цистерны с топливом, создавая вторичные волны поражения без расхода боеприпасов.
- **Почему это не жанровый шаблон**: В отличие от большинства мобильных тиров с мгновенным уроном, здесь реализована честная физическая задержка орбитальной баллистики (2.2 с для 105мм), требующая мышления на 2 секунды вперед и дарящая эйфорический выброс дофамина при точном попадании.
- **Псевдокод тика**:
```text
function tickWeapons(dt, input) {
  if (input.switchCaliber) currentCaliber = input.selectedCaliber;
  if (currentCaliber.isOverheated) {
    currentCaliber.heatTimer -= dt;
    if (currentCaliber.heatTimer <= 0) currentCaliber.isOverheated = false;
    return;
  }
  if (input.firePressed && currentCaliber.cooldown <= 0) {
    currentCaliber.cooldown = currentCaliber.baseCooldown;
    currentCaliber.heat += currentCaliber.heatPerShot;
    spawnBallisticProjectile(camera.getAimRay(), currentCaliber.speed, currentCaliber.splashRadius);
    triggerRecoilCameraShake(currentCaliber.shakeIntensity);
    playWebAudioShot(currentCaliber.soundId);
  }
  currentCaliber.cooldown = Math.max(0, currentCaliber.cooldown - dt);
  currentCaliber.heat = Math.max(0, currentCaliber.heat - currentCaliber.coolingRate * dt);
}
```

### Протокол Danger Close и ИК-стробоскопы Bravo-6 (Danger Close & IR Beacon Protocol) (SECONDARY)
- **Category**: Главный источник тактического риска, напряжения и регулятор точности стрельбы
- **Description**: Friendly Fire в игре — это не банальный штраф по очкам, а фундаментальное пространственное ограничение физического сплеша оружия с отложенным баллистическим таймингом, создающее киношную драму 'на волоске от гибели'.
- **Player Input**: Визуальное позиционирование прицела вдали от пульсирующего маркера союзников. Двойной клик/быстрый тап по кнопке '25mm' для мгновенного безопасного переключения при входе врагов в зону Danger Close.
- **Technical Complexity**: Medium
- **Решение игрока**: Стрелять ли тяжелым 105-мм/40-мм калибром по врагам, подошедшим вплотную к союзному спецназу, рискуя мгновенно уничтожить отряд дружественным огнем, или ювелирно вырезать врагов точечным 25-мм пулеметом за счет драгоценного времени.
- **Состояния**: `SAFE_CLEARANCE: союзники находятся на дистанции > 25 метров от точки прицеливания; HUD горит нейтральным белым цветом, все калибры разрешены.`, `DANGER_CLOSE_WARNING: точка прицеливания тяжелого калибра (40мм/105мм) находится в 10–24 метрах от отряда; на прицеле пульсирует красный индикатор 'DANGER CLOSE', радио возглас оператора 'Friendly footprint in splash zone!'.`, `FRIENDLY_FIRE_INCIDENT: снаряд 40мм/105мм сдетонировал в радиусе поражения союзников; мгновенная гибель бойцов, радиотишина с помехами, оперативный провал миссии.`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Радиус запрета огня 105-мм гаубицы (Danger Radius)` = `14.5 м от ИК-маяка Bravo-6` — Если уменьшить радиус ниже 8 м, опасность дружественного огня перестает ощущаться и игрок начинает бездумно спамить 105мм по своим; если увеличить свыше 22 м, стрелять в 3-й фазе становится некуда.
  - `Частота мерцания ИК-стробоскопа CID-01` = `2.0 Гц (2 вспышки в секунду)` — При частоте менее 1 Гц маяк теряется на фоне ночных пожаров и взрывов; при постоянном свечении без мерцания игрок путает спецназовцев с вражеской пехотой.
  - `Здоровье одного бойца спецназа` = `150 HP (выдерживает до 4 попаданий из АК-47)` — Если дать больше 300 HP, игрок перестает защищать союзников; если дать меньше 75 HP, спецназ погибает от случайной шальной пули за 0.5 с.
  - `Скорость движения отряда Bravo-6 на марше` = `4.2 м/с (при спринте: 6.5 м/с)` — Если замедлить ниже 2.5 м/с, миссия становится затянутой и скучной; если ускорить выше 8.0 м/с, баллистические снаряды 105мм отстают от темпа группы.
- **Слои отклика**:
  - Визуал/VFX: Нашлемные стробоскопы 4 бойцов спецназа вспыхивают ярко-белым тепловым импульсом с мягким ореолом; при наведении 105мм в опасную зону на земле проецируется пульсирующий пунктирный круг Danger Close с перечеркнутым черепом.
  - Звук (Web Audio): Предупреждающий прерывистый сигнал зуммера (частота 880 Гц / 440 Гц 'бип-бип'), тревожные доклады в радиоэфире командира спецназа: 'Bravo-6 to Spectre, you are Danger Close, watch your splash!'.
  - Камера (импульс/зум): Автоматическое легкое натяжение виньетки с красным хроматическим смещением по краям экрана при попадании прицела в зону Danger Close.
  - UI/Тактильность: Мерцающая надпись 'DANGER CLOSE: 12.4m' в центре HUD рядом с перекрестьем прицела; частая тактильная микро-вибрация смартфона (по 20 мс каждые 150 мс) как тактильный сигнал опасности.
- **Режим отказа игрока**: Игрок выпускает 105-мм снаряд в группу врагов, но спецназ успевает добежать до зоны поражения до момента приземления снаряда через 2.2 с. Сигнал: ослепительный взрыв накрывает стробоскопы, радиоканал заполняется белым шумом, экран покрывается статическими помехами с надписью 'MISSION FAILED: FRIENDLY FIRE CASUALTY'.
- **Кривая мастерства**: Новичок на 30-й секунде либо взрывает собственный отряд фугасом, либо боится стрелять тяжелым калибром и смотрит, как спецназ расстреливают враги. Мастер на 10-м забеге кладет 105-мм снаряд ровно в 15.5 метрах от головы бегущей колонны спецназа с идеальным расчетом упреждения, расчищая путь секунда-в-секунду без единой царапины союзникам.
- **Сопротивление игры**: Вражеские камикадзе-штурмовики и бронированные багги целенаправленно таранят позиции спецназа, сокращая дистанцию до 5–8 метров и вынуждая игрока отказываться от артиллерии.
- **Синергии**:
  - Связь с Механикой 4 (Термодинамика FLIR и подавление): игрок заливает зону контакта 25-мм очередью, подавляя штурмовиков, пока союзники разрывают дистанцию до безопасных 15 метров.
  - Связь с Механикой 1 (Калибры): мгновенное тактическое переключение на 25-мм пулемет как единственное безопасное хирургическое оружие в радиусе 10 метров.
- **Почему это не жанровый шаблон**: Friendly Fire в игре — это не банальный штраф по очкам, а фундаментальное пространственное ограничение физического сплеша оружия с отложенным баллистическим таймингом, создающее киношную драму 'на волоске от гибели'.
- **Псевдокод тика**:
```text
function checkDangerClose(impactPoint, alliesList) {
  for (let ally of alliesList) {
    let dist = impactPoint.distanceTo(ally.position);
    if (dist < currentCaliber.dangerRadius) {
      hud.setWarningState(true, dist);
      audio.playDangerWarningBeep();
      if (projectileExploded) {
        ally.applyDamage(calculateSplashDamage(dist));
        if (ally.health <= 0) triggerGameOver('FRIENDLY_FIRE');
      }
      return true;
    }
  }
  hud.setWarningState(false, 0);
  return false;
}
```

### Физическая цепная детонация и разрушение укрытий (Physics Chain Reactions & Structural Collapse) (SECONDARY)
- **Category**: Главный источник взрывного удовлетворения (награды), мультипликатор боевого счета и инструмент контроля толпы
- **Description**: Разрушаемость здесь — это не запеченная анимация, а честный физический расчет Three.js + Rapier3D, где обломки стен и ударные волны становятся органическим оружием игрока.
- **Player Input**: Наведение прицела на интерактивные объекты окружения (подсвечиваются горячим термоконтуром на FLIR) и выстрел калибром 40мм или 105мм.
- **Technical Complexity**: Medium
- **Решение игрока**: Стрелять ли напрямую во вражескую бронетехнику или поразить расположенную рядом топливную цистерну / опору моста / угол двухэтажного здания, вызвав обвал тонн бетона на головы врагов и цепную детонацию всего сектора.
- **Состояния**: `STRUCTURAL_INTACT: здание или топливный резервуар целы, имеют 100% запас прочности (HP_structure = 800–2000 ед).`, `CHAIN_IGNITING: прямое попадание снаряда пробивает корпус резервуара; начинается фаза утечки горящего топлива (0.4 с с нарастающим термо-свечением).`, `CATASTROPHIC_DETONATION: взрыв бензовоза/склада боеприпасов с генерацией мощной ударной волны Rapier3D (радиус 16м, урон 3500 ед) и разлетом 15–25 физических обломков.`, `STRUCTURAL_COLLAPSE: обрушение геометрии здания; падающие плиты наносят кинетический урон всей технике и пехоте под ними, блокируя улицу для подкреплений.`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Радиус вторичной детонации топливной цистерны` = `16.5 м (Урон 3500 ед)` — Если сделать больше 22 м, взрыв цистерны уничтожит полкарты включая спецназ; если сделать меньше 10 м, цепные реакции потеряют зрелищность и тактический смысл.
  - `Физическая сила взрывного импульса (Rapier3D Impulse)` = `85000 Н*с для техники, 14000 Н*с для рэгдоллов` — При занижении техника просто исчезает на месте без ощущения мощи; при завышении обломки улетают за пределы скайбокса на первой миллисекунде.
  - `Количество физических осколков разрушения (Debris chunks)` = `18 физических мешей на Three.js инстансинге` — Оптимальный баланс между кинематографичностью и стабильными 60 FPS на мобильных GPU (Mali/Adreno/WebGL).
  - `Множитель очков за комбо цепной реакции (Chain Combo)` = `x1.5 за каждый последующий сдетонировавший объект в цепочке (до x5.0)` — Стимулирует игрока искать неочевидные взрывные связки вместо унылого расстрела врагов поодиночке.
- **Слои отклика**:
  - Визуал/VFX: Взрыв резервуара окрашивает тепловизор в экстремальный белый цвет с радиальным разлетом раскаленных белых точек-осколков; обрушение фасада здания порождает густое облако объемной термопыли в Three.js с физической осыпью обломков.
  - Звук (Web Audio): Глубокий резонирующий взрыв с металлическим скрежетом рвущихся баков, треск ломающихся железобетонных перекрытий, звуки вторичных хлопков взрывающихся патронов и восторженный крик наводчика: 'Whole building coming down, multiple targets confirmed!'.
  - Камера (импульс/зум): Горизонтально-вертикальный импульс сотрясения камеры (шейк 0.42° длительностью 0.6 с) с кратковременным падением резкости термооптики (имитация расфокусировки линз от ударной волны).
  - UI/Тактильность: Всплывающий анимированный баннер 'CHAIN REACTION x3: +1500 PTS', серия из трех быстрых мощных тактильных вибраций (60 мс каждая) с интервалом в 100 мс.
- **Режим отказа игрока**: Игрок подрывает цистерну рядом с узким проходом, когда спецназ уже начал в него входить — ударная волна цистерны сносит союзников. Сигнал: телеметрия фиксирует причину поражения 'ENVIRONMENTAL_FRIENDLY_FIRE'.
- **Кривая мастерства**: Новичок на 30-й секунде игнорирует окружение и тратит 3 снаряда 105мм на 3 отдельных БТР. Эксперт на 10-м забеге одним выстрелом 40-мм автопушки в бочку с горючим запускает каскадный подрыв заправки, уничтожающий колонну из 4 бронемашин и перекрывающий улицу обломками.
- **Сопротивление игры**: Вражеская бронетехника на третьей фазе объезжает взрывоопасные объекты по широким дугам, а снайперы занимают укрепленные железобетонные бункеры, требующие точного попадания 105мм прямо в амбразуру.
- **Синергии**:
  - Связь с Механикой 1 (Калибры): легкий 40-мм снаряд идеально подходит для дистанционного подрыва цистерн без траты 105-мм гаубицы.
  - Связь с Механикой 4 (Подавление): обрушенные стены создают постоянные физические баррикады в Rapier3D, через которые пехота врага не может вести огонь по отряду Bravo.
- **Почему это не жанровый шаблон**: Разрушаемость здесь — это не запеченная анимация, а честный физический расчет Three.js + Rapier3D, где обломки стен и ударные волны становятся органическим оружием игрока.
- **Псевдокод тика**:
```text
function onExplosionImpact(epicenter, blastPower, blastRadius) {
  let nearbyPhysicsBodies = physicsWorld.querySphere(epicenter, blastRadius);
  for (let body of nearbyPhysicsBodies) {
    let dir = body.position.clone().sub(epicenter);
    let dist = dir.length();
    let forceMagnitude = blastPower / Math.max(1.0, dist * dist);
    body.applyImpulse(dir.normalize().multiplyScalar(forceMagnitude));
    if (body.isExplosive && !body.isDetonated) {
      scheduleDelayedDetonation(body, 0.25);
    }
    if (body.isDestructible) {
      body.takeStructuralDamage(blastPower * 0.8);
    }
  }
  vfx.spawnThermalShockwave(epicenter, blastRadius);
}
```

### Термодинамика FLIR и подавление живой силы (FLIR Thermal Contrast & Kinetic Suppression) (SECONDARY)
- **Category**: Тормоз/регулятор темпа стрельбы и механика тактического сдерживания противника
- **Description**: Подавление огнем превращает пулемет из примитивного инструмента нанесения урона в тактический щит для союзного отряда, моделируя реальную тактику непосредственной авиационной поддержки CAS (Close Air Support).
- **Player Input**: Удержание кнопки/зоны огня для непрерывной очереди; отпускание гашетки для принудительного охлаждения ствольного блока. Тап по переключателю тепловых палитр 'W-HOT / B-HOT' для смены контраста в дыму.
- **Technical Complexity**: Medium
- **Решение игрока**: Поливать ли сектор непрерывным огнем 25-мм пулемета для 100% подавления вражеских гранатометчиков (не давая им прицелиться в спецназ), рискуя словить заклинивание от перегрева ствола (Jam Lockout), или стрелять короткими расчетными отсечками.
- **Состояния**: `COOL_NOMINAL: температура стволов 20–60°C; максимальная кучность стрельбы (разброс 0.15°), стандартный термоконтраст FLIR.`, `HIGH_HEAT_WARNING: температура 61–99°C; стволы раскалены докрасна, разброс увеличивается до 0.45°, звучит предупредительный сигнал температурного датчика.`, `OVERHEAT_JAM: температура достигла 100°C; орудие блокируется на 3.5 с; оператор вынужден ждать сброса тепла или переключаться на другие калибры.`, `ENEMY_SUPPRESSED: вражеский отряд пехоты под воздействием близких разрывов 25мм (дистанция < 4м) падает ничком, меткость снижается на 90% на 3.0 секунды.`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Прирост температуры за 1 выстрел 25мм` = `1.85°C за выстрел (перегрев за 43 непрерывных выстрела ~ 1.4 с огня)` — Если сделать более 3.0°C, пулемет перегревается моментально и раздражает игрока; если сделать менее 0.8°C, механика перегрева теряет смысл и превращается в зажатую гашетку.
  - `Скорость охлаждения ствольного блока GAU-12` = `18.0°C / сек в покое` — При занижении ниже 10°C/с игрок слишком долго скучает без пулемета; при завышении выше 40°C/с перегрев сбрасывается мгновенно.
  - `Радиус зоны подавления пули 25мм` = `3.8 м от точки попадания пули` — Позволяет эффективно прижимать к земле группы пехоты даже без прямого попадания в силуэт.
  - `Длительность эффекта подавления пехоты (Suppression duration)` = `2.8 с после прекращения обстрела` — Дает союзному отряду Bravo ровно столько времени, чтобы безопасно пробежать простреливаемый перекресток.
- **Слои отклика**:
  - Визуал/VFX: По мере нагрева 25-мм блока пушка на HUD раскаляется от белого к ярко-оранжевому свечению; при стрельбе трассеры выбивают фонтаны термопыли; подавленная пехота падает на живот и закрывает голову руками (анимация 'hit_the_deck').
  - Звук (Web Audio): Непрерывный низкий гул вращения блока стволов Гатлинга (звук 'BRRRRT'), при критическом нагреве — нарастающий высокочастотный вой перегретого ротора и щелчок защитного соленоида при заклинивании.
  - Камера (импульс/зум): Высокочастотный мелкий дрожащий зуд камеры (шейк амплитудой 0.08° на частоте 30 Гц) при зажатой гашетке пулемета.
  - UI/Тактильность: Вертикальный секторный датчик HEAT GAU-12 с градиентным заполнением; непрерывная нарастающая по частоте виброотдача на мобильном устройстве (от 10 Гц до 50 Гц по мере приближения к 100°C).
- **Режим отказа игрока**: Игрок зажимает гашетку пулемета, ловит перегрев стволов на 100°C, в этот момент из-за угла выбегает гранатометчик РПГ-7 и стреляет по спецназу, пока игрок беспомощно ждет 3.5 секунды охлаждения. Сигнал: звуковой сигнал тревоги 'GUN OVERHEAT, LOCKOUT ACTIVE'.
- **Кривая мастерства**: Новичок на 30-й секунде постоянно ловит клин пулемета от паники. Эксперт стреляет четкими микро-отсечками по 4–6 патронов (0.15 с), удерживая температуру в идеальном диапазоне 50–75°C и одновременно подавляя 3 огневые точки на разных крышах.
- **Сопротивление игры**: Элитные снайперы и расчеты тяжелых пулеметов противника в бетонных дотах обладают 50% резистом к подавлению и возобновляют огонь уже через 1.0 с после окончания очереди.
- **Синергии**:
  - Связь с Механикой 1 (Калибры): во время 4.5-секундной перезарядки 105-мм гаубицы игрок удерживает поле боя под контролем короткими очередями 25мм.
  - Связь с Механикой 2 (Danger Close): подавление — главный инструмент спасения спецназа в ближнем бою, когда взрывные калибры применять запрещено.
- **Почему это не жанровый шаблон**: Подавление огнем превращает пулемет из примитивного инструмента нанесения урона в тактический щит для союзного отряда, моделируя реальную тактику непосредственной авиационной поддержки CAS (Close Air Support).
- **Псевдокод тика**:
```text
function updateGatlingSuppression(dt, isFiring, hitPoint) {
  if (isFiring && !gatling.isJammed) {
    gatling.heat += gatling.heatPerShot * (dt * 30);
    if (gatling.heat >= 100.0) {
      gatling.isJammed = true;
      audio.playJamAlarm();
    }
    suppressEnemiesInRadius(hitPoint, 3.8, 2.8);
  } else {
    gatling.heat = Math.max(0, gatling.heat - gatling.coolingRate * dt);
    if (gatling.isJammed && gatling.heat <= 30.0) {
      gatling.isJammed = false;
    }
  }
  hud.updateHeatGauge(gatling.heat, gatling.isJammed);
}
```


---

## 3b. ⚠️ СТРОГИЙ ЗАПРЕТ ЖАНРОВЫХ ШАБЛОНОВ И КЛОНОВ (CRITICAL ANTI-CLICHÉ RULES)
Кодовый агент ОБЯЗАН реализовать уникальную игру, спроектированную в этом ТЗ, а не шаблонный автошутер:
1. **ЗАПРЕТ ШАБЛОННЫХ РОГАЛИКОВ И КАРТОЧЕК**: Если игра прямо не требует карточный драфт в GDD, **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** добавлять спам волн врагов и всплывающее окно «Выберите 1 из 3 карт апгрейда».
2. **ЗАПРЕТ СЕРЫХ ПРИМИТИВОВ НА ПУСТОЙ ПЛОСКОСТИ**: Создавай выразительную процедурную низкополигональную 3D/2D графику, точно соответствующую `ART_DIRECTION.md` (тематические персонажи, рельеф, модули, объекты окружения, частицы), а не бегающий куб на сером полу.
3. **ТОЧНОЕ СОБЛЮДЕНИЕ МЕХАНИК**: Реализуй все состояния, формулы, тайминги и слои отклика из `MECHANICS.md` (включая Web Audio звуки, импульсы камеры, визуал и тач-инпут).
4. **СПЕЦИФИЧЕСКОЕ УПРАВЛЕНИЕ**: Реализуй схему управления под конкретную механику этой игры (свайпы, жесты, траектории, физический дрифт, прицеливание), а не стандартный стик.

---

## 3c. ГОТОВОЕ: ЧТО УЖЕ НАПИСАНО И ОТЛАЖЕНО — SHOULD

Ниже — рабочий код фабрики: не примеры из статьи, а модули, которые крутились в
живых сценах, и в которых числа выставлены не наугад. Каждый файл объясняет в
своей шапке, почему константы именно такие — это та часть работы, которую с
нуля не воспроизвести за один заход.

**Правило.** Прежде чем писать механику с нуля, посмотри, нет ли её здесь.
Нашёл — тяни файл и подгоняй под свою игру. Не нашёл или не подходит — пиши
сам, но запиши в `DEVLOG.md` строку «взял/не взял <файл>, потому что …».
Молча проигнорировать готовое нельзя: это стоило нам рэгдолла, который писали
заново, хотя он лежал написанным.

Загрузка любого файла — одной командой, путь берётся из таблицы:

```bash
node scripts/fetch-knowledge.mjs workspace/knowledge-showcase/src/game/stealthSensing.ts
```

Файл появится в `docs/ref/<путь>`, оттуда копируй в `src/`.

Колонка «Готовность» — это часы работы, а не оттенок смысла:
- **копируется как есть** — ни одного импорта, чистая логика и числа. Взял, переименовал, поехал.
- **нужен three, больше ничего** — тянет только `three`, чужого проекта в нём нет.
- **образец, переписать под себя** — тянет модули стенда. Читать как показ того, как это собирается, а не копировать.

**Похоже, пригодится именно этой игре:**

| Файл | Что даёт | Строк | Готовность |
|---|---|---|---|
| `workspace/knowledge-showcase/src/game/meleeCombat.ts` | Ближний бой: комбо-цепочка, окна отмены, парирование, hit-stop | 330 | копируется как есть |
| `workspace/knowledge-showcase/src/game/flowField.ts` | Флоу-филд и строй для RTS | 283 | копируется как есть |
| `workspace/knowledge-showcase/src/game/survivorRun.ts` | Забег в survivor-игре: кривая опыта, карточки апгрейдов, эскалация орды | 279 | копируется как есть |
| `workspace/knowledge-showcase/src/game/fluidPhysics.ts` | Fluid Buoyancy, Mining Drill & Physics Destruction logic module | 244 | копируется как есть |
| `workspace/knowledge-showcase/src/game/towerDefense.ts` | Tower defense: контракт волн, приоритет целей, экономика | 225 | копируется как есть |
| `workspace/knowledge-showcase/src/game/arcadeCar.ts` | Аркадная модель машины — общая для игрока и соперников | 201 | копируется как есть |
| `workspace/knowledge-showcase/src/game/stealthSensing.ts` | Стелс: конус зрения, шкала подозрения, шум | 145 | копируется как есть |
| `workspace/knowledge-showcase/src/input/TouchControls.ts` | TouchControls (input) — экспортирует TouchControls | 138 | копируется как есть |
| `workspace/knowledge-showcase/src/game/raceTrack.ts` | Трасса: одна кривая — один источник истины | 208 | нужен three, больше ничего |
| `workspace/knowledge-showcase/src/demos/TruckDemo.ts` | Эталонная вкладка стенда: ЗиЛ-130 на честной физике Rapier3D | 101 | образец, переписать под себя |

Это не весь каталог: всего готовых модулей 67. Полный список с описаниями — в `LIBRARY.md` рядом с этим файлом. Загляни туда, когда возьмёшься за механику, которой нет в таблице выше.

---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Core / ECS Layer**: 
- **Rendering & Shader Layer**: 
- **Physics & Collision Layer**: 
- **Audio & Radio Layer**: 
- **Platform & Bridge Layer**: 

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   └── PhysicsWorld.ts        # Rapier3D / Physics world manager & colliders
├── entities/
│   ├── Player.ts              # Player entity & input handling
│   └── EntityManager.ts       # Dynamic entity pool & lifecycle
├── systems/
│   ├── OrbitalBallisticsSystem.ts # Расчет траекторий, времени подлета (1.2–2.5 с
│   ├── FlirThermalVisionSystem.ts # Отрисовка монохромной высококонтрастной карти
│   ├── FriendlyEscortAiSystem.ts # Управление движением 4 бойцов отряда «Браво» 
│   ├── DestructionPhysicsSystem.ts # Симуляция кинетических импульсов, взрывных во
├── rendering/
│   ├── SceneManager.ts        # Three.js scene graph, lights, perspective camera lerp
│   ├── ProceduralModels.ts    # Styled models / geometry for AC-130: Ночной Тепловизор
│   └── ParticleSystem.ts      # Particle effects & visual feedback
├── ui/
│   ├── theme.css              # Единственное место со значениями: цвета, шрифты, шкала, слои
│   ├── UiRoot.ts              # Слои над канвасом, измеренный вьюпорт, safe-area + баннер
│   ├── ScreenRouter.ts        # Экраны как автомат: один видимый, один общий переход
│   ├── Hud.ts                 # Запись в закэшированные узлы только при изменении значения
│   ├── components/            # Button, IconButton, Panel, Modal, Toast, Meter, Stat, ListRow
│   ├── screens/               # По файлу на экран, каждый — со своими loading/empty/error
│   ├── icons.ts               # Один инлайновый SVG-спрайт, цвет через currentColor
│   └── TouchControls.ts       # Mobile touch input adapter
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic audio feedback
```

### Контракт модулей — MUST

Карта выше говорит, какие файлы завести. Ниже — чего им нельзя, потому что
именно на этом ломаются сгенерированные проекты:

- **DOM создаётся только в `src/ui/`.** Ни один файл вне `src/ui/components/` и
  `src/ui/screens/` не вызывает `document.createElement` и не присваивает
  `element.style.*`. Экран, собранный инлайновыми стилями в обход `theme.css`, —
  это второй экран, который перестанет совпадать с первым.
- **Значения — только в `src/ui/theme.css`.** Проверяется **B1**.
- **`src/core/` не знает про площадку.** Playgama Bridge живёт в
  `src/platform/`; всё остальное общается с ним через свой интерфейс. Иначе
  игру нельзя запустить локально без SDK.
- **`src/ui/` не знает про физику и рендер.** Интерфейс читает состояние
  через `EventBus` и не держит ссылок на `THREE.*` и на мир Rapier.
- **Числа игры — из `balance.yaml`.** Литерал скорости, урона, тайминга или
  радиуса в коде — баг: правка баланса не должна быть правкой кода.
- **Цикл обновления не аллоцирует.** Ни `new`, ни литерала объекта, ни
  `array.map` в кадре: пулы заводятся заранее. Проверяется **E4**.
- **Каждый модуль экспортирует один публичный класс или функцию** и не тянет
  импорт из соседнего слоя мимо своего интерфейса.

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Экстренная Реанимация Спецназа (`revive_squad`)**: Воскрешает погибших бойцов отряда 'Браво' с полным здоровьем и вызывает заградительный дымовой экран. (Trigger: Экран поражения при гибели всех бойцов спецназа., Limit: 1 раз за операцию)
- **Удвоение Боевых Кредитов (`double_credits`)**: Удваивает полученные за операцию тактические кредиты и боевой опыт. (Trigger: Экран победы и подсчета боевого рейтинга., Limit: После каждой успешной операции)
- **Армейский Контейнер Снаряжения (`supply_crate`)**: Мгновенно выдает 500 тактических кредитов и случайный чертеж улучшения орудий. (Trigger: Кнопка в главном меню / окне арсенала., Limit: 1 раз в 10 минут)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"player_credits"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Landscape
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Визуальный контракт интерфейса
Интерфейс — первое, что видит игрок: до геймплея он смотрит на меню. Меню,
собранное из умолчаний браузера, обесценивает всё, что сделано за ним, и это
самый заметный признак недоделанной игры. Всё ниже — требование, а не пожелание.

**Материал интерфейса**: Аутентичный военный прицельный комплекс бортового стрелка AC-130: зеленые цифровые индикаторы высоты, азимута, перекрестия калибров и шкалы перезарядки

**Визуальный язык**: Военный оптико-электронный интерфейс конца 2000-х / начала 2010-х годов: векторные зеленые маркеры, моноширинный технический шрифт, цифровые угловые шкалы, перекрестия с рисками упреждения и монохромная тепловая картинка White-Hot.

**Типографика**: Моноширинный технический шрифт 'Share Tech Mono' / 'Consolas' для HUD и телеметрии; строгий гротеск 'Oswald' / 'Roboto Condensed' для заголовков экранов.

**Сцена за меню.** Камера медленно облетает фюзеляж тяжелого AC-130 на аэродроме под ночным дождем в свете прожекторов, в кабине горят зеленые приборные панели.

Меню, пауза и итог сессии рисуются ПОВЕРХ этой сцены. Непрозрачный слой на весь экран (`background: #111` на корне экрана, полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка допускается только под текстовым блоком и кнопками. Игровой цикл в меню продолжает крутиться на сниженной нагрузке — камера едет, сцена живёт. Игрок обязан понять, во что он играет, до нажатия «Играть»; меню на глухой заливке — самый заметный признак недоделанной игры, и это первое, что увидит и игрок, и модератор площадки.

**Акценты — один цвет = один смысл** (разный цвет у каждой кнопки «чтобы отличались» читается как мусор):

| Токен | Единственный смысл |
|---|---|
| `danger_alert` | #FF3333 (красный цвет предупреждения Friendly Fire и опасности) |
| `primary_hud` | #4AF626 (зеленый люминесцентный цвет интерфейса) |
| `thermal_cold` | #1A1D20 (темно-серый цвет холодных статичных объектов) |
| `thermal_hot` | #FFFFFF (белый цвет максимальной температуры целей и взрывов) |

**Набор компонентов — закрытый.** Всё на экране обязано быть одним из них; одноразовый `<div>` с инлайновыми стилями — это баг, из-за которого второй экран перестаёт совпадать с первым:
- DynamicCrosshair (адаптивная сетка прицела с дальномерными рисками)
- CaliberButton (кнопка выбора оружия с радиальной шкалой перезарядки)
- RadioTranscriptBox (плашка радиопереговоров с эффектом печатной машинки)
- SquadStatusCard (индикатор количества живых союзников и их здоровья)
- ComboCounterBadge (анимированный бейдж серии попаданий с множителем)

**Якоря HUD** — пять и только пять; посередине экрана не висит ничего, кроме временной обратной связи:

| Якорь | Что там |
|---|---|
| `bottom_left` | Окно расшифровки радиопереговоров штаба и спецназа |
| `bottom_right` | Блок кнопок управления калибрами (25/40/105мм) и кнопка зума |
| `center` | Прицельная марка с индикаторами упреждения |
| `top_center` | Таймер обратного отсчета спецоперации (90с) |
| `top_left` | Телеметрия полета (высота, скорость, азимут) |
| `top_right` | Индикатор состояния спецназа и кнопка паузы |

**Экраны и их композиция.** У каждого экрана три зоны: чем он себя называет, единственное главное действие (самое крупное и единственное с основным акцентом) и второстепенный ряд одним весом. Карточка с колонкой кнопок по центру — не композиция, а её отсутствие:
- **ScreenMainMenu** · Главное меню, выбор спецоперации, переход в ангар/арсенал, настройки звука и чувствительности. · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **ScreenArmory** · Модернизация систем 25мм, 40мм и 105мм орудий за тактические кредиты. · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **ScreenBattleHUD** · Основной боевой экран: тепловизор, прицел, выбор калибров, радиоэфир, статус спецназа. · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **ScreenVictory** · Отчет об успешной эвакуации: спасенные бойцы, точность, комбо, начисление кредитов, кнопка удвоения за рекламу. · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **ScreenDefeat** · Отчет о провале (гибель отряда / Friendly Fire / тайм-аут), предложение реанимации за Rewarded Video. · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

**Состояние, показанное миром, а не оверлеем**:
- Цифровые маркеры прицела и угловые шкалы, имитирующие реальный экран наводчика AC-130
- Аутентичные помехи тепловизора и искажения линзы при переключении зума
- Радиошум и треск рации при воспроизведении реплик экипажа

**Переходы экранов**: MainMenu -> Armory (модернизация) -> MissionBriefing -> BattleHUD -> VictoryScreen / DefeatScreen -> MainMenu

**Отклик интерфейса на действие**:
- При попадании 105мм: экран озаряется белой термо-вспышкой, радио сообщает 'Direct hit!', счетчик комбо пульсирует увеличенным масштабом.
- При наведении на союзников: прицел окрашивается в красный цвет и начинает пульсировать надпись 'NO FIRE'.
- При перегреве 25мм пулемета: звучит предупреждающий зуммер, иконка мигает оранжевым с надписью 'OVERHEAT'.

**Состояния экранов** — загрузка, пустота и ошибка проектируются наравне с удачным путём: сохранение, таблица лидеров, покупки и реклама на этих площадках отваливаются регулярно:
- Загрузка сцены с анимированным тепловым калибровочным тестом
- Активный бой с полным отображением телеметрии
- Пауза с затемнением и меню настроек
- Экран победы со сводкой боевой эффективности
- Экран поражения с возможностью ревайва за рекламу

**Значения выводятся из этого мира, а не берутся из примеров.** Имена токенов (`--color-primary`, `--font-display`, `--space-*`) одинаковы во всех играх — на них держится код компонентов. Сами значения, обе гарнитуры, силуэт рамки и язык иконок выводятся из материала интерфейса, названного выше, по процедуре из раздела 12 `knowledge/ux/ui_design_system.md`: из чего сделана панель, при каком свете она лежит, чем в этом мире помечают главное и опасное, чем написаны цифры, как режут край. Скопировать палитру, шрифты или фаску из примера в базе знаний или из другой игры — это тот же дефект, что и оставить умолчания браузера: интерфейс выглядит решением, принятым не для этой игры. Обратная проверка: если от интерфейса другой игры фабрики ваш отличается только оттенком акцента — процедура была пропущена.

**Обязательные технические правила интерфейса** (каждое — из починенного бага, подробности в `knowledge/ux/ui_design_system.md` и `knowledge/ux/ui_implementation.md`):
- Все значения — токенами в одном `src/ui/theme.css`: цвета, две гарнитуры, шкала отступов, радиусы, длительности, `--z-*`. Литерал `#RRGGBB` или `z-index: 9999` внутри экрана запрещён; `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` мимо темы обязан быть пуст.
- Слои над канвасом прозрачны для ввода: контейнер — `pointer-events: none`, `auto` включают только листовые интерактивные элементы. Слой HUD не кликается никогда. Полноэкранный оверлей, оставленный на `auto`, съедает игровой ввод — и это выглядит как «на телефоне не работает управление».
- Интерфейс тянется одним множителем `--ui-scale`, а не отдельной вёрсткой под каждый телефон, — но множитель обязан где-то **читаться**. Он объявляется в `theme.css` брейкпоинтами (`:root { --ui-scale: 1 }`, `@media (max-width: 720px)` → `0.86`, `@media (max-width: 420px)` → `0.74`) и умножает отступы и кегли: `padding: calc(var(--space-4) * var(--ui-scale))`, `font-size: clamp(14px, calc(16px * var(--ui-scale)), 18px)`. Посчитать масштаб в JS и не использовать его ни в одном правиле — ровно тот же результат, что не считать вовсе: интерфейс, свёрстанный в фиксированных `px`, на 360-пиксельном экране разъезжается. Ноль `@media` в `theme.css` — признак, что этот пункт пропущен.
- Портрет и ландшафт — два разных экрана, а не один и тот же в другом масштабе. Вертикальный FOV камеры пересчитывается под аспект (иначе в портрете горизонтальный обзор схлопывается в трубу), а раскладка HUD и кнопок проверяется в обеих ориентациях.
- Размеры зон нажатия НЕ масштабируются: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px — палец не уменьшается вместе с экраном. Ниже 460 px по высоте кнопки допускается ужать до 56 px, и это единственное исключение.
- Меняющиеся числа — `font-variant-numeric: tabular-nums` в слоте фиксированной ширины; полосы — `transform: scaleX()`, не `width`. HUD пишет в закэшированные узлы и только при изменении значения: пересборка разметки в кадре стоит кадров.
- Один экран виден за раз, скрытый — `display: none`; переход один на всю игру и укладывается в 300 мс. Анимируются только `transform` и `opacity`. `prefers-reduced-motion: reduce` убирает трансформации.
- Любое нажатие отвечает в том же кадре; действие с ожиданием переводит кнопку в `loading` и снимает его в `finally`.
- Запрещено: `alert`/`confirm`/`prompt`, эмодзи вместо иконок (один инлайновый SVG-спрайт с `currentColor`), голый `<input type=range>` и `<select>`, текст над геймплеем без подложки или обводки, серая неактивная кнопка вместо отсутствующей на площадке возможности — такой элемент не рисуется вовсе.
- Геометрия меню считается от ИЗМЕРЕННОГО вьюпорта (`visualViewport`, пересчёт после поворота, выхода из фуллскрина и закрытия рекламы), а не от `100vh`; нижний отступ включает измеренную высоту липкого баннера. Страница под игрой не скроллится — длинные списки скроллятся во внутреннем контейнере.
- `backdrop-filter: blur()` на полноэкранном слое — самое дорогое свойство оверлея на мобильных GPU; только на маленьких панелях.

### Контракт тач-управления
- **Раскладка этой игры** (спроектирована в MOBILE_CONTROLS.md, переносить сюда раскладку из другой игры нельзя): Свайп одним пальцем в любой точке экрана — свободное наведение прицела; правый нижний угол — 3 крупные круглые кнопки калибров (25мм / 40мм / 105мм) для мгновенной стрельбы; правый край экрана — кнопка зума (1x/2x/4x); левый верхний угол — кнопка паузы.
- Раскладка выводится из главного действия игрока. Виртуальный джойстик уместен там, где игрок непрерывно ведёт персонажа; там, где действие точечное (тап по цели, свайп, удержание), джойстик — лишний элемент, который занимает половину экрана и ничего не делает.

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Если в раскладке есть стик** — он плавающий: зона захвата — вся левая
  половина экрана, база появляется под пальцем, мёртвая зона 8%, иначе
  управление дрожит. Если раскладка обходится без стика — не добавляйте его.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `WASD` / стрелки — движение
- Мышь — обзор через `requestPointerLock` (захват просится из обработчика нажатия, иначе браузер его не даст)
- ЛКМ — огонь: автоматическое оружие стреляет **пока кнопка удерживается**, полуавтомат — по событию нажатия
- ПКМ — прицеливание
- `R` — перезарядка, `Q` / колесо / `1`–`3` — смена оружия
- `Space` — прыжок, `Ctrl` — присед, `Shift` — тихий шаг (любую из трёх можно не делать, но тогда см. правило о вертикали ниже)
- `P` / `Esc` — пауза

**Раскладка выше — отправная точка под похожие игры, а не список того, что умеет ЭТА игра.** Профилей в фабрике меньше, чем жанров, и любой шаблон заведомо описывает не вашу игру: в нём есть лишние клавиши и нет нужных. Источник истины — глаголы игрока, перечисленные ниже.

**Контракт: каждый глагол получает и клавишу, и кнопку.** Таблицу «глагол → клавиша → тач-кнопка → что происходит на экране и в звуке» положи в `DESIGN.md` до первой строчки кода по управлению. Глагол без строки в таблице не реализован; клавиша из шаблона, которой в таблице нет, — удаляется.

**Отдельно решается то, чего в списке механик обычно нет, но игрок ждёт по виду уровня.** Вертикаль (прыжок, присед, тихий шаг), взаимодействие с предметом, отмена действия. Каждое — либо сделано (вертикаль — это гравитация, опора лучом по геометрии и высота глаз от ступней, а не константа пола), либо осознанно не сделано, и тогда в `DESIGN.md` стоит строка почему, **а уровень не обещает того, чего игрок не может**: без прыжка на карте нет ящиков, выступов и укрытий по пояс, к которым тянет забраться.

**Глаголы игрока этой игры** (каждому нужна строка в таблице управления):
- **Баллистическое жонглирование тройным калибром (Triple-Caliber Orbital Ballistics)** — игрок делает это так: Клавиатура: Клавиши 1, 2, 3 — мгновенный выбор калибра; Пробел / ЛКМ — стрельба; Колесо мыши — оптический зум 2x/4x/8x. Мобильный тач: Крупные боковые плашки выбора калибров '25mm', '40mm', '105mm' под правый палец; тап/удержание правой зоны экрана — ведение огня; свайп левой зоны — наведение турели.
- **Протокол Danger Close и ИК-стробоскопы Bravo-6 (Danger Close & IR Beacon Protocol)** — игрок делает это так: Визуальное позиционирование прицела вдали от пульсирующего маркера союзников. Двойной клик/быстрый тап по кнопке '25mm' для мгновенного безопасного переключения при входе врагов в зону Danger Close.
- **Физическая цепная детонация и разрушение укрытий (Physics Chain Reactions & Structural Collapse)** — игрок делает это так: Наведение прицела на интерактивные объекты окружения (подсвечиваются горячим термоконтуром на FLIR) и выстрел калибром 40мм или 105мм.
- **Термодинамика FLIR и подавление живой силы (FLIR Thermal Contrast & Kinetic Suppression)** — игрок делает это так: Удержание кнопки/зоны огня для непрерывной очереди; отпускание гашетки для принудительного охлаждения ствольного блока. Тап по переключателю тепловых палитр 'W-HOT / B-HOT' для смены контраста в дыму.

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/sozday_igru_po_tipu_call_of_duty/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ДИЗАЙН: ЕГО ПИШЕШЬ ТЫ — MUST

Готового арт-дирекшена в этом пакете нет намеренно. Дизайн, продиктованный
списком полей, получается одинаковым от игры к игре, и узнать в нём конкретную
игру невозможно. Поэтому первым делом, до кода, ты пишешь `DESIGN.md` сам.

**Что в нём должно быть** (без этих пяти пунктов файл не считается написанным):

1. **Одна фраза о том, как игра выглядит.** Не «мрачный киберпанк», а что
   именно видит игрок в первую секунду.
2. **Палитра** — 5–7 цветов с ролями (фон, поверхность, текст, акцент, опасность,
   успех) и HEX. Они же становятся токенами в `theme.css`.
3. **Камера и свет** — где стоит камера, что она захватывает, откуда светит и
   почему сцена читается на телефоне.
4. **Экраны** — для каждого: что на нём, где три зоны (заголовок, содержимое,
   действие), какое действие главное.
5. **Сцена за меню** — что живёт на фоне главного меню. Не заливка, не блюр
   поверх ничего: работающая сцена игры под пониженной нагрузкой.

**Чего в нём быть не должно** — это уровень **MUST**, проверяется приёмкой:
эмодзи вместо иконок, `alert()` вместо модалки, непрозрачная плашка поверх
канваса, фиолетово-синий градиент, интерфейс из умолчаний браузера.

**Отправная точка от фабрики** — это подсказка, а не приговор. Не подошло —
меняй и запиши в `DEVLOG.md`, что и почему:

- Стиль: Монохромный FLIR White-Hot Милитаризм
- Камера: Орбитальная камера на высоте 1000м с наклоном 50° вниз, медленно кружащая вокруг эпицентра боя (FOV 45°, наклон 50°)
- Среда: Ночная ближневосточная промзона и кварталы разрушенного города: склады, бетонные заборы, узкие улицы, остовы сгоревшей техники
- Свет: Глобальное ночное рассеянное освещение низкой интенсивности с акцентом на термо-эмиссионные источники (тела людей, двигатели машин, вспышки взрывов)
- Тема интерфейса: Аутентичный военный прицельный комплекс бортового стрелка AC-130: зеленые цифровые индикаторы высоты, азимута, перекрестия калибров и шкалы перезарядки (реализация — секция 6)
- Сцена за меню: Камера медленно облетает фюзеляж тяжелого AC-130 на аэродроме под ночным дождем в свете прожекторов, в кабине горят зеленые приборные панели.

Подробности отбора — в `ART_DIRECTION.md` и `UI_UX_SPECIFICATION.md` рядом.

---

## 8. РОАДМАП

Фаза закончена не тогда, когда код написан, а когда закрыты её проверки из
`ACCEPTANCE.md`. Прогоняй `node scripts/check-spec.mjs` в конце каждой фазы.

| Фаза | Чем закрывается |
|---|---|
| Подготовка | **F4**, **F1** — база загружена в `docs/ref/`, `DESIGN.md` написан |
| Каркас и площадка | **A1**, **A3**, **C1–C4** — игра собирается и стартует на площадке |
| Главная механика | **D1**, **B6** — в неё можно играть, ввод доходит до игры |
| Остальные механики и петля | **D2–D5**, **B9–B11** — петля проходится целиком |
| Интерфейс и подача | **B1–B5**, **B7–B8**, **B12** — интерфейс собран из токенов, меню стоит на живой сцене |
| Полировка и релиз | **C5–C11**, **E1–E5**, **A2**, **A4**, **F2–F3** — сохранения, реклама, кадры, сборка |

### Phase 1: Орбитальное Ядро и FLIR Шейдер (2 days)
- **Deliverable**: Рабочий прототип прицеливания и стрельбы тремя калибрами по мишеням в режиме ночного тепловизора.
  - Настройка сцены Three.js с орбитальной камерой на высоте 1000м и углом 50 градусов
  - Разработка кастомного шейдера постобработки FLIR White-Hot с термо-эмиссией и шумом
  - Реализация прицельного комплекса с баллистическим расчетом подлета снарядов 25/40/105мм
  - Интеграция сенсорного свайп-управления и мыши
### Phase 2: Физика Разрушений, Цепные Взрывы и ИИ Спецназа (3 days)
- **Deliverable**: Полноценный геймплейный цикл спецоперации с прикрытием спецназа и разрушаемым окружением.
  - Интеграция Rapier3D: сплеш-урон, импульсы взрывов, рэгдоллы пехоты и разрушаемые стены
  - Создание цепных реакций детонации техники и взрывоопасных объектов
  - Реализация ИИ союзного спецназа с маршрутом движения и зоной Danger Close
  - Система Friendly Fire и проверка условий победы/поражения
### Phase 3: HUD, Радиопереговоры, Звуковой Дизайн и Сок (2 days)
- **Deliverable**: Кинематографичная готовая боевая миссия с полным аудиовизуальным погружением.
  - Создание аутентичного военного HUD с индикаторами калибров, таймером и комбо-счетчиком
  - Озвучка орудийных выстрелов, взрывов и интеграция радиореплик экипажа AC-130
  - Полировка визуала: CRT-сканлайны, частицы дыма, тепловое свечение от эпицентров взрывов
  - Настройка кривой нарастания сложности в 90-секундной миссии
### Phase 4: Мета-прогрессия, Монетизация и Релиз на Playgama Bridge (2 days)
- **Deliverable**: Релизная версия игры, готовая к публикации на платформах Яндекс Игры, VK и Web.
  - Реализация меню ангара и системы модернизации орудий за тактические кредиты
  - Интеграция Playgama Bridge SDK (Rewarded реклама ревайва и удвоения, облачные сохранения, лидерборды)
  - Кроссплатформенная оптимизация для мобильных браузеров и Safari iOS
  - Финальное QA-тестирование и сборка релизного бандла

---

## 9. ПРАВИЛА ПЛОЩАДКИ — УРОВЕНЬ MUST
Каждое правило ниже — след бага, дошедшего до продакшена, или отказа модерации.
Они перекрывают любую привычку, туториал и пример — включая куски из документации
Playgama и Яндекса, многие из которых описывают устаревший контракт Bridge v1.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

### Orientation & pointer lock

69. `Object3D.lookAt` aims the object's **+Z** at the target; −Z is the convention
    for cameras and lights only. Geometry that will later be aimed with `lookAt`
    (viewmodel arms, tracers, arrows, turret beams) must be built along **+Z**.
    Built along −Z it points backwards after aiming, and the bug reads as "broken
    model" rather than a flipped axis.

70. `requestPointerLock()` is called **from the pointer-down handler**, never from
    the frame loop: after an Esc exit a request issued inside
    `requestAnimationFrame` is silently rejected and the game looks dead. While
    `document.pointerLockElement` is set, application hotkeys (level switch, menu,
    tabs) must stay silent — otherwise an in-game key throws the player out of the
    game.

71. A `MeshStandardMaterial` with high `metalness` and **no environment map
    renders black** — metal has nothing to reflect. Set `scene.environment`
    (a `RoomEnvironment` PMREM costs no file and no request) **and** keep
    `metalness ≤ 0.4` in the material: the environment is the first thing a
    low tier drops, and the asset must not turn into a hole in the frame
    when it does.

72. A node whose base orientation is not zero — anything aimed with
    `lookAt`, `setFromUnitVectors` or a baked rest pose — is animated
    through a **wrapper**, never by writing its own `rotation`. Code that
    returns a limb to rest with `rotation.x = lerp(rotation.x, 0, k)` reads
    the Euler of that very quaternion and erases the aim: the part turns
    away from what it was holding, and the bug reads as "the model is
    missing a piece".

73. Anything positioned from **world** coordinates — a prop placed by two
    hand positions, an effect anchored to a bone, a decal following a
    moving platform — must be converted into the parent's local space
    before it is written into `position`/`quaternion`. Writing world
    numbers into a child of a transformed node applies that transform a
    second time, and the object drifts away by exactly the owner's offset
    from the origin. It stays invisible while the owner sits at the origin
    (which is where every unit test and every bake script puts it), and
    appears only in the game, as "the weapon is flying around the level".

74. Where the character **looks** and where the weapon **points** are two
    different directions, and gameplay owns the second one. A mocap firing
    stance is bladed: the axis through the hands is 40–60° away from the
    model's forward, so turning the body at the target aims the barrel past
    it. Measure the offset once on the assembled rig, turn the body by
    `heading − offset`, and take up the remainder by rotating the chest —
    with an exact quaternion `from barrel to target`, not by tweaking a
    single Euler axis: a barrel that sits at an angle to the rotation plane
    turns by only `angle · cos(that angle)`.

### Interface

75. UI layers over the canvas are **transparent to input by default**: every
    layer container is `pointer-events: none` and only leaf interactive elements
    turn it back on. A full-screen overlay left at `auto` swallows every gameplay
    pointer, and the bug reads as "the car does not steer on mobile". The HUD
    layer is read-only and never becomes interactive at all — pause and settings
    are buttons on the screen layer.

76. Every colour, font, radius, spacing step and duration comes from tokens in
    one theme file. A literal `#RRGGBB`, `px` padding or `z-index: 9999` written
    inside a screen is the reason the second screen stops matching the first.
    `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` minus the theme file returns nothing.
    Имена токенов при этом одинаковы во всех играх, а **значения — нет**: палитра,
    обе гарнитуры и силуэт рамки выводятся из материала мира этой игры по
    процедуре в `knowledge/ux/ui_design_system.md`, раздел 12. Скопированный из
    примера или из соседней игры набор значений — такой же дефект, как умолчания
    браузера, только заметнее: он выглядит осознанным решением, принятым не для
    этой игры.

77. Browser dialogs (`alert`, `confirm`, `prompt`) and emoji used as icons are
    **banned in shipped UI**. The first blocks the game loop, cannot be styled and
    breaks immersion; the second renders differently on every OS and reads as a
    placeholder. Use the project's `Modal` component and one inline SVG sprite
    coloured by `currentColor`.

78. The HUD updates by writing to **cached nodes** and only when the value
    changed — never by rebuilding markup inside the frame loop, and never by
    reading layout (`offsetWidth`, `getBoundingClientRect`) there. Changing
    numbers carry `font-variant-numeric: tabular-nums` in a fixed-width slot, and
    bars animate with `transform: scaleX()`, not `width`; otherwise the row
    reflows on every tick and the whole overlay repaints.

79. Every screen defines its **loading, empty and error** states, not only the
    happy path. Saves, leaderboards, purchases and ads are network calls that fail
    routinely on these portals; a failure that renders an empty frame or says
    nothing costs the player their run and reads as a broken game.

80. Menu and HUD geometry is sized against the **measured** viewport
    (`visualViewport`, re-measured after orientation change, fullscreen exit and
    ad close), never `100vh`, and every UI layer is inset by
    `env(safe-area-inset-*)` **plus** the measured sticky-banner height. The page
    itself never scrolls — long content scrolls in an inner container.
    See `knowledge/ux/ui_design_system.md` and `knowledge/ux/ui_implementation.md`.

81. Меню, пауза и экран итога сессии рисуются **поверх живой игровой сцены**, а
    не поверх заливки. Тот же рендерер, та же сцена, медленная камера, работающий
    свет; игровой цикл в меню продолжает крутиться на сниженной нагрузке.
    Непрозрачный слой на весь экран (`background: #111` на корне экрана,
    полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка
    допускается только под текстовым блоком и кнопками. Меню — это первое и
    иногда единственное, что видит игрок: за глухой плашкой с колонкой кнопок
    вся работа над геймплеем невидима, и игра читается как недоделанная ещё до
    запуска. Постановку сцены задаёт `ART_DIRECTION.md` (раздел «Сцена за меню»).

82. У каждого экрана **три зоны**: чем экран себя называет, единственное главное
    действие (самое крупное и единственное с основным акцентом) и второстепенный
    ряд одним весом. Одна карточка с колонкой кнопок по центру — не композиция, а
    её отсутствие; композиция каждого экрана задана в `UI_UX_SPECIFICATION.md`,
    раздел «Каталог экранов», и придумывать её заново не нужно.

### Разделы, которые открываются по месту
Эти правила нужны не с первой строки, а когда доберёшься до соответствующей области. Полный текст — в `docs/ref/knowledge/CRITICAL_RULES.md` (файл обязательный, приезжает шагом 0). Обязательность от переноса не меняется.
- **Stack** — выбор библиотек стека — перед подключением любой из них
- **Renderer** — настройки рендерера — когда собираешь сцену
- **Physics vehicles** — физика транспорта — только если в игре есть машины

---

## 10. ПРИЁМКА — MUST

Полный список проверок с номерами лежит в `ACCEPTANCE.md`, рядом с этим файлом.
Он и есть определение готовности: пока хотя бы один пункт разделов **A–C**
красный, игра не готова — это не качество, это работоспособность.

Статическая часть проверяется командой (скрипт уже лежит в проекте, зависимостей
не требует — пропиши его в `package.json` как `check:spec`):

```bash
node scripts/check-spec.mjs
```

Прогонять после каждой фазы роадмапа, а не один раз в конце. Результат
записывать строкой в `DEVLOG.md`. Пункт, который не проходит, честнее оставить
красным с объяснением, чем отметить зелёным: следующий, кто откроет проект,
будет считать его проверенным.

Критерии, которые относятся именно к этой игре:

- [ ] Орбитальная камера на высоте 1000м с плавным гиростабилизированным наведением через мышь и мобильный свайп
- [ ] Монохромный FLIR White-Hot шейдер постобработки со сканлайнами, шумом матрицы и термо-подсветкой целей
- [ ] Три полностью функционирующих калибра (25мм пулемет, 40мм автопушка, 105мм гаубица) с честной баллистической задержкой полета (1.2–2.5 с)
- [ ] ИИ союзного отряда спецназа, продвигающегося по 3 фазам до точки эвакуации, со строгим правилом Friendly Fire
- [ ] Физическая система взрывов, цепных детонаций техники и разрушения укрытий на базе Rapier3D
- [ ] Аутентичный военный HUD с индикацией калибров, шкалами перезарядки, комбо-счетчиком и радиопереговорами экипажа
- [ ] Интеграция Playgama Bridge SDK: Rewarded Video для ревайва и удвоения очков, Interstitial реклама, облачные сохранения и лидерборды
- [ ] Стабильные 60 FPS на мобильных устройствах и в десктопных браузерах
- [ ] Project builds cleanly with zero TypeScript errors (`npm run build`).
- [ ] Playgama Bridge initializes and dispatches GAME_READY platform message.
- [ ] Петля играбельна целиком: Сканирование сектора в тепловизор FLIR -> Выбор калибра (25мм / 40мм / 105мм) -> Расчет баллистического упреждения и радиуса поражения -> Залп по технике и живой силе противника -> Физическая цепная детонация укрытий -> Обеспечение безопасного прохода союзного спецназа к эвакуации -> Получение боевого рейтинга..
- [ ] Реализованы механики проекта: Баллистическое жонглирование тройным калибром (Triple-Caliber Orbital Ballistics), Протокол Danger Close и ИК-стробоскопы Bravo-6 (Danger Close & IR Beacon Protocol), Физическая цепная детонация и разрушение укрытий (Physics Chain Reactions & Structural Collapse), Термодинамика FLIR и подавление живой силы (FLIR Thermal Contrast & Kinetic Suppression).
- [ ] Условие успеха (Успешная эвакуация союзного отряда спецназа (минимум 1 выживший боец достиг зоны посадки вертолета на 90-й секунде) и полная ликвидация тяжелой техники противника.) и условие проигрыша (Гибель всех 4 бойцов союзного отряда от огня противника ИЛИ дружественный огонь (попадание снаряда 105мм/40мм в радиус союзников, уничтожившее отряд) ИЛИ истечение лимита времени (100 секунд) до выхода к эвакуации.) работают.
- [ ] Тач-управление отвечает без задержки, страница под игрой не скроллится.
- [ ] Интерфейс собран из токенов одного `src/ui/theme.css`: в экранах нет литералов цвета, шрифта и `z-index`.
- [ ] Слои интерфейса прозрачны для игрового ввода — управление работает под всеми оверлеями, слой HUD не кликается.
- [ ] У каждого экрана работают состояния загрузки, пустоты и ошибки; возможность, которой нет на площадке, не нарисована вовсе.
- [ ] Меню и HUD помещаются в измеренный вьюпорт с учётом safe-area и баннера, а с закрытым игровым полем меню узнаётся как «AC-130: Ночной Тепловизор».
- [ ] За меню и экраном итога видна живая игровая сцена: полноэкранной непрозрачной заливки поверх канваса нет, подложка только под текстом и кнопками.
- [ ] Ни одного эмодзи в подписях интерфейса: иконки — инлайновый SVG с `currentColor`.
- [ ] Rewarded-реклама выдаёт награду ровно один раз (Экстренная Реанимация Спецназа, Удвоение Боевых Кредитов, Армейский Контейнер Снаряжения); interstitial соблюдает паузу 90 с.
- [ ] Audio and physics automatically pause/resume on tab blur and ad display.
- [ ] Persistent progress saves and loads from Playgama Cloud Storage.
- [ ] Runs at steady 60 FPS on desktop and >= 50 FPS on mobile.

---

## 11. ГДЕ ЛЕЖИТ ОСТАЛЬНОЕ

База знаний не лежит в пакете копиями — она приезжает по требованию:

```bash
node scripts/fetch-knowledge.mjs        # всё обязательное → docs/ref/
node scripts/fetch-knowledge.mjs <путь> # отдельный файл
```

Что именно тянуть, перечислено в `knowledge.manifest.json`. После загрузки адреса ниже открываются как обычные локальные файлы, и сеть больше не нужна.

Набор отобран под этот проект: Проект опирается на связку Rapier3D и postprocessing для орбитальной баллистики и аутентичного FLIR-тепловизора, процедурную геометрию и физику разрушений укрытий при эскорте спецназа; готовый архетип петли в patterns отсутствует из-за специфики рельсового авиа-шутера.

**Строка после адреса — ярлык документа, а не его содержание.** Она названа так, чтобы документ можно было отличить от соседнего, и покрывает от силы десятую часть файла: за ярлыком «управление от первого лица и покачивание вьюмодели» в прошлый раз лежали гравитация, прыжок, вторая рука на вьюмодели, анимации перезарядки, пружина отдачи и хитмаркер — и ничего из этого в игру не попало, потому что файл сочли уже понятным. Документы ядра открываются целиком, до первой строчки кода по своей теме.

Пункты `- [ ]` под адресом — чек-лист самого документа, дословно. Это не пожелания: каждый пункт закрывает уже сделанную кем-то ошибку и проверяется взглядом на запущенную игру.

Здесь стоят чек-листы только тех документов, что отобраны под эту игру. **Полный список, включая платформенные документы, лежит в `ACCEPTANCE.md`, раздел H** — там же он и закрывается: у каждого пункта либо `[x]`, либо `[~]` со строкой причины. Незакрытый пункт валит `node scripts/check-spec.mjs`, и проект не считается готовым.

**Ядро — прочитать до начала реализации:**
- `docs/ref/knowledge/stack/postprocessing.md` (выжимка — `skills/STACK_SKILL.md`) — Критически необходим для шейдерного пайплайна тепловизора FLIR с режимами White-Hot/Black-Hot, шумом матрицы и сканлайнами.
  - [ ] renderer.render() больше не вызывается: только composer.render()
  - [ ] toneMapping на рендерере выставлен в NoToneMapping, иначе цвет сжимается дважды
  - [ ] Набор эффектов пересобирается на смене тира качества, а не каждый кадр
  - [ ] На мобильном SSAO и DoF не включаются никогда, bloom — только малым ядром
  - [ ] Импульсные эффекты (удар, оглушение) меняют прозрачность прохода, а не пересобирают конвейер
  - [ ] Депт-зависимые эффекты получают буфер глубины, иначе они молча ничего не делают
  - [ ] Постобработка — первое, что отключается при просадке кадра
- `docs/ref/knowledge/stack/rapier3d.md` (выжимка — `skills/STACK_SKILL.md`) — Физический движок для симуляции орбитальной баллистики снарядов 25/40/105мм, кинетического сплеша взрывов и цепных реакций.
  - [ ] Физика только на Rapier: свои «скорости», Cannon-es и ammo.js в проект не тянутся
  - [ ] Шаг мира фиксированный: world.timestep не подменяется на dt
  - [ ] cuboid() получает ПОЛУразмеры — коробка 1×1×1 это cuboid(0.5, 0.5, 0.5)
  - [ ] trimesh используется только для статичных тел: динамический проваливается сквозь мир
  - [ ] Группы столкновений заданы с обеих сторон — иначе контакта не будет
  - [ ] Триггеры сделаны сенсорами, а не проверкой дистанции в update()
  - [ ] Синхронизация мешей идёт ПОСЛЕ world.step(), иначе кадр рисует прошлое состояние
  - [ ] «На земле» определяется контроллером персонажа, а не по velocity.y === 0
  - [ ] Рейкаст исключает собственное тело стрелка
  - [ ] Рестарт уровня — телепорт тел, а не пересборка мира
  - [ ] Физика проверена скриптом в Node до того, как её увидит игрок
- `docs/ref/knowledge/threejs/physics_integration.md` (выжимка — `skills/RENDERER_SKILL.md`) — Интеграция Three.js и Rapier3D для синхронизации физических тел снарядов, обломков разрушенных укрытий и рэгдоллов с рендером.
  - [ ] Движок выбран под задачу: Rapier для тысяч тел, рэгдоллов и рейкастов, Cannon-es для простых аркадных столкновений
  - [ ] Порядок кадра: силы → шаг мира → синхронизация мешей, а не наоборот
  - [ ] Позиция меша интерполируется между физическими шагами, иначе видно дрожание
  - [ ] Суставы рэгдолла имеют пределы углов, линейное и угловое затухание — без этого он трясётся
  - [ ] Тела и суставы освобождаются явно: память WASM не собирается сборщиком мусора
  - [ ] Шаг физики фиксированный, а накопитель ограничен сверху
- `docs/ref/knowledge/mechanics/chain_reaction.md` (выжимка — `skills/orbital_ballistics_engine.md`) — Расчет радиусов поражения, кинетического сплеша взрывов и пространственных ограничений Danger Close возле отряда спецназа.
  - [ ] У взрыва две зоны: эпицентр с полным уроном и внешняя с квадратичным спадом
  - [ ] Вторичная детонация идёт с задержкой около 0.12 с — волна взрывов, а не схлопывание в один кадр
  - [ ] Импульс разлёта зависит от расстояния до эпицентра
  - [ ] Цепочка читается глазом: видно, что этот взрыв поджёг следующий
  - [ ] Число одновременных взрывов ограничено, иначе кадр проседает на самой зрелищной сцене
  - [ ] Урон по игроку от собственной цепочки считается по тем же правилам, что и по врагам
- `docs/ref/knowledge/mechanics/physics_destruction.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Честный физический расчет динамического обрушения укрытий, стен и блокпостов от попаданий тяжелых калибров 40мм и 105мм.
  - [ ] У разрушаемых объектов есть физическое здоровье и меш разрушения, а не мгновенное исчезновение
  - [ ] Обломки берутся из пула инстансов: создание мешей в кадре даёт рывки сборщика мусора
  - [ ] Разрушение сопровождается тяжёлым звуком, тряской камеры и кольцом пыли
  - [ ] Врага можно столкнуть в опасную зону или обрушить на него конструкцию — арена работает как оружие
  - [ ] Обломки живут ограниченное время и убираются, а не копятся до конца боя
  - [ ] Число одновременных обломков ограничено бюджетом кадра
- `docs/ref/knowledge/mechanics/cover_and_suppression.md` (выжимка — `skills/orbital_ballistics_engine.md`) — Тактическое поведение врагов в укрытиях и подавление живой силы пулеметным огнем 25мм для прикрытия продвижения отряда «Браво».
  - [ ] Точки укрытия размечены заранее, а не ищутся рейкастом в кадре
  - [ ] У укрытия есть нормаль: обход с фланга его обесценивает
  - [ ] Подавление снижает точность и готовность высунуться, а не здоровье
  - [ ] Подавленный противник пригибается, кричит и стреляет вслепую — иначе игрок читает это как случайный разброс
  - [ ] Подавление игрока показано виньеткой и увеличенным качанием оружия
  - [ ] Слот укрытия занят одним бойцом: двое не втискиваются в одну точку

**Вспомогательные:**
- `docs/ref/knowledge/threejs/performance_guide.md` (выжимка — `skills/RENDERER_SKILL.md`) — Бюджет кадра и инстансинг снарядов, взрывных эффектов и обломков для удержания стабильных 60 FPS на мобильных устройствах.
- `docs/ref/knowledge/threejs/adaptive_quality.md` (выжимка — `skills/RENDERER_SKILL.md`) — Адаптивное масштабирование разрешения и тяжести пост-шейдеров FLIR под нагрузкой во время массированных детонаций.
- `docs/ref/knowledge/threejs/mobile_shaders.md` (выжимка — `skills/RENDERER_SKILL.md`) — Оптимизация кастомных тепловизионных шейдеров и материалов техники под ограничения мобильных GPU.
- `docs/ref/knowledge/threejs/procedural_mesh_builder.md` (выжимка — `skills/RENDERER_SKILL.md`) — Процедурная генерация построек боевой зоны, техники конвоя и элементов окружения без внешних 3D-моделей.
- `docs/ref/knowledge/threejs/game_map_and_world_design.md` (выжимка — `skills/RENDERER_SKILL.md`) — Структурирование трех тактических секторов зоны спецоперации с учетом высотной орбитальной перспективы под углом 50 градусов.
- `docs/ref/knowledge/threejs/procedural_character_rig.md` (выжимка — `skills/RENDERER_SKILL.md`) — Процедурный риг и контрастные тепловые силуэты бойцов союзного спецназа «Браво» и вражеской пехоты.
- `docs/ref/knowledge/threejs/vehicle_wheel_rig.md` (выжимка — `skills/RENDERER_SKILL.md`) — Процедурная сборка и риг колесной техники вражеских моторизованных колонн и пикапов.
- `docs/ref/knowledge/mechanics/ragdoll.md` (выжимка — `skills/MELEE_SKILL.md`) — Физическая реакция тел бойцов и рэгдолл-отлет при воздействии ударных волн и кинетических импульсов 105мм снарядов.
- `docs/ref/knowledge/threejs/juice_and_vfx_pool.md` (выжимка — `skills/RENDERER_SKILL.md`) — Инстансированный пул частиц для ослепительно белых тепловых вспышек, дыма, трассеров и отдачи камеры при выстрелах.
- `docs/ref/knowledge/audio/procedural_sound_synthesizer.md` (выжимка — `skills/RENDERER_SKILL.md`) — Процедурный Web Audio синтез залпов трех калибров, взрывов, радиопереговоров и низкочастотного гула двигателей AC-130.
- `docs/ref/knowledge/audio/web_audio_and_muting.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Корректная инициализация Web Audio по первому взаимодействию и автоглушение звука при потере вкладкой фокуса.
- `docs/ref/knowledge/monetization/interstitial_best_practices.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Показ межстраничной рекламы в естественных сюжетных паузах между секторами эскорта и на экране финального дебрифинга.

**Платформенные требования — обязательны всегда, независимо от жанра.** Без Playgama Bridge игра на площадке не стартует: платформа держит свою заставку до таймаута и снимает игру с модерации.
- `docs/ref/knowledge/playgama/game_ready_and_loading.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/auth_and_player.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/storage_and_cloud.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/ads_integration.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/banners_and_layout.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/compliance/yandex_moderation.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/localization_system.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/touch_controls.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_design_system.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_implementation.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)

**Осознанно НЕ включены**: `threejs/fps_controller_and_shooting.md`, `threejs/shooter_enemy_ai_and_combat.md`, `threejs/horde_survivor_core.md`, `patterns/survivor_loop.md`, `patterns/rts_skirmish_loop.md`, `threejs/rts_selection_and_command.md`, `mechanics/upgrade_choices.md`, `threejs/rapier_vehicle_controller.md`, `stack/recast_navigation.md`, `patterns/physics_arcade_loop.md`. Отклонены шаблоны FPS и RTS, так как управление ведется только бортовой турелью AC-130 с орбиты 1000м; исключены механики орды, карточных апгрейдов и автострельбы типа Vampire Survivors, запрещенные ТЗ; отброшены тяжелый навмеш Recast и полный контроллер машин, так как техника и пехота двигаются по срежиссированным тактическим маршрутам.
Не подтягивай решения из этих документов — они про другую игру.

**Документы проекта** (лежат рядом, в корне игры): `ACCEPTANCE.md` — приёмка, `AGENTS.md` — правила работы, `PROJECT_DIRECTION.md` — рамка проекта, `GAME_DESIGN_DOCUMENT.md`, `MECHANICS.md`, `CORE_LOOP.md`, `UI_UX_SPECIFICATION.md`, `ART_DIRECTION.md`, `MOBILE_CONTROLS.md`, `PLAYGAMA_INTEGRATION.md`, `MONETIZATION.md`, `QA_PLAN.md`, `DEVLOG.md` и `CHANGELOG.md` — их ведёшь ты.