# FINAL AI DEVELOPER PROMPT: ГАНГЕЙМ: КОНТЕЙНЕРНЫЙ ПРОРЫВ 🎮⚡

> **ИНСТРУКЦИЯ КОДОВОМУ АГЕНТУ**
> Ты ведущий разработчик этой игры. Задача — довести её до состояния, в котором
> она проходит приёмку из `ACCEPTANCE.md`: работающая, играбельная, принятая
> площадкой. Заглушки, TODO и «пока так» результатом не считаются.

### Как читать этот документ

Требования размечены по обязательности. Когда обязательно всё, не обязательно ничего.

| Уровень | Что значит | Что делать при сомнении |
|---|---|---|
| **MUST** | Без этого игра не работает или не проходит модерацию площадки. | Не обсуждается. Проверяется в `ACCEPTANCE.md`, разделы A–C. |
| **SHOULD** | Проектное решение фабрики: так игра задумана. | Отступать можно, но с записью в `DEVLOG.md` — что и почему. |
| **MAY** | На твоё усмотрение. | Решай сам, спрашивать не нужно. |

Неразмеченное считается **SHOULD**. Всё, что помечено «ЗАПРЕЩЕНО» в секции 1b, —
это **MUST**, даже если спецификация где-то умалчивает.

### Порядок работы

Читать документ целиком до первой строчки кода не нужно. Порядок такой:

0. `node scripts/fetch-knowledge.mjs` — забрать базу знаний в `docs/ref/`. Одна команда, дальше работа идёт офлайн. **MUST**, и раньше всего остального: без этого половина адресов в документе никуда не ведёт.
1. `ACCEPTANCE.md` — чем закончится работа. Дальше всё делается под него.
2. Секция 1b — чем эта игра является и чем она не является. Рамка всего остального.
3. `DESIGN.md` — написать самому, до первой строчки кода. Секция 7 говорит, что в нём должно быть и чего в нём быть не должно.
4. Каркас: `package.json`, Vite, TypeScript strict, пустая сцена Three.js, цикл с фиксированным шагом. Playgama Bridge и `game_ready` — сразу, а не в конце (проверки **C1–C4**).
5. Главная механика из секции 3 — одна, до играбельного состояния. Не все сразу. Перед тем как писать её с нуля — секция 3c: возможно, она уже написана.
6. Дальше по фазам роадмапа (секция 8), каждая фаза закрывает свои номера проверок.
7. `node scripts/check-spec.mjs` — после каждой фазы, а не один раз в конце.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: Гангейм: Контейнерный Прорыв
- **Project Slug**: `sozday_igru_po_tipu_call_of_duty_002`
- **Genre**: 3D Экшен-Шутер от первого лица (Аркадный Гангейм / Арена-Шутер / Fast-Paced FPS)
- **Target Platform**: Playgama Bridge (Yandex Games / VK Play / Web & Mobile)
- **Orientation**: Landscape
- **Target Audience**: Любители динамичных сессионных шутеров (Call of Duty, Gun Game, CS DM) от 12 до 35 лет, ценящие мгновенный тактильный отклик, скоростной ганплей, скольжение в подкатах и матчи на 1–2 минуты в браузере или на смартфоне.
- **Player Fantasy**: Вы — неудержимый элитный оперативник спецназа в эпицентре бешеной перестрелки на закрытом терминале, где каждое убийство мгновенно меняет оружие в ваших руках, превращая бой в непрерывный хореографический танец смерти на сверхскоростях.
- **Core Hook**: Вы скользите в низком подкате прямо под очередью врага, ваншотите его в упор из дробовика, оружие мгновенно превращается в снайперку, вы в прыжке без прицела снимаете второго на крыше контейнера и активируете дрон-разведчик, видя оставшихся противников сквозь стены.
- **Session Model**: Интенсивная гонка вооружений (Gun Game) на компактной арене длительностью 60–90 секунд до 12 фрагов с мгновенным респауном за 1.0 секунды.


---

## 1b. РАМКА ПРОЕКТА: ЧТО ЭТО ЗА ИГРА И ЧЕМ ОНА НЕ ЯВЛЯЕТСЯ


**Направление**: Гангейм: Контейнерный Прорыв

**Почему выбрано именно оно**: Направление D3 идеально переносит формулу Call of Duty в браузер и мобильный формат: сессия на 60–90 секунд дарит непрерывный адреналин, мгновенный респаун устраняет ожидание, смена оружия за фраги дает понятную цель на раунд, а тесная арена из контейнеров гарантирует 60 FPS даже на слабых смартфонах за счет низкого числа полигонов и минимальных дистанций отрисовки.

**Узнаваемая сцена**: Вид от первого лица из глаз бойца спецназа в тактических перчатках, находящегося в низком подкате по мокрому мощеному асфальту между двумя темно-красными грузовыми контейнерами. В руках — черный укороченный пистолет-пулемет с коллиматором, из дула которого вырывается оранжевая вспышка выстрела, а вбок летит латунная гильза с дымным следом. В трех метрах впереди оседает фигура вражеского бойца, окруженная белым крестиком хитмаркера и искрами. Сверху сквозь морось пробивается свет складского прожектора, выхватывая лужи с отражениями и силуэт парящего в небе разведывательного БПЛА с мерцающим зеленым огоньком.

**Без чего проект перестаёт быть собой** (реализовать обязательно):
- Мгновенная смена класса оружия в руках бойца при каждом совершении фрага (лестница Gun Game: Пистолет -> Дробовик -> ПП -> Автомат -> Снайперка -> РПГ) без пауз и открытия меню.
- Динамический подкат (Slide) на бегу с сохранением импульса скорости и возможностью непрерывной стрельбы от бедра.
- Тактильный аудиовизуальный отклик попадания: хитмаркер (белый крестик с сочным щелчком «тик-клак»), вспышки дульного пламени и вылетающие гильзы.
- Система наград за серию фрагов (Киллстрики): 3 убийства без смерти активируют тактический дрон-разведчик БПЛА (UAV), подсвечивающий противников сквозь контейнеры на 8 секунд.
- Компактная симметричная арена в стиле Shipment из грузовых контейнеров, обеспечивающая первый огневой контакт уже на 2-й секунде после старта матча.

**ЗАПРЕЩЕНО ВВОДИТЬ** — даже если спецификация где-то умалчивает и даже если «так принято в жанре»:
- Никаких серых прототипных кубов вместо стилизованных текстурированных контейнеров и оружия.
- Никаких окон выбора из 3 карточек апгрейда и рогаликовых способностей (никаких фаерболов, ядовитых аур и авто-стрельбы).
- Никаких медленных тактических разминирований бомб и кемпинга на точках в стиле Counter-Strike.
- Никаких гигантских пустых пространств королевской битвы с долгим сбором лута и бронежилетов.
- Никакого плавающего экранного джойстика, перекрывающего обзор — строго раздельные зоны касания и свайпов.
- Никаких бесконечных волн одинаковых зомби, идущих по прямой линии на игрока.
- волны врагов на арене
- окно выбора из трёх карт апгрейда
- универсальная связка «дэш + парирование + удар»
- заимствование Vampire Survivors / Brotato как основы
- джойстик слева + кнопки атаки справа как раскладка по умолчанию
- серые кубы вместо стилизованной геометрии
- мета-прогрессия «золото за забег -> постоянные апгрейды» без связи с темой

Если по ходу реализации кажется, что без запрещённого элемента игра не работает — это находка для `DEVLOG.md`, а не разрешение его добавить.

**Рассмотренные и отклонённые направления** (не возвращай их через механики):
- D1 (Киллхаус: Рекорд Зачистки) отвергнут, так как тир со статичными и откидными мишенями лишен сопротивления живого противника и превращается в заучивание таймингов, уступая полноценному динамичному бою по глубине вовлечения.
- D2 (Припять: Невидимый Выстрел) отвергнут, так как медленный позиционный стелс с длительным выжиданием окон шума создает избыточный простой и фрустрацию на сенсорных экранах, где аудитория браузерных игр требует мгновенного экшена с первых секунд.

---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite
- **Renderer**: **THREEJS** (^0.185.1)
  - *Selection Rationale*: Three.js + Rapier3D: пространственный геймплей, физика и освещение. Перспективная камера.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat ^0.20.0)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Web Audio API (Howler.js with html5: false)
- **State Management**: Reactive EventBus / State Store
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 65
- **Max Triangles / Active Sprites**: < 38000
- **Max Bundle Size**: < 3.8 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Спринт и подкат в узкий коридор контейнеров -> Захват цели и точный отстрел противника -> Мгновенная автоматическая смена оружия на следующий уровень лестницы -> Активация разведывательного БПЛА за серию фрагов -> Достижение 12-го финального фрага и победа в матче.
```

**Фирменный момент**: Скользя на мокром асфальте в низком подкате между двумя ржавыми контейнерами, вы разносите врага в упор из дробовика; оружие в руках со звонким щелчком мгновенно трансформируется в снайперскую винтовку — вы не прерывая скольжения выпрыгиваете на крышу ящика, без прицела в воздухе снимаете второго врага, зарабатываете 3-й фраг в серии, и небо озаряется сигналом БПЛА, подсвечивающим оставшихся соперников сквозь стальные стены.

**Чем петля отличается от соседей по жанру**: В отличие от классических Gun Game с задержкой смены оружия в 0.6–1.2 с и мобильных шутеров с автострельбой, здесь реализована бесшовная нулевая задержка смены ствола (0.08 с), инерционная физика подкатов со стрельбой от бедра и тактический 3D-рентген БПЛА за серию убийств, превращающий лабиринт из контейнеров в прозрачную арену для непрерывного скоростного спидрана.

**Шаблон жанра, который НЕ реализуем**: Шаблон классического FPS с ручной перезарядкой обоймы (кнопка R), покупкой оружия в бай-меню и кемперским отсиживанием в укрытиях для регенерации здоровья.
ПОЧЕМУ ОТВЕРГНУТ: В сессионном веб/мобильном шутере длительностью 90 секунд любая 3-секундная пауза на перезарядку или пассивное стояние за ящиком разрушает темп адреналинового флоу. Здесь перезарядка полностью заменена мгновенной трансформацией оружия за совершение фрага, что превращает матч в непрерывный скоростной танец смерти без единой секунды простоя.

**Схема петли**:
```text
+-------------------------------------------------------------------------------+
|                       ГАНГЕЙМ: КОНТЕЙНЕРНЫЙ ПРОРЫВ                           |
+-------------------------------------------------------------------------------+
                                       |
                               [СПАВН НА ТЕРМИНАЛЕ] (t = 0.0с)
                                       |
                                       v
    +-------------------> [СПРИНТ И МАНЕВРИРОВАНИЕ] <--------------------+
    |                                 |                                  |
    |                      (Обнаружен противник)                         |
    |                                 v                                  |
    |                    [БОЕВОЙ ПОДКАТ / ПЕРЕЛАЗ]                       |
    |                    (Скорость +35%, Хитбокс -50%)                   |
    |                                 |                                  |
    |                     (Огневой контакт CQB)                          |
    |                                 v                                  |
    |                [ПРИЦЕЛИВАНИЕ В УЯЗВИМУЮ ЗОНУ]                      |
    |              (Голова: 2.0x урон | Корпус: 1.0x)                    |
    |                                 |                                  |
    |                         (Фраг подтвержден)                         |
    |                                 v                                  |
    |                   [МГНОВЕННАЯ ТРАНСФОРМАЦИЯ]                       |
    |                 (Ствол N -> N+1 за 0.08с, 100% патронов)           |
    |                                 |                                  |
    |               +-----------------+-----------------+                |
    |               |                                   |                |
    |    [СЕРИЯ = 3 ФРАГА]                     [СЕРИЯ < 3 ФРАГА]         |
    |               |                                   |                |
    |               v                                   |                |
    |    [АКТИВАЦИЯ БПЛА «ОВЕРЛОРД»]                    |                |
    |   (Подсветка сквозь стены на 8.0с)                |                |
    |               |                                   |                |
    |               +-----------------> + <-------------+                |
    |                                   |                                |
    |                            (Ранг < 12)                             |
    +-----------------------------------+                                |
                                        |                                |
                                    (Ранг == 12)                         |
                                        v                                |
                           [ФИНАЛЬНЫЙ ФРАГ ИЗ ЗОЛОТОГО РПГ]              |
                                        |                                |
                                        v                                |
                             [ТРИУМФ И ПОБЕДА В МАТЧЕ]                   |
                               (Награды, Звезды, Скины)                  |
```

**Микро-петля (посекундно)**:
- **1. Сканирование коридора и позиционирование** (1.0–2.0 с): игрок — Спринт по узкому проходу между контейнерами с контролем углов поворотом камеры; игра — Отображение теней врагов на асфальте, звуки шагов за углом, маркеры на миникарте; решение — Атаковать в лоб через агрессивный боевой подкат или залезть на контейнер для атаки сверху
- **2. Инициация контакта и боевой подкат** (0.5–0.9 с): игрок — Вход в боевой подкат под линию прицела врага со смещением корпуса; игра — Снижение высоты камеры до 0.75 м, искры из-под подошв об асфальт, разгон скорости на +35%; решение — Стрелять от бедра в скольжении или выждать момент для точной наводки в голову
- **3. Огневой контакт и ликвидация цели** (0.2–0.5 с): игрок — Точный выстрел в верхнюю зону хитбокса противника с компенсацией отдачи; игра — Золотой хитмаркер, Web Audio FM-клик попадания, рэгдолл врага, регистрация фрага; решение — Продолжить движение вперед по инерции или резко развернуть камеру на 180° для проверки спины
- **4. Мгновенная трансформация оружия** (0.08–0.3 с): игрок — Мгновенная адаптация хвата и прицела под новый тип оружия лестницы без паузы на перезарядку; игра — Голографическая вспышка декомпиляции за 0.08 с, новый ствол в руках со 100% патронов, +1 к рангу лестницы; решение — Сокращать дистанцию под дробовик/ПП или занять длинный прострел под снайперку/марксманку

**Мезо-петля (этап)**:
- **1. Стартовый прорыв ближнего боя (Ранги 1–4: Пистолеты и Дробовики)** (15–20 с): игрок — Сверхскоростные подкаты и дуэли в упор в центральном узле контейнеров из пистолетов и дробовиков; игра — Высокая плотность спавна врагов в центре, быстрый набор первых 3–4 фрагов, переход на автоматическое оружие; решение — Агрессивно фармить первые фраги в узком центре арены или обойти по внешнему периметру терминала
- **2. Тактическая серия и активация БПЛА (Ранги 5–8: ПП и Штурмовые Винтовки)** (25–35 с): игрок — Контроль отдачи на средних дистанциях (MP5, P90, M4, AK), набор серии из 3 фрагов и вызов БПЛА «Оверлорд»; игра — Звук сонара, подсветка силуэтов врагов сквозь контейнеры на 8.0 с, рост плотности ответного огня ботов; решение — Использовать подсветку БПЛА для штурма плотной группы врагов или занять контейнер для отстрела сверху
- **3. Финальная дуэль на точность и триумфальный залп (Ранги 9–12: DMR, AWP, РПГ-7)** (20–30 с): игрок — Выцеливание уязвимых зон из DMR и снайперки AWP, уклонение от перекрестного огня и финальный выстрел из Золотого РПГ-7; игра — Таймер матча тикает (<20 с), звуковые сирены финала, боты стягиваются к позиции лидера гонки; решение — Рискнуть выстрелом ноу-скопом в прыжке с подката или занять выжидательную позицию на контейнере под таймер матча

**Макро-петля (забег)**:
- **1. Участие в рейтинговой гонке вооружений Gun Game** (60–90 с): игрок — Выбор режима матча и участие в 90-секундной рейтинговой гонке вооружений на выбывание; игра — Динамичный матч 1 vs 7 на арене из контейнеров, регистрация фрагов в реальном времени, финальный экран победы с фейерверком или поражения по таймеру; решение — Оценить эффективность стиля игры (агрессивный пуш в подкатах vs позиционный отстрел со снайперки) на основе итоговой статистики точности и фрагов
- **2. Подведение итогов, расчет очков и сбор трофеев** (10–15 с): игрок — Просмотр экрана итогов матча, сбор наград боевого пропуска и жетонов терминала; игра — Начисление опыта аккаунта, выдача звезд победы (+100 жетонов за 1-е место), обновление позиции в таблице лидеров терминала; решение — Потратить заработанные жетоны на новый неоновый камуфляж для дробовика/снайперки или копить на престижный золотой скин РПГ-7
- **3. Кастомизация арсенала в оружейной и подготовка к реваншу** (15–30 с): игрок — Экипировка разблокированных камуфляжей и тактических перчаток в оружейной, запуск нового раунда; игра — Отображение кастомизированного 3D-оперативника и оружия с PBR-материалами в главном меню, обновление рангового звания; решение — Сменить раскладку камуфляжей оружия и тактических перчаток для следующего матча или запустить реванш для побития рекорда времени (<50 с)

**Кривая напряжения**: Кривая напряжения представляет собой агрессивную пилообразную синусоиду с экспоненциальным взлетом в финале: 0–15 с (Ранги 1–3) — быстрый старт на 60% напряжения, разгоняющий ритм; 15–45 с (Ранги 4–8) — пик флоу на 75–80% с активацией БПЛА и доминированием на арене; 45–75 с (Ранги 9–11) — зона максимального тактического стресса на 90% напряжения (снайперки повышают цену промаха); 75–90 с (Ранг 12, РПГ-7) — финальный катарсис на 100% напряжения, когда все боты атакуют лидера, а единственный точный залп приносит мгновенную триумфальную победу.

**Формулы ядра (реализуй буквально)**:
- `Урон с учетом зон попадания: Damage_Final = Base_Weapon_Damage * Zone_Multiplier (где Head = 2.0, Torso = 1.0, Legs = 0.7)`
- `Скорость перемещения: V_Current = V_Base * Slide_Factor * Stagger_Penalty (где V_Base = 8.0 м/с, Slide_Factor = 1.35 в окне скольжения 0.90 с, Stagger_Penalty = 0.70 на 0.35 с при получении урона)`
- `Таймер мгновенной трансформации ствола: T_Morph = 0.08 с (мгновенный swap 3D-модели в руках и пополнение обоймы до 100% Capacity_Max без паузы на перезарядку)`
- `Эффективность БПЛА «Оверлорд»: UAV_Duration = 8.0 с, Pulse_Freq = 1.5 с, Stencil_Outline_Radius = 1.8 м вокруг меша врага с альфа-прозрачностью 0.85`
- `Итоговый боевой счет матча: Match_Score = (Frags * 100) + (Headshot_Count * 50) + (Killstreaks * 150) + max(0, (90 - Elapsed_Time) * 10)`
- `Адаптивный радиус спавна ботов: Spawn_Distance = clamp(12.0 + (Rank * 0.8), 12.0, 22.0) метров от игрока вне прямого конуса видимости камеры (FOV 80°)`

**Прогрессия внутри забега**:
- Ранг 1: Тактический пистолет P99 (высокая точность, 3 выстрела в тело / 1 хедшот + 1 тело)
- Ранг 2: Тяжелый револьвер Magnum .44 (высокий урон 70 ед, 1 хедшот — мгновенный фраг, отдача +40%)
- Ранг 3: Помповый дробовик SPAS-12 (ваншот на дистанции до 4 метров, разлет 8 дробин по 15 урона)
- Ранг 4: Автоматический тактический дробовик AA-12 (высокий темп стрельбы в подкатах, зачистка групп в упор)
- Ранг 5: Пистолет-пулемет MP5 (сбалансированная отдача, идеален для скоростных фланговых зачисток)
- Ранг 6: Скорострельный ПП P90 (обойма 50 патронов, скорострельность 900 выстр/мин для непрерывного пуша)
- Ранг 7: Штурмовая винтовка M4A1 (универсальный баланс дальности и контроля кучности)
- Ранг 8: Тяжелый автомат AK-47 (высокий урон 38 ед, жесткая вертикальная отдача, 1 тап в голову = 76 урона)
- Ранг 9: Ручной пулемет LMG SAW (огневая мощь, замедление бега на -10%, прострел фанерных укрытий)
- Ранг 10: Марксманская винтовка DMR-14 (полуавтомат, 2 выстрела в корпус, высокая скорость пули 600 м/с)
- Ранг 11: Крупнокалиберная снайперская винтовка AWP (ваншот в грудь и голову, огромный разброс от бедра)
- Ранг 12: Золотой РПГ-7 (1 финальный снаряд со сплешем 3.5 м для завершения матча триумфальным взрывом)

**Мета-прогрессия**:
- Оружейные скины: Разблокировка камуфляжей (Городской Цифра, Кибер-Неон, Дамасская Сталь, Чистое Золото) за фраги из каждого конкретного типа оружия (50 / 150 / 300 / 500 фрагов).
- Тактические перчатки и жетоны оперативника: Покупка кастомизации рук за заработанные в победах звезды терминала (дает уникальные визуальные эффекты искр при подкате).
- Рейтинговая лига терминала: Продвижение по рангам (Рекрут -> Оперативник -> Ветеран -> Элита -> Легенда Терминала) на основе победных серий и процента матчей на 1-м месте.
- Звания и карточки игрока (Callsigns): Эксклюзивные анимированные баннеры профиля за мастерские достижения («Завершить матч без смертей», «3 фрага в одном подкате»).

### Лестница Вооружений с Мгновенной Трансформацией (Instant Gun Ladder Morph) (CORE)
- **Category**: Главный двигатель темпа, динамический регулятор дистанции боя и источник адаптации игрока.
- **Description**: В отличие от традиционных Gun Game с долгой сменой оружия (0.6–1.2 с анимации доставания), здесь трансформация занимает 0.08 с с мгновенным пополнением патронов, удерживая игрока в непрерывном высокооктановом флоу.
- **Player Input**: ПК: ЛКМ — огонь, ПКМ — тактический зум, Q — удар прикладом; Тач: Правая половина экрана — плавающая кнопка огня с трекингом пальца, кнопка зума, свайп вниз для подката.
- **Technical Complexity**: Medium
- **Решение игрока**: Мгновенная перестройка тактики боя (от упора и CQB-спрея к precision-тэппингу на дальней дистанции) в долю секунды после совершения фрага прямо на открытом пространстве.
- **Состояния**: `STATE_READY`, `STATE_FIRING`, `STATE_RECOIL_RECOVERY`, `STATE_INSTANT_MORPH`, `STATE_LADDER_MAX`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `morph_transition_duration` = `0.08 с` — При >0.25 с теряется ощущение бешеного потока и разрывается комбо-серия фрагов; при <0.02 с игрок не успевает среагировать на изменение физики отдачи нового ствола.
  - `ladder_tier_count` = `12 рангов` — При <8 рангах матч завершается слишком быстро (до 40 с) без демонстрации контраста классов; при >16 рангах сессия затягивается свыше 120 с, вызывая усталость пальцев на тачскрине.
  - `frag_refill_ammo` = `100% обоймы` — При неполном боезапасе игрок наказывается пустой обоймой за успешный фраг, что вызывает фрустрацию; при бесконечной стрельбе обесценивается точность попаданий.
  - `headshot_skip_ladder` = `0 рангов бонусного перескока` — Сознательно отключен перескок через ранг за хедшот (0 рангов), чтобы гарантировать полное прохождение 12 контрастных паттернов стрельбы каждым участником гонки.
- **Слои отклика**:
  - Визуал/VFX: Вспышка сине-белой голографической декомпиляции ствола за 80 мс, разлет неоновых искр из затвора, мгновенная смена 3D-модели оружия в руках (Three.js mesh swap).
  - Звук (Web Audio): Резкий металлический лязг досылания патрона (850 Гц полосовой фильтр, 95 мс) + нарастающий синус-тон подтверждения ранга (от 440 Гц на 1 ранге до 1760 Гц на 12 ранге).
  - Камера: Мгновенный импульс приближения FOV (-3° на 60 мс с плавным возвратом lerp 0.15) и микро-встряска по оси Z (амплитуда 0.015 м, затухание 0.08 с).
  - UI/Тактильность: Всплывающий золотой баннер над прицелом «+1 РАНГ: M4A1», заполнение деления шкалы 1..12 в верхней части экрана, вибрация 45 мс на мобильных устройствах.
- **Режим отказа игрока**: Игрок по инерции зажимает гашетку после фрага из автомата, получая снайперскую винтовку с огромным разбросом от бедра в движении — выстрел уходит в молоко, игрок погибает от ответного огня; сигнал — глухой щелчок одиночного выстрела снайперки и резкий увод ствола вверх.
- **Кривая мастерства**: Новичок на 30-й секунде замирает на 0.5 с после каждого фрага, чтобы осознать полученный ствол; эксперт на 10-м матче наизусть помнит очередность 12 видов оружия и заранее начинает движение на нужную дистанцию еще до завершения анимации трансформации.
- **Сопротивление игры**: Боты и соперники адаптируют дистанцию боя под текущий ранг игрока: заметив снайперскую винтовку в руках игрока, боты сокращают дистанцию низкими подкатами, а против дробовика удерживают дальние прострелы.
- **Синергии**:
  - Боевой Подкат (трансформация оружия происходит прямо в скольжении без потери скорости)
  - БПЛА-Разведчик (позволяет заранее оценить дистанцию до следующего врага под полученный ствол)
  - Дифференцированный Хит-Отклик (хедшоты ускоряют прохождение трудных рангов со снайперками)
- **Почему это не жанровый шаблон**: В отличие от традиционных Gun Game с долгой сменой оружия (0.6–1.2 с анимации доставания), здесь трансформация занимает 0.08 с с мгновенным пополнением патронов, удерживая игрока в непрерывном высокооктановом флоу.
- **Псевдокод тика**:
```text
function onWeaponLadderTick(player, dt):
  if player.state == STATE_INSTANT_MORPH:
    player.morphTimer -= dt
    if player.morphTimer <= 0:
      player.state = STATE_READY
      player.currentWeapon = WeaponLadder[player.ladderRank]
      player.ammo = player.currentWeapon.magazineCapacity
  if player.hasPendingFrag:
    player.hasPendingFrag = false
    player.ladderRank = min(player.ladderRank + 1, 12)
    player.morphTimer = 0.08
    player.state = STATE_INSTANT_MORPH
    triggerFeedback('weapon_morph_fx', player.ladderRank)
```

### Боевой Подкат с Инерцией и Низкопрофильной Стрельбой (Combat Slide & Hipfire) (SECONDARY)
- **Category**: Двигатель маневренности, инструмент агрессивного сближения (gap-closer) и уклонения от хедшотов.
- **Description**: Сохраняет полноценную возможность ведения прицельного огня от бедра с динамическим расширением FOV и занижением высоты хитбокса на 50%, оптимизированный под Three.js/Rapier3D raycast-контроллер.
- **Player Input**: ПК: Зажатый Shift (Спринт) + Нажатие C / Ctrl (Подкат); Тач: Свайп вниз по правой половине экрана во время движения виртуального стика вперед.
- **Technical Complexity**: Medium
- **Решение игрока**: Срезать угол контейнера на форсажной скорости с риском влететь в засаду или войти шагом с сохранением максимальной кучности стрельбы.
- **Состояния**: `SLIDE_INACTIVE`, `SLIDE_ACCELERATING`, `SLIDE_GLIDING`, `SLIDE_BRAKING`, `SLIDE_COOLDOWN`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `slide_velocity_multiplier` = `1.35x (10.8 м/с от базы 8.0 м/с)` — При >1.6x игрок теряет контроль на поворотах между ящиками и врезается в стены; при <1.15x подкат теряет смысл как инструмент уклонения от пуль.
  - `hitbox_height_reduction` = `50% (с 1.80 м до 0.90 м)` — При <30% снижения вражеский огонь на уровне груди продолжает попадать в игрока; при >70% игрок визуально проваливается под геометрию сцены.
  - `slide_duration` = `0.90 с` — При <0.5 с подкат ощущается дерганым спамом; при >1.3 с игрок становится легкой предсказуемой мишенью на прямолинейной траектории.
  - `slide_cooldown` = `0.35 с` — При 0 с возникает абуз непрерывного слайд-кансела, ломающий баланс; при >0.8 с теряется текучесть паркур-движения.
  - `slide_spread_penalty` = `+25% к разбросу от бедра` — При 0% отпадает надобность в прицельной стрельбе; при >60% стрельба в подкате превращается в бесполезный визуальный трюк.
- **Слои отклика**:
  - Визуал/VFX: Сноп оранжевых искр из-под подошв об мокрый асфальт (Three.js particle emitter, 35 частиц/с), динамическое радиальное размытие краев экрана, наклон 3D-модели оружия на 15° влево.
  - Звук (Web Audio): Характерный визгливый скрежет трения кевларовых наколенников о гранулированный бетон (белый шум через Bandpass 1200 Гц с затуханием 0.85 с).
  - Камера: Падение высоты взгляда с 1.65 м до 0.75 м за 0.12 с (smoothstep), расширение FOV с 80° до 92° во время движения, наклон камеры Roll на 3.5°.
  - UI/Тактильность: Иконка подката в центре экрана загорается лазурным неоном, ритмичная тактильная пульсация 20 мс каждые 150 мс скольжения на смартфонах.
- **Режим отказа игрока**: Игрок активирует подкат без достаточной стартовой скорости бега (стоя на месте или при движении назад), вместо скольжения выполняя обычный присед — мгновенная потеря мобильности и гибель от хедшота; сигнал — глухой звук провалившегося приседания и красный значок перечеркнутого спидометра.
- **Кривая мастерства**: Новичок на 30-й секунде скользит по прямой в центр арены и ловит пули в конце анимации; мастер на 10-м матче входит в подкат за 1 метр до угла контейнера, срезает 90° с разворотом камеры на 180° и сносит врага в спину.
- **Сопротивление игры**: Узкая геометрия контейнерных проходов (ширина 2.2 м) ограничивает угол маневра; боты при звуке подката мгновенно опускают сектор прицеливания с высоты 1.6 м до 0.8 м.
- **Синергии**:
  - Лестница Вооружений (подкат позволяет безопасно пережить 0.08 с трансформации оружия на скорости)
  - Вертикальный Перелаз (плавный переход из подката в прыжок-зацеп на ящик без потери импульса)
- **Почему это не жанровый шаблон**: Сохраняет полноценную возможность ведения прицельного огня от бедра с динамическим расширением FOV и занижением высоты хитбокса на 50%, оптимизированный под Three.js/Rapier3D raycast-контроллер.
- **Псевдокод тика**:
```text
function onSlidePhysicsTick(player, input, dt):
  if player.isGrounded and input.slidePressed and player.velocity.length() > 6.0 and player.slideCooldown <= 0:
    player.slideTimer = 0.90
    player.slideCooldown = 0.35 + 0.90
    player.state = SLIDE_GLIDING
    player.cameraTargetHeight = 0.75
    player.targetFOV = 92.0
    triggerFeedback('slide_spark_vfx', player.position)
  if player.state == SLIDE_GLIDING:
    player.slideTimer -= dt
    player.velocity = player.slideDirection * (8.0 * 1.35 * (player.slideTimer / 0.90 + 0.2))
    if player.slideTimer <= 0 or input.jumpPressed:
      player.state = SLIDE_INACTIVE
      player.cameraTargetHeight = 1.65
      player.targetFOV = 80.0
```

### Тактический БПЛА Разведчик «Оверлорд» (Tactical UAV Killstreak) (SECONDARY)
- **Category**: Источник тактического превосходства, мультипликатор темпа и инструмент ускоренного поиска целей на арене.
- **Description**: Синхронизирован с шейдерной подсветкой контуров врагов прямо в 3D-сцене от первого лица (а не только точками на плоской 2D-миникарте), сохраняя концентрацию игрока в прицеле.
- **Player Input**: ПК: Клавиша E / Цифра 4; Тач: Большая светящаяся кнопка «БПЛА РАЗВЕДКА» в правой верхней части интерфейса при наборе 3 фрагов.
- **Technical Complexity**: Medium
- **Решение игрока**: Активировать сканер немедленно для агрессивного пуша кластера врагов или сохранить его на сложный ранг лестницы (снайперская винтовка / РПГ-7).
- **Состояния**: `UAV_CHARGING`, `UAV_READY`, `UAV_ACTIVE`, `UAV_SWEEP_PULSE`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `killstreak_cost` = `3 фрага без смертей` — При 2 фрагах радар работает непрерывно весь раунд, убивая интригу позиционирования; при 5 фрагах в коротком 90-секундном матче большинство игроков ни разу его не вызовут.
  - `uav_active_duration` = `8.0 с` — При <4 с игрок не успевает добежать до обнаруженных врагов; при >14 с контейнеры теряют смысл как тактическое укрытие.
  - `radar_pulse_interval` = `1.5 с` — При 0 с (постоянный рентген) теряется ценность предугадывания траектории; при >3.0 с враг успевает сменить позицию и метка дезинформирует.
  - `wallhack_outline_alpha` = `0.85 непрозрачность` — При <0.4 метки теряются на фоне ярких текстур контейнеров; при 1.0 плотная заливка перекрывает прицельное перекрестие.
- **Слои отклика**:
  - Визуал/VFX: Появление красных голографических силуэтов врагов сквозь геометрию контейнеров (Three.js custom stencil outline shader), расходящаяся зеленая волна сканирования по миникарте.
  - Звук (Web Audio): Радиосообщение штаба («UAV Recon Online», 24 кГц комколл) + низкочастотный сонарный пинг (320 Гц с реверберацией 1.2 с) каждые 1.5 с.
  - Камера: Кратковременный глитч-эффект хроматической аберрации (0.008 на 0.15 с) в момент активации сканера.
  - UI/Тактильность: Разворачивание расширенной сетки радара в левом верхнем углу интерфейса со стрелками направления взглядов врагов, двойной вибро-импульс (60 мс + 60 мс).
- **Режим отказа игрока**: Игрок активирует БПЛА, слепо бежит за силуэтом через 3 ряда контейнеров и не замечает врага, выскочившего из бокового переулка прямо в упор; сигнал — звуковой маркер шагов вблизи и резкий красный индикатор урона с фланга.
- **Кривая мастерства**: Новичок на 30-й секунде жмет БПЛА сразу при получении и бежит по прямой на ближайшую точку; мастер копит БПЛА до сложного 10–11 ранга (снайперка) и производит точнейший префайр через щели между контейнерами ровно в тайминг выхода силуэта.
- **Сопротивление игры**: Боты при активации БПЛА не стоят на открытых прострелах, а занимают глухие углы в контейнерах и берут прострелы на опережение с перекрестным сектором обзора.
- **Синергии**:
  - Лестница Вооружений (упрощает прохождение сложных рангов снайперок через знание позиций врагов)
  - Вертикальный Перелаз (позволяет заранее забраться на контейнер над обнаруженной группой врагов)
- **Почему это не жанровый шаблон**: Синхронизирован с шейдерной подсветкой контуров врагов прямо в 3D-сцене от первого лица (а не только точками на плоской 2D-миникарте), сохраняя концентрацию игрока в прицеле.
- **Псевдокод тика**:
```text
function onUAVSystemTick(player, enemies, dt):
  if player.killstreakCount >= 3 and player.uavState == UAV_CHARGING:
    player.uavState = UAV_READY
    triggerFeedback('uav_ready_hud')
  if player.uavState == UAV_ACTIVE:
    player.uavTimer -= dt
    player.radarSweepTimer -= dt
    if player.radarSweepTimer <= 0:
      player.radarSweepTimer = 1.5
      for enemy in enemies:
        enemy.applyHighlightOutline(duration=1.4, color='#FF2A2A')
      triggerFeedback('sonar_pulse_audio')
    if player.uavTimer <= 0:
      player.uavState = UAV_CHARGING
```

### Дифференцированный Хит-Отклик с Зонами Уязвимости (Precision Hit-Zone & Stagger Dynamics) (SECONDARY)
- **Category**: Источник микро-мастерства (skill ceiling), регулятор риска и мгновенная тактильная награда за меткость.
- **Description**: Четкая дифференциация частоты Web Audio FM-синтеза формирует рефлекс «услышал звон — мгновенно перевел прицел на следующую цель», экономя доли секунды.
- **Player Input**: ПК: Высокоточное позиционирование мыши; Тач: Сенсорный аим-ассист малого радиуса (15 пикселей) с доводом в область шеи/головы при микро-коррекции пальцем.
- **Technical Complexity**: Medium
- **Решение игрока**: Потратить 0.15 с на доводку прицела в голову ради ваншота или залить корпус безопасным спреем с риском получить ответный урон.
- **Состояния**: `HIT_NONE`, `HIT_BODY`, `HIT_HEAD`, `HIT_LEGS`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `headshot_damage_multiplier` = `2.0x` — При 1.2x игроки перестают целиться в голову и спамят в туловище; при >3.0x любое оружие превращается в случайный мгновенный ваншот от шальной пули.
  - `stagger_slowdown_duration` = `0.35 с (замедление цели на 30%)` — При <0.1 с противник игнорирует урон в движении; при >0.8 с бойцы превращаются в недвижимые мишени, ломая динамику перестрелок.
  - `hitmarker_duration` = `0.18 с` — При <0.08 с мозг не успевает зафиксировать подтверждение попадания; при >0.4 с визуальный мусор перекрывает цель.
  - `hit_audio_pitch_headshot` = `1320 Гц (против 880 Гц в тело)` — Разница частот в 1.5 раза дает мгновенный интуитивный аудио-отчет о критическом попадании без отвлечения взгляда на интерфейс.
- **Слои отклика**:
  - Визуал/VFX: 4 белые диагональные риски при обычном попадании; золотисто-красный увеличенный хитмаркер со вспышкой искр и вылетом гильз при хедшоте (Three.js canvas overlay).
  - Звук (Web Audio): Звонкий щелчок «Hit-Beep» (FM-синтез 880 Гц, атака 2 мс, затухание 45 мс) + сочный металлический звон каски на частоте 1320 Гц при хедшоте.
  - Камера: Микро-толчок отдачи камеры вверх-назад (pitch +0.8°, z -0.01 м) и кратковременное радиальное затухание размытия при смертельном попадании.
  - UI/Тактильность: Всплывающее число урона над головой цели (белый «50», золотой «100 КРИТ»), короткий резкий виброотклик (15 мс тело, 35 мс с двойным ударом хедшот).
- **Режим отказа игрока**: Игрок пытается выцеливать исключительно голову на бегу со скорострельного дробовика, пули разлетаются мимо узкого хитбокса головы, и игрок погибает от плотного спрея в корпус; сигнал — отсутствие хитмаркера и свист пуль по стенке контейнера за спиной врага.
- **Кривая мастерства**: Новичок на 30-й секунде стреляет длинными очередями по ногам и корпусу, затрачивая по 1.5–2.0 с на фраг; эксперт делает короткие отсечки по 2 выстрела ровно в верхний срез хитбокса, совершая фраги за 0.25–0.4 с.
- **Сопротивление игры**: Боты в перестрелке используют резкие стрейфы (strafe-jiggle 4.5 м/с) и приседания в момент получения урона, сбивая прицел с головы.
- **Синергии**:
  - Лестница Вооружений (хедшоты позволяют мгновенно проходить ранги револьверов и снайперок)
  - Боевой Подкат (подкат занижает собственный хитбокс головы, усложняя попадание врагу)
- **Почему это не жанровый шаблон**: Четкая дифференциация частоты Web Audio FM-синтеза формирует рефлекс «услышал звон — мгновенно перевел прицел на следующую цель», экономя доли секунды.
- **Псевдокод тика**:
```text
function processBulletImpact(raycastHit, weaponDamage):
  hitZone = raycastHit.colliderZone
  multiplier = (hitZone == 'head') ? 2.0 : (hitZone == 'legs' ? 0.7 : 1.0)
  finalDamage = weaponDamage * multiplier
  raycastHit.targetEntity.applyDamage(finalDamage)
  if hitZone == 'head':
    triggerFeedback('headshot_audio_chink', 1320)
    triggerFeedback('headshot_golden_crosshair')
    vibrateDevice([15, 30, 35])
  else:
    triggerFeedback('body_hit_beep', 880)
    triggerFeedback('white_hitmarker')
    vibrateDevice([15])
```

### Вертикальный Перелаз Контейнеров и Срез Высоты (Rapid Container Vault & Flanking) (SECONDARY)
- **Category**: Инструмент вертикальной мобильности, захода во фланг и неожиданного изменения угла атаки.
- **Description**: Скоростной контекстный климбинг без потери инерции взгляда, оптимизированный под Three.js коллизии без застревания в геометрии ящиков.
- **Player Input**: ПК: Пробел (Прыжок) в упор к контейнеру высотой до 2.6 м; Тач: Двойной тап по кнопке прыжка или свайп вверх при приближении к препятствию.
- **Technical Complexity**: Medium
- **Решение игрока**: Лезть на крышу контейнера с риском стать открытой мишенью на 0.4 с ради обзора всей карты или оставаться в узком коридоре в относительной безопасности.
- **Состояния**: `VAULT_IDLE`, `VAULT_LATCH`, `VAULT_PULL_UP`, `VAULT_RECOVERY`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `max_vault_obstacle_height` = `2.60 м (высота стандартного контейнера)` — При <2.2 м игрок не может забраться на стандартный 20-футовый контейнер; при >3.2 м теряется реализм тактического спецназа.
  - `vault_animation_duration` = `0.40 с` — При <0.2 с действие выглядит неестественной телепортацией; при >0.75 с игрок гарантированно погибает в процессе анимации от любого стрелка.
  - `fall_damage_threshold` = `6.0 м` — Спрыгивание с одинарного контейнера 2.6 м не наносит урона и не замедляет бег, сохраняя динамику потока.
- **Слои отклика**:
  - Визуал/VFX: Тактические перчатки бойца хватаются за рифленый кант контейнера, ствол временно опускается в правый угол, легкая пыль слетает с края ящика.
  - Звук (Web Audio): Глухой тяжелый удар ботинок и ладоней о полый металлический лист контейнера (НЧ удар 140 Гц + скрежет металла 2400 Гц).
  - Камера: Двухфазная траектория: наклон вверх на +12° при зацепе, затем рывок вперед-вниз на -8° при выходе на крышу с возвратом в нейтраль за 0.15 с.
  - UI/Тактильность: Кратковременный индикатор высоты «[ВЕРХНИЙ ЯРУС: КРЫША]», тактильный виброотклик с нарастанием силы (10 мс -> 30 мс).
- **Режим отказа игрока**: Игрок пытается залезть на контейнер под огнем двух врагов, зависает в безоружной фазе подтягивания на 0.35 с и погибает до возвращения оружия в боевое положение; сигнал — красные стрелки входящего урона и прерванный звук рывка.
- **Кривая мастерства**: Новичок на 30-й секунде перемещается только по нижним коридорам; мастер использует подкат для разгона, влетает в перелаз, уничтожает врага сверху в прыжке и без остановки продолжает движение.
- **Сопротивление игры**: На крышах контейнеров отсутствуют укрытия: забравшийся боец виден со всех четырех сторон терминала и уязвим для перекрестного снайперского огня.
- **Синергии**:
  - Боевой Подкат (вход в зацеп контейнера на полной инерции скольжения)
  - БПЛА-Разведчик (атака обнаруженных за углом врагов сверху через открытый верх контейнера)
- **Почему это не жанровый шаблон**: Скоростной контекстный климбинг без потери инерции взгляда, оптимизированный под Three.js коллизии без застревания в геометрии ящиков.
- **Псевдокод тика**:
```text
function onVaultCheck(player, input, dt):
  if player.state == VAULT_IDLE and input.jumpPressed:
    obstacle = raycastLedgeCheck(player.position, player.forward, maxDist=1.2, maxHeight=2.6)
    if obstacle.isValidLedge:
      player.state = VAULT_PULL_UP
      player.vaultTimer = 0.40
      player.vaultTargetPos = obstacle.topPosition
      triggerFeedback('vault_metal_impact')
  if player.state == VAULT_PULL_UP:
    player.vaultTimer -= dt
    t = 1.0 - (player.vaultTimer / 0.40)
    player.position = vectorLerp(player.position, player.vaultTargetPos, t)
    if player.vaultTimer <= 0:
      player.state = VAULT_IDLE
```


---

## 3b. ⚠️ СТРОГИЙ ЗАПРЕТ ЖАНРОВЫХ ШАБЛОНОВ И КЛОНОВ (CRITICAL ANTI-CLICHÉ RULES)
Кодовый агент ОБЯЗАН реализовать уникальную игру, спроектированную в этом ТЗ, а не шаблонный автошутер:
1. **ЗАПРЕТ ШАБЛОННЫХ РОГАЛИКОВ И КАРТОЧЕК**: Если игра прямо не требует карточный драфт в GDD, **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** добавлять спам волн врагов и всплывающее окно «Выберите 1 из 3 карт апгрейда».
2. **ЗАПРЕТ СЕРЫХ ПРИМИТИВОВ НА ПУСТОЙ ПЛОСКОСТИ**: Создавай выразительную процедурную низкополигональную 3D/2D графику, точно соответствующую `ART_DIRECTION.md` (тематические персонажи, рельеф, модули, объекты окружения, частицы), а не бегающий куб на сером полу.
3. **ТОЧНОЕ СОБЛЮДЕНИЕ МЕХАНИК**: Реализуй все состояния, формулы, тайминги и слои отклика из `MECHANICS.md` (включая Web Audio звуки, импульсы камеры, визуал и тач-инпут).
4. **СПЕЦИФИЧЕСКОЕ УПРАВЛЕНИЕ**: Реализуй схему управления под конкретную механику этой игры (свайпы, жесты, траектории, физический дрифт, прицеливание), а не стандартный стик.

---

## 3c. ГОТОВОЕ: ЧТО УЖЕ НАПИСАНО И ОТЛАЖЕНО — SHOULD

Ниже — рабочий код фабрики: не примеры из статьи, а модули, которые крутились в
живых сценах, и в которых числа выставлены не наугад. Каждый файл объясняет в
своей шапке, почему константы именно такие — это та часть работы, которую с
нуля не воспроизвести за один заход.

**Правило.** Прежде чем писать механику с нуля, посмотри, нет ли её здесь.
Нашёл — тяни файл и подгоняй под свою игру. Не нашёл или не подходит — пиши
сам, но запиши в `DEVLOG.md` строку «взял/не взял <файл>, потому что …».
Молча проигнорировать готовое нельзя: это стоило нам рэгдолла, который писали
заново, хотя он лежал написанным.

Загрузка любого файла — одной командой, путь берётся из таблицы:

```bash
node scripts/fetch-knowledge.mjs workspace/knowledge-showcase/src/game/stealthSensing.ts
```

Файл появится в `docs/ref/<путь>`, оттуда копируй в `src/`.

Колонка «Готовность» — это часы работы, а не оттенок смысла:
- **копируется как есть** — ни одного импорта, чистая логика и числа. Взял, переименовал, поехал.
- **нужен three, больше ничего** — тянет только `three`, чужого проекта в нём нет.
- **образец, переписать под себя** — тянет модули стенда. Читать как показ того, как это собирается, а не копировать.

**Похоже, пригодится именно этой игре:**

| Файл | Что даёт | Строк | Готовность |
|---|---|---|---|
| `workspace/knowledge-showcase/src/audio/AudioManager.ts` | Web Audio: Procedural Sound Synthesizer & Analyser (Zero MP3 files) | 530 | копируется как есть |
| `workspace/knowledge-showcase/src/game/meleeCombat.ts` | Ближний бой: комбо-цепочка, окна отмены, парирование, hit-stop | 330 | копируется как есть |
| `workspace/knowledge-showcase/src/game/towerDefense.ts` | Tower defense: контракт волн, приоритет целей, экономика | 225 | копируется как есть |
| `workspace/knowledge-showcase/src/game/stealthSensing.ts` | Стелс: конус зрения, шкала подозрения, шум | 145 | копируется как есть |
| `workspace/knowledge-showcase/src/game/fightAnimStates.ts` | Список анимаций бойца в одном месте: чем вызывается и сколько длится | 131 | копируется как есть |
| `workspace/knowledge-showcase/src/game/rhythmAudio.ts` | Web Audio Timing, Rhythm Beat Sync & Muting Logic | 80 | копируется как есть |
| `workspace/knowledge-showcase/src/game/raceTrack.ts` | Трасса: одна кривая — один источник истины | 208 | нужен three, больше ничего |
| `workspace/knowledge-showcase/src/world/carRig.ts` | Процедурный low-poly болид. Никаких .gltf: вся геометрия — код | 72 | нужен three, больше ничего |
| `workspace/knowledge-showcase/src/demos/AudioRhythmDemo.ts` | Демо AudioRhythm: как rhythmAudio собирается в живую сцену | 283 | образец, переписать под себя |
| `workspace/knowledge-showcase/src/world/ragdoll.ts` | Рэгдолл на Rapier: семь тел, шесть сферических суставов | 214 | образец, переписать под себя |

Это не весь каталог: всего готовых модулей 67. Полный список с описаниями — в `LIBRARY.md` рядом с этим файлом. Загляни туда, когда возьмёшься за механику, которой нет в таблице выше.

---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Core Gameplay Engine**: 
- **Graphics & VFX Layer**: 
- **Input Management Layer**: 
- **UI/UX Presentation Layer**: 
- **Platform SDK Layer**: 

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   └── PhysicsWorld.ts        # Rapier3D / Physics world manager & colliders
├── entities/
│   ├── Player.ts              # Player entity & input handling
│   └── EntityManager.ts       # Dynamic entity pool & lifecycle
├── systems/
│   ├── WeaponLadderProgressionSystem.ts # Управление детерминированной последовательнос
│   ├── SlideFpsMovementPhysicsSystem.ts # Расчет кинематики перемещения, ускорения спри
│   ├── KillstreakDroneRadarSystem.ts # Отслеживание серий фрагов и активация тактиче
│   ├── AggressiveCqbCombatAiSystem.ts # Управление поведением соперников-ботов на аре
│   ├── MatchFlowVictoryResolutionSystem.ts # Контроль длительности матча (90 сек), отслежи
├── rendering/
│   ├── SceneManager.ts        # Three.js scene graph, lights, perspective camera lerp
│   ├── ProceduralModels.ts    # Styled models / geometry for Гангейм: Контейнерный Прорыв
│   └── ParticleSystem.ts      # Particle effects & visual feedback
├── ui/
│   ├── theme.css              # Единственное место со значениями: цвета, шрифты, шкала, слои
│   ├── UiRoot.ts              # Слои над канвасом, измеренный вьюпорт, safe-area + баннер
│   ├── ScreenRouter.ts        # Экраны как автомат: один видимый, один общий переход
│   ├── Hud.ts                 # Запись в закэшированные узлы только при изменении значения
│   ├── components/            # Button, IconButton, Panel, Modal, Toast, Meter, Stat, ListRow
│   ├── screens/               # По файлу на экран, каждый — со своими loading/empty/error
│   ├── icons.ts               # Один инлайновый SVG-спрайт, цвет через currentColor
│   └── TouchControls.ts       # Mobile touch input adapter
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic audio feedback
```

### Контракт модулей — MUST

Карта выше говорит, какие файлы завести. Ниже — чего им нельзя, потому что
именно на этом ломаются сгенерированные проекты:

- **DOM создаётся только в `src/ui/`.** Ни один файл вне `src/ui/components/` и
  `src/ui/screens/` не вызывает `document.createElement` и не присваивает
  `element.style.*`. Экран, собранный инлайновыми стилями в обход `theme.css`, —
  это второй экран, который перестанет совпадать с первым.
- **Значения — только в `src/ui/theme.css`.** Проверяется **B1**.
- **`src/core/` не знает про площадку.** Playgama Bridge живёт в
  `src/platform/`; всё остальное общается с ним через свой интерфейс. Иначе
  игру нельзя запустить локально без SDK.
- **`src/ui/` не знает про физику и рендер.** Интерфейс читает состояние
  через `EventBus` и не держит ссылок на `THREE.*` и на мир Rapier.
- **Числа игры — из `balance.yaml`.** Литерал скорости, урона, тайминга или
  радиуса в коде — баг: правка баланса не должна быть правкой кода.
- **Цикл обновления не аллоцирует.** Ни `new`, ни литерала объекта, ни
  `array.map` в кадре: пулы заводятся заранее. Проверяется **E4**.
- **Каждый модуль экспортирует один публичный класс или функцию** и не тянет
  импорт из соседнего слоя мимо своего интерфейса.

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Удвоение награды за победу (`double_match_rewards`)**: Удваивает заработанные за матч монеты и опыт ранга в 2 раза (Trigger: Экран результатов матча, Limit: После каждого матча)
- **Тактическое подкрепление (`revive_keep_streak`)**: Мгновенное возрождение на месте с сохранением текущего набранного киллстрика (Trigger: Экран гибели при серии >= 2 фрагов, Limit: 1 раз за матч)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"player_rank"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Landscape
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Визуальный контракт интерфейса
Интерфейс — первое, что видит игрок: до геймплея он смотрит на меню. Меню,
собранное из умолчаний браузера, обесценивает всё, что сделано за ним, и это
самый заметный признак недоделанной игры. Всё ниже — требование, а не пожелание.

**Материал интерфейса**: Тактический минималистичный плоский интерфейс с неоновыми оранжевыми и зелеными акцентами

**Визуальный язык**: Строгий тактический милитари-стиль с контрастными оранжевыми акцентами, чистой читаемостью врагов на фоне контейнеров

**Типографика**: Roboto Condensed / Rajdhani Bold — четкие рубленые военные шрифты с высокой читаемостью

**Сцена за меню.** Оперативник спецназа перезаряжает оружие в ангаре на фоне освещенного прожекторами контейнерного лабиринта

Меню, пауза и итог сессии рисуются ПОВЕРХ этой сцены. Непрозрачный слой на весь экран (`background: #111` на корне экрана, полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка допускается только под текстовым блоком и кнопками. Игровой цикл в меню продолжает крутиться на сниженной нагрузке — камера едет, сцена живёт. Игрок обязан понять, во что он играет, до нажатия «Играть»; меню на глухой заливке — самый заметный признак недоделанной игры, и это первое, что увидит и игрок, и модератор площадки.

**Акценты — один цвет = один смысл** (разный цвет у каждой кнопки «чтобы отличались» читается как мусор):

| Токен | Единственный смысл |
|---|---|
| `danger_health` | #D50000 |
| `hitmarker_headshot` | #FF1744 |
| `hitmarker_normal` | #FFFFFF |
| `primary_action` | #FF6D00 |
| `uav_active` | #00E676 |

**Набор компонентов — закрытый.** Всё на экране обязано быть одним из них; одноразовый `<div>` с инлайновыми стилями — это баг, из-за которого второй экран перестаёт совпадать с первым:
- Top Match Info Bar (Timer, Frags, Leading Player)
- Center Reticle & Dynamic Hitmarker Crosshair
- Bottom Weapon Card (Current Weapon, Ammo, Ladder Step 1/12)
- Top-Left Mini-Radar with UAV sweep wave
- Bottom-Right Killstreak UAV Callout Button
- Left Slide Indicator & Touch Action Zone

**Якоря HUD** — пять и только пять; посередине экрана не висит ничего, кроме временной обратной связи:

| Якорь | Что там |
|---|---|
| `bottom_center` | Current Weapon Name, Ammo Count & Ladder Step |
| `bottom_right` | Mobile Fire & ADS Buttons / UAV Killstreak Trigger |
| `top_center` | Match Countdown Timer & Kill Counter |
| `top_left` | Mini-Radar / UAV Status Indicator |

**Экраны и их композиция.** У каждого экрана три зоны: чем он себя называет, единственное главное действие (самое крупное и единственное с основным акцентом) и второстепенный ряд одним весом. Карточка с колонкой кнопок по центру — не композиция, а её отсутствие:
- **MainMenu** · Главное меню: выбор камуфляжей, просмотр таблицы лидеров и большая кнопка В БОЙ · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **HUD** · Игровой экран: минималистичный HUD без загромождения зоны обзора · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **PauseMenu** · Меню паузы: настройки чувствительности мыши/тача, звука и кнопка выхода · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **VictoryDefeat** · Экран итогов: занятое место, количество фрагов, кнопка X2 награды за просмотр рекламы · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

**Состояние, показанное миром, а не оверлеем**:
- Светящаяся марка коллиматора на оружии
- Цифровой счетчик патронов на боковой планке футуристичного магазина
- Зеленый лазерный целеуказатель при наведении на цель

**Переходы экранов**: Splash -> Main Menu -> Matchmaking (1.5s) -> Combat HUD -> Post-Match Victory/Defeat -> Reward Double -> Main Menu

**Отклик интерфейса на действие**:
- Белый и красный хитмаркер с тактильным щелчком при попадании
- Анимация вспышки оружия и мгновенный щелчок смены ранга лестницы
- Голографическая волна сканирования при активации БПЛА
- Эффект замедления времени (Slow-Mo) при совершении финального 12-го фрага

**Состояния экранов** — загрузка, пустота и ошибка проектируются наравне с удачным путём: сохранение, таблица лидеров, покупки и реклама на этих площадках отваливаются регулярно:
- Loading
- InGame_Combat
- InGame_Sliding
- InGame_UAVActive
- Paused
- MatchFinished_Win
- MatchFinished_Lose

**Значения выводятся из этого мира, а не берутся из примеров.** Имена токенов (`--color-primary`, `--font-display`, `--space-*`) одинаковы во всех играх — на них держится код компонентов. Сами значения, обе гарнитуры, силуэт рамки и язык иконок выводятся из материала интерфейса, названного выше, по процедуре из раздела 12 `knowledge/ux/ui_design_system.md`: из чего сделана панель, при каком свете она лежит, чем в этом мире помечают главное и опасное, чем написаны цифры, как режут край. Скопировать палитру, шрифты или фаску из примера в базе знаний или из другой игры — это тот же дефект, что и оставить умолчания браузера: интерфейс выглядит решением, принятым не для этой игры. Обратная проверка: если от интерфейса другой игры фабрики ваш отличается только оттенком акцента — процедура была пропущена.

**Обязательные технические правила интерфейса** (каждое — из починенного бага, подробности в `knowledge/ux/ui_design_system.md` и `knowledge/ux/ui_implementation.md`):
- Все значения — токенами в одном `src/ui/theme.css`: цвета, две гарнитуры, шкала отступов, радиусы, длительности, `--z-*`. Литерал `#RRGGBB` или `z-index: 9999` внутри экрана запрещён; `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` мимо темы обязан быть пуст.
- Слои над канвасом прозрачны для ввода: контейнер — `pointer-events: none`, `auto` включают только листовые интерактивные элементы. Слой HUD не кликается никогда. Полноэкранный оверлей, оставленный на `auto`, съедает игровой ввод — и это выглядит как «на телефоне не работает управление».
- Интерфейс тянется одним множителем `--ui-scale`, а не отдельной вёрсткой под каждый телефон, — но множитель обязан где-то **читаться**. Он объявляется в `theme.css` брейкпоинтами (`:root { --ui-scale: 1 }`, `@media (max-width: 720px)` → `0.86`, `@media (max-width: 420px)` → `0.74`) и умножает отступы и кегли: `padding: calc(var(--space-4) * var(--ui-scale))`, `font-size: clamp(14px, calc(16px * var(--ui-scale)), 18px)`. Посчитать масштаб в JS и не использовать его ни в одном правиле — ровно тот же результат, что не считать вовсе: интерфейс, свёрстанный в фиксированных `px`, на 360-пиксельном экране разъезжается. Ноль `@media` в `theme.css` — признак, что этот пункт пропущен.
- Портрет и ландшафт — два разных экрана, а не один и тот же в другом масштабе. Вертикальный FOV камеры пересчитывается под аспект (иначе в портрете горизонтальный обзор схлопывается в трубу), а раскладка HUD и кнопок проверяется в обеих ориентациях.
- Размеры зон нажатия НЕ масштабируются: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px — палец не уменьшается вместе с экраном. Ниже 460 px по высоте кнопки допускается ужать до 56 px, и это единственное исключение.
- Меняющиеся числа — `font-variant-numeric: tabular-nums` в слоте фиксированной ширины; полосы — `transform: scaleX()`, не `width`. HUD пишет в закэшированные узлы и только при изменении значения: пересборка разметки в кадре стоит кадров.
- Один экран виден за раз, скрытый — `display: none`; переход один на всю игру и укладывается в 300 мс. Анимируются только `transform` и `opacity`. `prefers-reduced-motion: reduce` убирает трансформации.
- Любое нажатие отвечает в том же кадре; действие с ожиданием переводит кнопку в `loading` и снимает его в `finally`.
- Запрещено: `alert`/`confirm`/`prompt`, эмодзи вместо иконок (один инлайновый SVG-спрайт с `currentColor`), голый `<input type=range>` и `<select>`, текст над геймплеем без подложки или обводки, серая неактивная кнопка вместо отсутствующей на площадке возможности — такой элемент не рисуется вовсе.
- Геометрия меню считается от ИЗМЕРЕННОГО вьюпорта (`visualViewport`, пересчёт после поворота, выхода из фуллскрина и закрытия рекламы), а не от `100vh`; нижний отступ включает измеренную высоту липкого баннера. Страница под игрой не скроллится — длинные списки скроллятся во внутреннем контейнере.
- `backdrop-filter: blur()` на полноэкранном слое — самое дорогое свойство оверлея на мобильных GPU; только на маленьких панелях.

### Контракт тач-управления
- **Раскладка этой игры** (спроектирована в MOBILE_CONTROLS.md, переносить сюда раскладку из другой игры нельзя): Левая зона экрана: непрерывный спринт и свайп вниз для подката; Правая зона экрана: свободный поворот взгляда и крупная кнопка огня; Верхняя кнопка: вызов БПЛА.
- Раскладка выводится из главного действия игрока. Виртуальный джойстик уместен там, где игрок непрерывно ведёт персонажа; там, где действие точечное (тап по цели, свайп, удержание), джойстик — лишний элемент, который занимает половину экрана и ничего не делает.

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Если в раскладке есть стик** — он плавающий: зона захвата — вся левая
  половина экрана, база появляется под пальцем, мёртвая зона 8%, иначе
  управление дрожит. Если раскладка обходится без стика — не добавляйте его.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `WASD` / стрелки — движение
- Мышь — обзор через `requestPointerLock` (захват просится из обработчика нажатия, иначе браузер его не даст)
- ЛКМ — огонь: автоматическое оружие стреляет **пока кнопка удерживается**, полуавтомат — по событию нажатия
- ПКМ — прицеливание
- `R` — перезарядка, `Q` / колесо / `1`–`3` — смена оружия
- `Space` — прыжок, `Ctrl` — присед, `Shift` — тихий шаг (любую из трёх можно не делать, но тогда см. правило о вертикали ниже)
- `P` / `Esc` — пауза

**Раскладка выше — отправная точка под похожие игры, а не список того, что умеет ЭТА игра.** Профилей в фабрике меньше, чем жанров, и любой шаблон заведомо описывает не вашу игру: в нём есть лишние клавиши и нет нужных. Источник истины — глаголы игрока, перечисленные ниже.

**Контракт: каждый глагол получает и клавишу, и кнопку.** Таблицу «глагол → клавиша → тач-кнопка → что происходит на экране и в звуке» положи в `DESIGN.md` до первой строчки кода по управлению. Глагол без строки в таблице не реализован; клавиша из шаблона, которой в таблице нет, — удаляется.

**Отдельно решается то, чего в списке механик обычно нет, но игрок ждёт по виду уровня.** Вертикаль (прыжок, присед, тихий шаг), взаимодействие с предметом, отмена действия. Каждое — либо сделано (вертикаль — это гравитация, опора лучом по геометрии и высота глаз от ступней, а не константа пола), либо осознанно не сделано, и тогда в `DESIGN.md` стоит строка почему, **а уровень не обещает того, чего игрок не может**: без прыжка на карте нет ящиков, выступов и укрытий по пояс, к которым тянет забраться.

**Глаголы игрока этой игры** (каждому нужна строка в таблице управления):
- **Лестница Вооружений с Мгновенной Трансформацией (Instant Gun Ladder Morph)** — игрок делает это так: ПК: ЛКМ — огонь, ПКМ — тактический зум, Q — удар прикладом; Тач: Правая половина экрана — плавающая кнопка огня с трекингом пальца, кнопка зума, свайп вниз для подката.
- **Боевой Подкат с Инерцией и Низкопрофильной Стрельбой (Combat Slide & Hipfire)** — игрок делает это так: ПК: Зажатый Shift (Спринт) + Нажатие C / Ctrl (Подкат); Тач: Свайп вниз по правой половине экрана во время движения виртуального стика вперед.
- **Тактический БПЛА Разведчик «Оверлорд» (Tactical UAV Killstreak)** — игрок делает это так: ПК: Клавиша E / Цифра 4; Тач: Большая светящаяся кнопка «БПЛА РАЗВЕДКА» в правой верхней части интерфейса при наборе 3 фрагов.
- **Дифференцированный Хит-Отклик с Зонами Уязвимости (Precision Hit-Zone & Stagger Dynamics)** — игрок делает это так: ПК: Высокоточное позиционирование мыши; Тач: Сенсорный аим-ассист малого радиуса (15 пикселей) с доводом в область шеи/головы при микро-коррекции пальцем.
- **Вертикальный Перелаз Контейнеров и Срез Высоты (Rapid Container Vault & Flanking)** — игрок делает это так: ПК: Пробел (Прыжок) в упор к контейнеру высотой до 2.6 м; Тач: Двойной тап по кнопке прыжка или свайп вверх при приближении к препятствию.

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/sozday_igru_po_tipu_call_of_duty_002/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ДИЗАЙН: ЕГО ПИШЕШЬ ТЫ — MUST

Готового арт-дирекшена в этом пакете нет намеренно. Дизайн, продиктованный
списком полей, получается одинаковым от игры к игре, и узнать в нём конкретную
игру невозможно. Поэтому первым делом, до кода, ты пишешь `DESIGN.md` сам.

**Что в нём должно быть** (без этих пяти пунктов файл не считается написанным):

1. **Одна фраза о том, как игра выглядит.** Не «мрачный киберпанк», а что
   именно видит игрок в первую секунду.
2. **Палитра** — 5–7 цветов с ролями (фон, поверхность, текст, акцент, опасность,
   успех) и HEX. Они же становятся токенами в `theme.css`.
3. **Камера и свет** — где стоит камера, что она захватывает, откуда светит и
   почему сцена читается на телефоне.
4. **Экраны** — для каждого: что на нём, где три зоны (заголовок, содержимое,
   действие), какое действие главное.
5. **Сцена за меню** — что живёт на фоне главного меню. Не заливка, не блюр
   поверх ничего: работающая сцена игры под пониженной нагрузкой.

**Чего в нём быть не должно** — это уровень **MUST**, проверяется приёмкой:
эмодзи вместо иконок, `alert()` вместо модалки, непрозрачная плашка поверх
канваса, фиолетово-синий градиент, интерфейс из умолчаний браузера.

**Отправная точка от фабрики** — это подсказка, а не приговор. Не подошло —
меняй и запиши в `DEVLOG.md`, что и почему:

- Стиль: Стилизованный тактический милитари-реализм (Semi-Realistic CQB)
- Камера: First-Person Perspective (Вид от первого лица) (FOV 75°, наклон 0°)
- Среда: Морской контейнерный терминал под моросящим дождем (лабиринт из контейнеров в стиле Shipment)
- Свет: Сумеречный терминал с яркими точечными прожекторами на вышках, мокрым отражающим асфальтом и сигнальными фаерами
- Тема интерфейса: Тактический минималистичный плоский интерфейс с неоновыми оранжевыми и зелеными акцентами (реализация — секция 6)
- Сцена за меню: Оперативник спецназа перезаряжает оружие в ангаре на фоне освещенного прожекторами контейнерного лабиринта

Подробности отбора — в `ART_DIRECTION.md` и `UI_UX_SPECIFICATION.md` рядом.

---

## 8. РОАДМАП

Фаза закончена не тогда, когда код написан, а когда закрыты её проверки из
`ACCEPTANCE.md`. Прогоняй `node scripts/check-spec.mjs` в конце каждой фазы.

| Фаза | Чем закрывается |
|---|---|
| Подготовка | **F4**, **F1** — база загружена в `docs/ref/`, `DESIGN.md` написан |
| Каркас и площадка | **A1**, **A3**, **C1–C4** — игра собирается и стартует на площадке |
| Главная механика | **D1**, **B6** — в неё можно играть, ввод доходит до игры |
| Остальные механики и петля | **D2–D5**, **B9–B11** — петля проходится целиком |
| Интерфейс и подача | **B1–B5**, **B7–B8**, **B12** — интерфейс собран из токенов, меню стоит на живой сцене |
| Полировка и релиз | **C5–C11**, **E1–E5**, **A2**, **A4**, **F2–F3** — сохранения, реклама, кадры, сборка |

### Phase 1: Прототип FPS-контроллера и физики подката (2 days)
- **Deliverable**: Рабочий прототип перемещения бойца с плавным подкатом и вскидкой оружия
  - Интеграция Three.js сцены и физики Rapier3D
  - Реализация FPS-контроллера с инерцией спринта и подкатом
  - Настройка камеры от первого лица с эффектом покачивания и наклона
### Phase 2: Арена из контейнеров, стрельба и лестница Gun Game (2 days)
- **Deliverable**: Собранная арена Shipment и работающая лестница смены 12 стволов за фраги
  - Построение 3D геометрии контейнерной арены с материалами мокрого асфальта
  - Хитскан система стрельбы, разброс пуль и дифференцированные хитбоксы
  - Логика лестницы Gun Game с мгновенной заменой моделей оружия
### Phase 3: ИИ ботов, киллстрики БПЛА и тактильный аудио-фидбек (2 days)
- **Deliverable**: Умные боты на NavMesh, летающий БПЛА с подсветкой врагов и сочные хитмаркеры
  - Навигация NavMesh и боевой ИИ ботов с подкатами и укрытиями
  - Система киллстрика БПЛА с шейдерной подсветкой силуэтов сквозь стены
  - Интеграция аудио Web Audio API (выстрелы, щелчки хитмаркера, рация)
### Phase 4: Мобильное управление, UI/HUD, оптимизация 60 FPS и SDK (2 days)
- **Deliverable**: Полноценная игра 60 FPS со звуком, двухзонным тачем и SDK Playgama Bridge
  - Двухзонный тач-интерфейс со свайпом подката для мобильных
  - HUD интерфейс (радар, лестница оружия, таймер, хитмаркер)
  - Подключение Playgama Bridge SDK 2.x (сохранения, лидерборды, реклама)
  - Оптимизация дроуколлов, инстансинг гильз и финальное тестирование

---

## 9. ПРАВИЛА ПЛОЩАДКИ — УРОВЕНЬ MUST
Каждое правило ниже — след бага, дошедшего до продакшена, или отказа модерации.
Они перекрывают любую привычку, туториал и пример — включая куски из документации
Playgama и Яндекса, многие из которых описывают устаревший контракт Bridge v1.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

### Orientation & pointer lock

69. `Object3D.lookAt` aims the object's **+Z** at the target; −Z is the convention
    for cameras and lights only. Geometry that will later be aimed with `lookAt`
    (viewmodel arms, tracers, arrows, turret beams) must be built along **+Z**.
    Built along −Z it points backwards after aiming, and the bug reads as "broken
    model" rather than a flipped axis.

70. `requestPointerLock()` is called **from the pointer-down handler**, never from
    the frame loop: after an Esc exit a request issued inside
    `requestAnimationFrame` is silently rejected and the game looks dead. While
    `document.pointerLockElement` is set, application hotkeys (level switch, menu,
    tabs) must stay silent — otherwise an in-game key throws the player out of the
    game.

71. A `MeshStandardMaterial` with high `metalness` and **no environment map
    renders black** — metal has nothing to reflect. Set `scene.environment`
    (a `RoomEnvironment` PMREM costs no file and no request) **and** keep
    `metalness ≤ 0.4` in the material: the environment is the first thing a
    low tier drops, and the asset must not turn into a hole in the frame
    when it does.

72. A node whose base orientation is not zero — anything aimed with
    `lookAt`, `setFromUnitVectors` or a baked rest pose — is animated
    through a **wrapper**, never by writing its own `rotation`. Code that
    returns a limb to rest with `rotation.x = lerp(rotation.x, 0, k)` reads
    the Euler of that very quaternion and erases the aim: the part turns
    away from what it was holding, and the bug reads as "the model is
    missing a piece".

73. Anything positioned from **world** coordinates — a prop placed by two
    hand positions, an effect anchored to a bone, a decal following a
    moving platform — must be converted into the parent's local space
    before it is written into `position`/`quaternion`. Writing world
    numbers into a child of a transformed node applies that transform a
    second time, and the object drifts away by exactly the owner's offset
    from the origin. It stays invisible while the owner sits at the origin
    (which is where every unit test and every bake script puts it), and
    appears only in the game, as "the weapon is flying around the level".

74. Where the character **looks** and where the weapon **points** are two
    different directions, and gameplay owns the second one. A mocap firing
    stance is bladed: the axis through the hands is 40–60° away from the
    model's forward, so turning the body at the target aims the barrel past
    it. Measure the offset once on the assembled rig, turn the body by
    `heading − offset`, and take up the remainder by rotating the chest —
    with an exact quaternion `from barrel to target`, not by tweaking a
    single Euler axis: a barrel that sits at an angle to the rotation plane
    turns by only `angle · cos(that angle)`.

### Interface

75. UI layers over the canvas are **transparent to input by default**: every
    layer container is `pointer-events: none` and only leaf interactive elements
    turn it back on. A full-screen overlay left at `auto` swallows every gameplay
    pointer, and the bug reads as "the car does not steer on mobile". The HUD
    layer is read-only and never becomes interactive at all — pause and settings
    are buttons on the screen layer.

76. Every colour, font, radius, spacing step and duration comes from tokens in
    one theme file. A literal `#RRGGBB`, `px` padding or `z-index: 9999` written
    inside a screen is the reason the second screen stops matching the first.
    `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` minus the theme file returns nothing.
    Имена токенов при этом одинаковы во всех играх, а **значения — нет**: палитра,
    обе гарнитуры и силуэт рамки выводятся из материала мира этой игры по
    процедуре в `knowledge/ux/ui_design_system.md`, раздел 12. Скопированный из
    примера или из соседней игры набор значений — такой же дефект, как умолчания
    браузера, только заметнее: он выглядит осознанным решением, принятым не для
    этой игры.

77. Browser dialogs (`alert`, `confirm`, `prompt`) and emoji used as icons are
    **banned in shipped UI**. The first blocks the game loop, cannot be styled and
    breaks immersion; the second renders differently on every OS and reads as a
    placeholder. Use the project's `Modal` component and one inline SVG sprite
    coloured by `currentColor`.

78. The HUD updates by writing to **cached nodes** and only when the value
    changed — never by rebuilding markup inside the frame loop, and never by
    reading layout (`offsetWidth`, `getBoundingClientRect`) there. Changing
    numbers carry `font-variant-numeric: tabular-nums` in a fixed-width slot, and
    bars animate with `transform: scaleX()`, not `width`; otherwise the row
    reflows on every tick and the whole overlay repaints.

79. Every screen defines its **loading, empty and error** states, not only the
    happy path. Saves, leaderboards, purchases and ads are network calls that fail
    routinely on these portals; a failure that renders an empty frame or says
    nothing costs the player their run and reads as a broken game.

80. Menu and HUD geometry is sized against the **measured** viewport
    (`visualViewport`, re-measured after orientation change, fullscreen exit and
    ad close), never `100vh`, and every UI layer is inset by
    `env(safe-area-inset-*)` **plus** the measured sticky-banner height. The page
    itself never scrolls — long content scrolls in an inner container.
    See `knowledge/ux/ui_design_system.md` and `knowledge/ux/ui_implementation.md`.

81. Меню, пауза и экран итога сессии рисуются **поверх живой игровой сцены**, а
    не поверх заливки. Тот же рендерер, та же сцена, медленная камера, работающий
    свет; игровой цикл в меню продолжает крутиться на сниженной нагрузке.
    Непрозрачный слой на весь экран (`background: #111` на корне экрана,
    полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка
    допускается только под текстовым блоком и кнопками. Меню — это первое и
    иногда единственное, что видит игрок: за глухой плашкой с колонкой кнопок
    вся работа над геймплеем невидима, и игра читается как недоделанная ещё до
    запуска. Постановку сцены задаёт `ART_DIRECTION.md` (раздел «Сцена за меню»).

82. У каждого экрана **три зоны**: чем экран себя называет, единственное главное
    действие (самое крупное и единственное с основным акцентом) и второстепенный
    ряд одним весом. Одна карточка с колонкой кнопок по центру — не композиция, а
    её отсутствие; композиция каждого экрана задана в `UI_UX_SPECIFICATION.md`,
    раздел «Каталог экранов», и придумывать её заново не нужно.

### Разделы, которые открываются по месту
Эти правила нужны не с первой строки, а когда доберёшься до соответствующей области. Полный текст — в `docs/ref/knowledge/CRITICAL_RULES.md` (файл обязательный, приезжает шагом 0). Обязательность от переноса не меняется.
- **Stack** — выбор библиотек стека — перед подключением любой из них
- **Renderer** — настройки рендерера — когда собираешь сцену
- **Physics vehicles** — физика транспорта — только если в игре есть машины

---

## 10. ПРИЁМКА — MUST

Полный список проверок с номерами лежит в `ACCEPTANCE.md`, рядом с этим файлом.
Он и есть определение готовности: пока хотя бы один пункт разделов **A–C**
красный, игра не готова — это не качество, это работоспособность.

Статическая часть проверяется командой (скрипт уже лежит в проекте, зависимостей
не требует — пропиши его в `package.json` как `check:spec`):

```bash
node scripts/check-spec.mjs
```

Прогонять после каждой фазы роадмапа, а не один раз в конце. Результат
записывать строкой в `DEVLOG.md`. Пункт, который не проходит, честнее оставить
красным с объяснением, чем отметить зелёным: следующий, кто откроет проект,
будет считать его проверенным.

Критерии, которые относятся именно к этой игре:

- [ ] Разработан отзывчивый FPS-контроллер от первого лица с тактическим подкатом (Slide) и прицеливанием (ADS)
- [ ] Собрана компактная симметричная 3D арена из грузовых контейнеров с мокрым асфальтом и прожекторами
- [ ] Реализована детерминированная лестница из 12 видов оружия с мгновенной сменой за фраги
- [ ] Внедрена система киллстриков БПЛА с подсветкой силуэтов врагов сквозь стены на 8 секунд
- [ ] Настроен агрессивный ИИ ботов с реакцией на шаги и мгновенным респауном за 1.0 сек
- [ ] Сделан сочный хитмаркер и пространственный 3D-звук выстрелов и попаданий
- [ ] Реализовано адаптированное двухзонное мобильное управление без джойстика
- [ ] Интегрирован Playgama Bridge SDK 2.x (сохранения, лидерборды, реклама)
- [ ] Обеспечен стабильный фреймрейт 60 FPS на мобильных устройствах и бандл < 3.8 МБ
- [ ] Project builds cleanly with zero TypeScript errors (`npm run build`).
- [ ] Playgama Bridge initializes and dispatches GAME_READY platform message.
- [ ] Петля играбельна целиком: Спринт и подкат в узкий коридор контейнеров -> Захват цели и точный отстрел противника -> Мгновенная автоматическая смена оружия на следующий уровень лестницы -> Активация разведывательного БПЛА за серию фрагов -> Достижение 12-го финального фрага и победа в матче..
- [ ] Реализованы механики проекта: Лестница Вооружений с Мгновенной Трансформацией (Instant Gun Ladder Morph), Боевой Подкат с Инерцией и Низкопрофильной Стрельбой (Combat Slide & Hipfire), Тактический БПЛА Разведчик «Оверлорд» (Tactical UAV Killstreak), Дифференцированный Хит-Отклик с Зонами Уязвимости (Precision Hit-Zone & Stagger Dynamics).
- [ ] Условие успеха (Совершить 12 фрагов быстрее всех соперников, пройдя полную цепочку из 12 видов оружия и сделав финальное убийство из золотого РПГ/ножа за отведенные 90 секунд.) и условие проигрыша (Если другой боец на арене совершает 12-й фраг первым, либо по истечении таймера в 90 секунд игрок не занимает 1-е место по числу фрагов.) работают.
- [ ] Тач-управление отвечает без задержки, страница под игрой не скроллится.
- [ ] Интерфейс собран из токенов одного `src/ui/theme.css`: в экранах нет литералов цвета, шрифта и `z-index`.
- [ ] Слои интерфейса прозрачны для игрового ввода — управление работает под всеми оверлеями, слой HUD не кликается.
- [ ] У каждого экрана работают состояния загрузки, пустоты и ошибки; возможность, которой нет на площадке, не нарисована вовсе.
- [ ] Меню и HUD помещаются в измеренный вьюпорт с учётом safe-area и баннера, а с закрытым игровым полем меню узнаётся как «Гангейм: Контейнерный Прорыв».
- [ ] За меню и экраном итога видна живая игровая сцена: полноэкранной непрозрачной заливки поверх канваса нет, подложка только под текстом и кнопками.
- [ ] Ни одного эмодзи в подписях интерфейса: иконки — инлайновый SVG с `currentColor`.
- [ ] Rewarded-реклама выдаёт награду ровно один раз (Удвоение награды за победу, Тактическое подкрепление); interstitial соблюдает паузу 90 с.
- [ ] Audio and physics automatically pause/resume on tab blur and ad display.
- [ ] Persistent progress saves and loads from Playgama Cloud Storage.
- [ ] Runs at steady 60 FPS on desktop and >= 50 FPS on mobile.

---

## 11. ГДЕ ЛЕЖИТ ОСТАЛЬНОЕ

База знаний не лежит в пакете копиями — она приезжает по требованию:

```bash
node scripts/fetch-knowledge.mjs        # всё обязательное → docs/ref/
node scripts/fetch-knowledge.mjs <путь> # отдельный файл
```

Что именно тянуть, перечислено в `knowledge.manifest.json`. После загрузки адреса ниже открываются как обычные локальные файлы, и сеть больше не нужна.

Набор отобран под этот проект: Проект опирается на связку высокоскоростного FPS-контроллера с механикой подкатов и процедурной генерацией арены терминала на Three.js/Rapier3D, быстрый хитскан через three-mesh-bvh, шейдерный визор БПЛА через postprocessing, процедурный Web Audio синтез и оптимизацию под мобильные браузеры.

**Строка после адреса — ярлык документа, а не его содержание.** Она названа так, чтобы документ можно было отличить от соседнего, и покрывает от силы десятую часть файла: за ярлыком «управление от первого лица и покачивание вьюмодели» в прошлый раз лежали гравитация, прыжок, вторая рука на вьюмодели, анимации перезарядки, пружина отдачи и хитмаркер — и ничего из этого в игру не попало, потому что файл сочли уже понятным. Документы ядра открываются целиком, до первой строчки кода по своей теме.

Пункты `- [ ]` под адресом — чек-лист самого документа, дословно. Это не пожелания: каждый пункт закрывает уже сделанную кем-то ошибку и проверяется взглядом на запущенную игру.

Здесь стоят чек-листы только тех документов, что отобраны под эту игру. **Полный список, включая платформенные документы, лежит в `ACCEPTANCE.md`, раздел H** — там же он и закрывается: у каждого пункта либо `[x]`, либо `[~]` со строкой причины. Незакрытый пункт валит `node scripts/check-spec.mjs`, и проект не считается готовым.

**Ядро — прочитать до начала реализации:**
- `docs/ref/knowledge/patterns/arena_combat_loop.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Задает цикл сессионного матча арена-шутера на 60–90 секунд с гонкой до 12 фрагов и мгновенным респауном.
  - [ ] Волна заканчивается сама и видно, что она кончилась: пауза, счёт, следующая
  - [ ] Враги отличаются поведением, а не только здоровьем: скирмишер, щитоносец и брут требуют разных ответов
  - [ ] Опасности арены участвуют в бою, а не служат декорацией
  - [ ] Между волнами есть фаза покупки, и потраченное видно в следующем бою
  - [ ] Выносливость ограничивает натиск: бесконечное закликивание не работает
  - [ ] На пятой волне появляется мини-босс, требующий другого приёма, а не большего урона
  - [ ] Смерть даёт итог забега с накопленной валютой, а не пустой экран
  - [ ] Есть внятная причина запустить второй забег: другая сборка оружия или сложность
- `docs/ref/knowledge/threejs/fps_controller_and_shooting.md` (выжимка — `skills/RENDERER_SKILL.md`) — Реализует управление от первого лица, механику боевого подката с занижением хитбокса, мгновенную трансформацию оружия и стрельбу.
  - [ ] `forward`/`right` выведены из `yaw` с правильными знаками, покрыты тестом
  - [ ] Прыжок с гравитацией, койот-таймом и буфером нажатия
  - [ ] Опора — луч по геометрии уровня, а не константа пола
  - [ ] Захват мыши запрашивается из обработчика нажатия; горячие клавиши приложения молчат под захватом
  - [ ] Кнопки указателя различаются (ЛКМ ≠ ПКМ)
  - [ ] Полуавтомат читает событие, автомат — удержание
  - [ ] Минимум три ствола, различающихся режимом огня, а не только уроном
  - [ ] Патроны и запас — у каждого ствола свои
  - [ ] Вьюмодель с ДВУМЯ руками: наводка в отдельном узле, хват на видимом борту, оружие с развалом
  - [ ] Геометрия под `lookAt` построена вдоль +Z
  - [ ] Реквизит в руках переведён из мировых координат в локальные
  - [ ] Наводка оружия откалибрована отдельно от разворота тела
  - [ ] Смерть — рэгдолл с импульсом вдоль пули, оружие выпадает из рук
  - [ ] `metalness ≤ 0.4` и `scene.environment` — иначе металл чёрный
  - [ ] Положение вьюмодели «от бедра» и «в прицеле» — своё у каждого ствола
  - [ ] Позы бедро/прицел/бег интерполируются, не переключаются
  - [ ] Анимации перезарядки и смены ствола; подмена модели на дне дуги
  - [ ] Отдача двухуровневой пружиной; подброс камеры оседает не до нуля, но осевшая часть тает
  - [ ] Вспышка, трассер, декаль, искры, гильза, дым, взрыв, хитмаркер
  - [ ] Прицел показывает текущий разброс и убирается в прицеливании
  - [ ] Импульсные эффекты постобработки — через `blendMode.opacity`, без пересборки конвейера
  - [ ] `frustumCulled = false` на всех мешах с мировыми инстансами
  - [ ] Головной прогон и снимки из браузера в `check:all`
- `docs/ref/knowledge/threejs/shooter_enemy_ai_and_combat.md` (выжимка — `skills/RENDERER_SKILL.md`) — Определяет поведение ботов-противников на арене, регистрацию урона по хит-зонам и мгновенный респаун за одну секунду.
  - [ ] Скорострельное оружие — hitscan; луч по уровню через `three-mesh-bvh`.
  - [ ] Зоны урона — сферы (голова/тело/конечности), не меш.
  - [ ] Разброс детерминирован от seed; первый выстрел точный.
  - [ ] TTK задан явно; игрок живёт в 2–3 раза дольше врага.
  - [ ] У врагов есть время реакции, очереди с паузами и промах первой очередью.
  - [ ] Одновременно атакуют не больше 2–3 врагов (токены атаки).
  - [ ] Проверка видимости 10 Гц, распределена по кадрам.
  - [ ] Укрытия размечены заранее, есть `occupiedBy`.
  - [ ] Хитмаркер, звук попадания, звук убийства, направленный индикатор урона.
  - [ ] Враг анимирован: ноги от пройденного пути, цикл по фактическому
  - [ ] Попадание запускает реакцию тела и перезапускает её на каждой пуле.
  - [ ] Кровь тремя слоями: брызги, взвесь, пятно на поверхности.
  - [ ] Оружие врага ставится по двум кистям, а не привязано к одной кости.
  - [ ] У тела есть срок жизни: след боя нужен, вечное кладбище — нет.
  - [ ] Декали в кольцевом буфере с потолком.
  - [ ] Орда и снаряды — bitECS + `InstancedMesh` + равномерная сетка.
- `docs/ref/knowledge/threejs/game_map_and_world_design.md` (выжимка — `skills/RENDERER_SKILL.md`) — Необходим для процедурной сборки компактной арены контейнерного терминала с вертикальными ярусами и укрытиями.
  - [ ] **Вид сверху (Top-Down Overview)**: все участки трассы изолированы, встречные полосы не пересекаются, нет самопересечений сплайна.
  - [ ] **Стартовая зона**: стартовые слоты ориентированы строго по направлению движения, светофоры и арка развернуты навстречу машинам, трибуны не залезают на полотно.
  - [ ] **Разметка**: белые линии идут параллельно кромке, пунктир не имеет поперечных стяжек, отсутствует мерцание (Z-fighting).
  - [ ] **Кромка дороги и поребрики**: переход в повороты гладкий, без зазубрин; поребрики лежат точно на внутренних апексах.
  - [ ] **Заземление декораций**: деревья, столбы, таблички торможения и барьеры надежно стоят на поверхности холмов без левитации.
  - [ ] **Физика**: болиды и соперники стартуют вперед, проходят первый поворот на скорости без переворотов и провалов сквозь полигоны.
- `docs/ref/knowledge/threejs/juice_and_vfx_pool.md` (выжимка — `skills/RENDERER_SKILL.md`) — Создает сочный боевой отклик: вспышки выстрелов, хитмаркеры попаданий, тряску камеры при подкатах и эффекты через пул инстансов.
  - [ ] Частицы живут в пуле на InstancedMesh, без аллокаций в кадре
  - [ ] Гравитация, время жизни и размер — параметры частицы, а не константы пула: иначе дым падает как искры
  - [ ] Искры, гильзы и кровь летят направленным конусом вдоль нормали или выстрела, а не разлетаются шаром
  - [ ] Аддитивные искры и полупрозрачный дым — два меша и два пула, а не один материал
  - [ ] Затухание сделано цветом через setColorAt: прозрачность живёт в общем материале
  - [ ] frustumCulled = false на мешах с мировыми инстансами — иначе эффекты пропадают при отвороте камеры
  - [ ] Активные частицы пишутся в буфер подряд, mesh.count выставляется в конце, а неактивные не прячутся в (0,-999,0)
  - [ ] Шейк камеры затухает сам и не оставляет камеру смещённой
- `docs/ref/knowledge/threejs/adaptive_quality.md` (выжимка — `skills/RENDERER_SKILL.md`) — Удерживает стабильные 60 FPS на смартфонах во время динамичных перестрелок за счет адаптивного масштабирования.
  - [ ] Целевой кадр зафиксирован (60 или частота панели, если она ниже), качество подгоняется под него
  - [ ] 120 FPS не преследуется: это батарея и мигание качества на 60-герцовой панели
  - [ ] Игра стартует на полном качестве и опускается, а не стартует урезанной и ползёт вверх
  - [ ] Подъём качества не решается по среднему времени кадра: под vsync свободного запаса по нему не видно
  - [ ] Частота панели определяется как минимальный чистый интервал rAF за первые ~60 замеров
  - [ ] Регулятор сходится: качество не скачет вверх-вниз по кругу
  - [ ] Тени перенастраиваются на лету, без пересоздания сцены
  - [ ] Автотюнер проверен на реалистичных временах кадра, а не на синтетических 8 мс
- `docs/ref/knowledge/stack/rapier3d.md` (выжимка — `skills/STACK_SKILL.md`) — Обеспечивает физику перемещения персонажа, расчет инерции скольжения в подкате и коллизии с контейнерами.
  - [ ] Физика только на Rapier: свои «скорости», Cannon-es и ammo.js в проект не тянутся
  - [ ] Шаг мира фиксированный: world.timestep не подменяется на dt
  - [ ] cuboid() получает ПОЛУразмеры — коробка 1×1×1 это cuboid(0.5, 0.5, 0.5)
  - [ ] trimesh используется только для статичных тел: динамический проваливается сквозь мир
  - [ ] Группы столкновений заданы с обеих сторон — иначе контакта не будет
  - [ ] Триггеры сделаны сенсорами, а не проверкой дистанции в update()
  - [ ] Синхронизация мешей идёт ПОСЛЕ world.step(), иначе кадр рисует прошлое состояние
  - [ ] «На земле» определяется контроллером персонажа, а не по velocity.y === 0
  - [ ] Рейкаст исключает собственное тело стрелка
  - [ ] Рестарт уровня — телепорт тел, а не пересборка мира
  - [ ] Физика проверена скриптом в Node до того, как её увидит игрок
- `docs/ref/knowledge/stack/three_mesh_bvh.md` (выжимка — `skills/STACK_SKILL.md`) — Обеспечивает сверхбыстрый raycast для мгновенной хитскан-стрельбы по зонам уязвимости и детекции уступов при перелазе.
  - [ ] Расширения прототипов подключаются в отдельном модуле, который импортируют все потребители
  - [ ] BVH строится на экране загрузки, а не при первом выстреле
  - [ ] Статика уровня слита в один меш, и BVH построен по нему, а не по каждому камню
  - [ ] Луч, сфера и бокс переводятся в локальное пространство геометрии перед запросом
  - [ ] Урон по области и обзор считаются через shapecast, а не перебором объектов
  - [ ] disposeBoundsTree() вызывается рядом с geometry.dispose()
  - [ ] Построение в воркере имеет дедлайн, а не ждёт бесконечно
- `docs/ref/knowledge/audio/procedural_sound_synthesizer.md` (выжимка — `skills/RENDERER_SKILL.md`) — Синтезирует через Web Audio звуки выстрелов для всей цепочки оружия, звон попадания в голову и сигнал активации БПЛА.
  - [ ] AudioContext создаётся лениво, по первому pointerdown/keydown, а не в конструкторе
  - [ ] Все звуки идут через один мастер-гейн: громкость и мьют трогают только его
  - [ ] Свёрнутая вкладка приглушает звук через visibilitychange (suspend/resume контекста)
  - [ ] Осцилляторы и узлы отключаются после затухания, а не копятся за сессию
  - [ ] Каждое действие игрока имеет свой звук: выстрел, попадание, подбор, кнопка интерфейса
  - [ ] Непрерывный звук (двигатель, ветер) — один осциллятор с меняющейся частотой, а не рестарт каждый кадр
  - [ ] Звук интерфейса отличается от звука мира: клик не путается с событием игры

**Вспомогательные:**
- `docs/ref/knowledge/threejs/procedural_mesh_builder.md` (выжимка — `skills/RENDERER_SKILL.md`) — Позволяет процедурно сгенерировать стилизованную 3D-геометрию грузовых контейнеров, окружения и 12 видов оружия без внешних моделей.
- `docs/ref/knowledge/threejs/procedural_character_rig.md` (выжимка — `skills/RENDERER_SKILL.md`) — Обеспечивает процедурный риг и анимации моделей бойцов спецназа с четким разделением хитбоксов головы и тела.
- `docs/ref/knowledge/threejs/physics_integration.md` (выжимка — `skills/RENDERER_SKILL.md`) — Связывает физические капсулы игроков и коллизии контейнеров в Rapier3D с рендером Three.js.
- `docs/ref/knowledge/threejs/performance_guide.md` (выжимка — `skills/RENDERER_SKILL.md`) — Оптимизирует рендеринг множества контейнеров и декалей через InstancedMesh для минимизации вызовов отрисовки.
- `docs/ref/knowledge/threejs/mobile_shaders.md` (выжимка — `skills/RENDERER_SKILL.md`) — Предоставляет оптимизированные мобильные шейдеры для материалов контейнеров и оружия без перегрева устройства.
- `docs/ref/knowledge/stack/postprocessing.md` (выжимка — `skills/STACK_SKILL.md`) — Реализует шейдерную подсветку силуэтов врагов сквозь стены для БПЛА «Оверлорд» и эффект радиального размытия при спринте.
- `docs/ref/knowledge/audio/web_audio_and_muting.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Гарантирует корректную инициализацию Web Audio по первому тапу и глушение звуков при потере вкладкой фокуса.
- `docs/ref/knowledge/monetization/interstitial_best_practices.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Определяет показ полноэкранной рекламы строго на экране итогов матча без прерывания 90-секундного игрового флоу.
- `docs/ref/knowledge/monetization/rewarded_ads_patterns.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Реализует опциональный просмотр рекламы за удвоение наград после победы без нарушения соревновательного баланса.

**Архетип петли проекта**: `docs/ref/knowledge/patterns/arena_combat_loop.md` (выжимка — `skills/PROJECT_KNOWLEDGE_SKILL.md`)

**Платформенные требования — обязательны всегда, независимо от жанра.** Без Playgama Bridge игра на площадке не стартует: платформа держит свою заставку до таймаута и снимает игру с модерации.
- `docs/ref/knowledge/playgama/game_ready_and_loading.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/auth_and_player.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/storage_and_cloud.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/ads_integration.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/banners_and_layout.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/compliance/yandex_moderation.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/localization_system.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/touch_controls.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_design_system.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_implementation.md` (выжимка — `skills/PLAYGAMA_SKILL.md`)

**Осознанно НЕ включены**: `threejs/horde_survivor_core.md`, `patterns/survivor_loop.md`, `mechanics/upgrade_choices.md`, `patterns/roguelike_loop.md`, `mechanics/drone_swarm.md`, `mechanics/cover_and_suppression.md`, `stack/bitecs.md`, `threejs/melee_combat_and_ragdoll.md`. В игре нет механик ордынского выживания, рогаликового выбора карточек способностей и медленного позиционного кемпинга в укрытиях; оружие сменяется автоматически по цепочке Gun Game в быстром арена-матче, БПЛА является экранным визором через шейдерный контур, а не физическим роем дронов, и для малого числа бойцов не требуется архитектура bitECS.
Не подтягивай решения из этих документов — они про другую игру.

**Документы проекта** (лежат рядом, в корне игры): `ACCEPTANCE.md` — приёмка, `AGENTS.md` — правила работы, `PROJECT_DIRECTION.md` — рамка проекта, `GAME_DESIGN_DOCUMENT.md`, `MECHANICS.md`, `CORE_LOOP.md`, `UI_UX_SPECIFICATION.md`, `ART_DIRECTION.md`, `MOBILE_CONTROLS.md`, `PLAYGAMA_INTEGRATION.md`, `MONETIZATION.md`, `QA_PLAN.md`, `DEVLOG.md` и `CHANGELOG.md` — их ведёшь ты.