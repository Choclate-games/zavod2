# UI/UX Specification: Ван-Тап: Дуэли на Крыше

## 1. Материал интерфейса
Интерфейс живёт в том же мире, что и сцена. Проверка: если закрыть игровое поле,
меню обязано выдавать именно эту игру, а не любую другую.

- **Тема от арт-директора**: Минималистичный соревновательный HUD в стиле Cyber-Tactical: темно-угольные полупрозрачные панели, неоново-оранжевые и янтарные акценты, прецизионные тонкие перекрестия прицела.
- **Визуальный язык**: Строгая геометрия, высококонтрастные пиктограммы, отсутствие перегруженных градиентов, соревновательная читаемость шрифтов за 50 миллисекунд.
- **Типографика**: Primary: 'Rajdhani', sans-serif (для цифр счета, таймера и урона); Secondary: 'Inter', sans-serif (для интерфейсных текстов и настроек)

## 2. Акценты: один цвет — один смысл
Разный цвет у каждой кнопки «чтобы отличались» — главная ошибка игрового
интерфейса. На одном экране одновременно видно не больше двух акцентов.

| Токен | Единственный смысл |
|---|---|
| `critical_alert` | #E74C3C |
| `neutral_hud` | rgba(26, 29, 36, 0.75) |
| `primary_action` | #F39C12 |
| `success_headshot` | #2ECC71 |
| `text_bright` | #FFFFFF |

Все значения — токенами в `src/ui/theme.css`: цвета, гарнитуры, шкала отступов,
радиусы, длительности, порядок слоёв `--z-*`. Литерал цвета внутри экрана — баг.

## 3. Композиция экрана
Меню и экраны между сессиями лежат ПОВЕРХ живой игровой сцены — той же, что в
игре, только с медленной камерой. Непрозрачная заливка на весь экран запрещена:
подложка только под текстом и кнопками.

- **Сцена за меню**: Вид от первого лица: боец стоит на краю балки небоскреба, в правой руке плавно покачивается вороненый Desert Eagle, на заднем плане оранжевое солнце заходит за небоскребы, в воздухе медленно летают частицы строительной пыли.

Экран — не колонка кнопок по центру. Три зоны, и каждый элемент лежит ровно в одной:

1. **Идентичность** — что это за экран: заголовок, режим, состояние игрока.
2. **Главное действие** — ровно одно на экран, самое крупное и единственное с
   основным акцентом.
3. **Второстепенный ряд** — всё остальное, одним рядом или сеткой, одним весом.

Зоны нажатия: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px, отступы через
`env(safe-area-inset-*)` плюс измеренная высота липкого баннера. Ни один экран не
скроллит страницу: длинный список скроллится во внутреннем контейнере.

## 4. Набор компонентов (закрытый)
Всё на экране — один из этих компонентов. Одноразовый `<div>` с инлайновыми
стилями — это то, из-за чего второй экран перестаёт совпадать с первым.

- DuelTimerRing
- BestOf5ScoreDots
- CrosshairRenderer
- SpeedMeter
- AmmoCounter
- KillFeedBanner
- WeaponCard

Каждый интерактивный компонент несёт пять состояний: покой, наведение, нажатие,
недоступность, `:focus-visible`; асинхронное действие — ещё и `loading`.

## 5. HUD
Не больше пяти постоянных элементов на телефоне. Пять якорей, посередине экрана
не висит ничего, кроме временной обратной связи.

| Якорь | Что там |
|---|---|
| `ammo` | BottomRight |
| `fire_button` | BottomRightTouch |
| `score` | TopCenterBelowTimer |
| `speed_meter` | CenterBelowCrosshair |
| `strafe_zone` | BottomLeftTouch |
| `timer` | TopCenter |

Элементы:
- Динамическое перекрестие прицела с индикатором разброса и точкой Ван-Тапа
- 15-секундный круговой таймер раунда в верхней центральной части экрана
- Индикатор счета матча Best of 5 (точки побед игрока и соперника)
- Счетчик патронов в магазине и резерве в правом нижнем углу (7 / 35)
- Индикатор скорости персонажа (Speed: 0 km/h) для визуализации момента остановки
- Мини-радар дистанции до противника (без указания точного угла)
- Зона сенсорного спуска (Fire Zone) и кнопка приседа/шага на мобильных

Меняющиеся числа — `tabular-nums` в слоте фиксированной ширины, полосы —
`transform: scaleX()`, а не `width`. Текст поверх геймплея всегда с подложкой или
обводкой: белое число исчезает на светлой сцене.

## 6. Состояние, показанное миром, а не оверлеем
- Наручные цифровые часы бойца с отображением времени раунда
- Лазерная гравировка счетчика фрагов на затворе пистолета (StatTrak)
- Дым из ствола после серии выстрелов

## 7. Каталог экранов
### Экран: MainMenu
- **Что на экране**: Выбор режима дуэли (Рейтинг / Быстрый бой / Тренировка контр-стрейфа), переход в оружейную и таблицу лидеров
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: WeaponShop
- **Что на экране**: Выбор и экипировка скинов оружия, перчаток, просмотр 3D-моделей в реальном времени
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: DuelHUD
- **Что на экране**: Минималистичный соревновательный боевой интерфейс во время 15-секундного раунда
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: RoundEndOverlay
- **Что на экране**: Мгновенное отображение победителя раунда, урона и переход к следующему раунду за 2 секунды
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: MatchVictoryDefeat
- **Что на экране**: Итоги матча Best of 5, изменение ELO-рейтинга, кнопка удвоения награды за Rewarded Video и кнопка Реванша
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: LeaderboardModal
- **Что на экране**: Глобальная таблица лидеров по ELO и хедшотам с интеграцией Playgama
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

### Экран: SettingsModal
- **Что на экране**: Настройка чувствительности мыши/тача, громкости звуков и графических пресетов
- **Композиция**: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

**Переходы**: MainMenu -> Matchmaking (1.5s) -> DuelHUD (Round 1..5) -> RoundEndOverlay (2s) -> DuelHUD -> MatchVictoryDefeat -> MainMenu / Rematch

## 8. Состояния экрана
- LobbyState
- CountdownState (3..2..1)
- RoundActiveState
- RoundFreezeState (Slow-mo)
- MatchEndState
- AdBreakState

## 9. Отклик и движение
- Момент остановки: индикатор скорости вспыхивает зеленым и перекрестие сужается
- Хедшот: экранный металлический маркер попадания, замедление времени и золотой текст 'ONE TAP'
- Попадание по игроку: короткая красная направленная дуга урона и глухой стон
- Победа в матче: вспышка триумфального салюта над крышей и звук победной фанфары

Переход один на всю игру и укладывается в 300 мс; анимируются только `transform`
и `opacity`; `prefers-reduced-motion: reduce` убирает трансформации.

## 10. Вайрфрейм игрового экрана
```text
+-------------------------------------------------------------------------+
| [Esc]             [ 15.0s ]  ( 2 : 1 )  [Best of 5]            [FPS: 60]|
|                                                                         |
|                                                                         |
|                                                                         |
|                                   +                                     |
|                               -[  .  ]-                                 |
|                                                                         |
|                             [SPEED: 0.0]                                |
|                                                                         |
|                                                                         |
| ( SWIPE STRAFE )                                       ( FIRE / TRIGGER)|
|   <-- [ ] -->                                            [   BANG!   ]  |
| [SHIFT: WALK]                                            [ 7 / 35 DEAGLE]|
+-------------------------------------------------------------------------+
```

## 11. Чек-лист приёмки интерфейса
- [ ] Ни одного литерала цвета, шрифта, радиуса или длительности вне `theme.css`.
- [ ] Слои над канвасом не перехватывают игровой ввод: контейнеры `pointer-events: none`.
- [ ] Каждый экран помещается в измеренный вьюпорт, страница не скроллится.
- [ ] Одно главное действие на экран, не больше двух акцентов одновременно.
- [ ] Меняющиеся числа не дёргают строку HUD.
- [ ] У каждого экрана описаны загрузка, пустота и ошибка.
- [ ] Возможность, которой нет на площадке, не нарисована вовсе.
- [ ] Ни `alert`/`confirm`, ни эмодзи вместо иконок, ни `z-index` мимо токенов.
- [ ] Самая длинная переведённая строка помещается в кнопку.
- [ ] За меню видна живая сцена игры, а не заливка цветом.
- [ ] Ни одного эмодзи в подписи кнопки: иконка — инлайновый SVG с `currentColor`.
- [ ] У каждого экрана расписаны три зоны, а не «карточка по центру».
- [ ] С закрытым игровым полем меню всё ещё узнаётся как «Ван-Тап: Дуэли на Крыше».
