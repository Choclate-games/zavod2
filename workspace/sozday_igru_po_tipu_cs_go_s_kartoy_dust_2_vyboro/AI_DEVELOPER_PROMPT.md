# FINAL AI DEVELOPER PROMPT: DUST 2: РЕТЕЙК И ДУЭЛИ 🎮⚡

> **ИНСТРУКЦИЯ КОДОВОМУ АГЕНТУ**
> Ты ведущий разработчик этой игры. Задача — довести её до состояния, в котором
> она проходит приёмку из `ACCEPTANCE.md`: работающая, играбельная, принятая
> площадкой. Заглушки, TODO и «пока так» результатом не считаются.

### Как читать этот документ

Требования размечены по обязательности. Когда обязательно всё, не обязательно ничего.

| Уровень | Что значит | Что делать при сомнении |
|---|---|---|
| **MUST** | Без этого игра не работает или не проходит модерацию площадки. | Не обсуждается. Проверяется в `ACCEPTANCE.md`, разделы A–C. |
| **SHOULD** | Проектное решение фабрики: так игра задумана. | Отступать можно, но с записью в `DEVLOG.md` — что и почему. |
| **MAY** | На твоё усмотрение. | Решай сам, спрашивать не нужно. |

Неразмеченное считается **SHOULD**. Всё, что помечено «ЗАПРЕЩЕНО» в секции 1b, —
это **MUST**, даже если спецификация где-то умалчивает.

### Порядок работы

Читать документ целиком до первой строчки кода не нужно. Порядок такой:

0. `node scripts/fetch-knowledge.mjs` — забрать базу знаний в `docs/ref/`. Одна команда, дальше работа идёт офлайн. **MUST**, и раньше всего остального: без этого половина адресов в документе никуда не ведёт.
1. `ACCEPTANCE.md` — чем закончится работа. Дальше всё делается под него.
2. Секция 1b — чем эта игра является и чем она не является. Рамка всего остального.
3. `DESIGN.md` — написать самому, до первой строчки кода. Секция 7 говорит, что в нём должно быть и чего в нём быть не должно.
4. Каркас: `package.json`, Vite, TypeScript strict, пустая сцена Three.js, цикл с фиксированным шагом. Playgama Bridge и `game_ready` — сразу, а не в конце (проверки **C1–C4**).
5. Главная механика из секции 3 — одна, до играбельного состояния. Не все сразу. Перед тем как писать её с нуля — секция 3c: возможно, она уже написана.
6. Дальше по фазам роадмапа (секция 8), каждая фаза закрывает свои номера проверок.
7. `node scripts/check-spec.mjs` — после каждой фазы, а не один раз в конце.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: Dust 2: Ретейк и Дуэли
- **Project Slug**: `sozday_igru_po_tipu_cs_go_s_kartoy_dust_2_vyboro`
- **Genre**: Тактический 3D FPS-Шутер (Соревновательный тактический ретейк-шутер от первого лица)
- **Target Platform**: Playgama Bridge (Yandex Games / VK / Web)
- **Orientation**: Landscape
- **Target Audience**: Поклонники тактических соревновательных шутеров (CS:GO, CS2, Standoff 2) в возрасте 14-35 лет, ценящие честный скилл стрельбы, контр-стрейф и моментальный вход в бой прямо в браузере.
- **Player Fantasy**: Ты — элитный боец спецназа или хладнокровный террорист, решающий исход напряжённого 30-секундного штурма точки Dust 2 благодаря выверенному таймингу, точному контр-стрейфу и хэдшоту.
- **Core Hook**: На последних 4 секундах до детонации C4 сквозь рассеивающийся смок ставишь ван-тап в голову врагу и успеваешь обезвредить бомбу с купленным набором сапёра!
- **Session Model**: Матч из 5 быстрых раундов до 3 побед; каждый раунд длится 25–45 секунд в формате 3v3 со спавном в боевой позиции на точке A или B карты Dust 2.


---

## 1b. РАМКА ПРОЕКТА: ЧТО ЭТО ЗА ИГРА И ЧЕМ ОНА НЕ ЯВЛЯЕТСЯ


**Направление**: Dust 2: Дуэль и Ретейк

**Почему выбрано именно оно**: Вариант D2 сохраняет главную суть и драйв CS:GO — прямое управление оружием от первого лица, выбор стороны спецназа или террористов, установку/обезвреживание бомбы C4 и честную баллистику со спреем и хэдшотами. В отличие от долгой 45-минутной оригинальной игры, формат 'Ретейк' идеально адаптирован под браузер и мобильные экраны: игрок сразу спавнится в гуще боя на точке плента без 40 секунд пустой ходьбы, а управление с авто-остановкой дает интуитивный контр-стрейф без перегрузки кнопками.

**Узнаваемая сцена**: Кадр от первого лица в золотистом свете закатного солнца на точке A карты Dust 2: на переднем плане детализированный автомат AK-47 со следами потертостей на ствольной коробке и вылетающей дымящейся гильзой. По центру кадра сквозь рассеивающееся серое облако дымовой шашки виден силуэт спецназовца в темно-синем шлеме с забралом, вскидывающего винтовку M4A4. Справа за песчаным ящиком с граффити «A» лежит таймер бомбы C4 с горящим красным светодиодом и связкой проводов, а на заднем плане в глубоком небе над песчаными арками медленно тают трассеры рикошетов.

**Без чего проект перестаёт быть собой** (реализовать обязательно):
- Аутентичная геометрия точек карты Dust 2 (коробка, Long, ящики плента A/B, двойные двери) с читаемыми углами и укрытиями.
- Выбор стороны (Террористы / Спецназ) с соответствующей целью раунда: закладка и защита C4 против штурма и обезвреживания (дефьюза).
- Характерная баллистика стрельбы: зависимость кучности от движения (контр-стрейф), разброс при зажиме (спрей) и мгновенное летальное попадание в голову (one-tap headshot).
- Командные боты союзников и противников с поведением прикрытия секторов, реакцией на шум шагов и закладку бомбы.

**ЗАПРЕЩЕНО ВВОДИТЬ** — даже если спецификация где-то умалчивает и даже если «так принято в жанре»:
- Бесконечные волны зомби или бегущей толпы врагов (это соревновательный раундовый тактический шутер спецназа против террористов 3v3 / 5v5, а не horde survivor).
- Автоматическая стрельба и авто-наведение с круговой автоатакой (игрок лично контролирует прицел, спрей оружия и момент спуска курка).
- Виртуальный плавающий аналоговый джойстик в левом углу и россыпь мелких кнопок (управление адаптировано под прямое наведение пальцем / свайп-движение с мгновенной остановкой).
- Процедурно генерируемые лабиринты и фэнтези-подземелья (строго узнаваемая топология карты Dust 2: Long, Short, Plant A/B, Mid, Tunnels).
- Мета-прокачка в стиле рогаликов вида '+5% к здоровью, +10% к урону пули' (все персонажи равны по базовому здоровью и урону, решает скилл позиционирования, тайминг и точность попадания в голову).
- волны врагов на арене
- универсальная связка «дэш + парирование + удар»
- заимствование Vampire Survivors / Brotato как основы
- джойстик слева + кнопки атаки справа как раскладка по умолчанию
- серые кубы вместо стилизованной геометрии
- мета-прогрессия «золото за забег -> постоянные апгрейды» без связи с темой

Если по ходу реализации кажется, что без запрещённого элемента игра не работает — это находка для `DEVLOG.md`, а не разрешение его добавить.

**Рассмотренные и отклонённые направления** (не возвращай их через механики):
- Вариант D1 (Dust 2: Тактический Штаб 5v5) отвергнут, так как превращение шутера в изометрическую RTS с непрямым управлением отрядом лишает игрока личного тактильного ощущения стрельбы, отдачи и сочных хэдшотов, ради которых ищут CS. Кроме того, микроконтроль 5 бойцов на сенсорном экране перегружает интерфейс.
- Вариант D3 (Dust 2: Дуэль Снайперов на Миду) отвергнут из-за сильной ограниченности геймплея: снайперская дуэль через щель дверей исключает механику бомбы C4, командные тактики штурма плентов и всё разнообразие стрелкового оружия (AK-47, M4A4, пистолеты), что приводит к быстрой потере интереса игрока через 3-4 минуты.

---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite
- **Renderer**: **THREEJS** (^0.185.1)
  - *Selection Rationale*: Three.js + Rapier3D: пространственный геймплей, физика и освещение. Перспективная камера.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat ^0.20.0)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Web Audio API (Howler.js with html5: false)
- **State Management**: Reactive EventBus / State Store
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 75
- **Max Triangles / Active Sprites**: < 45000
- **Max Bundle Size**: < 4.5 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Выбор позиции и оружия -> Тактический выход с контр-стрейфом -> Прицельная перестрелка короткими отсечками -> Закладка/Обезвреживание бомбы C4 -> Завершение раунда и анализ тактики -> Победа в матче из 5 раундов.
```

**Фирменный момент**: Спецназовец остаётся 1v2 на точке A карты Dust 2. Таймер C4 отсчитывает последние 7 секунд до взрыва (зуммер пищит с частотой 4 Гц). Игрок запускает фейк-дефьюз (клик 'Е' на 0.2 с со звуком щелчка проводов), мгновенно прерывает его, делает контр-стрейф влево и ван-тапом из AK-47 ставит хэдшот выбежавшему из-за ящиков Гуся террористу (хруст шлема, красная плашка в киллфиде). Не теряя ни доли секунды, он зажимает дефьюз с набором сапёра (5.0 с) сквозь клубы рассеивающегося серого дыма и обезвреживает бомбу на отметке 0.4 секунды до детонации под триумфальный победный аккорд раунда!

**Чем петля отличается от соседей по жанру**: Концентрация 100% игрового времени на кульминации раунда — 30-секундном штурме/защите установленной бомбы C4 (Retake Scenario). В отличие от классических клонов CS, где 70% времени уходит на раскидки и перетяжки, здесь каждый раунд начинается с готовой тактической геометрии: террористы удерживают плент (Crossfire, Goose, Catwalk), спецназ атакует с двух направлений (Short, Long, CT-Spawn), C4 уже тикает (35.0 с). Высокоточная физика контр-стрейфа (сброс скорости с 250 units/s до 0 за 0.08 с), динамический прицел, учитывающий ускорение игрока, и честный Raycast-прострел укрытий с учетом плотности материалов создают аутентичный соревновательный микро-скилл CS прямо в WebGL.

**Шаблон жанра, который НЕ реализуем**: Отвергнут шаблон длительной экономической меты (Full Eco / Save раунды, покупка брони/оружия в меню магазина 15 секунд, 45-минутные матчи) и аркадный 'Run-and-Gun' с мобильным авто-аимом. В веб-3D шутере долгая закупка и беготня от спавна убивают динамику сессии. Вместо этого используется мгновенный спавн сразу в боевую ретейк-позицию на точке A или B Dust 2 с фиксированным сбалансированным лоадаутом (AK-47 / M4A4 / AWP, гранаты, дефьюз-кит) и бескомпромиссная баллистика: попадание на бегу невозможно из-за колоссального разброса, требуя от игрока чистого навыка контр-стрейфа и дисциплины стрельбы.

**Схема петли**:
```text
+-----------------------------------------------------------+
       |             СПАВН В РЕТЕЙК-ПОЗИЦИИ (3v3)                  |
       |  Лоадаут: AK-47 / M4A4 / Смок / Дефьюз-кит | C4: 35.0 с   |
       +-----------------------------+-----------------------------+
                                     |
                                     v
       +-----------------------------------------------------------+
       |             ТАКТИЧЕСКИЙ ВЫХОД И ЗОНИРОВАНИЕ               |
       |  Бросок Smoke на Long/CT -> Звуковая разведка шагов       |
       +-----------------------------+-----------------------------+
                                     |
                                     v
       +-----------------------------------------------------------+
       |         ДУЭЛЬ: КОНТР-СТРЕЙФ + КОНТРОЛЬ СПРЕЯ              |
       |  Остановка (D/A) -> Сброс разброса -> Ван-тап в голову    |
       +-----------------------------+-----------------------------+
                                     |
                                     v
       +-----------------------------------------------------------+
       |           КЛАТЧ: ФЕЙК-ДЕФЬЮЗ / ОБЕЗВРЕЖИВАНИЕ C4          |
       |  Клик 'E' (звуковой блеф) -> Добивание -> Дефьюз за 5.0 с |
       +-----------------------------+-----------------------------+
                                     |
                                     v
       +-----------------------------------------------------------+
       |             ИТОГИ РАУНДА И МАТЧА (Best of 5)              |
       |  +1 Очко раунда -> Анализ HS% -> Ранг и Скины за скилл    |
       +-----------------------------------------------------------+
```

**Микро-петля (посекундно)**:
- **Выход на угол и звуковая локализация** (1.0–2.0 с): игрок — Движение стрейфом (клавиши A/D или левый стик) к срезу угла (например, Long Doors или A-Short), слушая шаги врага; игра — Стерео-звук шагов террориста по песку/бетону (3D Spatial Audio), увеличение перекрестия динамического прицела из-за скорости; решение — Выйти широким пиком (Wide Peek) или аккуратно чекнуть ближний угол коротким шагом (Jiggle Peek)
- **Контр-стрейф и мгновенная стабилизация** (0.08–0.2 с): игрок — Резкое нажатие противоположной клавиши движения (D при движении влево) для экстренного гашения скорости до 0; игра — Скорость падает ниже порога 34 units/s (0.85 м/с), перекрестие прицела схлопывается в точку, разброс первого выстрела снижается до 0 MOA; решение — Стрелять одиночным ван-тапом в голову или зажать контролируемую отсечку из 3-4 пуль
- **Прицельный выстрел и контроль спрея** (0.3–1.0 с): игрок — Клик ЛКМ / тап кнопки огня с одновременным микро-ведением мыши/пальца вниз-влево для компенсации подъема ствола; игра — Raycast-трассировка пули, вылет гильзы, дульная вспышка, отдача камеры (Pitch +1.8°), попадание в хитбокс головы с хрустом шлема (108 урона); решение — Продолжить спрей во второго врага или уйти за укрытие на перезарядку/смену угла
- **Реакция на результат дуэли и смена позиции** (1.5–3.0 с): игрок — Уход за ящик/стену, оценка мини-карты и оставшегося времени C4; игра — Красная плашка в киллфиде (+1 килл), учащение тиканья зуммера C4 (звуковой сигнал дедлайна); решение — Бросить смок на простреливаемую позицию или сразу начать сближение с C4 для дефьюза

**Мезо-петля (этап)**:
- **Инициализация ретейк-сценария** (1.5–3.0 с): игрок — Оценка выданной позиции спавна (A-Long / Short / CT-Spawn) и лоадаута (AK-47 / Defuse Kit / Smoke); игра — Обратный отсчет 1.5 с фризтайма, звуковой сигнал запуска таймера C4 (35.0 с до детонации), отрисовка маркера бомбы на пленте; решение — По какому тактическому вектору штурмовать точку и какую гранату отдать первой
- **Зонирование и размен позиций** (10.0–18.0 с): игрок — Бросок дымовой гранаты для отсечения снайпера на CT-Spawn / Ramp и синхронный вход в плент; игра — Развертывание 12-секундной плотной дымовой завесы, подавление видимости, размен тиммейтов и ботов (3v3 -> 2v1); решение — Зачистить дальние укрытия (Goose/Pit) или форсировать борьбу за зону бомбы
- **Клатч-кульминация и разминирование C4** (5.0–10.0 с): игрок — Ликвидация последнего защитника точки и удержание клавиши взаимодействия на C4; игра — Прогресс-бар дефьюза (5.0 с с китами / 10.0 с без), нарастающий высокочастотный писк зуммера бомбы; решение — Дефьюзить до конца на тайминге или сделать фейк-дефьюз для провокации скрытого врага
- **Финал раунда и смена счёта** (3.0–5.0 с): игрок — Просмотр статистики раунда (урон, хэдшоты, MVP); игра — Оповещение 'Bomb Defused / Terrorists Win', обновление общего счёта матча (например, 2:1), начисление ранговых очков; решение — Скорректировать тактику на следующий раунд с учетом поведения ботов соперника

**Макро-петля (забег)**:
- **Вход в соревновательный матч** (5.0–10.0 с): игрок — Выбор стороны (Спецназ / Террористы) и запуск поиска матча формата Best of 5; игра — Генерация сида ретейк-сценариев на плентах A и B Dust 2, калибровка MMR ботов под ранг игрока; решение — Выбрать агрессивный стиль штурмовика или позиционную роль замыкающего сапёра
- **Серия из 5 тактических раундов до 3 побед** (2.5–4.0 мин): игрок — Отыгрыш раундов со сменой точек закладки (раунд 1: Плент A, раунд 2: Плент B, раунд 3: Плент A...); игра — Адаптация ИИ: боты начинают чаще контр-стрейфить, шифтить и простреливать дерево при росте счёта в пользу игрока; решение — Скорректировать выбор оружия при респавне (например, взять AWP для дальнего контроля Long)
- **Матч-поинт и итог матча** (10.0–20.0 с): игрок — Решающий клатч 5-го раунда, финальный выстрел или взрыв/дефьюз C4; игра — Экран триумфа 'VICTORY / DEFEAT', сводная таблица K/D/A, процент попаданий в голову (HS%); решение — Забрать награду за выполненные тактические контракты и открыть кейс мастерства
- **Мета-прогрессия и кастомизация** (15.0–45.0 с): игрок — Осмотр разблокированных скинов (AK-47 'Desert Rebel', AWP 'Dragon Lore (WebGL Edition)'), повышение ранга (от Silver IV до Global Elite); игра — Анимация повышения рангового дивизиона, обновление таблиц лидеров, выдача новых контрактов; решение — Запустить следующий матч для удержания победного винстрика или осмотреть инвентарь

**Кривая напряжения**: Напряжение стартует с умеренно-высокого уровня (40%) на фризтайме благодаря тикающему таймеру C4 и мгновенной близости врага. В первые 5-12 секунд штурма при взаимном разбросе смоков и первых перестрелках напряжение подскакивает до 70%. Если погибают тиммейты и игрок остаётся в ситуации 1v2 (Clutch State) с менее чем 12 секундами на таймере C4, напряжение достигает пиковых 100%: звуковое окружение заглушается низкочастотным фильтром, пульс зуммера C4 бьет с частотой 4 Гц, каждый промах смертелен. В момент успешного дефьюза бомбы на последних миллисекундах или ликвидации последнего врага напряжение мгновенно падает до 10%, сменяясь эйфорическим катарсисом победного джингла и экраном MVP.

**Формулы ядра (реализуй буквально)**:
- `Скорость и точность контр-стрейфа: AccuracyPenalty = clamp((CurrentVelocity / MaxVelocity - 0.136) / 0.864, 0.0, 1.0) * BaseInaccuracy (где MaxVelocity = 250 units/s, остановка за 0.08 с дает AccuracyPenalty = 0).`
- `Баллистический урон и множитель хитбоксов: FinalDamage = BaseWeaponDamage * HitboxMultiplier * ArmorPenetrationRatio * (1.0 - DistanceMeters * DistanceDropoffPerMeter) (AK-47: Base 36, Headshot x4.0 = 144 урона, мгновенный kill сквозь шлем).`
- `Прострел препятствий (Wallbang): PenetratedDamage = InitialDamage * SurfacePenetrationModifier^MaterialLayers * max(0.0, 1.0 - WallThicknessMeters / MaxPenetrationDepthMeters) (Деревянные двери Dust 2: удержание 60% урона, пробитие до 0.35 м).`
- `Тайминг детонатора и разминирования C4: RemainingTime = 35.0s - ElapsedTime; DefusalTimeRequired = HasDefuseKit ? 5.0s : 10.0s; ClutchMargin = RemainingTime - DefusalProgress (дефьюз успешен только если RemainingTime >= DefusalTimeRequired).`
- `Динамическая адаптация реакции ботов (MMR AI Scaling): BotReactionTimeMs = clamp(450.0 - (PlayerWinStreak * 45.0) - (PlayerRankTier * 25.0), 180.0, 500.0) ms.`
- `Ранговый ELO-расчет за матч: DeltaRating = (MatchWon ? +25 : -20) + (HeadshotKillCount * 2) + (SuccessfulDefuseCount * 5) + (ClutchWonCount * 10).`

**Прогрессия внутри забега**:
- Раундовая адаптация позиции: Выбор и закрепление приоритетного угла выхода (Long / Short / B-Tunnels) на основе разведки перемещений ботов в прошлых раундах.
- Тактический пул снаряжения раунда: Ротация случайных сбалансированных лоадаутов (Раунд 1: AK-47 + Smoke + Flash; Раунд 2: M4A4 + Defuse Kit + HE; Раунд 3: AWP + Desert Eagle).
- Накопление тактической уверенности (Momentum Meter): За серию из 2 хэдшотов подряд время визуального восстановления прицела после отдачи субъективно считывается чище за счёт более чёткого аудио-хитмаркера.
- Эволюция паттернов ИИ внутри матча: При счёте 2:0 в пользу игрока боты переходят из пассивного кемпинга в агрессивный контр-пик с префаером и флешками.

**Мета-прогрессия**:
- Ранговая лестница мастерства (Competitive Tiers): 6 дивизионов (Silver -> Gold Nova -> Master Guardian -> Eagle -> Supreme -> Global Elite), рассчитываемых по честному ELO без пейтувина.
- Тактические контракты на мастерство: Задания типа 'Поставить 3 хэдшота с контр-стрейфа за один раунд', 'Разминировать C4 за <1.0 с до взрыва', 'Оформить Wallbang-килл сквозь двери мида'.
- Оружейный арсенал скинов (Skin Showcase): Разблокировка скинов оружия различной степени редкости (Covert, Classified, Mil-Spec) со счетчиком убийств StatTrak™.
- Шевроны и нашивки ветерана ретейка: Косметические знаки отличия в профиле игрока за серии побед (5, 10, 25 побед подряд) и клатчи 1v3.

### Контр-стрейф и фиксация ствола (Counter-Strafing & Velocity Zeroing) (CORE)
- **Category**: Фундаментальный двигатель боевой точности и тайминга дуэли.
- **Description**: Честная физическая эмуляция механики Counter-Strike Source/GO с физикой трения и сбросом разброса без авто-наводки и искусственного занижения мобильной динамики.
- **Player Input**: ПК: Нажатие клавиши 'D' при беге влево ('A') или отпускание 'A' с тапом 'D'. Мобильные: Резкий свайп стика движения в противоположную сторону с отпусканием в центр.
- **Technical Complexity**: Medium
- **Решение игрока**: В какой микро-момент прекратить движение и нажать противоположный стрейф, чтобы обнулить разброс строго перед нажатием на спуск.
- **Состояния**: `moving_full_speed (250 u/s, разброс max)`, `counter_strafing (активное торможение противоходом)`, `zero_velocity_locked (<34 u/s, разброс 0 MOA)`, `firing_spread_cooldown (восстановление стабильности)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Тайминг идеального контр-стрейфа` = `0.08 с` — Если увеличить до 0.25 с — стрельба станет вязкой и 'мыльной'; если уменьшить до 0.02 с — исчезнет механический скилл остановки, игра превратится в аркадный run-and-gun.
  - `Порог скорости нулевой неточности` = `34.0 units/s (0.85 м/с)` — Если поднять до 100 units/s — игроки смогут стрелять на бегу без наказания; если опустить до 5 units/s — любое микро-касание стика будет портить выстрел.
  - `Максимальная скорость бега с AK-47` = `215.0 units/s (5.38 м/с)` — Если увеличить — пики из-за угла станут нечеловечески быстрыми (peeker's advantage); если уменьшить — персонаж покажется неповоротливым.
  - `Штраф неточности на бегу` = `85.0 MOA` — Если уменьшить — пули будут случайно залетать в голову на бегу; если увеличить — при стрельбе на бегу пули будут улетать строго в пол/небо.
- **Слои отклика**:
  - Визуал/VFX: Динамические риски перекрестия прицела резко сжимаются в компактную точку при достижении порога остановки; на бегу прицел расширяется в 4 раза.
  - Звук (Web Audio): Мгновенный тихий глухой шуршащий звук трения подошвы о песок/плитку ('scuff step') в момент идеального торможения.
  - Камера: Мгновенное гашение покачивания камеры (head bobbing) с легким микро-толчком вперед на 0.05 единиц по оси Z.
  - UI/Тактильность: Короткий тактильный микро-отклик вибрации (10 мс) на мобильных устройствах в момент входа в зону нулевого разброса.
- **Режим отказа игрока**: Игрок нажимает огонь до полной остановки (на скорости >34 u/s) — первая же пуля летит мимо цели на 1.5 метра в сторону, перекрестие прицела широко распахнуто, выдавая визуальный маркер ошибки тайминга.
- **Кривая мастерства**: Новичок на 30-й секунде стреляет во время бега и недоумевает, почему пули летят в молоко. Эксперт на 10-м матче делает jiggle-peek из-за угла с точностью до 2 кадров (33 мс), моментально выстреливая ровно одну пулю в пиксель головы врага и сразу уходя обратно в укрытие.
- **Сопротивление игры**: Боты высокой сложности считывают предсказуемые тайминги контр-стрейфа игрока и ставят прицел с пре-аимом на угол выхода, наказывая за повторный выход в той же позиции.
- **Синергии**:
  - Компенсация спрей-паттерна и темповые отсечки
  - Баллистический прострел укрытий и физика материалов
  - Обезвреживание C4, звуковой блеф и клатч-тайминги
- **Почему это не жанровый шаблон**: Честная физическая эмуляция механики Counter-Strike Source/GO с физикой трения и сбросом разброса без авто-наводки и искусственного занижения мобильной динамики.
- **Псевдокод тика**:
```text
function updateCounterStrafe(player, dt) {
  const vel = player.body.velocity.length();
  const isOppositePressed = (player.input.x * player.prevVelocity.x < 0);
  if (isOppositePressed) player.body.velocity.multiplyScalar(0.2);
  player.isInaccuracyZero = (vel < 34.0);
  player.crosshairSpread = player.isInaccuracyZero ? 0 : (vel / 250.0) * 85.0;
  if (player.isInaccuracyZero && !player.wasStable) playScuffSound();
  player.wasStable = player.isInaccuracyZero;
}
```

### Компенсация спрей-паттерна и темповые отсечки (Recoil Spray Pattern & Burst Fire) (SECONDARY)
- **Category**: Источник риска при затяжной стрельбе и главный инструмент нанесения взрывного урона на средней дистанции.
- **Description**: Детерминированный математический спрей-паттерн (фиксированная траектория смещения пуль), идентичный соревновательным ПК-шутерам, а не случайный конус разброса (Cone of Fire).
- **Player Input**: ПК: Зажатие ЛКМ с плавным смещением мыши вниз (пули 1-6), затем влево (пули 7-15) и вправо (пули 16-30). Мобильные: Удержание правой кнопки огня с одновременным свайпом правого пальца по контуру отдачи.
- **Technical Complexity**: Medium
- **Решение игрока**: Сделать ван-тап в голову, выпустить точную отсечку из 3 пуль или начать зажимать полный спрей с контролем формы 'семерки' при встрече с двумя врагами.
- **Состояния**: `idle_weapon`, `tapping_single (1-я пуля, 100% точность)`, `burst_firing (2-4 пули, линейный подъем)`, `full_spray_control (5-30 пуль, горизонтальные колебания)`, `recoil_recovery (восстановление исходного угла ствола)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Вертикальный подъем ствола AK-47` = `2.4 градуса/выстрел` — Если увеличить до 4.5° — экран будет мгновенно задираться в небо, делая зажим невозможным; если уменьшить до 0.8° — пропадет необходимость вести мышь вниз.
  - `Время восстановления отдачи (Recoil Decay)` = `0.32 с` — Если увеличить — стрельба одиночными станет слишком медленной; если уменьшить — зажим превратится в спам ван-тапами без пауз.
  - `Темп стрельбы AK-47` = `600 выстр/мин (10 выстр/с)` — Если ускорить — враги будут умирать за 100 мс без шанса на реакцию; если замедлить — оружие потеряет соревновательную хватку.
  - `Горизонтальный размах спрея` = `1.8 градуса` — Если убрать — отдача станет одномерной и примитивной; если сделать хаотичной — исчезнет механический навык заучивания паттерна.
- **Слои отклика**:
  - Визуал/VFX: Яркие конические вспышки дульного пламени с динамическим светом на стенах, трассеры пуль Three.js, вылетающие дымящиеся латунные гильзы 7.62 мм.
  - Звук (Web Audio): Сочный басовитый выстрел AK-47 с эхом в закрытых зонах (B-Tunnels) и сухим щелчком затвора при окончании патронов.
  - Камера: Процедурный толчок камеры вверх с последующим затухающим покачиванием по оси Pitch/Yaw (Spring Physics Euler).
  - UI/Тактильность: Динамическое смещение перекрестия прицела вверх вслед за спреем и нарастающая пульсирующая виброотдача каждого выстрела.
- **Режим отказа игрока**: Игрок не уводит прицел вниз при зажиме — пули с 4-й по 30-ю улетают на 2 метра выше головы врага, оставляя следы декалей на стене за спиной соперника.
- **Кривая мастерства**: Новичок панически зажимает всю обойму в тело и попадает только первой пулей. Эксперт за доли секунды компенсирует отдачу первых 5 пуль строго в грудь и голову, оформляя фраги за 300 мс.
- **Сопротивление игры**: Враги используют укрытия и стрейфят между вашими очередями, вынуждая сбрасывать зажим и тратить 0.32 с на восстановление точности.
- **Синергии**:
  - Контр-стрейф и фиксация ствола
  - Баллистический прострел укрытий и физика материалов
  - Тактическое зонирование дымовой завесой
- **Почему это не жанровый шаблон**: Детерминированный математический спрей-паттерн (фиксированная траектория смещения пуль), идентичный соревновательным ПК-шутерам, а не случайный конус разброса (Cone of Fire).
- **Псевдокод тика**:
```text
function processRecoil(weapon, isShooting, dt) {
  if (isShooting && weapon.ammo > 0) {
    weapon.shotIndex++;
    const patternOffset = getRecoilPattern(weapon.id, weapon.shotIndex);
    camera.pitch += patternOffset.pitch * dt;
    camera.yaw += patternOffset.yaw * dt;
    spawnBulletRaycast(camera.direction.add(patternOffset));
  } else {
    weapon.shotIndex = Math.max(0, weapon.shotIndex - dt * 15);
    camera.pitch = lerp(camera.pitch, 0, dt * 12.0);
  }
}
```

### Обезвреживание C4, звуковой блеф и клатч-тайминги (C4 Defusal & Audio Bluff Clutch) (SECONDARY)
- **Category**: Главный источник финального напряжения, риска и эмоциональной кульминации раунда.
- **Description**: Глубокая психологическая игра на слух (Mind Games): звук начала дефьюза слышен на весь плент и является главным катализатором дуэли 1v1.
- **Player Input**: ПК: Зажатие и удержание клавиши 'E' (или F) при наведении на C4. Мобильные: Удержание большой круглой кнопки 'DEFUSE' по центру экрана.
- **Technical Complexity**: Medium
- **Решение игрока**: Зажать дефьюз до конца, надеясь на укрытие в дыму, или сделать фейк-дефьюз на 0.15 с, спровоцировать врага на пик и встретить его префаером.
- **Состояния**: `bomb_ticking (свободный таймер 35.0 с)`, `defusing_active (игрок прикован к радиусу бомбы)`, `fake_defuse_cancelled (мгновенная отмена для стрельбы)`, `bomb_defused_victory`, `bomb_detonated_loss`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Базовое время разминирования C4` = `10.0 с` — Если уменьшить до 5.0 с — спецназу не понадобятся саперные наборы; если увеличить до 15.0 с — шансы на ретейк упадут до нуля.
  - `Время разминирования с набором сапёра (Kit)` = `5.0 с` — Если уменьшить до 3.0 с — террористы не успеют среагировать на звук; если увеличить до 7.0 с — ценность набора сапёра нивелируется.
  - `Таймер детонатора C4` = `35.0 с` — Если сделать 45 с — раунд станет слишком затянутым для браузера; если сделать 20 с — спецназ не успеет зайти на точку.
  - `Радиус взаимодействия с C4` = `2.2 метра` — Если увеличить — можно будет дефьюзить из-за глухих углов; если уменьшить — придется стоять вплотную на открытом месте.
- **Слои отклика**:
  - Визуал/VFX: Голографический круговой прогресс-бар дефьюза на экране, синие искры разрезаемых проводов C4, мигающий красным диод бомбы с нарастающей частотой.
  - Звук (Web Audio): Громкий характерный металлический щелчок открытия кейса C4 (слышен на 20 метров всем игрокам), ускоряющийся писк зуммера (1 Гц -> 2 Гц -> 4 Гц -> 8 Гц).
  - Камера: Ограничение угла поворота камеры до 70 градусов во время удержания дефьюза, фокусировка взгляда на объективе бомбы.
  - UI/Тактильность: Нарастающая частая пульсирующая вибрация смартфона в последние 5 секунд до взрыва, крупный таймер обратного отсчета.
- **Режим отказа игрока**: Игрок начинает дефьюз без набора сапёра на 7-й секунде до взрыва (требуется 10 с) — бомба детонирует прямо в руках на 50% прогресса, сотрясая экран вспышкой взрыва.
- **Кривая мастерства**: Новичок боится подойти к бомбе или сидит на дефьюзе 'намертво', погибая от первой пули. Эксперт по звуку шагов определяет позицию террориста, дает фейк-клик дефьюза (0.1 с), разворачивается и мгновенно убивает выбежавшего врага.
- **Сопротивление игры**: Террористы не выходят на первый звук щелчка C4, выжидая 2.5 секунды, или простреливают дым по позиции установки бомбы.
- **Синергии**:
  - Тактическое зонирование дымовой завесой
  - Баллистический прострел укрытий и физика материалов
  - Контр-стрейф и фиксация ствола
- **Почему это не жанровый шаблон**: Глубокая психологическая игра на слух (Mind Games): звук начала дефьюза слышен на весь плент и является главным катализатором дуэли 1v1.
- **Псевдокод тика**:
```text
function updateDefusal(c4, player, dt) {
  if (player.isHoldingDefuseKey && distance(player, c4) <= 2.2) {
    if (!c4.isDefusing) { c4.isDefusing = true; broadcast3DSound('defuse_start', c4.pos); }
    const rate = player.hasKit ? (dt / 5.0) : (dt / 10.0);
    c4.defuseProgress += rate;
    if (c4.defuseProgress >= 1.0) triggerRoundWin('CT', 'bomb_defused');
  } else {
    if (c4.isDefusing) { c4.isDefusing = false; broadcast3DSound('defuse_abort', c4.pos); }
  }
}
```

### Тактическое зонирование дымовой завесой (Smoke Screen Tactical Denial) (SECONDARY)
- **Category**: Инструмент временной блокады углов обзора и безопасного пересечения простреливаемых зон (Long/Cross).
- **Description**: Плотная физическая геометрия видимости Raycast: внутри дыма дистанция обзора падает до 0.8 метра, превращая дуэль в контактный бой в упор.
- **Player Input**: ПК: Выбор клавишей '4' (или колесом мыши) -> бросок на ЛКМ (дальний навес) или ПКМ (короткий подброс под ноги). Мобильные: Тап по иконке гранаты -> кнопка броска.
- **Technical Complexity**: Medium
- **Решение игрока**: Бросить смок в двери Long для блокировки AWP врага или положить смок прямо на C4 для скрытного дефьюза под прикрытием.
- **Состояния**: `grenade_in_hand`, `projectile_flight (баллистическая дуга Three.js)`, `smoke_expanding (раскрытие сферы 1.2 с)`, `smoke_active_full (непроницаемая зона 12.0 с)`, `smoke_dissipating (рассеивание 2.5 с)`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Длительность плотной фазы дыма` = `12.0 с` — Если увеличить до 25 с — раунд превратится в стояние вслепую; если уменьшить до 5 с — спецназ не успеет пересечь опасную зону.
  - `Радиус дымового облака` = `4.2 метра` — Если увеличить — один смок перекроет весь плент; если уменьшить — останутся зазоры (One-way gaps), ломающие баланс.
  - `Время полного раскрытия сферы` = `1.2 с` — Если сделать мгновенным — граната превратится в читерский сейв-щит; если сделать 4 с — враги расстреляют игрока до раскрытия.
  - `Задержка детонации после приземления` = `1.5 с` — Если убрать таймер — смоки будут раскрываться в воздухе, ломая расчет рикошетов от стен.
- **Слои отклика**:
  - Визуал/VFX: Густая объемная серая сфера частиц (Volumetric Particle Cloud) Three.js с эффектом полного затемнения экрана при заходе внутрь (Fog of War).
  - Звук (Web Audio): Громкий шипящий звук выброса углекислотного газа ('hiss-whoosh') при раскрытии и фоновое приглушение внешних звуков внутри облака.
  - Камера: Мягкое размытие (DoF/Blur) периферийного зрения при приближении к границе смока.
  - UI/Тактильность: Индикатор на миникарте, скрывающий маркеры противников, находящихся внутри или за дымовой завесой.
- **Режим отказа игрока**: Игрок криво кидает смок в край ящика — граната рикошетит обратно под ноги спецназу, закрывая обзор собственной команде и открывая спину террористам.
- **Кривая мастерства**: Новичок бросает смок наугад под ноги и бежит сквозь него вслепую, умирая от зажима. Эксперт знает точные ориентиры броска от текстур Dust 2 (Lineup), идеально отрезая снайперские позиции CT-Spawn.
- **Сопротивление игры**: Враги начинают спамить очередями и простреливать центр дымового облака по характерным точкам или закидывают осколочную гранату HE для рассеивания.
- **Синергии**:
  - Обезвреживание C4, звуковой блеф и клатч-тайминги
  - Баллистический прострел укрытий и физика материалов
  - Контр-стрейф и фиксация ствола
- **Почему это не жанровый шаблон**: Плотная физическая геометрия видимости Raycast: внутри дыма дистанция обзора падает до 0.8 метра, превращая дуэль в контактный бой в упор.
- **Псевдокод тика**:
```text
function updateSmokeGrenade(smoke, dt) {
  if (smoke.state === 'flying') {
    smoke.pos.addScaledVector(smoke.velocity, dt);
    smoke.velocity.y -= 9.81 * dt;
    checkWallBounces(smoke);
  } else if (smoke.state === 'active') {
    smoke.timer -= dt;
    applyVisibilityOcclusion(smoke.pos, smoke.radius);
    if (smoke.timer <= 0) smoke.state = 'dissipating';
  }
}
```

### Баллистический прострел укрытий и физика материалов (Wallbang & Material Penetration) (SECONDARY)
- **Category**: Тактический тормоз для кемперов за ящиками и источник награды за знание карты и позиционирование врага.
- **Description**: Двухсегментный Raycast с расчетом точки входа, точки выхода и толщины коллайдера Three.js/Rapier3D, рассчитывающий реальную толщину материала.
- **Player Input**: ПК: Прицеливание в текстуру стены/двери/ящика по звуку шагов + ЛКМ. Мобильные: Удержание прицела на ребре деревянного препятствия + кнопка стрельбы.
- **Technical Complexity**: Medium
- **Решение игрока**: Потратить 5 патронов на прострел деревянных дверей/ящиков по интуиции или поберечь магазин для прямого визуального контакта.
- **Состояния**: `solid_impact (рикошет от бетона, 0 урона сквозь)`, `wood_penetration (пробитие до 0.35 м со снижением урона на 40%)`, `metal_penetration (пробитие до 0.12 м со снижением на 70%)`, `kill_confirmed_wallbang`
- **Числовые параметры** — лежат в `balance.yaml`, читай оттуда и не переписывай литералами в код:
  - `Коэффициент сохранения урона сквозь дерево` = `60.0%` — Если поднять до 90% — укрытия потеряют смысл, игроков будут убивать через любые ящики; если опустить до 20% — прострелы станут бесполезны.
  - `Максимальная толщина пробития дерева` = `0.35 метра` — Если увеличить — пули будут шить двойные бетонные стены; если уменьшить — даже тонкие створки дверей Dust 2 перестанут простреливаться.
  - `Коэффициент пробития тонкого металла (двери B)` = `30.0%` — Если увеличить — пулеметы сломают баланс на пленте B; если уменьшить — позиция за дверью станет неуязвимой.
  - `Снижение скорости пули после препятствия` = `45.0%` — Если не снижать — дистанционное падение урона за препятствием не будет ощущаться.
- **Слои отклика**:
  - Визуал/VFX: Сноп летящих щепок и древесной пыли с лицевой стороны, точка выхода пули с тыльной стороны укрытия, желтая иконка сквозного пробития в киллфиде.
  - Звук (Web Audio): Глухой треск раскалываемого дерева ('wood splinter crack') с последующим глухим звуком попадания в тело за стеной.
  - Камера: Микро-вибрация прицела при попадании пули в твердое препятствие высокой плотности.
  - UI/Тактильность: Отдельный звуковой и визуальный маркер хитмаркера (красный шеврон пробития) при успешном нанесении урона через стену.
- **Режим отказа игрока**: Игрок тратит весь магазин AK-47 на стрельбу в толстую бетонную стену (где урон 0%), выдавая свою позицию грохотом и оставаясь с пустым магазином перед выходящим врагом.
- **Кривая мастерства**: Новичок стреляет только в видимых противников и боится ящиков. Эксперт знает префайр-точки деревянных дверей Mid и коробок на пленте A, нанося урон врагам ещё до того, как они покажутся из-за угла.
- **Сопротивление игры**: Защитники укрытий постоянно меняют позицию за ящиком при первых звуках прострела или делают контр-выход в момент перезарядки стреляющего.
- **Синергии**:
  - Контр-стрейф и фиксация ствола
  - Компенсация спрей-паттерна и темповые отсечки
  - Тактическое зонирование дымовой завесой
- **Почему это не жанровый шаблон**: Двухсегментный Raycast с расчетом точки входа, точки выхода и толщины коллайдера Three.js/Rapier3D, рассчитывающий реальную толщину материала.
- **Псевдокод тика**:
```text
function checkWallbangRay(origin, dir, maxDist) {
  const hit = raycastFirst(origin, dir, maxDist);
  if (!hit) return null;
  if (hit.material.penetrable) {
    const exitHit = raycastReverse(hit.point.add(dir.clone().multiplyScalar(hit.material.maxDepth)), dir.negate());
    const thickness = hit.point.distanceTo(exitHit.point);
    if (thickness <= hit.material.maxDepth) {
      const remainingDmg = baseDamage * hit.material.dmgModifier * (1.0 - thickness / hit.material.maxDepth);
      return applyDamageBehindWall(exitHit.point, dir, remainingDmg);
    }
  }
  spawnSparkDecal(hit.point, hit.normal);
}
```


---

## 3b. ⚠️ СТРОГИЙ ЗАПРЕТ ЖАНРОВЫХ ШАБЛОНОВ И КЛОНОВ (CRITICAL ANTI-CLICHÉ RULES)
Кодовый агент ОБЯЗАН реализовать уникальную игру, спроектированную в этом ТЗ, а не шаблонный автошутер:
1. **ЗАПРЕТ ШАБЛОННЫХ РОГАЛИКОВ И КАРТОЧЕК**: Если игра прямо не требует карточный драфт в GDD, **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** добавлять спам волн врагов и всплывающее окно «Выберите 1 из 3 карт апгрейда».
2. **ЗАПРЕТ СЕРЫХ ПРИМИТИВОВ НА ПУСТОЙ ПЛОСКОСТИ**: Создавай выразительную процедурную низкополигональную 3D/2D графику, точно соответствующую `ART_DIRECTION.md` (тематические персонажи, рельеф, модули, объекты окружения, частицы), а не бегающий куб на сером полу.
3. **ТОЧНОЕ СОБЛЮДЕНИЕ МЕХАНИК**: Реализуй все состояния, формулы, тайминги и слои отклика из `MECHANICS.md` (включая Web Audio звуки, импульсы камеры, визуал и тач-инпут).
4. **СПЕЦИФИЧЕСКОЕ УПРАВЛЕНИЕ**: Реализуй схему управления под конкретную механику этой игры (свайпы, жесты, траектории, физический дрифт, прицеливание), а не стандартный стик.

---

## 3c. ГОТОВОЕ: ЧТО УЖЕ НАПИСАНО И ОТЛАЖЕНО — SHOULD

Ниже — рабочий код фабрики: не примеры из статьи, а модули, которые крутились в
живых сценах, и в которых числа выставлены не наугад. Каждый файл объясняет в
своей шапке, почему константы именно такие — это та часть работы, которую с
нуля не воспроизвести за один заход.

**Правило.** Прежде чем писать механику с нуля, посмотри, нет ли её здесь.
Нашёл — тяни файл и подгоняй под свою игру. Не нашёл или не подходит — пиши
сам, но запиши в `DEVLOG.md` строку «взял/не взял <файл>, потому что …».
Молча проигнорировать готовое нельзя: это стоило нам рэгдолла, который писали
заново, хотя он лежал написанным.

Загрузка любого файла — одной командой, путь берётся из таблицы:

```bash
node scripts/fetch-knowledge.mjs workspace/knowledge-showcase/src/game/stealthSensing.ts
```

Файл появится в `docs/ref/<путь>`, оттуда копируй в `src/`.

Колонка «Готовность» — это часы работы, а не оттенок смысла:
- **копируется как есть** — ни одного импорта, чистая логика и числа. Взял, переименовал, поехал.
- **нужен three, больше ничего** — тянет только `three`, чужого проекта в нём нет.
- **образец, переписать под себя** — тянет модули стенда. Читать как показ того, как это собирается, а не копировать.

**Похоже, пригодится именно этой игре:**

| Файл | Что даёт | Строк | Готовность |
|---|---|---|---|
| `workspace/knowledge-showcase/src/audio/AudioManager.ts` | Web Audio: Procedural Sound Synthesizer & Analyser (Zero MP3 files) | 530 | копируется как есть |
| `workspace/knowledge-showcase/src/game/flowField.ts` | Флоу-филд и строй для RTS | 283 | копируется как есть |
| `workspace/knowledge-showcase/src/game/survivorRun.ts` | Забег в survivor-игре: кривая опыта, карточки апгрейдов, эскалация орды | 279 | копируется как есть |
| `workspace/knowledge-showcase/src/game/towerDefense.ts` | Tower defense: контракт волн, приоритет целей, экономика | 225 | копируется как есть |
| `workspace/knowledge-showcase/src/game/stealthSensing.ts` | Стелс: конус зрения, шкала подозрения, шум | 145 | копируется как есть |
| `workspace/knowledge-showcase/src/game/rhythmAudio.ts` | Web Audio Timing, Rhythm Beat Sync & Muting Logic | 80 | копируется как есть |
| `workspace/knowledge-showcase/src/world/carRig.ts` | Процедурный low-poly болид. Никаких .gltf: вся геометрия — код | 72 | нужен three, больше ничего |
| `workspace/knowledge-showcase/src/world/ragdoll.ts` | Рэгдолл на Rapier: семь тел, шесть сферических суставов | 214 | образец, переписать под себя |
| `workspace/knowledge-showcase/src/demos/TruckDemo.ts` | Эталонная вкладка стенда: ЗиЛ-130 на честной физике Rapier3D | 101 | образец, переписать под себя |
| `workspace/knowledge-showcase/src/stack/bvhSetup.ts` | Установка расширений three-mesh-bvh — ОДИН раз на приложение | 28 | образец, переписать под себя |

Это не весь каталог: всего готовых модулей 67. Полный список с описаниями — в `LIBRARY.md` рядом с этим файлом. Загляни туда, когда возьмёшься за механику, которой нет в таблице выше.

---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Rendering Layer**: 
- **Physics & Collision Layer**: 
- **Gameplay & AI Layer**: 
- **UI & Platform Layer**: 

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   └── PhysicsWorld.ts        # Rapier3D / Physics world manager & colliders
├── entities/
│   ├── Player.ts              # Player entity & input handling
│   └── EntityManager.ts       # Dynamic entity pool & lifecycle
├── systems/
│   ├── RaycastHitscanHitboxesSystem.ts # Расчет траекторий пуль, спрея, пробития укрыт
│   ├── TacticalCoverPeekingAiSystem.ts # Управление поведением союзников и противников
│   ├── C4BombObjectiveSystem.ts # Управление 35-секундным таймером бомбы, механ
│   ├── TouchFpsControllerSystem.ts # Обеспечение соревновательного комфорта и чест
├── rendering/
│   ├── SceneManager.ts        # Three.js scene graph, lights, perspective camera lerp
│   ├── ProceduralModels.ts    # Styled models / geometry for Dust 2: Ретейк и Дуэли
│   └── ParticleSystem.ts      # Particle effects & visual feedback
├── ui/
│   ├── theme.css              # Единственное место со значениями: цвета, шрифты, шкала, слои
│   ├── UiRoot.ts              # Слои над канвасом, измеренный вьюпорт, safe-area + баннер
│   ├── ScreenRouter.ts        # Экраны как автомат: один видимый, один общий переход
│   ├── Hud.ts                 # Запись в закэшированные узлы только при изменении значения
│   ├── components/            # Button, IconButton, Panel, Modal, Toast, Meter, Stat, ListRow
│   ├── screens/               # По файлу на экран, каждый — со своими loading/empty/error
│   ├── icons.ts               # Один инлайновый SVG-спрайт, цвет через currentColor
│   └── TouchControls.ts       # Mobile touch input adapter
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic audio feedback
```

### Контракт модулей — MUST

Карта выше говорит, какие файлы завести. Ниже — чего им нельзя, потому что
именно на этом ломаются сгенерированные проекты:

- **DOM создаётся только в `src/ui/`.** Ни один файл вне `src/ui/components/` и
  `src/ui/screens/` не вызывает `document.createElement` и не присваивает
  `element.style.*`. Экран, собранный инлайновыми стилями в обход `theme.css`, —
  это второй экран, который перестанет совпадать с первым.
- **Значения — только в `src/ui/theme.css`.** Проверяется **B1**.
- **`src/core/` не знает про площадку.** Playgama Bridge живёт в
  `src/platform/`; всё остальное общается с ним через свой интерфейс. Иначе
  игру нельзя запустить локально без SDK.
- **`src/ui/` не знает про физику и рендер.** Интерфейс читает состояние
  через `EventBus` и не держит ссылок на `THREE.*` и на мир Rapier.
- **Числа игры — из `balance.yaml`.** Литерал скорости, урона, тайминга или
  радиуса в коде — баг: правка баланса не должна быть правкой кода.
- **Цикл обновления не аллоцирует.** Ни `new`, ни литерала объекта, ни
  `array.map` в кадре: пулы заводятся заранее. Проверяется **E4**.
- **Каждый модуль экспортирует один публичный класс или функцию** и не тянет
  импорт из соседнего слоя мимо своего интерфейса.

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Бесплатный оружейный кейс (`case_unlock`)**: Открытие оружейного кейса с шансом выбить редкий скин Covert (Trigger: Экран победы / Меню арсенала, Limit: 3 раза в день)
- **Удвоение ранговых очков ELO (`elo_double`)**: Удваивает полученные очки рейтинга ELO за победу в матче (Trigger: Экран финальной победы 3:x, Limit: 1 раз за матч)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"player_elo_rating"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Landscape
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Визуальный контракт интерфейса
Интерфейс — первое, что видит игрок: до геймплея он смотрит на меню. Меню,
собранное из умолчаний браузера, обесценивает всё, что сделано за ним, и это
самый заметный признак недоделанной игры. Всё ниже — требование, а не пожелание.

**Материал интерфейса**: Минималистичный соревновательный HUD в тёмно-графитовых тонах с неоново-оранжевыми и белыми акцентами

**Визуальный язык**: Строгий соревновательный минимализм с высокой контрастностью силуэтов врагов на песчаном фоне Dust 2

**Типографика**: Oswald / Roboto Condensed, Bold, Uppercase для цифр, таймеров и счета

**Сцена за меню.** Динамическая 3D-сцена точки A карты Dust 2 на закате с позирующим оперативником, держащим автомат AK-47

Меню, пауза и итог сессии рисуются ПОВЕРХ этой сцены. Непрозрачный слой на весь экран (`background: #111` на корне экрана, полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка допускается только под текстовым блоком и кнопками. Игровой цикл в меню продолжает крутиться на сниженной нагрузке — камера едет, сцена живёт. Игрок обязан понять, во что он играет, до нажатия «Играть»; меню на глухой заливке — самый заметный признак недоделанной игры, и это первое, что увидит и игрок, и модератор площадки.

**Акценты — один цвет = один смысл** (разный цвет у каждой кнопки «чтобы отличались» читается как мусор):

| Токен | Единственный смысл |
|---|---|
| `c4_danger` | #E74C3C |
| `ct_color` | #4A90E2 |
| `primary_action` | #FF9900 |
| `success_green` | #2ECC71 |
| `t_color` | #E67E22 |

**Набор компонентов — закрытый.** Всё на экране обязано быть одним из них; одноразовый `<div>` с инлайновыми стилями — это баг, из-за которого второй экран перестаёт совпадать с первым:
- Button
- Crosshair
- HealthBar
- RadarView
- KillfeedItem
- WeaponCard

**Якоря HUD** — пять и только пять; посередине экрана не висит ничего, кроме временной обратной связи:

| Якорь | Что там |
|---|---|
| `ammo_weapon` | bottom-right |
| `crosshair` | screen-center |
| `health_armor` | bottom-left |
| `killfeed` | top-right |
| `match_score` | top-center |
| `radar` | top-left |

**Экраны и их композиция.** У каждого экрана три зоны: чем он себя называет, единственное главное действие (самое крупное и единственное с основным акцентом) и второстепенный ряд одним весом. Карточка с колонкой кнопок по центру — не композиция, а её отсутствие:
- **MainMenu** · Выбор стороны (CT / T), кнопка 'В БОЙ', доступ к Арсеналу и Рейтингу · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **GameplayHUD** · Минималистичный соревновательный боевой экран со всеми тактическими показателями · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **RoundEndModal** · Краткий 3-секундный оверлей с результатом раунда (Победа CT: Бомба обезврежена) · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **MatchResultScreen** · Итоговая таблица счета, изменение ELO, заработанные награды и кейсы · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.
- **ArsenalScreen** · 3D-осмотр открытых скинов оружия с вращением модели · композиция: Три зоны: чем экран себя называет, единственное главное действие, второстепенный ряд одним весом. Меню лежит поверх живой сцены.

**Состояние, показанное миром, а не оверлеем**:
- Цифровой таймер на корпусе самой бомбы C4
- Анимация затвора и счетчик патронов на корпусе винтовки
- Отверстия от пуль и следы копоти на песчаных стенах карты

**Переходы экранов**: MainMenu -> Matchmaking (3 сек) -> Retake Round 1-5 -> MatchEnd -> MainMenu / Arsenal

**Отклик интерфейса на действие**:
- Красный хитмаркер и характерный звук хруста шлема при хэдшоте
- Тряска экрана и виброотклик при близком взрыве гранаты
- Плавное затемнение экрана по краям при падении HP ниже 25
- Победный фанфарный звуковой сигнал при успешном дефьюзе C4

**Состояния экранов** — загрузка, пустота и ошибка проектируются наравне с удачным путём: сохранение, таблица лидеров, покупки и реклама на этих площадках отваливаются регулярно:
- Loading: прогресс-бар предзагрузки 3D-моделей и текстур
- Playing: чистый соревновательный HUD без лишнего визуального шума
- Paused: затемнение с настройками звука и чувствительности прицела
- GameOver: итоговая сводка матча со статистикой попаданий в голову

**Значения выводятся из этого мира, а не берутся из примеров.** Имена токенов (`--color-primary`, `--font-display`, `--space-*`) одинаковы во всех играх — на них держится код компонентов. Сами значения, обе гарнитуры, силуэт рамки и язык иконок выводятся из материала интерфейса, названного выше, по процедуре из раздела 12 `knowledge/ux/ui_design_system.md`: из чего сделана панель, при каком свете она лежит, чем в этом мире помечают главное и опасное, чем написаны цифры, как режут край. Скопировать палитру, шрифты или фаску из примера в базе знаний или из другой игры — это тот же дефект, что и оставить умолчания браузера: интерфейс выглядит решением, принятым не для этой игры. Обратная проверка: если от интерфейса другой игры фабрики ваш отличается только оттенком акцента — процедура была пропущена.

**Обязательные технические правила интерфейса** (каждое — из починенного бага, подробности в `knowledge/ux/ui_design_system.md` и `knowledge/ux/ui_implementation.md`):
- Все значения — токенами в одном `src/ui/theme.css`: цвета, две гарнитуры, шкала отступов, радиусы, длительности, `--z-*`. Литерал `#RRGGBB` или `z-index: 9999` внутри экрана запрещён; `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` мимо темы обязан быть пуст.
- Слои над канвасом прозрачны для ввода: контейнер — `pointer-events: none`, `auto` включают только листовые интерактивные элементы. Слой HUD не кликается никогда. Полноэкранный оверлей, оставленный на `auto`, съедает игровой ввод — и это выглядит как «на телефоне не работает управление».
- Один масштаб на весь интерфейс (`--ui-scale`), а не отдельная раскладка под каждый размер экрана. Размеры зон нажатия при этом не масштабируются: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px.
- Меняющиеся числа — `font-variant-numeric: tabular-nums` в слоте фиксированной ширины; полосы — `transform: scaleX()`, не `width`. HUD пишет в закэшированные узлы и только при изменении значения: пересборка разметки в кадре стоит кадров.
- Один экран виден за раз, скрытый — `display: none`; переход один на всю игру и укладывается в 300 мс. Анимируются только `transform` и `opacity`. `prefers-reduced-motion: reduce` убирает трансформации.
- Любое нажатие отвечает в том же кадре; действие с ожиданием переводит кнопку в `loading` и снимает его в `finally`.
- Запрещено: `alert`/`confirm`/`prompt`, эмодзи вместо иконок (один инлайновый SVG-спрайт с `currentColor`), голый `<input type=range>` и `<select>`, текст над геймплеем без подложки или обводки, серая неактивная кнопка вместо отсутствующей на площадке возможности — такой элемент не рисуется вовсе.
- Геометрия меню считается от ИЗМЕРЕННОГО вьюпорта (`visualViewport`, пересчёт после поворота, выхода из фуллскрина и закрытия рекламы), а не от `100vh`; нижний отступ включает измеренную высоту липкого баннера. Страница под игрой не скроллится — длинные списки скроллятся во внутреннем контейнере.
- `backdrop-filter: blur()` на полноэкранном слое — самое дорогое свойство оверлея на мобильных GPU; только на маленьких панелях.

### Контракт тач-управления
- **Раскладка этой игры** (спроектирована в MOBILE_CONTROLS.md, переносить сюда раскладку из другой игры нельзя): Левая зона экрана (45% ширины) — свободное перемещение пальцем со свайп-остановкой (авто-контр-стрейф при отрыве пальца). Правая зона (55% ширины) — наведение прицела. В правом нижнем секторе крупная полупрозрачная кнопка огня, рядом кнопка перезарядки и контекстная кнопка действия [ДЕФЬЮЗ / C4].
- Раскладка выводится из главного действия игрока. Виртуальный джойстик уместен там, где игрок непрерывно ведёт персонажа; там, где действие точечное (тап по цели, свайп, удержание), джойстик — лишний элемент, который занимает половину экрана и ничего не делает.

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Если в раскладке есть стик** — он плавающий: зона захвата — вся левая
  половина экрана, база появляется под пальцем, мёртвая зона 8%, иначе
  управление дрожит. Если раскладка обходится без стика — не добавляйте его.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `WASD` / стрелки — движение
- ЛКМ / `J` — основная атака
- ПКМ / `K` — тяжёлая атака / блок
- `Space` / `Shift` — рывок / уклонение
- `P` / `Esc` — пауза

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/sozday_igru_po_tipu_cs_go_s_kartoy_dust_2_vyboro/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ДИЗАЙН: ЕГО ПИШЕШЬ ТЫ — MUST

Готового арт-дирекшена в этом пакете нет намеренно. Дизайн, продиктованный
списком полей, получается одинаковым от игры к игре, и узнать в нём конкретную
игру невозможно. Поэтому первым делом, до кода, ты пишешь `DESIGN.md` сам.

**Что в нём должно быть** (без этих пяти пунктов файл не считается написанным):

1. **Одна фраза о том, как игра выглядит.** Не «мрачный киберпанк», а что
   именно видит игрок в первую секунду.
2. **Палитра** — 5–7 цветов с ролями (фон, поверхность, текст, акцент, опасность,
   успех) и HEX. Они же становятся токенами в `theme.css`.
3. **Камера и свет** — где стоит камера, что она захватывает, откуда светит и
   почему сцена читается на телефоне.
4. **Экраны** — для каждого: что на нём, где три зоны (заголовок, содержимое,
   действие), какое действие главное.
5. **Сцена за меню** — что живёт на фоне главного меню. Не заливка, не блюр
   поверх ничего: работающая сцена игры под пониженной нагрузкой.

**Чего в нём быть не должно** — это уровень **MUST**, проверяется приёмкой:
эмодзи вместо иконок, `alert()` вместо модалки, непрозрачная плашка поверх
канваса, фиолетово-синий градиент, интерфейс из умолчаний браузера.

**Отправная точка от фабрики** — это подсказка, а не приговор. Не подошло —
меняй и запиши в `DEVLOG.md`, что и почему:

- Стиль: Стилизованный тактический реализм (Semi-realistic PBR)
- Камера: First-Person (Камера от первого лица с динамическим покачиванием оружия) (FOV 75°, наклон 0°)
- Среда: Культовая карта Dust 2: залитый ярким солнцем ближневосточный городок с охристыми песчаными стенами, синим небом, простреливаемыми деревянными дверями и ящиками с граффити «A» и «B»
- Свет: Яркий направленный солнечный свет (Directional Light) тёплого золотистого оттенка с чёткими контрастными тенями и мягким рассеянным небесным светом (Ambient Skylight)
- Тема интерфейса: Минималистичный соревновательный HUD в тёмно-графитовых тонах с неоново-оранжевыми и белыми акцентами (реализация — секция 6)
- Сцена за меню: Динамическая 3D-сцена точки A карты Dust 2 на закате с позирующим оперативником, держащим автомат AK-47

Подробности отбора — в `ART_DIRECTION.md` и `UI_UX_SPECIFICATION.md` рядом.

---

## 8. РОАДМАП

Фаза закончена не тогда, когда код написан, а когда закрыты её проверки из
`ACCEPTANCE.md`. Прогоняй `node scripts/check-spec.mjs` в конце каждой фазы.

| Фаза | Чем закрывается |
|---|---|
| Подготовка | **F4**, **F1** — база загружена в `docs/ref/`, `DESIGN.md` написан |
| Каркас и площадка | **A1**, **A3**, **C1–C4** — игра собирается и стартует на площадке |
| Главная механика | **D1**, **B6** — в неё можно играть, ввод доходит до игры |
| Остальные механики и петля | **D2–D5**, **B9–B11** — петля проходится целиком |
| Интерфейс и подача | **B1–B5**, **B7–B8**, **B12** — интерфейс собран из токенов, меню стоит на живой сцене |
| Полировка и релиз | **C5–C11**, **E1–E5**, **A2**, **A4**, **F2–F3** — сохранения, реклама, кадры, сборка |

### Phase 1: Создание ядра FPS-контроллера, баллистики и геометрии точек A/B Dust 2 (2 days)
- **Deliverable**: Рабочий прототип перемещения с контр-стрейфом, стрельбы со спреем и базовой карты Dust 2
  - Сборка оптимизированной low-poly 3D-геометрии точек A и B карты Dust 2
  - Реализация FPS-контроллера с контр-стрейфом и спрей-паттернами AK-47 и M4A4
  - Настройка хитбоксов персонажей и рэйкаст-хитскана с ван-тапом в голову
### Phase 2: ИИ тактических ботов, система бомбы C4 и логика раундов 3v3 (2 days)
- **Deliverable**: Полноценный боевой матч 3v3 ретейка с закладкой, дефьюзом C4 и умными ботами
  - Разработка дерева поведений тактических ботов (удержание углов, реакция на шаги)
  - Создание механики бомбы C4 с таймером 35с, набором сапёра и звуковым фейк-дефьюзом
  - Реализация цикла матча из 5 раундов до 3 побед с подсчетом счета и K/D
### Phase 3: Мобильная адаптация управления, UI/HUD и интеграция Playgama Bridge (2 days)
- **Deliverable**: Готовый билд с удобным тач-управлением, HUD и интеграцией Playgama Bridge
  - Внедрение адаптивной мобильной раскладки со свайп-остановкой и плавающей кнопкой огня
  - Создание соревновательного HUD (радар, киллфид, счетчик патронов, статус C4)
  - Интеграция Playgama Bridge SDK (сохранение ELO, лидерборды, реклама)
### Phase 4: Полировка спрея, аудио-эффекты, ранговая система и финальное тестирование (1 days)
- **Deliverable**: Релизная версия игры, оптимизированная под 60 FPS на всех платформах
  - Финальный тюнинг спрей-паттернов, звуков выстрелов, шагов и дымовых гранат
  - Настройка ранговой системы от Silver до Global Elite и кейсов со скинами
  - Кросс-браузерное тестирование на WebGL, Safari iOS и Android

---

## 9. ПРАВИЛА ПЛОЩАДКИ — УРОВЕНЬ MUST
Каждое правило ниже — след бага, дошедшего до продакшена, или отказа модерации.
Они перекрывают любую привычку, туториал и пример — включая куски из документации
Playgama и Яндекса, многие из которых описывают устаревший контракт Bridge v1.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

### Orientation & pointer lock

69. `Object3D.lookAt` aims the object's **+Z** at the target; −Z is the convention
    for cameras and lights only. Geometry that will later be aimed with `lookAt`
    (viewmodel arms, tracers, arrows, turret beams) must be built along **+Z**.
    Built along −Z it points backwards after aiming, and the bug reads as "broken
    model" rather than a flipped axis.

70. `requestPointerLock()` is called **from the pointer-down handler**, never from
    the frame loop: after an Esc exit a request issued inside
    `requestAnimationFrame` is silently rejected and the game looks dead. While
    `document.pointerLockElement` is set, application hotkeys (level switch, menu,
    tabs) must stay silent — otherwise an in-game key throws the player out of the
    game.

71. A `MeshStandardMaterial` with high `metalness` and **no environment map
    renders black** — metal has nothing to reflect. Set `scene.environment`
    (a `RoomEnvironment` PMREM costs no file and no request) **and** keep
    `metalness ≤ 0.4` in the material: the environment is the first thing a
    low tier drops, and the asset must not turn into a hole in the frame
    when it does.

72. A node whose base orientation is not zero — anything aimed with
    `lookAt`, `setFromUnitVectors` or a baked rest pose — is animated
    through a **wrapper**, never by writing its own `rotation`. Code that
    returns a limb to rest with `rotation.x = lerp(rotation.x, 0, k)` reads
    the Euler of that very quaternion and erases the aim: the part turns
    away from what it was holding, and the bug reads as "the model is
    missing a piece".

73. Anything positioned from **world** coordinates — a prop placed by two
    hand positions, an effect anchored to a bone, a decal following a
    moving platform — must be converted into the parent's local space
    before it is written into `position`/`quaternion`. Writing world
    numbers into a child of a transformed node applies that transform a
    second time, and the object drifts away by exactly the owner's offset
    from the origin. It stays invisible while the owner sits at the origin
    (which is where every unit test and every bake script puts it), and
    appears only in the game, as "the weapon is flying around the level".

74. Where the character **looks** and where the weapon **points** are two
    different directions, and gameplay owns the second one. A mocap firing
    stance is bladed: the axis through the hands is 40–60° away from the
    model's forward, so turning the body at the target aims the barrel past
    it. Measure the offset once on the assembled rig, turn the body by
    `heading − offset`, and take up the remainder by rotating the chest —
    with an exact quaternion `from barrel to target`, not by tweaking a
    single Euler axis: a barrel that sits at an angle to the rotation plane
    turns by only `angle · cos(that angle)`.

### Interface

75. UI layers over the canvas are **transparent to input by default**: every
    layer container is `pointer-events: none` and only leaf interactive elements
    turn it back on. A full-screen overlay left at `auto` swallows every gameplay
    pointer, and the bug reads as "the car does not steer on mobile". The HUD
    layer is read-only and never becomes interactive at all — pause and settings
    are buttons on the screen layer.

76. Every colour, font, radius, spacing step and duration comes from tokens in
    one theme file. A literal `#RRGGBB`, `px` padding or `z-index: 9999` written
    inside a screen is the reason the second screen stops matching the first.
    `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` minus the theme file returns nothing.
    Имена токенов при этом одинаковы во всех играх, а **значения — нет**: палитра,
    обе гарнитуры и силуэт рамки выводятся из материала мира этой игры по
    процедуре в `knowledge/ux/ui_design_system.md`, раздел 12. Скопированный из
    примера или из соседней игры набор значений — такой же дефект, как умолчания
    браузера, только заметнее: он выглядит осознанным решением, принятым не для
    этой игры.

77. Browser dialogs (`alert`, `confirm`, `prompt`) and emoji used as icons are
    **banned in shipped UI**. The first blocks the game loop, cannot be styled and
    breaks immersion; the second renders differently on every OS and reads as a
    placeholder. Use the project's `Modal` component and one inline SVG sprite
    coloured by `currentColor`.

78. The HUD updates by writing to **cached nodes** and only when the value
    changed — never by rebuilding markup inside the frame loop, and never by
    reading layout (`offsetWidth`, `getBoundingClientRect`) there. Changing
    numbers carry `font-variant-numeric: tabular-nums` in a fixed-width slot, and
    bars animate with `transform: scaleX()`, not `width`; otherwise the row
    reflows on every tick and the whole overlay repaints.

79. Every screen defines its **loading, empty and error** states, not only the
    happy path. Saves, leaderboards, purchases and ads are network calls that fail
    routinely on these portals; a failure that renders an empty frame or says
    nothing costs the player their run and reads as a broken game.

80. Menu and HUD geometry is sized against the **measured** viewport
    (`visualViewport`, re-measured after orientation change, fullscreen exit and
    ad close), never `100vh`, and every UI layer is inset by
    `env(safe-area-inset-*)` **plus** the measured sticky-banner height. The page
    itself never scrolls — long content scrolls in an inner container.
    See `knowledge/ux/ui_design_system.md` and `knowledge/ux/ui_implementation.md`.

81. Меню, пауза и экран итога сессии рисуются **поверх живой игровой сцены**, а
    не поверх заливки. Тот же рендерер, та же сцена, медленная камера, работающий
    свет; игровой цикл в меню продолжает крутиться на сниженной нагрузке.
    Непрозрачный слой на весь экран (`background: #111` на корне экрана,
    полноэкранная `rgba(...)`-заливка, картинка-заставка) запрещён: подложка
    допускается только под текстовым блоком и кнопками. Меню — это первое и
    иногда единственное, что видит игрок: за глухой плашкой с колонкой кнопок
    вся работа над геймплеем невидима, и игра читается как недоделанная ещё до
    запуска. Постановку сцены задаёт `ART_DIRECTION.md` (раздел «Сцена за меню»).

82. У каждого экрана **три зоны**: чем экран себя называет, единственное главное
    действие (самое крупное и единственное с основным акцентом) и второстепенный
    ряд одним весом. Одна карточка с колонкой кнопок по центру — не композиция, а
    её отсутствие; композиция каждого экрана задана в `UI_UX_SPECIFICATION.md`,
    раздел «Каталог экранов», и придумывать её заново не нужно.

### Разделы, которые открываются по месту
Эти правила нужны не с первой строки, а когда доберёшься до соответствующей области. Полный текст — в `docs/ref/knowledge/CRITICAL_RULES.md` (файл обязательный, приезжает шагом 0). Обязательность от переноса не меняется.
- **Stack** — выбор библиотек стека — перед подключением любой из них
- **Renderer** — настройки рендерера — когда собираешь сцену
- **Physics vehicles** — физика транспорта — только если в игре есть машины

---

## 10. ПРИЁМКА — MUST

Полный список проверок с номерами лежит в `ACCEPTANCE.md`, рядом с этим файлом.
Он и есть определение готовности: пока хотя бы один пункт разделов **A–C**
красный, игра не готова — это не качество, это работоспособность.

Статическая часть проверяется командой (скрипт уже лежит в проекте, зависимостей
не требует — пропиши его в `package.json` как `check:spec`):

```bash
node scripts/check-spec.mjs
```

Прогонять после каждой фазы роадмапа, а не один раз в конце. Результат
записывать строкой в `DEVLOG.md`. Пункт, который не проходит, честнее оставить
красным с объяснением, чем отметить зелёным: следующий, кто откроет проект,
будет считать его проверенным.

Критерии, которые относятся именно к этой игре:

- [ ] Полная 3D-геометрия точек A и B карты Dust 2 оптимизирована под WebGL с 60 FPS на ПК и мобильных
- [ ] Реализован контроллер персонажа от первого лица с механикой контр-стрейфа на клавиатуре и сенсорном экране
- [ ] Настроена баллистика оружия: спрей-паттерны AK-47, M4A4, AWP, Deagle и хитбоксы с 1-tap headshot
- [ ] Работает тактический ИИ ботов 3v3 с поиском укрытий, реакцией на шаги и удержанием секторов плента
- [ ] Реализована система бомбы C4 с таймером 35 секунд и механикой дефьюза за 5/10 секунд
- [ ] Реализована система матчей из 5 раундов до 3 побед со сменой сторон и подсчётом статистики K/D
- [ ] Интегрирован SDK Playgama Bridge с сохранением ELO-рейтинга и рекламными плейсментами
- [ ] Успешно пройден чек-лист QA на Chrome, Safari iOS и мобильных браузерах
- [ ] Project builds cleanly with zero TypeScript errors (`npm run build`).
- [ ] Playgama Bridge initializes and dispatches GAME_READY platform message.
- [ ] Петля играбельна целиком: Выбор позиции и оружия -> Тактический выход с контр-стрейфом -> Прицельная перестрелка короткими отсечками -> Закладка/Обезвреживание бомбы C4 -> Завершение раунда и анализ тактики -> Победа в матче из 5 раундов..
- [ ] Реализованы механики проекта: Контр-стрейф и фиксация ствола (Counter-Strafing & Velocity Zeroing), Компенсация спрей-паттерна и темповые отсечки (Recoil Spray Pattern & Burst Fire), Обезвреживание C4, звуковой блеф и клатч-тайминги (C4 Defusal & Audio Bluff Clutch), Тактическое зонирование дымовой завесой (Smoke Screen Tactical Denial).
- [ ] Условие успеха (Одержать победу в 3 раундах из 5 за Спецназ (обезвредить C4 или ликвидировать всех террористов до закладки) либо за Террористов (успешно взорвать C4 после 35 секунд таймера или ликвидировать всех бойцов спецназа).) и условие проигрыша (Проиграть 3 раунда сопернику: детонация C4 при игре за спецназ, успешный дефьюз бомбы спецназом при игре за террористов, либо полная гибель вашей команды в раунде.) работают.
- [ ] Тач-управление отвечает без задержки, страница под игрой не скроллится.
- [ ] Интерфейс собран из токенов одного `src/ui/theme.css`: в экранах нет литералов цвета, шрифта и `z-index`.
- [ ] Слои интерфейса прозрачны для игрового ввода — управление работает под всеми оверлеями, слой HUD не кликается.
- [ ] У каждого экрана работают состояния загрузки, пустоты и ошибки; возможность, которой нет на площадке, не нарисована вовсе.
- [ ] Меню и HUD помещаются в измеренный вьюпорт с учётом safe-area и баннера, а с закрытым игровым полем меню узнаётся как «Dust 2: Ретейк и Дуэли».
- [ ] За меню и экраном итога видна живая игровая сцена: полноэкранной непрозрачной заливки поверх канваса нет, подложка только под текстом и кнопками.
- [ ] Ни одного эмодзи в подписях интерфейса: иконки — инлайновый SVG с `currentColor`.
- [ ] Rewarded-реклама выдаёт награду ровно один раз (Бесплатный оружейный кейс, Удвоение ранговых очков ELO); interstitial соблюдает паузу 90 с.
- [ ] Audio and physics automatically pause/resume on tab blur and ad display.
- [ ] Persistent progress saves and loads from Playgama Cloud Storage.
- [ ] Runs at steady 60 FPS on desktop and >= 50 FPS on mobile.

---

## 11. ГДЕ ЛЕЖИТ ОСТАЛЬНОЕ

База знаний не лежит в пакете копиями — она приезжает по требованию:

```bash
node scripts/fetch-knowledge.mjs        # всё обязательное → docs/ref/
node scripts/fetch-knowledge.mjs <путь> # отдельный файл
```

Что именно тянуть, перечислено в `knowledge.manifest.json`. После загрузки адреса ниже открываются как обычные локальные файлы, и сеть больше не нужна.

Набор отобран под этот проект: Проект опирается на физический FPS-стек (Three.js + Rapier3D + three-mesh-bvh) с процедурной генерацией карты Dust 2, оружия и бойцов, синтезом звука на Web Audio и оптимизацией под мобильный веб; готовый паттерн раундового ретейк-шутера в patterns отсутствует, поэтому loop_pattern оставлен пустым.

**Ядро — прочитать до начала реализации:**
- `docs/ref/knowledge/threejs/fps_controller_and_shooting.md` (выжимка и чек-лист — `skills/skills/fps_combat.md`) — Управление от первого лица, расчет контр-стрейфа, математика отдачи и спрей-паттерна оружия, а также покачивание вьюмодели.
- `docs/ref/knowledge/threejs/shooter_enemy_ai_and_combat.md` (выжимка и чек-лист — `skills/skills/bot_ai.md`) — Тактический ИИ ботов для формата 3v3 ретейка, секторы обзора, хитбоксы и регистрация урона при попаданиях.
- `docs/ref/knowledge/threejs/physics_integration.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Синхронизация физического мира Rapier3D с графикой Three.js для точного движения бойцов и бросков гранат.
- `docs/ref/knowledge/threejs/game_map_and_world_design.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Создание соревновательной геометрии плентов карты Dust 2 с ящиками, укрытиями и зонами закладки бомбы.
- `docs/ref/knowledge/threejs/procedural_mesh_builder.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Процедурная сборка 3D-моделей оружия, бомбы C4 и окружения без загрузки внешних ассетов.
- `docs/ref/knowledge/threejs/procedural_character_rig.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Создание процедурных низкополигональных моделей спецназа и террористов с ригом удержания оружия.
- `docs/ref/knowledge/stack/rapier3d.md` (выжимка и чек-лист — `skills/STACK_SKILL.md`) — Физический движок WASM для коллизий персонажей, трения при остановке и физики полета гранат.
- `docs/ref/knowledge/stack/three_mesh_bvh.md` (выжимка и чек-лист — `skills/STACK_SKILL.md`) — Ускорение рейкастов для мгновенной регистрации попаданий (hitscan) и расчета баллистического прострела стен (wallbang).
- `docs/ref/knowledge/audio/procedural_sound_synthesizer.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Процедурный синтез звуков выстрелов, шагов, пиканья таймера бомбы C4 и звука начала обезвреживания через Web Audio.

**Вспомогательные:**
- `docs/ref/knowledge/threejs/adaptive_quality.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Динамическая подстройка разрешения и дальности прорисовки для удержания стабильных 60 FPS на мобильных устройствах.
- `docs/ref/knowledge/threejs/performance_guide.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Оптимизация количества вызовов отрисовки (Draw Calls) и инстансинг повторяющихся элементов арены.
- `docs/ref/knowledge/threejs/mobile_shaders.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Легковесные шейдеры материалов окружения Dust 2 для исключения перегрева мобильных устройств.
- `docs/ref/knowledge/threejs/juice_and_vfx_pool.md` (выжимка и чек-лист — `skills/RENDERER_SKILL.md`) — Инстансированные частицы для создания дымовой завесы смока, вспышек выстрелов и шейка камеры при взрыве C4.
- `docs/ref/knowledge/mechanics/cover_and_suppression.md` (выжимка и чек-лист — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Тактическая оценка укрытий ботами при штурме плента и поведение под подавляющим огнем.
- `docs/ref/knowledge/mechanics/ragdoll.md` (выжимка и чек-лист — `skills/MELEE_SKILL.md`) — Физический рэгдолл при гибели бойцов от хэдшотов и детонации бомбы для сочного отклика на попадания.
- `docs/ref/knowledge/audio/web_audio_and_muting.md` (выжимка и чек-лист — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Корректная инициализация AudioContext по первому пользовательскому действию и автоглушение звука при сворачивании вкладки.
- `docs/ref/knowledge/monetization/interstitial_best_practices.md` (выжимка и чек-лист — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Показ полноэкранной рекламы строго между раундами и матчами без вмешательства в активный игровой процесс.
- `docs/ref/knowledge/monetization/rewarded_ads_patterns.md` (выжимка и чек-лист — `skills/PROJECT_KNOWLEDGE_SKILL.md`) — Награждаемая реклама за просмотр для разблокировки скинов оружия или получения бонусов после матча.

**Платформенные требования — обязательны всегда, независимо от жанра.** Без Playgama Bridge игра на площадке не стартует: платформа держит свою заставку до таймаута и снимает игру с модерации.
- `docs/ref/knowledge/playgama/game_ready_and_loading.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/auth_and_player.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/storage_and_cloud.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/ads_integration.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/playgama/banners_and_layout.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/compliance/yandex_moderation.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/localization_system.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/touch_controls.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_design_system.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)
- `docs/ref/knowledge/ux/ui_implementation.md` (выжимка и чек-лист — `skills/PLAYGAMA_SKILL.md`)

**Осознанно НЕ включены**: `threejs/horde_survivor_core.md`, `patterns/survivor_loop.md`, `patterns/arena_combat_loop.md`, `patterns/fighting_match_loop.md`, `mechanics/wave_survival.md`, `mechanics/upgrade_choices.md`, `stack/bitecs.md`, `threejs/melee_combat_and_ragdoll.md`, `threejs/fighting_game_core.md`, `mechanics/drone_swarm.md`, `threejs/stealth_and_vision_cones.md`, `threejs/skinned_character_models.md`, `monetization/in_app_purchases.md`. Проект является соревновательным тактическим FPS 3v3 с раундами на карте Dust 2 без волн врагов, без рогаликовой прокачки, без рукопашного боя и без внешних тяжелых FBX-моделей; ECS bitECS избыточен для 6 сущностей, а внутриигровые покупки не соответствуют веб-модели монетизации.
Не подтягивай решения из этих документов — они про другую игру.

**Документы проекта** (лежат рядом, в корне игры): `ACCEPTANCE.md` — приёмка, `AGENTS.md` — правила работы, `PROJECT_DIRECTION.md` — рамка проекта, `GAME_DESIGN_DOCUMENT.md`, `MECHANICS.md`, `CORE_LOOP.md`, `UI_UX_SPECIFICATION.md`, `ART_DIRECTION.md`, `MOBILE_CONTROLS.md`, `PLAYGAMA_INTEGRATION.md`, `MONETIZATION.md`, `QA_PLAN.md`, `DEVLOG.md` и `CHANGELOG.md` — их ведёшь ты.