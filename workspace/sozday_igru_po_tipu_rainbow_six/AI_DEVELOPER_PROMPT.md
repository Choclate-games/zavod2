# FINAL AI DEVELOPER PROMPT: ТАКТИКА ПРОРЫВА: CQB ШТУРМ 🎮⚡

> **INSTRUCTION FOR AI CODING AGENT**:
> You are the **Lead Game Developer & Systems Architect**. Your task is to build and deliver the complete, production-ready, fully playable HTML5/WebGL game described in this specification from start to finish.
> Follow the technical architecture, physics specifications, Playgama Bridge integration, and mobile ergonomics strictly.
> Do NOT omit systems, use fake placeholder stubs, or leave TODOs. The end result must satisfy every single item in the **Definition of Done**.

---

## 1. PROJECT IDENTITY & GOAL
- **Game Title**: Тактика Прорыва: CQB Штурм
- **Project Slug**: `sozday_igru_po_tipu_rainbow_six`
- **Genre**: Тактический 3D CQB-Шутер (Тактический симулятор штурма / Бричинг и зачистка)
- **Target Platform**: Playgama Bridge (Yandex Games / VK / Web)
- **Orientation**: Landscape
- **Target Audience**: Игроки 16–35 лет, увлеченные тактическими шутерами (Rainbow Six, Ready or Not), ценящие атмосферу спецназа, планирование штурма, летальный CQB и зрелищное разрушение укрытий без утомительного виртуального бега.
- **Player Fantasy**: Вы — элитный оперативник штурмовой группы спецназа 'Радуга'. За вашими плечами титановый баллистический щит и бричинг-заряды. Вы диктуете геометрию боя, пробивая стены там, где враг уверен в своей безопасности, и ликвидируете террористов за доли секунды без права на промах.
- **Core Hook**: Мгновенное превращение глухой стены в тактический пролом: взрыв разносит гипсокартон в щепки, время замирает, и за 2 секунды кинематографичного slow-mo вы решаете исход операции одним точным хедшотом.
- **Session Model**: Штурм захваченного объекта из 3 последовательных комнат (Периметр -> Офисный холл -> Серверное хранилище) длительностью 90–120 секунд с нарастающей сложностью укреплений и вооружения врагов.


---

## 1b. РАМКА ПРОЕКТА: ЧТО ЭТО ЗА ИГРА И ЧЕМ ОНА НЕ ЯВЛЯЕТСЯ


**Направление**: Тактика Прорыва: CQB Штурм

**Почему выбрано именно оно**: Выбрано направление D1 («Тактика Прорыва: CQB Штурм»), так как оно напрямую воплощает главное эмоциональное ядро Rainbow Six — напряжение тактического взлома укреплений (breaching) и взрывную зачистку помещения за доли секунды. На смартфонах и в браузере D1 избавляет от громоздкого управления одновременным бегом, прицеливанием и наклонами (lean) на экранном джойстике, концентрируя геймплей на четких тактических действиях: выбор точки бричинга, детонация и прицельный огонь в slow-mo. Это обеспечивает понятный и эффектный старт с первой секунды без многоминутного обучения и стабильную производительность Three.js в рамках одной детально проработанной комнаты.

**Узнаваемая сцена**: Камера от первого лица из-за баллистического щита: в стене взрывом выбит неровный пролом, сквозь летящие во все стороны куски штукатурки и клубы дыма пробиваются лучи тактических фонарей, а впереди в замедленном времени ослепленный боевик роняет автомат на фоне развороченного офисного стола.

**Без чего проект перестаёт быть собой** (реализовать обязательно):
- Разрушаемость поверхностей: обязательный физический пролом стен/дверей при детонации пробивного заряда с разлетом осколков
- Летальный CQB: мгновенная цена ошибки, отсутствие полосок здоровья у врагов, нейтрализация с одного точного попадания
- Фазовость штурма: четкое разделение на выбор точки проникновения (заряд/светошумовая) и фазу зачистки в замедленном времени
- Тактический звук: глухой направленный бас подрыва, звон в ушах после светошумовой гранаты и щелчки затвора на чистом Web Audio

**ЗАПРЕЩЕНО ВВОДИТЬ** — даже если спецификация где-то умалчивает и даже если «так принято в жанре»:
- Никаких бесконечных волн наступающих врагов на открытой арене — только статично укрепившиеся противники в закрытых комнатах
- Никаких случайных карточек апгрейдов (+5% к урону) после зачистки — выбор снаряжения только перед рейдом
- Никакого свободного бега с виртуальным джойстиком и прыжками — перемещение привязано к тактическим позициям и точкам штурма
- Никаких шкал здоровья врагов-губок — летальный урон с одного точного попадания в голову или двух в корпус
- Никаких монохромных серых коробок вместо интерьера — стилизованное окружение с разрушаемым гипсокартоном, баррикадами и световыми эффектами
- волны врагов на арене
- окно выбора из трёх карт апгрейда
- универсальная связка «дэш + парирование + удар»
- заимствование Vampire Survivors / Brotato как основы
- джойстик слева + кнопки атаки справа как раскладка по умолчанию
- серые кубы вместо стилизованной геометрии
- мета-прогрессия «золото за забег -> постоянные апгрейды» без связи с темой

Если по ходу реализации кажется, что без запрещённого элемента игра не работает — это находка для `DEVLOG.md`, а не разрешение его добавить.

**Рассмотренные и отклонённые направления** (не возвращай их через механики):
- D2 («Спецплан: Синхронный Штурм») отвергнут, поскольку изометрическое планирование траекторий превращает адреналиновый тактический штурм в сухую головоломку типа Door Kickers, теряя ощущение личного присутствия на острие атаки и перегружая мобильный экран мелкими путевыми точками.
- D3 («Дрон-Разведчик: Точка Входа») отвергнут, так как исключает стрелковый экшен и кульминацию штурма, сводя формулу Rainbow Six лишь к вспомогательной фазе разведки на радиоуправляемой машинке без финальной развязки.

---

## 2. TECHNOLOGY STACK & RENDERING ENGINE
- **Language**: TypeScript (strict mode)
- **Bundler & Dev Server**: Vite
- **Renderer**: **THREEJS** (^0.185.1)
  - *Selection Rationale*: Three.js + Rapier3D: пространственный геймплей, физика и освещение. Перспективная камера.
- **Physics Simulation**: **Rapier3D (@dimforge/rapier3d-compat ^0.20.0)** (Fixed 60Hz timestep with accumulator)
- **Audio Engine**: Web Audio API (Howler.js with html5: false)
- **State Management**: Reactive EventBus / State Store
- **Platform SDK**: `@playgama/bridge 2.x`

### Performance Budgets
- **Target FPS**: 60 FPS (Desktop & Mobile)
- **Max Draw Calls**: < 65
- **Max Triangles / Active Sprites**: < 40000
- **Max Bundle Size**: < 4.0 MB (Gzipped + assets)

---

## 3. CORE GAMEPLAY LOOP & MECHANICS
**Core Loop Sequence**:
```text
Тактическая разведка эндоскопом -> Выбор слабой зоны и установка C4 -> Детонация с физическим проломом стены -> Вход в штурмовой Slow-Mo -> Ликвидация угроз точными выстрелами из-за щита -> Обезвреживание бомбы / переход к следующей комнате -> Получение тактических кредитов и улучшение снаряжения.
```

**Фирменный момент**: Подведение оптоволоконного эндоскопа под щель армированной двери серверного хранилища: на экране сканера видны силуэты 3 наемников за стойками и лазерная растяжка у порога. Игрок решает не идти через заминированную дверь, а смещается к прилегающей стене, лепит термо-бричинг заряд C4. Клик детонатора — глухой кинематографичный 'БАМ', трехмерные обломки бетона и арматуры на физике Rapier3D разлетаются внутрь комнаты, сбивая с ног крайнего боевика. Время мгновенно сжимается в 0.2x Slow-Mo: звук переходит в низкочастотный басовый гул и звон контузии в ушах, в воздухе зависают клубы штукатурки и искр. Игрок за 1.8 секунды замедления делает резкий наклон из-за титанового щита (Lean Right), ставит два выверенных хедшота из пистолета-пулемета MP5 по дезориентированным целям, в падении сбивает третью выстрелом по ногам под столом, и время с сочным щелчком затвора возвращается в 1.0x прямо в момент падения гильз на пол.

**Чем петля отличается от соседей по жанру**: В отличие от классических аркадных рельсовых шутеров (Time Crisis, Virtua Cop) или хаотичных арена-батлеров, здесь игрок управляет не просто стрельбой из укрытия, а полным циклом тактического штурма в замкнутом микро-пространстве: 1) Фаза сбора разведданных через эндоскоп с маркировкой угроз и ловушек; 2) Тактический выбор точки взлома (дверь, гипсокартон или армированная перегородка); 3) Физический импакт бричинга (разрушение геометрии через физику твердых тел Rapier3D и поражение осколками); 4) Менеджмент ограниченного ресурса кинематографичного Slow-Mo и углов наклона баллистического щита (Lean Q/E) без права на лишнее движение.

**Шаблон жанра, который НЕ реализуем**: Отвергаем шаблон 'Традиционного коридорного шутера со свободным блужданием, сбором ключ-карт и врагами-губками для пуль (Bullet Sponges)'. В браузерных и мобильных сессиях (90–120 секунд) 5 минут беготни по пустым лабиринтам убивают концентрацию. Вместо поиска ключей игрок сам создает проходы взрывчаткой, а вместо врагов с полосками здоровья по 1000 HP здесь работает хардкорная CQB-летальность: 1 выстрел в голову или 2 в грудь гарантированно нейтрализуют цель.

**Схема петли**:
```text
+-----------------------------------------------------------------------------+
|                         ТАКТИКА ПРОРЫВА: CQB ШТУРМ                          |
+-----------------------------------------------------------------------------+
                                      |
                                      v
                        [ 1. РАЗВЕДКА ЭНДОСКОПОМ ]
                        - Сканирование щели двери / трещины стены
                        - Маркировка позиций врагов и ловушек
                                      |
                                      +-------------------------+
                                      |                         |
                                      v (Стена)                 v (Дверь)
                        [ 2А. МОНТАЖ C4 НА СТЕНУ ]   [ 2Б. ТАКТИЧЕСКИЙ ВЗЛОМ ]
                        - Выбор слабой зоны           - Срез замка / выбивание
                                      \                         /
                                       \                       /
                                        v                     v
                                  [ 3. ДЕТОНАЦИЯ И БРИЧИНГ ]
                                  - Физический разлом преграды (Rapier3D)
                                  - Оглушение и контузия врагов в радиусе
                                                |
                                                v
                                  [ 4. ШТУРМОВОЙ SLOW-MO (0.2x) ]
                                  - Замедление времени на 1.5 - 3.0 сек
                                  - Трассеры, взвесь бетона, шоковый гул
                                                |
                                                v
                                  [ 5. РАСПРЕДЕЛЕНИЕ ОГНЯ ИЗ-ЗА ЩИТА ]
                                  - Тактические наклоны (Lean Q/E)
                                  - Прицельные хедшоты по уязвимым целям
                                  - Уничтожение капсюлей лазерных мин
                                                |
                                                v
                                  [ 6. ОБЕЗВРЕЖИВАНИЕ СВУ И КОНТРОЛЬ ]
                                  - Перерезание проводов детонатора
                                  - Сбор тактических данных с терминала
                                                |
                                                +--------------+
                                                |              |
                                 (Остались комнаты)     (Объект зачищен)
                                                |              |
                                                v              v
                                  [ ПЕРЕХОД К СЛЕД. ]   [ ДЕБРИФИНГ И АПГРЕЙДЫ ]
                                  - Пополнение зарядов  - Покупка снаряжения в Armory
                                  - Рост сложности      - Новые типы взрывчатки и щитов
```

**Микро-петля (посекундно)**:
- **Оценка сектора через смотровое окно щита** (0.8 с): игрок — Удерживает стойку за ростовым щитом, наводит прицел на дверной проем / щель в стене.; игра — Отображение маркеров траекторий пуль противника (искры рикошетов от титановой пластины щита, падение стойкости щита на 12 ед.).; решение — Принять огонь на щит или выполнить резкий наклон вправо (Lean E) для контрудара.
- **Выбор окна для наклона (Tactical Lean)** (0.4 с): игрок — Нажатие кнопки 'E' (или свайп вправо на тачскрине) для полушага из-за щита.; игра — Камера сдвигается на 0.45 м вправо под углом 14 градусов, ствол пистолета выносится в открытую линию огня, открывая плечо оперативника.; решение — Стрелять навскидку по ближайшей угрозе или выцелить голову командира за укрытием.
- **Тактический выстрел в уязвимую зону** (0.3 с): игрок — Клик ЛКМ / тап по зоне головы террориста.; игра — Выстрел 9x19mm с отдачей 4.2 градуса, трассер пробивает деревянную перегородку, рэгдолл врага отбрасывается назад импульсом 380 Н*с.; решение — Добить второго противника одиночным или уйти обратно в глухую защиту щитом на перезарядку.
- **Возврат в укрытие и парирование огня** (1.2 с): игрок — Отпускание клавиши наклона, фронтальная перегруппировка.; игра — Щит снова закрывает центр экрана, звук глухих попаданий пуль 7.62mm в бронесталь, легкий блюр от вибрации.; решение — Начать перезарядку магазина (1.4 с) или метнуть светошумовую гранату вслепую через верхний край щита.
- **Контроль сектора и зачистка** (1.5 с): игрок — Быстрый досыл патрона, проверка углов комнаты.; игра — Снятие статуса угрозы в секторе, аудио-подтверждение 'Sector Clear' по рации, начисление комбо-очков за скорость.; решение — Перезарядить оружие перед следующим проломом или сразу подойти к следующей точке входа.

**Мезо-петля (этап)**:
- **Разведка и планирование точки входа** (4.0 с): игрок — Активация гибкого эндоскопа под дверью или сканирование стены тепловизором.; игра — Рендер силуэтов террористов (красные контуры), обнаружение растяжки с C4 у двери и слабой гипсокартонной секции слева.; решение — Штурмовать заминированную дверь через обезвреживание или пробить слабую гипсокартонную стену зарядом бричинга.
- **Установка бричинг-заряда и позиционирование** (3.0 с): игрок — Размещение накладного заряда C4 на выбранную стену, отход на безопасную дистанцию (2.5 м) и поднятие щита.; игра — Светодиод на детонаторе мигает зеленым, звучит предупреждающий писк таймера 0.5 с, маркер готовности к подрыву.; решение — Взорвать мгновенно или дождаться, пока патрульный террорист подойдет вплотную к стене для поражения осколками.
- **Детонация, физический пролом и активация Slow-Mo** (2.0 с): игрок — Нажатие кнопки детонатора на кликере.; игра — Физический взрыв разрушает меш стены на 32 обломка (Rapier3D rigidbodies), ударная волна глушит врагов на 2.5 с, таймскейл падает до 0.2x.; решение — Приоритет целей в Slow-Mo: сначала ликвидировать террориста с гранатой, затем стрелка за пулеметом, затем сбить лазерную мину.
- **Динамическая зачистка в замедленном темпе** (2.5 с): игрок — Серия прицельных выстрелов по уязвимым зонам противников во время полета обломков.; игра — Кинематографичные хедшоты, физика разрушения столов и мониторов от шальных пуль, счетчик комбо Breach & Clear x3.; решение — Израсходовать остаток Slow-Mo на обезвреживание растяжки выстрелом в капсюль или на добивание бронированного наемника.
- **Обезвреживание бомбы / Фиксация рубежа** (5.0 с): игрок — Подход к терминалу взрывного устройства, срезание контрольных проводов по цветовой схеме.; игра — Звуковой сигнал разрядки таймера, открытие бронедвери в следующий штурмовой сектор, пополнение 1 бричинг-заряда.; решение — Использовать аптечку (+50 HP) сейчас или сохранить бронепластину щита на финальное серверное хранилище.

**Макро-петля (забег)**:
- **Выбор тактического контракта и брифинг операции** (8.0 с): игрок — Выбор объекта на глобальной тактической карте (Банковское хранилище, Посольство, Серверный узел синдиката).; игра — Отображение плана этажей, модификаторов сложности (тяжелая броня у врагов, лазерные сенсоры, ограниченное время).; решение — Взять дробовик M870 для мгновенного разлома дверей или бесшумный MP5-SD с увеличенным запасом Slow-Mo.
- **Трехфазный CQB-штурм объекта (3 комнаты)** (90.0 с): игрок — Последовательное прохождение Периметра, Офисного холла и Серверного хранилища с зачисткой всех угроз.; игра — Динамическая симуляция комнат, нарастающее сопротивление ИИ, проверка прочности щита и таймера детонатора.; решение — Рисковать ради идеального счета (все хедшоты, 0 урона щиту) или играть надежно от глухой обороны.
- **Дебрифинг, оценка эффективности и выплата тактических кредитов** (12.0 с): игрок — Просмотр итоговой статистики: точность попаданий, время зачистки, процент сохранения щита, заработанные кредиты.; игра — Начисление кредитов спецназа (Credits), опыта оперативника (XP) и тактических жетонов за выполнение доп. условий.; решение — Потратить кредиты на покупку термо-бричинг ленты или инвестировать в титановое напыление баллистического стекла.
- **Оружейная и кастомизация снаряжения (Armory & Loadout)** (15.0 с): игрок — Установка коллиматорного прицела Holo-1, лазерного ЛЦУ, покупка усиленного щита Вулкан-М с ослепляющей LED-вспышкой.; игра — Обновление статов оперативника: увеличение угла защиты щита на +10 градусов, продление базового Slow-Mo на +0.4 с.; решение — Начать штурм объекта повышенной сложности (Хардкор / Ночной рейд) или перепройти миссию на ранг S.

**Кривая напряжения**: 0–10 с (Тишина и напряженное планирование): Полный штиль. Слышно только тяжелое дыхание оперативника и жужжание сервопривода эндоскопа. Напряжение копится из-за неизвестности за дверью. 10–13 с (Взрывной пик и кинематографичный штурм Комнаты 1): Детонация C4, резкий звуковой удар, выброс адреналина, 3 секунды идеального тайминга в Slow-Mo. Эйфория от первого чистого хедшота. 13–20 с (Спад и переходная зачистка): Короткая передышка, звук падающих гильз, оценка повреждений щита, перезарядка. 20–45 с (Офисный холл — среднее напряжение и скрытая угроза): Боевики за укрытиями уже настороже, натянут трипвайр растяжки. Напряжение возрастает: ошибка при наклоне стоит половины здоровья щита. Бричинг через окно/стену, 2.0 секунды Slow-Mo, ликвидация 3 целей в перекрестном огне. 45–55 с (Тактическая пауза и менеджмент ресурсов): Обезвреживание мины, выбор между сохранением гранаты или ремонтом смотрового стекла. 55–90 с (Кульминация — Серверное хранилище с бомбой): Максимальное давление. Тикающий таймер бомбы (25 секунд), бронированные наемники с дробовиками, армированная стена требует 4 секунды прожига термолентой. 1.5 секунды Slow-Mo, мгновенный хардкорный размен выстрелами, блокирование шквального огня щитом на последних единицах прочности, обезвреживание СВУ за 2 секунды до детонации. 90–100 с (Катарсис и награда): Снятие напряжения, победный рапорт командования, водопад тактических кредитов и разблокировка новой экипировки.

**Формулы ядра (реализуй буквально)**:
- `Damage_Taken = Raw_Incoming_Damage * (1.0 - Shield_Block_Factor) * Armor_Piercing_Mod, где при попадании в титановый щит Shield_Block_Factor = 1.0 (полный блок), при попадании в смотровое окно Shield_Block_Factor = 0.85 с расходом прочности стекла Glass_HP = Glass_HP - (Bullet_Caliber * 12.5), а при открытии плеча во время наклона Shield_Block_Factor = 0.0.`
- `SlowMo_Duration_Seconds = Base_SlowMo * (1.0 + Adrenaline_Perk_Level * 0.15) * Breach_Quality_Multiplier, где Base_SlowMo составляет 3.0 с (Комната 1), 2.0 с (Комната 2), 1.5 с (Комната 3), а Breach_Quality_Multiplier = 1.25 при установке заряда точно в центр слабого узла стены.`
- `Breach_Destruction_Impulse = Explosive_Mass_Kg * 450.0 / (Distance_To_Debris_M + 0.5)^1.8, передающий линейный импульс (Н*с) и угловую скорость каждому из 24-36 полигональных осколков стены в физическом движке Rapier3D.`
- `Enemy_Stun_Duration_Seconds = clamp(Base_Stun * (Explosive_Power / Distance_To_Breach_M), 0.5, 4.0) * (1.0 - Enemy_Armor_Resist), определяющий время дезориентации террориста (выпадение оружия из рук, анимация контузии).`
- `Tactical_Score_Points = (Headshot_Count * 500 + Breach_Kills * 350 + Speed_Bonus_Seconds * 50 + Shield_Integrity_Percent * 10) * Accuracy_Multiplier * Difficulty_Mod, формирующий финальный рейтинг операции (ранги D, C, B, A, S).`

**Прогрессия внутри забега**:
- Комната 1 -> Комната 2 (Тактический адреналин): успешная зачистка первой комнаты без повреждений дает оперативнику статус 'В Фокусе' (+15% к скорости перезарядки и подсветка капсюлей вражеских мин через гипсокартон).
- Комната 2 -> Комната 3 (Штурмовой арсенал): зачистка офисного холла позволяет подобрать с оружейной стойки синдиката подкалиберные патроны 9x19mm AP (+40% пробития брони наемников) или термитную шашку для мгновенного прожига армированной стены.
- Экстренное усиление щита: замена треснувшего триплекс-стекла смотрового окна на запасную бронепанель перед финальным штурмом серверной (+100 ед. прочности баллистического экрана).
- Серийный комбо-множитель штурма: каждый непрерывный хедшот в фазе Slow-Mo увеличивает окно замедления времени на +0.3 с для следующей комнаты объекта.

**Мета-прогрессия**:
- Взрывной арсенал инженерного корпуса: Открытие тяжелых зарядов C4 (увеличенный радиус осколков), термических лент Thermite-X (бесшумный прожиг армированной стали) и направленных зарядов 'Кумулятивный молот' (пробивают сразу две смежные комнаты).
- Модификации штурмового щита: Апгрейд базового щита спецназа до щита 'Вулкан-М' со встроенным стробоскопическим фонарем (ослепляет врагов на 2.0 с при входе) и титановым карнизом с отражением рикошетов.
- Штурмовое стрелковое вооружение: Разблокировка компактного дробовика Breaching-12 (мгновенный отстрел дверных петель), тактического пистолета-пулемета MP5-SD с глушителем и крупнокалиберного револьвера Rhino .357 (пробивает укрытия навылет).
- Тактические перки оперативника: Пассивные навыки спецподразделения 'Радуга' — ускорение наклонов (Lean Speed +35%), снижение отдачи при стрельбе с одной руки на 25%, замедление скорости расхода таймера бомбы на 20%.
- Оружейные скины и камуфляжи спецназа: Камуфляжи 'Городской штурм', 'Черный монолит', 'Ночной рейд', открываемые за ранги 'S' на всех операциях.

### Взрывной Бричинг Укреплений (Breach Dynamics) (CORE)
- **Category**: combat
- **Description**: Установка направленного бричинг-заряда C4 или термической ленты на слабую зону стены/двери с последующим дистанционным подрывом, физическим разломом преграды и оглушением противников за ней.
- **Player Input**: ПКМ / Тап по подсвеченной зоне стены — прикрепить заряд C4; Пробел / Кнопка кликера детонатора — дистанционный подрыв; Свайп двумя пальцами вниз — отмена и снятие заряда.
- **Hit & Sensory Feedback**: Визуал/VFX: Вспышка взрыва с объемным дымовым грибом, разлет 32 трехмерных фрагментов штукатурки и арматуры с расчетом коллизий Rapier3D, оседающая белая взвесь строительной пыли.; Звук (Web Audio): Низкочастотный саб-басовый взрыв (45 Гц sine-drop) с последующим высокочастотным свистом в ушах (Tinnitus notch-filter на 4.2 кГц) и грохотом падающих обломков.; Камера: Травматический импульсный шейк (амплитуда 0.35 м, затухание 0.4 с), динамический зум FOV с 75 градусов до 62 градусов в момент вспышки и плавный возврат.; UI/Тактильность: Короткая двойная вибрация (Haptic feedback 80 мс / 120 мс) на мобильных устройствах, красный индикатор радиуса ударной волны на полу, маркер BREACH SUCCESS.
- **Technical Complexity**: Medium
- **Решение игрока**: Куда и какой заряд установить: пробить деревянную дверь по центру или наложить C4 на слабую гипсокартонную стену сбоку, чтобы зайти во фланг террористам и поразить их разлетающимися обломками.
- **Состояния**: `READY_TO_DEPLOY (оперативник держит заряд, зоны стен подсвечены желтым/зеленым)`, `ATTACHED_ARMED (заряд на стене, диод мигает зеленым с частотой 2 Гц, активна кнопка подрыва)`, `DETONATION_IMPACT (триггер подрыва, спавн взрывной волны, фрагментация полигональной сетки на 32 твердых тела Rapier3D)`, `BREACH_SMOKE_CLEARING (рассеивание пылевого облака в течение 3.5 с, образование постоянного проема)`
- **Числовые параметры (реализуй именно эти значения)**:
  - `charge_placement_time` = `0.65 с` — Если уменьшить до 0.2 с — теряется вес и уязвимость установки; если увеличить до 1.5 с — ломается динамика мобильной сессии.
  - `blast_radius_lethal` = `2.2 м` — Если увеличить до 4.0 м — взрыв убивает всех врагов за стеной без участия игрока; если уменьшить до 1.0 м — осколки не задевают прилегающие укрытия.
  - `wall_fragment_count` = `32 шт` — Если сделать 8 шт — разрушение выглядит дешево и плоско; если сделать больше 60 шт — просаживается FPS в браузере Three.js на мобильных устройствах.
  - `safe_standoff_distance` = `2.5 м` — Если игрок стоит ближе 2.5 м к стене без щита — получает 45 ед. урона и контузию на 1.8 с; если дистанция 0 м — исчезает необходимость тактического отхода.
- **Слои отклика**:
  - Визуал/VFX: Вспышка взрыва с объемным дымовым грибом, разлет 32 трехмерных фрагментов штукатурки и арматуры с расчетом коллизий Rapier3D, оседающая белая взвесь строительной пыли.
  - Звук (Web Audio): Низкочастотный саб-басовый взрыв (45 Гц sine-drop) с последующим высокочастотным свистом в ушах (Tinnitus notch-filter на 4.2 кГц) и грохотом падающих обломков.
  - Камера: Травматический импульсный шейк (амплитуда 0.35 м, затухание 0.4 с), динамический зум FOV с 75 градусов до 62 градусов в момент вспышки и плавный возврат.
  - UI/Тактильность: Короткая двойная вибрация (Haptic feedback 80 мс / 120 мс) на мобильных устройствах, красный индикатор радиуса ударной волны на полу, маркер BREACH SUCCESS.
- **Режим отказа игрока**: Игрок подрывает стену, находясь слишком близко без поднятого щита (получает контузию и потерю 45 HP), либо устанавливает C4 на армированную сталь без термоленты (заряд детонирует вхолостую). Сигнал: красный крест на детонаторе и металлический звон.
- **Кривая мастерства**: Новичок на 30-й секунде взрывает только центральные двери и попадает под встречный огонь. Эксперт на 10-м забеге использует накладной заряд для кинетического рикошета обломков стены, убивая зазевавшегося наемника физикой летящего куска бетона без единого выстрела.
- **Сопротивление игры**: Враги укрепляют стены титановыми кевларовыми щитами (требуют термического прожига 4.0 с) и устанавливают сейсмические датчики движения, открывая огонь насквозь через стену при шуме установки C4.
- **Синергии**:
  - Штурмовой Slow-Mo Фокус: детонация стены автоматически триггерит окно замедления времени 0.2x.
  - Эндоскопическая Разведка: эндоскоп подсвечивает точные стыки арматуры для максимального радиуса разлета обломков.
  - Баллистический Щит: щит полностью поглощает фронтальную взрывную волну при близком подрыве.
- **Почему это не жанровый шаблон**: В отличие от скриптовых взрывов в обычных шутерах, здесь разрушение является честным физическим процессом на Rapier3D: выбитая дверь превращается в тяжелый физический снаряд, сбивающий врагов с ног.
- **Псевдокод тика**:
```text
function onBreachDetonate(charge: C4Charge, wall: DestructibleWall): void {
  if (wall.isReinforced && !charge.hasThermite) { playDudSparks(charge.pos); return; }
  const fragments = RapierPhysics.fractureMesh(wall.mesh, 32);
  fragments.forEach(f => f.applyImpulse(calcBlastVector(charge.pos, f.pos, 450)));
  const enemiesInRadius = SpatialGrid.queryRadius(charge.pos, 3.5);
  enemiesInRadius.forEach(e => e.applyStun(calcStunDuration(e.distance, 2.5)));
  TimeDilationSystem.triggerSlowMo(2.0, 0.2);
  AudioEngine.playSpatialSubBoom(charge.pos, 1.0);
}
```

### Штурмовой Slow-Mo Фокус (Breach-Focus Time) (CORE)
- **Category**: combat
- **Description**: В момент детонации время замедляется до 20% на 1.5–3.0 секунды. Ослепленные террористы медленно роняют оружие или пытаются вскинуть стволы, пока игрок методично распределяет прицельные выстрелы.
- **Player Input**: Активируется автоматически при детонации пролома; Повторный клик Shift / Тап по индикатору пульса — досрочный выход из Slow-Mo для сохранения энергии фокуса.
- **Hit & Sensory Feedback**: Визуал/VFX: Эффект радиального размытия по краям экрана (Radial Blur), яркие замедленные трассеры выстрелов с дымовым следом, зависшие в воздухе гильзы и крупицы штукатурки.; Звук (Web Audio): Приглушение всех высоких частот через Low-Pass Filter (срез 400 Гц), ритмичный тяжелый стук сердца (Heartbeat 55 BPM), выстрелы звучат как раскатистые басовые удары.; Камера: Плавный кинематографичный наезд камеры вперед (Dolly Zoom +0.3 м), динамическая стабилизация прицельной марки.; UI/Тактильность: Пульсирующая полоса адреналина в центре экрана, замедленное тиканье таймера штурма, легкая непрерывная микровибрация устройства.
- **Technical Complexity**: Medium
- **Решение игрока**: Как распределить 2.0 секунды замедления: сделать 3 быстрых хедшота по открытым целям или потратить время на поиск и отстрел капсюля лазерной мины-растяжки.
- **Состояния**: `REALTIME_ACTIVE (время течет 1.0x, прицел стандартного размера)`, `SLOWMO_ENTER (плавная интерполяция таймскейла от 1.0x до 0.2x за 0.12 с, включение хроматической аберрации)`, `SLOWMO_RUNNING (посекундный расход шкалы адреналина, подсветка трассеров пуль и уязвимых зон врагов)`, `SLOWMO_EXIT (щелчок затвора, возврат таймскейла в 1.0x за 0.18 с, восстановление звукового спектра)`
- **Числовые параметры (реализуй именно эти значения)**:
  - `time_dilation_scale` = `0.20x` — Если сделать 0.05x (Superhot) — бой превращается в пошаговую головоломку без адреналина; если сделать 0.5x — мобильные игроки не успевают навестись пальцем.
  - `base_focus_duration` = `2.0 с` — Если уменьшить до 0.8 с — игрок успевает сделать лишь 1 выстрел; если увеличить до 4.5 с — пропадает напряжение и цена промаха.
  - `headshot_time_refund` = `0.35 с` — Если убрать — нет стимула стрелять точно в голову; если сделать 1.0 с — опытный игрок находится в бесконечном Slow-Mo.
  - `player_turn_speed_multiplier` = `1.8x` — Враги замедлены в 5 раз, но прицел игрока сохраняет высокую чувствительность (1.8x) для удобной микро-доводки на тачскрине.
- **Слои отклика**:
  - Визуал/VFX: Эффект радиального размытия по краям экрана (Radial Blur), яркие замедленные трассеры выстрелов с дымовым следом, зависшие в воздухе гильзы и крупицы штукатурки.
  - Звук (Web Audio): Приглушение всех высоких частот через Low-Pass Filter (срез 400 Гц), ритмичный тяжелый стук сердца (Heartbeat 55 BPM), выстрелы звучат как раскатистые басовые удары.
  - Камера: Плавный кинематографичный наезд камеры вперед (Dolly Zoom +0.3 м), динамическая стабилизация прицельной марки.
  - UI/Тактильность: Пульсирующая полоса адреналина в центре экрана, замедленное тиканье таймера штурма, легкая непрерывная микровибрация устройства.
- **Режим отказа игрока**: Игрок паникует, делает хаотичные выстрелы в корпус, не убивает ключевых врагов за отведенные 2.0 секунды и оказывается в реальном времени под перекрестным огнем с пустым магазином. Сигнал: резкий срыв звукового фильтра в 1.0x, визг врагов и красный экран попаданий.
- **Кривая мастерства**: Новичок за время замедления убивает 1 врага и тратит весь магазин. Эксперт за 1.8 с делает 3 выверенных хедшота с компенсацией отдачи и выбивает мину, продлевая Slow-Mo бонусами точности.
- **Сопротивление игры**: Элитные наемники в Комнате 3 экипированы тактическими шлемами со стимуляторами адреналина — их время реакции замедляется лишь до 0.6x (они открывают ответный огонь прямо в Slow-Mo).
- **Синергии**:
  - Взрывной Бричинг: качественный подрыв в центр стены дает +25% к длительности Slow-Mo.
  - Баллистический Щит: возможность безопасно оценить траектории пуль противников во время замедления.
  - Эндоскопическая Разведка: заранее размеченные цели подсвечиваются яркими силуэтами в Slow-Mo.
- **Почему это не жанровый шаблон**: Slow-Mo здесь не бесконечное и не включается кнопкой когда угодно — это строго регламентированное тактическое окно кинематографичной зачистки, открываемое только успешным бричингом.
- **Псевдокод тика**:
```text
function updateSlowMoTick(dt: number, focusState: FocusData): void {
  if (!focusState.isActive) return;
  focusState.remainingTime -= dt;
  AudioEngine.setLowPassCutoff(THREE.MathUtils.lerp(400, 20000, 1.0 - focusState.remainingTime / 2.0));
  if (PlayerWeapon.onEnemyHit(HitZone.HEAD)) {
    focusState.remainingTime = Math.min(focusState.remainingTime + 0.35, 3.0);
    VFXEngine.spawnHeadshotSpark(PlayerWeapon.targetPos);
  }
  if (focusState.remainingTime <= 0) {
    TimeDilationSystem.setTimeScale(1.0);
    focusState.isActive = false;
  }
}
```

### Баллистический Щит и Тактические Наклоны (Tactical Shield & Lean) (CORE)
- **Category**: defense
- **Description**: Использование ростового бронещита со смотровым окном. Защищает от фронтального огня, позволяя выглядывать влево/вправо (Lean Q/E) для ведения беглого прицельного огня.
- **Player Input**: Клавиши Q / E (или кнопки-стрелки по краям экрана на мобильных) — наклон влево/вправо; Зажатие ПКМ — поднятие щита в глухую оборону; ЛКМ — выстрел из пистолета с упора.
- **Hit & Sensory Feedback**: Визуал/VFX: Искры и вмятины на бронеплите щита при попаданиях, нарастающая паутина трещин на баллистическом стекле, дым от пороховых газов при выстреле с упора.; Звук (Web Audio): Звонкий металлический лязг рикошетов пуль о сталь (Bell-metallic ping), глухой стук попаданий в титан, механический скрежет фиксаторов щита при наклоне.; Камера: Смещение оси камеры на 0.45 м вбок с креном 14 градусов, легкий толчок назад (0.05 м) при поглощении тяжелых пуль щитом.; UI/Тактильность: Индикатор целостности щита (Shield Durability bar) и смотрового окна, сильная прерывистая виброотдача при пулеметном обстреле щита.
- **Technical Complexity**: Medium
- **Решение игрока**: Оставаться в глухой фронтальной защите, принимая пули на бронесталь, или выглянуть вправо (Lean E) на 0.4 секунды для прицельного выстрела, подставив открытое плечо.
- **Состояния**: `FULL_GUARD (щит закрывает 80% экрана, скорость движения -40%, 100% защита от пуль спереди)`, `LEAN_PEEKING (камера и ствол вынесены в сторону на 0.45 м, открыта уязвимая зона руки/плеча)`, `GLASS_CRACKED (смотровое стекло получило критический урон, обзор затруднен паутиной трещин)`, `SHIELD_STAGGER (попадание тяжелой пули/гранаты выбивает щит в сторону на 0.65 с)`
- **Числовые параметры (реализуй именно эти значения)**:
  - `shield_hp_max` = `350 ед` — Если сделать 100 ед — щит разбивается за 2 выстрела и теряет смысл; если сделать 1000 ед — игрок становится бессмертным танком.
  - `lean_angle_deg` = `14 градусов` — Если уменьшить до 5 градусов — наклон не дает угла обзора за угол; если увеличить до 30 градусов — моделька неестественно падает вбок.
  - `lean_transition_time` = `0.18 с` — Если уменьшить до 0.05 с — нет ощущения массы титанового щита; если увеличить до 0.5 с — игрок не успевает стрелять между очередями врагов.
  - `glass_durability_shots` = `12 пуль` — После 12 попаданий в триплекс стекло покрывается плотной сеткой трещин, вынуждая выглядывать из-за щита вслепую.
- **Слои отклика**:
  - Визуал/VFX: Искры и вмятины на бронеплите щита при попаданиях, нарастающая паутина трещин на баллистическом стекле, дым от пороховых газов при выстреле с упора.
  - Звук (Web Audio): Звонкий металлический лязг рикошетов пуль о сталь (Bell-metallic ping), глухой стук попаданий в титан, механический скрежет фиксаторов щита при наклоне.
  - Камера: Смещение оси камеры на 0.45 м вбок с креном 14 градусов, легкий толчок назад (0.05 м) при поглощении тяжелых пуль щитом.
  - UI/Тактильность: Индикатор целостности щита (Shield Durability bar) и смотрового окна, сильная прерывистая виброотдача при пулеметном обстреле щита.
- **Режим отказа игрока**: Игрок жадничает, задерживает наклон (Lean) дольше 0.5 с во время очереди пулеметчика и ловит летальные пули в открытое плечо. Сигнал: кровавый всплеск на экране и стон ранения бойца.
- **Кривая мастерства**: Новичок сидит в глухой обороне, пока враги не разобьют стекло щита. Эксперт ритмично использует Quick-Lean Peek за 0.18 с в паузах между выстрелами террористов, не получая урона в тело.
- **Сопротивление игры**: Враги бросают гранаты под ноги за щит и заходят с двух флангов одновременно, перегружая фронтальный сектор блокирования.
- **Синергии**:
  - Штурмовой Slow-Mo: в замедлении наклоны позволяют филигранно уворачиваться от летящих трассеров.
  - Взрывной Бричинг: щит полностью блокирует осколки собственной взрывчатки при близком подрыве.
  - Обезвреживание СВУ: возможность одной рукой держать щит в сторону врага во время разрезания проводов.
- **Почему это не жанровый шаблон**: Щит — это не пассивная прибавка к броне, а физический трехмерный объект в пространстве: игрок вручную позиционирует его плоскость относительно вражеских дул.
- **Псевдокод тика**:
```text
function processShieldBlock(incomingBullet: Bullet, shield: BallisticShield, isLeaning: boolean): HitResult {
  if (isLeaning && isHitExposedArm(incomingBullet.ray)) {
    return PlayerHealth.applyDirectDamage(incomingBullet.damage);
  }
  if (shield.bounds.intersectsRay(incomingBullet.ray)) {
    shield.durability -= incomingBullet.caliberPower;
    VFXEngine.spawnSparks(incomingBullet.impactPoint, shield.normal);
    AudioEngine.playMetalRicochet(incomingBullet.caliber);
    if (shield.isGlassHit(incomingBullet.impactPoint)) shield.addGlassCrack();
    return HitResult.BLOCKED;
  }
  return PlayerHealth.applyDirectDamage(incomingBullet.damage);
}
```

### Эндоскопическая Разведка (Optic-Wand Recon) (SUPPORTING)
- **Category**: tactics
- **Description**: Просовывание гибкого кабеля с камерой под дверную щель или в технологическое отверстие перед штурмом для обнаружения позиций террористов и растяжек с взрывчаткой.
- **Player Input**: Удержание клавиши 'F' / Кнопка 'Камера' на экране — опустить кабель эндоскопа под дверь; Перемещение мыши / Джойстик — вращение микрокамеры на 120 градусов; Клик ЛКМ — расстановка тактических меток.
- **Hit & Sensory Feedback**: Визуал/VFX: Зернистый монохромный зеленый интерфейс микрокамеры (Night-Vision CRT-noise), красные тепловые силуэты наемников сквозь дым, желтые контуры растяжек C4.; Звук (Web Audio): Высокочастотный электронный писк включения ночного видения (12 кГц beep), приглушенные радиопереговоры террористов за дверью, звук жужжания микромотора кабеля.; Камера: Переключение на широкоугольный объектив типа 'рыбий глаз' (Fisheye distorsion 0.35) с легким виньетированием.; UI/Тактильность: Наложение тактической сетки с дальномером, появление постоянных 3D-маркеров врагов над дверным полотном после выхода из режима.
- **Technical Complexity**: Low
- **Решение игрока**: Потратить 3 секунды на детальный осмотр комнаты через щель, чтобы выявить растяжки и расстановку наемников, или пойти на быстрый слепой штурм ради сохранения таймера миссии.
- **Состояния**: `RECON_INACTIVE (вид от первого лица перед запертой дверью)`, `CABLE_DEPLOYING (анимация просовывания гибкого зонда под дверное полотно 0.8 с)`, `OPTIC_SCANNING (монохромный интерфейс ночного видения/тепловизора, маркировка угроз)`, `RECON_RETRACTING (быстрый возврат камеры в штурмовую позицию 0.4 с)`
- **Числовые параметры (реализуй именно эти значения)**:
  - `scan_field_of_view` = `120 градусов` — Если уменьшить до 45 градусов — игрок не видит засады по углам комнат; если сделать 360 градусов — теряется необходимость крутить камеру зонда.
  - `recon_time_cost` = `3.5 с` — Время штурмового таймера продолжает тикать во время разведки, создавая баланс между безопасностью и спешкой.
  - `max_threat_tags` = `4 метки` — Ограничение на количество подсвечиваемых сквозь стену целей (приоритезация самых опасных врагов).
  - `recon_noise_level` = `15 дБ` — Если держать камеру под дверью дольше 6 секунд, патрульный боевик может заметить зонд и прострелить дверь сверху.
- **Слои отклика**:
  - Визуал/VFX: Зернистый монохромный зеленый интерфейс микрокамеры (Night-Vision CRT-noise), красные тепловые силуэты наемников сквозь дым, желтые контуры растяжек C4.
  - Звук (Web Audio): Высокочастотный электронный писк включения ночного видения (12 кГц beep), приглушенные радиопереговоры террористов за дверью, звук жужжания микромотора кабеля.
  - Камера: Переключение на широкоугольный объектив типа 'рыбий глаз' (Fisheye distorsion 0.35) с легким виньетированием.
  - UI/Тактильность: Наложение тактической сетки с дальномером, появление постоянных 3D-маркеров врагов над дверным полотном после выхода из режима.
- **Режим отказа игрока**: Игрок задерживается в режиме эндоскопа слишком долго, террорист замечает объектив под дверью, кричит Контакт! и стреляет очередью сквозь дверь в лицо оперативнику. Сигнал: резкий треск помех на экране сканера и падение HP.
- **Кривая мастерства**: Новичок осматривает только середину комнаты и пропускает растяжку у порога. Эксперт за 1.5 секунды сканирует углы, ставит метку на пулеметчика и сразу переходит к установке C4 точно напротив головы командира.
- **Сопротивление игры**: Боевики устанавливают глушилки радиосигнала (создающие артефакты на экране эндоскопа) и прячут растяжки за мебелью вне прямого сектора обзора щели.
- **Синергии**:
  - Взрывной Бричинг: метки эндоскопа позволяют разместить заряд C4 с идеальным поражением врагов за стеной.
  - Штурмовой Slow-Mo: заранее размеченные цели подсвечиваются в замедлении, экономя доли секунды на прицеливание.
  - Обезвреживание СВУ: разведка серверной заранее показывает световую схему верного провода детонатора.
- **Почему это не жанровый шаблон**: Разведка эндоскопом дает осязаемое преимущество, перенося тактическую информацию в основную фазу боя в виде сквозных AR-маркеров (Augmented Reality) на Three.js.
- **Псевдокод тика**:
```text
function updateOpticRecon(opticCable: OpticWand, dt: number): void {
  if (!opticCable.isDeployed) return;
  opticCable.exposureTimer += dt;
  const visibleThreats = FrustumCuller.getObjectsInCone(opticCable.headPos, opticCable.lookDir, 120);
  visibleThreats.forEach(threat => {
    if (threat.isSpottedByPlayerRay()) threat.addPersistentARMarker();
  });
  if (opticCable.exposureTimer > 6.0 && Math.random() < 0.25) {
    EnemyAI.triggerDoorWallbangAlert(opticCable.doorRef);
    opticCable.forceRetract();
  }
}
```

### Обезвреживание СВУ под Огнем и Разрезание Проводов (Defusal Tension QTE) (SECONDARY)
- **Category**: Финальный тормоз и кульминационный источник напряжения миссии
- **Description**: Обезвреживание бомбы — это не скучное стояние с зажатой кнопкой на 5 секунд (как в Counter-Strike), а микро-тактическая головоломка в реальном времени под звуки боя.
- **Player Input**: Удержание 'E' / Тап по корпусу бомбы — открыть панель взрывателя; ЛКМ / Свайп кусачками по нужному проводу (Красный/Синий/Желтый); ПКМ — удерживать щит свободной рукой.
- **Hit & Sensory Feedback**: Визуал/VFX: Крупный план цифрового дисплея СВУ с быстро меняющимися красными цифрами, искры от кусачек при перерезании медной жилы, зеленый залп светодиодов успеха.; Звук (Web Audio): Ускоряющийся частый писк зуммера бомбы (от 1 Гц до 8 Гц на последних секундах), металлический щелчок инструмента, облегченный выдох бойца.; Камера: Жесткий динамический фокус на электронный блок бомбы с размытием заднего плана (Depth of Field), тревожное покачивание при близких выстрелах.; UI/Тактильность: Пульсирующий красный оверлей тревоги, частые короткие виброимпульсы в такт таймеру бомбы, кнопка CUT WIRE.
- **Technical Complexity**: Medium
- **Решение игрока**: Приступить к обезвреживанию бомбы прямо сейчас, прикрываясь щитом от выжившего стрелка, или сначала добить врага, рискуя опоздать по таймеру детонатора.
- **Состояния**: `BOMB_ARMED_COUNTDOWN (таймер бомбы пищит с нарастающей частотой, до взрыва 15-25 с)`, `DEFUSAL_PANEL_OPEN (открытие электронной схемы с 3 цветными проводами и микрочипом)`, `WIRE_SNIP_VERIFICATION (анализ логической схемы: провод питания, заземление, триггер детонации)`, `BOMB_DEFUSED_SUCCESS (отключение звука таймера, зеленый светодиод, победа в операции)`
- **Числовые параметры (реализуй именно эти значения)**:
  - `bomb_timer_limit` = `25.0 с` — Если сделать 10 с — игрок физически не успевает зачистить комнату; если сделать 60 с — таймер перестает создавать нерв и спешку.
  - `wire_cut_reaction_window` = `1.2 с` — Время на выбор верного провода по подсказке из эндоскопа (соответствие цвета маркировке на стене).
  - `one_handed_shield_coverage` = `55%` — Во время обезвреживания бомбы щит прикрывает только торс сбоку, оставляя спину и ноги уязвимыми.
  - `wrong_wire_penalty` = `-8.0 с от таймера` — Ошибка при срезе провода мгновенно сжигает 8 секунд на таймере детонатора, вводя игрока в критический стресс.
- **Слои отклика**:
  - Визуал/VFX: Крупный план цифрового дисплея СВУ с быстро меняющимися красными цифрами, искры от кусачек при перерезании медной жилы, зеленый залп светодиодов успеха.
  - Звук (Web Audio): Ускоряющийся частый писк зуммера бомбы (от 1 Гц до 8 Гц на последних секундах), металлический щелчок инструмента, облегченный выдох бойца.
  - Камера: Жесткий динамический фокус на электронный блок бомбы с размытием заднего плана (Depth of Field), тревожное покачивание при близких выстрелах.
  - UI/Тактильность: Пульсирующий красный оверлей тревоги, частые короткие виброимпульсы в такт таймеру бомбы, кнопка CUT WIRE.
- **Режим отказа игрока**: Игрок в спешке перерезает провод-ловушку (Trigger wire) вместо провода питания или не успевает обезвредить заряд до истечения таймера. Сигнал: непрерывный гудок зуммера, белый экран взрыва и провал миссии.
- **Кривая мастерства**: Новичок пытается сначала полностью убить всех врагов, теряет время и взрывается на последних секундах. Эксперт блокирует огонь щитом в угол, за 2.5 секунды считывает схему и обезвреживает бомбу прямо под градом пуль.
- **Сопротивление игры**: Последний бронированный наемник в серверной бросает дымовую шашку перед бомбой, ослепляя оперативника и мешая разглядеть цветовую маркировку проводов.
- **Синергии**:
  - Эндоскопическая Разведка: разведка серверной заранее показывает световую схему верного провода на мониторе схем.
  - Баллистический Щит: возможность одной рукой держать щит в сторону врага, пока вторая орудует кусачками.
- **Почему это не жанровый шаблон**: Обезвреживание бомбы — это не скучное стояние с зажатой кнопкой на 5 секунд (как в Counter-Strike), а микро-тактическая головоломка в реальном времени под звуки боя.
- **Псевдокод тика**:
```text
function onCutWire(bomb: C4BombLogic, wireColor: WireColor): DefusalResult {
  if (wireColor === bomb.disarmTargetWire) {
    bomb.isArmed = false;
    AudioEngine.playDisarmSuccessJingle();
    GameState.triggerMissionVictory();
    return DefusalResult.SUCCESS;
  } else {
    bomb.remainingTimeSec = Math.max(0, bomb.remainingTimeSec - 8.0);
    AudioEngine.playWarningBuzzer();
    VFXEngine.spawnShortCircuitSparks(bomb.panelPos);
    if (bomb.remainingTimeSec <= 0) GameState.triggerBombExplosionDefeat();
    return DefusalResult.TIME_PENALTY;
  }
}
```


---

## 3b. ⚠️ СТРОГИЙ ЗАПРЕТ ЖАНРОВЫХ ШАБЛОНОВ И КЛОНОВ (CRITICAL ANTI-CLICHÉ RULES)
Кодовый агент ОБЯЗАН реализовать уникальную игру, спроектированную в этом ТЗ, а не шаблонный автошутер:
1. **ЗАПРЕТ ШАБЛОННЫХ РОГАЛИКОВ И КАРТОЧЕК**: Если игра прямо не требует карточный драфт в GDD, **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** добавлять спам волн врагов и всплывающее окно «Выберите 1 из 3 карт апгрейда».
2. **ЗАПРЕТ СЕРЫХ ПРИМИТИВОВ НА ПУСТОЙ ПЛОСКОСТИ**: Создавай выразительную процедурную низкополигональную 3D/2D графику, точно соответствующую `ART_DIRECTION.md` (тематические персонажи, рельеф, модули, объекты окружения, частицы), а не бегающий куб на сером полу.
3. **ТОЧНОЕ СОБЛЮДЕНИЕ МЕХАНИК**: Реализуй все состояния, формулы, тайминги и слои отклика из `MECHANICS.md` (включая Web Audio звуки, импульсы камеры, визуал и тач-инпут).
4. **СПЕЦИФИЧЕСКОЕ УПРАВЛЕНИЕ**: Реализуй схему управления под конкретную механику этой игры (свайпы, жесты, траектории, физический дрифт, прицеливание), а не стандартный стик.

---

## 4. SOFTWARE ARCHITECTURE & SYSTEMS
The game must be built with a clean, decoupled layer architecture:

- **Core Layer**: Game loop, Time manager, State machine, EventBus
- **Physics Layer**: Rapier3D integration, Raycasting for bullets, Debris collision rigidbodies
- **Rendering Layer**: Three.js Scene Graph, Mesh Batching, Custom shaders for slowmo/smoke, Dynamic lighting
- **Gameplay Systems**: Breach controller, Tactical AI FSM, Weapon ballistic raycaster, Shield dynamics
- **Audio Layer**: Web Audio API procedural sound synthesizer, Spatial audio nodes, Low-pass filter for concussion
- **Platform Layer**: Playgama Bridge SDK wrapper, Storage, Ads manager, Responsive canvas handler

### Module Map (`src/`):
```text
src/
├── main.ts                    # Bootstrap, Playgama Bridge init, Game launch
├── core/
│   ├── Game.ts                # Main coordinator & state machine
│   ├── GameLoop.ts            # 60Hz fixed update loop with delta clamping
│   └── EventBus.ts            # Typed publish/subscribe event dispatcher
├── platform/
│   ├── PlaygamaService.ts     # Wrapper for @playgama/bridge (Ads, Save, Leaderboards)
│   └── StorageService.ts      # Cloud & LocalStorage sync with debouncing
├── physics/
│   └── PhysicsWorld.ts        # Rapier3D / Physics world manager & colliders
├── entities/
│   ├── Player.ts              # Player entity & input handling
│   └── EntityManager.ts       # Dynamic entity pool & lifecycle
├── systems/
│   ├── DestructionSystem.ts     # Процедурный расчет взлома стен и дверей, гене
│   ├── TacticalAiSystem.ts      # Управление поведением боевиков: секторы наблю
│   ├── TimeDilationScoringSystem.ts # Модуляция шкалы времени во время прорыва и ра
├── rendering/
│   ├── SceneManager.ts        # Three.js scene graph, lights, perspective camera lerp
│   ├── ProceduralModels.ts    # Styled models / geometry for Тактика Прорыва: CQB Штурм
│   └── ParticleSystem.ts      # Particle effects & visual feedback
├── ui/
│   ├── theme.css              # Единственное место со значениями: цвета, шрифты, шкала, слои
│   ├── UiRoot.ts              # Слои над канвасом, измеренный вьюпорт, safe-area + баннер
│   ├── ScreenRouter.ts        # Экраны как автомат: один видимый, один общий переход
│   ├── Hud.ts                 # Запись в закэшированные узлы только при изменении значения
│   ├── components/            # Button, IconButton, Panel, Modal, Toast, Meter, Stat, ListRow
│   ├── screens/               # По файлу на экран, каждый — со своими loading/empty/error
│   ├── icons.ts               # Один инлайновый SVG-спрайт, цвет через currentColor
│   └── TouchControls.ts       # Mobile touch input adapter
└── audio/
    └── AudioManager.ts        # Sound effects pool & dynamic audio feedback
```

---

## 5. PLAYGAMA BRIDGE INTEGRATION SPECIFICATION
Platform integration is powered by `@playgama/bridge`.

### 1. Initialization & Ready Event
`game_ready` is **NOT** sent after `initialize()` — that dismisses the platform splash over an unloaded game. It is sent once, after assets are loaded and the menu is interactive.

```typescript
export async function bootstrapPlatform(): Promise<void> {
    // A blocked sdk.js (ad blocker, CDN failure) must not mean a permanent black screen.
    await Promise.race([bridge.initialize(), new Promise((r) => setTimeout(r, 10_000))]);
    bridge.platform.sendMessage('in_game_loading_started');
}

let gameReadySent = false;
export function sendGameReady(): void {
    if (gameReadySent) return;                  // a second send can re-arm the platform splash
    gameReadySent = true;
    try { bridge.platform.sendMessage('game_ready'); } catch {}
    try { bridge.platform.sendMessage('in_game_loading_stopped'); } catch {}
}
```

**Boot order (strict).** Nothing in this chain may wait on a player decision:
page guards → `initialize()` → language → silent VK/OK auth → load save → redeem pending purchases → build engine/UI → progress to 100% → `sendGameReady()` → arm banners → first-launch tutorial.
Keep a 15 s watchdog that sends `game_ready` regardless of boot failures.

### 2. Advertisement Flow
- **Interstitial Ads**:
  - Minimum **90 seconds** cooldown, and never below the platform's configured minimum.
  - Only at natural breaks traceable to a real click (run over, level complete, leaving to menu). Never at boot, never mid-combat, never right after a purchase.
  - Arm the slot when the run ends; fire it when the player taps to leave the result screen.
  - Never call `showInterstitial()` from a state method — the click handler decides.
- **Rewarded Ads** — the reward is granted **only** on `state === 'rewarded'`, never when the promise resolves. Always `off()` the listener and guard re-entry, or one ad pays out twice:
- **Экстренный Медпакет (Второе Дыхание) (`tactical_revive`)**: Мгновенное воскрешение оперативника с восстановленным щитом для завершения текущей комнаты (Trigger: Экран поражения при гибели бойца, Limit: 1 раз за операцию)
- **Удвоение Тактических Кредитов (`double_mission_reward`)**: Умножение заработанных за зачистку объекта средств на 2 (Trigger: Экран победы и подсчета очков, Limit: После каждого успешного рейда)
- **Премиум Боезапас Спецсредств (`supply_airdrop`)**: Дополнительный термозаряд и светошумовая граната перед сложным штурмом (Trigger: Экран планирования экипировки, Limit: 1 раз в 10 минут)
- Every ad surface is capability-gated: if `isRewardedSupported` is false the button is **not rendered at all**.

### 3. Cloud Storage & Save State
- Persistent storage key: `"player_credits"` — one key, one JSON object.
- `bridge.storage.set(key, value)` / `get(key)` take **no `storageType` argument**; v2 picks cloud vs. local.
- Normalize on read: a corrupted or truncated save must boot on defaults, not crash.
- Mirror to `localStorage` for instant/offline boot, but never as the only copy — it is partitioned third-party storage inside the platform iframe. Settings (mute, volume, language) live in the save.
- Debounce writes by 1.5 s **and** flush on `pagehide` / `visibilitychange`.
- Daily/timed content uses `bridge.platform.getServerTime()`, never the device clock.

### 4. Lifecycle & Auto-Pause
- Subscribe to `bridge.platform.on(bridge.EVENT_NAME.PAUSE_STATE_CHANGED)` and `AUDIO_STATE_CHANGED` — not `visibility_state_changed`, which misses interstitials.
- Fire the callback once with the current value at subscribe time; a game booted in a hidden tab otherwise starts in the wrong state.
- On resume, reset the delta accumulator and clamp `dt` to 0.1 s.

### 5. Authorization
- `authorize()` only from an explicit player action — **except** VK/OK, where it is silent, runs at boot before saves, and is time-boxed to 5 s.
- Guests have a non-null `id` and `name`: the only reliable check is `player.isGuest`.
- Never `await` a dialog-showing `authorize()` inside boot.

---

## 6. USER INTERFACE & MOBILE CONTROLS
Мобильное управление — обязательная часть поставки, а не «доделаем потом».
Большинство игроков на Яндекс Играх / VK / Playgama заходят с телефона: игра без
рабочего тач-управления не проходит приёмку, даже если на клавиатуре всё идеально.

- **Orientation**: Landscape
- **Safe Area Insets**: `padding: calc(18px + env(safe-area-inset-bottom))` и аналогично
  для left/right — кнопки не должны попадать под вырез камеры и системные жесты.

### Визуальный контракт интерфейса
Интерфейс — первое, что видит игрок: до геймплея он смотрит на меню. Меню,
собранное из умолчаний браузера, обесценивает всё, что сделано за ним, и это
самый заметный признак недоделанной игры. Всё ниже — требование, а не пожелание.

**Материал интерфейса**: Mil-Spec HUD: высокотехнологичный минималистичный интерфейс тактического шлема с бирюзовыми и янтарными акцентами, четкой типографикой и индикацией прочности щита

**Визуальный язык**: Интерфейс спецназа: матовые темно-серые плашки с 1px неоновой рамкой, моноширинные шрифты тактических приборов, контрастные индикаторы опасности.

**Типографика**: Roboto Mono / Orbitron (чистый military-tech гротеск высокой читаемости)

**Акценты — один цвет = один смысл** (разный цвет у каждой кнопки «чтобы отличались» читается как мусор):

| Токен | Единственный смысл |
|---|---|
| `critical_danger` | #ff1e27 (Красный - маркеры врагов и растяжек) |
| `primary_action` | #ff6a00 (Оранжевый взрывной - для кнопок детонации и штурма) |
| `safe_success` | #00ff66 (Зеленый - индикатор зачищенной зоны) |
| `tactical_info` | #4deeea (Бирюзовый - для статуса щита и фокуса) |

**Набор компонентов — закрытый.** Всё на экране обязано быть одним из них; одноразовый `<div>` с инлайновыми стилями — это баг, из-за которого второй экран перестаёт совпадать с первым:
- TacticalButton
- ShieldHealthGauge
- SlowMoFocusBar
- AmmoCounterHUD
- ThreatMarker3D
- RewardSummaryCard

**Якоря HUD** — пять и только пять; посередине экрана не висит ничего, кроме временной обратной связи:

| Якорь | Что там |
|---|---|
| `bottom_center` | Кнопка детонации C4 / шкала Slow-Mo Focus |
| `bottom_left` | Индикатор прочности баллистического щита и кнопки Lean |
| `bottom_right` | Счетчик патронов и кнопка перезарядки |
| `center` | Динамическое прицельное перекрестие |
| `top_center` | Таймер операции и статус комнат (1/3, 2/3, 3/3) |

**Состояние, показанное миром, а не оверлеем**:
- Трещины и сколы на стекле баллистического щита, отражающие реальное состояние брони
- Лазерные целеуказатели врагов, рисующие красные точки на стенах и щите
- Светодиодный таймер детонатора на руке бойца

**Переходы экранов**: MainMenu -> Armory (снаряжение) -> BreachPlanning (выбор точки взлома) -> GameplayLoop (Штурм комнат 1-3) -> AfterActionReport (награды) -> MainMenu

**Отклик интерфейса на действие**:
- При наведении на уязвимую точку стены: контур подсвечивается неоновым оранжевым
- При попадании в голову врага: сочный щелчок каски и появление красного перекрестия 'Headshot Kill'
- При попадании вражеской пули в щит: вспышка искр, звук удара по металлу и микро-сотрясение камеры

**Состояния экранов** — загрузка, пустота и ошибка проектируются наравне с удачным путём: сохранение, таблица лидеров, покупки и реклама на этих площадках отваливаются регулярно:
- Экран паузы при потере фокуса окна браузера
- Индикация потери соединения с облаком сохранений
- Экран окончания патронов с визуальной подсказкой перезарядки

**Значения выводятся из этого мира, а не берутся из примеров.** Имена токенов (`--color-primary`, `--font-display`, `--space-*`) одинаковы во всех играх — на них держится код компонентов. Сами значения, обе гарнитуры, силуэт рамки и язык иконок выводятся из материала интерфейса, названного выше, по процедуре из раздела 12 `knowledge/ux/ui_design_system.md`: из чего сделана панель, при каком свете она лежит, чем в этом мире помечают главное и опасное, чем написаны цифры, как режут край. Скопировать палитру, шрифты или фаску из примера в базе знаний или из другой игры — это тот же дефект, что и оставить умолчания браузера: интерфейс выглядит решением, принятым не для этой игры. Обратная проверка: если от интерфейса другой игры фабрики ваш отличается только оттенком акцента — процедура была пропущена.

**Обязательные технические правила интерфейса** (каждое — из починенного бага, подробности в `knowledge/ux/ui_design_system.md` и `knowledge/ux/ui_implementation.md`):
- Все значения — токенами в одном `src/ui/theme.css`: цвета, две гарнитуры, шкала отступов, радиусы, длительности, `--z-*`. Литерал `#RRGGBB` или `z-index: 9999` внутри экрана запрещён; `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` мимо темы обязан быть пуст.
- Слои над канвасом прозрачны для ввода: контейнер — `pointer-events: none`, `auto` включают только листовые интерактивные элементы. Слой HUD не кликается никогда. Полноэкранный оверлей, оставленный на `auto`, съедает игровой ввод — и это выглядит как «на телефоне не работает управление».
- Один масштаб на весь интерфейс (`--ui-scale`), а не отдельная раскладка под каждый размер экрана. Размеры зон нажатия при этом не масштабируются: основная ≥ 96 px, остальные ≥ 64 px, зазор ≥ 12 px.
- Меняющиеся числа — `font-variant-numeric: tabular-nums` в слоте фиксированной ширины; полосы — `transform: scaleX()`, не `width`. HUD пишет в закэшированные узлы и только при изменении значения: пересборка разметки в кадре стоит кадров.
- Один экран виден за раз, скрытый — `display: none`; переход один на всю игру и укладывается в 300 мс. Анимируются только `transform` и `opacity`. `prefers-reduced-motion: reduce` убирает трансформации.
- Любое нажатие отвечает в том же кадре; действие с ожиданием переводит кнопку в `loading` и снимает его в `finally`.
- Запрещено: `alert`/`confirm`/`prompt`, эмодзи вместо иконок (один инлайновый SVG-спрайт с `currentColor`), голый `<input type=range>` и `<select>`, текст над геймплеем без подложки или обводки, серая неактивная кнопка вместо отсутствующей на площадке возможности — такой элемент не рисуется вовсе.
- Геометрия меню считается от ИЗМЕРЕННОГО вьюпорта (`visualViewport`, пересчёт после поворота, выхода из фуллскрина и закрытия рекламы), а не от `100vh`; нижний отступ включает измеренную высоту липкого баннера. Страница под игрой не скроллится — длинные списки скроллятся во внутреннем контейнере.
- `backdrop-filter: blur()` на полноэкранном слое — самое дорогое свойство оверлея на мобильных GPU; только на маленьких панелях.

### Обязательный контракт тач-управления
- **Слева — ДВИЖЕНИЕ**: плавающий виртуальный джойстик на 2 оси, зона захвата — вся левая половина экрана.
- **Справа — ДЕЙСТВИЯ**: крупная основная кнопка (атака/использование) и 2–3 второстепенные (дэш, блок, спец-умение).
- Движение и атака должны работать одновременно.

- **Реализация только на Pointer Events** (`pointerdown/move/up/cancel`) с
  `setPointerCapture` и учётом `pointerId` для каждой кнопки: `touchstart/end`
  теряет палец на границе элемента, а второй палец сбрасывает первый.
- **Плавающий стик**: зона захвата — вся левая половина экрана, база стика
  появляется под пальцем. Мёртвая зона 8%, иначе управление дрожит.
- **Отмена браузерных жестов**: `touch-action: none`, отмена `contextmenu`,
  `dragstart` и `touchmove` с `{ passive: false }`; `-webkit-tap-highlight-color: transparent`.
- **Видимость по состоянию**: слой управления показан только в игровом процессе,
  скрыт в меню / гараже / паузе / модалках и при скрытии сбрасывает все оси и
  кнопки (также по `blur` и `visibilitychange`).
- **Размеры**: основная кнопка действия ≥ 96 px, второстепенные ≥ 64 px, зазор ≥ 12 px.
- **Отладочный флаг** `?touch=1` принудительно включает мобильную раскладку на
  десктопе (и `?touch=0` выключает) — без него управление невозможно проверить.
- Клавиатура и тач работают параллельно и не глушат друг друга.

### Desktop Controls
- `WASD` / стрелки — движение
- ЛКМ / `J` — основная атака
- ПКМ / `K` — тяжёлая атака / блок
- `Space` / `Shift` — рывок / уклонение
- `P` / `Esc` — пауза

---

## 6a. ЖУРНАЛ РАЗРАБОТКИ И CHANGELOG (ЧАСТЬ DEFINITION OF DONE)
Проект живёт в песочнице `workspace/sozday_igru_po_tipu_rainbow_six/`, и вся работа за её пределы
не выходит. Правила работы продублированы в `AGENTS.md` в корне проекта —
прочитай его первым. В корне также ведутся два журнала; они обновляются в конце
**каждой** рабочей сессии, до отчёта о завершении:

- **`DEVLOG.md`** — запись вида `## ГГГГ-ММ-ДД ЧЧ:ММ — <суть>` с пунктами
  **Задача**, **Сделано**, **Затронутые файлы**, **Проверено**,
  **Известные проблемы / следующий шаг**.
- **`CHANGELOG.md`** — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/):
  раздел `## [Unreleased]`, подразделы Added / Changed / Fixed / Removed,
  формулировки на языке игрока, а не описание диффа.
- **`README.md`** — как запустить (`npm install`, `npm run dev`), управление на
  клавиатуре и на телефоне, структура каталогов.

Игра обязана запускаться командой `npm run dev` и открываться в браузере без
ошибок в консоли: именно так её проверяет фабрика (вкладка «Играть»).

---

## 7. ART DIRECTION & VISUAL GUIDELINES
- **Style**: Тактический Полуреализм (Low-Poly / Hard-Surface CQB)
- **Camera Perspective**: First-Person (Вид от первого лица) с динамическим FOV и покачиванием при отдаче (FOV: 75°, Pitch: 0°)
- **Environment**: Захваченное дипломатическое посольство: бетонные колонны, офисные перегородки из гипсокартона, серверные стойки, армированные двери и тактическое освещение
- **Lighting**: Контрастное интерьерное освещение: холодный рассеянный свет офисных ламп, теплые лучи тактических подствольных фонарей, яркие объемные вспышки от выстрелов и детонации C4
- **UI Theme**: Mil-Spec HUD: высокотехнологичный минималистичный интерфейс тактического шлема с бирюзовыми и янтарными акцентами, четкой типографикой и индикацией прочности щита (реализация — секция 6, «Визуальный контракт интерфейса»)
- **Visual Feedback**: Screen-space hitstop (40ms on critical hit), directional particle sparks, additive ribbon weapon trails.

---

## 8. STEP-BY-STEP DEVELOPMENT ROADMAP
### Phase 1: Core Prototype (Бричинг и Slow-Mo) (2 days)
- **Deliverable**: Играбельный прототип подрыва стены и ликвидации 2 манекенов в замедленном времени.
  - Настройка сцены комнаты Three.js и контроллера камеры FPS
  - Интеграция Rapier3D и прототипа разрушаемой стены
  - Реализация замедления времени (Slow-Mo) при детонации C4
### Phase 2: Combat & Shield System (Щит и Перестрелка) (2 days)
- **Deliverable**: Полноценный боевой контакт в комнате с блокированием пуль щитом и наклонами.
  - Реализация баллистического щита и механики наклонов Lean Q/E
  - Система точечной летальной баллистики (ваншот в голову)
  - ИИ противников: укрытия, оглушение от взрыва, ответный огонь
### Phase 3: Level Flow & Tactics (3 Комнаты и Разведка) (2 days)
- **Deliverable**: Полная цепочка штурма объекта на 90-120 секунд с переходом между секторами.
  - Создание 3 последовательных комнат посольства с нарастающей сложностью
  - Механика эндоскопической разведки и растяжек
  - Система подсчета очков, звезд и таймеров комнат
### Phase 4: Polish, Audio & Playgama SDK (Релизная полировка) (1 days)
- **Deliverable**: Готовый релизный билд v1.0, оптимизированный под Яндекс Игры и мобильные браузеры.
  - Интеграция Playgama Bridge SDK (сохранения, реклама, лидерборды)
  - Внедрение пространственного звука Web Audio и фильтра контузии
  - Оптимизация для мобильных устройств (Object pooling, Lod)

---

## 9. NON-NEGOTIABLE PLATFORM RULES
Every rule below corresponds to a bug that reached production or a moderation rejection in a shipped game. They override any conflicting habit, tutorial or example — including snippets found in the Playgama/Yandex docs, many of which describe the deprecated Bridge v1 contract.

Distilled from shipped HTML5 games on Yandex Games, VK, OK, CrazyGames and
Playgama. Every rule here corresponds to a bug that reached production or a
moderation rejection. Violating any of them ships a broken or rejected game.

### Boot & lifecycle

1. `game_ready` is sent **once**, only after assets are loaded and the menu is
   interactive — never right after `bridge.initialize()`.
2. **Nothing in the boot path may await a player decision.** An `await
   authorize()` during boot hangs the game for 100 % of guests.
3. Wrap `bridge.initialize()` in a timeout (~10 s) and keep a boot watchdog
   (~15 s) that sends `game_ready` anyway — a blocked SDK must not mean a
   permanent black screen.
4. Drive `bridge.setGameLoadingProgress()` from real milestones, and hold
   `game_ready` until the splash has reached 100 %.
5. Any boot step awaiting an animation frame needs a deadline: a hidden tab
   delivers no frames.
6. Pause and audio come from `bridge.platform.on(EVENT_NAME.PAUSE_STATE_CHANGED /
   AUDIO_STATE_CHANGED)`, not from `visibilitychange` alone. Reset the delta
   accumulator on resume and clamp `dt`.

### Authorization

7. `authorize()` is called only from an explicit player action — **except** VK/OK,
   where it is silent and runs at boot before saves, time-boxed to ~5 s.
8. Guests have a non-null `id` and `name`. The only reliable check is
   `player.isGuest`.
9. On VK/OK report the player as authorized regardless of the token.
10. `authorize()` may resolve `false` instead of rejecting — treat that as a
    refusal and fall back to the raw `isAuthorized` flag.
11. Filter placeholder names (`Guest…`, `player`, `unknown`) and use your own
    localized label instead.

### Storage

12. One save key, one JSON object, normalized on read — a corrupt or truncated
    save must boot on defaults.
13. Bridge v2 takes **no `storageType` argument**; it picks cloud vs. local.
14. `localStorage` is a mirror only — it is third-party storage in the platform
    iframe (partitioned in Chrome, culled in Safari). Settings such as mute,
    volume and language live in the save, not in `localStorage`.
15. Never swallow a cloud read failure silently — it downgrades a cloud save to a
    device-local one.
16. Flush on `pagehide` and `visibilitychange`, not `beforeunload`.
17. Daily/timed content uses `bridge.platform.getServerTime()`, never the device
    clock.

### Ads

18. A rewarded reward is granted **only** on `state === 'rewarded'`, never when
    the promise resolves.
19. Always `off()` the rewarded listener, and guard re-entry — otherwise two
    clicks pay out twice for one ad.
20. No interstitial at boot, mid-gameplay, over a screen being read, or right
    after a purchase. Natural breaks only, traceable to a click.
21. Never call `showInterstitial()` from a state method; the click handler decides.
22. Keep the game-side interstitial floor ≥ the platform's configured minimum.
23. Never re-request a banner that is already shown; on VK/OK request it once per
    session and never refresh.
24. A sticky banner can be drawn over the game — measure whether the viewport
    actually shrank and reserve the strip if not, or the bottom UI row becomes
    unreachable.
25. Premium ("no ads") suppresses interstitials and banners but **keeps** rewarded.

### Payments

26. `consumePurchase(productId)` — the product id, never the purchase token.
27. Check `getPurchases()` at every launch: **grant first, then consume.**
    Consuming without granting destroys paid goods.
28. Keep one exported list of consumable ids; divergent copies drop products.
29. Never hardcode prices — read `getCatalog()` asynchronously and cache.
30. If `payments.isSupported` is false, paid items show as free — no locks, no
    prices.

### Capability gating

31. UI is built on capability flags. A control for an unsupported feature is
    **not rendered at all** — not disabled, not erroring on tap. Applies to
    leaderboards, payments, rewarded, auth and every social action.

### Compliance (Yandex)

32. Lock the page globally before anything paints: `position: fixed` body,
    `overscroll-behavior: none`, guarded `touchmove`, and document-level capture
    handlers for `contextmenu` / `selectstart` / `dragstart`.
33. Refuse multi-touch on `touchmove`, never on `touchstart` — cancelling the
    second finger breaks two-thumbed controls. Never `preventDefault()` inside a
    real scroller or a form control.
34. Inset every UI layer by `env(safe-area-inset-*)`; only the art layer reaches
    the physical edge. Keep `viewport-fit=cover`.
35. Never size UI from `100vh`: publish the measured height to `--vp-h` and
    re-measure across a settling window after fullscreen changes.
36. Audio via the **Web Audio API** only — an `<audio>` element puts a media
    player in the notification panel and on the desktop UI.
37. Menus never scroll the page; long content goes into an explicit inner
    scroller.
38. The game title must be byte-identical everywhere in the draft.
39. Every language-dependent string is translated; keys exist in all locales and
    placeholders match. Never concatenate sentences.

### Delivery

40. `dist/` is what ships — rebuild after any change to `src/` or the bridge
    config; a zip step alone re-packs a stale build.
41. Redeploy the whole `dist/`: `platform-bridges/*.js` are separate chunks and a
    stale one keeps the old behaviour.
42. One codebase, one build. Branch on `bridge.platform.id` and capability flags,
    never fork per platform.
43. Test in the platform's own frame (Yandex draft via `sdk-dev-proxy`) as guest
    **and** authorized before submitting.

### Audio

44. All sound goes through **one master `GainNode`**; mute, ducking and the
    platform's audio flag touch nothing else. Ramp the gain — an instant change
    clicks.
45. The `AudioContext` starts **suspended** until a real user gesture. Resume from
    the first gesture, and never block boot or `game_ready` on it.
46. Keep the player's mute and the platform's mute as separate inputs, or
    returning from an ad un-mutes a player who muted deliberately.
47. Do not additionally mute around your own `showRewarded()` call — the platform
    pause event already covers it, and doubling up leaves the game silent when the
    ad fails to open.

### Social

48. Every social action is capability-gated and its flags are **properties, not
    functions**. Hide the whole entry point when nothing is supported.
49. Call social methods **synchronously inside the real pointer handler** — an
    engine-frame callback loses the popup on VK/OK. Grant rewards after the call.
50. A rejection is the player closing a dialog, not an error: never show a failure
    message, and never gate progression behind a social action — portals reject it.
51. Publisher data (community ids, share URLs) belongs in the bridge config, not in
    game code: on v2 the config wins, and an `undefined` runtime value overwrites a
    configured one.

### Stack

**S1.** The factory ships **Three.js only**. A 2D game is the same scene under an
orthographic camera — never a second renderer. See
`knowledge/threejs/orthographic_2d_and_pointer_input.md`.

**S2.** The stack is fixed and pinned: `three ^0.185.1`,
`@dimforge/rapier3d-compat ^0.20.0`, `three-mesh-bvh ^0.9.14`, `yuka ^0.7.8`,
`recast-navigation ^0.43.1`, `bitecs ^0.4.0`, `postprocessing ^6.39.4`. Online
snippets written for `bitecs 0.3` (`defineComponent`/`defineQuery`) or
`rapier 0.13` do not compile against these — check `knowledge/stack/`.

**S3.** **If the stack solves it, take the library.** A hand-rolled A\*, boids
flock, broadphase, character controller, vision/memory timer or bloom chain is a
review defect, not an optimisation. The full "task → library" table is
`knowledge/stack/README.md` §1: physics and vehicles → Rapier; raycast against
static level geometry → three-mesh-bvh; steering, FSM, fuzzy decisions and
perception → Yuka; NPC pathfinding and crowds → recast-navigation; hundreds of
identical entities → bitECS + `InstancedMesh`; screen effects → postprocessing.

**S4.** Frame order is fixed: input → AI → controllers → `world.step()` → ECS →
transform sync → camera → quality changes → `composer.render()`.
`renderer.render()` is **not** called next to `composer.render()` — that draws the
scene twice.

**S5.** Physics steps at a **fixed** timestep with a substep cap (≤ 4). Passing
`dt` into the solver destabilises suspension and joints; an uncapped catch-up loop
turns a slow frame into a hang.

**S6.** WASM (Rapier, Recast) is initialised on the loading screen behind the boot
watchdog, and Recast is loaded **only** when the game actually needs a navmesh.

### Renderer

52. Auto-tuning quality from raw frame time does not work under vsync — every frame
    takes ≈ the refresh interval regardless of GPU load. Measure the cadence between
    **rendered** frames and discover headroom by optimistic probing.
53. Never target above the panel's refresh rate, and never launch in reduced quality
    and crawl up — start optimistic and step down.
54. Apply resolution and shadow-map changes **before** `render()`, on a frame you
    actually render; doing it after clears the canvas and flashes a blank frame.
55. One group, one degree of freedom. A mesh that both steers and spins (wheel,
    turret, limb) needs **nested** groups — `Euler('XYZ')` applies the inner
    rotation around the parent's axis and visibly skews the part otherwise.
    See `knowledge/threejs/vehicle_wheel_rig.md`.

### Touch controls

56. Touch controls are built on **Pointer Events** with `setPointerCapture` and
    per-`pointerId` tracking — `touchstart/end` alone loses fingers at element
    borders and lets a second finger cancel the first (nitro kills throttle).
57. Movement and the primary action must be usable **at the same time**. In a
    driving game throttle never shares an axis with steering: it is a separate
    pedal button, the largest control on screen.
58. The control layer sets `touch-action: none`, cancels `contextmenu`,
    `dragstart` and non-passive `touchmove`, and respects `env(safe-area-inset-*)`.
    Without this the page scrolls, pull-to-refreshes and gets rejected by
    moderation.
59. Controls are visible only during gameplay and are **reset** whenever hidden,
    on `blur` and on `visibilitychange` — a transparent control layer over a menu
    swallows button taps, and a held throttle survives an ad break otherwise.
    See `knowledge/ux/touch_controls.md`.

### Physics vehicles

60. A physics vehicle is **never** driven by writing `setLinvel()` every frame.
    That overwrites the solver, ignores slopes and heading, and leaves the wheels
    as decoration — the "wheels don't turn, the truck doesn't move" bug. Use the
    engine's own ray-cast vehicle controller (`world.createVehicleController`).
61. Visual wheels of a physics vehicle are **children of the chassis group**, and
    their suspension travel, steering angle and roll angle are read from the
    controller. A separate wheel root that copies only `translation()` detaches
    the wheels from the body the moment it tilts, and leaves parts overlapping
    where the body has moved on. The opposite rule in `vehicle_wheel_rig.md` §3
    applies to kinematic arcade cars only.
62. `updateVehicle(dt)` runs **before** `world.step()`, and the wheels' ray-casts
    are filtered to the ground collision group — unfiltered they hit the chassis
    or the cargo and the vehicle climbs its own load.
63. Cargo and props carried by a body spawn from **body-local** slots that do not
    intersect its colliders. An overlap at spawn is resolved by ejection: the load
    fires out of the bed on frame one. Anything meant to stay put needs real wall
    colliders, not a painted lip.
64. Terrain is one continuous displaced ribbon whose collider is built from the
    **same buffers** as the visible mesh. A road assembled from individually
    rotated boxes has a ledge at every joint. Trimesh colliders are for static
    bodies only.
65. Restarting a run **teleports** existing bodies (translation, rotation and both
    velocities, then `wakeUp()`); it never rebuilds meshes and bodies. Rebuilds
    leak the old body, dispose shared geometry and orphan the vehicle controller.
    Removed items are disabled, not destroyed.
66. Physics handling is verified head-lessly before it is verified by eye — the
    physics engine runs in Node without a renderer. Keep the vehicle spec in a
    renderer-free module and assert acceleration, wheel-rotation sign, steering
    sign, suspension settling and cargo retention in a script.
    See `knowledge/threejs/rapier_vehicle_controller.md`.

### Build hygiene

67. In a Vite + TypeScript project `tsc` must run with **`noEmit: true`**. Vite
    resolves `./Foo` to `Foo.js` before `Foo.ts`, so a compiled file left beside a
    source silently shadows it and every later edit to the `.ts` does nothing.
    Never commit `src/**/*.js` in a TypeScript project.

### Development log

68. Every work session ends with an entry in `DEVLOG.md` (task, what was done,
    files touched, what was verified, what remains) and in `CHANGELOG.md` under
    `## [Unreleased]` in player-facing language. A change nobody can reconstruct
    later is a change that will be redone from scratch.

### Orientation & pointer lock

69. `Object3D.lookAt` aims the object's **+Z** at the target; −Z is the convention
    for cameras and lights only. Geometry that will later be aimed with `lookAt`
    (viewmodel arms, tracers, arrows, turret beams) must be built along **+Z**.
    Built along −Z it points backwards after aiming, and the bug reads as "broken
    model" rather than a flipped axis.

70. `requestPointerLock()` is called **from the pointer-down handler**, never from
    the frame loop: after an Esc exit a request issued inside
    `requestAnimationFrame` is silently rejected and the game looks dead. While
    `document.pointerLockElement` is set, application hotkeys (level switch, menu,
    tabs) must stay silent — otherwise an in-game key throws the player out of the
    game.

71. A `MeshStandardMaterial` with high `metalness` and **no environment map
    renders black** — metal has nothing to reflect. Set `scene.environment`
    (a `RoomEnvironment` PMREM costs no file and no request) **and** keep
    `metalness ≤ 0.4` in the material: the environment is the first thing a
    low tier drops, and the asset must not turn into a hole in the frame
    when it does.

72. A node whose base orientation is not zero — anything aimed with
    `lookAt`, `setFromUnitVectors` or a baked rest pose — is animated
    through a **wrapper**, never by writing its own `rotation`. Code that
    returns a limb to rest with `rotation.x = lerp(rotation.x, 0, k)` reads
    the Euler of that very quaternion and erases the aim: the part turns
    away from what it was holding, and the bug reads as "the model is
    missing a piece".

73. Anything positioned from **world** coordinates — a prop placed by two
    hand positions, an effect anchored to a bone, a decal following a
    moving platform — must be converted into the parent's local space
    before it is written into `position`/`quaternion`. Writing world
    numbers into a child of a transformed node applies that transform a
    second time, and the object drifts away by exactly the owner's offset
    from the origin. It stays invisible while the owner sits at the origin
    (which is where every unit test and every bake script puts it), and
    appears only in the game, as "the weapon is flying around the level".

74. Where the character **looks** and where the weapon **points** are two
    different directions, and gameplay owns the second one. A mocap firing
    stance is bladed: the axis through the hands is 40–60° away from the
    model's forward, so turning the body at the target aims the barrel past
    it. Measure the offset once on the assembled rig, turn the body by
    `heading − offset`, and take up the remainder by rotating the chest —
    with an exact quaternion `from barrel to target`, not by tweaking a
    single Euler axis: a barrel that sits at an angle to the rotation plane
    turns by only `angle · cos(that angle)`.

### Interface

75. UI layers over the canvas are **transparent to input by default**: every
    layer container is `pointer-events: none` and only leaf interactive elements
    turn it back on. A full-screen overlay left at `auto` swallows every gameplay
    pointer, and the bug reads as "the car does not steer on mobile". The HUD
    layer is read-only and never becomes interactive at all — pause and settings
    are buttons on the screen layer.

76. Every colour, font, radius, spacing step and duration comes from tokens in
    one theme file. A literal `#RRGGBB`, `px` padding or `z-index: 9999` written
    inside a screen is the reason the second screen stops matching the first.
    `grep -rE '#[0-9a-fA-F]{3,8}' src/ui` minus the theme file returns nothing.
    Имена токенов при этом одинаковы во всех играх, а **значения — нет**: палитра,
    обе гарнитуры и силуэт рамки выводятся из материала мира этой игры по
    процедуре в `knowledge/ux/ui_design_system.md`, раздел 12. Скопированный из
    примера или из соседней игры набор значений — такой же дефект, как умолчания
    браузера, только заметнее: он выглядит осознанным решением, принятым не для
    этой игры.

77. Browser dialogs (`alert`, `confirm`, `prompt`) and emoji used as icons are
    **banned in shipped UI**. The first blocks the game loop, cannot be styled and
    breaks immersion; the second renders differently on every OS and reads as a
    placeholder. Use the project's `Modal` component and one inline SVG sprite
    coloured by `currentColor`.

78. The HUD updates by writing to **cached nodes** and only when the value
    changed — never by rebuilding markup inside the frame loop, and never by
    reading layout (`offsetWidth`, `getBoundingClientRect`) there. Changing
    numbers carry `font-variant-numeric: tabular-nums` in a fixed-width slot, and
    bars animate with `transform: scaleX()`, not `width`; otherwise the row
    reflows on every tick and the whole overlay repaints.

79. Every screen defines its **loading, empty and error** states, not only the
    happy path. Saves, leaderboards, purchases and ads are network calls that fail
    routinely on these portals; a failure that renders an empty frame or says
    nothing costs the player their run and reads as a broken game.

80. Menu and HUD geometry is sized against the **measured** viewport
    (`visualViewport`, re-measured after orientation change, fullscreen exit and
    ad close), never `100vh`, and every UI layer is inset by
    `env(safe-area-inset-*)` **plus** the measured sticky-banner height. The page
    itself never scrolls — long content scrolls in an inner container.
    See `knowledge/ux/ui_design_system.md` and `knowledge/ux/ui_implementation.md`.

---

## 10. DEFINITION OF DONE (MANDATORY VERIFICATION CHECKLIST)
To mark this game as complete, every single requirement below must be verified and working:

- [ ] Реализованы 3 последовательные комнаты штурма (Периметр, Офис, Серверная) с физическим разрушением преград
- [ ] Механика бричинга C4 с разлетом обломков и переходом в Slow-Mo работает со стабильными 60 FPS
- [ ] Баллистический щит блокирует выстрелы, механика наклонов Lean (Q/E и тач) позволяет вести прицельный огонь
- [ ] Летальная система попаданий: 1 выстрел в голову нейтрализует любого врага без полосок здоровья
- [ ] Playgama Bridge SDK полностью интегрирован: работают облачные сохранения, реклама и лидерборды
- [ ] Управление адаптировано под смартфоны (тач-стрельба в slow-mo) и ПК (клавиатура/мышь)

---

## 11. FACTORY KNOWLEDGE BASE
Deep, worked-out detail behind the rules in section 9 — read the relevant file before implementing that area:

Набор отобран под этот проект: Проект опирается на связку FPS-контроллера, физики разрушения стен Rapier3D и тактического ИИ CQB-шутера; стандартные циклические паттерны не используются, так как игра построена на линейном штурме 3 последовательных комнат.

**Ядро — прочитать до начала реализации:**
- `knowledge/threejs/fps_controller_and_shooting.md` — Реализация вида от первого лица, тактического наклона (lean), стрельбы с отдачей и удержания баллистического щита.
- `knowledge/threejs/physics_integration.md` — Интеграция физического движка Rapier3D для разрушения стен, баллистики пуль и замедления времени (slow-mo).
- `knowledge/threejs/shooter_enemy_ai_and_combat.md` — Тактический ИИ врагов в помещениях, летальная модель урона с хедшотами и реакция на ослепление/контузию.
- `knowledge/mechanics/physics_destruction.md` — Физика подрыва C4, разрушение гипсокартонных стен и генерация тактических проломов с разлётом обломков.
- `knowledge/mechanics/cover_and_suppression.md` — Баллистическая защита ростового щита, механика укрытий и подавление дезориентированных врагов при штурме.
- `knowledge/stack/rapier3d.md` — WASM-физика Rapier3D для расчёта осколков разрушенных стен, коллизий щита и трассировки выстрелов.

**Вспомогательные:**
- `knowledge/threejs/juice_and_vfx_pool.md` — Пул частиц для эффектов взрыва стены, пыли, вспышек выстрелов и кинематографичного slow-mo.
- `knowledge/threejs/stealth_and_vision_cones.md` — Отображение и проверка линий видимости противников при разведке эндоскопической камерой под дверью.
- `knowledge/mechanics/stealth_detection.md` — Система скрытности и состояния бдительности врагов до момента детонации штурмового заряда.
- `knowledge/threejs/procedural_mesh_builder.md` — Процедурная генерация моделей оружия, щита, комнат и разрушаемых сегментов стен без внешних ассетов.
- `knowledge/audio/procedural_sound_synthesizer.md` — Синтез процедурного звука на Web Audio API для взрывов C4, глухих выстрелов, рикошетов и таймера бомбы.

**Платформенные требования (обязательны всегда):**
- `knowledge/playgama/game_ready_and_loading.md`
- `knowledge/playgama/auth_and_player.md`
- `knowledge/playgama/storage_and_cloud.md`
- `knowledge/playgama/ads_integration.md`
- `knowledge/playgama/banners_and_layout.md`
- `knowledge/compliance/yandex_moderation.md`
- `knowledge/ux/localization_system.md`
- `knowledge/ux/touch_controls.md`
- `knowledge/ux/ui_design_system.md`
- `knowledge/ux/ui_implementation.md`

**Осознанно НЕ включены**: `patterns/arena_combat_loop.md`, `patterns/survivor_loop.md`, `patterns/roguelike_loop.md`, `threejs/horde_survivor_core.md`, `mechanics/upgrade_choices.md`, `mechanics/wave_survival.md`, `stack/recast_navigation.md`, `stack/bitecs.md`, `mechanics/parry.md`. Игра является камерным тактическим CQB-симулятором с фиксированными позициями врагов в трёх комнатах, поэтому отброшены паттерны арены, орды (survivor) и роуглайка со случайными картами апгрейдов, а также избыточные для статичных засад навмеш (recast-navigation), ECS (bitECS) и механики рукопашного боя (парирование).
Не подтягивай решения из этих документов — они про другую игру.

---

## 12. DETAILED REFERENCE DOCUMENTS
For extended deep specifications, refer to the accompanying project documentation files:
- [Инструкция агенту (AGENTS.md)](./AGENTS.md)
- [Журнал разработки (DEVLOG.md)](./DEVLOG.md)
- [Changelog](./CHANGELOG.md)
- [Game Design Document](./GAME_DESIGN_DOCUMENT.md)
- [Gameplay Specification](./GAMEPLAY_SPECIFICATION.md)
- [Technical Specification](./TECHNICAL_SPECIFICATION.md)
- [Architecture Document](./ARCHITECTURE_DOCUMENT.md)
- [Playgama Integration](./PLAYGAMA_INTEGRATION.md)
- [Monetization Specification](./MONETIZATION.md)
- [Mobile Controls](./MOBILE_CONTROLS.md)
- [QA Plan](./QA_PLAN.md)
- [Game Skill Guidelines](./skills/GAME_SKILL.md)
- [Renderer Skill](./skills/RENDERER_SKILL.md)