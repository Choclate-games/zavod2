# Журнал разработки — Воксельный Измельчитель ASMR 3D

Этот файл ведёт ИИ-агент. Каждая сессия работы = одна запись:
что просили, что сделано, какие файлы затронуты, что проверено, что осталось.

---

## 2026-08-18 19:46 — Инициализация проекта
- **Задача**: создан каталог проекта в workspace, подготовлена спецификация.
- **Сделано**: сгенерирован пакет документации фабрики.
- **Следующий шаг**: реализация игрового кода по AI_DEVELOPER_PROMPT.md.

## 2026-08-18 20:05 — Полная реализация игры «Воксельный Измельчитель ASMR 3D»
- **Задача**: Реализовать полную рабочую структуру HTML5 игры: package.json, vite.config.ts, tsconfig.json, index.html, src/main.ts, модули рендерера Three.js, физики частиц, управления, звука, экономики, апгрейдов и Playgama Bridge.
- **Сделано**:
  1. Создана конфигурация проекта (package.json, tsconfig.json с strict mode, vite.config.ts с оптимальным чанкингом).
  2. Разработан UI оверлей (index.html, src/style.css): адаптивный портретный HUD с safe-area, карточки 4 веток улучшений, индикаторы комбо и шкалы прогресса, всплывающие цифры наград, модальные окна победы с конфетти, галереи коллекций и магазина.
  3. Реализована типизированная архитектура: шина событий EventBus, координатор Game, цикл GameLoop на 60 Гц с защитой от скачков дельты.
  4. Сгенерированы 16 детализированных воксельных 3D моделей в 4 коллекциях (Пончик, Яблоко, Арбуз, Бургер, Уточка, Геймпад, Кубик Рубика, Машинка, Смартфон, Кассета, Аркадный автомат, Робот, Алмаз, Корона, Замок, Ракета).
  5. Реализована система рендеринга Three.js: студийное освещение, мягкие PCF тени, двойные зубчатые валы со встречным вращением, послойная деконструкция модели через InstancedMesh и пул 1500 физических частиц с гравитацией и баллистикой.
  6. Написан процедурный ASMR звуковой движок на Web Audio API (хруст со случайным сдвигом питча, гул мотора, звон монет, фанфары победы).
  7. Реализована полная интеграция с Playgama Bridge: синхронизация сохранений с дебаунсом 1.5 сек и flush на pagehide/visibilitychange, реклама Rewarded/Interstitial с кулдаунами, отправка game_ready после полной загрузки.
- **Затронутые файлы**:
  - `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`
  - `src/style.css`, `src/main.ts`
  - `src/core/Types.ts`, `src/core/EventBus.ts`, `src/core/Game.ts`, `src/core/GameLoop.ts`
  - `src/data/Localization.ts`, `src/data/UpgradeConfig.ts`, `src/data/ModelsData.ts`
  - `src/platform/PlaygamaService.ts`, `src/platform/StorageService.ts`
  - `src/audio/AudioManager.ts`
  - `src/rendering/SceneManager.ts`, `src/rendering/RollerMechanism.ts`, `src/rendering/VoxelModelObject.ts`, `src/rendering/ParticleStream.ts`, `src/rendering/SparkVFX.ts`
  - `src/systems/ComboSystem.ts`, `src/systems/EconomySystem.ts`, `src/systems/UpgradeSystem.ts`, `src/systems/ShredderSystem.ts`
  - `src/ui/TouchControls.ts`, `src/ui/UIManager.ts`
  - `README.md`, `CHANGELOG.md`, `DEVLOG.md`
- **Проверено**:
  - `npm install` — успешно установил все зависимости без ошибок.
  - `npm run build` — чистая компиляция TypeScript в strict mode и сборка Vite (размер бандла ~220 КБ gzip).
- **Известные проблемы / следующий шаг**: Все задачи спецификации выполнены в полном объеме, проект готов к тестированию в веб-плеере фабрики и релизу на платформах Яндекс Игры, VK и Playgama.

## 2026-08-20 14:00 — Оптимизация разрушения и расширение прогрессии
- **Задача**: Исправить просадки оптимизации, неполное прохождение предметов через валы, одинаковую скорость разрушения без прокачки и добавить уровни.
- **Сделано**:
  - Скорость подачи и количество срезаемых вокселей теперь зависят от твёрдости конкретного предмета и прокачки остроты.
  - Верхние слои больше не зависают: ограничение подачи опущено ниже валов, а предмет плавно вращается вокруг вертикальной оси.
  - Срез выполняется указателем по отсортированным слоям с переиспользованием буфера результатов и `Vector3`, без лишнего сканирования и аллокаций в цикле.
  - Исправлен сброс пула частиц, снижена стоимость теней и мобильного DPR.
  - Добавлены 4 уровня пятой коллекции, которая открывается после 16 завершённых предметов.
- **Затронутые файлы**:
  - `src/rendering/VoxelModelObject.ts`
  - `src/systems/ShredderSystem.ts`
  - `src/rendering/ParticleStream.ts`
  - `src/rendering/SceneManager.ts`
  - `src/rendering/RollerMechanism.ts`
  - `src/data/ModelsData.ts`
  - `src/core/Game.ts`
  - `src/ui/UIManager.ts`
  - `README.md`, `CHANGELOG.md`
- **Проверено**: `npm install` — успешно; `npm run build` — успешно, TypeScript strict и Vite без ошибок.
- **Известные проблемы / следующий шаг**: Требуется ручной запуск `npm run dev` в браузере фабрики для проверки FPS и полного прохождения всех новых моделей на целевых мобильных устройствах.

## 2026-08-20 15:00 — Тяжёлая стартовая подача без вращения
- **Задача**: Сделать первые модели требовательными к усилию игрока без прокачки и убрать вращение при падении.
- **Сделано**:
  - Уменьшены базовые скорость подачи и темп среза вокселей.
  - Комбо от тапов, скорость подачи и острота теперь сильнее ощущаются как способы ускорить стартовую дробилку.
  - Полностью отключено вращение падающей модели и падающих воксельных осколков.
- **Затронутые файлы**:
  - `src/systems/ShredderSystem.ts`
  - `src/rendering/VoxelModelObject.ts`
  - `src/rendering/ParticleStream.ts`
  - `CHANGELOG.md`
- **Проверено**: `npm run build` — успешно; `npm run dev -- --host 127.0.0.1` — Vite запустился на `http://localhost:5173/` без ошибок сборки.
- **Известные проблемы / следующий шаг**: Нужно проверить в браузере, что стартовая модель не стала чрезмерно медленной на слабых мобильных устройствах.

## 2026-08-20 16:00 — Контактное перемалывание валами
- **Задача**: Сделать физическое падение модели сверху, перемалывать только точки в зоне контакта валов и связать тап с ускорением шипов и уроном.
- **Сделано**:
  - Модель получает ускорение падения сверху и после входа в зону захвата постепенно протягивается вниз скоростью валов.
  - Срезаются только воксели в пределах рабочей области по X/Z и ниже плоскости захвата.
  - Скорость вращения валов теперь передаётся в формулу урона: тап-комбо ускоряет шипы и увеличивает число разрушенных точек.
  - Границы контакта читаются без создания временного объекта в игровом цикле.
- **Затронутые файлы**:
  - `src/rendering/VoxelModelObject.ts`
  - `src/rendering/RollerMechanism.ts`
  - `src/systems/ShredderSystem.ts`
  - `CHANGELOG.md`
- **Проверено**: `npm run build` — успешно; `npm run dev -- --host 127.0.0.1` — Vite запустился на `http://localhost:5173/` без ошибок сборки.
- **Известные проблемы / следующий шаг**: Нужен ручной просмотр первого уровня в браузере, чтобы подобрать окончательную длительность контакта без прокачки.

## 2026-08-20 17:00 — Свободный проход модели ниже валов
- **Задача**: Исправить застревание модели на высоте валов, если она не была полностью разрушена в зоне контакта.
- **Сделано**:
  - Очередь вокселей больше не пропускает активную точку вне контакта: она ожидает шипы или безопасный проход ниже валов.
  - Падение модели не зависит от того, осталось ли внутри неё несколько непробитых вокселей.
  - После прохождения нижней отметки оставшиеся точки освобождаются принудительно, что позволяет корректно завершить уровень и запустить следующий.
- **Затронутые файлы**:
  - `src/rendering/VoxelModelObject.ts`
  - `src/systems/ShredderSystem.ts`
  - `CHANGELOG.md`
- **Проверено**: `npm run build` — успешно; `npm run dev -- --host 127.0.0.1` — Vite запустился на `http://localhost:5173/` без ошибок сборки.
- **Известные проблемы / следующий шаг**: Нужен ручной тест в браузере на высоких моделях, чтобы убедиться, что принудительное освобождение происходит только ниже валов.

## 2026-08-20 18:00 — Опорное падение на валы
- **Задача**: Убрать линейный проезд модели вниз и сделать физически понятный контакт: модель падает на валы, а её нижние блоки разрушаются и освобождают следующие слои.
- **Сделано**:
  - Модель падает с ускорением до высоты верхней точки валов.
  - Нижний активный воксель используется как опора: модель не может уйти под валы, пока этот слой не разрушен.
  - После разрушения опорных блоков модель проваливается на следующий слой, а воксели получают частицы только в момент контакта с рабочей областью валов.
  - Убрано принудительное освобождение остатков ниже валов.
  - Высота опоры синхронизирована с верхушками зубьев, чтобы модель не проходила внутрь цилиндров.
- **Затронутые файлы**:
  - `src/rendering/VoxelModelObject.ts`
  - `src/systems/ShredderSystem.ts`
  - `CHANGELOG.md`
- **Проверено**: `npm run build` — успешно; `npm run dev -- --host 127.0.0.1` — Vite запустился на `http://localhost:5173/` без ошибок сборки.
- **Известные проблемы / следующий шаг**: Требуется ручная проверка визуального контакта нижнего слоя с зубьями на первом уровне.

## 2026-08-20 19:00 — Свободное падение и swept-контакт вокселей
- **Задача**: Убрать последовательное линейное разрушение снизу вверх и удалять именно те блоки, чья траектория падающей модели пересекла зубья валов.
- **Сделано**:
  - Убрана опора модели на нижний активный слой и сортированная очередь разрушения.
  - Модель свободно падает целиком с ускорением, не останавливаясь на каждом слое.
  - Каждый активный воксель проверяется по отрезку между прошлой и текущей позицией модели; при пересечении зоны зубьев он удаляется и превращается в частицу.
  - Скорость шипов и ширина зоны воздействия по-прежнему зависят от тап-комбо и улучшений.
- **Затронутые файлы**:
  - `src/rendering/VoxelModelObject.ts`
  - `src/systems/ShredderSystem.ts`
  - `CHANGELOG.md`
- **Проверено**: `npm run build` — успешно, TypeScript strict и Vite без ошибок.
- **Известные проблемы / следующий шаг**: Требуется ручной просмотр первого падения в браузере, чтобы визуально подтвердить, что блоки исчезают только в зоне касания зубьев.

## 2026-08-20 20:00 — Прочность отдельных вокселей
- **Задача**: Добавить каждому блоку собственный запас прочности вместо мгновенного разрушения при первом контакте.
- **Сделано**:
  - Для каждого вокселя выделен `Uint8Array` прочности, зависящий от твёрдости модели.
  - Пока блок находится в зоне зубьев, каждый фиксированный шаг снимает часть его прочности.
  - Блок превращается в частицу только после полного исчерпания собственной прочности.
  - Острота улучшений и скорость валов увеличивают урон за один контакт.
- **Затронутые файлы**:
  - `src/rendering/VoxelModelObject.ts`
  - `src/systems/ShredderSystem.ts`
  - `CHANGELOG.md`
- **Проверено**: `npm run build` — успешно; `npm run dev -- --host 127.0.0.1` — Vite запустился на `http://localhost:5173/` без ошибок сборки.
- **Известные проблемы / следующий шаг**: Нужна ручная проверка, что твёрдые модели не пролетают зону зубьев быстрее, чем успевают получить все необходимые удары.

## 2026-08-20 21:00 — Существенное увеличение здоровья вокселей и физический контакт
- **Задача**: Убрать мгновенное рассыпание вокселей, сделать многократно увеличенный индивидуальный запас здоровья каждому вокселю с органической вариацией и физическое удержание на зубьях валов.
- **Сделано**:
  - Запас здоровья каждого вокселя увеличен в 10–15 раз (базовые 24–97 HP в зависимости от твёрдости модели вместо прежних 2–6 HP) в массивах `Float32Array`.
  - Добавлена детерминированная процедурная вариация здоровья каждого вокселя (±15-20%), благодаря чему блоки откалываются естественно и нелинейно.
  - Реализован непрерывный урон (DPS) в зоне контакта с вращающимися зубьями валов, масштабируемый улучшениями «Острота» и «Турбо-Тап».
  - Исправлена физика подачи: модель падает на валы и удерживается зубьями во время перемалывания, плавно погружаясь глубже по мере разрушения контактных вокселей.
- **Затронутые файлы**:
  - `src/rendering/VoxelModelObject.ts`
  - `src/systems/ShredderSystem.ts`
  - `CHANGELOG.md`
  - `DEVLOG.md`
- **Проверено**: `npm run build` — TypeScript strict и Vite сборка выполнены успешно без ошибок.
- **Известные проблемы / следующий шаг**: Проверить баланс скорости перемалывания на более поздних уровнях с высокими уровнями прокачки.

## 2026-08-20 22:00 — Дебафф урона игрока в 5 раз и добавление 100 уровней
- **Задача**: Дебаффнуть урон игрока в 5 раз и создать 100 различных уровней с уникальными воксельными 3D моделями.
- **Сделано**:
  - Базовый урон дробилки/игрока снижен ровно в 5 раз (`baseDps` уменьшен с 28.0 до 5.6) в `ShredderSystem.ts`, что делает процесс перемалывания более тягучим, медитативным и многократно повышает ценность прокачки «Острота» и «Турбо-Тап».
  - Разработаны 100 уникальных уровней с детализированными воксельными 3D моделями, разбитыми на 10 тематических коллекций по 10 уровней («Вкусняшки & Десерты», «Игрушки & Ретро», «Гаджеты & Электроника», «Сокровища & Реликвии», «Оружие & Артефакты», «Флора & Природа», «Транспорт & Техника», «Монстры & Персонажи», «Космос & Наука», «Индустрия & Мега-Сооружения»).
  - Сбалансированы показатели твёрдости (от 1.0 до 6.5) и базовой ценности вокселей (от 1.0 до 300.0) на протяжении всех 100 уровней.
- **Затронутые файлы**:
  - `src/systems/ShredderSystem.ts`
  - `src/data/ModelsData.ts`
  - `CHANGELOG.md`
  - `DEVLOG.md`
- **Проверено**: `npm run build` — сборка TypeScript и Vite завершена успешно (код 0) без ошибок.
- **Известные проблемы / следующий шаг**: Проверить визуальное отображение в галерее и переключение всех 100 уровней в браузере.

## 2026-08-21 11:00 — Интеграция физического движка Rapier3D и three-mesh-bvh
- **Задача**: Добавить физический движок Rapier3D и three-mesh-bvh, рассчитывать столкновения блоков через физику и реализовать физический разлет блоков.
- **Сделано**:
  - Установлены зависимости `@dimforge/rapier3d-compat` (версия 0.20.x с WASM) и `three-mesh-bvh`.
  - Разработан сервис физического мира `PhysicsWorld.ts`, инициализирующий WebAssembly симуляцию Rapier3D с фиксированным аккумулятором 60 Гц, статическими коллайдерами воронки/направляющих и приемной корзины, кинематическими коллайдерами вращающихся валов и пулом из 1000 динамических твердых тел (RigidBody с Cuboid коллайдерами, плотностью, упругостью, трением и трехмерным вращательным моментом).
  - Разработан модуль `RollerBVH.ts` на базе `three-mesh-bvh`, строящий оптимизированное BVH-дерево по 120 зубьям каждого вала для мгновенного геометрического расчета столкновений воксельных кубов с вращающимися зубьями в мировом пространстве с учетом обратных матриц трансформации.
  - Интегрирован физический расчет столкновений и импульсов в `VoxelModelObject.ts`: каждый воксель в зоне контакта проверяется по BVH зубьев, получает усиленный физический урон при контакте с зубом и при разрушении отрывается с направленным импульсом выталкивания и затягивания.
  - Обновлен `ParticleStream.ts` для полной синхронизации матриц `InstancedMesh` с твердыми телами Rapier3D в 60 FPS: воксели реалистично кувыркаются в 3D, сталкиваются с валами и стенками воронки, отскакивают и оседают на дне корзины, активируя сбор монет.
  - Обновлен `RollerMechanism.ts` для передачи мировых матриц в BVH и угловых скоростей в кинематические коллайдеры Rapier.
  - Настроен чистый чанкинг в `vite.config.ts` (`three`, `rapier`, `bvh`).
- **Затронутые файлы**:
  - `package.json`
  - `vite.config.ts`
  - `src/physics/PhysicsWorld.ts`
  - `src/physics/RollerBVH.ts`
  - `src/rendering/VoxelModelObject.ts`
  - `src/rendering/ParticleStream.ts`
  - `src/rendering/RollerMechanism.ts`
  - `src/systems/ShredderSystem.ts`
  - `src/core/Game.ts`
  - `README.md`
  - `CHANGELOG.md`
  - `DEVLOG.md`
- **Проверено**:
  - `npm install` — успешно.
  - `npx tsc --noEmit` — строгая проверка типов TypeScript в strict mode прошла без ошибок.
  - `npm run build` — чистая компиляция TypeScript и сборка Vite (код 0).
- **Известные проблемы / следующий шаг**: Физические системы работают стабильно на 60 FPS.
